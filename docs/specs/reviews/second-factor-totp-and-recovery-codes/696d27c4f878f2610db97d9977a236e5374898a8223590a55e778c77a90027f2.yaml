schema_version: 1
kind: acceptance_review
subject:
  repository: PRO-Robotech/kaname
  path: docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md
  sha256: 696d27c4f878f2610db97d9977a236e5374898a8223590a55e778c77a90027f2
  static_status: DRAFT
  static_status_note: >-
    Шапка документа намеренно не правлена: слово DRAFT — статическая форма объявления, а не
    вердикт (`change-graph.md` §2). Строка состояния редакции 9 говорит правду — «кругов ревью
    пять, действующее одобрение у редакции 8, редакция 9 ждёт»; после этой записи кругов шесть,
    и это выводится из каталога записей, а не из шапки.
  base_commit: 39628487099fe44990e9e03b09b725f3d7e37abb
  content_commit: 4ba95cf8a8154e48642f1b0d2354819736a24142
  branch: issue-254-f12-reset-owner
  pull_request: pending
  pull_request_note: >-
    PR нет: `gh pr list -R PRO-Robotech/kaname --head issue-254-f12-reset-owner --state all` →
    пусто (2026-09-18). Ветка на origin есть и стоит на базе — `git ls-remote --heads origin
    issue-254-f12-reset-owner` → 39628487…; коммит редакции 9 (4ba95cf8) и эта запись живут
    только в рабочей копии tmp/w-254-f12. Толкать — не момент рецензента.
  task: PRO-Robotech/kacho#1281
  edition_task: PRO-Robotech/kaname#254
  lines: 2420
  round: 6
  predecessor_record: docs/specs/reviews/second-factor-totp-and-recovery-codes/2582c5e1101c44cda591e7c2745645b8989b595a8f000b32ec8d729927055364.yaml
  fingerprint_predicate: >-
    sha256sum docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md
    (рабочая копия tmp/w-254-f12, HEAD 4ba95cf8, `git status --porcelain` → пусто)
  fingerprint_note: >-
    Отпечаток снят СВОИМ замером — 696d27c4…, 2420 строк (`wc -l`); равен объявленному в
    задании. Редакция 8 — в стволе: `git show 39628487:<документ> | sha256sum` → 2582c5e1…
    (предмет записи круга 5, событие 5711101932). Коммит редакции 9 трогает ТОЛЬКО документ:
    `git diff --numstat 39628487 4ba95cf8` → одна строка, +288/−81. Вне документа дерево HEAD
    побайтово равно стволу (`git diff --quiet 39628487 HEAD -- ':!docs/engineering/acceptance'`
    → 0), поэтому всё, что ниже измерено «на HEAD», измерено на стволе 39628487.
  measurement_revisions: >-
    Дом службы: kaname `origin/main` 39628487 (сверено `git fetch` + `git rev-parse
    origin/main`, 2026-09-18); край: kacho `origin/main` b5fa093341f (как в шапке редакции 9);
    пин службы у края — `github.com/PRO-Robotech/kaname v0.4.1-0.20260917021918-94352d9c4a88`
    (kacho `go.mod:120`). Воркспейс: `origin/main` 870e3132e5.
  task_premise_remeasured: >-
    Каждая посылка задания перемерена, исход по каждой. (1) sha256 редакции 9 = 696d27c4…, 2420
    строк — ПОДТВЕРДИЛОСЬ. (2) коммит 4ba95cf8 на ветке issue-254-f12-reset-owner, родитель
    39628487 = kaname origin/main — ПОДТВЕРДИЛОСЬ. (3) действующее одобрение — APPROVED круга 5
    на 2582c5e1… (редакция 8), событие issuecomment-5711101932 — ПОДТВЕРДИЛОСЬ записью в
    каталоге и отпечатком файла в стволе. (4) решение владельца дословно — ПОДТВЕРДИЛОСЬ:
    kaname#254, комментарий 5730003325 (`pointpu`, MEMBER, 2026-09-18T12:29:57Z) несёт «Ответ
    владельца: «2fa может сбросить или пользователь или администратор облака»». (5) «модель
    уже выражает решение» — ПОДТВЕРДИЛОСЬ тремя способами: чтением (`fga_model.fga:779`
    `identity_suspender: super_admin from account`, `:308` `account#super_admin: any_admin from
    cluster`), гейтом (`TestGoverningTheIdentityHasNoAccountLevelSource` — PASS, «RPC
    «распоряжение строкой» сверено 3») и посевом (constructibility, п.1). (6) «производитель 403
    — край: у записи ResetSecondFactor нет флага сокрытия» — ПОДТВЕРДИЛОСЬ для записи
    (`permission_catalog.json`: `identity_suspender`, пол «2», ключа `hide_existence` нет);
    сопутствующее общее утверждение «сокрытие у края только на чтениях» — ОПРОВЕРГНУТО (Н23).
    (7) состав правки со слов автора — ПОДТВЕРДИЛОСЬ диффом (delta_vs_declared). (8) решение
    диспетчера о самосбросе (remove с кодом, без отдельной свежести и без «2», прочтение «без
    кода» отвергнуто) — Р9 выражает его дословно — ПОДТВЕРДИЛОСЬ.

verdict: CHANGES_REQUESTED
reviewer_role: acceptance-reviewer
reviewed_at: '2026-09-18'

# ─── ОДОБРЕНИЕ ВЫВОДИТСЯ ИЗ ЗАПИСИ И СОБЫТИЯ, А НЕ ИЗ СЛОВА В ШАПКЕ ──────────
effective_approval:
  issued: false
  ban1_lifted: false
  why: >-
    Вердикт круга 6 — CHANGES_REQUESTED: два блокирующих (Б7 — DoD, Б8 — NEGATIVE), оба
    правятся строками, существо редакции 9 (кто сбрасывает, самосброс кодом, 403 не держателю)
    ПРИНИМАЕТСЯ и подтверждено посевом. Отказу внешнее полномочие не нужно. Действующее
    одобрение остаётся одобрением РЕДАКЦИИ 8 (2582c5e1…), и цену надо назвать прямо: редакция 8
    отдаёт сброс распорядителю аккаунта и требует 404 чужому распорядителю — против решения
    владельца 2026-09-18. Модель и каталог в стволе уже отвечают решению владельца (посев: `D`,
    `D₂` — «нет», `A` — «да»), а комментарии кода, контракта и страница продукта — редакции 8;
    санкции на правку этих мест по редакции 9 до её одобрения нет.
  what_the_event_would_lift: >-
    (Для круга, который придёт к одобрению.) То же, что событие круга 5, на новом отпечатке, плюс
    изменение объёма редакции 9: держатель ResetSecondFactor — администратор облака; не держателю
    — 403 края; самосброс — `remove` с кодом без отдельной свежести; ResetSecondFactor в закрытом
    перечне гейта «распоряжение строкой личности»; правка описаний держателя по дереву. Не шире:
    §0.2 «не разрешает» редакцией 9 не расширен.
  publication_pending:
    required_for_sanction: false
    expected_from: оркестратор волны
    expected_actor: pointpu
    expected_mode: courier_of_agent_verdict
    expected_form: >-
      issue_comment в kacho#1281 с полями role, verdict, subject, subject_revision (4ba95cf8),
      subject_sha256 (696d27c4…) дословно; блок `event` дописывается ОТДЕЛЬНЫМ коммитом поверх
      этой записи. Для отказа событие санкцию не выдаёт и не отзывает; публиковать ли — решение
      владельца.

authority:
  mode: versioned_policy_authority
  epoch: post-cutover
  epoch_derivation: >-
    git merge-base --is-ancestor 1202e3cf5f6cb4600294ceb2e30fa72b61630eac origin/main в
    PRO-Robotech/kacho-workspace → 0 (исполнено ЭТИМ кругом; origin/main воркспейса 870e3132e5)
  repository: PRO-Robotech/kacho-workspace
  cutover_commit: 1202e3cf5f6cb4600294ceb2e30fa72b61630eac
  policy_document: docs/changes/policy.yaml
  policy_field: review_authority.acceptance-reviewer
  authorized_actors: [pointpu]
  allowlisted_for_role: false

event:
  type: none
  status: not_performed
  reason: >-
    ТРЕТЬЯ КАТЕГОРИЯ — «не выполнилось», а не «да» и не «нет». Событие выпускает владелец
    отдельным коммитом поверх записи, если решит публиковать отказ; для CHANGES_REQUESTED это
    безвредно: ban #1 на редакцию 9 остаётся в силе ровно так же, как до ревью.

attribution:
  reviewer_kind: claude_agent
  publishing_actor: pointpu
  publication_mode: courier_of_agent_verdict
  personal_review_by_publishing_actor_claimed: false

scope:
  reviewed: >-
    Редакция 9 против решения владельца (kaname#254) и решения диспетчера о самосбросе. Предмет
    круга — ДЕЛЬТА 8 → 9 (41 ломоть, delta_vs_declared) плюс всё, на что она опирается: модель,
    каталог обеих копий, край (решение о сокрытии), код глаголов remove и ResetSecondFactor,
    страница продукта, гейт модели и фикстура вердикта. Ф12-30 и Ф12-45 прочитаны целиком и
    ПОСЕЯНЫ; Р9, Р10, §0.2, §1.2 Ф-л/Ф-м, §3 Д6/Д12 и сводка, §4.1 п.7, §6, §7 инв. 2/3/13, §8,
    §9 DoD, §11 — прочитаны целиком в новой редакции. Разделы, не тронутые редакцией 9 (Р1…Р5,
    Р7, Р8, Р11, прочие сценарии), заново не перечитывались: их вердикт — записи кругов 2…5;
    исключение — числа «27 → 28», на которые опирается дельта (Н24).
  method: experiment_not_reading
  document_edited_by_reviewer: false

# ─── ДЕЛЬТА ПРОТИВ ОБЪЯВЛЕННОГО СОСТАВА — НЕ ШИРЕ И НЕ УЖЕ ─────────────────
delta_vs_declared:
  predicate: >-
    git diff -U0 39628487 4ba95cf8 -- <документ> — 41 ломоть; каждый положен на ближайший
    заголовок раздела либо сценария редакции 9 (`grep -nE '^(## |### |\*\*Ф12-[0-9]+ —)'`).
  declared: >-
    строка состояния · «Сервис» и §0 · §0.2 · §0.4 · §1.2 Ф-л и Ф-м · Р6 · Р9 · Р10 · Р12 ·
    §3 Д6, Д12, сводка · §4.1 п.7 · Ф12-28 (последнее «И») · Ф12-30 · Ф12-45 (новый) · §6 ·
    §7 инв. 2, 3, 13 · §8 · §9 DoD · §11.
  got: >-
    шапка (Статус, строка «Редакция 9») · «Сервис» · §0 · §0.2 (два ломтя) · §0.4 · §1.2 · Р6 ·
    Р9 (три ломтя) · Р10 (шесть) · Р12 · §3 (четыре: Д6, Д12, строка категории, абзац сводки) ·
    §4.1 · Ф12-28 · Ф12-30 и следующий за ним Ф12-45 (четыре ломтя) · §6 (три) · §7 (три) · §8
    (два) · §9 (два) · §11 (два). Сверх объявленного — ничего; из объявленного не сделано —
    ничего.
  verdict: СОВПАДАЕТ

# ─── КОНСТРУИРУЕМОСТЬ: ПОСЕВ, А НЕ ПЕРЕЧИСЛЕНИЕ ──────────────────────────────
constructibility:
  isolation: >-
    Рабочая копия tmp/w-254-f12 (названа заданием), TMPDIR=/var/tmp/f12-r6; одноразовые пробы
    положены в дерево на время прогона и удалены — `git status --porcelain` после → пусто.
  seeded:
    - given: >-
        Ф12-30 уровень I — держатели отношения записи каталога ResetSecondFactor: владелец
        аккаунта `D`, делегированный администратор `D₂`, посторонний `O` (владелец и admin
        второго аккаунта), сам человек, администратор облака `A`
      how: >-
        (а) существующая фикстура `internal/service/governing_the_identity_integration_test.go`
        (настоящий Postgres) — PASS, перепись «запрета iam_user.identity_suspender · субъектов
        спрошено 5»; (б) одноразовая проба на той же фикстуре, отношение взято у каталога по FQN
        ResetSecondFactor, контроль фикстуры — структурное `admin` аккаунта у `D`, `D₂`, у `O`
        на своём и НЕ на аккаунте `U`
      got: >-
        rel=iam_user.identity_suspender · D=false · D₂=false · O=false · сам человек=false ·
        A=true · O на N=false · A на N=true (N — well-formed несуществующий id)
      verdict: >-
        ПОСТРОЕНО. «Дано» Ф12-30 и Ф12-45 (б) на уровне вердикта строимо, и вердикт совпадает с
        «Тогда». «A на N = true» — исход, которого документ не называет (Б8).
    - given: Ф12-45 (а) — несвежая сессия «1», запасной код, затем enroll по новому носителю
      how: >-
        одноразовая проба на харнессе `second_factor_usecase_test.go` (управляемые часы,
        `sfWindow` 15m): enrolled → вход паролем без кода → часы + окно + 1 s → контроль
        enroll под S1 → remove запасным кодом №4 → enroll по новому носителю
      got: >-
        контроль: enroll до remove — ErrSessionNotFresh; remove — успех, уровень «2», строк
        totp и lookup_secret нет; enroll после — успех; `BackupCodesRemaining` в ответе remove
        = 9 (подтверждает находку §11 о поле после снятого набора)
      verdict: ПОСТРОЕНО; исход совпадает с «Тогда» (а)
    - given: Ф12-45 (в) — L, все десять кодов потреблены
      how: >-
        та же проба — десять церемоний запасными кодами, затем remove потреблённым кодом
      got: ErrAuthenticationFailed; счёт по адресу 0 → 1; строка totp у L на месте
      verdict: ПОСТРОЕНО; исход совпадает с «Тогда» (в)
  named_fixture_not_seeded:
    - given: >-
        сессии «2» у `A`, `D`, `D₂`, `O`, `W` посевом записи с множеством {password, totp}
        (Ф11 §3.0а) и `A₁` («1») — Ф3-01
      note: форма посева названа документом и живёт в харнессе сессий; этим кругом не сеялась.
  not_executed:
    - given: >-
        Ф12-30 и Ф12-45 (б) через край: 403 · code 7 · AUTHZ_DENIED, тело на U и на N, 401 по
        полу у A₁, маршрут ResetSecondFactor
      why: >-
        ТРЕТЬЯ КАТЕГОРИЯ. Пин службы у края (94352d9c) глагола не несёт — `git grep -c 'rpc
        ResetSecondFactor' 94352d9c4a88 -- <proto>` → 0, копия каталога края — 0 записей
        (kacho b5fa093341f). Документ это оговаривает сам (§8 строка Ф12-28, 29, 45; §9 DoD —
        «маршрут края для Ф12-30 и Ф12-45 «б»» в третьей категории) и засчитывает (б) только в
        паре с проходом `A` через тот же край — верно.
  injections:
    - what: в `sf_remove.go` внесено требование свежести (`requireFresh`) перед частотой
      got: проба Ф12-45 (а) КРАСНАЯ — «самосброс кодом не требует отдельной свежести»; файл восстановлен (0 изменений)
    - what: >-
        у `identity_suspender` добавлен источник уровня аккаунта (`or admin from account`) —
        сперва в `internal/authzmodel/fga_model.fga`, затем в канонической
        `proto/kaname/cloud/iam/v1/fga_model.fga`
      got: >-
        посев КРАСНЫЙ (D=true, D₂=true); гейт модели КРАСНЫЙ на канонической копии («UserService/
        Block гейтится отношением iam_user.identity_suspender, у которого источник уровня
        аккаунта»); обе копии восстановлены (0 изменений)
  counts: >-
    нетривиальных «Дано» дельты 6; посеяно 4 (держатели, N, Ф12-45 а, Ф12-45 в); названной
    фикстурой 1 (сессии «2»); «не выполнилось» 1 (край — пин без глагола). Инъекций 2, обе
    красные.

# ─── БЛОКИРУЮЩИЕ ────────────────────────────────────────────────────────────
blocking:
  - id: Б7
    kind: DoD
    title: >-
      §9 DoD обещает «кто вправе сбросить второй фактор — одно утверждение во ВСЕХ местах, где
      оно стоит», а его предикаты меряют 4 файла из 11: 8 вхождений прежнего держателя из 16 вне
      предиката. §11 называет расхождение «в трёх местах».
    sections: [§9 DoD (:2263–2284), §11 строка «описание ResetSecondFactor расходится» (:2339)]
    predicate: >-
      Единица — вхождение фразы, называющей держателем сброса распорядителя аккаунта (либо 404
      «чужому аккаунту»), в рукописном файле вне приёмок и записей ревью, без тестов и без
      порождённых *.pb.go. Два прохода, потому что каждый в одиночку упускает одно место:
      построчный `git grep -n -E 'второго фактора распорядител|сбросом распорядител|
      актором-распорядител|распорядитель повторяет вызов|личность распорядителя|чужой аккаунт —
      сокрытие|чужой аккаунт получает|the person.s Account' 39628487 -- ':!docs/engineering/
      acceptance' ':!docs/specs/reviews' ':!*_test.go' ':!*.pb.go'` упускает перенос строки
      (`reset_second_factor.go:6–7` «сброс второго / фактора распорядителем»); проход со склейкой
      строк по файлам, называющим сброс, упускает `sf_remove.go:116` (там нет слова «сброс
      второго»). Объединение — 16 вхождений в 11 файлах. Контроль в обе стороны: комментарий
      модели у `identity_suspender` (называет распорядителя в верном смысле) — 0 попаданий;
      файл из предиката DoD — попадания есть.
    outcomes: >-
      Покрыто предикатами DoD — 8 вхождений в 4 файлах: `user_service.proto` (1), `user.mdx`
      (1), `reset_second_factor.go` (5), `catalog_copy_parity.go` (1). НЕ покрыто — 8 в 6:
      `cmd/kaname/loginlane.go:144`, `api/user/audit.go:28`, `api/user/handler.go:55` и `:184`
      («ResetSecondFactor — сброс второго фактора распорядителем (Ф12-30)» — описание самого
      RPC), `operationresolver/resolver.go:155` и `:160`, `domain/human_session.go:233`,
      `humansession/sf_remove.go:116`. Сверх счёта: `reset_second_factor_test.go:182` — довод
      проверки «байт-идентичным сокрытию существования», которого после редакции 9 нет.
    why_blocking: >-
      Пункт DoD измерим, но его предикат у́же его утверждения: после правки четырёх файлов все
      предикаты дадут «→ 0», #1281 закроется «выводом предикатов», а восемь комментариев
      продолжат называть держателем распорядителя аккаунта со ссылкой на Р10 и Ф12-30.
      Комментарий о праве, противоречащий модели, — ровно класс `security-hardening.md` п.5:
      он провоцирует «починку» модели под текст, то есть возврат права аккаунту, которое
      решение владельца отнимает. Сумма по названным строкам не видит строки, которой нет.
    needed: >-
      В §9 заменить (или дополнить) предикат `reset_second_factor.go` одним предикатом по дереву
      с названной единицей, двумя проходами либо явным перечнем 11 файлов и «сегодня 16 → 0»; в
      §11 — «в трёх местах» заменить измеренным числом с единицей.
  - id: Б8
    kind: NEGATIVE
    title: >-
      Ф12-30 не называет исход ДЕРЖАТЕЛЯ на несуществующем и на кривом `user_id`; редакция 9
      сняла единственное место, где промах этого глагола был утверждением, а посев показал, что
      держатель до промаха доходит.
    sections: [Ф12-30 «Когда»/«Тогда» (:1650–1690), §6 строка Ф12-30, §7 инв. 2 строка «снятие и сброс»]
    predicate: >-
      Посев (constructibility, п.1): дверь `CheckRelation` отвечает `A` на N — «да» (плоское
      замыкание надзора облака, `authorize_service.go:299/315/702`), `O` на N — «нет». Значит
      запрос `A` на N проходит край и решается службой: `reset_second_factor.go` — кривой id →
      INVALID_ARGUMENT «invalid user id…», несуществующий → NOT_FOUND `User <id> not found`
      (юнит `reset_second_factor_test.go:170–182`). В документе: `grep -n 'invalid user id\|User
      <' <документ>` → одна строка, :967 — исторический абзац о снятом 404, не «Тогда».
      Редакция 8 держала промах в «Тогда» Ф12-30 компаратором («байт-идентичным промаху по
      несуществующему <id>»).
    outcomes: >-
      Отказы не держателям (403 на U и на N) названы; direct-read линия держателя — промах и
      кривой id (`api-conventions.md` §Error-format, §By-lane code-split) — не названа ни одним
      сценарием. Вместе с ней нет и контраста к утверждению «тела двух отказов O различаются
      только user_id»: неразличимость U и N у не держателя осмысленна рядом с их различимостью у
      держателя.
    why_blocking: >-
      Чек-лист §3.2: отрицательные сценарии обязаны покрыть NOT_FOUND и INVALID_ARGUMENT
      глагола; Ф12-30 переписан целиком, и исход, до которого в проде доходит администратор
      облака, контрактом не является. Пропуск всплыл бы только в чужой отладке.
    needed: >-
      В Ф12-30 одно «И»: `A` для N — NOT_FOUND / 404, `User <N> not found`, состояние у всех не
      тронуто; `A` для кривого id — INVALID_ARGUMENT / 400 с текстом службы; в §7 инв. 2 — пара
      «A на N ↔ A на U» (одно различие — существование цели), в §6 — род (отказ 404, 400).

# ─── ВАЖНЫЕ, НЕ БЛОКИРУЮЩИЕ (нумерация продолжает Н22 круга 5) ─────────────
non_blocking:
  - id: Н23
    where: шапка :120–121, §1.2 Ф-м :397, Р10 :955–957 и :968
    what: >-
      Общее утверждение «сокрытие существования у края — только на чтениях, у глагола-мутации
      отказ — 403» неверно о дереве: флаг `hide_existence` стоит на двух мутациях
      (`RegistryService/Update`, `/Delete` в каталоге службы), а край читает его ДО эвристики
      и для любого глагола (kacho b5fa093341f, `gateway/internal/middleware/permission_catalog.go`,
      `HidesExistenceOnDeny`: флаг → true; иначе `/Get` на `v_get`). Верно частное: у записи
      ResetSecondFactor флага нет, поэтому 403. Решение (403) от этого не меняется — Р10 уже
      несёт настоящий довод («флаг дал бы 404 и своему распорядителю, который человека видит»).
      Правка: заменить общее утверждение частным.
    predicate: >-
      python3 -c 'import json; print([e["fqn"] for e in json.load(open("internal/apps/kaname/
      seed/embedded/permission_catalog.json")) if e.get("hide_existence")])' → три записи, из
      них две мутации реестра.
  - id: Н24
    where: Р3 :514–515, §0.4 :292, §3 Д27 :1082, Ф12-34 :1789, §9 DoD :2246
    what: >-
      Редакция 9 сняла «28» из Ф12-30 доводом Ф11-27 и сама называет на 39628487 «30 записей с
      полом «2»», но «27 → 28» оставлено в пяти местах, из них одно — целевое значение DoD. На
      стволе предикат DoD даёт 30, и цель «28» недостижима на верном продукте. Якорь «сегодня» =
      4f6ab4be объявлен в шапке, поэтому не блокирует; предложение — выразить пункт DoD долей:
      «запись ResetSecondFactor с полом «2» — в обеих копиях» (уровень P Ф12-30 это уже
      утверждает) либо «+1 к дереву без записи».
    predicate: >-
      python3 -c '…sum(1 for e in d if e.get("required_acr_min")=="2")' на 39628487 → 30
      (записей всего 349).
  - id: Н25
    where: Р10 :959–960 («не держатель — … и сам человек на своей записи»), Ф12-45 (б) «Близнец»
    what: >-
      Верно для человека, не являющегося администратором облака. Посев: дверь отвечает `A` «да»
      даже на несуществующем N (плоское замыкание), значит и на своей записи. Близнец Ф12-45 (б)
      — «A из Ф12-30» — отличается от (б) двумя фактами (держит ли отношение И цель — сам
      вызывающий); одно-фактный близнец — тот же W с `system_admin` на своей записи. Правка —
      формулировкой; решение владельца такой путь не запрещает (он требует «2» у сессии).
  - id: Н26
    where: Ф12-45 (в) «Дано»
    what: >-
      Свежесть сессии `L` не названа, а у (а) сессия несвежа: пара (а)/(в) различается лицом,
      годностью кода и, возможно, свежестью. Назвать сессию `L` так же, как `S1` (несвежая «1»),
      — тогда различие одно: годен ли код.
  - id: Н27
    where: §11, две строки «задача не заведена — заказана вместе с редакцией 9»
    what: >-
      Правка F4d-20 у её владельца и поле `backupCodesRemaining` в ответе `remove` после снятого
      набора — находки без задачи. Вторая подтверждена посевом: ответ remove запасным кодом
      несёт 9 при `enrolled: false`. Заводит диспетчер; документ номер допишет.
non_blocking_carried_over: >-
  Н22 круга 5 (перечень причин клетки `second-factor-refused` в Р11 у́же дерева на два вердикта)
  — Р11 редакцией 9 не тронут, открыт. Н9, Н10, Н12…Н16 круга 2 — как в кругах 3…5.

# ─── ПОЗИЦИЯ ПО РЕШЕНИЯМ, ТРОНУТЫМ РЕДАКЦИЕЙ 9 ──────────────────────────────
decisions:
  Р9:
    verdict: ПРИНИМАЮ
    basis: >-
      Самосброс — `remove` с кодом, без отдельной свежести и без «2»; прочтение «без кода»
      отвергнуто с доводом (второй фактор не сильнее пароля; почтовый ящик доводил бы до «2»).
      Посеяно (а) и (в); инъекция свежести в remove красит (а).
  Р10:
    verdict: ПРИНИМАЮ ПО СУЩЕСТВУ
    basis: >-
      Держатель — администратор облака, третьего нет; не держателю — 403 края; `404` отвергнут
      верным доводом (флаг дал бы ложь своему распорядителю). Посев вердикта: D, D₂, O, сам
      человек — «нет», A — «да»; инъекция источника уровня аккаунта красит посев и гейт модели.
      Остаток — Б8 (направление держателя) и Н23, Н25 (формулировки).
  Р6, Р12, §0.2, §3:
    verdict: ПРИНИМАЮ
    basis: >-
      Правки — следствие Р10; §3: 29 строк, 29 уникальных, сумма категорий 29, «отступлений»
      два (Д5, Д6) — согласно §0.2 и §7 инв. 13.
  Р1…Р5, Р7, Р8, Р11:
    verdict: КАК В КРУГАХ 2…5
    basis: редакцией 9 не тронуты — доказано диффом; числа Р3 — Н24.

neighbours:
  kaname#254: >-
    Решение владельца дословно — Р10, §0.2, §3 Д6, §11. Задача OPEN; её предикаты в §9 DoD —
    см. Б7.
  F4d-20: >-
    Отступление в части актора и формы отказа названо (Д6) и видно владельцу в событии; правка
    строки у владельца F4d — без задачи (Н27).
  user.mdx: >-
    Страница продукта уже называет держателем «облако» (:115) и 403 распорядителю у Block/
    Unblock (:396) — довод Р10 «один факт — один код» опирается на верное; :350 («чужой аккаунт
    получает NOT_FOUND») — в предикате DoD.
  край: >-
    Пин службы без глагола — край-половина Ф12-30/45 (б) в третьей категории до перекатки пина;
    документ это называет.

predicates_run:
  - what: предикаты самого документа
    got: >-
      сценариев §5 — 45; строк §6 — 45; латиницей — 0; строк Д — 29 (уникальных 29), сумма
      категорий 29; сверка §4.3 ↔ §6 — пусто; `23505|23514` в §5 — 0; `ENROLLMENT_EXPIRED` в §5 — 0
  - what: «сегодня» всех семи предикатов DoD редакции 9 на 39628487 и одного в kacho b5fa093341f
    got: >-
      `define identity_suspender` — `super_admin from account`; две копии модели равны; гейт —
      «сверено 3»; ResetSecondFactor в фикстуре вердикта — 0; «the person's Account» — 1;
      «чужой аккаунт получает» — 1; «распорядител|сокрытие существования» в
      reset_second_factor.go — 5; запись в ведомости паритета копий — 2; копия каталога края — 0.
      Все восемь равны объявленным.

gates_run:
  - name: go test -count=1 -short ./internal/check/ — ДО записи, на 4ba95cf8
    result: ok (17.86 s)
  - name: go test -count=1 ./internal/authzmap/ -run TestGoverningTheIdentityHasNoAccountLevelSource -v
    result: PASS — «записей каталога прочитано 349 · RPC «распоряжение строкой» сверено 3 · контроль чтения записи iam_user.v_get (источников 6)»
  - name: go test -count=1 ./internal/service/ -run TestGoverningTheIdentityIsNotReachableFromInsideTheAccount -v
    result: PASS (настоящий Postgres) — «субъектов спрошено 5»
  - name: go test -count=1 -short ./internal/check/ — С ЭТОЙ записью в индексе
    result: >-
      ok (19.76 s). TestHistoryQuestionsAreAskedOfTheTrunk без записи / с записью: файлов 3136 →
      3137, вопросов об истории 76 → 77, ствол 51 → 52, merge-base 57 → 58, вершина не названа
      22 → 22, находок 0 — гейт видел запись и её единственный вопрос истории (вывод эпохи,
      задан стволу); записи ведомости не потребовалось.

verification:
  method: >-
    дифф редакций против объявленного состава; посев «Дано» Ф12-30 и Ф12-45 на настоящей базе и
    на харнессе с управляемыми часами; две инъекции в обратную сторону; чтение модели, каталога
    обеих копий, решения края о сокрытии, кода remove и ResetSecondFactor; перепись описаний
    держателя по дереву двумя проходами с контролем; предикаты документа и DoD.
  subject_digest_matches_current_document: true
  throwaway_probe: >-
    Две одноразовые пробы (`internal/service/zz_r6_reset_probe_integration_test.go`,
    `internal/apps/kaname/api/humansession/zz_r6_selfreset_probe_test.go`) — удалены после
    прогона; две инъекции (`sf_remove.go`, обе копии `fga_model.fga`) — восстановлены из копий,
    `git status --porcelain` перед записью → пусто.
  header_left_untouched: true

reviewer_errors_named_first:
  - >-
    Первая инъекция в модель легла в `internal/authzmodel/fga_model.fga`, а гейт модели читает
    каноническую `proto/kaname/cloud/iam/v1/fga_model.fga`: гейт остался зелёным, и это чуть не
    ушло в запись как «гейт слеп к источнику уровня аккаунта». Повтор на канонической копии —
    красный. Находкой это не является; ошибка моя.
  - >-
    Первый прогон пробы держателей покраснел на МОЁМ контроле фикстуры: я проверял «владелец
    видит свой аккаунт» через `v_get`, а в плоской модели `v_get` на аккаунте материализуется
    выдачей. Контроль переведён на структурное `admin` аккаунта.
  - >-
    Мой первый предикат переписи (построчный) не видел фразу, перенесённую на новую строку, а
    второй (со склейкой) — файл без слова «сброс второго». Число 16 — объединение двух проходов;
    каждое в одиночку дало бы меньше.

limits:
  - >-
    Край-половина Ф12-30 и Ф12-45 (б) не исполнялась — третья категория (пин без глагола).
  - >-
    Сессии «2» посевом записи для `A`, `D`, `D₂`, `O`, `W` не сеялись: вердикт двери от уровня
    сессии не зависит, пол судит край.
  - >-
    Страницы продукта сверх `user.mdx` и `auth-lane.mdx` на предмет держателя читались
    переписью, а не построчно.
