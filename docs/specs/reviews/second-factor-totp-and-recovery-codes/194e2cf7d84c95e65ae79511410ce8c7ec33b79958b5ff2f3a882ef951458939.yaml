schema_version: 1
kind: acceptance_review
subject:
  repository: PRO-Robotech/kaname
  path: docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md
  sha256: 194e2cf7d84c95e65ae79511410ce8c7ec33b79958b5ff2f3a882ef951458939
  static_status: DRAFT
  static_status_note: >-
    Токен `DRAFT` в шапке документа — статическая форма объявления, а не действующий вердикт
    (change-graph.md §2). Шапка НЕ правится этой записью: одобрение выводится из внешнего
    события и этой записи, не из слова в заголовке. Прежние записи каталога не трогаются —
    эта редакция (12) получает соседнюю запись по своему отпечатку.
  base_commit: 0ffe0c7f3993991b8969e498d296bf5740cbca6e
  content_commit: none
  content_commit_note: >-
    Редакция 12 на момент круга НЕ закоммичена: рабочая копия ветки `287` @ 0ffe0c7f, изменён один
    файл (`git status --short` → ` M docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md`).
    Вердикт привязан к отпечатку, а не к коммиту: после коммита
    `git show <коммит>:docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md | sha256sum`
    обязан дать 194e2cf7… дословно — иначе эта запись о другом содержимом.
  branch: '287'
  wave_base: 20a5dcaa11b769cbd19eec033f158e5eac6a5b88
  pull_request: pending
  task: PRO-Robotech/kacho#1281
  edition_task: PRO-Robotech/kaname#304
  edition_task_note: >-
    Правка исполняет исход 2 `kaname#304` (предикат снятия `kaname#287` переформулирован на
    собственный замок полосы); сама находка — `kaname#287`.
  lines: 2623
  round: 9
  predecessor_record: docs/specs/reviews/second-factor-totp-and-recovery-codes/02b30c2e50c34933650fcbc87d64a16cf75f52c7a7ccad25c4b58ec1f9f048cf.yaml
  predecessor_note: >-
    Непосредственный предшественник — редакция 11 (02b30c2e…, круг 8 APPROVED, внешнее событие
    kacho#1281 comment 5737617038, 2026-09-18T23:58:56Z). Она же лежит в стволе и в HEAD ветки:
    `git show origin/main:<путь> | sha256sum` (origin/main = cbbac984) и
    `git show HEAD:<путь> | sha256sum` (HEAD = 0ffe0c7f) → 02b30c2e… оба. Одобрение редакции 11
    на редакцию 12 НЕ переносится (change-graph.md §2).
  fingerprint_predicate: >-
    sha256sum docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md в рабочей копии
    tmp/kaname-287 (ветка 287 @ 0ffe0c7f) → 194e2cf7d84c95e65ae79511410ce8c7ec33b79958b5ff2f3a882ef951458939,
    совпал с объявленным в задании дословно; wc -l → 2623.
  measurement_revisions: >-
    Дом службы: kaname, ветка 287 @ 0ffe0c7f (коммит фикса), база волны 20a5dcaa, ствол
    origin/main cbbac984. Ссылок в клоне (refs/heads + refs/remotes) — 187 на момент круга.
  task_premise_remeasured: >-
    (1) Отпечаток 194e2cf7… — ПОДТВЕРДИЛОСЬ своим sha256sum. (2) «Отсылок было три, а не две» —
    ПОДТВЕРДИЛОСЬ: `grep -c 'имя — в теле\|приведено в теле'` на HEAD → 3, в рабочей копии → 0;
    прочие вхождения «в теле» (11) — про код в теле запроса и про решение в теле другой задачи, к
    предмету не относятся. (3) «Тело kaname#287 имени пробы-опыта не несёт» — ПОДТВЕРДИЛОСЬ:
    `gh issue view 287 -R PRO-Robotech/kaname --json body` — имени нет, комментариев нет.
    (4) «Предикат kaname#304 даёт 0 в 187 ссылках, контроль — 3» — ПОДТВЕРДИЛОСЬ перемером:
    имя пробы-опыта — 0 находок, имя пробы-замка — 3 (refs/heads/287,
    refs/heads/issue-287-second-factor-window, refs/remotes/origin/issue-287-second-factor-window).
    (5) «В `git diff 20a5dcaa...HEAD -- '*_test.go'` 4 файла, 10 функций» — ПОДТВЕРДИЛОСЬ.
    Ни одна посылка не опровергнута.

verdict: APPROVED
reviewer_role: acceptance-reviewer
reviewed_at: '2026-09-23'

# ─── ОДОБРЕНИЕ ВЫВОДИТСЯ ИЗ ЗАПИСИ И СОБЫТИЯ, А НЕ ИЗ СЛОВА В ШАПКЕ ──────────
effective_approval:
  issued: false
  ban1_lifted: false
  why: >-
    Вердикт круга 9 — APPROVED: блокирующих ноль. Дельта против одобренной редакции 11 —
    переадресация трёх отсылок на пробу, существующую в дереве ветки, и строка редакции 12;
    сценарии, их ID и счёт (46), решения Р1…Р12 и величины не тронуты. Названная проба
    исполнена рецензентом в обе стороны (см. experiment). Санкция этим не выдана: роль без
    внешнего события полномочия не даёт (change-graph.md §3); политика
    (docs/changes/policy.yaml → review_authority.acceptance-reviewer) называет `pointpu`.
  what_the_event_would_lift: >-
    Перенос действующего одобрения с редакции 11 (02b30c2e…) на редакцию 12 (194e2cf7…). Нового
    предмета для кода редакция 12 не вводит: санкция на код по дельте редакций 9…11 уже выдана
    событием 5737617038 и остаётся; событие на 194e2cf7… нужно, чтобы документ, входящий в ствол
    вместе с фиксом, нёс одобренный отпечаток.
  what_the_owner_sees_in_the_event: >-
    Приёмка Ф12, редакция 12: предикат снятия находки kaname#287 переадресован на замок полосы,
    живущий в дереве (исход 2 kaname#304); контракт Ф12 Р7 / Ф3 Р10 не изменён.
  publication_pending:
    required_for_sanction: true
    expected_from: диспетчер/курьер волны
    expected_actor: pointpu
    expected_mode: courier_of_agent_verdict
    expected_form: >-
      issue_comment в kacho#1281 с полями role, verdict, subject, subject_revision (коммит
      редакции 12, после его создания), subject_sha256 (194e2cf7…) дословно; блок `event`
      дописывается ОТДЕЛЬНЫМ коммитом поверх этой записи — вердикт и разбор задним числом не
      правятся.

authority:
  mode: versioned_policy_authority
  epoch: post-cutover
  policy_document: docs/changes/policy.yaml
  policy_field: review_authority.acceptance-reviewer
  authorized_actors: [pointpu]
  allowlisted_for_role: false

attribution:
  reviewer_kind: claude_agent
  publishing_actor: pointpu
  publication_mode: courier_of_agent_verdict
  personal_review_by_publishing_actor_claimed: false

scope:
  reviewed: >-
    Дельта редакций 11 → 12, каждый ломоть диффа положен на раздел: строка редакции 11 (шапка,
    скобка о пробе-опыте); новая строка редакции 12 (шапка); §8, строка «Ф12-31…33, 46 подбор и
    время»; §9 DoD, пункт замка kaname#287. Сверено с kaname#304 (исходы и предикат снятия) и с
    деревом ветки 287 (проба, её файл и ветвь). Разделы, не тронутые диффом, заново не
    перечитывались: их вердикт — записи кругов 1…8.
  method: experiment_not_reading
  document_edited_by_reviewer: false

# ─── ДЕЛЬТА ПРОТИВ ОБЪЯВЛЕННОГО СОСТАВА — НЕ ШИРЕ И НЕ УЖЕ ─────────────────
delta_vs_declared:
  predicate: >-
    git diff --numstat -- <путь> → 30 6; git diff -U0 → четыре ломтя: @@ -179,3 +179,5 @@ (шапка,
    строка редакции 11), @@ -209,0 +212,18 @@ (шапка, строка редакции 12), @@ -2323 +2343 @@
    (§8, 2326…2366), @@ -2377,2 +2397,6 @@ (§9, 2366…2489).
  declared: >-
    строка редакции 11 (скобка о пробе-опыте) · §8 (строка «подбор и время») · §9 DoD (пункт
    замка kaname#287) · новая строка редакции 12
  measured: >-
    Дифф касается РОВНО объявленных мест; не шире и не уже. Счёт сценариев не сдвинулся:
    §5 → 46 уникальных меток, §6 → 46 строк. Отсылок на тело kaname#287 как место имени пробы
    в документе не осталось (0).

# ─── ЭКСПЕРИМЕНТ: ПРОБА СУЩЕСТВУЕТ, ИСПОЛНЯЕТСЯ И СПОСОБНА УПАСТЬ ─────────
experiment:
  probe: TestF12_46_OnlyCodeSuccessOpensTheWindow
  file: internal/apps/kaname/api/humansession/second_factor_window_lock_test.go
  subtest: 'пара «вход»: между сериями вход ПАРОЛЁМ'
  exists_in_tree: >-
    Да: файл и функция добавлены коммитом 0ffe0c7f (`git diff 20a5dcaa...HEAD -- '*_test.go'`),
    имя ветви дословно совпадает с документом.
  green_after_fix: >-
    go test -run '^TestF12_46_OnlyCodeSuccessOpensTheWindow$' -count=1 -v
    ./internal/apps/kaname/api/humansession/ на ветке 287 @ 0ffe0c7f → ok; исполнено ветвей 3,
    зелёных 3, красных 0 (go1.26.8).
  red_before_fix: >-
    Тот же прогон на экспорте базы волны (`git archive 20a5dcaa`) с подложенным файлом пробы,
    обвязка пробы на базе не менялась (`git diff --quiet 20a5dcaa HEAD` по файлам обвязки) → FAIL:
    ветвь «вход ПАРОЛЁМ» красная, ветвь «церемония ПАРОЛЕМ» красная, ветвь-близнец «церемония
    КОДОМ» зелёная. Замок краснеет до фикса и зеленеет после — пункт 2 предиката снятия
    kaname#304 показан прогоном в обе стороны. Экспорт удалён после прогона.

# ─── ПРОВЕРКИ ПО КАНОНУ ────────────────────────────────────────────────────
checks:
  fingerprint_match: true
  scenario_count: '§5 = 46, §6 = 46, дельтой не тронуты'
  producer_present_for_every_then: >-
    Не изменилось против круга 8: §6 не тронут. Держатель в дереве для Ф12-46 теперь назван
    координатой (функция, файл, ветвь) вместо отсылки на тело задачи. Сценариев без
    производителя — 0.
  positive_twin_for_every_negation: >-
    Не изменилось против круга 8; близнец Ф12-46 (б) в названной пробе — ветвь «церемония
    КОДОМ», зелёная на обеих ревизиях.
  constructibility: >-
    Нового «Дано» дельта не вводит. Конструируемость «Дано» Ф12-46 показана ИСПОЛНЕНИЕМ: обвязка
    пробы строит личность с паролём и фактором и сессию «1» на обеих ревизиях (прогоны выше).
    Посеяно 1 из 1 (через фикстуру пробы), «не выполнилось» 0.
  dod_measurable: >-
    Пункт замка kaname#287 измерим: проба, файл и ветвь названы, обе стороны показаны прогоном.
  open_questions: none
  conventions: 'Контракт, RPC, поля, отказы — не тронуты дельтой.'
  disclosure: >-
    Новый текст не добавляет ни условия, ни следствия сверх одобренной редакции 11: он называет
    место пробы (тестовый файл) и то, что проба-опыт в дерево не вошла. Имя пробы-опыта в документ
    не внесено. Текст этой записи и тела события удержан на уровне «предикат переадресован,
    проба исполнена в обе стороны», без описания наблюдаемого до фикса. НЕ блокирующее.

blocking_findings: []

non_blocking_findings:
  - id: N1
    kind: finding
    class: neighbour-same-defect
    text: >-
      Сосед Ф3 (`login-lane-issues-our-session-and-logout-ends-it-server-side.md`, 713ff0bd…,
      редакция 11) несёт ту же мёртвую отсылку: строка 16 называет местом имени пробы-опыта тело
      kaname#287 (`grep -c 'имя — в теле\|приведено в теле'` → 1). Предмет этого круга — только
      Ф12, поэтому не блокер; но пункт 1 предиката снятия kaname#304 («предикат ссылается на пробу
      в дереве») закрыт не во всех местах, пока Ф3 не получит свою редакцию и свой круг по своему
      отпечатку.
  - id: N2
    kind: observation
    class: tracker-predicate
    text: >-
      Пункт 3 предиката снятия kaname#304 — «изменение предиката записано комментарием в #287» —
      не выполнен: у kaname#287 комментариев ноль. Это действие трекера, не содержание приёмки.
  - id: N3
    kind: observation
    class: doc-truthfulness
    text: >-
      Строка состояния в шапке отстаёт от каталога записей (называет шесть кругов и редакцию 11;
      записей теперь девять, текст — редакция 12, санкция по дельте 9…11 выдана событием
      5737617038). Документ отставание сам называет в строке редакции 12 и выносит его за объём
      правки. Класс повторяется (редакция 9 уже приводила эту строку к правде), поэтому при
      следующей правке лучше заменить числа в строке предикатом по каталогу записей, чтобы строка
      перестала устаревать.
  - id: N4
    kind: observation
    class: holder-independence
    text: >-
      Пункт DoD замка теперь держится одной пробой в обеих половинах (весь замок и его ветвь
      «вход ПАРОЛЁМ»); независимое воспроизведение заменено замком полосы. Это прямо разрешённый
      исход 2 kaname#304, и замок показан способным упасть, поэтому не блокер.

seed_attempt:
  performed: true
  outcome: green
  reason: >-
    Изоляция — собственный экспорт `git archive 20a5dcaa` в tmp/ воркспейса (удалён) и рабочая
    копия ветки 287; стенд не нужен: «Дано» Ф12-46 строит обвязка пробы в процессе.
