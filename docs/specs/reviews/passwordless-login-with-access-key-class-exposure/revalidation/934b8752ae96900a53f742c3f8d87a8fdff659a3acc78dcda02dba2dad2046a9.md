# Экспозиция классов — ФОРМАЛЬНАЯ СВЕЖАЯ ПЕРЕСВЕРКА (лёгкая): вход без пароля — ключ доступа как первичный метод (iam Ф13)

> **Issue:** [PRO-Robotech/kacho#1282](https://github.com/PRO-Robotech/kacho/issues/1282) · эпик `#1266`
> **Subject (замысел):** `docs/engineering/acceptance/passwordless-login-with-access-key-design.md` (дом `PRO-Robotech/kaname`)
> **SHA-256 замысла:** `934b8752ae96900a53f742c3f8d87a8fdff659a3acc78dcda02dba2dad2046a9` (перемерено `sha256sum` в worktree — **совпало**; блокера по отпечатку нет)
> **Предыдущая пересверка:** `…-class-exposure/revalidation/37d613d944e0c646810680d54c6e8a4059068b6731a18a564ad664163d7c2f4d.md` («к-замыслу 14/14, к-коду по существу; единственный блокер — §7 открытые вопросы»)
> **Первичный разбор:** `…-class-exposure/initial/3f4ee3b1…2964599d.md` (D1…D14)
> **Отпечаток приёмки (APPROVED, ред.4, НЕ трогалась):** `3f4ee3b1bc845e0ae6af3e6e8fc35b90fc93c544b40d65530f08865f2964599d`
> **Роль:** class-exposure-analyst · **Режим полномочия:** пересверка (на отпечаток замысла, ДО его одобрения; контур `change-graph.md` §4)
> **Actor:** pointpu (`docs/changes/policy.yaml`)
> **Ревизии измерений:** база сверки кода `kaname@ae1338bae42583543cdd656a5d22183dcc2f344f` (worktree HEAD — **та же**, что у предыдущей пересверки; дрейфа базы нет); край `kacho@ac33f733`.
> **Снимок:** 2026-09-19

Это **формальная свежая пересверка** на новый отпечаток замысла (`934b8752…`) после сворачивания §7.
Она **лёгкая** by construction: относительно `37d613d9…` изменён только §7, а существо D1…D14 предыдущая
пересверка признала к-коду-готовым на той же базе. Вердикта рецензента у разбора классов нет — решение
несёт строка «Итог».

---

## Предмет пересверки: чем эта редакция отличается от `37d613d9…`

Предыдущая пересверка вынесла ровно один (мягкий) блокер: §7 назывался «Открытые вопросы» и держал три
вопроса, а `change-graph.md §4` запрещает одобрение замысла при **нерешённом вопросе**. Разрешения были
предоставлены в теле той пересверки и заказаны design-author'у строкой «нужен следующий». Автор их свернул
в §7 — это и породило отпечаток `934b8752…`, требующий свежей пересверки (`change-graph.md §2`: вердикт
привязан к отпечатку, а не к документу; сам §7 текущей редакции это и проговаривает, стр. 320–326).

**Осмотрено:** замысел целиком (§0–§8), в особенности §7 (стр. 288–326) и §3 (стр. 183–211); предыдущая
пересверка `37d613d9…` целиком. В дереве базы `kaname@ae1338ba` перемерены четыре несущих предиката (ниже).

---

## Пункт 1 — §7 более НЕ держит открытого вопроса; три триггера сохранены

**Заголовок:** `## §7 Разрешено пересверкой` (стр. 288) — переименован с «Открытые вопросы». Тело —
три **разрешения**, не три вопроса.

| # | вопрос прежней редакции | разрешение (§7 текущей) | триггер заведения заново — сохранён? |
|---|---|---|---|
| **q1** | форма порта kratos | РАЗРЕШЕНО: ссылочная адаптация; существо — наше (Ф7 сверка · Ф1 выдача · Ф11 уровень · Ф3/Ф7 единый отказ) | **да** (стр. 301–302): «вендоринг кода kratos · копия файла kratos в дерево (ban #20) · новая go-зависимость» → новый пункт + новая пересверка |
| **q2** | опциональное `AccessKeyID` в `IssueInput` | РАЗРЕШЕНО: покрыт классом D4/D10; в payload события, не в записи сессии; sentinel чист; PII-гейт покрывает | **да** (стр. 310–313): «поле в запись сессии · второй эмитент значения · `ak-…` перестал быть непустым» → снятие покрытия + новая пересверка |
| **q3** | «текущая сессия» при снятии ключа | РАЗРЕШЕНО: беспредметен — дословное переиспользование `EndOtherSessions(…, keep, …)` (Ф3 Р6); нового механизма нет | нечего заводить (нового механизма нет); поведение Ф3 Р6, приёмкой так и названо |

**Явное закрытие:** стр. 320 — «Открытых вопросов §7 больше не держит». Барьер `change-graph.md §4`
(«одобрение запрещено при нерешённом вопросе») со стороны §7 снят. Триггеры внутренне непротиворечивы:
q1-триггер зеркалится в §3 (стр. 209) и §4 (стр. 230); q2-триггер — в §6 (стр. 278–279, 281–282).

**Граница измерения (назвать честно):** «сохранены **дословно**» относительно прежнего документа
байт-в-байт не проверяемо — содержимое `37d613d9…` в дереве не сохранено (файл untracked, истории git
нет). Проверено **по существу и по внутренней согласованности**: все три триггера присутствуют, совпадают
по смыслу с разрешениями предыдущей пересверки и не противоречат §3/§4/§6. Один нюанс в §7.2: перечень
триггера снятия покрытия называет «второй эмитент значения», но не «новый вид события» отдельной строкой
(предыдущая пересверка называла их связкой). Это **внутри изменённого §7**, входит в ожидаемую дельту и не
теряет покрытия: «новый вид события» независимо есть триггер `change-graph.md §4` и назван в §6. Не блокер.

---

## Пункт 2 — существо D1…D14 неизменно относительно `37d613d9…`

Байт-диффа против `37d613d9…` нет (содержимое не сохранено). Сверка проведена **чтением текущей редакции
против гранулярных цитат предыдущей пересверки** — она перечисляла координату отображения каждого `D` и
измерения §2.1–§2.4/§3/§4. Совпадение по всем осям:

| ось | предыдущая пересверка (`37d613d9…`) | текущая редакция (`934b8752…`) | совпало? |
|---|---|---|---|
| §1 таблица D1…D14 | 14 строк, D→координата | стр. 52–65, те же 14 решений и цели дерева | **да** |
| §2.1 рукоятка | `userHandle` обяз., безусловная сверка, охрана `len>0` не наследуется, `NULL`=совпасть не может | стр. 77–103 — то же | **да** |
| §2.2 анти-replay | своя таблица в БД, ключ = контекст формы, атомарное сгорание, cross-replica, TTL = литерал Ф7 | стр. 105–131 — то же | **да** |
| §2.3 сверка одна | 2-й ВЫЗЫВАЮЩИЙ, не 2-е объявление; добавки в use-case; приманка у вызывающего; гейт Ф13-31 | стр. 133–160 — то же | **да** |
| §2.4 D4/D10 | `access_key_id` в payload, запись сессии не расширена, sentinel чист | стр. 162–179 — то же | **да** |
| §3 карта порта | ссылочная адаптация, существо — Ф7; ban #2, ban #20 | стр. 183–211 — то же (см. косметику ниже) | **да** |
| §4 рёбра | 4 ребра, три внутрипроцессных + пред-существующее kacho→kaname | стр. 215–230 — те же 4 | **да** |
| §5/§6/§8 | полосы L0–L8; триггеры отсутствия; границы публичного текста | стр. 234–339 — согласованы | **да** |

**Косвенное подтверждение «дельта = только §7»:** §3 стр. 211 всё ещё содержит устаревшую ссылку «относит
к открытым вопросам (§7)». Она была истинной под `37d613d9…` (§7 = открытые вопросы) и стала косметически
устаревшей после переименования. То, что §3 её **не** обновил, доказывает, что §3 правился не был —
редактура хирургическая, ровно в §7. Это и есть отпечаток точечной правки одного раздела.

Мои перемеры на `kaname@ae1338ba` (та же база) — числа совпали с предыдущей пересверкой, дрейфа нет:

| утверждение | команда @`ae1338ba` | что оказалось |
|---|---|---|
| проверяющий один, вызывающий один | `git grep -nE 'func .*VerifyAssertion' -- ':!*_test.go'`; `git grep -n 'VerifyAssertion' -- ':!*_test.go' \| grep -v 'func '` | 1 объявление (`verify.go:409`), 1 нетестовый вызывающий (`finish_assertion.go:128`) |
| `IssueSession` — 3 вызывающих сегодня | `git grep -n 'IssueSession(' -- ':!*_test.go' \| grep -v 'func '` | 3 (`login.go:466`, `recovery_complete.go:263`, `register.go:186`); Ф13 — 4-й |
| `AccessKeyID` в `IssueInput` отсутствует | чтение `humansession/issue.go:6–17`; `git grep -n 'AccessKeyID' -- '…/issue.go'` | `Presented`, `EmitAudit` есть; `AccessKeyID` — нет (реальная дельта) |
| маркеров отсрочки в замысле нет | `grep -nE 'TODO\|FIXME\|XXX\|SKIP\|на потом\|потом доделаем\|TBD'` по замыслу | пусто |

---

## Пункт 3 — ни один из пяти запретов одобрения не действует

| запрет (`change-graph.md §4` / `dispatcher.md §7`) | состояние | доказательство |
|---|---|---|
| **маркер отсрочки** | не действует | `grep` по замыслу — пусто; ban #11 чист |
| **нерешённый вопрос** | не действует | §7 переименован в «Разрешено пересверкой»; три разрешения; стр. 320 «открытых вопросов больше не держит». Вхождения `открыт` (стр. 211 — устаревшая ссылка; 250 — «открытие кода»; 290 — «прежняя редакция держала») — не вопросы |
| **неотображённый пункт** | не действует | все 14 `D` отображены (§1 + §2), перечень предыдущей пересверки был пуст, дельта его не трогала |
| **устаревшая пересверка** | не действует | это и есть свежая пересверка на текущий отпечаток `934b8752…` |
| **отсутствующее ревью** | не действует | эта запись — ревью на отпечаток `934b8752…` |

---

## Известное НЕ-блокирующее (косметика, не ⛔)

**§3 стр. 211: «…относит к открытым вопросам (§7)»** — устаревшая **кросс-ссылка** после переименования
§7. Сам концерн (вендоринг → новый пункт + новая пересверка) **РАЗРЕШЁН** в §7.1 с сохранённым триггером;
устарела только метка названия раздела, не механизм. Это не «нерешённый вопрос» по `change-graph.md §4`, а
косметика ссылки — правка её не меняет ни отображение, ни существо. `doc-truthfulness` (два места об одном
предмете): §3 называет §7 «открытыми вопросами», §7 называет себя «Разрешено пересверкой». Отметить для
позднейшей чистки; **одобрение не блокирует**.

**Мелочь того же рода (отметить, не блокировать):** шапка стр. 17 «История пересверки: — (ещё не
проводилась)» — при внесении строки об этой пересверке она **дополняется**, не переписывается (как сама
шапка и предписывает). Доп-строка снова изменит отпечаток — это ожидаемо и не рекурсия-блокер: одобрение
привязано к отпечатку, на котором пересверка проведена (`934b8752…`).

---

## Итог

- **к-замыслу:** ✅ §7 свёрнут корректно — три разрешения, три триггера сохранены (по существу и внутренне
  согласованно); открытого вопроса нет. Отображение 14/14 неизменно; существо D1…D14 совпало с
  `37d613d9…` по всем осям. Отпечаток совпал.
- **к-коду:** ✅ **замысел Ф13 одобрен** — существо к-коду-готово (подтверждено предыдущей пересверкой на
  той же базе, дельта его не трогала). Условия на код перечислены построчно в первичном разборе и
  предыдущей пересверке, проверяемы «сделал/не сделал».
- **вернуть-в-приёмку:** ПУСТО (приёмка `3f4ee3b1…` не менялась).
- **блокер:** НЕТ. Единственный остаток — косметическая кросс-ссылка §3 стр. 211 (не ⛔).

## Вернуть в приёмку

**Пусто.** Неизмеренных утверждений приёмки о дереве нет; Given, которого схема не допускает, нет
(подтверждено первичным разбором; приёмка не менялась).

## Неотображённые пункты (пересверка)

**Пусто.** Все D1…D14 отображены в конкретные решения замысла; дельта `37d613d9→934b8752` = только §7,
отображения не трогала.

## Заказы (адресат · предмет · основание)

Заказы предыдущей пересверки для `integration-tester` **переносятся без изменений** — существо D1…D14 не
менялось. Повторены здесь для полноты:

- **integration-tester** · **D10:** событие `iam.session.issued` несёт `access_key_id`, когда сессию выдал
  ключ; `domain.HumanSession` при этом **не** несёт ни `access_key_id`, ни флагов; пустой `AccessKeyID` →
  ключа в payload **нет** (путь sentinel) · `code-authoring §1.3`, §2.4 замысла, §7 инв.11.
- **integration-tester** · анти-replay: 2 горутины над одним живым испытанием — ровно одно проходит,
  второе — единый отказ; плюс cross-replica · `code-authoring §2.1`, `security-surface S7.3`.
- **integration-tester** · рукоятка входа: отсутствующая/пустая `userHandle` → 400 до сверки; несовпавшая →
  единый отказ; `key.UserHandle == NULL` → единый отказ (не «пропуск») · `code-authoring §1.3, §6.1`.
- **integration-tester** · атомарность снятия ключа: подставной отказ любой записи (ключ · прочие сессии ·
  слот) откатывает всё; текущая сессия жива · `code-authoring §2`, `security-surface S10.3`.
- **integration/measurement-tester** · единый отказ по времени: работа по неизвестному удостоверению
  неразличима с известным · `security-hardening §Hardening п.6`.
- **автор кода (gate-authoring), НЕ integration-tester** · гейт дерева Ф13-31: 1 объявление, 2 вызывающих;
  инъекция копии → красный с координатой; обёртка → молчит; пустой обход → «предмета нет».

**Подтверждение формы порта:** ссылочная адаптация (не вендоринг) — подтверждено предыдущей пересверкой
замером (`git grep -rnE 'github.com/ory' -- '*.go' go.mod go.sum` → пусто; 63 нетестовых «kratos» —
комментарии о пред-существующем внешнем IdP). Дельта §7 форму порта не изменила; §7.1 сохранил триггер
вендоринга. Форма порта — **ссылочная адаптация**, вендоринга в дереве нет.

**Мягкий (не блокирующий) заказ:**

- **design-author** · при удобном случае обновить кросс-ссылку §3 стр. 211 («открытым вопросам (§7)» →
  «разрешено пересверкой в §7») · `doc-truthfulness`. Правка косметическая, породит новый отпечаток —
  свежей пересверки не требует (существо не меняется), но одобрение к коду уже действует на `934b8752…`.
