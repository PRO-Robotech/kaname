# Экспозиция классов: принудительный выход имеет свою причину окончания сессии — пересверка

> **Issue:** [PRO-Robotech/kaname#334](https://github.com/PRO-Robotech/kaname/issues/334) · волна `#358` · эпик `#357` · цель линии `PRO-Robotech/kacho#2564`
> **Subject:** `docs/engineering/acceptance/forced-exit-has-its-own-session-end-reason.md`, ред. 3 (дом `PRO-Robotech/kaname`)
> **SHA-256:** `ae35d0e1c1968d2e58281ff2ad5904f68b4bab7b08a3d1bcf53f82eeef353e1b`
> **Effective acceptance verdict:** `APPROVED`, круг 3, запись `docs/specs/reviews/forced-exit-has-its-own-session-end-reason/ae35d0e1….yaml`. Слово `DRAFT` в шапке документа — статическая форма (`change-graph.md` §2)
> **Роль:** class-exposure-analyst · **Режим:** пересверка
> **Режим полномочия:** `versioned_policy_authority`, post-cutover. `git merge-base --is-ancestor 1202e3cf… HEAD` в воркспейсе даёт 0
> **Actor:** pointpu (`docs/changes/policy.yaml:43`, `class-exposure-analyst`)
> **Ревизии:** `workspace@13e0d0c1`; `kaname@46ccd69cf8cd83cc2dc2b574e8f73f9bfec80ff5` (ветка `334`, копия `tmp/kaname-334-acc`). Код дерева равен `origin/358` = `cca75858`, то есть волне после вливания `#340`: `git diff --name-only cca75858 HEAD` показывает только документ и записи ревью. Ствол `main` = `cbbac984`, эпик `origin/357` = `2f2415ec`
> **Снимок:** `2026-09-23T15:20:32Z`, `git status --porcelain | wc -l` → 0 до записи

**Почему пересверка привязана к приёмке, а не к замыслу.** По норме
(`change-graph.md` §4) пересверку привязывают к отпечатку замысла. Документа замысла у `#334` нет:
`git grep -l 'kaname#334\|forced-exit-has-its-own-session-end-reason' -- docs` находит только
приёмку и записи ревью. Поэтому по заданию пересверка привязана к отпечатку редакции 3 приёмки.
Решения, в которые отображены пункты первичного разбора, — это решения Р1–Р5 и сценарии этой
редакции. Первичный разбор (`initial/0dd1c853….md`) остаётся свидетельством о редакции 2 и не
переписывается.

**Отпечаток подтверждён.** `sha256sum` файла на диске и `git show HEAD:<путь> | sha256sum`
дают `ae35d0e1…`. Это совпадает с subject записи круга 3 и с заданием.

**Дельта ред. 2 → 3 перемерена.** `git diff 2f69d90ab 46ccd69cf -- <приёмка>` меняет шапку
(статус, история, журнал «ред. 3», ревизию измерения), §0.1, §0.3, §0.5, §0.6 пп.2, 3, 4, 6,
§2 п.1, производители KN-SER-01, 02, 04, 05 и 10, обе таблицы §5 и §6 пп.3, 4, 7. Строки
Дано/Когда/Тогда не тронуты. По существу правка меняет только одно: предикат §6 п.7 и его
контроль. Остальное — координаты.

**Осмотрено.** Дифф документа целиком и запись круга 3. `git diff 9dc46b47 cca75858`, то есть
вливание `#340`: 31 файл. В нём не менялись `internal/domain`, `internal/migrations`,
`internal/check`, `proto`, `cmd/kaname/hooks_mux.go` и `humansession/logout.go`
(`git diff --quiet` → 0). Прочитаны:
- `force_logout.go` — :195–240, :320–420, :524–640, :660–888;
- `humansession/iface.go` — порты `Store` и `Writer`;
- дифф `change_password.go`, `recovery_complete.go`, `sf_remove.go`, `hook_pool_adapters.go`;
- `human_session_repo.go` — :405–470.

Предикаты приёмки прогнаны дословно.

---

## Решения, которые вносит приёмка (редакция 3)

Перечень прежний, E1…E10. Решения Р1–Р5 и сценарии редакцией 3 не менялись. Нового внешнего
вызова, асинхронного пути, sentinel-значения и ребра между доменами дельта не вводит.
Вливание `#340` внесло в дерево новую среду, в которую ложатся E2, E6, E7, E9 и E10:

- принудительный выход на посадке `own` — одна транзакция `ForceLogoutWriter`: строка
  личности → снятие → отсечка → событие с исходом;
- при отказе снятия — вторая транзакция частичного исхода.

---

## Экспозиция и условия на код

Столбец «Δ» показывает, что изменилось против первичного разбора: `=` — условие верно как было,
`К` — сдвинулись координаты, `С` — уточнено по существу.

| решение | Δ | класс (раздел скилла) | что должно быть верно в коде | чем держится |
|---|---|---|---|---|
| **E2** закрытое слово против свободной причины | К, С | `code-authoring` §1.3 «Sentinel-коллизия» | аргументом причины в `w.EndOtherSessions` внутри `commitOwnForceLogout` (`force_logout.go:736`) стоит **селектор константы домена**. Это не `marker.Reason`: в этой функции в области видимости только `marker`, так что соблазн сместился с `reason` на поле маркера. Это и не `reason`, и не что-либо, выведенное из `req` или `marker`. Параметр `reason string` — в **5** сигнатурах (было 6), и компилятор разницы не видит | **KN-SER-02 — единственный держатель**; на новой базе прочитано, что он держит. Неверная реализация с `incident-4711` получает отказ ограничения на снятии. Дальше идёт частичный исход (`warrantsPartialOutcome` → true для `ownStepTeardown`), операция с ошибкой, `UNAVAILABLE could not end the login session` (`:856`), строка не снята. Несущие «Тогда» — первое и второе. Третье (журнал отсечек несёт `incident-4711`) зелено у обеих реализаций, потому что отсечку кладёт вторая транзакция |
| **E1/E3** одно слово в четырёх местах | К | §5.1 «Два написания одного значения» | (а) умолчание `:338-341`: `reason = domain.<Новая>`, литерала `:340` нет. (б) SQL-литерал миграции — единственное второе написание, его сверяет E4. (в) комментарий `proto` (`pb.go:808`) не трогается | (а) KN-SER-10 и §6 п.4. (б) KN-SER-07. (в) вниманием |
| **E4** перечень словаря | = | §6.1 «Намеренная узость становится контрактом абстракции» | без изменений: одно объявление; функция со свежим срезом; собран из констант по имени; 4 элемента, без `second-factor-reset`; у новой константы свой комментарий; комментарий блока `human_session.go:218` её причиной отсечки «только» не называет | KN-SER-07; литерал нового слова — KN-SER-10; литералы трёх прежних — вниманием |
| **E4** проба паритета | = | §7.1 «замыкание по одной стороне» | без изменений: все литералы из `pg_get_constraintdef`, равенство множеств, лишнее и недостающее названы значением, чистая функция над текстом. Цикл `human_session_schema_integration_test.go:113-122` не копируется. Печать N и M | KN-SER-07 и инъекция §6 п.12 |
| **E5** накат | = | §5.2 «Обратное заполнение выбирает направление по семантике правила» | без изменений. Версия больше `20260920175119`: старшая не сдвинулась, и открытые ветки 314, 321, 357, 365, 377 миграций не добавляют. Оба маркера goose; шапка со ссылкой на приёмку и «обратного заполнения нет, потому что…»; одна транзакция; форма соседа; операторов данных 0 | `TestNewMigrationOutranksEveryAppliedOne`, KN-SER-06, KN-SER-08, `schemaguard` |
| **E6** откат | С | §2 «Решение и его следствие разнесены» | прежнее: первым оператором идёт захват `kaname.human_sessions`, затем перевод `WHERE ended_reason = 'admin-force-logout'` без правки `ended_at`, затем ограничение из трёх и комментарий направления. **Новое:** откат не берёт ни таблицы, ни строк личности. Писатели `#340` держат порядок «личность → строки сессии». Откат трогает одну `human_sessions`, а ограничение CHECK таблицу личности не блокирует, поэтому цикла нет. Захват личности в откате этот цикл создал бы | значения — KN-SER-09. Окно — вниманием. Под живым новым образом после отката слово отвергается на снятии. Это частичный исход (отсечка + событие `failed`, операция с ошибкой), пока проверка `schemaguard` в readiness не выведет под из трафика. Отказ громкий, искажения нет |
| **E7** первый снявший пишет причину | К, С | §2; `data-integrity` db-cas | не меняются: операторы `human_session_repo.go:417`/`:447` с `ended_at IS NULL` на `:418`/`:448`, ветви `logout.go:65-70`/`:83-86` и сигнатуры `End*`. `SET ended_reason` без `ended_at IS NULL` — 0 из 2. Второго оператора снятия нет. **Новое:** правка не ставит оператора раньше захвата строки личности в `ForceLogoutWriter` и не меняет порядок «снятие → отсечка → событие» в `commitOwnForceLogout` (`:736` → `:742` → `:745`) | 05/1 (`:448`), 05/4 (`:418` и `:83-86`) — круг 3 воспроизвёл это посевом на новой базе. `TestSessionEndingWritersRevokeWhatTheSessionHolds` |
| **E7** проба | К, С | §2.1 п.3: управляемые часы и порядок без сна | обёртка **встраивает** `humansession.Store`. Методов теперь **7**: `#340` добавил `SessionSetWriter`. Выход зовёт `Writer` (`logout.go:71`), поэтому переопределения `Resolve` и `Writer` хватает; явная реализация порта не собралась бы. Задержка — только после `SessionFound` и снимается каналом. `UpsertCutoff` считается на `Writer` выхода. Моменты сверяются база с базой. События считаются по `session_id`. 05/3: не меньше 20 свежих раундов, печатаются выигрыши **и число отказов** принудительного выхода с шагом отказа (N7: `forceLogoutLockWait = 2s`, `:528`). «Отказов нет» — это счёт, а не молчание. Старт 05/3 сводится на входе `OwnSessionsWriter.EndOtherSessions` через обёртку `OwnSessions.ForceLogoutWriter`, а не на `EndAllSessions`: такого порта нет | сама проба и её инъекции (заказ) |
| **E8** проба дерева | К | §7.3 «Гейт сверяет ИМЯ, а не идентичность» | без изменений: корпус `check.CorpusFrom` + `check.ProductionGoFile` (есть, `tree_corpus.go:84`, `:133`); `strconv.Unquote`; селектор по пути импорта; перепись печатается; пустой обход — `t.Fatalf`. Естественный красный: литерал `:340` при объявленном слове; селектор `domain.RevokeReasonLogout` — `:736` | инъекции в пакете; слепая зона — сложение строк |
| **E9** снятие остатка | С | §4.2 «Порядок снятия ресурса» | предикат §6 п.7 ред. 3 → 0 (сейчас 4: `:704`, `:706`, `:716`, `:718`); контроль → 1 (`:711`). **Сверх предиката (N6 круга 3):** абзац `:704-708` снят целиком. На `:705-706` перечень словаря из трёх значений, и предикат его не знает. Условие: комментарий `force_logout.go` словаря не перечисляет, `git grep -c 'second-factor-removed' -- internal/apps/kaname/api/internal_iam/force_logout.go` → 0 (сейчас 1, `:706`); перечень живёт только в домене. Состав событий `:710-714` остаётся; вывод «обе строки несут `logout` … совпадением моментов» (`:714-716`) заменён | предикат и контроль §6 п.7 — проверка посадки. Перечень в комментарии — **вниманием**, гейта нет: это второе написание перечня (§5.1), и так уже стала ложной строка `:705` |
| **E10** порядок вливания | С | §1.2 «Запасной путь, фабрикующий утверждение» | условный блокер первичного разбора **сработал и снят** по его же правилу. `#340` влит в `358` (`cca75858`) раньше `#334`, приёмка вернулась автору, ред. 3 одобрена кругом 3. **Осталось:** `#334` вливается в `358` раньше, чем `358` уходит в `357` (запрос эпика `#359`, черновик, в `main`). Радиус правки проб — ровно одно утверждение, `force_logout_own_session_test.go:209` (`[]string{domain.RevokeReasonLogout}` → новая константа), тем же изменением | вниманием диспетчера. Проверка — `git merge-base --is-ancestor origin/334 origin/358` перед вливанием `358` в `357` |

Экспозиций без противоядия нет.

---

## Числа, полученные до кода

Ревизия — `kaname@46ccd69c` (код = `cca75858`), если не сказано иное.

| утверждение | как измерено | что оказалось |
|---|---|---|
| координаты ред. 3 | предикаты §0.1, §0.3, §0.5, §0.6 п.4, §6 пп.3, 4, 7 дословно; чтение `:87`, `:342`, `:670-683`, `:745`, `human_session_repo.go:51`, `:90-92`, `:227`, `:677`, `:759` | **всё совпало**: SET `:417`/`:447`; `IS NULL` 418, 448, 635, 659; мест вызова 5 (logout:78, change_password:197, recovery_complete:234, sf_remove:125, force_logout:736); `EndAllSessions` в `*.go` 0; `ended_reason` в не-тестовом Go 4 строки; моменты `:346 · :736 · :742`; п.3 → 1 (`:736`); п.4 → 1 (`:340`); п.7 → 4, контроль → 1 |
| 13 проб, названных приёмкой | `git grep -c "func <имя>("` по каждому имени из документа | **13 из 13** |
| сигнатуры с `reason string` у снятия | `git grep -n -E '(EndSession\|EndOtherSessions\|EndAllSessions)\(ctx context\.Context'` | **5**: `iface.go:142`, `:145`, `force_logout.go:216`, `human_session_repo.go:415`, `:460`. Было 6 |
| методы порта `Store` | чтение `iface.go:96-…` | **7**: добавлен `SessionSetWriter` (`#340`). Выход его не зовёт, зовёт `Writer` (`logout.go:71`) |
| пробы, утверждающие причину снятия у принудительного выхода | `git grep 'RevokeReasonLogout\|ended_reason\|admin-force-logout'` по тестам вне `humansession/`; `git grep 'reasons'` в тестах `internal_iam` | **1 утверждение**, `force_logout_own_session_test.go:209`. Интеграционных проб, читающих `ended_reason` после принудительного выхода, 0. Литерал `admin-force-logout` в тестах — свободная причина отсечки или маркера, правкой не задевается. Пробы уровня репозитория, где `EndOtherSessions` получает `logout`, не краснеют: дверь принимает любое слово словаря |
| миграции | `ls internal/migrations/*.sql`; `git diff --name-only origin/358...origin/<N> -- internal/migrations/` по 314, 321, 357, 365, 377 | файлов 35, старшая `20260920175119`; открытые ветки миграций не добавляют |
| строки ствола и эпика, помеченные неверно | `git grep -c -E '\.(EndSession\|EndOtherSessions)\(' <рев> -- …/force_logout.go` на `cbbac984` и `origin/357`; `merge-base --is-ancestor origin/358 origin/357` | 0 и 0: снятия наших записей принудительным выходом нет ни на стволе, ни в эпике. `358` в `357` не влита (→ 1). Посылка E10 верна на сегодня |
| отказ снятия на новой базе | чтение `force_logout.go:610-640`, `:826-857` | отказ шага снятия **всегда** ведёт ко второй транзакции (отсечка + событие `failed`), ответ `UNAVAILABLE could not end the login session`. Отсюда уточнения E2 и E6 |
| что вливание `#340` не трогало | `git diff --quiet 9dc46b47 cca75858 -- internal/domain internal/migrations internal/check proto cmd/kaname/hooks_mux.go …/humansession/logout.go` | 0 — не трогало. E3 (в), E4, E5, E8 стоят на прежнем тексте |

Числа, не меняющие вывод, не приложены.

---

## Чего измерить нельзя

- **05/3 на новой базе** — свойство прогона. Круг 3 её не перепрогонял (N7), я тоже. Отсюда условие печатать число отказов.
- **Порядок `#334` → `358` → `357`** — будущее событие; сегодня `358` в `357` не влита.
- **«Узел литерала один после кода»** — свойство будущего кода; держится KN-SER-10.

---

## Итог

- **к-замыслу:** все E1…E10 отображены в решения ред. 3, неотображённых нет. Уточнения по существу есть в четырёх пунктах: E2 (`marker.Reason`), E6 (не брать личность), E7-проба (7 методов, счёт отказов, порт старта 05/3), E9 (абзац целиком, перечень в комментарии).
- **к-коду:** 11 условий, построчно проверяемы, координаты на `cca75858`.
- **вернуть-в-приёмку:** пусто.
- **блокер:** нет. Условный блокер первичного разбора сработал и снят ред. 3.

## Вернуть в приёмку

Пусто. Координаты ред. 3 перемерены дословно и верны на `cca75858`. Given, которого схема не
допускает, нет. Круг 3 посеял 3 из 4 «Дано» KN-SER-05 на новой базе, четвёртое — названной
фикстурой.

## Неотображённые пункты (пересверка)

Нет. Отображение пунктов первичного разбора в решения ред. 3:

| пункт | решение ред. 3 |
|---|---|
| E1 | Р1, KN-SER-01, KN-SER-06 |
| E2 | Р2, KN-SER-02 |
| E3 | Р3, §6 п.4, KN-SER-10 |
| E4 | KN-SER-07 |
| E5 | Р4, KN-SER-08 |
| E6 | Р5, KN-SER-09 |
| E7 | KN-SER-05, §5 |
| E8 | KN-SER-10 |
| E9 | §6 п.7 ред. 3 |
| E10 | §6 пп.1, 11; ред. 3 как исполненный возврат |

## Заказы (адресат · предмет · основание)

- **dispatcher** · `#334` вливается в `358` раньше, чем `358` уходит в `357` (запрос `#359`). Сверка — `git merge-base --is-ancestor origin/334 origin/358` · `verdict-and-landing` (параллельные писатели).
- **integration-tester** · KN-SER-05 с уточнениями строки «E7 проба»:
  - обёртка встраивает `Store` из 7 методов;
  - старт 05/3 — на входе `OwnSessionsWriter.EndOtherSessions`;
  - печатать число отказов с шагом;
  - инъекции §6 п.12 на `:418`, `:448`, `logout.go:83-86`.

  Основание: `code-authoring` §2, `testing.md` concurrency-fix-race-deterministic.
- **go-implementer** · правка `force_logout_own_session_test.go:209` тем же изменением. Абзац `force_logout.go:704-708` снять целиком, перечня словаря в комментарии не оставить · `doc-truthfulness`, `code-authoring` §5.1.
- Прочие заказы первичного разбора (KN-SER-01–04, 06–09, KN-SER-10 автору гейта, находка `human_session_schema_integration_test.go:113`) — **без изменений**. Строка `:113` на новой базе та же: `internal/migrations` не менялись.
