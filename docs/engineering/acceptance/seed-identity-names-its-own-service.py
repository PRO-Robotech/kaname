#!/usr/bin/env python3
# Copyright (c) PRO-Robotech
# SPDX-License-Identifier: AGPL-3.0-or-later
"""Перепись посевной идентичности — предикат §0.1 приёмки
`seed-identity-names-its-own-service.md`.

ЭТО ПЕРЕПИСЬ, А НЕ ГЕЙТ. Он ничего не роняет и никого не судит: он печатает
числа, на которые ссылается §0.2, и объём осмотренного, которым «ноль находок»
отличается от «ноль прочитанного». Единственный ненулевой исход — ОТКАЗ по
беспредметности (код 2), когда мерить оказалось нечего.

ЕДИНИЦА СЧЁТА — вхождение литерала посевной идентичности в ОТСЛЕЖИВАЕМОМ файле,
счёт от длинного написания к короткому (`kacho-system.admin` не пересчитывается
вторично как `kacho-system`), с адъюдикацией по КОНТЕКСТУ СТРОКИ.

ПОЧЕМУ ОН ЛЕЖИТ ЗДЕСЬ, А НЕ В `scripts/`. Замер одним фактом: заготовка из
шести литералов, положенная под `scripts/`, двигает ЧУЖУЮ ведомость остатка
(монорепо, полоса «имя объекта»: найдено 638 → 644, файлов 226 → 227, гейт
краснеет); она же под каталогом приёмок не двигает ничего. Различие одно —
путь. Литералы поиска неотличимы от посева по форме, а этот файл несёт их
больше заготовки, то есть сдвиг был бы только крупнее. Под каталогом приёмок
он изъят по приставке (`kanamenameresidue.go`, `kanameApprovedAcceptanceDir`) и
ведомость не двигает. Изъятие здесь ЗАКОННО и по существу, а не по удобству:
дерево монорепо уже признаёт этот довод для проб — «имя названо как предмет
проверки» (`kanamemigratorshowcase.go`). Ведомость чужая (`#2553`, монорепо),
и эта приёмка её не правит.

ГРАНИЦА СУДИТСЯ ПЕРВОЙ И ИЗ ВЁДЕР ИЗЪЯТА — у ТЕКСТА ПРИЁМКИ ДВА ДОМА
(2026-09-16, задача #158).

  дом 1 — `docs/engineering/acceptance/`: сам документ приёмки и предикат рядом
          с ним;
  дом 2 — `docs/specs/reviews/<документ>/<sha256>.yaml`: запись ревью. Она
          НОСИТЕЛЬ того самого отпечатка, к которому привязан вердикт, и
          цитирует сценарии приёмки ДОСЛОВНО — вместе с литералами посевной
          идентичности.

Довод у обоих домов ОДИН и он же довод границы: вердикт приёмки привязан к
отпечатку её текста, поэтому этот текст — предмет проверки, а не значение,
которое дерево пишет или читает. Цитата в записи ревью есть тот же текст, что и
цитируемая строка приёмки; считать первую остатком, а вторую границей значило бы
судить один предмет двумя правилами по признаку каталога.

Прежняя редакция знала ОДИН дом — приставку каталога приёмок, — и цитата из
записи ревью попадала в ведро «живой», то есть в остаток, обязанный дойти до
нуля. Замер одним фактом на merge-ревизии `PRO-Robotech/kaname#105` (в дифф
входят ровно документ приёмки и две записи ревью): перепись даёт живой
110 · 46 ф.; `git rm --cached docs/specs/reviews/<каталог>/*.yaml` возвращает
107 · 45 ф. Различие между прогонами ровно одно.

ГРАНИЦА ИЗЪЯТА ИЗ ВЁДЕР, А НЕ ПЕРЕИМЕНОВАНА В НИХ, и это второй половине того же
замера. Ведро сверяется с объявлением §0 гейтом
(`internal/check/seed_identity_census_test.go`), а величина границы САМОССЫЛОЧНА:
она растёт с каждой правкой любого текста приёмки и с каждой записью ревью — то
есть от работы, к предмету ЭТОЙ приёмки отношения не имеющей. Цена наблюдалась
дважды за одну неделю: `#105` (запись ревью, живой 107 → 110) и `#150` (новый
документ приёмки, граница 109 · 8 ф. → 110 · 9 ф. плюс четырнадцать строк прозы
в §0 ЧУЖОЙ приёмки). Останься граница ведром — перенос цитат туда лишь сдвинул
бы ту же плату с одного ведра на другое. Величина не спрятана: она печатается
переписью рядом с объёмом осмотренного, поэтому «ноль находок» остаётся отличимо
от «ноль прочитанного».

Граница судится ПЕРВОЙ, и это замер, а не вкус: на ревизии замера оба порядка
дают дословно одно (зонт 31·16, граница 40·6), а после посадки приёмки только
этот оставляет ведро зонта неподвижным — её собственная проза о личности зонта
есть текст приёмки, а не остаток имени.

ПОРЯДОК ОСТАЛЬНЫХ ВЁДЕР НЕСУЩИЙ — вхождение попадает в ПЕРВОЕ подошедшее, и
порядок объявлен здесь, потому что он и есть спорная часть:

  1. зонт     — литерал принадлежит личности, которую выдаёт ЗОНТ платформы;
                предмет службы он не называет ни в одном контексте (§0.4);
  2. ns       — строка несёт признак пространства имён кластера; написание
                `kacho-system` бывает и аккаунтом, и пространством имён, и
                РАЗДЕЛЯЕТ их только контекст строки. Предмет `#2553`;
  3. манифест — файл манифеста модуля, строка не комментарий: цитата имени
                аккаунта чужим продуктом — посев, а не проза;
  4. свод     — применённая миграция (`internal/migrations/*.sql`). Прежнее
                написание там ЗАМЁРЗШЕЕ: свод его посеял и правке не подлежит
                (ban #5), а переводящая миграция обязана его назвать, чтобы
                найти строку. Ноль здесь недостижим BY CONSTRUCTION — и
                недостижим в сторону, ОБРАТНУЮ цели: каждый выполненный шаг
                перехода это число УВЕЛИЧИВАЕТ;
  5. живой    — остальное: написание, которое дерево сегодня ПИШЕТ или ЧИТАЕТ
                как значение. Ровно это ведро и обязано дойти до нуля.

ЧЕГО ОН НЕ ДЕЛАЕТ. Он не сводит свою единицу с единицей держателя ведомости
остатка: тот считает вхождения сегмента `kacho` по полосам ПОСЛЕ границ и
после прощения, здесь считаются литералы целиком. Расхождение названо в §0.1
и снимается П4, а не этим файлом.

ВЕНКА ПЕРЕЕХАЛА — И ЭТО СНЯЛО ОДНО ВЕДРО, А НЕ ДОБАВИЛО ЕГО (2026-09-11).

Первая редакция читала перечень `KanameSurface`
(`internal/repohygiene/kanamenameresidue.go`) ИЗ ДЕРЕВА, потому что предикат
писался внутри монорепо, где каталог приёмок (`services/iam/docs/...`) и этот
держатель (`internal/repohygiene/...`) — соседи по одному git-дереву. Служба
выделена в отдельный репозиторий (`PRO-Robotech/kaname`, задача #2554 —
одноимённая с этой приёмкой), и с этой ревизии предикат живёт ТАМ, а держатель
монорепо — нет: обход по прежнему пути отказывал кодом 2 на КАЖДОМ прогоне,
здесь и всегда.

Прежний перечень называл ЧЕТЫРЕ каталога: `deploy/helm/umbrella/charts/kaname`
и `proto/{kaname/cloud/iam,corelib/authz}` — все три вне пределов этого
репозитория (первый принадлежит зонту платформы, два вторых — контракту, чей
дом решается отдельно, вне предмета этой приёмки); `services/iam` — ровно то,
чем стал корень ЭТОГО репозитория. Внутри одного репозитория «путь внутри
поверхности службы» и «путь вне неё, но внутри дерева» — уже НЕ разные
множества: второго множества здесь не существует, а первое — весь
отслеживаемый обход. Поэтому ведро «поверхность» и ведро «прочее» слиты в одно
ведро «предмет»: разделявший их вопрос («это ещё каталог службы, или уже
соседний код монорепо?») здесь не имеет смысла — соседнего кода в этом дереве
нет по построению.

Единственный НАСТОЯЩИЙ пробел, а не переименование удобства: три
внерепозиторных каталога прежнего перечня отсюда НЕ измеримы, и предикат
больше не делает вид, что мерит их. §0 документа обязан называть это прямо, а
не молчать: «манифесты» (цитаты имени пятью ЧУЖИМИ продуктами) — предмет,
измеримый ТОЛЬКО из монорепо, где эти пять `manifest.yaml` лежат; отсюда его
величина навсегда «не измерено», а не «ноль».
"""

from __future__ import annotations

import argparse
import re
import subprocess
import sys

# Литералы, от ДЛИННОГО к короткому. Порядок несущий: он и есть правило
# «длинное написание не пересчитывается вторично как короткое».
#
# `kacho-bootstrap-grant` в перечень НЕ входит намеренно: единственное его
# вхождение — проза соседней приёмки о том, что имя УШЛО. Оно называет
# снятое, а не посеянное.
LITERALS = (
    "kacho-bootstrap-operator",
    "kacho-bootstrap-seeder",
    "kacho-bootstrap-admin",
    "kacho-bootstrap-soc",
    "kacho-system.viewer",
    "kacho-system.admin",
    "kacho-system",
    "kacho-root",
)

# Личности, которые выдаёт ЗОНТ платформы, а не служба (§0.4).
UMBRELLA = frozenset({"kacho-bootstrap-operator", "kacho-bootstrap-seeder"})

# Признаки пространства имён кластера в СТРОКЕ (§0.4, пограничный случай).
NS_MARKS = ("/ns/", "namespace", ".svc")

# ДВА ДОМА ТЕКСТА ПРИЁМКИ — см. шапку. Оба изымаются из вёдер одним доводом.
ACCEPTANCE_DIR = "docs/engineering/acceptance/"
REVIEW_RECORDS_DIR = "docs/specs/reviews/"

# Каталог применённых миграций: прежнее написание здесь ЗАМЁРЗШЕЕ (ban #5).
MIGRATIONS_DIR = "internal/migrations/"

BUCKETS = (
    "ПРЕДМЕТ: живой",
    "ПРЕДМЕТ: свод и миграции",
    "ПРЕДМЕТ: манифесты",
    "остаётся: ns",
    "остаётся: зонт",
)


class Refusal(Exception):
    """Мерить нечего — исход, который не выдаётся ни за зелёное, ни за красное."""


def _git(args: list[str]) -> bytes:
    done = subprocess.run(["git", *args], capture_output=True)
    if done.returncode != 0:
        raise Refusal(f"git {' '.join(args)}: {done.stderr.decode('utf-8', 'replace').strip()}")
    return done.stdout


def tracked(rev: str | None) -> list[str]:
    out = _git(["ls-files", "-z"] if rev is None else ["ls-tree", "-r", "-z", "--name-only", rev])
    return [p for p in out.decode("utf-8", "surrogateescape").split("\0") if p]


def blob(path: str, rev: str | None) -> bytes:
    if rev is None:
        try:
            with open(path, "rb") as fh:
                return fh.read()
        except OSError:
            return b""
    return subprocess.run(["git", "cat-file", "blob", f"{rev}:{path}"], capture_output=True).stdout


def scan_line(line: str) -> list[str]:
    """Непересекающийся проход слева направо: длинное написание съедает короткое."""
    found: list[str] = []
    i = 0
    while i < len(line):
        for lit in LITERALS:
            if line.startswith(lit, i):
                found.append(lit)
                i += len(lit)
                break
        else:
            i += 1
    return found


def is_acceptance_text(path: str) -> bool:
    """Оба дома текста приёмки (см. шапку) — граница, а не остаток.

    Второй дом сужен до `*.yaml`: запись ревью есть ИМЕННО такой файл, и
    приставка каталога без этого прощала бы всякий соседний файл, случайно
    туда положенный, — граница стала бы маской.
    """
    if path.startswith(ACCEPTANCE_DIR):
        return True
    return path.startswith(REVIEW_RECORDS_DIR) and path.endswith(".yaml")


def bucket(literal: str, line: str, path: str) -> str | None:
    """Ведро вхождения; None — граница, изъятая из вёдер."""
    if is_acceptance_text(path):
        return None
    if literal in UMBRELLA:
        return "остаётся: зонт"
    if any(mark in line.lower() for mark in NS_MARKS):
        return "остаётся: ns"
    if path.endswith("manifest.yaml") and not line.lstrip().startswith("#"):
        return "ПРЕДМЕТ: манифесты"
    if path.startswith(MIGRATIONS_DIR) and path.endswith(".sql"):
        return "ПРЕДМЕТ: свод и миграции"
    return "ПРЕДМЕТ: живой"


def main() -> int:
    ap = argparse.ArgumentParser(description="перепись посевной идентичности (предикат §0.1)")
    ap.add_argument("--rev", default=None,
                    help="ревизия замера; без неё — рабочее дерево")
    args = ap.parse_args()

    try:
        files = tracked(args.rev)
    except Refusal as why:
        print(f"ОТКАЗ: {why}", file=sys.stderr)
        return 2

    hits = {b: 0 for b in BUCKETS}
    seen: dict[str, set[str]] = {b: set() for b in BUCKETS}
    # Граница копится ОТДЕЛЬНО от вёдер и печатается переписью: она изъята из
    # сверки с §0, но не из наблюдения.
    edge_hits = 0
    edge_files: set[str] = set()
    read = binary = 0

    for path in files:
        data = blob(path, args.rev)
        if b"\0" in data[:8000]:
            binary += 1
            continue
        read += 1
        if b"kacho-" not in data:
            continue
        for line in data.decode("utf-8", "replace").splitlines():
            if "kacho-" not in line:
                continue
            for lit in scan_line(line):
                b = bucket(lit, line, path)
                if b is None:
                    edge_hits += 1
                    edge_files.add(path)
                    continue
                hits[b] += 1
                seen[b].add(path)

    print(f"ревизия: {args.rev or 'рабочее дерево'}")
    print(f"осмотрено: в индексе {len(files)} · прочитано {read} · двоичных {binary}")
    # Форма строки НАМЕРЕННО не форма ведра: гейт §0 разбирает вёдра по образцу
    # «<имя><два+ пробела><N> · <M> ф.», и граница, напечатанная этой формой,
    # вернулась бы в сверку — то есть вернулась бы и плата, ради снятия которой
    # она изъята.
    print(f"изъято как текст приёмки: {edge_hits} вхождений в {len(edge_files)} ф. "
          f"(два дома: {ACCEPTANCE_DIR} и {REVIEW_RECORDS_DIR}*.yaml)")

    # Предпосылка переписи, проверяемая ею самой: пустой обход — ОТКАЗ, а не
    # тихий ноль. Иначе переезд репозитория сузил бы популяцию молча.
    if read == 0:
        print("ОТКАЗ: обход пуст — вердикт беспредметен", file=sys.stderr)
        return 2

    print()
    for b in BUCKETS:
        print(f"{b:<25} {hits[b]:>4} · {len(seen[b]):>3} ф.")
    print()
    print("манифесты чужих продуктов (5 файлов монорепо) — НЕ измеримо из "
          "этого репозитория; величина навсегда «не измерено», не «ноль»")
    print("предикат перехода (§7-П2) — ПАРА: «живой» = 0 ПРИ «свод и миграции» != 0. "
          "Одна половина без второй ничего не значит: ноль живого бывает и от "
          "ослепшего обхода, а непустой свод доказывает, что написание всё ещё "
          "находится там, где ban #5 держит его вечно")
    return 0


if __name__ == "__main__":
    sys.exit(main())
