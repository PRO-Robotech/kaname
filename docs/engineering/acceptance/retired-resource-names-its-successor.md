<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка: снятый ресурс НАЗЫВАЕТ преемника — одним чтением каталога

- **Статус:** DRAFT — **круг 3**, ждёт `acceptance-reviewer`. Кода по §3 не писать
  до вердикта (ban #1). Автор вердикта себе не ставит. Круг 1 — два блокирующих
  (чёрный ящик и механизм изоляции снимка), круг 2 — одно (точек чтения снимка две,
  названа одна). Цикл сходится: **2 → 1 → 0 ожидается**; что сделано по каждому
  кругу — §A
- **Ревизия измерения:** `61053691` — **база линии**, то есть ствол службы, от
  которого ветвится это изменение; на день записи она же и вершина `origin/main`.
  Все числа §0 сняты на ней. **Коммит полосы здесь НЕ называется, и это решение, а
  не удобство:** работа едет в ствол схлопыванием, исходный коммит предком ствола
  не становится НИКОГДА, и гейт датировки (`internal/check`,
  `TestMeasurementRevisionsAreDatedByHashAndBelongToThisHistory`) судит вхождение
  относительно `origin/main`, а не рабочей вершины. Ревизия названа **одним
  местом**: вторая, поставленная прозой в другом разделе, уже стоила этой линии
  находки
- **Задача:** `PRO-Robotech/kacho#1814` (`P2`, `size:M`, `area:iam`, `home:kaname`,
  `release:iam-closeout`) — метки сняты `gh issue view`
- **Эпик:** `PRO-Robotech/kacho#1027` — каталог модуля как данные. Предшественники:
  `#1030` (`rule-segments-have-a-referent.md`, которым эта задача и заведена),
  `#1976` (витрина читает живые строки), `#1816`
  (`catalog-readers-move-to-the-table.md`), `#1994`/`#1816` (превью роли)
- **Префикс сценариев:** `IAM-SUC`. Свободен — предикат `git grep -c 'IAM-SUC'` по
  дереву даёт пусто; занятые соседи iam — `IAM-CT`, `IAM-CT-2`, `IAM-ID`, `IAM-RV`,
  `IAM-MV`, `IAM-SV`, `IAM-RM`
- **Тип изменения:** ЗАВОДЯЩЕЕ **плюс** расширение публичного контракта. Заводится
  вторая полоса чтения витрины — **снятая**; живая половина не трогается ни одним
  полем и ни одним сценарием (§2.1). Сверх того — осознанная правка **текста
  отказа** (§2.5), и она названа отдельно, потому что тон сообщений есть часть
  контракта (`api-conventions.md` §Error-format)
- **Сервис:** `kaname`, предмет целиком внутри него — документ живёт рядом с кодом
- **`proto/` ЗАТРАГИВАЕТСЯ** — новые поля публичного сообщения витрины. Ломающим
  изменением это не является, и §2.2 называет, чем это доказано
- **Миграции:** **НЕТ**. Данные уже посеяны и уже проверяются: три снятых ресурса
  с преемником стоят в `internal/migrations/0001_initial.sql`, преемник у каждого
  обязан быть ЖИВЫМ ключом каталога, и это требование держит гейт (§0.6). Записи в
  ведомость приёмок не заводится — её в этом дереве нет вовсе (§0.7)

---

## A. Ответ автора — круги 2 и 3

| круг | блокирующих | не блокирующих | где ответ |
|---|---:|---:|---|
| 1 | **2** — чёрный ящик, механизм изоляции снимка | 6 | §A.1, §A.2, §A.4 |
| 2 | **1** — точек чтения снимка две, названа одна | 0 | §A.3 |

**Цикл сходится: 2 → 1.** Каждый круг закрывался **внутри документа**, чужих задач
не ждал. **Каждое замечание перед исполнением перемерено своим предикатом**, и это
не формальность: в круге 1 рецензент оказался прав в обоих блокирующих и в пяти
не блокирующих из шести (шестое снято как чужой предмет, §6 п. 9); в круге 2 —
прав в блокирующем и **сам назвал свою постановку неполной**, а я, исполнив её
дословно, унаследовал неполноту (§A.3).

**Урок круга 3, который стоит назвать отдельно:** дважды подряд неверной оказывалась
не норма, а **граница замера** — сперва «производитель есть» без вопроса, на каком
слое он наблюдается (Б1), затем «точка чтения одна» без вопроса, сколько их (B3).
Оба раза замер был верен в своих границах и ложен за ними. Отсюда форма, которой
круг 3 закрывает B3: §3 п. 3 называет точки **таблицей** — перечень, который видно
целиком, а не строкой прозы, где отсутствие второй позиции незаметно.

### A.1. Б1 — ни один сценарий не проверялся через КРАЙ

**Подтверждено моим замером.** Предикат `grep -cniE 'newman|e2e|сквозн|чёрн[оы]'` по
круга 1 давал **0**: чёрного ящика не было ни в одном сценарии, ни в DoD, и граница
«его здесь нет» не была названа — то есть это был **пропуск**, а не решение.

«Здесь не запустить» мне недоступно, и я это перемерил: кейсов в наборе службы —
**41 файл**, у этого RPC **выделенный** набор `iam-permission-catalog.py` с двумя
кейсами, назван прогонщиком (`run.sh:420`), объявлен в `CASES-INDEX.md:56`, гоняется
`e2e-newman.yml`.

**Сделано:** заведён сценарий `IAM-SUC-10` — один вызов через край, утверждается
**пара** (HTTP-статус и содержимое поля в camelCase), обе половины в **одном теле**;
§5 получил колонку **«слой»** (`код` / `край`) и пять строк края; DoD п. 5 требует
теперь не регенерации, а **зелёного `IAM-SUC-10` на поднятом стенде**; DoD п. 6
требует кейса, заведённого **полностью** — объявление, индекс, прогонщик, процесс, —
иначе он исполняется и никого не роняет.

### A.2. Б2 — названный производитель требуемого свойства не производит

**Подтверждено моим замером, и он строже, чем в замечании.** `readCatalogHalf` шлёт
**три** оператора (`catalog_repo.go:155,164,176`), `CatalogRepo` держит **пул**
(`:46–48`); две половины = шесть операторов = шесть снимков. Godoc `Rows:93–99`, на
который я опёрся кругом 1, говорит о **форме** порта, об изоляции — ничего.

**Довод круга 1 был не только слабым, он был НЕПОЛНЫМ.** Замечание предлагало два
механизма на выбор — фиксированный порядок либо общую транзакцию. Замер показывает,
что **выбора нет**: порядок аномалию не убирает, а меняет её вид, и каждый вид
запрещён своим сценарием этого же документа (§3 п. 3, таблица). Живая-затем-снятая
даёт **дубль** (запрещает `IAM-SUC-01`), снятая-затем-живая — **пропажу** (запрещает
`IAM-SUC-09`). Исполним ровно один сценарий из двух при любом порядке.

**Сделано:** §3 п. 3 называет **один** механизм — обе половины под одной
транзакцией, — с образцом в дереве (`catalog_writer.go:270–279`, его godoc называет
предмет дословно) и точкой чтения (`snapshot.go:97`); `IAM-SUC-09` перенёс механизм
в своё «Тогда» **проверяемой** формулировкой (операторов вне общей транзакции —
ноль) и называет **оба** запрещённых состояния; §5 больше не ссылается на
`rows.go:93–99` как на держателя и не несёт гибридного состояния.

### A.3. B3 круга 2 — точек чтения снимка ДВЕ, названа была одна

**Подтверждено моим замером по каждой координате.** Снимок строится
`NewSnapshot(catalogCensus.Live, catalogRepo, …)` (`serve.go:395`), строки приходят
**параметром** (`snapshot.go:72`), ценз отдаёт наружу только `Live catalog.Rows`
(`catalog_parity.go:246`), а от снятой половины оставляет **числа**
(`:229–231`) — при том что читает её **всегда** (`:337`). Круги 1 и 2 называли
одну точку (`Refresh`) и тем оставляли путь старта без производителя.

Все три следствия рецензента верны: `IAM-SUC-02` не имел производителя на старте;
проверяемая форма `IAM-SUC-09` на этом пути **вакуумна** (ноль операторов истинен
уже сегодня — конструктор не шлёт их вовсе); живой держатель `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog` попадал
под удар и не был назван.

**Сделано:** §3 п. 3 называет **обе** точки таблицей и что делает каждая; механизм
старта — **донос уже прочитанного**, а не второе чтение; `IAM-SUC-09` покрывает оба
пути и рядом с **каждым** нулём требует **непустой** снятой половины (иначе счётчик
вакуумен); заведён `IAM-SUC-11` — необновлённый процесс уже называет преемника; §8
получил **три** строки риска, включая `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog` **по имени**; DoD п. 8 требует
его зелёным и прямо запрещает зачитывать `SKIP`.

**Оценку «поле в цензе плюс параметр конструктора» ПЕРЕМЕРИЛ — подтверждается, и
теперь с числом.** Прогнал живого держателя на поднятой базе (рецензент этого не
делал и сказал об этом прямо):

```
TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog
операторов к таблицам каталога: у стража отдельно K=6, за путь старта со снимком N=6
--- PASS (2.69s)   RC=0
```

`K=6` раскладывается как **3 + 3**: страж зовёт обе половины, каждая стоит трёх
операторов. **Снятые строки уже входят в эти шесть** — значит донести их в снимок
стоит **нуля** новых обращений, и `N == K` держится by construction, а не
аккуратностью. Это сильнее, чем оценка «дёшево»: цена измерена и равна нулю.

**Вторая своя ошибка круга 3, найденная до отправки:** пять раз я назвал держателя
усечённо — `TestIAMCT2_01` вместо полного
`TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog`. Координатный гейт на это
**молчит by construction**: он резолвит по ПРЕФИКСУ
(`check.ProbeCoordinateResolves` — `strings.HasPrefix(declared[i], name)`), и
усечённое имя проходит. А `git grep "func TestIAMCT2_01("` даёт **ноль** — то есть
читатель, пошедший за координатой, не находит ничего и делает вывод о дереве.
Тот же класс, что кириллическая буква в идентификаторе круга 2: имя, которое
грепается в ноль **молча**. Исправлено во всех пяти местах; предикат для следующего:
имя пробы из документа обязано давать **непустой** `git grep "func <имя>("`, а не
просто проходить гейт.

**Радиус правки назван честно, потому что «дёшево» ≠ «в одном месте»:** смена
сигнатуры `NewSnapshot` трогает **1** прод-вызов (`serve.go:395`) и **24** вызова в
**12** файлах проб. Предикаты: `git grep -c 'NewSnapshot(' -- '*.go' ':!*_test.go'
':!internal/catalog/snapshot.go'` и то же по `'*_test.go'`. Это не дорого, но и не
одно место, и планировать надо на 25 точек.

### A.4. Не блокирующие круга 1 — что закрыто и чем

| # | замечание | исход |
|---|---|---|
| 1 | §0.7, число «8» не даёт ни одна единица | **закрыто**: на базе линии строк **9**, файлов **3**; сверх того предикат был **самоссылочным** — без явной ревизии он считает этот же документ (сегодня 14/4). Ревизия внесена **в предикат** |
| 2 | §5, итог не сходится с таблицей | **закрыто**: гибрид разведён на две строки, итог пересчитан **по таблице** — строк **26**, жив **17**, заводится **9**; единица счёта названа (строка данных = одно «Тогда»); DoD п. 2 берёт число оттуда же |
| 3 | DoD п. 6 требует того, чего не просит «Когда» | **закрыто**: `IAM-SUC-04` называет **оба** глагола, и сказано почему — у правки свой порядок гейтов (`update.go:182` против `:191`) |
| 4 | `IAM-SUC-05а` несёт кириллическую `а` | **закрыто**: три вхождения переведены на латинскую. Форма суффикса конвенциональна — в корпусе она уже есть (`MOD-RL-04a` и соседи, латиница) |
| 5 | §2.2 перечисляет только Go-пробы | **закрыто**: перечень разведён по **обоим** слоям таблицей; вывод не менялся — кейс края утверждает членством (`.to.include` / `.to.have.property`), равенства множества ключей нет ни на одном слое |
| 6 | шапка кейса набора пережила `#1976` | **не чинится здесь осознанно**: чужой предмет, `multi-agent-flow.md` §«Находка по дороге». Назван §6 п. 8 с предикатом снятия |

### A.5. Что круги 2 и 3 НЕ трогали

Ни одно опровержение посылок (§0.1–§0.4), ни одно решение (§2), ни один
производитель §5 со состоянием **жив**, ни одна координата, стоявшая с круга 1.
Рецензент проверил их своими предикатами и инъекцией.

- **круг 2:** §0.7 (число и предикат), §2.2 (перечень слоёв), §3 п. 3,
  `IAM-SUC-04`, `IAM-SUC-09`, `IAM-SUC-05a`, новый `IAM-SUC-10`, §5, §6, DoD;
- **круг 3:** §3 п. 3 (вторая точка чтения и измеренный ноль), `IAM-SUC-09`
  (оба пути, контроль на содержимом), новый `IAM-SUC-11`, §5 (семь строк), §8
  (три строки риска), §6 п. 8 (находка круга 3), DoD пп. 2 и 8.

**Решение §2.1 (отдельный перечень) кругами 2 и 3 не пересматривалось** и от
находки о путях чтения не зависит: она про то, **чем наполняется** снятая половина,
а не про то, **каким полем** она едет клиенту.

**Своя ошибка круга 2, названная до вердикта:** первая редакция §3 п. 3 и §A.2
называла операторы чтения строками `152,161,173`. Это **смещения внутри среза**
`sed -n '150,200p'`, принятые за абсолютные номера: 152 — объявление самой функции,
161 и 173 — возвраты ошибки. Верные координаты `155,164,176`, и число операторов
(**три**) от описки не зависело. Класс — `testing-verdict.md` §8: окно с посчитанным
на глаз смещением; поймано сверкой каждой названной координаты построчно, а не
перечитыванием.

**Две ошибки предикатов рецензент назвал сам** (обрезанный `grep -A 40` дал «1..5»
как «1..4»; поиск третьего вызывающего по имени не видел прямого
`readCatalogHalf(ctx, w.tx, retiredCatalogHalf)` в `catalog_writer.go:275`) — в обоих
случаях верным было число круга 1, и оно **оставлено как было**.

---

## 0. Замеры посылок — что перемерено и что НЕ сошлось

Тело задачи `#1814` называет **три** половины предмета. Перемерены все три.
**Две с половиной опровергнуты**, и опровержения ниже сняты не чтением тела задачи,
а собственными предикатами на названной ревизии. Сверх того найдена **четвёртая**
неверная посылка, о которой задача не знала и которая делает её собственный
предикат неисполнимым дословно (§0.4).

**Единица счёта названа у каждого числа.** Там, где мой предикат разошёлся с ранее
названным, расхождение разобрано УТОЧНЕНИЕМ ЕДИНИЦЫ, а не выбором правдоподобного.

### 0.1. Половина 1 — «у `superseded_by` нет читателя в прод-коде (0)». ОПРОВЕРГНУТА, и число зависит от единицы

```sh
# единица: ОТСЛЕЖИВАЕМЫЙ файл .go вне проб, упоминающий колонку ЛИБО поле Go
git grep -l 'superseded_by\|SupersededBy' -- '*.go' ':!*_test.go' | wc -l   # → 6
git grep -n 'superseded_by\|SupersededBy' -- '*.go' ':!*_test.go' | wc -l   # → 13 (строк)
```

Шесть файлов, тринадцать строк. Тело задачи называло **0**, разбор задачи от
2026-09-05 — **4**, поручение этой полосы — **5**. Ни одно из трёх не ошибка: они
считали **разное**, и разрешается это единицей, а не выбором.

| источник | что считал | почему меньше моего |
|---|---|---|
| тело задачи (**0**) | файлы по путям `services/iam/**`, `gateway/**` | таких путей в дереве службы нет — служба вынесена, предикат адресует чужую раскладку |
| разбор 09-05 (**4**) | те же пути на дереве монорепо | замер снят до разъезда и до появления зеркала ресурсов |
| поручение (**5**) | `superseded_by` — только **змеиное** написание | не видит файла, где есть только поле Go `SupersededBy` |
| **этот документ (6)** | оба написания, любые пути дерева службы | — |

**Расхождение с «5» — ровно тот класс, что `testing-verdict.md` §14: свой
распознаватель тоже молчит.** Шестой файл —
`internal/repo/kaname/pg/resource_mirror/divergence_census.go` — колонку в SQL не
пишет, а читает уже прочитанное поле `SupersededBy`; предикат на одном написании
обошёл его молча, не красным и не зелёным.

**Адъюдикация всех шести — ни один не на пути чтения арендатора:**

| файл | что делает с преемником |
|---|---|
| `internal/apps/kaname/modulecatalog/confirm.go:25` | **комментарий**: что НЕ входит в подтверждение |
| `internal/repo/kaname/pg/catalog_writer.go:80,298,313` | комментарий + `SET … superseded_by = NULL` при **оживлении** строки |
| `internal/repo/kaname/pg/relverdict/catalogtype.go:108` | **комментарий** о форме переименования |
| `internal/check/catalog_seed_parity.go:400` | **внутренняя сверка посева** (гейт, §0.6) |
| `internal/repo/kaname/pg/resource_mirror/divergence.go:75–76,94,98,128` | читает колонку и несёт `SupersededBy` в **расхождении зеркала** |
| `internal/repo/kaname/pg/resource_mirror/divergence_census.go:62` | печатает его в **переписи** оператору |

Пятый и шестой — единственные, кто преемника реально ЧИТАЕТ и ПОКАЗЫВАЕТ; оба
показывают его **оператору**, разбирающему расхождение зеркала, а не арендатору, и
ни один не достижим ни одним RPC. **Предмет задачи жив; устарел только её
предикат** — он считает файлы, а не путь чтения.

### 0.2. Половина 2 — «`CatalogResource` строится из литерала `authzmap.objectTypes`». ОПРОВЕРГНУТА, подтверждаю

```sh
grep -c 'authzmap'      internal/apps/kaname/api/permission_catalog/list_catalog.go   # → 1
grep -c 'catalogSource' internal/apps/kaname/api/permission_catalog/list_catalog.go   # → 6
```

Единственное вхождение `authzmap` — **проза** (`list_catalog.go:14`), объясняющая
снятую редакцию. Витрина спрашивает `catalog.Source` и строит ответ из
`Facts()` — живых строк (`list_catalog.go:116–122,138,145`). Закрыто `#1976`.

### 0.3. Половина 3 — «превью роли берёт ВСЕ глаголы платформы на нерезолвящемся правиле». ОПРОВЕРГНУТА, подтверждаю

Запасной словарь даётся **подстановке**, а не всякому промаху:
`internal/domain/role_effective_verbs.go:67` (`WithCommonFallback`), и её godoc
называет `kacho#1814` дословно, разделом «Запасной словарь даётся ПОДСТАНОВКЕ, а не
всякому промаху». Промах **названной** пары остаётся промахом.

```sh
grep -c 'kacho#1814' internal/domain/role_effective_verbs.go   # → 1
```

### 0.4. ЧЕТВЁРТАЯ посылка, о которой задача не знала: код отказа на трёх снятых типах — `INVALID_ARGUMENT`, а не `FAILED_PRECONDITION`

Предикат снятия задачи говорит дословно: «клиент, получивший `FAILED_PRECONDITION`
на `compute.disk`». **На трёх посеянных снятых типах такого отказа не бывает** —
раньше ключа срабатывает доменный гейт снятия, и он валидационный:

| полоса | где | текст | код |
|---|---|---|---|
| **домен** (три типа из `domain.retiredTypes`) | `internal/domain/rule.go:186–188` → `internal/apps/kaname/shared/errors.go` `MapValidationErr` | `type compute.disk is retired (no resource of this type exists)` | **`INVALID_ARGUMENT`** |
| **грантуемость** (пара вне живого каталога) | `internal/apps/kaname/api/role/rules_catalog.go:163–165` | `Illegal argument resources (unknown type '…'; grantable types are published by GET /iam/v1/permissionCatalog)` | **`INVALID_ARGUMENT`** |
| **ключ** (строка снята применением в рантайме) | `internal/repo/kaname/pg/pgmaperr.go:465` → `internal/errors/errors.go:83` (`ErrReferenceMissing` обёрнут `ErrFailedPrecondition`) | `resources: compute.disk is not a live platform resource` | **`FAILED_PRECONDITION`** |

Порядок держится построением и проверяем: `create.go:137` (домен) стоит раньше
`create.go:154` (грантуемость); в `update.go` — `:182` раньше `:191`. Ключ до трёх
посеянных типов не доходит **никогда**, потому что доменный гейт короткозамыкает.

**Почему это важно для приёмки, а не мелочь.** Сценарий, записанный дословно по
телу задачи («Когда клиент получает `FAILED_PRECONDITION` на `compute.disk`»), не
имеет производителя ни в одной строке дерева — он утверждает исход, которого на этом
входе не бывает. Приёмка обязана назвать **обе** полосы (§4, `IAM-SUC-04` и
`IAM-SUC-05`), и обе вправе рассчитывать на одно и то же чтение каталога.

### 0.5. Предикат снятия ПРОТИВОРЕЧИТ посаженному решению `#1976` — и это разрешается ФОРМОЙ, а не отзывом

Предикат говорит: «снятый ресурс **виден в ответе каталога** вместе с преемником».
`#1976` закрыл ровно это в противоположную сторону и закрыл **осознанно**: витрина
не называет того, на что ключ выдачу не примет. Свойство держит проба:

```sh
grep -n 'func TestCatalogOffersNoRetiredType' \
  internal/apps/kaname/api/permission_catalog/offers_only_live_types_test.go   # → :67
```

Она берёт `resp.GetModules()[].GetResources()[]` и требует **пустого** пересечения
с `domain.RetiredTypes()`. Положить снятый ресурс в тот же перечень — значит
покрасить её, то есть **отозвать чужую посадку**.

**Противоречие снимается тем, что «ответ каталога» и «перечень грантуемого» — не
одно и то же.** Ответ — это `ListPermissionCatalogResponse` целиком; перечень
грантуемого — его поле `modules`. Предикат задачи требует первого, `#1976` защищает
второе, и **оба исполнимы одновременно**, если снятое едет **своим** полем (§2.1).
Отзывать `#1976` не требуется, и эта приёмка его не отзывает.

### 0.6. Где цепочка обрывается — ровно ОДНО место, и оно названо

Производители данных **живы и проверяются**; не хватает ровно двух звеньев переноса.

| звено | состояние | координата |
|---|---|---|
| колонка `superseded_by` + `CHECK` «преемник только у снятой» | **есть** | `internal/migrations/0001_initial.sql:1768,1782` |
| посев: 30 строк ресурса, из них **3 снятых**, у каждой преемник | **есть** | `0001_initial.sql` — `INSERT INTO kaname.catalog_resource` |
| гейт: каждый снятый обязан нести преемника, преемник обязан быть ЖИВЫМ | **есть** | `internal/check/catalog_seed_parity.go:390,399–407` |
| читатель снятой половины из базы (`ReadRetiredCatalog`) | **есть** | `internal/repo/kaname/pg/catalog_repo.go:76`, `retiredCatalogHalf` — `:130–137` |
| **`catalog.ResourceRow` несёт преемника** | **НЕТ** | `internal/catalog/rows.go:53–73` — поля `Module`, `Resource`, `ObjectType`, и всё |
| **`CatalogResource` / ответ витрины несут снятое** | **НЕТ** | `proto/kaname/cloud/iam/v1/permission_catalog_service.proto` |

```sh
# сколько снятых ресурсов посеяно и все ли с преемником — вердиктом гейта
go test -count=1 -short -v ./internal/check/ -run CatalogSeedMatchesTheLiteral
# → осмотрено: … прочитано строк — … ресурса 30 …; классифицировано — …
#   живых ресурсов 27, снятых ресурсов 3, пообъектных глаголов 109

# контракт витрины о снятии и преемнике не говорит ВООБЩЕ
grep -c 'superseded\|retired' proto/kaname/cloud/iam/v1/permission_catalog_service.proto   # → 0
```

**Кто зовёт снятую половину сегодня — трое, и ни один не арендаторский:**
`internal/apps/kaname/api/module/plan.go:240` (план применения каталога),
`internal/apps/kaname/seed/catalog_parity.go:337` (страж старта),
`internal/repo/kaname/pg/catalog_writer.go:275` (применитель, в своей транзакции).
Их общий вопрос сформулирован в godoc самого метода (`catalog_repo.go:53–75`):
«строка снята РЕШЕНИЕМ или НЕ ДОЕХАЛА вовсе». Вопрос арендатора — другой («что
назвать вместо»), и четвёртого вызывающего у метода нет.

### 0.7. Смежное из тела задачи: `workspace_revision` в ЭТОМ дереве НЕ СУЩЕСТВУЕТ — решение принято и названо

Задача предлагает решить поле `workspace_revision` в `docs/acceptance-ledger.yaml`
«тем же изменением либо своей задачей». **Тем же изменением это неисполнимо**:

```sh
# ведомости в дереве службы НЕТ ВОВСЕ — это и есть предмет
git ls-files | grep -c 'acceptance-ledger'                      # → 0

# упоминания поля — ПРОЗА приёмок о нём, а не само поле.
# Ревизия названа ЯВНО, и документ исключён из собственного предиката:
git grep -n 'workspace_revision' 61053691 -- docs/ | wc -l      # → 9   (строк)
git grep -l 'workspace_revision' 61053691 -- docs/ | wc -l      # → 3   (файлов)
```

**Две поправки круга 2, обе мои:**

- **число «8» не давала ни одна единица счёта.** На базе линии: строк **9**, файлов
  **3**, вхождений **9**, строк вывода `git grep -c` — **3**. Существо было верным,
  число — нет, и предикат стоял рядом напечатанным: следующий не отличил бы движение
  дерева от описки;
- **предикат был САМОССЫЛОЧНЫМ.** Без явной ревизии он читает рабочее дерево и
  считает **этот самый документ** (сегодня он даёт 14 строк в 4 файлах — пять
  вхождений принадлежат его §0.7). Проверка, считающая собственное объяснение, —
  ровно класс `testing.md` §«Гейт на класс», п. 4, применённый не к гейту, а к
  автору. Отсюда ревизия в предикате, а не после него.

Ведомость живёт в дереве **продукта** — `PRO-Robotech/kacho:docs/acceptance-ledger.yaml`
(поле встречается там 6 раз), и её гейт (`internal/repohygiene`) судит то дерево.
Изменение в `PRO-Robotech/kaname` до неё не дотягивается ни одним путём, а
скопировать файл сюда запрещено прямо (`polyrepo.md` §«Копия между репозиториями
ЗАПРЕЩЕНА»).

**РЕШЕНИЕ: своей задачей в `PRO-Robotech/kacho`, не этим изменением.** Заводится
условием готовности (§7 п. 8) — до слияния линии, а не «когда-нибудь». Три места
корпуса службы, называющие эту трудность прозой
(`catalog-readers-move-to-the-table.md`, `rule-segments-have-a-referent.md`,
`system-role-segments-resolve.md`), правятся **той** задачей, а не этой: иначе
получится два места об одном предмете в двух репозиториях.

---

## 1. Предмет

**Признак.** Преемник снятого ресурса существует ДАННЫМИ, проверяется гейтом и не
доезжает до арендатора ни одним путём чтения (§0.1, §0.6).

**Предмет.** Отказ приходит из ПРЕРВАННОЙ транзакции — там чтения нет by
construction, — поэтому назвать преемника текстом отказа нельзя, и предшествующая
приёмка это обещание сняла как неисполнимое. Восстановить следующий шаг клиента
обязан **путь чтения**: он дёшев, происходит один раз и отвечает ровно на возникший
вопрос — «что назвать вместо». Сегодня следующий шаг восстановлен **частично**:
отказ называет ТИП и говорит, что он снят; витрина показывает ЖИВОЕ. Между этими
двумя половинами преемника нет.

**Предикат снятия.** Клиент, чьё правило отвергнуто на `compute.disk`, узнаёт
`storage.volumes` **одним** вызовом витрины — и это чтение он делает **и без нашей
подсказки**, потому что вторая полоса отказа на него уже указывает
(`rules_catalog.go:92`, `GET /iam/v1/permissionCatalog`).

### 1.1. Чего это изменение НЕ добьётся — сказано первым

- **отказ преемника по-прежнему не назовёт.** Транзакция прервана, чтения в ней
  нет; приёмка `#1030` это уже решила, и решение не пересматривается. Отказ
  научится называть, ГДЕ смотреть (§2.5), — это другое утверждение;
- **снятый ресурс не станет грантуемым.** Ни один сценарий не ослабляет ни
  доменного гейта, ни ключа: витрина рассказывает о снятии, а не разрешает его;
- **разбивки превью роли по ресурсам не будет.** Её граница названа тем же `#1814`
  и остаётся вне области (§6);
- **правило с ЯВНЫМ глаголом на снятом типе** по-прежнему не даст по нему ничего —
  и по-прежнему будет отвергнуто синхронно.

---

## 2. Решения — и признак, по которому каждое отвергается

### 2.1. ФОРМА НА ПРОВОДЕ: отдельный перечень. Общий перечень с признаком — ОТВЕРГНУТ

**РЕШЕНО:** снятые ресурсы едут **отдельным полем верхнего уровня** ответа витрины,
а не внутри `modules[].resources[]`. Поле плоское — перечень записей, каждая несёт
точечное имя снятого и точечное имя преемника.

**Отвергнутый исход — те же `CatalogResource` с признаком снятия.** Цена названа
тремя доводами, и первый самодостаточен:

| довод | цена |
|---|---|
| **новое поле не учит СТАРОГО клиента фильтровать** | признак снятия — поле, добавленное сегодня. Клиент, написанный вчера, его не читает **by construction** и увидит снятый ресурс в перечне грантуемого как живой. То есть исход, заявленный как «безопасный, потому что признак есть», безопасен ровно для тех, кто уже обновился |
| **редактор ролей предложит выдать то, что ключ отвергнет** | `modules[].resources[]` — источник выпадающего списка редактора (`CatalogResource.verbs`, шапка поля в контракте называет это дословно). Снятый ресурс там даёт форму, которая ничего не значит: правило авторуется, отвергается доменным гейтом либо ключом |
| **отзывает посаженное решение** | `TestCatalogOffersNoRetiredType` (`offers_only_live_types_test.go:67`) краснеет с именем типа. Это не «поправить пробу»: проба **держит свойство от возврата**, и её шапка это объявляет |

**Цена ВЫБРАННОГО исхода названа тоже, а не умолчана:**

- у ресурса появляется **два места**, где он может быть назван, и читатель обязан
  знать, что второе — не грантуемое. Закрывается тем, что поле лежит на **верхнем
  уровне** ответа, а не рядом с модулями: спутать перечень модулей с перечнем
  снятого нельзя, не прочитав имени поля;
- **`closed_verbs` не меняется** и меняться не должен: он пересечение наборов ЖИВЫХ
  типов, снятые в него не входили и не входят;
- запись снятого **не несёт глаголов** — намеренно. Глаголы у снятого типа спросить
  не у кого: строка `catalog_verb` снята вместе с ресурсом, а выдавать глаголы
  преемника под именем снятого значило бы утверждать, что наборы совпадают, чего
  никто не проверял.

### 2.2. Расширение публичного сообщения — НЕ ломающее, и вот чем это доказано

Поля **добавляются**; ни одно не снимается, ни одно не меняет номера и типа.
Свободные номера: у `ListPermissionCatalogResponse` заняты `1..3`
(`modules`, `closed_verbs`, `wildcard_policy`); у `CatalogResource` — `1..5`.

Гейт существует и **исполняется**, а не объявлен:

```sh
grep -n 'breaking' -A 4 proto/buf.yaml          # → use: FILE ; ignore: google
grep -n 'buf breaking' .github/workflows/ci.yml # → :288 (шаг конвейера, с адъюдикацией)
```

Набор `FILE` — самый строгий из стандартных и запрещает СНЯТИЕ и ПЕРЕНОС
(поля, сообщения, файла); добавление поля с новым номером им не ловится ни в одной
категории. **Предикат проверки — прогон, а не это утверждение:** §7 п. 4 требует
приложить вывод `buf breaking` против ствола, а не сослаться на эту строку.

**Обратная совместимость на проводе — свойство формата, а не обещание.** Клиент, не
знающий поля, получает тот же JSON плюс один незнакомый ключ; клиент gRPC —
неизвестное поле в `unknown fields`.

**Проверено, что ни один существующий читатель не сверяет ПОЛНОЕ множество ключей —
по обоим слоям, а не только по Go** (круг 1 перечислял лишь Go-пробы и тем делал
заключение шире осмотренного):

| слой | что читает ответ | форма утверждения |
|---|---|---|
| код | `usecase_test.go`, `shows_the_live_catalog_test.go`, `registry_catalog_test.go` | состав `modules`, `closed_verbs`, `wildcard_policy` **поимённо** |
| край | `tests/newman/cases/iam-permission-catalog.py` | `.to.include` / `.to.have.property` — членство, а не равенство множеств |

Ни одна из двух форм на новом ключе не краснеет. Сверка **равенства** множества
ключей нашлась бы предикатом по признаку точного сравнения — её нет ни на одном слое.

### 2.3. Поверхность: что видит арендатор, у которого прав нет

**Витрина — этаж аутентификации и по арендатору НЕ сужается**, и это не упущение, а
объявленное решение:

```sh
grep -n 'ListPermissionCatalog' -A 8 internal/apps/kaname/seed/embedded/permission_catalog.json
# → "required_relation": "viewer", "scope_extractor": {"object_type":"cluster","from_request_field":"*"}
```

Отношение `viewer` кластерного синглтона выполняется системной выдачей с
подстановочным субъектом — значит читает **всякий аутентифицированный**, а
анонимный отвергается fail-closed (`usecase_test.go:331`,
`TestListPermissionCatalog_AnonymousFailClosed`). Это ровно тот случай, который
`security-hardening.md` §«Отношение, выполнимое подстановочным знаком» называет
законным: глобальный справочник, который каждый арендатор обязан читать, чтобы
вообще пользоваться платформой.

**Добавляет ли перечень снятого поверхность — отвечаю прямо, а не подразумеваю.**

| ось | ответ |
|---|---|
| **чьи это данные** | платформенные, не арендаторские: те же три строки у всех, посеяны миграцией, от содержимого проекта не зависят |
| **инфра-чувствительны ли** | нет. `security.md` §«Инфра-чувствительные данные» перечисляет размещение, подлежащий транспорт, обвязку и числовой инфра-идентификатор; точечное имя типа не из этого перечня — оно того же рода, что уже публикуемые живые пары |
| **даёт ли оракул существования** | нет: ответ не зависит ни от вызывающего, ни от его выдач — он побайтово один и тот же для каждого аутентифицированного |
| **расширяется ли множество читателей** | нет: тот же RPC, тот же этаж, тот же слушатель; нового глагола не заводится |
| **что меняется** | арендатор узнаёт, что платформа **когда-то** знала `compute.disk` и больше не знает. Это факт о нашей истории, а не о чужом арендаторе |

**Остаток назван честно:** перечень снятого сообщает посторонним, что блочное
хранение переезжало из одного домена в другой. Мы считаем это приемлемым — обе
стороны переезда уже публичны (живые `storage.*` в той же витрине, репозиторий
открыт), — но **скрытым** этот размен не оставляем: он решён, а не пропущен.

### 2.4. `retired_reason` НЕ публикуется — решение с причиной

Колонка `retired_reason` существует и читается внутренними путями. **На провод она
не выносится**, и это решение, а не забывчивость:

- у текста **нет формы контракта**. Это свободная проза, которую пишет тот, кто
  снимает ресурс; ни один гейт её содержания не судит. Вынесенная на публичную
  поверхность, она становится тоном сообщений — то есть контрактом, который никто
  не объявлял и не сможет удержать;
- **предикат задачи её не требует.** Клиенту нужен следующий шаг («что назвать
  вместо»), а не причина нашего решения;
- **не «на будущее» и не отсрочка** (ban #11): поле не заводится вовсе, скелета не
  остаётся. Понадобится — будет своим изменением, и первым его вопросом станет
  форма текста.

Это **не** «принято-и-проигнорировано» (`api-conventions.md`): проигнорированным
бывает поле ЗАПРОСА, которое приняли и не прочли. Здесь поля контракта нет.

### 2.5. Отказ научится называть, ГДЕ узнать преемника — осознанная правка текста

**РЕШЕНО:** доменный гейт снятия называет в тексте отказа **точку чтения каталога**
— ту же, что уже называет соседняя полоса. Преемника отказ по-прежнему не называет
(§1.1).

**Почему это правка КОНТРАКТА и названа отдельно.** Тон сообщений стабилен и
меняется осознанно (`api-conventions.md` §Error-format). Цена измерена:

```sh
git grep -c 'is retired (no resource' -- '*.go'   # → 1 — только сама прод-строка
```

Ни одна проба точного текста не пинит. Пинится **свойство**: отказ обязан называть
тип (`internal/domain/rule_retired_type_test.go:170–173`,
`strings.Contains(err.Error(), dotted)`) — и расширение текста этому не противоречит.

**Почему обе полосы, а не одна.** Полоса грантуемости адрес каталога уже называет
(`rules_catalog.go:92,163–165`), полоса снятия — нет. Две полосы одного механизма
обязаны сверяться МЕЖДУ СОБОЙ (`architecture.md` §«Параллельные полосы»): их
различие сегодня никем не решалось, оно побочный эффект того, что гейты писались
порознь.

### 2.6. ЦЕНА ДЛЯ КЛИЕНТА В ШАГАХ — измерена, а не оценена

Клиент авторует роль с правилом на `compute.disk`.

| | сегодня | после |
|---|---|---|
| **1** | `Role.Create` → `INVALID_ARGUMENT` `type compute.disk is retired (no resource of this type exists)` | тот же отказ **плюс** имя точки чтения каталога |
| **2** | `GET /iam/v1/permissionCatalog` — и `compute.disk` в ответе **отсутствует** (`TestCatalogOffersNoRetiredType`). Клиент видит `storage.images`, `storage.snapshots`, `storage.volumes` и не знает, который из трёх | тот же вызов: снятое названо, `compute.disk → storage.volumes` |
| **3…N** | **преемника нет ни в одном ответе.** Остаётся чтение исходников либо догадка по имени — а имена намеренно не единообразны (`rules_catalog.go:89–91`: `compute.instance` единственного числа, `storage.volumes` множественного), поэтому догадка неверна ровно там, где нужна | — |

**Сегодня цена не «три вызова», а НЕДОСТИЖИМА чтением.** Второй вызов вопроса не
закрывает: витрина снятого не называет, и три кандидата `storage.*` между собой не
различает ничем. **После — один вызов**, тот самый, на который отказ уже указывает.

---

## 3. Что меняется

1. **`catalog.ResourceRow`** (`internal/catalog/rows.go:53–73`) получает поле
   преемника. Значение **приезжает строкой** — тем же способом, каким приехал
   `ObjectType`, и по той же причине, которую его комментарий уже называет: вывести
   преемника из пары нечем.
2. **Чтение снятой половины** (`retiredCatalogHalf`,
   `internal/repo/kaname/pg/catalog_repo.go:130–137`) начинает выбирать колонку
   преемника. Живая половина **не трогается**: у живой строки преемника не бывает
   (`CHECK catalog_resource_successor_only_when_retired`, `0001_initial.sql:1782`).
3. **Снимок каталога** получает вторую половину — снятую, и читает обе **ОДНОЙ
   ТРАНЗАКЦИЕЙ**. Сегодня `catalog.RowSource` объявляет только `ReadLiveCatalog`
   (`rows.go:108`), а снятую половину порт витрины не видит вовсе.

   **Расширения ПОРТА недостаточно, и это замер, а не осторожность.** `CatalogRepo`
   держит пул (`catalog_repo.go:46–48`), а `readCatalogHalf` шлёт на сервер **три**
   отдельных оператора (`catalog_repo.go:155,164,176`). Значит две половины с пула —
   **шесть операторов и шесть снимков**; «один вызов порта» об изоляции не говорит
   ничего.

   **ТОЧЕК ЧТЕНИЯ СНИМКА ДВЕ, И ВТОРАЯ — СТАРТ.** Круги 1 и 2 называли одну
   (`Snapshot.Refresh`, `snapshot.go:97`) и тем оставляли путь старта без
   производителя вовсе:

   | точка | что читает сегодня | чем наполняется снятая половина |
   |---|---|---|
   | **старт** — `NewSnapshot` (`internal/catalog/snapshot.go:72`), зовётся из `cmd/kaname/serve.go:395` | **ничего**: строки приходят **параметром**, `catalogCensus.Live` | ничем — **это и есть дефект** |
   | **обновление** — `Snapshot.Refresh` (`snapshot.go:97`) | `ReadLiveCatalog` — только живую | §3 п. 3, общая транзакция |

   Свежезапущенный процесс отдаёт арендатору **ПУСТОЙ** перечень снятого — до
   первого обновления, то есть окно ограничено периодом петли
   (`catalogSnapshotRefreshPeriod()`, `cmd/kaname/catalog_snapshot_wiring.go:52`,
   умолчание **15 с**). Это ровно тот продуктовый дефект, ради которого приёмка
   написана: клиент, получивший отказ на снятом типе, читает витрину и не узнаёт
   преемника — потому что процесс только что поднялся.

   **Снятые СТРОКИ на старте уже прочитаны — их выбрасывают.** Страж паритета зовёт
   `ReadRetiredCatalog` **всегда** (`internal/apps/kaname/seed/catalog_parity.go:337`,
   и его собственный комментарий объявляет это безусловным), берёт `len()` трёх
   множеств в `RetiredModules`/`RetiredResources`/`RetiredVerbs` (`:229–231`) и
   структуру отбрасывает; наружу ценз отдаёт только `Live catalog.Rows` (`:246`).

   **Отсюда механизм старта — НЕ второе чтение, а ДОНОС уже прочитанного:** ценз
   несёт снятые строки тем же полем, что живые, и `NewSnapshot` принимает **обе**
   половины. **Новых операторов на старте — ноль.**

   **«Ноль» здесь ИЗМЕРЕН, а не выведен.** Прогон живого держателя на поднятой базе
   печатает своё число: `K=6, N=6` — у стража отдельно шесть операторов, и снимок не
   добавляет ни одного. Шесть раскладываются как **3 + 3**: страж зовёт обе половины
   (`catalog_parity.go:320` живую, `:337` снятую), а каждая половина стоит трёх
   операторов (`catalog_repo.go:155,164,176`). То есть снятые строки **уже входят в
   эти шесть** — донести их в снимок стоит **нуля** новых обращений, и это свойство
   арифметики пути старта, а не обещание исполнителя.

   **Живой держатель этого свойства под ударом, и он назван по имени:**
   `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog`
   (`internal/repo/kaname/pg/catalog_snapshot_integration_test.go:105`) считает
   операторы через `QueryTracer` и требует `N == K` — у стража отдельно и за весь
   путь старта со снимком. Исполнитель, сделавший «естественное» (прочитать обе
   половины транзакцией **при построении**), получит `N > K` и красное. Проба при
   этом **права**, а неверен был бы механизм: второе чтение об одном предмете — два
   места, и разойдутся они молча. Требование «ноль новых операторов» есть прямое
   следствие её существования, а не отдельное пожелание.

   **Механизм выбран ОДИН — общая транзакция; фиксированный порядок ОТВЕРГНУТ, и
   довод не «так надёжнее».** Порядок аномалию не убирает, а **меняет её вид**, и
   каждый из двух видов запрещён своим сценарием этого же документа:

   | порядок чтения | что наблюдается, если снятие легло МЕЖДУ половинами | чем запрещено |
   |---|---|---|
   | живая, затем снятая | ресурс **в обоих** перечнях: живая прочитана до снятия, снятая — после | `IAM-SUC-01` |
   | снятая, затем живая | ресурса **ни в одном**: снятая прочитана до снятия, живая — после | `IAM-SUC-09` |

   То есть фиксированный порядок исполним ровно для ОДНОГО из двух сценариев, каким
   бы он ни был выбран. Обе половины под одной транзакцией снимают оба вида сразу —
   **by construction**, а не проверкой.

   **Образец в дереве ЕСТЬ, и он делает ровно это:** `catalogWriter.ReadCatalog`
   (`internal/repo/kaname/pg/catalog_writer.go:270–279`) читает обе половины на
   `w.tx`, и его godoc называет предмет дословно — «Шесть операторов под одной
   транзакцией — то есть ОДИН снимок: собранный из разных моментов, он показал бы
   состояние, которого в базе не бывает». Витрина обязана взять **этот** механизм, а
   не завести второй об одном предмете.

   Godoc `Rows` (`rows.go:93–99`), на который опиралась прежняя редакция этого
   пункта, говорит о **форме** порта — что перечни приходят вместе, — и об изоляции
   чтения не утверждает ничего. Ссылаться на него как на держателя свойства было
   ошибкой круга 1.
4. **Контракт витрины** (`proto/kaname/cloud/iam/v1/permission_catalog_service.proto`):
   новое поле верхнего уровня ответа — перечень записей снятого; каждая запись несёт
   точечное имя снятого и точечное имя преемника. Шапка поля обязана сказать три
   вещи: это **не** грантуемое; преемник **жив**; глаголов запись не несёт и почему
   (§2.1).
5. **Витрина** (`internal/apps/kaname/api/permission_catalog/`) заполняет новое поле
   из снятой половины снимка. `modules[]`, `closed_verbs`, `wildcard_policy` не
   трогаются ни одной строкой.
6. **Текст отказа доменного гейта** (`internal/domain/rule.go:186–188`) называет
   точку чтения каталога. Константа адреса **одна на дерево**: берётся та, что уже
   объявлена (`rules_catalog.go:92`), второй не заводится.
7. **Пробы** — §4 и §7.

**Чего НЕ меняется, и это утверждается сценарием, а не обещанием:** миграции,
посев, доменный словарь снятого (`retired_types.go`), ключи `role_rule_ref_res_fk`
и `role_verb_type_fk`, `closed_verbs`, этаж авторизации витрины, превью роли.

---

## 4. Приёмочные сценарии (Given-When-Then)

### IAM-SUC-01 — перечень ГРАНТУЕМОГО снятого не называет (положительный контроль `#1976`)

**Дано** служба после изменения, каталог в посеянном состоянии.
**Когда** арендатор читает витрину разрешений.
**Тогда** `modules[].resources[]` **не содержит** ни `compute.disk`, ни
`compute.image`, ни `compute.snapshot`.
**И** пересечение перечня грантуемого со снятым словарём **пусто**.

Несущий контроль: без него `IAM-SUC-02` зеленел бы на исходе, который отзывает
`#1976`.

> **Производитель — ЖИВ и не заводится этим изменением:**
> `TestCatalogOffersNoRetiredType` (`offers_only_live_types_test.go:67`). Он обязан
> остаться **зелёным** и остаться **значащим**: его шапка объявляет, что он держит
> свойство от возврата, а перепись печатает два числа рядом — пар у витрины и пар у
> перечня сборки.

### IAM-SUC-02 — снятый ресурс назван СВОИМ перечнем и несёт преемника

**Дано** тот же каталог.
**Когда** арендатор читает витрину тем же единственным вызовом.
**Тогда** ответ несёт перечень снятого из **трёх** записей.
**И** запись `compute.disk` называет преемником `storage.volumes`; `compute.image`
— `storage.images`; `compute.snapshot` — `storage.snapshots`.
**И** порядок записей детерминирован — по точечному имени снятого.

> **Производителя СЕГОДНЯ НЕТ, и это сказано прямо.** Заводится этим изменением,
> §3 пп. 1–5. Живы и не заводятся: **данные** (три посеянных строки с преемником) и
> **чтение снятой половины** (`catalog_repo.go:76,130–137`). Обрыв — ровно два
> звена: `catalog.ResourceRow` преемника не несёт (`rows.go:53–73`), контракт о
> снятии молчит (`grep -c 'superseded\|retired' … → 0`).
>
> **Порядок назван требованием, а не оставлен умолчанием** (`api-conventions.md`
> §«Пустое значение обязано означать „пусто"», подкласс A): у повторяющегося поля
> либо порядок значим и закреплён колонкой порядка, либо это набор и так сказано.
> Здесь выбран первый исход, потому что перечень читает человек.

### IAM-SUC-03 — преемник ЖИВ, и это не совпадение

**Дано** тот же каталог.
**Когда** клиент берёт имя преемника из `IAM-SUC-02` и ищет его в перечне
грантуемого того же ответа.
**Тогда** каждый из трёх преемников там **есть**.

Без этого `IAM-SUC-02` был бы исполним преемником, указывающим на снятое, — то есть
восстанавливал бы шаг, которого не существует.

> **Производитель — ЖИВ:** `internal/check/catalog_seed_parity.go:399–407` требует у
> каждой снятой строки непустого преемника **и** членства преемника в множестве
> ЖИВЫХ ключей; текст находки — `посев снятого: преемник … не является живым ключом
> каталога`. Перепись того же гейта печатает `живых ресурсов 27, снятых ресурсов 3`
> (`TestIAMCT114_CatalogSeedMatchesTheLiteral`). Ниже — ключ `CHECK
> catalog_resource_successor_only_when_retired` (`0001_initial.sql:1782`):
> преемник у **живой** строки невыразим в базе.

### IAM-SUC-04 — отказ на снятом типе приходит из ДОМЕНА и кодом `INVALID_ARGUMENT`

**Дано** арендатор авторует роль с правилом `module=compute`, `resources=[disk]`.
**Когда** он зовёт **создание** роли — и отдельно **правку** существующей роли тем же
правилом.
**Тогда** **оба** глагола отвечают одинаково: отказ синхронный, код
**`INVALID_ARGUMENT`**, сообщение называет тип `compute.disk` и слово «retired».
**И** сообщение называет точку чтения каталога — ту же, что называет соседняя
полоса.
**И** ключ до этого правила **не доходит** ни на одном из двух: отказ вынесен раньше
любой записи.

Правка названа наравне с созданием потому, что порядок гейтов у неё **свой**
(`update.go:182` против `:191`), и совпадение с созданием есть свойство, а не данность.
Круг 1 требовал обоих глаголов в DoD, а «Когда» называло только создание — то есть
готовность спрашивала того, чего не просил ни один сценарий.

**Положительный контроль в том же сценарии:** правило над **живым** типом
(`compute.instance`) на всех трёх плечах принимается. Без него «снятое отвергнуто»
неотличимо от «отвергается всё».

> **Производители — РАЗДЕЛЬНО по половинам «Тогда»:**
> код и порядок — **живы**: `internal/domain/rule.go:186–188` → `MapValidationErr`
> (`internal/apps/kaname/shared/errors.go`) → `codes.InvalidArgument`; домен раньше
> грантуемости — `create.go:137` против `:154`, `update.go:182` против `:191`;
> «сообщение называет тип» — **живо**: `rule_retired_type_test.go:142`
> (`TestRetiredTypeIsRejectedOnEveryArm`, три плеча, `strings.Contains(err, dotted)`),
> и положительный контроль **живого** типа стоит в **той же** пробе первым
> (`:150–157`), то есть заводить его не нужно;
> «сообщение называет точку чтения» — **производителя НЕТ**, заводится §3 п. 6.

### IAM-SUC-05 — `FAILED_PRECONDITION` живёт на ДРУГОЙ полосе, и она тоже ведёт к каталогу

**Дано** ресурс, снятый **применением манифеста в работающем процессе** (его нет в
доменном словаре снятого, строка каталога не жива).
**Когда** арендатор авторует правило над ним.
**Тогда** отказ — **`FAILED_PRECONDITION`**, сообщение формы
`resources: <тип> is not a live platform resource`.
**И** тот же единственный вызов витрины называет его в перечне снятого с преемником,
если применитель преемника назвал.

**Отличие от `IAM-SUC-04` — один факт:** тип **не** в доменном словаре снятого.
Всё остальное в мире сценария то же.

> **Производители — ЖИВЫ по коду и тексту:** `internal/repo/kaname/pg/pgmaperr.go:465`
> (формат `resources: %s is not a live platform resource`) и
> `internal/errors/errors.go:83` — `ErrReferenceMissing` обёрнут `ErrFailedPrecondition`,
> отсюда код. Комментарий рядом (`pgmaperr.go:460–462`) объявляет различие кодов
> доказательством того, что отвечал КЛЮЧ, а не грамматика.
> **Вторая половина «Тогда» производителя НЕ имеет** и заводится §3 пп. 1–5, причём
> **условно**: преемник у снятой строки обязателен для ПОСЕВА (гейт §0.6), но
> применитель вправе снять строку без преемника. Сценарий это учитывает клаузой
> «если применитель преемника назвал», и §4а называет, что видит клиент, когда не
> назвал.

### IAM-SUC-05a — снятый БЕЗ преемника назван, но преемника не обещает

**Дано** строка, снятая применением, у которой преемник не назван.
**Когда** арендатор читает витрину.
**Тогда** запись в перечне снятого **есть**, а поле преемника **пусто**.
**И** пустое означает «преемник не назван», а не «преемник — пустая строка»: клиент
отличает это от `IAM-SUC-02` по самому полю, не по длине списка.

Отрицание без этого зеленело бы на реализации, которая молча выбрасывает снятое без
преемника, — то есть скрывала бы от клиента, что ресурс снят.

> **Производителя НЕТ**, заводится §3 пп. 1–5. Требование — прямое следствие
> `api-conventions.md` §«Пустое значение обязано означать „пусто"»: отсутствие
> представимо отдельно от значения, иначе оно лжёт.

### IAM-SUC-06 — старый клиент не изменился ни в одном поле

**Дано** клиент, написанный до этого изменения.
**Когда** он читает витрину.
**Тогда** `modules[]`, `closed_verbs` и `wildcard_policy` побайтово те же, что до
изменения.
**И** сверка контракта против ствола разрывов не находит.

> **Производители — ЖИВЫ:** `buf breaking` исполняется шагом конвейера
> (`.github/workflows/ci.yml:288`) при наборе `FILE` (`proto/buf.yaml`); состав трёх
> прежних полей утверждается поимённо
> (`usecase_test.go:84,116,150,255`, `shows_the_live_catalog_test.go:87,115,152`,
> `registry_catalog_test.go:27`). Заводить ничего не требуется — требуется **не
> покрасить**.

### IAM-SUC-07 — снятый ресурс не стал грантуемым оттого, что он назван

**Дано** витрина после изменения, перечень снятого в ответе.
**Когда** арендатор берёт имя ОТТУДА и авторует правило над ним.
**Тогда** отказ тот же, что в `IAM-SUC-04`, — перечень снятого выдачи не открывает.
**И** роль не создана.

> **Производители — ЖИВЫ и не трогаются:** доменный гейт (`rule.go:186–188`), гейт
> грантуемости (`rules_catalog.go:157–166` — источник `FGAObjectType` по **живым**
> строкам), ключи `role_rule_ref_res_fk` и `role_verb_type_fk`
> (`0001_initial.sql:5506` и таблица `role_verb`). Сценарий требует, чтобы изменение
> не тронуло ни один из четырёх.

### IAM-SUC-08 — витрина по-прежнему не сужается по арендатору и отвергает анонима

**Дано** два арендатора с разными выдачами и анонимный вызывающий.
**Когда** каждый читает витрину.
**Тогда** оба аутентифицированных получают **побайтово одинаковый** ответ, включая
перечень снятого.
**И** анонимный отвергается fail-closed.

Положительный контроль здесь — первая половина: без неё «аноним отвергнут»
выполнялось бы и на витрине, отвергающей всех.

> **Производители — ЖИВЫ:** этаж авторизации
> (`internal/apps/kaname/seed/embedded/permission_catalog.json:3174–3180` —
> `viewer` @ `cluster`, `from_request_field: "*"`) и
> `TestListPermissionCatalog_AnonymousFailClosed` (`usecase_test.go:331`).
> **Половина «включая перечень снятого» производителя НЕ имеет** и заводится вместе
> с полем.

### IAM-SUC-09 — снятая половина читается ТЕМ ЖЕ снимком, что живая

**Дано** каталог, в котором применитель снимает ресурс.
**Когда** витрину читают в момент, когда снятие уже применено.
**Тогда** ресурс исчез из перечня грантуемого **и** появился в перечне снятого — в
одном и том же ответе.
**И** на пути **ОБНОВЛЕНИЯ** обе половины прочитаны **одной транзакцией**:
операторов, ушедших на сервер вне неё, — **ноль**, **и при этом снятая половина
снимка НЕПУСТА**.
**И** на пути **СТАРТА** новых операторов к таблицам каталога — **ноль**
(`N == K`), **и при этом снятая половина снимка НЕПУСТА**.
**И** ни одно из двух состояний не наблюдается **ни при каком** порядке применения:
ресурс **в обоих** перечнях сразу либо **ни в одном**.

Оба запрещённых состояния названы, а не одно: фиксированный порядок убирает ровно
одно из них и заводит второе (§3 п. 3, таблица). Сценарий, называющий только «ни в
одном», был бы исполним порядком — то есть зеленел бы на реализации, дающей дубль.

**«Непуста» стоит рядом с каждым нулём НЕ для симметрии.** Голый счётчик операторов
здесь **вакуумен**, и это проверяется подстановкой: «операторов вне общей транзакции
при построении — ноль» истинно **уже сегодня**, потому что конструктор не шлёт
операторов **вовсе** (`snapshot.go:72`: строки приходят параметром). Такое
утверждение проходит на реализации, где снятая половина не доезжает **никогда**, и
не отличает «ноль, потому что всё под транзакцией» от «ноль, потому что ничего не
читали». Круг 2 нёс ровно эту вакуумную форму. Различает их **положительный
контроль на содержимом** — тот же приём, что уже стоит в живом держателе
(`TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog`, хвост: «снимок пуст при `N==K` — равенство выполнено тем, что
снимок ничего не взял»).

> **Производитель — ЗАВОДИТСЯ, и механизм назван ОДИН** (§3 п. 3: обе половины на
> одной транзакции). **Образец в дереве жив** — `catalogWriter.ReadCatalog`
> (`internal/repo/kaname/pg/catalog_writer.go:270–279`), обе половины на `w.tx`, и
> его godoc называет предмет дословно.
>
> **Держатель — проба, считающая ОПЕРАТОРЫ, а не читающая код.** В дереве есть
> механизм счёта (`QueryTracer`; эталон применения —
> `internal/repo/kaname/pg/relverdict/batch_roundtrip_integration_test.go`), и его
> единица — каждый оператор, уходящий на сервер. Требование проверяемо: операторов
> вне общей транзакции **ноль**.
>
> Круг 1 ставил здесь «жив по замыслу» со ссылкой на godoc `Rows` — **неверно**:
> тот godoc о форме порта, а не об изоляции, и свойства не держит.

### IAM-SUC-11 — процесс, который ещё НЕ обновлялся, преемника уже называет

**Дано** только что поднятый процесс: страж паритета отработал, снимок построен,
петля обновления **ещё ни разу не сработала**.
**Когда** арендатор читает витрину.
**Тогда** перечень снятого **непуст** и называет те же три пары с преемниками, что
и после обновления.
**И** операторов к таблицам каталога за путь старта — **ровно столько же**, сколько
шлёт страж отдельно (`N == K`): снятые строки **донесены**, а не прочитаны заново.

**Положительный контроль:** живая половина того же снимка тоже непуста — иначе
«непусто» выполнялось бы снимком, взявшим всё подряд, а `N == K` — снимком, не
взявшим ничего.

> **Почему это отдельный сценарий, а не половина `IAM-SUC-02`.** `IAM-SUC-02`
> утверждает **состав** ответа и исполним на процессе, поработавшем минуту.
> Здесь предмет — **окно**: от старта до первого обновления (период
> `catalogSnapshotRefreshPeriod()`, умолчание **15 с**) витрина отвечает
> арендатору, и ответ обязан быть полным уже в нём. Круг 2 этого окна не видел
> вовсе и оставил бы клиента без преемника ровно тогда, когда процесс только что
> перезапустили.
>
> **Производитель — ЗАВОДИТСЯ** (§3 п. 3, путь старта: поле снятых строк в цензе
> плюс вторая половина параметром конструктора). **Держатель равенства `N == K`
> ЖИВ и правится НЕ он, а механизм:**
> `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog`
> (`catalog_snapshot_integration_test.go:105`). Его положительный контроль
> расширяется на снятую половину — и это единственная правка, которую он
> принимает: ослабить `N == K` значило бы снять свойство, ради которого он написан.

### IAM-SUC-10 — преемник доезжает ПО ПРОВОДУ, а не только до слоя use-case

**Дано** поднятый стенд и аутентифицированный арендатор.
**Когда** он делает **один** вызов через край: `GET /iam/v1/permissionCatalog`.
**Тогда** ответ — **HTTP 200**.
**И** тело несёт поле `retiredResources` — **camelCase на проводе**, как и все
соседние поля витрины.
**И** запись `compute.disk` называет преемником `storage.volumes`, а поле преемника
пишется `supersededBy`.
**И** `modules[]` этого же тела `compute.disk` **не содержит** — обе половины
утверждаются одним телом, а не двумя прогонами.

**Положительный контроль в том же кейсе:** `modules[]`, `closedVerbs` и
`wildcardPolicy` на месте и непусты. Без него «снятого нет в грантуемом» зеленело бы
на пустом ответе.

**Анонимный** вызов по-прежнему отвергается — существующий негатив набора
(`NEG-G-02-catalog-anonymous-unauthenticated`), и перечень снятого его не ослабляет.

> **Почему сценарий обязателен, а не «то же самое другим слоем».** Предикат задачи и
> §1 говорят «арендатор узнаёт преемника ОДНИМ вызовом» — это утверждение о том, что
> приходит клиенту **по проводу**. Проба слоя use-case зовёт хендлер напрямую и о нём
> не высказывается: мимо неё проходят край, перекодирование в camelCase и пересборка
> потребителя. Это ровно `api-conventions.md` §«Поле контракта не выходит наружу, пока
> КРАЙ не пересобран»; DoD п. 5 это правило цитировал, но требовал только
> регенерации стабов, а регенерация наблюдения по проводу не даёт.
>
> **«Здесь не запустить» недоступно, и это ИЗМЕРЕНО.** Набор чёрного ящика у службы
> есть: `tests/newman/cases/*.py` — **41** файл (единица: отслеживаемый файл кейсов);
> у ЭТОГО RPC есть **выделенный** набор `tests/newman/cases/iam-permission-catalog.py`
> с **двумя** кейсами (`CONF-G-01-catalog-happy`,
> `NEG-G-02-catalog-anonymous-unauthenticated`); он назван прогонщиком
> (`tests/newman/scripts/run.sh:420`), объявлен в `tests/newman/docs/CASES-INDEX.md:56`
> и гоняется процессом `.github/workflows/e2e-newman.yml`.
>
> **Производитель — ЗАВОДИТСЯ:** третий кейс того же набора,
> `CONF-G-03-catalog-retired-successor`. Номер свободен — предикат
> `grep -rhoE 'id="(CONF|NEG)-G-[0-9]+[^"]*"' tests/newman/cases/` даёт ровно два
> занятых. Форма утверждения — **пара**: HTTP-статус и содержимое поля, как того
> требует `e2e-flow.md` §3; техника кейса объявляется `CONF`, как у соседа.

---

## 5. Перечень «сценарий → производитель»

Колонка «состояние» отвечает на один вопрос: существует ли в дереве строка,
производящая утверждение «Тогда», **сегодня**. `ЗАВОДИТСЯ` означает, что
производителя нет и он появляется этим изменением — координата названа.

Колонка **«слой»** заведена кругом 2 и отвечает на другой вопрос — **чем именно**
утверждение проверяется: `код` — проба против прод-кода (use-case, домен, репозиторий,
дерево); `край` — кейс чёрного ящика через `GET /iam/v1/permissionCatalog` на поднятом
стенде. Различие несущее: проба слоя `код` о том, что приходит клиенту **по проводу**,
не высказывается вовсе (`IAM-SUC-10`).

| сценарий | что утверждает «Тогда» | производитель | слой | состояние |
|---|---|---|---|---|
| `IAM-SUC-01` | снятого нет в перечне грантуемого | `offers_only_live_types_test.go:67` | код | **жив** |
| `IAM-SUC-02` | три записи снятого с преемником | §3 пп. 1–5 | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-02` | данные существуют (3 строки с преемником) | посев `0001_initial.sql`; перепись `catalog_seed_parity_test.go:186` | код | **жив** |
| `IAM-SUC-02` | снятая половина читается из базы | `catalog_repo.go:76,130–137` | код | **жив** |
| `IAM-SUC-03` | преемник — живой ключ каталога | `catalog_seed_parity.go:399–407` | код | **жив** |
| `IAM-SUC-03` | преемник невыразим у живой строки | `CHECK`, `0001_initial.sql:1782` | код | **жив** |
| `IAM-SUC-04` | код `INVALID_ARGUMENT` | `rule.go:186–188` → `shared/errors.go` `MapValidationErr` | код | **жив** |
| `IAM-SUC-04` | домен раньше грантуемости и ключа | `create.go:137`/`:154`; `update.go:182`/`:191` | код | **жив** |
| `IAM-SUC-04` | сообщение называет тип; живой тип принят | `rule_retired_type_test.go:142` (негатив) и `:150–157` (контроль) | код | **жив** |
| `IAM-SUC-04` | сообщение называет точку чтения каталога | §3 п. 6; константа — `rules_catalog.go:92` | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-05` | код `FAILED_PRECONDITION` | `errors.go:83` (`ErrReferenceMissing` ⊂ `ErrFailedPrecondition`) | код | **жив** |
| `IAM-SUC-05` | текст `resources: … is not a live platform resource` | `pgmaperr.go:465` | код | **жив** |
| `IAM-SUC-05` | снятый применением назван витриной | §3 пп. 1–5 | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-05a` | пустой преемник отличим от отсутствия записи | §3 пп. 1–5 | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-06` | прежние три поля не изменились | `usecase_test.go:84,116,150,255`; `shows_the_live_catalog_test.go:87,115,152`; `registry_catalog_test.go:27` | код | **жив** |
| `IAM-SUC-06` | разрывов контракта нет | `ci.yml:288` + `proto/buf.yaml` (`FILE`) | код | **жив** |
| `IAM-SUC-07` | снятое не грантуемо | `rule.go:186–188`; `rules_catalog.go:157–166`; ключи `role_rule_ref_res_fk`, `role_verb_type_fk` | код | **жив** |
| `IAM-SUC-08` | аноним отвергнут; ответ не сужается | `permission_catalog.json:3174–3180`; `usecase_test.go:331` | код | **жив** |
| `IAM-SUC-08` | перечень снятого одинаков у всех | §3 пп. 1–5 | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-09` | путь ОБНОВЛЕНИЯ: обе половины одной транзакцией, операторов вне неё ноль | §3 п. 3; образец `catalog_writer.go:270–279`; счётчик — `QueryTracer`, эталон `relverdict/batch_roundtrip_integration_test.go` | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-09` | путь СТАРТА: новых операторов ноль (`N == K`) | `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog` (`catalog_snapshot_integration_test.go:105`) | код | **жив** |
| `IAM-SUC-09` | при каждом нуле снятая половина снимка НЕПУСТА | положительный контроль на содержимом; форма жива в хвосте `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog` | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-09` | оба запрещённых состояния (дубль и пропажа) невозможны | там же — общая транзакция снимает оба by construction | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-11` | необновлённый процесс называет преемника | §3 п. 3, путь старта: снятые строки в цензе + вторая половина параметром конструктора | код | **ЗАВОДИТСЯ** |
| `IAM-SUC-11` | снятые строки на старте УЖЕ прочитаны — их доносят, а не читают заново | `seed/catalog_parity.go:337` (читает всегда), `:229–231` (остаются числа), `:246` (наружу только `Live`) | код | **жив** |
| `IAM-SUC-11` | `N == K` не ослаблен ради наполнения | `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog` (`catalog_snapshot_integration_test.go:105`) | код | **жив** |
| `IAM-SUC-10` | HTTP 200 по проводу | набор витрины — `iam-permission-catalog.py`, `run.sh:420`, `e2e-newman.yml` | край | **жив** |
| `IAM-SUC-10` | `retiredResources` в camelCase-теле, `supersededBy` у записи | кейс `CONF-G-03-catalog-retired-successor` | край | **ЗАВОДИТСЯ** |
| `IAM-SUC-10` | `modules[]` того же тела снятого не содержит | там же (то же тело, не второй прогон) | край | **ЗАВОДИТСЯ** |
| `IAM-SUC-10` | положительный контроль: три прежних поля непусты | `CONF-G-01-catalog-happy` | край | **жив** |
| `IAM-SUC-10` | аноним отвергнут | `NEG-G-02-catalog-anonymous-unauthenticated` | край | **жив** |

**Итог переписи — единица счёта названа: СТРОКА ДАННЫХ этой таблицы, то есть одно
утверждение «Тогда».**

| | строк | из них слой `код` | слой `край` |
|---|---:|---:|---:|
| производитель **жив** | **20** | 17 | 3 |
| производитель **ЗАВОДИТСЯ** | **11** | 9 | 2 |
| **всего** | **31** | 26 | 5 |

Ни одно «Тогда» не оставлено без ответа на вопрос «чем это будет производиться», и
**гибридного состояния в таблице больше нет**: круг 1 держал одну строку и живой, и
заводящейся сразу, из-за чего его итог («21, жив 14, заводится 6, плюс один гибрид»)
складывался в 21 при 20 строках — величина, не дававшаяся ни одной единице счёта.
Строка разведена на две (§`IAM-SUC-09`), и DoD п. 2 пересчитан по **этой** таблице,
а не по памяти.

---

## 6. Вне области

Каждый пункт назван с предикатом, чтобы «вне области» не читалось как «когда-нибудь».

1. **Разбивка превью роли по ресурсам.** Превью объединяет глаголы по всем правилам
   роли одним множеством, поэтому «ни одного глагола ПО СНЯТОМУ ресурсу» выразимо
   только для разворота подстановки — и это **уже** сделано
   (`role_effective_verbs.go:67`, §0.3). Правило с ЯВНЫМ глаголом на снятом типе
   по-прежнему добавит глагол в объединение. Граница названа тем же `#1814` и
   требует пересмотра ФОРМЫ превью; предикат снятия: превью отдаёт глаголы,
   разложенные по ресурсам, а не одним множеством.
2. **`retired_reason` на проводе.** Решение и причина — §2.4. Предикат пересмотра:
   у текста причины появилась объявленная форма и гейт, её держащий.
3. **Глаголы снятого типа.** Не публикуются (§2.1). Предикат пересмотра: у снятой
   строки появился объявленный набор глаголов, переживающий снятие.
4. **`workspace_revision` в ведомости приёмок.** В этом дереве предмета нет вовсе
   (§0.7). Уходит своей задачей в `PRO-Robotech/kacho`, заводимой до слияния линии
   (§7 п. 8).
5. **Отказ, называющий преемника ТЕКСТОМ.** Неисполнимо: транзакция прервана,
   чтения в ней нет. Решено приёмкой `#1030` и не пересматривается.
6. **Сужение витрины по арендатору.** Витрина — платформенные метаданные, этаж
   аутентификации объявлен и обоснован (§2.3). Предикат пересмотра: в витрине
   появилось поле, чьё значение зависит от вызывающего.
7. **Предикат снятия задачи `#1814`.** Он написан так, что дословное исполнение
   отзывает `#1976` (§0.5), и называет код отказа, которого на этом входе не бывает
   (§0.4). Переписывается **вместе** с закрытием задачи, а не этим документом:
   формулировка принадлежит тому, кто задачу закрывает.
8. **Величина периода обновления объявлена ДВАЖДЫ, и читается одна — НАХОДКА ПО
   ДОРОГЕ круга 3.** Петлю ведёт ручка окружения
   (`catalogSnapshotRefreshPeriod()`, `cmd/kaname/catalog_snapshot_wiring.go:52`,
   умолчание **15 с**); рядом живёт настройка `jobs.catalog-snapshot.refresh-interval`
   (`internal/apps/kaname/config/jobs.go:69`, умолчание **1 минута**), которую
   **никто не читает** для задания периода — её касается только собственная
   проверка (`:78`). Предикат: `git grep -n 'RefreshInterval' -- '*.go' ':!*_test.go'`
   — вне `jobs.go` вхождений по этому предмету нет.
   Класс — `api-conventions.md` §«Принято-и-проигнорировано» на уровне настройки:
   оператор, выставивший её, получает успех и уверен, что период изменил.
   **Здесь НЕ чинится** — чужой предмет; заводится своей задачей. Размер окна, на
   который ссылается `IAM-SUC-11`, взят у **читаемой** ручки, а не у объявленной.
9. **Шапка кейса набора пережила свой предмет — НАХОДКА ПО ДОРОГЕ, чужой предмет.**
   `tests/newman/cases/iam-permission-catalog.py` объявляет источником витрины
   «`kaname authzmap.objectTypes … No DB, no migration`». Это перестало быть правдой
   вместе с `#1976`: витрина читает **живые строки** каталога (§0.2), то есть базу и
   миграцию. Утверждение неверно **сегодня** и живёт в шапке набора, который эта
   приёмка расширяет, — поэтому названо, а не умолчано.
   **Здесь оно НЕ чинится**, и это решение по `multi-agent-flow.md` §«Находка по
   дороге попадает в СВОЙ релиз»: чужой предмет в чужом PR делает вердикт
   непрослеживаемым. Заводится своей задачей; предикат снятия —
   `grep -c 'No DB, no migration' tests/newman/cases/iam-permission-catalog.py` → 0.
   Кейс `CONF-G-03` (§4, `IAM-SUC-10`) от этого не зависит: он утверждает **тело
   ответа**, а не шапку набора.

---

## 7. Готовность (DoD)

1. **Приёмка APPROVED** рецензентом (`acceptance-reviewer`) — до первой строки кода
   (ban #1).
2. **Красный ДО кода** по каждой строке §5 со состоянием `ЗАВОДИТСЯ` — их
   **одиннадцать** (девять на слое `код`, две на слое `край`), и число берётся **из
   таблицы §5**, а не из памяти. Красный обязан быть **по существу**: проба исполнилась и не сошлась,
   потому что предмета нет. «Не выполнилось» красным не считается.
3. **`IAM-SUC-01` остаётся зелёным** и остаётся значащим: его перепись печатает два
   числа рядом. Прогон приложен — он и есть доказательство, что `#1976` не отозван.
4. **`buf lint` и `buf breaking` против ствола** — вывод приложен, не пересказан.
   Ноль разрывов; при ненулевом коде вердикт — «гейт не сделал своей работы», а не
   «разрывов нет».
5. **Регенерация стабов И пересборка КРАЯ** — тем же заходом, что правка контракта.
   Регенерация сама по себе наблюдения по проводу не даёт: неизвестное краю поле он
   отбрасывает молча (`api-conventions.md` §«Поле контракта не выходит наружу, пока
   КРАЙ не пересобран»). Доказательство — не факт пересборки, а **зелёный
   `IAM-SUC-10`** на поднятом стенде.
6. **Кейс `CONF-G-03-catalog-retired-successor` заведён полностью**, а не наполовину:
   объявлен в `tests/newman/cases/iam-permission-catalog.py`, записан в
   `tests/newman/docs/CASES-INDEX.md`, назван прогонщиком `tests/newman/scripts/run.sh`
   и попадает в `.github/workflows/e2e-newman.yml`. Пропуск любого из четырёх даёт
   кейс, который исполняется и никого не роняет (`e2e-flow.md` §8). Исполнение
   проверяемо: `git grep -c 'retiredResources' -- tests/newman/` — **не ноль**; вывод
   прогона приложен, а не пересказан.
7. **Порядок полос отказа не сломан** — `IAM-SUC-04` утверждает его вызовом обоих
   глаголов (создание **и** правка роли), а не чтением исходников.
8. **`TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog` — ЗЕЛЁНЫЙ, а не
   ослабленный.** Прогон интеграционный (`-short` его пропускает), поэтому вывод
   прикладывается с поднятой базой; `N == K` держится, и его положительный контроль
   расширен на снятую половину. Пропуск (`SKIP`) зелёным **не считается** — это
   третья категория.
9. **Прогон гейта датировки** — `go test -count=1 -short ./internal/check/ -run
   MeasurementRevisionsAreDatedByHash` даёт **0**; перепись печатает объявление
   этого документа.
10. **Задача о `workspace_revision` заведена** в `PRO-Robotech/kacho` — до слияния
   линии, с признаком, предметом и предикатом снятия, и с перечнем трёх мест корпуса
   службы, которые она правит (§0.7).
11. **Записка трейла** и закрытие `#1814` артефактом — с переписанным предикатом
   снятия (§6 п. 7), а не с прежним.

---

## 8. Риски

| риск | признак, по которому он наступил | чем закрыт |
|---|---|---|
| исполнитель кладёт снятое в `modules[]`, «раз признак есть» | `IAM-SUC-01` краснеет с именем типа | §2.1 и сама проба; она **старше** этого изменения и свойство держит от возврата |
| второй читатель снятой половины вместо расширения порта | снимок собран из двух моментов времени; наблюдаемо как «ресурс уже не грантуем и ещё не назван снятым» | `IAM-SUC-09`; §3 п. 3 |
| **исполнитель читает обе половины транзакцией ПРИ ПОСТРОЕНИИ** — «естественное» прочтение §3 п. 3 | `TestIAMCT2_01_SnapshotAddsNoSecondReadOfTheCatalog` (`internal/repo/kaname/pg/catalog_snapshot_integration_test.go:105`) краснеет: `N > K` | §3 п. 3 требует ДОНОСА уже прочитанного, а не второго чтения: поле снятых строк в цензе плюс вторая половина параметром `NewSnapshot`. Проба **права**, правится механизм, а не она |
| **`N == K` ослабляют, чтобы наполнить снятую половину** | равенство заменено на «не больше чем» либо проба помечена пропуском | запрещено прямо: `IAM-SUC-09` и `IAM-SUC-11` требуют `N == K` **и** непустой снятой половины одновременно. Ослабление снимает свойство, ради которого проба написана |
| **счётчик операторов принят за доказательство без контроля на содержимом** | «операторов ноль» зелено на реализации, где снятая половина не доезжает никогда | `IAM-SUC-09`: рядом с каждым нулём стоит «снятая половина непуста». Вакуумность проверена подстановкой — на пути старта ноль истинен уже сегодня |
| пустой преемник выброшен вместо того, чтобы быть названным | снятая строка без преемника исчезает из ответа целиком | `IAM-SUC-05a` |
| текст отказа правится в **двух** местах, и они расходятся | адрес каталога записан литералом второй раз | §3 п. 6 — константа одна, `rules_catalog.go:92` |
| перечень снятого начинает расти применением и становится длинным | записей больше, чем живых пар | не закрыт и назван: сегодня их **3** при 27 живых. Предикат наблюдения — перепись гейта посева, печатающая обе величины рядом |
| правка приёмки после вердикта переносит одобрение молча | шапка утверждает APPROVED редакции, которую никто не читал | вердикт относится к СОДЕРЖИМОМУ; новая редакция получает свой круг, а не наследует чужой |
