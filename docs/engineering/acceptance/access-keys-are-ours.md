<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка Ф7: ключи доступа — наш сервер, наши ключи (iam Ф7)

- **Статус:** DRAFT — кругов ревью **один**, редакция **3**; вердикт выносит
  `acceptance-reviewer`. Санкции на объём этого документа нет (ban #1): одобренная Ф1 ключей
  доступа не решает вовсе (§0.1), а Ф11 прямо передаёт сюда два своих выноса (§0.2). Редакция 2
  — правки по **восьми** находкам встречного разбора (**§4.6**). Редакция 3 — правки по
  **шести** блокирующим и **пяти** не блокирующим круга 1; каждая находка **перемерена этой
  редакцией**, у одной посылка опровергнута замером, и опровержение названо вместе с предикатом.
  Перечень с исходом по каждой — **§4.8**
- **Ревизии измерения — по дому, строкой над каждым предикатом.** Дом службы
  `PRO-Robotech/kaname@83ddd08a84` (ствол; предмет ревью впереди него на редакции этого
  документа и на записи ревью круга 1); дерево платформы `PRO-Robotech/kacho@45d1719fe8`;
  воркспейс `PRO-Robotech/kacho-workspace@0290f73e94` (ствол; рабочая копия рядом —
  `e229059ab3`, и числа §4.3 сняты по ней, поскольку обе редакции дают один и тот же корпус —
  предикат в §4.3 печатает его знаменателем); фундамент — пин `go.mod` службы,
  `PRO-Robotech/corelib v1.8.0`. Ветка Ф2 П1 — `PRO-Robotech/kaname` `issue-1268-p1-login-method`,
  голова `0fdacffc2a`, **в ствол не влита**; ветка Ф11 — `acceptance/f11-assurance-level`, голова
  **`644b95e27b`**, документ на ней — `85c2a9cb47`, отпечаток
  `68c2cc6cda07e7ecfbd5efe209954748146970cfcbbd9fbc670a737527f3a324`. Популяция §1.6 — **не
  перемерена**, и там же сказано, почему и что это значит
- **Ревизия головы Ф11 в редакции 1 была ПРОСРОЧЕНА на момент собственного замера.** Стояло
  `a65bab135d`; на origin уже лежал `644b95e27b`, ровно на один коммит впереди, и коммит этот
  лёг **на 32 минуты раньше** коммита редакции 1 (`644b95e2` — 2026-09-16 04:22:02 +0300,
  `5dabd24b` — 04:54:10 +0300). Предикат, которым это снимается за секунду:
  `git ls-remote origin refs/heads/acceptance/f11-assurance-level`. Отпечаток документа Ф11 при
  этом **не сдвинулся** (один блоб `5d521dfb` на обеих головах), поэтому ложным был не предмет, а
  вывод о санкции — см. строку ниже
- **Чужие координаты-пути привязаны РЕВИЗИЕЙ, и это их единственный держатель.** Все четыре
  адреса дерева платформы, на которые опирается §1, названы с приставкой
  `PRO-Robotech/kacho@45d1719fe8:` — ведомость и разбор того, почему держателя у них нет, стоят
  в **§4.7**
- **Задача:** `PRO-Robotech/kacho#1273` (Ф7), эпик `#1266`. Открыта — предикат
  `gh issue view 1273 -R PRO-Robotech/kacho --json state` → `OPEN`, 2026-09-16. Открыты и все
  фазы, названные ведомостью §5: `#1268` (Ф2), `#1269` (Ф3), `#1274` (Ф8), `#1276` (Ф10),
  `#1280` (Ф11), `#1281` (Ф12), `#1282` (Ф13); `#1267` (Ф1) — `CLOSED`, её приёмка APPROVED
- **Над чем стоит:** Ф1 —
  `docs/engineering/acceptance/login-session-and-credentials-are-our-contract.md`, APPROVED круга
  3; запись ревью в том же доме, каталог `docs/specs/reviews`, внешнее событие — комментарий в
  `kacho#1267` от 2026-09-14T23:33:08Z. Ф1 ключей доступа **не решает** и выносит их §6 строкой
  «беспарольный вход ключом доступа → Ф13»; ключ как ВТОРОЙ фактор она не выносит вовсе — §4.4
- **Сосед, на которого этот документ опирается сильнее всего:** Ф11 —
  `docs/engineering/acceptance/assurance-level-is-declared-by-our-session.md` (ветка выше, в
  ствол не влита). Её запись ревью круга 3 объявляет **APPROVED**, блокирующих ноль, и
  **эффективная санкция ВЫДАНА**: внешнее событие — комментарий `issue_comment` **5690585262** в
  `kacho#1280` от **2026-09-16T01:21:38Z**, автор `pointpu`, `author_association: MEMBER`,
  `role: acceptance-reviewer`, `verdict: APPROVED`; `subject_sha256` события совпадает с
  отпечатком документа, `ban1_lifted: true`. Перемерено этой редакцией двумя способами: диффом
  коммита `644b95e27b` записи ревью и прямым чтением события
  (`gh api repos/PRO-Robotech/kacho/issues/comments/5690585262`). Редакция 1 утверждала обратное
  («санкции ею не выдано») — это было следствием просроченной ревизии, а не чтением записи.
  Ветка Ф11 при этом по-прежнему **в ствол не влита**, поэтому документ ссылается на неё как на
  **одобренное внешним событием и не влитое**, а не как на норму ствола; что из этого следует
  для порядка работ — §9
- **Соседи по контракту:** F4d
  (`PRO-Robotech/kacho-workspace:docs/specs/sub-phase-F4d-identity-provider-posture-acceptance.md`)
  и F4e
  (`PRO-Robotech/kacho-workspace:docs/specs/sub-phase-F4e-phishing-resistant-sign-in-acceptance.md`),
  обе APPROVED; ID-MAIL-1 там же; ID-PW-1 — `docs/engineering/acceptance/password-verifier-follows-the-stored-value.md`,
  APPROVED 2026-09-16. Обойдены **по свойству, обоими языками**; расхождение и исход по каждому
  — §4.4
- **Сервис:** `kaname` — сервер церемонии, хранилище ключей, проверка утверждения, привязка,
  перенос. Край и консоль — потребители: экран церемонии рисует Ф8, полосу входа без пароля
  ведёт Ф13, значение уровня объявляет Ф11
- **Формат:** Given-When-Then, только markdown — кода здесь нет по построению
- **Публичный репозиторий:** документ написан так, чтобы выдерживать публикацию — §11

---

## §0 Обзор

Ключи доступа у продукта **уже есть у живых людей** и целиком принадлежат чужому компоненту: его
настройка объявляет привязку, его сервер проводит церемонию, его хранилище держит открытые ключи.
В нашем дереве нет ни библиотеки, ни таблицы, ни строки кода, которая проверила бы хоть одну
подпись (§1.1). Эпик снимает этот компонент целиком — значит либо ключи переезжают вместе с
предметом, либо трое людей теряют способ входа, который у них работает сегодня.

Документ закрепляет девять вещей. **Ключ — не способ входа, а его экземпляр**: строка своей
таблицы, ключуемая идентификатором удостоверения, потому что человеку положено иметь несколько
ключей, а строка способа по построению одна на пару «человек, вид» (Р1). **Величин контракта три**: имя
доверяющей стороны, перечень происхождений и перечень алгоритмов открытого ключа; перечень
происхождений объявляет наша настройка, а не заголовок запроса, и незаданная величина роняет
старт у каждой из трёх (Р2). **Три живых ключа переносятся**, и перенос возможен ровно
пока имя доверяющей стороны не меняется; пересоздание — не альтернатива решению, а исход
переписи источника (Р3). **Аттестация не требуется и не хранится**: ступень «3» решают флаги
предъявления, и это решила Ф11, а сюда передала только политику ключей (Р4). **Регистрация ключа
— действие в окне свежести**, и остаток, которого свежесть не лечит, назван, а не умолчан (Р5).
**Счётчик подписи судится односторонне**: ноль не судится, положительный обязан расти (Р6).
**Держателем свойства объявлен свой набор проб**, а сертификационный прогон держателем не
является — у него ноль вызывающих и предмет, который снимается (Р7). **Число ключей человека
ограничено своим потолком**: ключ — ресурс, а не значение поля-дискриминатора, поэтому вид учёта
у него свой, величину объявляет посадка, и умолчания нет (Р8). **Регистрация ключа предъявлением
не является**: множество предъявленного в сессии она не пополняет, и это вторая половина того,
что Ф11 §6 передала сюда (Ф7-33 — первая половина в Р5).

### §0.1 Что уже решили Ф1 и Ф11 — названо номером, не переизлагается

| где | что решено | что из этого здесь |
|---|---|---|
| **Ф1 §6** | беспарольный вход ключом доступа вынесен целиком: «0 сценариев, своя фаза», условие возврата — Ф13 `#1282` | Ф1 ключей **не решает**; настоящий документ не является правкой Ф1 и ни одного её сценария не отменяет |
| **Ф1 §7 инв. 4** | ни одна величина профиля не подставляется молча; незаданная — отказ старта | Р2 и Ф7-13: перечень происхождений незадан — отказ старта, а не «принимаем любое» |
| **Ф1 §7 инв. 3** | ни один отказ не является оракулом существования — по телу и по времени (Ф1-48…50) | Ф7-09, Ф7-27: отказ по неизвестному ключу побайтово равен отказу по негодной подписи; тот же критерий времени |
| **Ф1 §4.1** | окно свежести правки своих данных — 15 минут; владелец величины — наш контракт | Р5: регистрация и снятие ключа — действия этого окна |
| **Ф1 §1.5** | уровень уверенности сессия обязана нести сразу; неназванный уровень отвергается полом, а не проходит молча | §1.4: ключ, чей уровень не доезжает, гасит браузерную полосу — это отказ, а не ослабление |
| **Ф11 Р2** | уровень — наивысшая строка правила по МНОЖЕСТВУ предъявленного; утверждение ключа (любое) даёт «2» | §3.8: Ф7 производит **флаги**, значение уровня — Ф11 |
| **Ф11 Р3** | «3» — ключ с проверкой пользователя, НЕ допускающий резервного копирования; различитель берётся из флагов ЭТОГО утверждения, а не из памяти о регистрации | Р4 целиком: аттестация сильнее флага и дороже, и Ф11 передала её сюда явно |
| **Ф11 Р8** | словарь способов предъявления закрыт, `webauthn` в обеих ролях называется одним литералом | Р1: этот словарь — **не** словарь видов хранимого способа; пересечение литералов названо, чтобы его не приняли за один словарь |
| **Ф11 Р6, §5.1 п.6** | якорь окна свежести — момент последнего предъявления; окно **на действии** несёт владелец действия, и для ключа владелец назван — Ф7 | Р5 |
| **Ф11 §6** | «правило заведения ключа» и «аттестация как доказательство аппаратности» вынесены с носителем **Ф7 `#1273`** — «обязаны ответить на это своей приёмкой» | Р4 и Р5: это не инициатива документа, а переданное обязательство |
| **Ф11 Р9** | страж посадки `own` считает предъявимые уровни правилом; пока провязан один пароль, посадка отказывает в старте всюду, где каталог требует «2» — **27 записей** | §9: Ф7 либо Ф12 — предусловие посадки `own`; порядок назван, а не выведен из красного |

### §0.2 Что APPROVED этого документа разрешает — и чего не разрешает

**Разрешает:** кодировать объём §4.1 в фазе Ф7 `#1273` — и только то в ней, что называет
перепись §5. Разрешение нужно именно отсюда: строка Ф7 в теле эпика `#1266` несёт одну строку
(«ключи доступа (WebAuthn)»), ведомость Ф1 §5 фазы Ф7 **не содержит вовсе**, а Ф1 §0.2 прямо
запрещает считать разрешённым то, чего в её §5 нет.

**Не разрешает:** полосу входа без пароля и её последствия (Ф13 `#1282`), значение уровня
уверенности и его правило (Ф11 `#1280`), второй фактор и политику его обязательности
(Ф12 `#1281`), экран церемонии (Ф8 `#1274`), хранилище сессий и их отзыв (Ф3 `#1269`),
проверяющего пароля и перенос личностей (Ф2 `#1268`), снятие чужого компонента (Ф10 `#1276`).
У каждого пункта §4.2 назван свой носитель.

### §0.3 Почему документ лежит здесь

Дом приёмки — решение (`polyrepo.md` §«У приёмки домов ДВА»): предмет, целиком лежащий внутри
одного сервиса, описывается рядом с его кодом. Сервер церемонии, хранилище ключей, проверка
утверждения и перепись переноса — предмет `kaname`. Ни одного ребра к другому сервису документ
не заводит: край читает наш ответ, а не наши таблицы, и это уже устроено Ф1 и Ф9.

Координаты чужих домов пишутся с приставкой `владелец/репозиторий@ревизия:`. **Вставлять
приставку в оболочку нельзя:** `git grep` примет её как часть пути-спецификации, исполнится,
найдёт ноль и промолчит. Чтобы прогнать — исполняй в рабочей копии названного дома, сняв
приставку.

**Что с этими координатами делает гейт дома — сказано по замеру, а не по названию гейта.**
`TestAcceptancePathCoordinateResolves` чужие координаты-пути **не судит**: он их СЧИТАЕТ и
печатает их дома. Названные **прозой** (а здесь все четыре названы прозой — см. абзац выше) он не
видит вовсе. Держатель у них поэтому один — **привязка ревизией**, и она делает утверждение
срочным, а не вечным. Полный разбор с числами переписи и с тем, что гейт делает по-другому для
имён проб, — **§4.7**.

---

## §1 Перепись производителей: что в дереве СЕГОДНЯ производит ключ

Перепись составлена **до** единого сценария — класс «Тогда без производителя» отверг эту линию
больше двух раз, поэтому он шаг работы, а не замечание ревью
(`multi-agent-flow-orchestration.md` §14а). Единица счёта названа у каждого предиката, дом —
строкой над командой.

### §1.1 Своего сервера ключей нет: ни библиотеки, ни таблицы, ни проверяющего

```sh
# ДОМ: PRO-Robotech/kaname · единица — ОБЪЯВЛЕНИЕ зависимости в пине
grep -cE 'webauthn|fido|go-webauthn' go.mod
#   → 0 — библиотеки проверки утверждения в пине нет
# единица — ФАЙЛ прод-дерева, называющий предмет
git grep -lil 'webauthn' -- 'internal' ':!*_test.go' | wc -l
#   → 4
# КОНТРОЛЬ в обратную сторону — предикат находить умеет:
git grep -lil 'password' -- 'internal' ':!*_test.go' | wc -l
#   → непусто
```

Четыре файла — **читатели**, ни одного производителя. Все четыре читают литерал `webauthn` как
значение, пришедшее извне: условие модели прав, обогащение токена и его обновление, вердикт
условного отношения. Ни один не разбирает данные аутентификатора, не проверяет подпись и не
хранит открытый ключ.

```sh
# ДОМ: PRO-Robotech/kaname · единица — ОБЪЯВЛЕНИЕ таблицы
grep -ohE 'CREATE TABLE kaname\.[a-z_]+' internal/migrations/*.sql | sort -u | wc -l
#   → 49 — таблиц всего
grep -ohE 'CREATE TABLE kaname\.[a-z_]+' internal/migrations/*.sql | sort -u \
  | grep -iE 'key|credential|webauthn|passkey'
#   → CREATE TABLE kaname.token_signing_keys — ЕДИНСТВЕННОЕ попадание, и оно НЕ о ключе
#     доступа: это ключи НАШЕЙ подписи. Ключей доступа в хранилище ноль
```

**Ноль здесь честный, и единственное попадание названо именем намеренно.** Предикат по слову
`key` даёт таблицу подписанта, а не ключа доступа; не назвав её, перепись оставила бы читателю
вывод «что-то есть». Это тот же приём, которым Ф1 §1.1 назвала ведомость отзыва: у нас есть
ведомость и нет того, что она отзывает.

### §1.2 Производитель привязки — настройка чужого компонента, и величины у неё две

Шаблон настройки поставщика личности живёт в дереве платформы; в доме службы этого файла нет —
предикат `git ls-files | grep -c _kratos-identity.tpl` даёт здесь 0. Координата:
`PRO-Robotech/kacho@45d1719fe8:deploy/helm/umbrella/charts/kaname/templates/_kratos-identity.tpl`.

Блок ключей этого шаблона объявляет сегодня:

| величина | сегодня | кто объявляет после Ф7 |
|---|---|---|
| имя доверяющей стороны | корневой домен, eTLD+1 — «credentials работают для всех поддоменов» | наш контракт, Р2 |
| перечень происхождений | **один** элемент: происхождение консоли | наш контракт, Р2 |
| видимое имя доверяющей стороны | `"Kacho Cloud"` | наш контракт, Р2 |
| беспарольный режим | включён — ключ объявлен ПЕРВЫМ фактором | Ф13, не здесь |

Комментарий шаблона называет основание выбора eTLD+1 нормой WebAuthn Level 3 §5.1.3 и
происхождение — «строго совпадает с происхождением консоли». Обе величины — **часть контракта
ключа**, а не настройка развёртывания: их смена делает существующие ключи негодными (§3.3), и
именно поэтому Р2 забирает их в контракт, а не оставляет профилю.

### §1.3 Посадка без доменного имени ключи ВЫКЛЮЧАЕТ — это факт о посадке, а не о фазе

Тот же шаблон на посадке без доменного имени печатает ключи выключенными, с разбором: имя
доверяющей стороны требует валидного доменного имени, у литерала адреса его нет.

**Следствие для этого документа названо здесь, чтобы его не приняли за поломку:** ни один
сценарий §3 не исполним на посадке без доменного имени. Такой прогон — **«условие не создано»**,
третья категория (`testing.md` §E2E, `e2e-flow.md` §6), а не красный вердикт и не зелёный. Гейт,
объявляющий выключенность выразимой, в платформе уже стоит —
`PRO-Robotech/kacho@45d1719fe8:deploy/identity_domainless_landing_is_expressible_test.go`, — и после Ф7 его
предмет переезжает к нам вместе с величинами Р2.

### §1.4 Уровень ключа сегодня — «1», и источники нормы называют три разных значения

Объявление способов церемонии консоли
(`PRO-Robotech/kacho@45d1719fe8:ui-future/shared/src/lib/step-up-methods.ts`) говорит дословно: «`passkey`
(и `webauthn` в беспарольной посадке) — ПЕРВЫЙ фактор, а не второй: служба личности выдаёт по
нему `aal1` и в потоке `aal=aal2` не предлагает его вовсе».

То есть **сегодняшний производитель уровня даёт ключу «1»**. Рядом три нормы называют другое:

| источник | состояние | ключ получает |
|---|---|---|
| поведение поставщика (объявление консоли выше) | факт дерева | **«1»** |
| F4d Р6, F4e Р1 | APPROVED | **«3»**, без условия |
| тела `#1280` (Ф11) и `#1282` (Ф13) | задачи | «2», а при проверке присутствия — «3» |
| **Ф11 Р2 и Р3** | решено, в ствол не влито | «2» любому утверждению; «3» — только с проверкой пользователя и без признака резервного копирования |

**Выбор сделан не здесь.** Его сделала Ф11 Р3, и она же назвала, что заменяет в двух одобренных
приёмках (её Р10 п.1). Настоящий документ этот выбор **не пересматривает** и от него зависит
ровно одним способом: правило Ф11 читает флаги, а флаги производит проверка ключа — то есть Ф7
(§3.8, Р4). Исход расхождения по каждому соседу — §4.4.

**Величина, на которую опирается §9, измерена здесь:**

```sh
# ДОМ: PRO-Robotech/kaname · единица — ЗАПИСЬ каталога разрешений, несущая пол уровня
python3 -c "
import json,collections
d=json.load(open('internal/apps/kaname/seed/embedded/permission_catalog.json'))
def w(o):
    if isinstance(o,dict):
        if 'required_acr_min' in o: yield o['required_acr_min']
        for v in o.values(): yield from w(v)
    elif isinstance(o,list):
        for v in o: yield from w(v)
print(len(d), collections.Counter(w(d)))"
#   → 338 записей каталога, из них с полом: «1» — 284, «2» — 27, «3» — 0
```

Записей с полом «3» — **ноль**. Значит различие «3» и «2» сегодня арендатором **не наблюдаемо**,
и это несущий факт для Р4 (§10 п. 5), а не подробность.

### §1.5 Строка способа входа второго ключа не выражает — и это верно, а не дефект

```sh
# ДОМ: PRO-Robotech/kaname, ветка issue-1268-p1-login-method @ 0fdacffc2a — В СТВОЛ НЕ ВЛИТА;
# в стволе предикат ниже даёт 0, и это факт о СТВОЛЕ, а не об отсутствии работы
git grep -l 'user_login_methods' -- 'internal' | wc -l
#   → на стволе 0 · на ветке непусто
# Счёт через `| wc -l` здесь намеренный: голый `git grep -c` при нуле совпадений НЕ ПЕЧАТАЕТ
# НИЧЕГО и выходит кодом 1, то есть ноль становится неотличим от промаха путём. Дом назван
# строкой выше, чтобы читатель знал, где команду исполнять, а не выводил это из пустоты.
```

Ветка Ф2 П1 заводит таблицу способов входа с первичным ключом **по паре** «человек, вид»,
единственным скалярным полем проверочного материала, закрытым словарём вида (сегодня — один
литерал `password`) и следствием: второй способ того же вида отвергает база. Это ровно то, чего
требует одобренная F4d-14, и для пароля и одноразового кода это верно: у человека один
проверочный материал на вид.

**Для ключа это неверно by construction.** Человеку положено иметь несколько ключей — на машине,
на телефоне, аппаратный про запас; на этом стоит весь путь восстановления беспарольного входа
(`#1282` прямо требует ответить, что делать с человеком, у которого ключ утрачен). Пара «человек,
вид» второго ключа не выражает.

**Второе свойство той же строки, названное отдельно, потому что оно сильнее первого.** Поле
проверочного материала объявлено СЕКРЕТОМ: комментарий таблицы это говорит, статистика
планировщика по нему не собирается, индекса нет **никакого и намеренно** (индекс копирует колонку
в свои страницы, а уникальный делает каждый отказ уникальности каналом выдачи), и два гейта ветки
требуют, чтобы имя таблицы называл ровно один файл дерева. Открытый ключ секретом **не является**,
а искать его приходится **по идентификатору удостоверения, который присылает вызывающий**, — то
есть по индексированной колонке. Положить ключ туда значит либо завести индекс там, где его
запретили с доводом, либо читать все строки на каждое предъявление.

Отсюда Р1, и он **не правит F4d-14**: см. §4.4.

### §1.6 Популяция: ни один замер не считает КЛЮЧИ

Замеров живой базы поставщика два, и оба чужие — этой редакцией **не перемерены** (почему —
ниже). Единицы счёта у них разные, и это надо назвать раньше чисел.

| замер | дата | что названо | единица, как её называет автор |
|---|---|---|---|
| тела эпика `#1266` и `#1281` | 2026-08-25 | `password = 80`, **ключи доступа: 3 личности**, `totp = 0`, `lookup_secret = 0`; «схемы: default 77 · kacho_user_v2 3» | **личность** |
| контроль ID-PW-1 §1.3 | 2026-08-27 | `password` 83 · **`webauthn` 6** · `lookup_secret` 1 | **строка способа входа** у поставщика |

**Числа разошлись по всем трём осям сразу** (+3 у пароля, +3 у ключа, +1 у резервного кода), и
разрешать это выбором правдоподобного нельзя (`writing.md` §2). Механизм **не установлен**: двух
дней хватило бы и на три новые личности со всеми тремя способами, и на иную единицу счёта у
эпика. ID-PW-1 §1.4 уже разбирала такое же расхождение по паролю и оставила его неустановленным
— здесь повторяется тот же вывод и по тем же причинам.

**Но для этого документа важно другое, и оно верно при любом объяснении: ключей не считает ни
один из двух.** Первый считает людей, второй — строки способа у поставщика. Число ключей у нас
**не измерено ни разу**, а именно оно решает цену переноса: человек с тремя ключами и человек с
одним стоят по-разному ровно втрое.

**Заголовок задачи «три живых ключа» поэтому есть число ЛИЧНОСТЕЙ, а не ключей**, и документ не
пользуется им как числом ключей нигде. Из него следует лишь верхняя граница радиуса: людей,
которых затронет пересоздание, — три, и **тело задачи `#1273`** говорит дословно: «три человека
переживут пересоздание».

**Цитата принадлежит задаче, а НЕ эпику, и редакция 1 приписывала её эпику** — то есть решению
владельца, а не собственной постановке фазы, чем повышала её полномочие. Снимается предикатом с
контролем: `gh issue view 1266 -R PRO-Robotech/kacho --json body -q .body | grep -c 'переживут'`
→ **0** при `grep -c -i 'ключи'` → **2** (тело прочитано, эпик говорит другое — «ключи доступа:
3 личности», то есть число **личностей**). Та же фраза в теле `#1273` — есть; поиск по трём
репозиториям (`gh search issues 'переживут пересоздание' --repo PRO-Robotech/<репо>`) даёт
`kacho` 1 · `kaname` 0 · `kacho-workspace` 0.

**Почему не перемерено.** Стенда с этой популяцией в досягаемости редакции нет: kube-контекстов в
среде ноль, а поднятые на машине кластеры чужие — чужое состояние не трогают
(`multi-agent-flow-shared-tree.md` §13). Это **«не выполнилось»**, а не «ноль». Не блокирует
потому, что ни один сценарий §3 на эти числа не опирается: Ф7-21 требует переписи **перед
переносом**, и решает она, а не эта таблица.

### §1.7 Сертификационный прогон в дереве есть, вызывающих у него ноль, предмет снимается

Прогонщик сертификации существует в дереве платформы — 148 строк, координата
`PRO-Robotech/kacho@45d1719fe8:deploy/tests/conformance/fido/run-fido-conformance.sh`. Перепись вызывающих
(единица — строка дерева, называющая прогонщик **не** в нём самом) даёт **ноль**: единственное
упоминание имени — строка примера в его собственной справке. Сверх того:

- он требует **JAR, выдаваемый по членству в FIDO Alliance**, и при его отсутствии выходит кодом
  **3** — то есть «не выполнилось», а не красное. Разделение исходов у него верное, и его стоит
  сохранить при переносе, если перенос когда-нибудь случится;
- его предмет — **эндпоинты снимаемого компонента**: шапка говорит «against deployed Kratos
  WebAuthn endpoints». После Ф10 у него не останется, к чему обращаться;
- имя доверяющей стороны он берёт ручкой либо из переменной среды — то есть **не из нашего
  контракта**, и после Р2 разошёлся бы с ним молча.

Вывод, из которого вырос Р7: **этот прогон держателем свойства не является и им не станет**. Он
не исполнялся ни разу, судит чужой предмет и требует условия, которого у нас нет. Держатель —
свой набор проб, и его состав назван §8 по свойству, а не числом.

### §1.8 Согласие устройства: АТРИБУТА условия никто не производит, а ЗНАЧЕНИЕ-утверждение производится — и оба носителя обещания привязаны к ключу

```sh
# ДОМ: PRO-Robotech/kaname · единица — ОБЪЯВЛЕНИЕ условия в каноне
grep -cE '^condition ' internal/authzmodel/fga_model.fga
#   → 6
# единица — УСЛОВИЕ, навешенное хотя бы на одно отношение
for c in $(grep -oE '^condition [a-z_]+' internal/authzmodel/fga_model.fga | awk '{print $2}'); do
  printf '%-22s %s\n' "$c" "$(grep -c "with $c" internal/authzmodel/fga_model.fga)"; done
#   → mfa_fresh 3 · non_expired 0 · source_ip_in_range 0 · jit_window 0 · business_hours 0
#     · device_compliant 0
```

**Обе величины печатаются намеренно.** «Навешено ноль» у согласия устройства — не особенность
этого условия, а состояние **пяти из шести**. Одно число скрыло бы ровно это, и вместе с ним —
довод, по которому Ф7 берёт себе не условие, а одну строку (ниже).

**Единиц здесь ДВЕ, и редакции 1 и 2 считали их за одну.** «Атрибут условия» и
«значение-утверждение, которое продукт выдаёт о согласии устройства» — разные предметы, у них
разные имена и разная судьба; сведи их в одну единицу, и вторая окажется ничьей. Перепись
печатает обе.

```sh
# ЕДИНИЦА 1 — СТРОКА прод-дерева, называющая АТРИБУТ условия
git grep -n device_attestation -- internal ':!*_test.go'
#   → 4 строки в 2 файлах: объявление условия в каноне (2) и `internal/service/authorize_service.go` (2)
#   → производителей атрибута — 0: единственный сборщик контекста его УДАЛЯЕТ (ниже)
# ЕДИНИЦА 2 — МЕСТО прод-дерева, выставляющее ЗНАЧЕНИЕ-УТВЕРЖДЕНИЕ о согласии устройства.
# Имя у него ДРУГОЕ (`kaname_device_compliance`), и предикат единицы 1 его не видит вовсе:
grep -cE 'device_attestation|device_compliant|DeviceAttestation|attestation' \
  internal/service/token_enrichment_service.go   # форма редакции 2      → 0
grep -cE 'device_compliance|DeviceCompliance|attested' \
  internal/service/token_enrichment_service.go   # ИМЕННАЯ форма          → 7
git grep -n 'kaname_device_compliance' -- internal ':!*_test.go'
#   → 6 мест, все в `internal/service/token_enrichment_service.go`:
#     ПЯТЬ полос выдачи ставят «unknown» (userClaims · saClaims · federatedClaims ·
#     userTokenClaims · MinimalClaims), ОДНА переводит значение в «attested»
```

В `authorize_service.go` имя стоит в перечне **серверно-авторитетных** ключей, и сборка контекста
условия **удаляет** его из того, что прислал вызывающий; комментарий там же говорит зачем — иначе
вызывающий объявлял бы о себе то, чего не доказал. Атрибут поэтому остаётся **отсутствующим**, а
зависящее от него условие не выполняется **ни при каком входе**: fail-closed by construction.
Удаление здесь **верное**, и снимать его нельзя.

> [!warning] Редакция 1 назвала верный ФАЙЛ и неверный МЕХАНИЗМ; редакция 2 объявила файл
> чистым — и обе ошиблись одним и тем же способом, слепым распознавателем
> Редакция 1 стояла на «значение выводится из **поля запроса** вызывающего» — это ложно.
> Редакция 2 сняла утверждение целиком предикатом `grep -cE
> 'device_attestation|device_compliant|DeviceAttestation|attestation'` → **0** — и это тоже
> ложно: предикат знает признаковую форму (`compliant`) и не знает **именной** (`compliance`),
> знает `attestation` и не знает `attested`. Та же форма даёт **7** (выше). Круг 1 ревью назвал
> это блокирующим Б1, и перемер этой редакции **подтвердил** находку.
>
> **Производитель значения ЕСТЬ, и вход у него — сам факт ключа:**
> `claims["kaname_device_compliance"] = "attested"` под условием, что среди **выданных областей**
> токена есть `webauthn` либо `passkey`. То есть продукт утверждает **аттестованность
> устройства** на основании наличия ключа — ровно ту проверку, которую Р4 решает не строить, — и
> делает это **кодом**, а не строкой страницы.
>
> **Класс назван точно, и он тот самый, которым документ гордится** (`testing.md` §«Гейт на
> класс», п. 7): распознаватель, знающий одну форму записи предмета, **не краснеет и не
> зеленеет — он молчит**. Экранированную форму подчёркивания редакция 2 поймала (ниже, радиус),
> именную — нет. Отсюда правило для следующего перемера: форм записи у имени этого предмета в
> дереве **три** — `device_attestation` · `device_compliant` · `device_compliance` (плюс
> экранированная и значение `attested`), и перепись обязана идти всеми.

**Класс поэтому другой, и называть его надо точно.** Это не «принято-и-проигнорировано» (поле
принимают и не читают), а **«неисполнимая возможность»** (`api-conventions.md` §«Неисполнимая
возможность: ДВА ПРАВИЛА ОБ ОДНОМ ПОЛЕ»): одно правило требует атрибут, второе правило того же
тракта его отвергает, исполнимого входа не существует ни при каком входе. Второе правило —
**верное**. Неверна **пара** «объявленное условие + публичная строка, обещающая по нему проверку
ключа»:

```sh
# ДОМ: PRO-Robotech/kaname · единица — СТРОКА таблицы условий публичной страницы
A='/<thead><tr><th>Условие<\/th>/,/<\/tbody>/'
awk "$A" docs/content/architecture/authz.mdx | grep -c '<tr><td>'
#   → 6 — строк таблицы
awk "$A" docs/content/architecture/authz.mdx | grep -c 'device-attestation'
#   → 1 — из них обещающих проверку АТТЕСТАЦИИ КЛЮЧА
```

Строка обещает арендатору сверку идентификатора модели **ключа** со списком одобренных — ровно ту
проверку, которую Р4 решает не строить.

**Радиус измерен ВСЕМИ ТРЕМЯ формами имени, и он шире того, что называла редакция 2.**

```sh
# ДОМ: PRO-Robotech/kaname · единица — ОТСЛЕЖИВАЕМЫЙ ФАЙЛ, называющий предмет.
# Артефакты САМОГО КРУГА исключены: этот документ и запись ревью цитируют предикат целиком и
# потому совпадают с ним всегда — самопопадание, а не место дерева. Их два, и они названы,
# чтобы «15» и «17» не читались как расхождение.
EX='^docs/(specs/reviews|engineering/acceptance/access-keys-are-ours)'
git grep -lE 'device_compliant|device&#95;compliant|device_attestation|allowed_attestations' \
  | grep -vE "$EX" | wc -l
#   → 10 — форма редакции 2 (признаковая + экранированная)
git grep -lE 'device_compliant|device&#95;compliant|device_attestation|allowed_attestations|device_compliance|device&#95;compliance|DeviceCompliance' \
  | grep -vE "$EX" | wc -l
#   → 15 — прибавляет ИМЕННУЮ форму: `token_enrichment_service.go` и ЧЕТЫРЕ пробы, которые
#     это значение утверждают (`hook_lanes_claim_parity_test.go` · `refresh_hook_handler_test.go`
#     · `token_hook_handler_test.go` · `token_enrichment_usertoken_test.go`)
# С артефактами круга те же предикаты дают 12 и 17 — величина зависит от того, лежит ли уже в
# ветке запись ревью, поэтому единица счёта названа, а не подразумевается
```

**Число росло дважды, и оба раза от расширения распознавателя, а не от правок дерева:** 2 (редакция
1) → 11 с экранированной формой (редакция 2, из них документ — один) → **15 мест дерева** с именной
(редакция 3). Прибавка каждый раз была **слепой зоной**, а не регрессией: перепись выросла, а полоса
прежней формы не изменилась — признак, которым `testing.md` §«Гейт на класс», п. 7 велит это
различать.

| место | что там сегодня | чей предмет |
|---|---|---|
| `docs/content/architecture/authz.mdx` | строка таблицы обещает сверку модели **ключа** | **Ф7** — Ф7-32 |
| `internal/service/token_enrichment_service.go` | **производитель значения-утверждения**: пять полос ставят «unknown», одна выводит «attested» из наличия области `webauthn`/`passkey` среди выданных | **Ф7** — Ф7-39: это ВТОРОЙ носитель того же обещания, и вход у него — сам ключ |
| `internal/handler/iamhooks/{hook_lanes_claim_parity,refresh_hook_handler,token_hook_handler}_test.go` · `internal/service/token_enrichment_usertoken_test.go` | четыре пробы, утверждающие это значение на своих полосах | **Ф7** — правятся тем же изменением, что Ф7-39; проба, переживившая свой предмет, утверждала бы снятое |
| `internal/authzmodel/fga_model.fga` · `proto/kaname/cloud/iam/v1/fga_model.fga` | объявление условия в двух побайтово равных копиях (равенство держит `TestEmbeddedModelIsByteIdenticalToCanonical`) | область прав: условие про **устройство**, не про ключ — §10 п. 11 |
| `internal/service/authorize_service.go` | имя в перечне серверно-авторитетных ключей и два комментария | область прав; удаление клиентского значения **верное**, снимать нельзя |
| `internal/service/authorize_context_test.go` | проба, утверждающая это удаление | она держит **верное** свойство |
| `internal/domain/constants_extended.go` | константа имени условия; читателей **0** — как и у четырёх её соседей по блоку | область прав — §10 п. 11 |
| `proto/.../authorize_service.proto` · `pkg/api/.../authorize_service.pb.go` | **строка комментария** в перечне принимаемых ключей контекста. Поля с номером у этого имени **нет** — это ключ внутри `google.protobuf.Struct`, поэтому `reserved` здесь предметом не является | область прав: строка называет **пять** ключей, и сервер удаляет **все пять** |
| `docs/content/api/authorize.mdx` | тот же перечень пяти ключей на публичной странице | то же: выбрать один из пяти значило бы закрыть экземпляр и оставить класс |
| `docs/engineering/acceptance/composed-model-admits-only-what-it-owns.md` (**APPROVED**) | посылка §2.1 «условий канона **6**», перечисляет `device_compliant` поимённо | **её круг**, а не побочный эффект Ф7: правка одобренного документа отвязывает его вердикт от отпечатка (`change-graph.md` §2) |

**Предмет Ф7 — ДВА НОСИТЕЛЯ ОДНОГО ОБЕЩАНИЯ, а не одна строка; редакция 2 говорила «ровно одна
публичная строка», и это опровергнуто замером выше.** Обещание проверки, которой нет, продукт
делает дважды: **строкой публичной страницы** (Ф7-32) и **значением токена**, выводимым из наличия
ключа (Ф7-39). Оба привязаны к КЛЮЧУ — первый по модели ключа, второй по факту его наличия, — и
поэтому оба принадлежат Ф7 по одному и тому же доводу. Снять один и оставить другой значило бы
закрыть экземпляр и оставить класс (`testing.md` §«Гейт на класс», п. 1): арендатор по-прежнему
получал бы утверждение об аттестации устройства, не подкреплённое ничем.

**Что при этом Ф7 по-прежнему НЕ трогает, и это решение.** Само **объявление условия** в каноне.
Снять его значило бы разом: выбрать одно из **пяти** ненавешенных условий (закрыть экземпляр,
оставить класс), тронуть канон, чья перепись есть посылка **одобренного** соседа, и сделать это
побочным эффектом фазы о ключах. Значение `kaname_device_compliance` при этом **не исчезает** —
оно остаётся на всех шести местах со значением «unknown»; исчезает **деривация** «attested», то
есть ровно утверждение, а не поле. Цена сужения названа числом в §10 п. 11, а не умолчана.

**Цена снятия деривации измерена, и она нулевая для вердикта.** Значение доезжает до края
(`PRO-Robotech/kacho@45d1719fe8:gateway/internal/middleware/context_extractor.go` — читателей у
претензии в дереве платформы **два файла**, оба там), край переводит его в `device_attestation`
контекста запроса, а сборщик контекста службы этот атрибут **удаляет** — то есть цепь мертва на
последнем шаге при любом значении. Меняется поэтому ровно одно: продукт перестаёт утверждать
арендатору то, чего не проверял.

**Радиус назван, следствия — нет.** Отношений с этим условием ноль, поэтому сегодня значение
ничего не решает; разбор того, чем это было бы при ненулевом радиусе, в публичный текст не идёт
(§11).

### §1.9 Сводка

| | обещано эпиком | дерево службы сегодня | что отсюда следует |
|---|---|---|---|
| проверяющих утверждения ключа | 1 | **0** | Ф7 пишет первого |
| таблиц под ключи | 1 | **0** из 49 | Ф7 заводит первую |
| производителей привязки и величин контракта | наш контракт | **чужая настройка**; перечня алгоритмов не объявляет никто | Р2 забирает **три** величины |
| уровень ключа | «2»/«3» | **«1»** | различитель — флаги, Ф11 Р3 |
| число живых ключей | «три» | **не измерено ни разу** | Ф7-21, перепись до переноса |
| потолок числа ключей человека | — | **вида учёта нет**: множество `kaname.own_ceilings` закрыто тремя именами, ключа среди них нет | Р8 заводит четвёртое; механизм списания уже есть |
| держатель свойства | сертификационный прогон | **ноль вызывающих, чужой предмет** | Р7, свой набор проб |
| производителей **атрибута условия** согласия устройства | — | **0**: единственный сборщик контекста его **удаляет** | fail-closed верен, снимать нельзя |
| производителей **значения-утверждения** о согласии устройства | обещано публичной страницей | **1 файл, 6 мест**: пять ставят «unknown», одна выводит «attested» из наличия ключа | Ф7-39 снимает **деривацию**; Ф7-32 — строку страницы; условие не трогается |
| условий канона, навешенных хотя бы на одно отношение | — | **1 из 6** | §10 п. 11: Ф7 снимает ноль условий, и это решение |

---

## §2 Решения — ратифицировать явно, ревьюер проверяет каждое

### Р1. Ключ — строка СВОЕЙ таблицы; строки способа вида `webauthn` не заводится

**Ключ доступа хранится строкой отдельной таблицы, ключуемой идентификатором удостоверения.**
Строка несёт: человека, идентификатор удостоверения, открытый ключ и его алгоритм, счётчик
подписи, названное человеком имя ключа, момент заведения и момент последнего успешного
предъявления. Идентификатор удостоверения **уникален глобально** — так его определяет норма
WebAuthn, — поэтому уникальность держит он, а не пара с человеком.

**Признака резервного копирования в строке НЕТ, и это решение.** Редакция 1 держала его колонкой
«на момент регистрации»; колонка снята по трём доводам, каждый самостоятелен:

1. **читателя у неё не было ни одного.** Ни одно «Тогда» §3 её не наблюдает: все три сценария
   флагов (Ф7-28…30) читают флаги, пришедшие **с утверждением**. Предикат ревьюеру — у каждой
   колонки перечня выше назван сценарий §3, который её наблюдает: человек — Ф7-14 и Ф7-27,
   идентификатор — Ф7-05, Ф7-08, Ф7-09, Ф7-15, открытый ключ и алгоритм — Ф7-07 и Ф7-22, счётчик —
   Ф7-17…20, имя и момент заведения — Ф7-01, момент предъявления — Ф7-06 и Ф7-07. У снятой колонки
   такого сценария нет;
2. **её запрещает Ф11 Р3 дословно:** «различитель берётся из флагов, которые ключ сообщает В ЭТОМ
   утверждении, а не из памяти о его регистрации». Хранимая копия — ровно память о регистрации;
3. **она разошлась бы молча.** Флаг синхронизируемого ключа меняется со временем, а хранимая
   копия — нет: два места об одном предмете, и второе никем не сверяется.

Довод Р4 применим к ней дословно: собрать значение, не имея читателя, значит завести хранение без
предмета. **Условие возврата:** появится читатель — колонка возвращается как свойство
**последнего утверждения**, а не регистрации, и вместе с ней заводится сценарий, наблюдающий
**расхождение** хранимого признака с предъявленным. Без такого сценария колонка неотличима от
мёртвой.

**Строки в таблице способов входа вида `webauthn` не заводится, и словарь видов Ф2 не
расширяется.** Заводить её было бы нечем: у ключа нет одного скалярного проверочного материала,
а ограничение непустоты потребовало бы положить в поле материала то, что материалом не является,
— то есть заставить колонку лгать.

**Чем это отличается от F4d-14 — различие несущее, и оно НЕ является правкой F4d.** F4d-14
требует: «попытка завести второй способ **того же вида** отвергается уровнем базы». Требование
остаётся истинным и ненарушенным: **второго способа вида `webauthn` не бывает, потому что первого
нет**. Второй ключ не является вторым способом — он второй **экземпляр** одного способа, и
F4d-14 об экземплярах не высказывается ни словом. Проверяемый признак различия: после Ф7 предикат
F4d-14 («две строки одного вида у одного человека») по-прежнему даёт ноль, а предикат «две строки
ключа у одного человека» даёт столько, сколько ключей человек завёл. Оба верны одновременно —
значит противоречия нет, а есть две разные единицы.

**Кто это исполняет.** Таблицу ключей заводит **Ф7 `#1273`** своей миграцией. **Ф2 `#1268` не
трогается вовсе**: ни её таблица, ни её словарь, ни её гейты — у неё свой предмет, и вносить в
него ключ значило бы править чужую фазу под свою нужду.

**Два словаря с пересекающимися литералами — названы, чтобы их не свели в один.** Словарь Ф2
(вид **хранимого** способа, связанный с ограничением таблицы) перечисляет сегодня `password`,
после Ф12 — `totp`. Словарь Ф11 Р8 перечисляет виды **предъявленного**: `password`, `totp`,
`lookup_secret`, `webauthn`, `recovery_code`, и живёт вместе с правилом уровня. `webauthn` входит
во **второй** и не входит в **первый**; `recovery_code` тоже. Сведи их — и придётся либо завести
строку способа, которой нечем быть (выше), либо снять литерал из правила уровня, и тогда ключ
перестанет давать уровень вовсе.

**Что «способ входа ключом заведён» выводится из строк ключа, а не хранится вторым местом.**
Человек, у которого есть хотя бы один принятый ключ, умеет войти ключом; отдельного признака для
этого не заводится — он разошёлся бы со своим предметом при снятии последнего ключа.

### Р2. Величин контракта ТРИ: имя доверяющей стороны, перечень происхождений, перечень алгоритмов

**Три величины, и они разные, хотя первые две обе называются «привязкой».** Редакция 2 называла
две и при этом строила на третьей два сценария (Ф7-21, Ф7-22) и одну строку §8 — круг 1 ревью
назвал это блокирующим Б5, перемер подтвердил: у величины не было ни объявителя, ни стороны
«не задана».

| величина | что делает | где живёт после Ф7 | цена смены |
|---|---|---|---|
| имя доверяющей стороны | входит в подписываемые данные; ключ привязан к нему **при регистрации** | наш контракт, значение **переносится дословно** из §1.2 | **необратима**: каждый существующий ключ становится негодным, миграции у этого нет |
| перечень происхождений | сверяется с происхождением, которое назвал браузер, **на каждом** предъявлении | наш контракт, объявляется профилем; незаданный — **отказ старта** | обратима: перечень правится, ключи целы |
| **перечень алгоритмов открытого ключа** | объявляется церемонии **в испытании** и сверяется **дважды**: на приёме результата церемонии (Ф7-35) и предполётной переписью переноса (Ф7-21, Ф7-22) | наш контракт, объявляется профилем; незаданный — **отказ старта** (Ф7-13) | **частично необратима**: сужение делает негодными уже принятые ключи вне нового перечня — они не ломаются, но перестают приниматься, и перепись обязана назвать их числом |

**Почему перечень алгоритмов — величина КОНТРАКТА, а не подробность реализации.** Он решает,
какой ключ продукт готов принять, то есть **кого он впускает**; он же решает судьбу переноса
(Ф7-22 роняет перенос на непокрытом ключе). Величина, от которой зависит и приём, и перенос,
профилю не принадлежит — иначе две посадки одного продукта принимали бы разные ключи, не сказав
об этом ни строкой.

**Незаданный перечень алгоритмов — отказ старта, и по той же причине, что у происхождений.** Он
**разрешает**, поэтому умолчание здесь имеет ровно два вида, и оба негодны: «принимаем любой»
снимает предмет Ф7-22 целиком, «не принимаем ни одного» ломает церемонию. Пустого значения у него
не бывает — пустой перечень означает «церемония невозможна», и это не посадка, а поломка;
поэтому незаданное и пустое дают отказ старта оба, как у имени доверяющей стороны.

**Происхождение сверяется с ОБЪЯВЛЕННЫМ перечнем и никогда не берётся из запроса.** Вывести его
из заголовка запроса значит принять то, что назвал вызывающий, — и устойчивость к посреднику,
ради которой ключ и заведён, исчезает целиком, не оставив следа в поведении: годная полоса
продолжает работать.

**Незаданный перечень — отказ старта, а не «принимаем любое».** Это Ф1 §7 инв. 4 и ровно тот же
класс, что «пустой список — это „не сужаем", а не „запрещаем"» (`security.md`): перечень
происхождений **разрешает**, поэтому пустое значение означает «никого» и безопасно, а
**отсутствие объявления** означает «величину забыли» и обязано ронять старт. Различие между
пустым и незаданным здесь несущее, и Ф7-13 утверждает обе стороны.

**Почему имя доверяющей стороны переносится, а не выбирается заново.** Выбор здесь уже сделан и
уже исполнен живыми ключами. Смена значения — единственное необратимое действие этого документа:
она обесценивает материал, которого у нас нет второй копии. Перенос делает Р3 возможным; смена
делает Р3 невыполнимым.

### Р3. Три ключа ПЕРЕНОСЯТСЯ; пересоздание — исход переписи, а не альтернатива

**Решение: переносить.** Основание — свойство самого ключа, а не удобство: ключ привязан к имени
доверяющей стороны, а не к программе, которая проводила церемонию. Пока имя не меняется (Р2),
открытый ключ и идентификатор удостоверения остаются годными, и перенос есть копирование
материала, а не новая церемония.

**Но решение не полно без переписи, и перепись — часть решения, а не приложение к нему.** Ни один
существующий замер не считает ключи (§1.6), а перенести можно ровно то, чей материал источник
несёт. Поэтому:

1. **перепись источника прогоняется ДО переноса** и печатает объём осмотренного: людей, строк
   способа, ключей, и по каждому ключу — покрыт ли его алгоритм перечнем (Ф7-21);
2. **ключ, чьего материала источник не несёт**, — не переносится, и перепись **роняет перенос**,
   называя число и причину (Ф7-22). Это тот же ход, что PWV-12 у ID-PW-1: непокрытое находится
   переписью, а не людьми, которые не смогли войти;
3. **пересоздание — исход этой переписи**, объявленный заранее и стоящий трём людям одной
   церемонии каждому. **Тело задачи `#1273`** говорит дословно: «три человека переживут
   пересоздание» — это её постановка, а не слово эпика (§1.6, предикат с контролем);
4. **порядок необратим и назван прямо:** перенос идёт **до** снятия компонента. После снятия
   источника материала не существует, и «пересоздать» перестаёт быть исходом — становится
   единственным путём.

**Цена пересоздания названа, потому что она не нулевая.** У поставщика объявлен беспарольный
режим (§1.2): человек, чей единственный способ входа — ключ, после непереноса **заперт**. Поэтому
Ф7-24 требует, чтобы перепись называла таких людей отдельным числом, а перенос не объявлялся
состоявшимся, пока это число не ноль либо пока каждому из них не назван второй способ войти.

**Чего перенос НЕ переносит.** Счётчик подписи переносится, если источник его несёт, и
устанавливается в ноль, если нет (Р6 делает это безопасным). Флаги предъявления не переносятся
вовсе — они свойство **утверждения**, а не хранимой строки (Ф11 Р3), и приходят заново на каждом
предъявлении.

### Р4. Аттестация не требуется, не проверяется и не хранится

**Церемония регистрации запрашивает аттестацию «нет»**, и это решение, а не умолчание.

**Почему не требуется.** Ступень «3» решает Ф11 Р3 — по флагам **этого** утверждения. Аттестация
доказывала бы аппаратность сильнее флага, но её проверка требует внешнего авторитета — службы
метаданных о моделях аутентификаторов — **на пути запроса регистрации**. Тогда исходов два, и оба
плохи: fail-closed делает регистрацию ключа недоступной всякий раз, когда недоступен чужой
авторитет; fail-open делает проверку украшением, которое ничего не отвергает, — ровно класс
«форма проверки без содержания».

**Почему не хранится, хотя могла бы быть получена.** Аттестация несёт идентификатор модели
аутентификатора — данные об устройстве человека. Собрать их, не имея читателя, значит завести
хранение без предмета; а читатель появился бы только вместе с той самой службой метаданных.

**Почему запрашивается «нет», а не «запросим, но не проверим».** Запросить и не проверять —
собрать идентификатор модели и не воспользоваться им: цена для человека есть, выигрыша нет.
Отсутствие обязано быть представимо отдельно от значения, и здесь оно представимо тем, что поля
для аттестации в строке ключа **нет** (Ф7-31 проверяет это попыткой положить туда значение, а не
чтением).

**Что Ф7 обязана произвести взамен — и это и есть её вклад в уровень.** Флаги предъявления —
проверка пользователя и признак резервного копирования — доезжают до точки решения уровня в виде,
который читает правило Ф11 Р3, **на каждом** утверждении (§3.8). Ключ, не сообщающий флагов,
сообщает их нулями, и это отличимо от «флаги не прочитаны» (Ф7-30).

**Условие возврата названо и оно внешнее:** требование арендатора о допустимых моделях ключей.
Тогда заводится и служба метаданных, и поле, и политика, и **строка публичной страницы, снятая
Ф7-32**, — своим изменением и своей приёмкой, а не полем «на будущее».

**Исход класса §1.8 — здесь же, и он СУЖЕН до одной строки.** Снимается **обещание**: строка
публичной страницы, описывающая согласие устройства как сверку идентификатора модели **ключа** со
списком одобренных, — потому что она обещает арендатору ровно ту проверку, которую это решение
строить отказывается (Ф7-32). Само объявление условия Ф7 **не трогает**: оно про устройство, а не
про ключ, ненавешенных условий в каноне **пять из шести**, и его снятие обесценило бы посылку
одобренного соседа побочным эффектом чужой фазы (§1.8, таблица радиуса; остаток — §10 п. 11).

### Р5. Регистрация и снятие ключа — действия в окне свежести; остаток назван

**Регистрация ключа требует, чтобы момент последнего предъявления в этой сессии был не старше
окна свежести** (Ф1 §4.1 — 15 минут; якорь — Ф11 Р6). Снятие ключа — то же действие и то же окно.

**Почему это обязательство, а не усмотрение.** Ф11 §6 вынесла «правило заведения ключа» с
носителем Ф7 и назвала довод: фактор, который можно завести из сессии «1» **без повторного
предъявления первого**, делает «2» не сильнее «1». Правило уровня этого не лечит — лечит правило
заведения, и владелец правила заведения ключа — эта приёмка.

**Первый ключ заводится из сессии «1», и это не послабление.** Требовать «2» для получения «2»
недостижимо by construction у человека, у которого второго фактора ещё нет. Свежесть при этом
обязательна: она отделяет «человек только что доказал, что он — он» от «носитель сессии лежит
открытым третьи сутки».

**Остаток назван, а не умолчан.** Свежесть не лечит случая, когда пароль знает не тот: предъявив
его, он заведёт свой ключ и получит «2» честно — правило не солжёт, множество предъявленного
действительно содержит утверждение ключа. **Это свойство любого заведения второго фактора, а не
дефект нашего правила** (Ф11 Р2 назвала ту же цену у восстановления и тем же словом). Что документ
противопоставляет остатку — **наблюдаемость**: заведение и снятие ключа попадают в журнал (Ф7-01,
Ф7-25), и человек видит перечень своих ключей. Чего документ **не** обещает: уведомления по почте
— его носитель другой (§4.2).

### Р6. Счётчик подписи судится односторонне: ноль не судится, положительный обязан расти

| наблюдение | исход | почему |
|---|---|---|
| ключ сообщает **ноль** и сообщал ноль прежде | проходит, счётчик не судится | так ведёт себя большинство современных ключей и все синхронизируемые; судить их значило бы отвергать годные |
| сообщённый счётчик **больше** сохранённого | проходит, сохранённое сдвигается | штатный путь |
| сообщённый **положителен и не больше** сохранённого | **отказ** плюс громкое состояние | единственный сигнал клонирования, который ключ вообще способен дать |
| сообщённый ноль при **положительном** сохранённом | отказ, тот же | ключ, умевший считать, перестал: сохранённое значение говорит, что ноль здесь не «не умеет» |

**Сдвиг сохранённого значения атомарен** — одним оператором с условием на прежнее значение, а не
чтением с последующей записью (ban #10). Под одновременными предъявлениями одного ключа это
наблюдаемо: разнесённая пара теряет сдвиг (Ф7-20).

**Отказ по счётчику отличим от отказа по подписи ВНУТРИ, но не снаружи.** Снаружи он входит в
единый отказ аутентификации (Ф7-09): различимый текст сказал бы предъявителю, что ключ существует
и признан, а подозрителен лишь счётчик.

### Р7. Держатель — свой набор проб; сертификационный прогон держателем не является

**Сертификационный прогон не переносится и не чинится.** Основание — §1.7: ноль вызывающих, ни
одного исполнения, предмет — эндпоинты снимаемого компонента, имя доверяющей стороны берётся мимо
нашего контракта. Держателем свойства он не был никогда.

**Его судьба — исход, а не умолчание:** он снимается **вместе со своим предметом**, то есть в
фазе Ф10 `#1276`, которая снимает компонент. Снять его раньше нельзя — до Ф10 его предмет ещё
стоит; оставить после — значит оставить прогон, обращающийся в никуда.

**Что держит свойство вместо него** — состав своего набора назван §8 **по свойству**, а не числом
проб: числу проб не с чем сходиться, а свойству есть. **Четыре** предиката снятия из тела задачи покрыты поимённо: ключ регистрируется и принимается
(Ф7-01, Ф7-06) · подделанная подпись отвергается (Ф7-07) · смена происхождения ломает проверку
(Ф7-10) · **все три живых ключа работают либо пересозданы по объявленному решению** (Р3,
Ф7-21…24, DoD §9.1 п. 7). Редакция 2 называла три и покрывала четыре — число в тексте было
меньше числа в источнике; круг 1 назвал это не блокирующим В2, и перемер подтвердил: предикат
`gh issue view 1273 -R PRO-Robotech/kacho --json body -q .body` печатает строку «ПРЕДИКАТ
СНЯТИЯ» с **четырьмя** утверждениями через точку с запятой.

**Чего свой набор не заменяет — сказано прямо.** Он не является сертификацией и ею не станет:
сертификация есть утверждение внешнего авторитета, а не наше. Документ этого и не обещает;
условие возврата к сертификации — требование арендатора либо членство, и оба внешние.

### Р8. Число ключей человека ограничено СВОИМ потолком — ключ есть ресурс, а не значение поля

**Ключ получает собственный вид учёта `iam.user.accessKey`, объявляемый посадкой, со своей
величиной и без умолчания.** Решение заведено редакцией 3 по блокирующему Б3 круга 1: документ
молчал о потолке вовсе, а Ф7-14 утверждал, что «третий ключ заводится не требуя правки ни одной
существующей строки» — то есть рост без границы, и никто этого не решал.

**Почему свой вид, а не счёт в `iam.user.credential`.** Одобренный сосед
`credential-ceiling-per-principal.md` (APPROVED, дом службы) решил своим **Д1**: «потолок
ставится на РЕСУРС, а не на значение его поля-дискриминатора». Р1 заводит **новый ресурс** —
свою таблицу, свой идентификатор, свои глаголы, — значит его потолок и есть потолок нового
ресурса. Считать ключ в `iam.user.credential` нельзя по тому же Д1 с другой стороны: подчинённый
ресурс `iam.credential` **анкерен таблицами** (`kaname.user_oauth_clients`,
`kaname.service_account_oauth_clients`), и таблица ключей в их числе не стоит и стоять не будет —
это другой класс сущности.

**Почему потолок вообще нужен — доводы соседа применимы без изменения.** Его B1 (поверхность
утечки растёт с числом живых путей входа), B2 (стоимость обзора и отзыва равна числу строк) и B3
(накопление за спиной владельца) сформулированы про «самостоятельный путь входа: пока оно живо,
предъявитель действует от имени принципала» — а ключ после Ф13 и есть путь входа целиком. Ни один
из трёх доводов не опирается на вид предъявления.

**Второй одобренный сосед требует механизма ПРЯМО, и это не тот же довод.**
`retention-sweep-has-a-caller.md` (APPROVED, дом службы) объявляет нормой: «у каждой таблицы
службы, чей рост задаёт внешний, есть **названный механизм ограничения**». Таблица ключей ровно
такова — её рост задаёт человек. Механизмом выбран **потолок**, а не уборка, и выбор сделан **её
же критерием**: уборка законна там, где у читателя есть момент, после которого строка перестаёт
менять его исход; у ключа такого момента нет — проверка утверждения читает его при каждом
предъявлении, срока у него нет by construction, а снятие ключа за спиной владельца прямо
запрещено Ф7-26. То есть предыдущий сосед говорит, **на что** ставится потолок, а этот — **что
без механизма нельзя вовсе**.

**Перенос расходует те же слоты, и порядок работ отсюда следует, а не подразумевается.** Строка,
вставленная переносом (Р3), списывает слот наравне со строкой церемонии: величина, объявленная
ниже наибольшего числа ключей у одного человека в источнике, роняет перенос у него. Поэтому
перепись Ф7-21 печатает это число, а посадка объявляет величину **после** переписи (§9 п. 7).
Обратный порядок обнаруживает расхождение на переносе — в самый дорогой момент.

**Величина НЕ называется числом здесь, и это решение.** Три собственных потолка службы объявляет
**посадка**, и умолчания у них нет **by construction**: величина, которую подставляет построение,
предметом стража быть не может — он зелен при любом входе. Ф7 обязана добавить четвёртую ручку
той же формы и ронять старт при незаданной величине; какое число поставит оператор, решает
посадка, а не приёмка. Обе стороны — Ф7-37 и Ф7-38.

**Ноль законен и означает «ключей не заводить».** Это уже действующая семантика вида: величина
хранится указателем именно затем, чтобы ноль был отличим от «не объявлено». Столкновения с Ф7-26
(«последний способ не снимается») тут нет: потолок судит **заведение**, а не снятие, и уже
заведённые ключи не удаляет — то же решение, что у соседа (его §7.4).

**Кто исполняет.** Ф7 `#1273`: **своей миграцией** — множество видов закрыто ограничением
таблицы `kaname.own_ceilings` (`kind = ANY(...)`, сегодня три имени), и применённую миграцию не
правят (ban #5); **своей ручкой** посадки рядом с тремя существующими; **своим списанием** —
триггер `kacho_quota_count` списывает слот на вставке и возвращает на удалении **в той же
транзакции**, что строка ресурса, то есть ban #10 исполняется оператором базы, а не проверкой
перед вставкой.

---

## §3 Сценарии (Given-When-Then)

### §3.0 Как наблюдается ключ — определено один раз

Чтобы «Тогда» описывали наблюдаемое, а не устройство, четыре вещи определены здесь и ниже не
повторяются:

- **«ключ принят»** — последующее утверждение этим ключом проходит проверку, и ключ виден в
  перечне ключей человека. Не «строка появилась в таблице»: строку читатель не наблюдает;
- **«утверждение проходит»** — ответ службы называет человека и **не** содержит отказа; что из
  этого следует для уровня, решает Ф11, и здесь не утверждается;
- **«единый отказ аутентификации»** — отказ, чьи код и тело **побайтово** равны у всех полос §3.2
  и §3.3, и чьё время неразличимо по критерию Ф1-48…50. Побайтовое равенство — то же требование,
  что Ф1-02/03 предъявляет паролю;
- **«поток аудита»** — тот **единственный** поток службы, который уже несёт выдачу и отзыв ключа
  служебной учётки (`iam.sa_key.issued` / `iam.sa_key.revoked`) и доставляется стоку дренажем.
  Определено здесь раз и навсегда, потому что журналов в доме **три**, и два других считают
  другое: один копит личности ради метрики роста, второй — ресурсы. «Попало в журнал» без этого
  уточнения читателем не проверяемо — круг 1 назвал это не блокирующим В3. Носитель и форма
  события названы в §4.1 п. 11, **а не в сценарии**: §7 инв. 1 запрещает «Тогда» называть
  таблицу, и Ф7-01/Ф7-25 наблюдают **поток**, а не строку.

**«Дано» строится названным домом и без второго факта.** Человек, его сессия и её свежесть
производятся Ф3 `#1269`; строка способа «пароль» — Ф2 `#1268`; ключ в источнике переноса —
поставщиком, то есть настройкой §1.2. Где «Дано» приходится строить записью, а не действием, это
сказано в самом сценарии.

### §3.1 Полоса церемонии регистрации

**ID: Ф7-01** — положительный близнец всей §3.1
**Given** человек полосы `own` с заведённым паролем, вошедший паролем; момент его последнего
предъявления — внутри окна свежести
**And** ключей у него ноль
**When** он проводит церемонию регистрации ключа и присылает её результат
**Then** ключ **принят**: последующее утверждение этим ключом проходит, и ключ виден в перечне
ключей человека с названным им именем и моментом заведения
**And** заведение попало в **поток аудита** (§3.0) событием **своего вида**, и событие появляется
там **тогда и только тогда**, когда появился ключ: отказы Ф7-02…Ф7-05, Ф7-34, Ф7-35 события не
порождают

**ID: Ф7-02** · отрицание; близнец — Ф7-01, отличается **одним** фактом: результат церемонии
собран не на выданном испытании
**Given** то же, что в Ф7-01
**When** он присылает результат церемонии, собранный на испытании, которого служба не выдавала
**Then** отказ; ключ не заведён, перечень ключей человека не изменился

**ID: Ф7-03** · отрицание; близнец — Ф7-01, отличается **одним** фактом: то же испытание
предъявлено второй раз
**Given** человек, только что успешно завёдший ключ по Ф7-01
**When** он присылает тот же результат церемонии повторно
**Then** отказ: испытание однократно
**And** число ключей человека равно одному — повтор не завёл второго и не изменил первого

**ID: Ф7-04** · отрицание; близнец — Ф7-01, отличается **одним** фактом: момент последнего
предъявления старше окна свежести
**Given** человек полосы `own` с паролем, чей момент последнего предъявления старше окна свежести
Ф1 §4.1
**When** он проводит церемонию регистрации ключа
**Then** отказ, называющий **следующий шаг** — повторно предъявить способ входа
**And** после повторного предъявления та же церемония проходит: положительный контроль внутри
сценария — окно открывается предъявлением, а не ожиданием

**ID: Ф7-05** · отрицание с двумя ветвями; близнец — Ф7-01, отличается **одним** фактом:
идентификатор удостоверения уже принят. Ветви а и б отличаются друг от друга тоже **одним**
фактом — чей он
**Given** ключ с идентификатором удостоверения `K`, уже принятый
**When** церемония присылает результат с тем же `K` — **ветвь а:** от того же человека;
**ветвь б:** от другого человека
**Then** в обеих ветвях — отказ, и ключ ни у кого не переназначен
**And** тела обоих отказов **побайтово равны**: различимый текст сказал бы второму человеку, что
такой ключ у продукта уже есть и принадлежит кому-то ещё

**ID: Ф7-34** · отрицание; близнец — Ф7-01, отличается **одним** фактом: с момента выдачи
испытания прошло больше его срока
**Given** то же, что в Ф7-01; служба выдала испытание, и с момента выдачи прошло больше
объявленного срока испытания
**When** он присылает результат церемонии, собранный на этом испытании
**Then** отказ; ключ не заведён, перечень ключей человека не изменился
**And** положительный контроль внутри сценария: то же испытание **внутри** срока проходит — то
есть отказ был о сроке, а не о самом испытании
**And** отказ **называет следующий шаг** — начать церемонию заново, — и три состояния испытания
(просрочено · не выдавалось · уже предъявлено) вызывающему **различимы**. Это не оракул и не
послабление §7 инв. 3: там предмет — **чужой** ключ, здесь все три состояния относятся к
**собственному** испытанию вызывающего, и различить их значит назвать ему, что делать, а не
рассказать о постороннем

> [!note] Срок испытания — величина КОНТРАКТА, и её верхняя граница ВЫВЕДЕНА, а не выбрана
> Срок объявлен числом в контракте и **строго меньше окна свежести** Ф1 §4.1 (15 минут). Это не
> вкус: испытание, живущее дольше окна, позволяет собрать его внутри окна и предъявить снаружи —
> то есть **обойти Р5** тем самым механизмом, ради которого Р5 написан. Нижняя граница — одно
> взаимодействие человека с ключом; церемония повторяема и бесплатна, поэтому отошедший от
> машины ничего не теряет, начав заново. Смена величины — правка **контракта**, а не профиля:
> профилю она не принадлежит по тому же доводу, что и перечень происхождений (Р2).

**ID: Ф7-35** · отрицание; близнец — Ф7-01, отличается **одним** фактом: алгоритм открытого
ключа вне объявленного перечня
**Given** то же, что в Ф7-01; перечень алгоритмов объявлен непустым (Р2, Ф7-13)
**When** церемония присылает результат, чей открытый ключ имеет алгоритм вне перечня
**Then** отказ; ключ не заведён, перечень ключей человека не изменился
**And** испытание **называло** допустимые алгоритмы — то есть сверка на приёме есть вторая
сторона того же объявления, а не единственная: аутентификатор, выбравший алгоритм по испытанию,
до этого отказа не доходит вовсе, и отказ остаётся тому, кто перечень проигнорировал
**And** положительный контроль: тот же результат при алгоритме **из** перечня принимается
**And** это полоса **церемонии**; вторая полоса той же оси — **перенос** (Ф7-22), и они не
заменяют друг друга: перенос читает чужое хранилище, церемония — результат живого ключа

### §3.2 Проверка утверждения

**ID: Ф7-06** — положительный близнец всей §3.2 и §3.3
**Given** человек с принятым ключом; испытание выдано; происхождение, названное браузером, входит
в объявленный перечень; имя доверяющей стороны то, к которому ключ привязан
**When** он присылает годное утверждение этого ключа
**Then** утверждение **проходит**: ответ называет человека
**And** момент последнего успешного предъявления этого ключа сдвинулся

**ID: Ф7-07** · отрицание; близнец — Ф7-06, отличается **одним** фактом: подпись подделана
**Given** то же, что в Ф7-06
**When** он присылает утверждение, чья подпись не сверяется с открытым ключом строки
**Then** **единый отказ аутентификации** (§3.0)
**And** момент последнего успешного предъявления **не** сдвинулся, счётчик не сдвинулся

**ID: Ф7-08** · отрицание; близнец — Ф7-06, отличается **одним** фактом: утверждение подписано
другим ключом того же человека
**Given** человек с двумя принятыми ключами
**When** он присылает утверждение, называющее идентификатор первого ключа и подписанное вторым
**Then** тот же единый отказ: сверяется ключ **названной** строки, а не любой ключ человека

**ID: Ф7-09** · отрицание, закрывающее оракул; близнец — Ф7-06, отличается **одним** фактом:
идентификатора удостоверения в хранилище нет вовсе
**Given** испытание выдано
**When** присылается утверждение с идентификатором удостоверения, которого в хранилище нет вовсе
**Then** тот же единый отказ, **побайтово равный** отказу Ф7-07
**And** время ответа неразличимо по критерию Ф1-48…50: работа по неизвестному идентификатору
выполняется наравне с работой по известному

### §3.3 Привязка: имя доверяющей стороны и перечень происхождений

**ID: Ф7-10** · отрицание; близнец — Ф7-06, отличается **одним** фактом: происхождение вне
перечня
**Given** то же, что в Ф7-06
**When** браузер называет происхождение, которого в объявленном перечне нет
**Then** единый отказ; утверждение не проходит **ни при какой** годности подписи

**ID: Ф7-11** · отрицание, отделяющее перечень от заголовка; близнец — Ф7-10,
отличается **одним** фактом: заголовок происхождения самого запроса совпадает с названным
браузером
**Given** то же, что в Ф7-10
**When** заголовок происхождения самого запроса **совпадает** с тем, что назвал браузер, и оба
вне объявленного перечня
**Then** единый отказ — то есть решает **перечень**, а не то, что назвал вызывающий
**And** положительный контроль в том же сценарии: происхождение из перечня при **отсутствующем**
заголовке запроса проходит

**ID: Ф7-12** · отрицание; близнец — Ф7-06, отличается **одним** фактом: имя доверяющей стороны
иное
**Given** ключ, принятый при имени доверяющей стороны `A`
**When** служба объявляет имя `B` и приходит годное утверждение этого ключа
**Then** единый отказ: ключ привязан к `A`, и это не чинится ни настройкой, ни повторной попыткой
**And** при возврате объявления к `A` тот же ключ проходит — положительный контроль того, что
отказ был о привязке, а не о порче материала

**ID: Ф7-13** · отказ старта, обе стороны названы у **каждой** из трёх величин контракта
**Given** профиль службы
**When** перечень происхождений **не объявлен** — служба **не стартует**, и отказ называет ручку;
когда перечень объявлен **пустым** — служба стартует и отвергает всякое утверждение; когда
перечень объявлен непустым — служба стартует и Ф7-06 проходит
**Then** три исхода различимы: незаданное — отказ старта, пустое — «никого», непустое — работа
**And** то же самое для имени доверяющей стороны, с одним отличием: у него пустого значения не
бывает — незаданное и пустое дают отказ старта оба
**And** то же самое для **перечня алгоритмов открытого ключа** (Р2, третья величина): незаданный
и пустой дают отказ старта оба, непустой — работу. Пустого «никого» у него не бывает by
construction: перечень, не допускающий ни одного алгоритма, делает церемонию невыполнимой, то
есть объявляет не политику, а поломку
**And** отказ старта по каждой из трёх называет **свою** ручку: один текст на три величины
оставил бы оператора гадать, которую он забыл

### §3.4 Несколько ключей у одного человека — прямое отличие от F4d-14

**ID: Ф7-14** — положительный близнец всей §3.4
**Given** человек с одним принятым ключом
**When** он проводит церемонию второго ключа с иным идентификатором удостоверения
**Then** оба ключа сосуществуют, и **первый не изменён**: его утверждение проходит по-прежнему
**And** третий ключ заводится не требуя правки ни одной существующей **строки** — утверждение
о строках, а не о росте без границы: число ключей ограничено потолком Р8, и это другая ось
(Ф7-37, Ф7-38)

**ID: Ф7-15** · конкуренция, обе стороны
**Given** человек с нулём ключей
**When** два одновременных запроса заводят ключи с **разными** идентификаторами удостоверения —
проходят **оба**; два одновременных запроса заводят **один и тот же** идентификатор — проходит
**ровно один**
**Then** во втором случае второй получает отказ, произведённый **ограничением базы**, а не
сравнением прочитанного: под конкуренцией исход не зависит от порядка чтений

**ID: Ф7-16** · положительный контроль независимости таблиц
**Given** человек с двумя принятыми ключами
**When** ему заводится способ входа вида «пароль»
**Then** обе строки ключа не изменены, и оба ключа проходят
**And** обратно: заведение третьего ключа не трогает строки способа «пароль»
**And** предикат F4d-14 на этом человеке по-прежнему даёт ноль вторых способов одного вида — то
есть Р1 требования F4d-14 не ослабил

**ID: Ф7-37** · ограниченная ось, обе стороны названы **внутри** сценария
**Given** объявленный потолок вида `iam.user.accessKey` положителен, и принятых ключей у
человека на один меньше него
**When** он проводит церемонию очередного ключа — **ветвь а:** ключей становится ровно потолок;
**ветвь б:** он проводит церемонию ещё одного
**Then** ветвь **а** проходит; ветвь **б** получает отказ, называющий **следующий шаг** — снять
ненужный ключ, и называющий действующую величину
**And** отказ произведён **оператором базы** в той же транзакции, что вставка строки ключа, а не
проверкой перед вставкой: два одновременных заведения на последний слот дают **ровно одно**
принятие, и исход не зависит от порядка чтений (ban #10)
**And** снятие ключа **возвращает слот**: после Ф7-25 та же церемония проходит
**And** потолок другого человека не расходуется: счёт ведётся у **одного** человека
**And** **перенос расходует те же слоты**: строка, вставленная переносом (Ф7-23), списывает слот
наравне со строкой церемонии, поэтому величина, объявленная ниже наибольшего числа ключей у
человека в источнике, роняет перенос. Порядок отсюда — §9 п. 7: перепись Ф7-21 называет это
число раньше, чем посадка объявляет величину

**ID: Ф7-38** · три исхода величины потолка, и они различимы
**Given** профиль службы
**When** величина потолка ключей **не объявлена** — служба **не стартует**, и отказ называет
ручку; объявлена **нулём** — служба стартует, и всякое заведение ключа отвергается отказом,
называющим величину; объявлена положительной — работает Ф7-37
**Then** три исхода различимы: незаданное — отказ старта, ноль — «ключей не заводить»,
положительное — работа
**And** уже принятые ключи не удаляются **ни в одном** из трёх исходов: потолок судит заведение,
а не хранение (то же решение, что у одобренного соседа о потолке удостоверений)
**And** отказ по исчерпанию отличим от отказа по **необъявленной** величине: слияние их в один
исход скрыло бы ровно то состояние, ради которого страж старта и заведён

### §3.5 Счётчик подписи

**ID: Ф7-17** — положительный близнец §3.5
**Given** ключ, чьё сохранённое значение счётчика равно `N`, и `N` больше нуля
**When** приходит годное утверждение, сообщающее счётчик больше `N`
**Then** утверждение проходит, и сохранённое значение стало сообщённым

**ID: Ф7-18** · отрицание; близнец — Ф7-17, отличается **одним** фактом: сообщённый счётчик не
больше сохранённого
**Given** то же
**When** приходит годное во всём остальном утверждение, сообщающее счётчик, не больший `N`
**Then** **единый отказ аутентификации** — снаружи неотличимый от Ф7-07
**And** внутри состояние громкое: событие названо и сосчитано отдельно от прочих отказов

**ID: Ф7-19** · вторая сторона той же оси; близнец — Ф7-18,
отличается **одним** фактом: сохранённое значение счётчика равно нулю
**Given** ключ, чьё сохранённое значение счётчика равно нулю
**When** приходит годное утверждение, сообщающее ноль
**Then** утверждение **проходит**: ноль не судится
**And** ключ, сообщивший ноль при **положительном** сохранённом, — отказ (Р6, четвёртая строка)

**ID: Ф7-20** · атомарность
**Given** ключ с сохранённым счётчиком `N`
**When** два одновременных годных утверждения сообщают разные значения больше `N`
**Then** сохранённое значение равно **наибольшему** из принятых, и ни один сдвиг не потерян
**And** внесённое различие — разнесение чтения и записи — роняет сценарий **детерминированно**, а
не в одном прогоне из десяти

### §3.6 Перенос трёх ключей

**ID: Ф7-21** — перепись до переноса, и «ноль» отличимо от «не прочитано»
**Given** источник переноса — хранилище поставщика, доступное переписи
**When** перепись прогоняется **до** первой записи переноса
**Then** она печатает объём осмотренного: людей, строк способа вида ключа, **ключей**, по
каждому ключу — покрыт ли его алгоритм перечнем, и **наибольшее число ключей у одного человека**
**And** источник с нулём ключей даёт **«не выполнилось»**, а не успех; недоступный источник даёт
«условие не создано», а не ноль
**And** перепись исполняется и тогда, когда переносить нечего: её отсутствие неотличимо от её
пустоты
**And** наибольшее число ключей у одного человека печатается **не ради полноты**: перенос
вставляет строки в ту же таблицу, что церемония, и списывает те же слоты (Р8). Величина потолка,
объявленная **ниже** этого числа, роняет перенос у самого богатого ключами человека — то есть
перепись обязана назвать её **до** того, как посадка объявит величину, а не после

**ID: Ф7-22** · отрицание; близнец — покрытый источник, отличается **одним** фактом: у одного
ключа алгоритм вне перечня
**Given** источник, где у одного ключа алгоритм открытого ключа вне объявленного перечня
**When** запускается перенос
**Then** перенос **не начинается**: перепись роняет его, называя число непокрытых ключей и
причину по каждому
**And** тот же источник без непокрытого ключа — перенос проходит

**ID: Ф7-23** · положительный близнец §3.6
**Given** ключ, перенесённый из источника, при **неизменном** имени доверяющей стороны
**When** его держатель присылает годное утверждение **нашей** службе
**Then** утверждение проходит **без повторной церемонии регистрации**
**And** тот же ключ при изменённом имени доверяющей стороны — отказ Ф7-12: положительный контроль
того, что перенос удерживает именно привязка, а не копирование строки

**ID: Ф7-24** · запертость названа числом
**Given** источник, где у человека единственный способ входа — ключ, и этот ключ непереносим по
Ф7-22
**When** перепись прогоняется
**Then** такие люди названы **отдельным числом**, а не растворены в числе непокрытых ключей
**And** перенос не объявляется состоявшимся, пока это число не ноль либо пока каждому из них не
назван второй способ войти
**And** положительный контроль: человек с непереносимым ключом **и** заведённым паролем в это
число не входит

### §3.7 Снятие ключа

**ID: Ф7-25** — положительный близнец §3.7
**Given** человек с двумя принятыми ключами, вошедший, момент последнего предъявления в окне
свежести
**When** он снимает первый ключ
**Then** утверждение снятым ключом перестаёт проходить, а утверждение вторым проходит
**And** снятие попало в **поток аудита** событием своего вида — тем же способом, что заведение в
Ф7-01, и с тем же «тогда и только тогда»: отказы Ф7-26, Ф7-27 и Ф7-36 события не порождают

**ID: Ф7-26** · отрицание; близнец — Ф7-25, отличается **одним** фактом: снимаемый способ
последний
**Given** человек, у которого единственный способ входа — один ключ
**When** он снимает его
**Then** отказ, называющий **следующий шаг** — завести другой способ входа прежде
**And** тот же человек, завёдший пароль, снимает ключ успешно — положительный контроль

**ID: Ф7-27** · отрицание, закрывающее оракул; близнец — Ф7-25, отличается **одним** фактом:
ключ принадлежит другому человеку
**Given** два человека, у каждого свой ключ
**When** первый пытается снять ключ второго — и, отдельно, снять ключ, которого нет вовсе
**Then** оба отказа **побайтово равны**, и ключ второго на месте

**ID: Ф7-36** · отрицание; близнец — Ф7-25, отличается **одним** фактом: момент последнего
предъявления старше окна свежести
**Given** человек с двумя принятыми ключами, вошедший; момент последнего предъявления старше
окна свежести Ф1 §4.1
**When** он снимает первый ключ
**Then** отказ, называющий **следующий шаг** — повторно предъявить способ входа; **оба** ключа на
месте, и утверждение каждого проходит
**And** после повторного предъявления то же снятие проходит: положительный контроль внутри
сценария — окно открывается предъявлением, а не ожиданием
**And** это **вторая полоса** оси Ф7-04, а не её повторение: решение Р5 связывает регистрацию и
снятие одним окном, и продукт, проверяющий окно только на регистрации, проходил бы Ф7-04 и все
прочие сценарии. Снятие при этом опаснее: носитель сессии, лежащий открытым, позволяет снять
чужие ключи по одному и тем запереть владельца — Ф7-26 закрывает только **последний** способ

### §3.8 Флаги предъявления доезжают до правила уровня

**ID: Ф7-28** — положительный близнец §3.8
**Given** ключ, сообщающий проверку пользователя и **не** сообщающий признака возможности
резервного копирования
**When** приходит годное утверждение
**Then** оба флага доезжают до точки, где принимается решение об уровне, **в виде, который читает
правило Ф11 Р3** — то есть различимо, а не сведённые в один признак
**And** значение уровня здесь **не утверждается**: его производит Ф11

**ID: Ф7-29** · вторая сторона той же оси; близнец — Ф7-28, отличается **одним** фактом: признак
резервного копирования сообщён
**Given** ключ, сообщающий проверку пользователя **и** признак возможности резервного копирования
**When** приходит годное утверждение
**Then** флаги доезжают различимо от Ф7-28

**ID: Ф7-30** · отсутствие представимо отдельно от значения
**Given** ключ, выпущенный до появления флагов резервного копирования и потому сообщающий их
нулями
**When** приходит годное утверждение
**Then** «флаги сообщены нулями» отличимо от «флаги не прочитаны»: второе — состояние ошибки, а
не законный вход
**And** ключ, чьё утверждение не сообщает проверки пользователя, тоже доезжает различимо: это
законное утверждение (Ф11 Р2 даёт ему «2»), а не отказ

### §3.9 Аттестация и снятие обещания

**ID: Ф7-31** · невыразимость; предмет — представление
**Given** ключ, чья церемония вернула аттестацию
**When** ключ принимается
**Then** содержимое аттестации в хранилище **не выражается** — поля для него нет; это проверяется
**попыткой положить** его туда, а не чтением
**And** живой близнец: открытый ключ строка несёт, и попытка снять с неё это поле роняет пробу —
иначе утверждение об отсутствии молчало бы одинаково и когда предмет исчез, и когда сломался сам
гейт

**ID: Ф7-32** · снятие обещания, ПРИВЯЗАННОГО К КЛЮЧУ
**Given** публичная страница службы, чья строка таблицы условий описывает согласие устройства как
сверку идентификатора модели **ключа** со списком одобренных (§1.8)
**When** Ф7 садится
**Then** ни одна страница службы не обещает арендатору проверки **аттестации ключа**: строка
снята, и второй, обещающей то же другими словами, не заведено
**And** перепись печатает **обе** величины — «строк таблицы условий N · из них обещающих проверку
аттестации ключа M»; на дереве до Ф7 это `6 · 1`, после — `6 · 0`. Одна величина скрыла бы ровно
тот случай, ради которого перепись и делается
**And** положительный контроль: соседние пять строк той же таблицы **не тронуты** — то есть
утверждение о снятии отличимо от «таблицы больше нет»
**And** объявление условия в каноне Ф7 **не трогает**, и это часть утверждения, а не умолчание:
перепись объявленных условий после Ф7 даёт те же **6**, ненавешенных — те же **5**

> [!note] Почему это «Тогда» стало другим — редакция 1 требовала того, что УЖЕ верно
> Стояло «значение согласия устройства **не производится** из поля запроса ни на одной полосе».
> Замер (§1.8): производителя нет ни одного, а клиентское значение сборка контекста **удаляет**.
> То есть требуемое истинно на дереве **до** первой строки кода, и честного красного такой
> сценарий дать не может — он проверяет не отсутствие предмета, а его наличие.

**ID: Ф7-39** · снятие ВТОРОГО носителя того же обещания; близнец — Ф7-32, отличается **одним**
фактом: носитель — **значение токена**, а не строка публичной страницы
**Given** человек с принятым ключом, чей токен несёт область `webauthn` среди выданных (§1.8)
**When** он предъявляет этот токен
**Then** значение согласия устройства в утверждениях токена **не выводится из наличия области**:
оно то же, что у токена без ключа
**And** перепись печатает **пару** — «полос выдачи, ставящих это значение, N · из них выводящих
аттестованность из наличия ключа M»; на дереве до Ф7 это `5 · 1`, после — `5 · 0`. Единица —
**полоса выдачи**, а не место в файле, и выбрана она затем, что знаменатель при этом **не
двигается**: мест шесть до правки и пять после (§1.8), и пара `6 · 1` → `5 · 0` меняла бы обе
величины сразу, скрывая, какая из них о чём
**And** положительный контроль: значение по-прежнему **выставляется** на всех пяти полосах — то
есть «не выводится» отличимо от «поля больше нет» и от «полоса исчезла»
**And** четыре пробы, утверждающие это значение (§1.8, таблица радиуса), правятся **тем же**
изменением: проба, пережившая свой предмет, утверждала бы снятое

> [!note] Почему это «Тогда» даёт ЧЕСТНЫЙ красный, а Ф7-32 редакции 1 не давал
> Требуемое здесь **ложно на дереве сегодня**: деривация существует и исполняется на каждой
> выдаче токена человеку (§1.8, перепись обеими формами). Значит проба, написанная до кода,
> падает **оттого, что предмета нет**, — а не оттого, что предмет уже сделан. Это ровно та
> проверка, которую §9.1 п. 3 велит прогнать по каждому «Тогда», требующему отсутствия.

### §3.10 Шов с Ф11: регистрация ключа предъявлением НЕ является

Полоса одна и сценарий один. Она стоит отдельно от §3.1, потому что наблюдается **не** перечень
ключей, а множество предъявленного в сессии, и «исполнимо с» у неё поэтому своё (§5).

**ID: Ф7-33** · невмешательство в состояние соседа; близнец — **Ф7-01**: тот же человек, то же
действие, тот же вход, наблюдается другое
**Given** человек полосы `own`, вошедший паролем; его сессия несёт множество предъявленного —
поле приходит **вместе с таблицей сессии**, и Ф11 Р1 говорит именно так: «какая бы из двух фаз
её ни завела» (Ф2 либо Ф3). Строка ведомости §5 поэтому называет **обе**
**And** множество содержит ровно одно утверждение — пароль. Тем самым сказано, что множество
**пишется**: иначе «не пополнилось» было бы истинно оттого, что в него не пишет никто
**When** он проводит церемонию регистрации ключа по Ф7-01, и она проходит
**Then** множество предъявленного этой сессии **не пополнилось**: запись о регистрации в него не
попадает — предъявлено то, что **сверено с уже хранимым** (Ф11 Р2), а церемония ни с чем хранимым
не сверяется
**And** значение уровня здесь **не утверждается**: его выводит правило Ф11 над этим множеством, и
что оно выведет, решает Ф11
**And** положительный контроль в том же сценарии: перечень ключей человека тем же действием
**пополнился** — то есть «множество не пополнилось» отличимо от «церемония ничего не сделала»

**Почему это сценарий Ф7, а не Ф11.** Правило уровня читает множество; **пишет** его тот, кто
исполняет действие. Ф11 §6 вынесла «правило заведения ключа» с носителем **Ф7 `#1273`** и назвала
довод: фактор, который можно завести из сессии «1» без повторного предъявления первого, делает «2»
не сильнее «1». Р5 отвечает на это окном свежести; Ф7-33 отвечает на вторую половину — что само
заведение **не засчитывается** за предъявление. Сценариев об этом у Ф11 ноль (её §6, строка
«правило заведения второго фактора и ключа»), то есть второго места об одном предмете здесь не
заводится.

**Чего сценарий НЕ утверждает.** Ни того, что сессия выдана (это Ф13), ни значения уровня (это
Ф11). Он утверждает **неизменность** множества под действием Ф7 и **изменение** перечня ключей под
тем же действием — обе половины наблюдаемы, и обе принадлежат Ф7.

---

## §4 Объём и границы

### §4.1 В объёме

**Каждый пункт назван вместе со своими сценариями, и соответствие проверяется В ОБЕ СТОРОНЫ.**
Круг 1 ревью нашёл здесь двустороннюю дыру (блокирующее Б4): ось «срок испытания» была объявлена
объёмом и не описана ни одним сценарием, а «перечень ключей человека» — описан §3.0 и назван
ведомостью §5, но объёмом **не объявлен**, то есть по правилу §0.2 не разрешён. Обе закрыты,
и перепись соответствия стоит в §5.

1. **Сервер церемонии регистрации**: выдача испытания, его **однократность** (Ф7-03) и **срок**
   (Ф7-34), приём результата, принятие ключа, **сверка алгоритма открытого ключа с объявленным
   перечнем** (Ф7-35) — Ф7-01…05, Ф7-34, Ф7-35.
2. **Проверка утверждения**: сверка подписи с открытым ключом названной строки, единый отказ,
   неразличимость по телу и времени (Ф7-06…09).
3. **Привязка и величины контракта**: имя доверяющей стороны, перечень происхождений **и
   перечень алгоритмов** как величины **контракта**, отказ старта на незаданном у каждой
   (Ф7-10…13, Р2).
4. **Хранилище ключей**: строка своей таблицы на ключ, уникальность по идентификатору
   удостоверения, сосуществование нескольких ключей у человека, конкуренция (Ф7-14…16, Р1);
   **перечень ключей человека** как наблюдаемое, которым §3.0 определяет «ключ принят»
   (Ф7-01, Ф7-25, Ф7-33); **потолок числа ключей** — свой вид учёта, величина посадки, три
   исхода величины (Ф7-37, Ф7-38, Р8).
5. **Счётчик подписи**: односторонняя дисциплина и атомарный сдвиг (Ф7-17…20, Р6).
6. **Перенос**: перепись источника до переноса, падение на непокрытом, запертость числом
   (Ф7-21…24, Р3).
7. **Снятие ключа**: окно свежести на снятии (Ф7-36), отказ на последнем способе и
   неразличимость чужого от несуществующего (Ф7-25…27).
8. **Флаги предъявления** — до точки решения уровня, различимо (Ф7-28…30, Р4).
9. **Политика аттестации**: не требуется, не хранится, и снимаются **оба** носителя обещания
   проверки аттестации **ключа** — строка публичной страницы (Ф7-32) и деривация значения токена
   из наличия ключа (Ф7-39); Ф7-31, Р4.
10. **Невмешательство в состояние соседа**: регистрация ключа не пополняет множество
    предъявленного в сессии (Ф7-33, Р5) — вторая половина того, что Ф11 §6 передала сюда.
11. **Журнал аудита**: заведение и снятие ключа ложатся событием **своего вида** в поток аудита
    (§3.0) **в той же транзакции**, что строка ключа. Носитель — `kaname.audit_outbox`; форма
    вида взята у соседей (`iam.sa_key.issued` / `iam.sa_key.revoked`); эмиттер и дренаж в дереве
    **уже есть**, и Ф7 их не заводит — она добавляет два вида события. Это противовес остатку,
    названному Р5.
12. **Свой набор проб** — состав по свойству, §8; судьба сертификационного прогона — Р7.

### §4.2 За границей — с носителем на каждую строку

| вынесено | носитель | предикат границы |
|---|---|---|
| полоса входа без пароля и её последствия | **Ф13 `#1282`** | §4.5 — шов назван целиком |
| значение уровня уверенности и правило его вывода | **Ф11 `#1280`** | Ф7 производит флаги, Ф11 — значение |
| второй фактор, его обязательность, запасные коды | **Ф12 `#1281`** | ключ в роли второго фактора — та же проверка Ф7, иное правило заведения |
| экран церемонии, перечень ключей на экране, окно повышения | **Ф8 `#1274`** | предмет приёмки — что консоль **вызывает**, а не как выглядит (Ф1 §6, та же строка) |
| хранилище сессий, их срок и отзыв; момент последнего предъявления как поле сессии | **Ф3 `#1269`** | Р5 отсчитывает окно **от** этого момента и его не заводит |
| проверяющий пароля, строка способа, перенос личностей | **Ф2 `#1268`** | Р1 её словарь и таблицу не трогает |
| снятие компонента и сертификационного прогона вместе с ним | **Ф10 `#1276`** | Р7 |
| уведомление человека о заведении и снятии ключа | **почта, `#1773`** | у неё метка `blocked`; Р5 обещает журнал и перечень, а не письмо |
| служба метаданных о моделях аутентификаторов, список одобренных моделей | **носителя нет** — заводится своим изменением | требование арендатора о допустимых моделях (Р4) |
| сертификация FIDO как внешнее утверждение | **носителя нет** — внешний авторитет | членство либо требование арендатора (Р7) |

### §4.3 Перепись источников нормы — обход по свойству, обоими языками

Обход сделан **по свойству** («документ, решающий ключ доступа, его привязку, его уровень либо
его хранение»), а не по памяти, и **на двух языках**: предикат на одном недобирает молча
(`testing.md` §«Предикат по ДВУЯЗЫЧНОМУ корпусу»).

**Печатается ТРОЙКА по каждому дому: «корпус N · совпало M · адъюдицировано K».** Круг 1 ревью
назвал здесь блокирующее Б3, и перемер **подтвердил**: редакции 1 и 2 печатали «осмотрено 3» и
«осмотрено 30», выдавая **числитель за знаменатель**. Корпуса — **36** и **217**; совпавших — 3 и
30. Перепись без знаменателя есть та же «ноль находок при ноль прочитанном», и здесь она ею
была.

**Предикатов ДВА, и второй — не украшение.** Первый ищет **имя вида** (`webauthn`, `passkey`,
«ключ доступа»); документ, решающий наш предмет **другими словами**, выпадает из него молча — и
выпал: `credential-ceiling-per-principal.md` (APPROVED) не содержит ни одного вхождения первого
предиката и при этом определяет предмет дословно нашим («удостоверение — самостоятельный путь
входа: пока оно живо, предъявитель действует от имени принципала»). Второй предикат ищет
**предмет** — путь входа, удостоверение, способ входа, — и добирает ровно этот класс. Обход «по
свойству» без второго предиката свойством не был.

```sh
# ДОМ: PRO-Robotech/kaname · единица — ФАЙЛ корпуса приёмок дома
git ls-files 'docs/engineering/acceptance/*.md' | wc -l                    # КОРПУС → 36
git grep -l -i -E 'webauthn|passkey|ключ доступа|аппаратный ключ' -- 'docs/engineering/acceptance' | wc -l
#   → совпало 3 · адъюдицировано 3: Ф1 · ID-PW-1 · ЭТОТ документ (он лежит в том же каталоге и
#     попадает в собственный обход — ожидаемое самопопадание, а не находка)
# ВТОРОЙ ПРЕДИКАТ — ПО ПРЕДМЕТУ, а не по имени вида:
git grep -l -i -E 'путь входа|пути входа|удостоверен|способ входа|способа входа' \
  -- 'docs/engineering/acceptance' | grep -v access-keys-are-ours | wc -l
#   → совпало 6 · адъюдицировано 6; ЧЕТЫРЕ из них первый предикат не находит вовсе:
#     credential-ceiling-per-principal (APPROVED) · expired-credential-reclaim (APPROVED) ·
#     retention-sweep-has-a-caller (APPROVED) · roles-come-as-data-not-migrations (APPROVED)
# ДОМ: PRO-Robotech/kacho-workspace · те же два предиката по каталогу docs/specs
git ls-files 'docs/specs/*.md' | wc -l                                      # КОРПУС → 217
#   → первый: совпало 30 · адъюдицировано 12 (F4d · F4e · ID-MAIL-1 · 5.4 · SEC-acr-stepup ·
#     черновик 3.2 · черновик 3.3 · черновик 3.12 · черновик ID-PW-1 воркспейса · COMP-E1a ·
#     3.7b · IAM-INT-1). Остальные 18 — приёмки прежней службы личности, называющие ключ в
#     перечислении способов и о НАШЕМ предмете (привязка, хранение, перенос, уровень, потолок)
#     не решающие ничего. Их состояние ПРОВЕРЕНО, а не предположено: DRAFT — 17, и один
#     (`sub-phase-3.0-kac127-test-migration-matrix.md`) приёмкой не является вовсе — он сам
#     объявляет себя таблицей соответствий, статуса не несёт и вердикта не получал.
#     Одобренных среди этих 18 — НОЛЬ, и это несущая половина утверждения: одобренный сосед
#     числом не называется, он адъюдицируется поимённо
#   → второй: совпало 19, из них первым НЕ пойманы 14; по предмету адъюдицированы три,
#     чей предмет — удостоверение как РЕСУРС: BAT-1 (APPROVED) · KAN-AUTHN-1 (APPROVED) ·
#     KAN-QUOTA-1 (DRAFT). Остальные 11 называют удостоверение, решая членство, границу
#     фундамента, снятие семейства проверок и прочее, и о ключе не высказываются
#
# РАЗДЕЛЯЕТ ИХ ОДИН ВОПРОС: решает ли документ ключ доступа, его привязку, его хранение, его
# перенос, его уровень либо его УЧЁТ — или упоминает его, решая другое. Ответ по каждому
# адъюдицированному стоит в таблице ниже; остаток назван числом, а не растворён
#
# ФЛАГИ РАЗВЁРНУТЫ НАМЕРЕННО. Слитная форма `-lieE` разбирается как `-l -i -e E`: `-e` забирает
# следующую лексему как ОБРАЗЕЦ, и команда падает с «не удалось определить редакцию» — то есть
# промах слышен. Но соседний класс тише: флаг, взятый по аналогии с другой программой, бывает
# ПРОГЛОЧЕН, и тогда предикат меряет шире объявленного, ничего не сказав
# (`testing-verdict.md` §16). Эта редакция поймала себя на первой форме и оставляет вторую.
```

| источник | состояние | что решает о нашем предмете | исход здесь |
|---|---|---|---|
| **Ф1** | APPROVED | ключей не решает; выносит беспарольный вход в Ф13 | основание §0.1; расхождения нет |
| **Ф11** | APPROVED круга 3, санкция ВЫДАНА внешним событием (шапка), в ствол **не влито** | правило уровня; передаёт сюда политику аттестации, правило заведения ключа и поле множества предъявленного | Р4, Р5, Ф7-33 — исполнение переданного |
| **F4d** Р6 | APPROVED | ступень приписана **виду**, ключ → «3» без условия | заменено Ф11 Р3/Р10 п.1; здесь **не пересматривается** — §4.4 |
| **F4e** Р1, Р2 | APPROVED | словарь видов со ступенями, `webauthn` → «3»; уровень — максимум по видам | то же: заменено Ф11 Р10 п.1 и п.2, §4.4 |
| **F4d** Р4 | APPROVED | модель способов **уже допускает** ключ устройства, миграции данных не потребуется | §4.4 — утверждение верно о **виде** и не покрывает **экземпляра** |
| **F4d-13…15** | APPROVED | форма строки способа, уникальность парой | Р1 — не нарушается, §4.4 |
| **ID-PW-1** (дом службы) | APPROVED 2026-09-16 | проверяющий пароля, перепись форматов, предполётная перепись как форма | форма Ф7-21/Ф7-22 взята у PWV-12 сознательно; расхождения нет |
| `sub-phase-ID-PW-1-password-hash-verifier-acceptance.md` воркспейса | DRAFT | **та же** фаза до переезда дома приёмки; ключ называет одной строкой перечисления | нормой не является и предмета не решает; действует одобренная редакция дома службы (строка выше). Названа, чтобы «две ID-PW-1» не читались как расхождение |
| **ID-MAIL-1** | APPROVED | подтверждённость адреса | предмета не пересекает: ключ адреса не требует |
| **`sub-phase-5.4-iam-acr-on-internal-acceptance.md`** воркспейса | **APPROVED** (2026-06-16) | **цитирует** нормативную лестницу фундамента дословно: «hardware-bound UV passkey (AAL3)» | **адъюдицировано Ф11 Р3** — её таблица «условие „3“ называют десять документов семью формулировками» перечисляет обе поимённо. Расхождения с Р4 нет: документ цитирует лестницу, а не решает о ключе; ступень решает Ф11, здесь не пересматривается |
| **`sub-phase-SEC-acr-stepup-refinement-acceptance.md`** воркспейса | **APPROVED** (2026-07-22, R3) | то же сокращённо: «`"3"` hardware-bound / AAL3»; ею же заведено, что смена пола каталога — отдельное решение | **то же**: адъюдицировано Ф11 Р3. Здесь она нужна вторым предметом — пол «3» в каталоге сегодня не заведён ни одной записью (§1.4), и это её область, а не Ф7 |
| черновики воркспейса о ключе с DPoP (`sub-phase-3.2-…`) и о сертификационном прогоне (`sub-phase-3.12-…`) | DRAFT | «3» по флагу проверки пользователя; повышение аппаратным ключом с аттестацией | нормой не являются; Ф11 Р3 их уже перечислила и выбор сделала. Р4 расходится со вторым по аттестации — §4.4 |
| **`credential-ceiling-per-principal.md`** (дом службы) | **APPROVED**, круг 3 | потолок числа удостоверений принципала; **Д1**: «потолок ставится на РЕСУРС, а не на значение его поля-дискриминатора»; счёт ведётся по ресурсу, истёкшие считаются, существующие сверх предела не удаляются | **Р8** — ключ есть НОВЫЙ ресурс (Р1), значит по её же Д1 получает **свой** вид учёта `iam.user.accessKey`. Первым предикатом документа **не находится** (0 вхождений) — §4.4 расх. 7 |
| **`retention-sweep-has-a-caller.md`** (дом службы) | **APPROVED**, круг 7 | «у каждой таблицы iam, чей рост задаёт внешний, есть **названный механизм ограничения**»; уборка выбрана там, где у читателя есть предикат бессмысленности строки | **Р8** — таблица ключей ровно такова (рост задаёт человек), механизм назван и это **потолок**, а не уборка: у ключа нет срока и нет читателя, для которого строка становится бессмысленной, а снятие ключа за спиной владельца прямо запрещено Ф7-26 — §4.4 расх. 7 |
| **`expired-credential-reclaim.md`** (дом службы) | **APPROVED**, круг 2 | истёкшее удостоверение снимается платформой | предмета не пересекает: у ключа доступа **срока нет by construction**, истекать нечему, и сборщик остался бы без предмета. Названа, чтобы «удостоверения убирают по сроку» не читалось как норма о ключах |
| **`roles-come-as-data-not-migrations.md`** (дом службы) | **APPROVED**, круг 3 | роли модуля приезжают данными | предмета не пересекает; поймана вторым предикатом по одному вхождению слова «удостоверение» |
| **`sub-phase-3.3-iam-authz-fga-conditions-opa-acceptance.md`** воркспейса | **DRAFT** — и это ПЕРЕМЕРЕНО, см. врезку ниже | её P3.GWT-29 объявляет, что параметр условия `device_attestation` берётся из утверждения токена `ext_claims.kacho_device_compliance`, P3.GWT-30/31 — отказ и умолчание «unknown» | **решает НАШ предмет**, и редакция 2 отнесла её в «не решающие ничего» — ошибка, названная кругом 1 (Б2). Исход — §4.4 расх. 8 |
| **BAT-1** воркспейса | **APPROVED**, круг 4 | долгоживущий секрет-удостоверение с отзывом; ресурс `iam.credential` | предмета не пересекает: ключ — **другой** ресурс (Р1), и его потолок — Р8, а не её ось |
| **KAN-AUTHN-1** воркспейса | **APPROVED**, круг 10 | публичный слушатель принимает **предъявленное удостоверение** | предмета не пересекает: её предмет — отсечка анонима на публичном слушателе, а проверка утверждения ключа — наш глагол (§3.2). Вхождений имени вида — **0** |
| **KAN-QUOTA-1** воркспейса | DRAFT | модуль квотирования уходит из службы; три собственных потолка остаются и объявляются **посадкой**, без умолчания | нормой не является, но **механизм, который она произвела, в дереве есть и измерен** (Р8): таблица `kaname.own_ceilings` с закрытым множеством видов, ручки посадки, триггер списания. Р8 добавляет четвёртую величину той же формы |
| **COMP-E1a · 3.7b · IAM-INT-1** воркспейса | **APPROVED** | «ключ доступа гостя» (чужой домен, префикс `gak-`) · «re-Passkey» как «Дано» для ступени · перечень перехватов снимаемого компонента | ни одна не решает нашего предмета; **IAM-INT-1** называет перехваты `registration.after.webauthn` — они уходят с компонентом в **Ф10**. Внесены поимённо, потому что **одобрены**: остаток из 18 черновиков законно назван числом, одобренный сосед — нет |

> [!warning] Круг 1 ревью назвал приёмку 3.3 **одобренной** — она DRAFT, и предикат ревьюера
> был слеп ровно тем же способом, каким были слепы редакции 1 и 2
> Запись круга 1 снимает её статус предикатом `grep -m1 -oE 'APPROVED' docs/specs/sub-phase-3.3-*.md`
> → `APPROVED`. Перемер этой редакцией: слово совпало **строкой 3 того же файла**, и строка
> говорит дословно `> **Status**: DRAFT — awaiting `acceptance-reviewer` APPROVED.` То есть
> предикат нашёл слово в предложении, **отрицающем** то, что он им доказывал. Предикат с
> контролем, снимающий это за секунду:
>
> ```sh
> # ДОМ: PRO-Robotech/kacho-workspace
> grep -n -m1 'APPROVED' docs/specs/sub-phase-3.3-iam-authz-fga-conditions-opa-acceptance.md
> #   → 3:> **Status**: DRAFT — awaiting `acceptance-reviewer` APPROVED.
> grep -m1 -oE '(Статус|Status)[^|]{0,40}(APPROVED|DRAFT)' docs/specs/sub-phase-3.3-*.md
> #   → Status**: DRAFT
> ```
>
> **Половина находки от этого не падает, и это сказано прямо.** Блокирующее Б2 состояло из двух
> утверждений: (а) 3.3 одобрена — **опровергнуто**; (б) 3.3 **решает наш предмет**, а редакция 2
> отнесла её в «не решающие ничего» — **подтверждено** чтением её сценариев. Исход по (б) стоит в
> §4.4 расх. 8. Опровержение записано здесь, а не проглочено: возражение рецензента — тоже
> утверждение, и оно несёт предикат и ревизию наравне с утверждением автора
> (`git-issues-issue-lifecycle.md` §«Возражение рецензента — ТОЖЕ утверждение»).
>
> **Класс у всех трёх промахов один** — распознаватель, знающий одну форму записи предмета
> (`testing.md` §«Гейт на класс», п. 7). У автора это была именная форма имени условия (Б1), у
> рецензента — слово `APPROVED` без привязки к строке статуса. Поймано в обоих случаях **не
> внимательностью, а перемером**.

> [!warning] Редакция 1 пропустила ОБЕ одобренные строки выше — и это ровно тот класс, на котором
> сосед уже краснел
> Обход был объявлен «по свойству», но объём осмотренного по чужому дому **не печатался**, и
> шесть адъюдицированных строк читались как весь обход. Ф11 в своём круге 2 получила то же
> замечание теми же словами («редакции 1 и 2 называли пять документов и пропустили обе
> одобренные») — то есть класс известен и переносится в форму работы, а не в замечание:
> **обход по свойству обязан печатать знаменатель по каждому дому**. Числа выше и есть этот
> знаменатель.

### §4.4 Расхождения с одобренными соседями — исход по каждому

**Расхождение 1: ступень ключа. F4d Р6 и F4e Р1 дают «3» без условия; Ф11 Р3 даёт «3» только при
проверке пользователя и отсутствии признака резервного копирования.**
**Исход: заменено НЕ здесь.** Замену объявила Ф11 своим Р10 п.1, поимённо и с доводом; настоящий
документ её **не повторяет и не пересматривает**. Что из этого следует для Ф7 — единственная
вещь: правило читает **флаги**, значит их обязана доносить проверка ключа, и это Ф7-28…30.

**Санкция Ф11 ВЫДАНА** — внешнее событие названо в шапке (`issue_comment` 5690585262, 2026-09-16,
`ban1_lifted: true`); редакция 1 утверждала обратное по просроченной ревизии ветки. На исход спора
это не влияет: он о **замене** F4d/F4e, а не о существовании Ф11. Если замену когда-нибудь
отзовут, сценарии §3.8 остаются верными дословно — они утверждают доезд флагов, а не значение
уровня. То есть Ф7 исполнима при обоих исходах спора, и это проверяемый признак того, что документ
в чужой спор не влез.

**Одно место зависит от Ф11 сильнее — и оно названо отдельно.** Ф7-33 наблюдает **множество
предъявленного**, а это поле объявлено Ф11 Р1, то есть частью, которая в споре **не участвует**:
спор идёт о лестнице Р3. Столбец «исполнимо с» §5 у Ф7-33 поэтому называет Ф11 явно, а не
подразумевает её.

**Расхождение 2: F4d Р4 — «ключ устройства эта модель УЖЕ допускает, миграции данных при его
добавлении не потребуется».**
**Исход: утверждение верно о ВИДЕ и не покрывает ЭКЗЕМПЛЯРА; расширяется, а не отменяется.**
Модель способов действительно допускает новый **вид** без переноса строк — ровно это и
демонстрирует F4d-14 строкой «заведение третьего вида не требует правки ни одной существующей
строки». Но «несколько ключей у одного человека» — не вид, а экземпляры одного вида, и о них F4d
Р4 не высказывается. Р1 поэтому не правит F4d-14 и не ослабляет его: его предикат после Ф7 даёт
тот же ноль (Ф7-16). **Кто исполняет: Ф7 `#1273`** — своей таблицей; Ф2 `#1268` не трогается.

**Расхождение 3: F4e Р2 — «уровень сессии есть МАКСИМУМ по названным видам».**
**Исход: заменено Ф11 Р2 и Р10 п.2 (правило по множеству предъявленного), здесь не
пересматривается.** Для Ф7 различия нет ни в одном сценарии: оба правила читают один и тот же
литерал `webauthn`, и оба зависят от того, доехало ли утверждение ключа до точки решения, — а это
и есть предмет §3.8.

**Расхождение 4: черновик воркспейса о сертификационном прогоне — повышение аппаратным ключом с
ПОДТВЕРЖДЁННОЙ аттестацией.**
**Исход: Р4 расходится с ним осознанно и называет цену.** Черновик нормой не является (DRAFT), а
его требование сделало бы ступень «3» зависимой от внешнего авторитета на пути запроса. Условие
возврата названо в Р4 и оно внешнее — требование арендатора о допустимых моделях. Документ не
объявляет черновик неверным: он объявляет, что **сегодня** у его требования нет ни носителя, ни
производителя, и заводить их ради ступени, которую флаги дают дешевле, — работа без предмета.
Отдельно: сегодня эта ступень **не наблюдаема арендатором вовсе** — записей каталога с полом «3»
ноль (§1.4).

**Расхождение 5: продукт обещает проверку аттестации КЛЮЧА, которой Р4 не строит, и носителей
обещания ДВА (§1.8).**
**Исход: снимаются ОБА носителя — строка публичной страницы (Ф7-32) и деривация значения токена
из наличия ключа (Ф7-39).** Редакция 2 называла один носитель («ровно одна публичная строка») и
закрывала расхождение **правкой текста**; круг 1 ревью назвал это блокирующим Б1, и перемер
подтвердил: второй носитель — **код**, и вход у него тот же самый ключ. Снять один и оставить
другой значило бы закрыть экземпляр и оставить класс.

Предмет по-прежнему **сужен**, и сужение стало точнее: Ф7 не трогает **объявления условия** в
каноне — оно описывает устройство, его снятие означало бы выбрать одно из пяти ненавешенных
(закрыть экземпляр, оставить класс) и тронуть канон, чья перепись есть посылка одобренного
соседа. Значение `kaname_device_compliance` **остаётся** на всех шести полосах; исчезает
**утверждение** «attested». Остаток — §10 п. 11, а не умолчание.

**Расхождение 6: посылка одобренного соседа `composed-model-admits-only-what-it-owns.md` (§2.1,
«условий канона **6**», `device_compliant` поимённо).**
**Исход: расхождения НЕТ, потому что Ф7 канона не трогает** (расхождение 5). Строка внесена сюда
не как спор, а как **измеренная цена** отвергнутого решения: сними Ф7 объявление условия — и
посылка одобренного документа стала бы неверной побочным эффектом чужой фазы, а её правка
отвязала бы вердикт соседа от отпечатка (`change-graph.md` §2). Цена названа затем, чтобы
следующая редакция не приняла отказ за недосмотр.

**Расхождение 7: одобренные соседи дома требуют механизма ограничения для новой таблицы, и Ф7
о нём молчала.**
`credential-ceiling-per-principal.md` (APPROVED) своим **Д1** ставит потолок **на ресурс**;
`retention-sweep-has-a-caller.md` (APPROVED) требует **названного механизма ограничения** у
каждой таблицы службы, чей рост задаёт внешний. Таблица ключей Р1 — новый ресурс, и рост её
задаёт человек.
**Исход: расхождение снято решением Р8** — ключ получает **свой** вид учёта `iam.user.accessKey`,
величина объявляется посадкой, умолчания нет, три исхода величины различимы (Ф7-37, Ф7-38).
Механизмом выбран **потолок**, а не уборка, и выбор обоснован тем же критерием, которым
`retention-sweep` выбирала уборку: у ключа нет срока и нет читателя, для которого строка
становится бессмысленной, а снятие ключа за спиной владельца прямо запрещено Ф7-26. Считать ключ
в `iam.user.credential` нельзя по Д1 того же соседа: подчинённый ресурс `iam.credential` анкерен
двумя таблицами строк, и таблицы ключей среди них нет. **Кто исполняет: Ф7 `#1273`** — своей
миграцией (множество видов закрыто ограничением таблицы, применённую не правят — ban #5), своей
ручкой посадки и существующим триггером списания.

**Расхождение 8: черновик 3.3 воркспейса специфицирует ПРОИЗВОДЯЩУЮ ЦЕПЬ того, что Р4 строить
отказывается.**
Её P3.GWT-29 объявляет, что параметр условия `device_attestation` берётся из утверждения токена
`ext_claims.kacho_device_compliance`; P3.GWT-30/31 описывают отказ и умолчание «unknown».
Редакция 2 отнесла её в «не решающие ничего» — ошибка, названная кругом 1 (Б2, половина (б)).
**Исход: расхождения по существу НЕТ, и оснований тому три, каждое проверено предикатом.**

1. **Она не норма.** DRAFT — перемерено этой редакцией (врезка §4.3); запись круга 1 назвала её
   одобренной слепым предикатом, и это опровергнуто.
2. **Имени, которое она называет, в деревьях НЕ существует.** `kacho_device_compliance` —
   ноль вхождений и в доме службы, и в дереве платформы на пине `45d1719fe8`; продукт производит
   `kaname_device_compliance`. То есть её координата пережила свой предмет вместе с
   переименованием службы.
3. **Цепь, которую она специфицирует, мертва на последнем шаге при любом значении**: сборщик
   контекста службы удаляет `device_attestation` из присланного краем (§1.8), и удаление —
   верное. Ф7-39 снимает **начало** той же цепи; её середина (перевод значения в атрибут на
   крае) принадлежит краю платформы, конец — области прав.

**Её собственный предмет — условия и перехваты снимаемого компонента — уходит с ним в Ф10
`#1276`**, ровно как у сертификационного прогона (Р7) и у перехватов IAM-INT-1. Настоящий
документ её сценариев не отменяет и не переписывает: чужой черновик правится своим кругом.

### §4.5 Шов с Ф13 — назван целиком, в обе стороны

Тело `#1282` проводит границу само: «Регистрацию и проверку самого ключа делает Ф7; здесь —
полоса ВХОДА и её последствия. Разделено намеренно: Ф7 нужна и второму фактору тоже». Здесь та же
граница названа предикатами, чтобы её можно было проверить, а не вспомнить.

| предмет | чей | предикат шва |
|---|---|---|
| церемония регистрации, испытание, его однократность | **Ф7** | §3.1 |
| сверка подписи, привязка, счётчик | **Ф7** | §3.2, §3.3, §3.5 |
| хранилище ключей и несколько ключей у человека | **Ф7** | §3.4 |
| перенос трёх ключей | **Ф7** | §3.6 |
| **обнаружение ключа до входа** — кому какие ключи предлагать, не назвав человека | **Ф13** | ни один сценарий §3 не описывает шага, предшествующего названию человека |
| **выдача сессии по утверждению ключа** | **Ф13** | ни одно «Тогда» §3 не утверждает **выдачи** сессии — вторая величина предиката ниже; §3.0 определяет «утверждение проходит» без неё |
| **множество предъявленного в сессии: что в него попадает** | **Ф11** — правило; **Ф7** — что регистрация в него не попадает | Ф7-33 утверждает **неизменность** множества под действием Ф7 и не утверждает, что в нём окажется после предъявления |
| **путь при утрате ключа у человека без пароля** | **Ф13** — тело задачи требует это решить | Ф7-24 называет таких людей **числом** на переносе и решения о пути не принимает |
| **уровень после входа ключом** | **Ф11**, исполняет Ф13 | §3.8 утверждает доезд флагов, не значение |
| **потолок числа ключей человека** | **Ф7** — Р8 | Ф13 полосу входа ключом строит поверх уже принятых ключей; сколько их допустимо, решает заведение, и заведение — наш глагол |

**Предикат шва в обратную сторону, который ревьюер обязан прогнать.** Он судит **строки «Тогда» и
«И»**, а не число вхождений слова: считать строки со словом «сессия» пробовала редакция 1, и такой
счёт разошёлся с предметом при первой же правке — слово законно стоит в «Дано», в прозе и в
разборе, а запрещено ровно одно, и не слово, а **утверждение**.

```sh
# ДОМ: PRO-Robotech/kaname · единица — СТРОКА цепочки «Тогда/И» одного сценария
python3 -c "
import re,io
s=io.open('docs/engineering/acceptance/access-keys-are-ours.md',encoding='utf-8').read()
sec=s.split('## §3 ')[1].split('## §4 ')[0]
lvl=re.compile(r'(?:уровн|уровен)\w*[^.]{0,40}(«1»|«2»|«3»)')
iss=re.compile(r'сесси\w* выдан|выдан\w* сесси')
lines=[]
for b in re.split(r'(?=^\*\*ID: Ф7-)', sec, flags=re.M):
    m=re.match(r'\*\*ID: (Ф7-\d+)\*\*', b)
    if not m: continue
    i=b.find('**Then**')
    if i<0: print('БЕЗ «ТОГДА»:', m.group(1)); continue
    for ln in b[i:].split('\n\n')[0].splitlines(): lines.append((m.group(1), ln))
a=[t for t in lines if lvl.search(t[1])]; c=[t for t in lines if iss.search(t[1])]
print('меток', len({t[0] for t in lines}), '· строк «Тогда/И» осмотрено', len(lines),
      '· называющих ЗНАЧЕНИЕ уровня', len(a), '· утверждающих ВЫДАЧУ сессии', len(c))
for t in a+c: print('  НАРУШЕНИЕ ШВА:', t)"
#   → меток 39 · строк «Тогда/И» осмотрено 163 · называющих ЗНАЧЕНИЕ уровня 0
#     · утверждающих ВЫДАЧУ сессии 0
# Редакция 2 давала «33 · 97 · 0 · 0»; шесть сценариев редакции 3 подняли обе первые величины,
# и обе печатаются рядом намеренно: выросшая перепись при неизменных нулях есть свидетельство
# того, что шов держится на БОЛЬШЕМ объёме, а не что предикат перестал смотреть
```

**ТРИ величины печатаются вместе, и это не украшение.** «Ноль нарушений» при нуле осмотренных
строк — не вердикт; поэтому метки и строки стоят рядом с находками, а метка без «Тогда» называется
отдельной строкой и роняет предикат.

**Оба корня слова «уровень» обязательны, и эта редакция поймала себя на одном.** Первая форма
предиката несла только `уровн` — и **не видела именительного падежа**: «уровень» пишется через
`е` между `в` и `н`, поэтому `уровн` находит «уровня», «уровне», «уровнем» и молчит на «уровень».
Проба формы это показала сразу: строка-дефект `уровень сессии становится «2»` предикатом **не
ловилась**. Тот же класс, что в `testing.md` §«Предикат по ДВУЯЗЫЧНОМУ корпусу», только на
склонении вместо языка: распознаватель, знающий одну форму, не краснеет и не зеленеет — он молчит.

**Способность предиката упасть доказана в обе стороны** — семь входов, и каждый назван:
дефект `уровень сессии становится «2»` · дефект `уровня «3» достаточно` · дефект
`выдана сессия по утверждению ключа` — **ловятся**; законные близнецы
`значение уровня здесь не утверждается` · `множество предъявленного этой сессии не пополнилось` ·
`оба флага доезжают до точки, где принимается решение об уровне` · `отказ; ключ не заведён` —
**молчат**. Без близнецов предикат, ловящий всякую строку со словом «уровень», был бы красным на
самом шве.

**Что Ф7 даёт Ф13 и чего не даёт, названо парой.** Даёт: принятый ключ, проверенное утверждение,
флаги этого утверждения, перечень ключей человека. Не даёт: полосы, на которой человек ещё не
назван; сессии; уровня; ответа на вопрос, что делать с утратившим единственный ключ.

### §4.6 Что правила редакция 2: восемь находок встречного разбора, исход по каждой

Разбор был **до** первого круга ревью и вне его. Его находки взяты **как данные** и перемерены
этой редакцией; по двум уточнена единица счёта, и уточнение изменило исход. Раздел нужен затем,
чтобы ревьюер видел, что уже разобрано, и не платил за тот же класс третий раз.

| # | предмет находки | перемер этой редакцией | исход в тексте |
|---|---|---|---|
| **Б1** | §1.8 называла производителем согласия устройства обогащение токена и «поле запроса» | **подтверждено**: `grep -cE 'device_attestation\|device_compliant\|attestation' internal/service/token_enrichment_service.go` → **0** при контроле `grep -c -i webauthn` → **2** (652 строки, файл читается). Прод-сайты — только канон и `authorize_service.go`, где значение **удаляется** из клиентского контекста | §1.8 переписана по факту; класс переназван на **«неисполнимую возможность»**; Ф7-32 переписан — прежнее «Тогда» было **истинно на дереве уже сегодня** и честного красного дать не могло |
| **Б2** | радиус снятия назван двумя местами, а их больше | **подтверждено, единица уточнена**: `git grep -lE 'device_compliant\|device&#95;compliant\|device_attestation\|allowed_attestations'` → **11** файлов; без экранированной формы — **10**, и молча теряется страница архитектуры. Инъекция (снятие условия из обеих копий канона, откачено) не покраснила **ничего**: `internal/{authzmodel,authzplan,domain,service,repo/.../relverdict}` и `internal/check` — `ok`. **Уточнение:** поля с номером у `device_attestation` в контракте **нет** — это строка комментария о ключе внутри `google.protobuf.Struct`, поэтому `reserved` предметом не является | §1.8 несёт таблицу радиуса с исходом по каждому месту; **предмет Ф7 сужен до одной публичной строки** — довод в §1.8 и §4.4 расх. 6; остаток назван числом в §10 п. 11 |
| **Б3** | Ф7-01 утверждал «уровень сессии не изменился», а инв. 12 запрещал утверждать о сессии и уровне | **подтверждено**: противоречие дословное; сверх того «не изменился» наблюдаемо лишь там, где уровню есть чем быть, а поле объявила Ф11 Р1 | третье «И» вынесено из Ф7-01 в **свой сценарий Ф7-33** (§3.10) с «исполнимо с» = Ф2, Ф3, **Ф11**; инв. 12 переписан в две точные половины; §3.1 от Ф11 **не** зависит |
| **Б4** | шапка называла голову Ф11 `a65bab135d` и утверждала, что санкции не выдано | **подтверждено**: `git ls-remote` → **`644b95e27b`**; коммит события лёг на **32 минуты раньше** редакции 1; событие `issue_comment` **5690585262** от 2026-09-16T01:21:38Z прочитано через API, `verdict: APPROVED`, `ban1_lifted: true`, `subject_sha256` совпал | шапка приведена к факту; §4.4 расх. 1 переписано; отпечаток документа Ф11 **не менялся**, поэтому ложным был вывод о санкции, а не координата предмета |
| **Б5** | Р1 держал колонку «признак резервного копирования на момент регистрации» без читателя | **подтверждено**: ни одно «Тогда» §3 её не наблюдает; Ф11 Р3 берёт различитель из флагов **этого** утверждения, а не из памяти о регистрации | колонка **снята** из Р1 с тремя доводами и условием возврата; рядом назван сценарий-читатель для **каждой** оставшейся колонки |
| **Б6** | DoD п. 10 объявлял держателем координат гейты, которые чужих путей не судят | **подтверждено инъекцией**: мёртвый **чужой** путь — оба гейта `ok`; мёртвый **свой** — `FAIL` с координатой. Перепись гейта: координат-путей **290**, в чужом доме **31**, связаны ревизией **0**, и путь, названный **прозой**, не судится вовсе. (Здесь стояло **287** против **288** в §4.7 — два числа об одном предмете; круг 1 назвал это не блокирующим В1. Прогон редакции 3 даёт **290**, и оно стоит в обоих местах.) Обратная сторона: **имена проб** в чужом доме гейт резолвит — **107** проверено в доме | §4.7 — ведомость четырёх чужих координат, все привязаны ревизией `@45d1719fe8`; DoD п. 10 называет границу дословно; §0.3 больше не утверждает, что гейт их резолвит |
| **Б7** | обход соседей объявлен «по свойству», знаменатель по чужому дому не печатался, две одобренные пропущены | **подтверждено**: предикат по `docs/specs` воркспейса → **30** файлов; пропущены `sub-phase-5.4-iam-acr-on-internal` (APPROVED 2026-06-16) и `sub-phase-SEC-acr-stepup-refinement` (APPROVED 2026-07-22) | §4.3 печатает знаменатель по **обоим** домам (3 и 30) и адъюдицирует **8** файлов воркспейса; обе одобренные внесены с исходом «адъюдицировано Ф11 Р3 — цитируют лестницу фундамента, о ключе не решают» |
| **Б8** | дословная цитата «три человека переживут пересоздание» приписана эпику | **подтверждено**: `gh issue view 1266 … \| grep -c 'переживут'` → **0** при контроле `grep -c -i 'ключи'` → **2**; фраза стоит в теле **задачи `#1273`**; поиск по трём репозиториям даёт `kacho` 1 · `kaname` 0 · `kacho-workspace` 0 | оба места (§1.6 и Р3 п. 3) названы правильно, с предикатом и контролем прямо в тексте |

**Опровергнутого целиком нет; уточнена единица счёта у двух.** Б2 — `reserved` у имени
`device_attestation` предметом не является (поля с номером нет); Б8 — эпик говорит **своё** число
(«ключи доступа: 3 личности»), и оно осталось в тексте как число **личностей**.

### §4.7 Чужие координаты: ведомость, привязка ревизией и честная граница гейта

Документ опирается на **четыре** адреса дерева платформы. Все четыре названы **прозой** и
привязаны ревизией `@45d1719fe8`; ни один не стоит путём-спецификацией в команде.

| координата (дом `PRO-Robotech/kacho@45d1719fe8`) | что доказывает | когда её предмет умрёт |
|---|---|---|
| `deploy/helm/umbrella/charts/kaname/templates/_kratos-identity.tpl` | §1.2 целиком: обе величины привязки и беспарольный режим | **Ф10** — компонент снимается вместе с шаблоном |
| `deploy/identity_domainless_landing_is_expressible_test.go` | §1.3: выключенность ключей на посадке без доменного имени выразима | **после Ф7** предмет переезжает к нам вместе с величинами Р2 |
| `ui-future/shared/src/lib/step-up-methods.ts` | §1.4: сегодняшний производитель даёт ключу «1» (цитата дословная) | живёт дольше Ф7; переживёт её |
| `deploy/tests/conformance/fido/run-fido-conformance.sh` | §1.7 и Р7: прогон существует, вызывающих ноль, предмет чужой | **Ф10** — снимается вместе с компонентом (Р7) |

**Все четыре живы на названной ревизии** — проверено `git cat-file -e 45d1719fe8:<путь>` в рабочей
копии дерева платформы, четыре успеха из четырёх.

**Что делает гейт дома, сказано по ЗАМЕРУ, а не по имени гейта:**

| ось | что делает `TestAcceptancePathCoordinateResolves` | замер |
|---|---|---|
| **свой** путь в команде после `--` | **судит**: мёртвый — находка с координатой | инъекция: `FAIL`, «МЁРТВЫЙ ПУТЬ ПРЕДИКАТА … :1312» |
| **чужой** путь в команде после `--` | **не судит**: считает и печатает дом | инъекция: `ok`, находок 0 |
| путь, названный **прозой** | **не видит вовсе** | перепись гейта говорит это своей строкой |
| **имя пробы** в чужом доме | **судит** — резолвит копию дома рядом | вторая перепись: «координат проверено в доме 107 · находок в доме 0» |

Перепись гейта на дереве этой редакции (**ориентир**, не гейт — числа растут с каждой приёмкой):
приёмок 36 · координат-путей **290** · резолвится 259 · в чужом доме 31 · связаны ревизией **0** ·
находок 0. **Величина сдвинулась от правки САМОГО документа**, а не от чужой работы: второй
предикат §4.3 добавил две координаты-пути, и обе резолвятся. Это ровно тот механизм, из-за
которого редакции 2 разошлись два её собственных числа; здесь он назван, а не снят — снять его
нельзя, документ попадает в собственный обход by construction. Рядом — вторая перепись того же гейта: «путей-спецификаций без `--` **55** · магических **80**
· эллипсисов прозы 2», и её последняя строка говорит дословно, что путь, названный прозой, гейт не
судит вовсе. Первые две величины сдвинулись правкой **этого** документа (редакция 2 давала 53 и
78): второй предикат §4.3 и строки §4.8 добавили свои формы — то есть перепись растёт вместе с
текстом, который она осматривает, и число здесь **ориентир**, а не гейт.

**Читать эти числа надо так: четырёх координат этого документа в них НЕТ ни в одном столбце.** Они
прозаические, значит не попали ни в 290 «координат-путей», ни в 31 «в чужом доме»; `0` в столбце
«связаны ревизией» относится к тем 31, а не к ним. То есть привязка `@45d1719fe8` **гейту не
видна** и адресована человеку — ровно поэтому она названа единственным держателем, а не
подкреплением к машинному.

**Держатель у этих четырёх поэтому один — привязка ревизией, и это сказано прямо.** Она не делает
их проверяемыми машиной; она делает утверждение **срочным**: оно о названной ревизии, а не о
дереве вообще, и потому не протухает молча. Второй держатель выразим — перепись в доме платформы,
— и его здесь **нет**; заводить его дороже, чем стоит предмет, и это решение, а не недосмотр
(§10 п. 12).

### §4.8 Круг 1 ревью: шесть блокирующих и пять не блокирующих — исход по каждому

Вердикт круга 1 — **CHANGES REQUESTED**; запись ревью привязана к отпечатку редакции 2
(`change-graph.md` §2) и лежит в доме службы, каталог `docs/specs/reviews`. Раздел нужен затем,
чтобы следующий круг видел, что уже разобрано, и не платил за тот же класс третий раз, — и затем,
чтобы **опровержение одной посылки рецензента было названо вместе с предикатом**, а не проглочено:
возражение рецензента есть утверждение того же рода, что утверждение автора, и связано теми же
требованиями (`git-issues-issue-lifecycle.md` §«Возражение рецензента — ТОЖЕ утверждение»).

**Перемерено ВСЁ; опровергнута одна посылка из одиннадцати, и опровержение частичное.**

| # | предмет находки | перемер этой редакцией | исход в тексте |
|---|---|---|---|
| **Б1** | перепись §1.8 слепа ко ВТОРОЙ форме имени (`compliance` против `compliant`, `attested` против `attestation`); производитель значения ЕСТЬ, и вход у него — сам ключ | **подтверждено**: предикат редакции 2 → **0**, именная форма → **7**; `git grep -n 'kaname_device_compliance' -- internal ':!*_test.go'` → **6 мест**, пять ставят «unknown», одна выводит «attested» по выданной области `webauthn`/`passkey`. Радиус обеими формами — **15** мест дерева против **10** одной (артефакты круга исключены, единица названа) | §1.8 переписана: две единицы вместо одной, три формы имени, таблица радиуса с двумя новыми строками; §1.9 разведена на две строки; **заведён Ф7-39** — снятие второго носителя; §4.4 расх. 5 переписано; §4.1 п. 9, §8, §9.1 п. 9, §10 п. 13 |
| **Б2** | `sub-phase-3.3-…` решает наш предмет и отнесена в «не решающие ничего»; названа **одобренной** | **половина ОПРОВЕРГНУТА, половина подтверждена**. Опровергнуто: она **DRAFT** — предикат записи `grep -m1 -oE 'APPROVED'` совпал со словом внутри строки `> **Status**: DRAFT — awaiting acceptance-reviewer APPROVED.`, то есть в предложении, **отрицающем** доказываемое (врезка §4.3). Подтверждено: её P3.GWT-29 действительно специфицирует производящую цепь | §4.3 несёт её строку с **верным** состоянием и врезку с опровержением; §4.4 — **расхождение 8** с тремя основаниями, каждое с предикатом: не норма · имя `kacho_device_compliance` не существует ни в одном дереве (**0** вхождений на пине и в доме) · цепь мертва на последнем шаге |
| **Б3** | «осмотрено 3» и «осмотрено 30» — ЧИСЛИТЕЛЬ, поданный знаменателем; обход «по свойству» исполнен по словам, и одобренный сосед о потолке выпал молча | **подтверждено**: корпуса — **36** и **217**; `credential-ceiling-per-principal.md` (APPROVED) даёт **0** вхождений первого предиката при определении предмета дословно нашим | §4.3 печатает **тройку** по каждому дому и несёт **второй предикат — по предмету**; им добраны четыре одобренных соседа дома; заведено **решение Р8** (свой вид учёта `iam.user.accessKey`), сценарии **Ф7-37** и **Ф7-38**, §4.4 расх. 7, §4.1 п. 4, §9 п. 7, §9.1 п. 4, §10 п. 12 |
| **Б4** | §4.1 и §5 расходятся В ОБЕ СТОРОНЫ: срок испытания объявлен объёмом и не описан; перечень ключей описан и не объявлен | **подтверждено**: сценариев о сроке в §3 — **0** при объявлении «однократность и срок» в §4.1; «перечень ключей» в §4.1 — **0** вхождений при том, что им §3.0 определяет «ключ принят» | заведён **Ф7-34** (срок, обе стороны, с выведенной верхней границей); §4.1 переписан на **12** пунктов, перечень ключей и журнал названы; §5 несёт **перепись соответствия** «пункт §4.1 → его сценарии» |
| **Б5** | величин контракта ТРИ, а не две: перечень алгоритмов объявлен сценариями и не имеет ни объявителя, ни стороны «не задана»; полосы церемонии у оси нет | **подтверждено**: `Ф7-13`-блок содержал **0** вхождений слова «алгоритм»; в §3 их было **3**, все в полосе переноса | **Р2 переписан на три величины**; Ф7-13 получил третью ветвь и требование называть **свою** ручку; заведён **Ф7-35** — полоса церемонии; §7 инв. 5 переписан на четыре величины; §8 — две новые строки |
| **Б6** | ось свежести на снятии имеет только положительную сторону | **подтверждено**: сценариев со словом «свежест» — Ф7-01, Ф7-04, Ф7-25, Ф7-33, и отрицания на снятии среди них нет; §8 называл держателем окна **один** Ф7-04 | заведён **Ф7-36** с положительным близнецом; §8 — строка «обе полосы» с разбором того, какая половина держится, если оставить одну; §4.1 п. 7 |
| **В1** | одна перепись гейта названа двумя числами: 287 и 288 | **подтверждено**; прогон редакции 3 даёт **290** — величина сдвинулась снова, и снова от правки самого документа (два новых предиката §4.3), то есть механизм расхождения **назван**, а не устранён | обе координаты приведены к **290**, и рядом сказано, почему число будет двигаться дальше: документ попадает в собственный обход by construction |
| **В2** | «три предиката снятия» — в теле `#1273` их четыре | **подтверждено** чтением тела через API | Р7 называет **четыре** и предикат, которым это проверяется |
| **В3** | «попало в журнал» — журнал не назван, а в доме их несколько | **подтверждено, единица уточнена**: журналов **три**, а не два (`kaname.audit_outbox` · `kaname.identity_journal` · `kaname.resource_journal`), и два других считают другое — личности ради метрики роста и ресурсы | Ф7-01 и Ф7-25 называют **журнал аудита** `kaname.audit_outbox` поимённо, с формой события и требованием одной транзакции; §4.1 п. 11 |
| **В4** | «Дано» Ф7-33 говорит «кладёт её Ф3», Ф11 Р1 говорит мягче | **подтверждено**: `grep -n 'какая бы из двух фаз'` на документе Ф11 → **1** | «Дано» Ф7-33 и строка ведомости §5 приведены к формулировке Ф11 Р1 — «Ф2 либо Ф3» |
| **В5** | DoD п. 7 требует исхода числом, тогда как §1.6 объявляет стенд недосягаемым; оговорки «условие не создано» у п. 7 нет | **подтверждено**: оговорка стояла только у п. 11 | п. 7 несёт оговорку третьей категории |

**Что круг 1 НЕ оспорил, и это важно для следующего:** ни одно решение Р1…Р7 этим кругом не
отвергнуто. Запись прямо говорит, что судит **полноту и истинность утверждений о дереве и о
соседях**, а не выбор; Р4 (аттестацию не требовать) подтверждён независимым перемером каталога
(**338** записей, пола «3» среди них нет), Р1 — чтением F4d-14 и миграции ветки Ф2 П1. Редакция 3
поэтому ни одного решения не пересматривает и **добавляет одно** — Р8, которого требовали два
одобренных соседа.

---

## §5 Ведомость производителей: сценарий → производитель

Раздел существует потому, что приёмка пишется **до** кода (ban #1): ни одно «Тогда» этого
документа сегодня не производится ничем (§1.1), и это надо сказать прямо, а не оставить читателю
выводить из тишины. Ведомость сверяется по трём осям, как у Ф1 §5: наличие в обе стороны · **род**
требуемого против того, что фаза обязана произвести · сверка номеров фаз с телом эпика `#1266`.

**Столбцов четыре.** «Тогда производит» — фаза, чьё изменение делает исход верным. «Исполнимо с» —
фазы, без которых не построить «Дано». Сценарий исполняется в DoD фазы, влитой последней из
названных в «исполнимо с».

| сценарий | Тогда производит | род | исполнимо с | в DoD |
|---|---|---|---|---|
| Ф7-01 | **Ф7** — приём ключа, перечень ключей, запись журнала | запись + значение | Ф2 (пароль), Ф3 (сессия и её свежесть) | **Ф7** |
| Ф7-02 | **Ф7** — связанность результата с выданным испытанием | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-03 | **Ф7** — однократность испытания | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-04 | **Ф7** — окно свежести на действии; **якорь** момента — Ф3 (Ф11 Р6) | отказ + текст следующего шага | Ф2, Ф3 | **Ф7** |
| Ф7-05 | **Ф7** — уникальность идентификатора удостоверения и неразличимость отказа | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-34 | **Ф7** — срок испытания как величина контракта и отказ по нему | отказ + проход | Ф2, Ф3 | **Ф7** |
| Ф7-35 | **Ф7** — сверка алгоритма открытого ключа с перечнем **на приёме церемонии** | отказ + проход | Ф2, Ф3 | **Ф7** |
| Ф7-06 | **Ф7** — сверка подписи | проход + значение | Ф2, Ф3 | **Ф7** |
| Ф7-07 | **Ф7** — единый отказ | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-08 | **Ф7** — сверка ключа названной строки | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-09 | **Ф7** — побайтовое равенство; критерий времени — Ф1-48…50, его форма уже одобрена | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-10 | **Ф7** — сверка происхождения с перечнем | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-11 | **Ф7** — решает перечень, а не заголовок | отказ + проход | Ф2, Ф3 | **Ф7** |
| Ф7-12 | **Ф7** — привязка к имени доверяющей стороны | отказ + проход | Ф2, Ф3 | **Ф7** |
| Ф7-13 | **Ф7** — страж старта; форма отказа старта — Ф1 §7 инв. 4 | отказ старта | — | **Ф7** |
| Ф7-14 | **Ф7** — таблица ключей | запись | Ф2, Ф3 | **Ф7** |
| Ф7-15 | **Ф7** — ограничение базы на идентификатор удостоверения | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-16 | **Ф7** — независимость таблиц; строка способа — **Ф2** | неизменность | **Ф2**, Ф3 | **Ф7** |
| Ф7-37 | **Ф7** — вид учёта `iam.user.accessKey`, его миграция и списание оператором базы; **механизм потолка** — уже в дереве (`kaname.own_ceilings`, триггер `kacho_quota_count`) | отказ + проход | Ф2, Ф3 | **Ф7** |
| Ф7-38 | **Ф7** — четвёртая ручка посадки и страж старта на незаданной величине; форма отказа старта — Ф1 §7 инв. 4 | отказ старта + отказ | — | **Ф7** |
| Ф7-17 | **Ф7** — сдвиг счётчика | значение | Ф2, Ф3 | **Ф7** |
| Ф7-18 | **Ф7** — отказ и громкое состояние | отказ + величина | Ф2, Ф3 | **Ф7** |
| Ф7-19 | **Ф7** — односторонность дисциплины | проход | Ф2, Ф3 | **Ф7** |
| Ф7-20 | **Ф7** — атомарный сдвиг | неизменность под конкуренцией | Ф2, Ф3 | **Ф7** |
| Ф7-21 | **Ф7** — перепись источника и её объём | величина | — (источник — настройка §1.2) | **Ф7** |
| Ф7-22 | **Ф7** — падение переноса на непокрытом | отказ + величина | — | **Ф7** |
| Ф7-23 | **Ф7** — принятие перенесённого материала | проход | — | **Ф7** |
| Ф7-24 | **Ф7** — отдельное число запертых | величина | **Ф2** (наличие второго способа) | **Ф7** |
| Ф7-25 | **Ф7** — снятие ключа и журнал | запись | Ф2, Ф3 | **Ф7** |
| Ф7-26 | **Ф7** — отказ на последнем способе; «какие способы есть» — **Ф2** | отказ + текст следующего шага | **Ф2**, Ф3 | **Ф7** |
| Ф7-27 | **Ф7** — неразличимость чужого и несуществующего | отказ | Ф2, Ф3 | **Ф7** |
| Ф7-36 | **Ф7** — окно свежести на **снятии**; **якорь** момента — Ф3 (Ф11 Р6) | отказ + текст следующего шага | Ф2, Ф3 | **Ф7** |
| Ф7-28 | **Ф7** — флаги доезжают; **значение уровня — Ф11** | значение (флаги) | Ф2, Ф3 | **Ф7** |
| Ф7-29 | **Ф7** — то же, вторая сторона оси | значение (флаги) | Ф2, Ф3 | **Ф7** |
| Ф7-30 | **Ф7** — отсутствие отдельно от значения | значение + отказ | Ф2, Ф3 | **Ф7** |
| Ф7-31 | **Ф7** — невыразимость аттестации и живой близнец | представление | — | **Ф7** |
| Ф7-32 | **Ф7** — снятие публичного обещания проверки аттестации ключа; страница правится **Ф7** тем же изменением | снятие + величина | — | **Ф7** |
| Ф7-39 | **Ф7** — снятие деривации «attested» из наличия области токена; страница и четыре пробы правятся тем же изменением | снятие + величина | Ф2, Ф3 | **Ф7** |
| Ф7-33 | **Ф7** — регистрация НЕ пополняет множество предъявленного; **поле множества — Ф11 Р1**, кладёт его **Ф2 либо Ф3** («какая бы из двух фаз её ни завела»); правило над множеством — **Ф11** | неизменность + запись | Ф2, Ф3, **Ф11** | **Ф7** |

**Сценариев 39, производителей названо у 39.** Шесть заведены редакцией 3 по блокирующим круга 1:
Ф7-34 и Ф7-35 (Б4 и Б5 — ось, объявленная объёмом, и третья величина контракта), Ф7-36 (Б6 —
вторая полоса окна свежести), Ф7-37 и Ф7-38 (Б3 — потолок), Ф7-39 (Б1 — второй носитель
обещания). Предикат (единица — **различная метка** в сценарной части файла):

```sh
# ДОМ: PRO-Robotech/kaname
awk '/^## §3 /,/^## §4 /' docs/engineering/acceptance/access-keys-are-ours.md \
  | grep -coE '\*\*ID: Ф7-[0-9]+\*\*'
#   → 39 — и это число равно числу различных меток в столбце «сценарий» таблицы выше
# КОНТРОЛЬ на вторую форму записи — перепись на одной недобирала бы молча:
awk '/^## §3 /,/^## §4 /' docs/engineering/acceptance/access-keys-are-ours.md \
  | grep -coE '\*\*ID: F7-[0-9]+\*\*'
#   → 0
```

**Ф11 стоит в «исполнимо с» РОВНО У ОДНОЙ строки, и это названо, чтобы не читалось шире.** Только
Ф7-33 наблюдает поле, объявленное Ф11 (множество предъявленного, её Р1). Все остальные 38
сценариев от Ф11 не зависят ни «Дано», ни «Тогда»: §3.8 утверждает **доезд флагов**, а не значение
уровня, и остаётся верным при любом исходе спора о лестнице (§4.4 расх. 1). Предикат ревьюеру —
единица счёта **строка ведомости**, а не вхождение слова:

```sh
awk -F'|' '/^\| Ф7-[0-9]+ \|/{ if ($5 ~ /Ф11/) print $2, "→", $5; n++ } END{print "строк ведомости", n}' \
  docs/engineering/acceptance/access-keys-are-ours.md
#   → ` Ф7-33  →  Ф2, Ф3, **Ф11** ` · строк ведомости 39
```

**Ни один сценарий не остался вне таблицы, и это проверяется в ОБЕ стороны:** метка без строки —
блокирующая находка ревью, строка без метки — та же находка с другой стороны. Столбец «сценарий»
несёт **только** метки: раздел, поставленный туда вместо метки, не считался бы переписью и не
попал бы в §8 — класс, названный Ф1 §5 после круга 3.

**§4.1 и §5 сведены В ОБЕ СТОРОНЫ, и это перепись, а не заявление.** Круг 1 назвал здесь
двустороннюю дыру (Б4): ось объёма без сценария и сценарий-производимое без пункта объёма. После
редакции 3: пунктов §4.1 — **12**, и у каждого назван хотя бы один сценарий этой ведомости;
строк ведомости — **39**, и каждая накрыта пунктом §4.1. Соответствие проверяется чтением двух
списков рядом, и обе величины печатаются, потому что одна скрывает ровно ту сторону, которой не
хватает: «12 пунктов покрыты» молчит о строке ведомости, которой в объёме нет.

| пункт §4.1 | его сценарии |
|---|---|
| 1 церемония, срок, однократность, алгоритм | Ф7-01…05, **Ф7-34**, **Ф7-35** |
| 2 проверка утверждения | Ф7-06…09 |
| 3 привязка и три величины контракта | Ф7-10…13 |
| 4 хранилище, перечень ключей, потолок | Ф7-14…16, Ф7-01/25/33 (перечень), **Ф7-37**, **Ф7-38** |
| 5 счётчик | Ф7-17…20 |
| 6 перенос | Ф7-21…24 |
| 7 снятие и окно свежести на нём | Ф7-25…27, **Ф7-36** |
| 8 флаги | Ф7-28…30 |
| 9 политика аттестации, оба носителя | Ф7-31, Ф7-32, **Ф7-39** |
| 10 невмешательство в соседа | Ф7-33 |
| 11 журнал аудита | Ф7-01, Ф7-25 |
| 12 свой набор проб | §8 целиком |

**Род требуемого сверен со столбцом «что производит».** Каждое «Тогда» §3 требует одного из пяти
родов — отказ · проход · значение · запись · неизменность, — и у каждой строки род назван. Три
сценария требуют **двух** родов сразу (Ф7-01: запись и значение; Ф7-18: отказ и величина; Ф7-30:
значение и отказ), и оба рода названы у каждого — сведи их в один, и второй окажется ничьим.

**Сверка с эпиком исполнена, её вывод стоит здесь.** Предикат:
`gh issue view 1266 -R PRO-Robotech/kacho --json body -q .body`, раздел «ДЕКОМПОЗИЦИЯ». Фазы,
названные ведомостью и §4.2, сверены поимённо: `#1268` Ф2 · `#1269` Ф3 · `#1273` Ф7 · `#1274` Ф8 ·
`#1276` Ф10 · `#1280` Ф11 · `#1281` Ф12 · `#1282` Ф13 — совпадают номером **и** предметом.
Обратная сторона: фазы эпика, которых здесь нет, — Ф1 (`#1267`, закрыта, её приёмка — основание),
Ф4 (`#1270`), Ф5 (`#1271`), Ф6 (`#1272`), Ф9 (`#1275`) — сценариев этого документа не производят и
в «исполнимо с» не входят.

**Ни одна фаза не разрешена вперёд.** Все **39** сценариев лежат в DoD **Ф7**: ни один не отложен в
чужую фазу. Это следствие §4.5 — всё, что потребовало бы Ф11, Ф12 или Ф13, вынесено §4.2, а не
оставлено строкой ведомости с чужим DoD. Различие с Ф11, у которой строки ведомости уезжали в DoD
соседей, названо намеренно: там предметом был **уровень**, наблюдаемый только вместе со способом;
здесь предмет — сам ключ, и он наблюдаем своей полосой.

**«Исполнимо с Ф11» и «в DoD Ф11» — РАЗНЫЕ столбцы, и Ф7-33 путать их не даёт.** Его «Тогда»
производит Ф7 (она **не пополняет** множество), поэтому строка лежит в DoD Ф7; Ф11 стоит в
«исполнимо с», потому что без её поля не построить «Дано». Порядок работ это уже требовал и до
редакции 2: §9 п. 1 ставит Ф11 не позже Ф7.

---

## §6 Out-of-scope (с числом и условием возврата)

| вынесено | число здесь | почему вне объёма | условие возврата |
|---|---:|---|---|
| ключ как ВТОРОЙ фактор поверх пароля | 0 сценариев | проверка ключа — та же (Ф7-06…09), иное **правило заведения** и иное место в полосе входа; правило заведения второго фактора Ф11 §6 отдала Ф12 | **Ф12 `#1281`** |
| беспарольный вход и обнаружение ключа до названия человека | 0 сценариев | §4.5 | **Ф13 `#1282`** |
| путь при утрате ключа у человека без пароля | 0 решений | Ф7-24 называет число, решение о пути требует полосы входа | **Ф13 `#1282`** — её тело это требование несёт |
| значение уровня, правило вывода, пересчёт внутри сессии | 0 сценариев | Ф7 производит флаги | **Ф11 `#1280`** |
| экран церемонии и перечень ключей на экране | 0 сценариев | предмет — что консоль вызывает, а не как выглядит (Ф1 §6, та же строка) | **Ф8 `#1274`** |
| уведомление о заведении и снятии ключа | 0 сценариев | у почты свой носитель и метка `blocked` | **`#1773`** |
| служба метаданных о моделях аутентификаторов и список одобренных | 0 сценариев | Р4: внешний авторитет на пути запроса; ступень «3» флаги дают дешевле, и пола «3» в каталоге сегодня нет вовсе (§1.4) | требование арендатора о допустимых моделях — **носителя нет**, заводится своим изменением |
| сертификация FIDO | 0 сценариев | внешнее утверждение, а не наше; Р7 | членство либо требование арендатора |
| условие `mfa_fresh` и его исполнимость | 0 сценариев | Ф11 §6 вынесла его с держателем; его первое условие — уровень «3», который производит **не** Ф7 | **`PRO-Robotech/kaname#133`**, открыта |
| поведение ключей на посадке без доменного имени | 0 сценариев | §1.3: ключи там **выключены by construction**; прогон — «условие не создано» | посадка получает доменное имя |
| **объявление условия согласия устройства в каноне** и всё, что его называет, кроме **двух носителей обещания о ключе** (строка страницы — Ф7-32, деривация значения — Ф7-39) | **0 сценариев** (снимается 0 условий из 6) | условие описывает **устройство**, а не ключ; ненавешенных условий в каноне **5 из 6**, и выбрать одно значило бы закрыть экземпляр, оставив класс. Снятие тронуло бы канон, чья перепись есть посылка **одобренного** соседа (§1.8, §4.4 расх. 6) | **носителя нет** — предмет области прав; заводится своим изменением с переписью по всем шести условиям |

Каждая строка — осознанное сужение объёма (ban #14): фаза production-complete в своих границах, а
отложенное названо и посчитано.

---

## §7 Инварианты (MUST hold — для ревьюера)

1. **Каждое «Тогда» описывает наблюдаемое**, а не устройство: ни один сценарий не называет
   таблицу, функцию или библиотеку. Исключение ровно одно и оно объявлено — Ф7-31, чей предмет и
   есть **представление** (та же форма, что у одобренного F4d-13). Проверяется чтением §3 целиком.
2. **Каждое отрицание стоит в паре с положительным близнецом, и близнец назван для КАЖДОЙ полосы
   §3:**

   | полоса | близнец |
   |---|---|
   | §3.1 церемония | **Ф7-01** — ключ принят; он же близнец Ф7-34 и Ф7-35 |
   | §3.2 подпись | **Ф7-06** — годное утверждение проходит |
   | §3.3 привязка | **Ф7-06** (та же) плюс контроль внутри Ф7-11 и Ф7-12 |
   | §3.4 несколько ключей и потолок | **Ф7-14** — сам положительный; у Ф7-37 и Ф7-38 обе стороны названы **внутри** |
   | §3.5 счётчик | **Ф7-17** и **Ф7-19** — обе стороны оси |
   | §3.6 перенос | **Ф7-23** — перенесённый ключ проходит |
   | §3.7 снятие | **Ф7-25** — снятие проходит, соседний ключ жив; он же близнец Ф7-36 |
   | §3.8 флаги | **Ф7-28** — флаги доезжают |
   | §3.9 аттестация | живой близнец назван **внутри** Ф7-31, Ф7-32 и Ф7-39 |
   | §3.10 шов с Ф11 | **Ф7-33** — его близнец **Ф7-01** (то же действие), а положительный контроль назван внутри: перечень ключей тем же действием пополнился |
3. **Каждое отрицание отличается от близнеца РОВНО ОДНИМ фактом**, и этот факт назван в самой
   строке метки словами «отличается **одним** фактом». Проверяется переписью **в обе стороны**,
   и обе величины печатаются рядом — одна скрывала бы ровно тот случай, ради которого перепись
   и делается:

   ```sh
   # ДОМ: PRO-Robotech/kaname · единица — ЗАГОЛОВОК сценария (всё до первого «Дано»)
   # Читается БЛОК, а не окно строк: пометка переносится на следующую строку, и оконный
   # предикат (`grep -A2`) недобирал бы её молча — эта редакция поймала себя на такой форме.
   python3 -c "
   import re,io
   s=io.open('docs/engineering/acceptance/access-keys-are-ours.md',encoding='utf-8').read()
   sec=s.split('## §3 ')[1].split('## §4 ')[0]
   need=has=0
   for b in re.split(r'(?=^\*\*ID: Ф7-)', sec, flags=re.M):
       m=re.match(r'\*\*ID: (Ф7-\d+)\*\*', b)
       if not m: continue
       head=b.split('**Given**')[0]
       if ' · отрицание' in head or 'вторая сторона' in head:
           need+=1
           if 'отличается **одним** фактом' in head: has+=1
           else: print('БЕЗ ПОМЕТКИ:', m.group(1))
   print('полагается', need, '· есть', has)"
   #   → полагается 19 · есть 19, и ни одной строки «БЕЗ ПОМЕТКИ».
   #      Редакция 2 давала 16 · 16; три новых отрицания редакции 3 — Ф7-34, Ф7-35, Ф7-36.
   #      Расхождение двух чисел — находка; печатаются ОБА, потому что одно скрывает
   #      ровно тот случай, ради которого перепись и делается
   ```

   Метки, которым пометка **не** полагается, названы поимённо, чтобы их не приняли за недостачу:
   Ф7-13, Ф7-15, Ф7-21, Ф7-24, Ф7-30, **Ф7-37**, **Ф7-38** — обе стороны оси названы **внутри**
   сценария, близнеца вне его нет; Ф7-16, Ф7-20, Ф7-31, Ф7-32, **Ф7-33**, **Ф7-39** —
   положительные либо парные внутри себя; Ф7-01, Ф7-06, Ф7-14, Ф7-17, Ф7-23, Ф7-25, Ф7-28 —
   сами близнецы. **Ф7-39 несёт пометку и в счёт `полагается` не входит**, потому что заголовком
   себя отрицанием не объявляет: предикат считает **объявленное**, и это сказано здесь, чтобы
   расхождение «пометок больше, чем требуется» не читалось как ошибка переписи.
4. **Ни один отказ не является оракулом существования** — и это ДВА утверждения. **По телу:**
   Ф7-05, Ф7-09, Ф7-27 требуют **побайтового** равенства. **По времени:** Ф7-09 требует критерия
   Ф1-48…50; тело, равное побайтово, но расходящееся на десятки миллисекунд, — тот же оракул,
   считываемый секундомером.
5. **Ни одна величина не подставляется молча, и величин ЧЕТЫРЕ.** Три величины контракта — имя
   доверяющей стороны, перечень происхождений, **перечень алгоритмов открытого ключа** (Р2) —
   при незаданном значении дают **отказ старта** (Ф7-13). Четвёртая — **потолок числа ключей**
   (Р8) — тоже: незаданная величина роняет старт, ноль означает «ключей не заводить»
   (Ф7-38). Редакция 2 называла две, строя при этом два сценария на третьей; круг 1 назвал это
   блокирующим Б5.

   **Пятой величиной срок испытания НЕ является, и это сказано отдельно.** Он объявлен числом в
   контракте, а не ручкой посадки, и его верхняя граница **выведена** из окна свежести Р5
   (врезка у Ф7-34): величина, у которой есть вывод, отличается от величины, которую выбирает
   оператор, — и подставленной молча она поэтому не бывает.
6. **Пустое отличимо от незаданного** (Ф7-13): пустой перечень означает «никого», незаданный —
   «величину забыли». У имени доверяющей стороны пустого не бывает, и это сказано отдельно.
7. **Отсутствие представимо отдельно от значения**: Ф7-30 требует, чтобы «флаги сообщены нулями»
   отличалось от «флаги не прочитаны», Ф7-21 — чтобы «ключей ноль» отличалось от «источник не
   прочитан».
8. **Ни одна фаза не разрешена вперёд** (§0.2): сценариев Ф11, Ф12 и Ф13 здесь нет, и это
   проверяется §6 и §4.5, а не выводится из их отсутствия.
9. **Необратимое названо поимённо, и оно ОДНО; частично необратимое — второе, и оно названо
   отдельно.** Необратимо: смена имени доверяющей стороны обесценивает весь существующий
   материал (Р2) — форма доказательства Ф7-12 с положительным контролем возврата и Ф7-23.
   Частично необратимо: **сужение перечня алгоритмов** (Р2, третья величина) — уже принятые
   ключи вне нового перечня не ломаются, но перестают приниматься, и вернуть их можно
   расширением перечня обратно. Различие между «обесценен материал» и «перестал приниматься»
   несущее: первое не чинится ничем, второе чинится величиной. Инвариант, у которого не названо,
   чем он краснеет, есть объявление, а не инвариант.
10. **Р1 не ослабляет F4d-14**, и это проверяется предикатом, а не доводом: после Ф7 «две строки
    одного вида у одного человека» по-прежнему даёт ноль (Ф7-16).
11. **Перепись предшествует необратимому**: перенос не начинается, пока перепись не прогнана
    (Ф7-21), и роняется ею (Ф7-22, Ф7-24). Обратный порядок делает «пересоздать» единственным
    исходом после снятия источника (Р3 п. 4).
12. **Ни одно «Тогда» не называет ЗНАЧЕНИЯ уровня и ни одно не утверждает ВЫДАЧИ сессии** —
    предикат §4.5 в обратную сторону, три величины рядом.

    **Редакция 1 писала это одной фразой — «ни одно „Тогда" не утверждает о сессии и об
    уровне», — и она противоречила собственному Ф7-01**, чьё третье «И» говорило об уровне
    текущей сессии. Два места об одном предмете разошлись внутри одного документа, и верным
    оказалось **ни одно**: инвариант был шире того, что охраняет шов, а сценарий утверждал о
    поле, которого без Ф11 не бывает. Обе половины разведены: **о сессии говорить можно** (Ф7-33
    говорит о её множестве предъявленного — это предмет Ф7), **о её выдаче — нет** (Ф13); **об
    уровне говорить можно** («не изменился», «здесь не утверждается»), **называть его значение —
    нет** (Ф11).

---

## §8 Чем свойство держится впредь

Правило без механизма — пожелание; ниже названо, что краснеет, а где механизма нет — так и
написано. **Состав своего набора проб назван по свойству, а не числом:** числу проб не с чем
сходиться, свойству есть.

| свойство | чем держится | способность упасть доказывается |
|---|---|---|
| подпись сверяется, а не принимается | проба Ф7-07 на подделанной подписи | подмена сверки на «всегда годна» — красное; годная подпись — молчит |
| привязка к происхождению жива | пара проб Ф7-10 и Ф7-11 | происхождение, взятое из заголовка запроса вместо перечня, — красное на Ф7-11 и **зелёное** на Ф7-10: пара нужна целиком, одна Ф7-10 этого дефекта не различает |
| привязка к имени доверяющей стороны жива | проба Ф7-12 с возвратом | сверка имени, снятая с пути проверки, — красное; возврат к прежнему имени — молчит |
| величины привязки объявлены **контрактом**, а не профилем | страж старта Ф7-13 | незаданный перечень — отказ с именем ручки; пустой — старт; непустой — старт и Ф7-06 проходит |
| **третья величина контракта объявлена**, а не подставлена | страж старта Ф7-13, третья ветвь | незаданный и пустой перечень алгоритмов — отказ с именем **своей** ручки; общий текст на три величины — красное: оператор не узнаёт, которую забыл |
| **срок испытания ограничивает окно повтора церемонии** | парная проба Ф7-34 с **управляемыми часами** | испытание за пределом срока — красное; оно же внутри срока — молчит. Ожидание вместо управляемых часов делает пробу функцией загрузки машины и потому запрещено |
| **срок испытания меньше окна свежести** — Р5 не обходится сбором испытания внутри окна | декларативная проба, сравнивающая **две объявленные величины** контракта | величина, поднятая до окна свежести либо выше, — красное **с обеими величинами в тексте**; величина ниже — молчит. Проба читает объявления, а не поведение: свойство арифметическое, и поднимать стенд ради него нечем |
| **алгоритм ключа сверяется на ЦЕРЕМОНИИ**, а не только на переносе | проба Ф7-35 | сверка, снятая с приёма результата церемонии, — красное на Ф7-35 и **зелёное** на Ф7-22: пара нужна целиком, одна полоса переноса этого дефекта не различает |
| несколько ключей у человека выразимы | пробы Ф7-14 и Ф7-15 | перенос уникальности с идентификатора удостоверения на пару «человек, вид» — красное на Ф7-14; F4d-14 при этом **остаётся зелёным**, и в этом весь довод Р1 |
| уникальность держит база, а не проверка перед вставкой | интеграционная проба гонки Ф7-15 на настоящей базе | разнесение чтения и записи — красное **детерминированно**, а не в одном прогоне из десяти |
| счётчик сдвигается атомарно | интеграционная проба гонки Ф7-20 | разнесённая пара теряет сдвиг — красное; условный оператор — молчит |
| односторонность дисциплины счётчика | парная проба Ф7-18 и Ф7-19 | судить ноль — красное на Ф7-19; не судить положительный — красное на Ф7-18; **одна из двух** оставляет дефект живым в свою сторону |
| отказы неразличимы по телу | проба Ф7-09, сверяющая **байты** ответов трёх полос | внесённое различие в один байт любого из трёх — красное с именем полосы |
| отказы неразличимы по времени | измерительная проба по критерию Ф1-48…50 | внесённое различие порядка стоимости сверки подписи — красное с именем полосы; стенд, где шум больше потолка годности, — **«не выполнилось»**, а не зелёный |
| непокрытое находится ДО переноса | предполётная перепись, роняющая перенос; форма взята у ID-PW-1 PWV-12 | синтетический источник с ключом вне перечня алгоритмов, без открытого ключа, с человеком без второго способа — красное с числом по каждой причине; покрытый источник — молчит, напечатав объём осмотренного |
| «ноль» отличимо от «не прочитано» | перепись Ф7-21 печатает объём осмотренного всегда | пустой источник даёт «не выполнилось», недоступный — «условие не создано», а не успех |
| аттестация невыразима | проба Ф7-31 попыткой записи плюс живой близнец | попытка положить аттестацию — красное; снятие поля открытого ключа — красное (близнец); обычная запись ключа — молчит |
| публичного обещания проверки аттестации ключа больше нет | проба, читающая **таблицу условий** страницы архитектуры и печатающая пару «строк N · обещающих проверку аттестации ключа M» | возврат строки (в любом написании — прямом либо с экранированным подчёркиванием) — красное; соседние пять строк той же таблицы — молчат; **пустая таблица — красное**, иначе «обещаний ноль» было бы истинно оттого, что таблицы нет |
| **аттестованность устройства не выводится из наличия ключа** | проба Ф7-39 на утверждениях выданного токена плюс перепись «полос N · выводящих M» | возврат деривации в любой из **пяти** полос выдачи — красное **с именем полосы**; токен без области ключа — молчит; **ноль полос — красное**, иначе «выводящих ноль» было бы истинно оттого, что значение перестали ставить вовсе. Распознаватель пробы знает **три** формы имени (`device_attestation` · `device_compliant` · `device_compliance`): перепись на одной недобирает молча, и редакция 2 на этом ошиблась |
| регистрация не засчитывается за предъявление | проба Ф7-33 на множестве предъявленного в сессии | внесённое пополнение множества записью о регистрации — красное; предъявление пароля, наполнившее множество до церемонии, — молчит (иначе «не пополнилось» зеленело бы на множестве, в которое не пишет никто) |
| флаги доезжают различимо | парная проба Ф7-28/Ф7-29 плюс Ф7-30 | сведение двух флагов в один признак — красное на паре; ключ без флагов — молчит, а «флаги не прочитаны» — красное |
| окно свежести на действии живо — **обе полосы** | парные пробы Ф7-04 (регистрация) и **Ф7-36** (снятие), обе с **управляемыми часами** | снятие проверки окна на регистрации — красное на Ф7-04 и **зелёное** на Ф7-36; снятие её на снятии ключа — наоборот. Одна из двух оставляет решение Р5 держащимся наполовину, и держится при этом **более безопасная** половина: снять чужие ключи опаснее, чем завести свой |
| **число ключей человека ограничено** | интеграционная проба Ф7-37 на настоящей базе | заведение сверх потолка — красное с действующей величиной; заведение под потолком — молчит; перенос списания из оператора базы в проверку перед вставкой — красное **детерминированно** на конкурентной ветви; снятие ключа, не вернувшее слот, — красное |
| **величина потолка не подставляется молча** | страж старта Ф7-38 | незаданная величина — отказ старта с именем ручки; **ноль** — старт и отказ всякого заведения; положительная — работа. Слияние отказа по исчерпанию с отказом по незаданной величине — красное: это два состояния, а не одно |
| человек не запирает себя сам | проба Ф7-26 | снятие проверки последнего способа — красное; человек с паролем — молчит |
| фикстура не снисходительнее продукта | подставной аутентификатор исполняет контракт настоящего **тем же кодом**, а не копией | вход, на котором настоящая сверка отвечает отказом, обязан давать отказ и на подставном; подставной, не производящий подписи вовсе, негоден by construction |

**Чего в этой таблице НЕТ и почему.** Сертификационного прогона — Р7 и §1.7: он не держатель.
Числа проб — оно не является свойством и растёт от правок, не меняя того, что доказано. **И
держателя у чужих координат-путей** — §4.7: гейт дома их не судит, привязка ревизией делает
утверждение срочным, а не проверяемым, и это сказано там прямо, а не спрятано в слово «гейты».

**Строка держателя, снятая редакцией 2, названа поимённо.** Стояло: «возврат производителя из
поля запроса — красное». Такой инъекции не существует: производителя из поля запроса не было
никогда (§1.8), то есть строка описывала возврат того, чего не возвращают. Инъекция, которую
нельзя внести, доказательством способности упасть не является.

---

## §9 Порядок работ после APPROVED

Порядок задан зависимостями, а не удобством.

1. **Ф11 кодируется не позже Ф7, и лучше раньше.** Её правило читает флаги, которые производит
   Ф7; но её же страж посадки `own` (Ф11 Р9) требует предъявимого «2» — и пока провязан один
   пароль, посадка `own` отказывает в старте на **27 записях каталога** из 338 (замер §1.4). То
   есть Ф7 **и** Ф12 — каждая по отдельности снимает этот отказ, а Ф11 без любой из них его
   заводит. Кодировать Ф7 вместе с Ф11 — законный исход; **после** Ф11 и до Ф12 — тоже, потому
   что Ф7 и есть та вторая полоса.

   **Редакция 2 добавила к этому ЖЁСТКОЕ требование, и оно ровно одно.** Ф7-33 наблюдает
   множество предъявленного, поле которого объявлено Ф11 Р1: пока Ф11 не влита, этот **один**
   сценарий не исполним — его прогон даёт «условие не создано», третью категорию, а не красный и
   не зелёный (`testing.md` §E2E). Остальные 32 от Ф11 не зависят (§5), поэтому требование
   меняет порядок **внутри** фазы, а не саму возможность её кодировать.
2. **Ф2 и Ф3 — предусловие «Дано» почти всех сценариев.** Без строки способа и без сессии со
   свежестью §3 не построить: это видно столбцом «исполнимо с» §5. Ф7 кодируется после них.
3. **Перепись источника пишется ПЕРВОЙ внутри фазы.** Она решает судьбу трёх ключей (Р3) и роняет
   перенос; написанная после переноса, она проверяет то, что уже случилось.
4. **Перенос идёт ДО снятия компонента (Ф10).** Обратный порядок делает пересоздание единственным
   исходом, а у человека без пароля — запертость (Р3 п. 4, Ф7-24).
5. **Страница про согласие устройства правится ТЕМ ЖЕ изменением, что и снятие значения**
   (Ф7-32): иначе обещание переживёт решение.
6. **Сертификационный прогон снимается вместе с компонентом в Ф10**, не раньше и не позже (Р7).
7. **Вид учёта заводится ДО первой строки, которая его считает; величина объявляется ПОСЛЕ
   переписи.** Миграция, расширяющая закрытое множество `kaname.own_ceilings`, и ручка посадки
   идут прежде таблицы ключей: триггер списания ссылается на вид, и вид, которого в множестве
   нет, не принимается схемой даже опечаткой (Р8). Обратный порядок даёт таблицу, растущую без
   механизма ограничения, — ровно то, что запрещает одобренный `retention-sweep-has-a-caller.md`.
   **А вот ВЕЛИЧИНУ посадка объявляет, уже зная перепись Ф7-21**: перенос списывает те же слоты,
   и величина ниже наибольшего числа ключей у одного человека роняет перенос у него. Порядок
   поэтому: вид → перепись → величина → перенос, и нарушение последней пары обнаруживается
   **на переносе**, то есть в самый дорогой момент.

### §9.1 DoD фазы Ф7

Фаза **production-complete в своих границах** (ban #14): отложенное вынесено §6 с носителем.

1. **Приёмка APPROVED** — этот документ, вердикт `acceptance-reviewer`, запись ревью привязана к
   отпечатку содержимого (`change-graph.md` §2).
2. **Разбор классов** — `class-exposure-analyst` на одобренном замысле, до первой строки кода.
3. **Строгий TDD (ban #12):** все **39** сценариев существуют **красными** прежде кода, и красный
   честный — проба исполнилась и не сошлась, потому что предмета нет, а не потому что не
   поднялось окружение. Порядок первых красных: Ф7-13 и **Ф7-38** (стражи старта — исполнимы без
   хранилища), затем Ф7-15, Ф7-20 и **Ф7-37** (гонки и списание на настоящей базе), затем полосы
   §3.2 и §3.3.

   **Проверка на «красный, который уже зелёный», обязательна и названа поимённо.** Прежде чем
   объявить красный честным, спроси не «падает ли проба», а **«падает ли она оттого, что предмета
   нет»**. Редакция 1 несла ровно такой сценарий (Ф7-32: требуемое было истинно на дереве до
   первой строки кода — §1.8), и отличить его от годного по слову «красный» нельзя. Кандидаты
   здесь — все «Тогда», требующие **отсутствия** чего-либо: Ф7-31, Ф7-32, Ф7-33, **Ф7-39**; по каждому
   покажи вход, на котором проба **зелёная** после Ф7 и **красная** при внесённом дефекте.
   У **Ф7-39** эта проверка **уже исполнена текстом**: требуемое сегодня ложно, потому что
   деривация в дереве есть (§1.8, перепись обеими формами), — то есть его красный честен by
   construction, и этим он отличается от Ф7-32 редакции 1.
4. **Миграции — ДВЕ, и обе новые** (ban #5, применённую не правят): таблица ключей, чей откат
   отказывается уничтожать материал при непустой таблице (форма взята у Ф2 П1 и здесь не
   переизобретается), и **расширение закрытого множества видов учёта** `kaname.own_ceilings`
   именем `iam.user.accessKey` вместе с провязкой триггера списания на таблицу ключей (Р8).
   Вторая названа отдельной строкой намеренно: множество видов закрыто **ограничением схемы**,
   и вид, которого в нём нет, не попадает туда даже опечаткой — значит без миграции ручка
   посадки не имеет куда лечь.
5. **Гонки на настоящей базе** под детектором гонок: Ф7-15, Ф7-20 и **Ф7-37** (последний — на
   списании слота), по форме `data-integrity.md` §«Чек-лист нового ссылочного поля».
6. **Перепись источника** прогнана и её вывод приложен: людей, строк способа, ключей, покрытых и
   непокрытых, запертых. **Ноль ключей — «не выполнилось»**, а не успех.
7. **Три живых ключа** проходят проверку на нашем сервере либо пересозданы по Ф7-22/Ф7-24, и исход
   назван числом по каждому человеку. **На посадке, где популяции нет** (стенда с источником
   переноса в досягаемости нет — §1.6), пункт объявляет **«условие не создано»** — третью
   категорию, — и не считается ни зелёным, ни красным. Оговорка дописана редакцией 3 по не
   блокирующему В5 круга 1: у п. 11 она стояла с редакции 2, у п. 7 — нет, и пункт DoD без неё
   читается как красный.
8. **Свой набор проб** по §8, и по каждой строке таблицы прогнана инъекция в обе стороны:
   внесённое различие краснеет **с координатой**, законный близнец молчит.
9. **Оба носителя обещания проверки аттестации ключа сняты** — строка публичной страницы
   (Ф7-32) и деривация значения токена из наличия ключа (**Ф7-39**), вместе с четырьмя пробами,
   которые это значение утверждают (§1.8, таблица радиуса); тем же изменением, что и
   остальное по Р4; перепись печатает обе величины — `6 · 0`. Объявление условия в каноне **не
   трогается**, и в изменении это сказано словами, чтобы следующий читатель не принял отказ за
   недосмотр (§1.8, §10 п. 11).
10. **Гейты дома зелены:** `go test -count=1 -short ./internal/check/`. **Граница этих гейтов
    названа, а не подразумевается:** `TestAcceptancePathCoordinateResolves` судит **свои**
    координаты-пути, стоящие путём-спецификацией после `--`; **чужие он не судит** — считает и
    печатает их дома, — а путь, названный **прозой**, не видит вовсе.
    `TestAcceptanceProbeCoordinateResolves` в обратную сторону **резолвит имена проб в чужом
    доме** по рабочей копии рядом. Все четыре чужие координаты этого документа — прозаические и
    привязаны ревизией; их держатель и замер инъекции — §4.7. Читать зелёный этих гейтов как
    «координаты документа резолвятся» **нельзя**: редакция 1 так и написала, и это было заявление
    шире сделанного.
11. **Сквозная проба через край** — регистрация ключа и вход им, на посадке **с доменным именем**;
    на посадке без него шаг объявляет «условие не создано» и не считается ни зелёным, ни красным
    (§1.3).
12. **Trail в vault** и закрытие `#1273` с артефактами.

---

## §10 Что осталось решением, а не установленным фактом

Раздел существует, чтобы ревьюер не принял выбор за замер.

1. **Что именно несёт источник по каждому ключу — не установлено.** §1.6: перемерить нечем,
   стенда в досягаемости нет. Документ на это не опирается: решает перепись Ф7-21, а не таблица
   §1.6. **Риск, названный прямо:** если источник не несёт открытого ключа в пригодном виде,
   перенос вырождается в пересоздание целиком — и Р3 это допускает, назвав пересоздание исходом
   переписи, а не провалом.
2. **Механизм расхождения двух замеров популяции не установлен** (§1.6) — как и у ID-PW-1 §1.4 по
   паролю. Три объяснения равноправны; различить нечем.
3. **«Три живых ключа» — число ЛИЧНОСТЕЙ.** Число ключей неизвестно и может быть больше. Верхняя
   граница радиуса пересоздания — три человека — от этого не зависит.
4. **Судьба спора об уровне ключа решена НЕ здесь** (§4.4 расхождение 1). Если внешнее событие
   владельца замену Ф11 не утвердит, §3.8 остаётся верным дословно — но **довод** Р4 («флаги
   дешевле аттестации») опирается на то, что различитель вообще нужен. Отмени различитель — и Р4
   станет проще, а не сложнее: аттестация не понадобится тем более.
5. **Порога «3» в каталоге сегодня нет** — записей с полом «3» ноль (§1.4). Значит различие «3» и
   «2» сегодня **не наблюдаемо арендатором**, и Р4 выбирает более дешёвый различитель на
   непроверяемом сегодня предмете. Это осознанно: Ф11 Р3 назвала будущий пол «3» не гипотезой, а
   уже отданным требованием владельца.
6. **Односторонность дисциплины счётчика (Р6) — выбор, а не норма.** Судить ноль строже значило бы
   отвергать годные современные ключи; не судить положительный значило бы потерять единственный
   сигнал клонирования. Оба края названы, середина выбрана.
7. **Окно свежести 15 минут взято у Ф1 §4.1 дословно** и здесь не пересматривается. Сменит
   владелец величины — Ф7-04 переедет за ней тем же изменением.
8. **Остаток Р5 не лечится этим документом** и назван там же: пароль, известный не тому, даёт
   «2» честно. Противопоставлена наблюдаемость, а не запрет.
9. **Ф7-33 зависит от Ф11, и зависимость эта односторонняя.** Множество предъявленного объявлено
   Ф11 Р1 — частью, в споре о лестнице **не участвующей**. Отзови замену F4d/F4e — Ф7-33 останется
   верным; сними Ф11 целиком — сценарий станет неисполнимым, и тогда его предмет («заведение не
   есть предъявление») придётся выразить иначе. Второго носителя у этого предмета нет: Ф11 §6
   отдала его сюда, а сценариев о нём у неё **ноль**.
10. **Что источник несёт по каждому ключу, решает Ф7-21, а не §1.6** — см. п. 1. Повторено здесь
    затем, что редакция 2 сузила и другое место: Ф7-24 называет запертых **числом**, а решение о
    пути для них принимает Ф13.
11. **ПЯТЬ условий канона из шести не навешены ни на одно отношение, и Ф7 снимает НОЛЬ из них.**
    Замер — §1.8: объявлено 6, навешено хотя бы на одно отношение — 1 (`mfa_fresh`, три места).
    Предмет Ф7 — **публичное обещание**, привязанное к ключу, и оно одно. Остаётся несделанным:
    объявленное условие согласия устройства, которое не выполняется ни при каком входе; строка
    комментария контракта и страница API, перечисляющие **пять** ключей, которые сервер удаляет
    все пять; осиротевшая константа, у которой читателей ноль — как и у четырёх её соседей.
    **Носителя у этого остатка нет**, и он принадлежит области прав, а не ключам: выбрать один из
    пяти значило бы закрыть экземпляр и оставить класс (`testing.md` §«Гейт на класс», п. 1).
    Отдельно: комментарий каталога условий в самом каноне говорит «7 frozen built-ins» при
    шести объявленных — расхождение **старше Ф7** и ею не заводится.
12. **Величина потолка числа ключей — решение ПОСАДКИ, а не приёмки** (Р8). Документ требует
    ручку, страж старта и три различимых исхода величины; какое число поставит оператор, решает
    он. Оценка «сколько ключей человеку нужно одновременно» здесь **не называется числом**
    намеренно: приёмка, назвавшая его, объявила бы величину, которую сама не выводит, — а
    величина, подставляемая построением, предметом стража быть не может.
13. **Снятие деривации «attested» — решение, а не устранение дефекта** (Ф7-39). Деривация
    написана осознанно и по-своему защитима: наличие ключа действительно говорит о свойствах
    устройства больше, чем его отсутствие. Отвергнута она потому, что **утверждает проверку,
    которой нет**, — тот же довод, что у строки публичной страницы. Цена измерена и названа
    нулевой для вердикта: атрибут, в который значение переводится краем, служба удаляет при
    сборке контекста (§1.8). Если когда-нибудь появится настоящий источник аттестации,
    возвращается не деривация, а источник.
14. **Держателя у чужих координат-путей нет, и это решение** (§4.7). Гейт дома их не судит;
    привязка ревизией делает утверждение срочным, а не проверяемым. Второй держатель выразим —
    перепись в доме платформы, — и он дороже предмета: четыре координаты, у трёх из которых срок
    смерти уже назван (Ф10 и переезд предмета после Ф7).

---

## §11 Границы текста этого документа

Оба репозитория **публичны** (`security-disclosure.md`). Документ написан так, чтобы выдерживать
публикацию, и три места названы отдельно, потому что соблазн написать больше в них наибольший:

1. **§1.8** называет предмет — «атрибут условия никто не производит, клиентское значение
   удаляется, отношений с условием ноль, а значение-утверждение выводится из наличия ключа» — и
   **не** называет, что из этого следовало бы при ненулевом радиусе. Сборка «координата + условие
   + следствие» не производится; радиус (ноль отношений) назван как раз затем, чтобы читатель
   видел цену предмета, а не путь к ней. **Редакция 3 добавила сюда второй носитель, и граница
   от этого не сдвинулась**: снятие деривации описано как снятие **утверждения**, а не как
   описание того, что сегодня проходит, — и цена названа нулевой для вердикта ровно потому, что
   последний шаг цепи мёртв. Обхода здесь нет: дефект даёт лишнее **утверждение** арендатору, а
   не лишнее разрешение.
2. **§3.2 и §3.3** описывают **наблюдаемое**: что отвергается и что проходит. Ни один сценарий не
   называет порядка проверок внутри сверки — он не является контрактом, и его публикация была бы
   рецептом, а не требованием.
3. **§1.6** называет числа популяции, уже опубликованные телами задач и одобренной ID-PW-1, и
   ничего сверх них. Персональных данных ни одно число не несёт: считаются строки и люди, не
   адреса.

**Три места из-под ограничения выведены намеренно** (`security-disclosure.md` — те же три): текст
отказа, который видит оператор при отказе старта (Ф7-13) — он обязан называть ручку, иначе стенд
не поднять; комментарий у будущего гейта — он обязан называть то, что запрещает, иначе его снимут
как непонятный; имя и текст падения пробы — они и есть описание защищаемого свойства.
