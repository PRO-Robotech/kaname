<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка: второй фактор — одноразовый код по времени и запасные коды (iam Ф12)

- **Статус:** DRAFT — кругов ревью **ноль**; санкции на код нет (ban #1). Токен состояния
  остаётся `DRAFT` до записи ревью по отпечатку: слово в шапке — статическая форма объявления,
  а не действующий вердикт (`change-graph.md` §«Вердикт привязан к ОТПЕЧАТКУ, а не к документу»)
- **Правки ПОСЛЕ объявления состояния — до первого круга, и вот их состав:** редакция 1 —
  черновик целиком; редакция 2 — ствол сдвинулся под ногами: накопительная линия
  `release/iam-lines` влита в `main` (`#178`) и снята, Ф2 и Ф3 в стволе, ревизия измерения
  перенесена на `4f6ab4be`, предикаты §1 прогнаны заново (ни одно число не сдвинулось), слова
  «на линии» сняты; редакция 3 — две находки по дороге (§11) заведены задачами и названы
  номерами; **редакция 4 — доводка по восьми находкам держателя до первого круга, каждая
  перемерена и подтвердилась**: Ф12-05 требовала три «Тогда», не строимых одним механизмом под
  ключом Ф2 П1, и называла SQLSTATE устройством (Д1 — интерпретация, §7 инв. 1); у однократности
  запасного кода не была названа вторая сторона оси — два разных годных кода одновременно
  (Р6, Ф12-24 б); форма материала набора не была названа вовсе (Р6 — медленный хеш под ёмкостью
  пароля, PWV-15 для набора возвращён в объём); расширение закрытого набора ограничений гейта
  сдерживания и перечня разрешённых файлов не было названо решением (§4.1 п.1, Ф12-39); Ф12-21
  считала защиту от повтора свойством сессии (Р5, Ф12-22 в); «фактор не заведён» получал на
  входе другой код, чем в церемонии (Ф12-13 е, §7 инв. 14); Ф12-33 не мерила личность без
  фактора (классы 7…10); §0.4 называла обёртку формой Ф-о, сводка §3 не сходилась, Р5
  приписывала «±1» телу задачи. Вердикта ни об одной редакции нет, переносить нечего; строка
  названа потому, что гейт правки после объявления состояния читает отметки, а не вердикт
- **Редакция 6 — правка ПОСЛЕ вердикта круга 2 (`APPROVED`), и вот её полный состав.** Круг 2
  одобрил редакцию 5 — запись
  `docs/specs/reviews/second-factor-totp-and-recovery-codes/b71c3ff7777987e4bc48f9a06aee86f52155c3f8856e5352cd61eb6f408bc726.yaml`,
  внешнее событие в `kacho#1281` (`issued: true`, 2026-09-16). Тот вердикт — о редакции 5 и на
  эту редакцию **не переносится**: действующее одобрение выводится из записи ревью на **новый**
  отпечаток и внешнего события, которых ещё нет (`change-graph.md` §2, §3); прежние записи не
  переписываются. Предмет — **один**: решение оркестратора `PRO-Robotech/kacho#2697` (комментарий
  от 2026-09-17; владелец делегировал решения 2026-09-16), исход 1 — завершение восстановления
  остаётся **одним** обращением, сессия восстановления требования сменить пароль **не несёт**,
  поле сессии «требование сменить пароль» (контракт: `password_change_required`, номер 8) и
  его читатели снимаются с контракта (реализация —
  `PRO-Robotech/kaname#201`; эта редакция — `PRO-Robotech/kaname#202`). Правятся: §0.1 (строка
  Ф3 Р1 — состав записи без поля, как в Ф3 редакции 8); Р4 (абзац «под требованием сменить
  пароль» — состояния нет, шесть глаголов идут под сессией восстановления как под всякой);
  §3 Д19 (та же строка); **Ф12-38 — носитель `R` снят вместе с предметом**, а не ослаблен:
  «Дано» с сессией, у которой поле стоит в `true`, после `kaname#201` **неконструируемо**
  (состояния, которого схема не допускает, «Дано» быть не может), три носителя `A`/`B`/`C` и
  половины «недоступность» и «чужие заголовки» — как есть. **Не тронуты:** Р1…Р12 сверх абзаца
  Р4, прочие сценарии, §6, §7, §8; §12 (запись круга 1, где Н1 закрыта носителем `R`) — история
  круга и переписыванию не подлежит: она свидетельствует о том, чем находка закрывалась **на
  своей** редакции. Ревизии измерения редакции 6: дом службы — база линии
  `release/iam-lines@0cb0eddd`, где предикат `git grep -c PasswordChangeRequired -- '*.go'
  ':!*_test.go'` даёт **10** файлов (читатели и провод), и голова ветки PR `kaname#228` (эта
  редакция лежит на ней), где тот же предикат даёт **0**
- **Редакция 7 — правка ПОСЛЕ вердикта круга 3 (`APPROVED`), и вот её полный состав.** Круг 3
  одобрил редакцию 6 — запись
  `docs/specs/reviews/second-factor-totp-and-recovery-codes/06984af979a6d7a1ca384e5d356d075627cb9a2c56a42942dff75a17a5fae1ad.yaml`,
  внешнее событие `https://github.com/PRO-Robotech/kacho/issues/1281#issuecomment-5709977842`
  (`issued: true`, 2026-09-17T06:27:34Z). Тот вердикт — о редакции 6 и на эту редакцию **не
  переносится** (`change-graph.md` §2, §3); прежние записи не переписываются. Предмет — **один**:
  решение оркестратора `PRO-Robotech/kaname#257` (владелец делегировал решения 2026-09-16) —
  отказ по **состоянию** второго фактора на ВХОДЕ неразличим от отказа входа: код при
  незаведённом факторе после совпавшего пароля отвечает тем же `401 authentication failed` с тем
  же телом, что неверный пароль (Ф3-02), и считается попыткой; различимые отказы состояния
  (`SECOND_FACTOR_NOT_ENROLLED`) остаются у глаголов, которые зовёт уже аутентифицированный, —
  `step-up`, `remove`, `backup-codes` под живой сессией и `ResetSecondFactor` распорядителем.
  Найдено имитацией клиента (ban #18) над линией `release/iam-lines` (голова на день находки —
  `7c3b493d`, как в задаче): редакции 4…6 доказывали, что (е) сообщает держателю пароля лишь то,
  что он прочтёт статусом, — и молчали о том, что оно сообщает **не** держателю: совпал ли
  пароль, у всякой личности без фактора, а без фактора всякая свежезарегистрированная. Правятся: §0.1 (строка Ф1 Р3, Ф3-02); Р4 (абзац
  «фактор не заведён — не форма, а состояние»: перечень глаголов — четыре, вход выведен; строка
  матрицы «вход с `secondFactor`»; абзац «оракула здесь нет»); Р5 (фраза абзаца о холостой
  сверке); Р7 (отказ по состоянию **на входе** — попытка; «Наблюдаемое»); Р9 (фраза «вход с
  кодом»); Ф12-01 («И» о входе с кодом при `pending`); **Ф12-13** («Тогда» (е) и два «И»); §3 —
  новая строка **Д29** (Ф1 Р3, Ф3-02: один отказ — расширен на состояние фактора), сводка
  28 → 29; §6 (строка Ф12-13); §7 инв. 3 и 14; §8 (строка «вход с кодом»); §10; §11 (состав Р4 и
  Р7). **Не тронуты:** Р1…Р3, Р6, Р8, Р10…Р12, §0.2…§0.4, §1, §4, Ф12-33 (классы (7), (8) —
  неверный пароль — как были), §12 (история кругов — свидетельствует о своих редакциях).
  Чисел эта редакция не заводит: её предмет — исход, а не величина. Где правится код, сказано
  предметом, а не хешем (хеш головы линии в историю ствола не входит — линия вливается
  схлопыванием): на базе ветки реализации исход входа по состоянию рождается общей сверкой
  (`sf_present.go`, `refusalOf`) и выходит наружу в `login.go` через `refuseSecondFactor` —
  правка идёт в точке, где вход решает, что из сверки выходит наружу. Реализация —
  `PRO-Robotech/kaname#257`, ветка `issue-257-second-factor-not-enrolled-oracle` от головы
  линии; PR называет базу и голову
- **Редакция 8 — доводка по кругу 4 (`CHANGES_REQUESTED` на редакцию 7, запись
  `docs/specs/reviews/second-factor-totp-and-recovery-codes/6bc95772bcb5b9f2f2db0789aefca17513a4d21e626b5827011094313f9176ea.yaml`,
  `event: none`), и вот её полный состав.** Оба блокирующих перемерены и подтвердились:
  **Б5** — Ф12-25 (в) живым «Тогда» называла вход среди мест того же токена (одна строка;
  предикат — в записи круга) — перечень приведён к Р4 и §7 инв. 14: церемония, снятие, сброс;
  **Б6** — третье «И» Ф12-13 считало «три обращения (е)» при двух в «Когда»
  (`B` и `C`) — теперь «на каждом обращении (е) — у `B` и у `C`». Важные того же круга закрыты
  тем же заходом: **Н19** — Р11 и Ф12-43 объявляют обе клетки, на которые оперлась редакция 7:
  исход входа `second-factor-not-enrolled` (закрытый перечень Ф3-48 расширяется здесь) и отказы
  второго фактора по виду (пять клеток — как в дереве); якорь третьего «И» Ф12-13 — на Р11;
  **Н20** — довод Р7 о темпе сокращён до предмета; **Н21** — Ф12-33 получила класс (11) `B`,
  верный пароль, `method: "totp"`, и пару (7)/(11): утверждение Р5 о времени после совпавшего
  пароля теперь измеряется, а не выводится. Из перенесённых: **Н11** круга 2 — §7 инв. 2
  «Ф12-13 (а…ж)», инв. 3 «семь отказов входа»; **Н17** круга 3 — якорь скобки Р4 о путях без
  записи каталога переведён на Ф3 §1.8 и Р2 (Ф3 Р8 редакции 8 об этом перечне говорит только как
  о снятом); **Н18** круга 3 — сравнение «Дано» Ф5 в §4.3 переведено с снятого Ф3-23 на Ф5-24.
  **Не тронуты:** Р1…Р3, Р6, Р8…Р10, Р12, §0.2…§0.4, §1, §4.1, §4.2, все сценарии сверх Ф12-13,
  Ф12-25 (в), Ф12-33, Ф12-43, §6, §8, §10, §12. Чисел редакция не заводит
- **Задача:** `PRO-Robotech/kacho#1281` (Ф12), эпик `#1266`. Открыта
  (`gh issue view 1281 -R PRO-Robotech/kacho --json state` → `OPEN`, 2026-09-16); вход фазы —
  Ф3 (`#1269`) и Ф11 (`#1280`) — тело задачи: «Вход: Ф3 (#1269) и Ф11»
- **Над чем стоит — три одобренные приёмки этого дома, и этот документ ДЕЛЬТА к ним:**
  - **Ф11** — `assurance-level-is-declared-by-our-session.md`, APPROVED круга 3: запись
    `docs/specs/reviews/assurance-level-is-declared-by-our-session/68c2cc6cda07e7ecfbd5efe209954748146970cfcbbd9fbc670a737527f3a324.yaml`,
    внешнее событие `https://github.com/PRO-Robotech/kacho/issues/1280#issuecomment-5690585262`
    (`issued: true`); отпечаток файла в стволе равен `subject.sha256` записи
    (`git show origin/main:docs/engineering/acceptance/assurance-level-is-declared-by-our-session.md | sha256sum`
    → `68c2cc6c…`). **Реализована в стволе наполовину** (`#163`, влит): правило и словарь
    (`internal/assurance`), страж посадки, два гейта; 14 её сценариев ждут Ф3, **8 ждут этой
    фазы** — Ф11-02, 08, 25, 26, 28, 30, 32, 33 (Ф11 §5.2) — и ещё три ждут последней из Ф5 и
    Ф12 — Ф11-07, 11, 29. Каждая из одиннадцати строк отвечена здесь поимённо (§6.1);
  - **Ф3** — `login-lane-issues-our-session-and-logout-ends-it-server-side.md`, APPROVED круга 2:
    запись
    `docs/specs/reviews/login-lane-issues-our-session-and-logout-ends-it-server-side/1a7f6846d102068bd7023eab4527010b573a22f43a1c988416a4c5f63e1a1170.yaml`,
    внешнее событие `https://github.com/PRO-Robotech/kacho/issues/1269#issuecomment-5701542562`
    (`issued: true`); в стволе с `#178` (влита накопительная линия `release/iam-lines`, ветка
    снята) — читать с `origin/main`, отпечаток файла там равен `subject.sha256` записи
    (`git show origin/main:docs/engineering/acceptance/login-lane-issues-our-session-and-logout-ends-it-server-side.md | sha256sum`
    → `1a7f6846…`). Её §4.2 отдаёт этой фазе строку «второй фактор, повышение, церемония»; её поверхность (Р2), носитель (Р3), защита
    форм (Р12), частота (Р10), журнал (Р14) здесь **не переизлагаются** — берутся номером;
  - **Ф1** — `login-session-and-credentials-are-our-contract.md`, APPROVED круга 8 (в стволе,
    `#104`); её §6 отдаёт сюда «второй фактор (код по времени, запасные коды)» и её карта §5.1
    называет производителем Ф12 четыре сценария F4d — F4d-14, -17, -18, -51 (§3 здесь)
- **Соседи, чьи строки называют Ф12 поимённо** (обход — §3): ID-PW-1 (`#1281` в §4.2), Ф5
  (`recovery-of-access.md`, Д12 и §4.2 — «требование второго фактора в самом восстановлении»),
  Ф4 (Д11 — тот же вопрос, переданный Ф5), Ф2 П1 (шапка миграции: «состояние —
  PRO-Robotech/kacho#1281»); в воркспейсе — F4d (`sub-phase-F4d-identity-provider-posture-acceptance.md`,
  APPROVED круга 3) и IAM-INT-1 (`sub-phase-IAM-INT-1-interactive-login-capability-acceptance.md`,
  APPROVED круга 2, Р5 — «второй фактор заводится без браузера»)
- **Ревизия измерения — дом службы:** `4f6ab4be` (`origin/main` `PRO-Robotech/kaname` на
  2026-09-16 после 18:04Z — слияние накопительной линии `release/iam-lines`, PR `#178`, двумя
  родителями: `6331c925` — прежний ствол, `b969ced7` — голова линии). Всё, что ниже названо
  «сегодня в дереве службы», снято на ней. Первая редакция мерилась на `6331c925` (тогдашний
  `origin/main`) при линии рядом; дерево слияния **побайтово равно** дереву головы линии
  (`git diff --stat b969ced7 4f6ab4be` → пусто), поэтому ни одно число §1 не сдвинулось, и это
  проверено повторным прогоном предикатов на `4f6ab4be`, а не выведено
- **Ф2 и Ф3 — в стволе с `#178`:** хранилище способов входа (Ф2 П1, `#95`), проверяющий пароля
  (Ф2 П2, `#137`) и приёмка Ф3 (`#173`) приехали в `main` вместе с линией; ветка линии на origin
  снята. Предикаты §1 читают их путём дерева, как всё остальное
- **Ревизия измерения — дерево платформы:** `PRO-Robotech/kacho@898754db1c` (`origin/main`).
  Край, консоль и объявление второго фактора в посадке живут там; предикаты по ним несут
  приставку дома по правилу Ф1 §1
- **Ревизия измерения — воркспейс:** `PRO-Robotech/kacho-workspace@3b43964b1a` (`origin/main`).
  F4d и IAM-INT-1 читались на ней
- **Тип изменения:** ввод отсутствующего производителя. Для арендатора — **расширение, не
  ломающее**: вход одним паролем остаётся как есть; поле второго фактора в форме входа
  необязательно; ни один пол каталога не меняется; форма отказа по полу — Ф11 §4.1 дословно
- **Сервис:** `kaname` — секрет и его хранение, заведение, предъявление на входе и внутри
  сессии, запасные коды, снятие, сброс распорядителем, окно часов, защита от подбора, посадка.
  Край — **потребитель**: ретранслирует шесть новых глаголов формы тем же механизмом, что четыре
  глагола Ф3, и ничего не решает (Ф11 Р7). Консоль — потребитель края; её экраны — Ф8
- **Формат:** Given-When-Then, только markdown — кода здесь нет по построению
- **Публичный репозиторий:** документ написан так, чтобы выдерживать публикацию — §10

---

## §0 Обзор и границы

Сегодня второй фактор **объявлен и не живёт ни у кого**: посадка платформы объявляет
`totp` и `lookup_secret` вторым фактором чужого компонента (четыре файла, §1.1), живая база
2026-08-25 давала `totp = 0`, `lookup_secret = 0` (тело `#1281`; здесь не перемерялось — кластер
чужой), а в нашем дереве нет ни хранения, ни глагола, ни читателя кода (§1.1: два прод-файла
называют имена способов, и оба лишь **объявляют словарь**). Ф11 уже решила, **что даёт**
предъявление второго фактора — строку «пароль и второй фактор» правила (`internal/assurance`,
`#163`) — и назвала восемь сценариев, исполнимых только вместе с ним. Ф3 решила, **какой
поверхностью** живёт вход, **чем** представлена сессия и **как** защищена форма, и отдала сюда
«второй фактор, повышение, церемонию».

Этот документ решает то, чего не решает ни одна из двух: **чем** фактор заводится и что при
этом хранится (Р1, Р2, Р8); **какими глаголами** он предъявляется — на входе и внутри живой
сессии — и что отвечает церемония (Р4, Р5); **что такое запасные коды** и чем они отличаются от
кода восстановления (Р6); **чем** ограничен подбор и как считается окно часов (Р7, Р5); **что
происходит при снятии** фактора — самим человеком и распорядителем — с живыми сессиями (Р9,
Р10); **обязателен ли второй фактор и для кого** — решение, которое тело `#1281` требует принять,
а не обойти (Р3); и **чего здесь нет** — условие `mfa_fresh` не предмет этой фазы по решению
одобренной Ф11, и это сказано первым (§0.4, §4.2).

### §0.1 Что уже решили соседи — названо номером, не переизлагается

| где | что решено | что из этого здесь |
|---|---|---|
| **Ф11 Р2, Р8** | «2» = утверждение ключа **либо** пароль **и** второй фактор (код по времени либо запасной код); «1» = пароль либо код восстановления; словарь закрыт: `password` · `totp` · `lookup_secret` · `webauthn` · `recovery_code` | имена способов — дословно; строки правила уже в стволе; этот документ их **не правит** и не дополняет — новых способов не заводит |
| **Ф11 Р1** | уровень — поле сессии, писатель один — правило; в строке способа входа уровня нет; «то, что о состоянии способа решит Ф12» — несёт строка способа | Р1: состояние способа — `pending` · `active`; уровня в строке нет и после Ф12 |
| **Ф11 Р5, Р6** | всякое успешное предъявление перевыпускает носитель и двигает момент последнего предъявления; момент аутентификации неподвижен | каждое предъявление кода здесь — предъявление в смысле Ф11: носитель перевыпускается, окно свежести освежается (Ф12-02, -15, -18, -28); на входе с кодом носитель выдаётся, а не перевыпускается (Ф12-11) |
| **Ф11 Р7, §3.0** | край ничего не выводит; уровень наблюдается по исходу — пол пройден либо отказ 401 · `code` 16 · `WWW-Authenticate` с `acr_values` | «уровень L» в сценариях ниже — определение Ф11 §3.0 дословно |
| **Ф11 Р9, Ф11-27/28** | страж посадки `own` считает предъявимые уровни правилом из провязанных способов | Ф12-34: провязка `totp` и `lookup_secret` даёт перечень «1», «2» |
| **Ф11 §1.7, §6 (первая строка)** | условие `mfa_fresh` не исполняется ни на одной полосе; **носителем не может быть Ф12** — её фактор даёт не выше «2» и именем `webauthn` не является; держатель возврата — `PRO-Robotech/kaname#133` | §0.4, §4.2: не предмет этой фазы; ни один сценарий условия не наблюдает |
| **Ф11 §6** | три строки, чьё условие возврата называет Ф12: правило заведения фактора; судьба уровня живых сессий при снятии; восстановление при заведённом факторе | Р8, Р9, Р3 — каждая отвечена решением, а не сценарием «на потом» (§3 Д11…Д13) |
| **Ф11 Р3, Р10 п.1** | «3» — только ключ с проверкой пользователя, не допускающий резервного копирования | не предмет: код по времени и запасной код дают «2» вместе с паролем и не выше |
| **Ф3 Р1** | состав записи сессии закрыт: субъект · момент аутентификации · момент последнего предъявления · срок · уровень · множество предъявленного · подтверждённость адреса; требования сменить пароль в составе **нет** (Ф3 редакция 8, `kacho#2697`) | состав **не расширяется**: всё, что нужно второму фактору, читается из множества и момента последнего предъявления |
| **Ф3 Р2, Р16** | семейство `/iam/v1/auth/*` на origin консоли; край ретранслирует через полосу сессии; слушатель формы допускает ровно край | Р4: шесть новых глаголов — то же семейство, тот же слушатель, та же ретрансляция (Ф12-38) |
| **Ф3 Р7** | «сессии нет» на глаголах формы ретранслируется; недоступность и отсечка на смене пароля — не ретранслируются (F4d-23, F4d-22) | Р4: шесть глаголов меняют состояние под сессией носителя, как смена пароля, — та же ветка (Ф12-38) |
| **Ф3 Р10** | два счёта неверных предъявлений — по адресу и по источнику; величины — профилем; успешный вход обнуляет счёт по адресу | Р7: неверный код идёт в **тот же** счёт; успешное предъявление кода обнуляет его так же (Ф12-31) |
| **Ф3 Р12, Ф3-35** | признак формы привязан к контексту и к виду из закрытого перечня; виды регистрации и восстановления дописывают Ф4 и Ф5 в тот же перечень | Р4: виды `step-up` и `second-factor` — в тот же перечень, одним объявлением (Ф12-41) |
| **Ф3 Р14** | события аудита в существующую очередь одной транзакцией, без адреса и имени; счётчики с нулевыми клетками; уборка существующей петлёй | Р11: четыре новых события, клетки счётчиков, уборка неподтверждённых заведений (Ф12-42…44) |
| **Ф3 Р6, Ф3-19** | подтверждение текущим паролем — предъявление; ответ несёт новый носитель; отказов два: поле не прислано — 400 с именем поля, не подошло — 401 `authentication failed` | Р9: снятие и перечеканка подтверждаются кодом той же парой отказов (Ф12-29) |
| **ID-PW-1 Р1, Р2, PWV-16** | «что писать» — настройка: записываемый формат перечня, параметры между полом и потолком его записи (обе границы включены); «как читать» — признак значения; потолок — запись перечня, не ручка | Р6: набор запасных кодов чеканится тем же хешером по той же настройке и читается по признаку; порог Ф12-39 — **пол** записи объявленного формата (круг 1, Б4) |
| **Ф3 Р3** | носитель `kaname_session` с атрибутами Ф1 §4.1; перевыпуск выдаёт новое значение с прежним сроком | перевыпуск на предъявлении кода — тем же механизмом |
| **Ф1 §4.1** | «правка своих данных — требует свежести сессии 15 мин»; Ф11 Р6 — отсчёт от момента последнего предъявления; Ф11 §5.1 п.6 — окно на действии несёт владелец действия: подключение второго фактора — Ф12 | Р8: заведение — правка своих данных; Ф11-32/33 исполняются здесь (Ф12-09/10) |
| **Ф1 Р3, Ф3-02** | отказ входа один на все причины, побайтово и по времени | Ф12-13: неверный код на входе — тот же `401 authentication failed`, побайтово равный неверному паролю; с редакции 7 — и код при незаведённом факторе после совпавшего пароля (`kaname#257`, Д29): состояние наружу не выходит |
| **F4d Р4, §9.1 п.6** | способы: пароль и код по времени; резервные коды; заведение и снятие второго фактора — отдельный глагол со своим сценарием | Р4, Р9; сценарии F4d — §3 |
| **F4d-13** (в части «состояние»), шапка миграции Ф2 П1 | строка способа несёт состояние; носитель половины «состояние» — Ф12 | Р1 |
| **IAM-INT-1 Р5** | второй фактор заводится **без браузера** — иначе чувствительный набор каталога не проверяется автоматически никогда | Р4: всё семейство — запросы к слушателю формы; браузер нужен только экрану (Ф12-08) |
| **Ф5 Д12, §4.2** | Ф5 второго фактора в самом восстановлении **не требует** (Ф1-25…27 дословно); «вводить ли» — вопрос об обязательности, предмет Ф12 | Р3: не вводится; «Дано» Ф11-29 не меняется; строка Ф5 остаётся с записанным ответом |
| **Ф11 §0.2, Ф3 §0.2, Ф1 §0.2** | одобрение документа не разрешает ничего вне его ведомости | §0.2 здесь |

### §0.2 Что APPROVED этого документа разрешает — и чего не разрешает

**Разрешает:** кодировать объём §4.1 в фазе Ф12 `#1281` — и только то в ней, что ведомость §6
называет поимённо: секрет и его обёртка, заведение с подтверждением первым кодом, предъявление
на входе и внутри сессии, запасные коды, снятие, сброс распорядителем, окно часов, защита от
подбора, свежесть правки своих данных, посадка, шесть глаголов формы, их ретрансляция краем и
две записи в закрытых перечнях Ф3 (виды формы, пути ретрансляции). Вместе с этим — исполнить в
DoD Ф12 восемь сценариев Ф11 под их номерами (§6.1) и три сценария «последней из Ф5 и Ф12»,
если Ф12 окажется последней.

**Не разрешает:** ключи доступа как второй либо первый фактор (Ф7 `#1273`, Ф13 `#1282` —
`webauthn` словаря здесь не производится); экраны консоли — заведение, показ секрета и кодов,
окно повышения, предупреждение об исчерпании (Ф8 `#1274`); политику **обязательности по
личности** и по аккаунту (§4.2 — условие возврата Р3); условие модели `mfa_fresh` и его
доводы (`kaname#133`); сброс **пароля** распорядителем (F4d-20 — сирота, Ф1 §5.1); всё, что
Ф3 оставила себе (сессия, выход, смена пароля, отзыв, частота как механизм). У каждого свой
владелец и своя приёмка; разрешение, выданное вперёд, неотличимо от разрешения, выданного по
невнимательности.

**Ничего в одобренных соседях этот документ не заменяет** — все расхождения §3 разрешены
принятием либо интерпретацией с названным исходом; единственное отступление — от одной строки
F4d-51 («нельзя снять последний фактор»), названо с доводом и ценой (Д5) и видно владельцу в
событии одобрения этого документа.

### §0.3 Почему документ лежит здесь

Секрет, его хранение, глаголы заведения и предъявления, счёт попыток, снятие — предмет
`kaname`, и лежат они рядом с кодом (`polyrepo.md` §«У приёмки домов ДВА»; Ф1 §0.3, Ф3 §0.3).
Край здесь потребитель ровно так же, как у Ф3: ретранслирует и не решает. Консоль — потребитель
края, и её экраны — Ф8.

### §0.4 Перемер посылки задания и тела задачи — в обе стороны

Задание велело перемерить посылку первым делом. Исход по каждому утверждению:

| утверждение | исход | чем |
|---|---|---|
| тело `#1281`: «наш конфиг внешнего провайдера объявляет `totp` и `lookup_secret`, в нашем коде — ноль» | **подтвердилось** — с уточнением единицы | §1.1: в посадке платформы — **4** файла; в прод-Go службы имена стоят в **2** файлах, и оба лишь объявляют и судят словарь Ф11 — проверяющего кода **0**, библиотеки одноразовых кодов в `go.mod` **0** |
| тело `#1281`: «у запасных кодов ноль вызывающих; в консоли — единственное вхождение в комментарии» | **подтвердилось наполовину** | у службы вызывающих 0 (§1.1); в консоли имя стоит в **трёх** прод-файлах — окно повышения, объявление способов церемонии и тип ответа поставщика (§1.1) — не в одном комментарии. На предмет не влияет: все три ведут поток **поставщика**, не наш |
| задание: «пол «2» у 27 записей каталога — перемерь» | **подтвердилось** | §1.1: 338 записей, «2» — **27**, все 27 — глаголы этой службы; после Р10 их станет **28** (сброс распорядителем), и число контрактом не является (Ф11-27) |
| задание: «Ф11 DoD называет 8 сценариев, ждущих Ф12: Ф11-02/08/25/26/28/30/32/33» | **подтвердилось** | Ф11 §5.2 дословно; сверх восьми — три «последняя из Ф5 и Ф12» (Ф11-07, 11, 29), заданием не названные; все одиннадцать в §6.1 |
| задание: Ф11 «реализована в main, #163; правило `internal/assurance`» | **подтвердилось наполовину** (как у Ф3 §0.4) | правило, страж и гейты — в стволе; церемония повышения, перевыпуск носителя, журнал повышения — нет: их «Дано» — сессия, которой нет (Ф3 не влита) |
| задание: Ф3 «APPROVED сегодня, на линии» | **подтвердилось на день задания и уточнено на день записи**: линия влита в `main` (`#178`, 18:04Z) и снята — Ф3 в стволе | запись круга 2, `issued: true`; отпечаток файла на линии равен `subject.sha256` |
| задание: Ф12 добавляет «окно свежести `mfa_fresh`» | **ОПРОВЕРГНУТО** одобренной Ф11 | Ф11 §1.7, §6: условие требует «3» и `webauthn`; Ф12 даёт не выше «2» и именем `webauthn` не является — «носителем не может быть Ф12». Перемерено на линии: `"webauthn" in amr_claims` в модели — строка 1400, условных отношений 2, записей каталога с `ssh`/`console` — 0 (§1.1). Держатель — `kaname#133` (OPEN). Что у фазы **есть** из окон — два других: окно часов кода (Р5) и окно свежести правки своих данных Ф1 §4.1 (Р8, Ф11-32/33); оба здесь |
| тело `#1281`: «хранение секрета (шифрованным, не в открытую)» | **производитель формы в дереве есть** | §1.2 Ф-г: обёртка AES-256-GCM с перечнем ключей (первый оборачивает, все открывают) — `internal/keywrap`; ключ для секретов второго фактора — свой (Р2). Прежняя редакция этой строки называла Ф-о — это потолок темпа, не обёртка; поправлено доводкой |
| тело `#1281`: «обязателен ли второй фактор и для кого — решение, которое задача обязана принять» | **принято** | Р3: обязательность — действием (пол каталога), не личностью; для кого — ни для кого на входе; условие возврата названо |

---

## §1 Перепись производителей — составлена ДО сценариев

Класс «Тогда без производителя» отверг эту линию больше двух раз, поэтому перепись — **шаг
работы, а не её вердикт** (`multi-agent-flow-orchestration.md` §«Класс, отвергший работу
ДВАЖДЫ, переезжает в форму задания»). Единица счёта названа у каждого предиката, команда —
рядом, дом — строкой над командой. Форма `владелец/репозиторий:путь` — координата, а не строка
для оболочки; правило подстановки — Ф1 §1.

### §1.1 Отсутствия — на них держится каждое «сегодня: ∅»

```sh
# ДОМ: PRO-Robotech/kaname · единица — не-тестовый файл Go, называющий имя способа литералом
git grep -lE '"totp"|"lookup_secret"' -- '*.go' ':!*_test.go' | wc -l                       # → 2
#   internal/assurance/level.go (словарь и строки правила) · internal/check/assurance_method_vocabulary.go (гейт словаря)
# единица — строка правила, называющая второй фактор: строки «2» правила УЖЕ ждут его
git grep -c 'MethodTOTP\|MethodLookupSecret' -- internal/assurance/level.go                   # → 8
# единица — зависимость в go.mod, реализующая одноразовые коды
grep -cE 'otp' go.mod                                                                         # → 0
# единица — не-тестовый файл службы, считающий HMAC (stdlib достаточно: crypto/hmac, crypto/sha1)
git grep -n 'hmac' -- '*.go' ':!*_test.go' | wc -l                                             # → 0
# единица — объявление таблицы по ВСЕМУ каталогу миграций (21 файл, 52 объявления `CREATE TABLE`; различных таблиц 51 — `kaname.limits` объявлена дважды, Н7 круга 1)
git ls-files 'internal/migrations/*.sql' | wc -l                                              # → 21
git ls-files -z 'internal/migrations/*.sql' | xargs -0 cat \
  | grep -oE 'CREATE TABLE (IF NOT EXISTS )?kaname\.[a-z_]+' | sed 's/.*kaname\.//' \
  | grep -cE 'session|second|factor|totp|lookup'                                              # → 1 (session_revocations — авторитет отзыва Ф1, не сессия)
# единица — вид способа входа, допущенный ограничением схемы Ф2 П1 (в стволе с #178)
git grep -c "ARRAY\['password'::text\]" -- internal/migrations/20260915111233_login_methods_live_in_their_own_rows.sql   # → 1: только password
git grep -n 'loginMethodKinds = ' -- internal/domain/login_method.go                          # → []LoginMethodKind{LoginMethodPassword}
# единица — способ, чей проверяющий собран корнем композиции (страж посадки, Ф11 Р9)
git grep -n -A1 'func wiredSignInMethods' -- cmd/kaname/laneposture.go | tail -1              # → return nil
# единица — прод-файл, считающий неверные предъявления (два языка — Ф3 §1.7)
git grep -lniE 'failed.?(login|sign.?in|attempt)|attempts?.?(per|window)' -- 'internal/**/*.go' ':!*_test.go' | wc -l   # → 0
# единица — ручка окна свежести правки своих данных в настройке службы
git grep -niE 'freshness|свежест' -- 'internal/apps/kaname/config/*.go' ':!*_test.go' | wc -l   # → 0
# единица — прод-файл службы, работающий с печеньем либо признаком формы (Ф3 не влита)
git grep -lE 'csrf|kaname_form|kaname_session' -- 'internal/**/*.go' ':!*_test.go' | wc -l  # → 0
```

- **∅1 — проверяющего кода нет**: имена способов в дереве только объявляют словарь; сверить код
  с чеканенным нечем и негде (`hmac` в прод-коде — 0).
- **∅2 — хранилища секрета второго фактора нет**: словарь видов строки способа входа —
  `['password']`, и ограничение схемы, и объявление домена (Ф2 П1).
- **∅3 — сессии нет** (Ф3 §1.4): ни записи, ни носителя, ни признака формы. Каждый сценарий
  ниже, чьё «Дано» — сессия, до вливания Ф3 — «не выполнилось» (§4.3).
- **∅4 — церемонии повышения нет**: Ф11 её обещает (§6 там: «Ф11 производит серверную
  операцию и её ответ»), адреса не называет; `#163` её не принёс — её «Дано» сессия.
- **∅5 — счёта неверных предъявлений нет** (Ф3 §1.7, перемерено: 0); Ф3 заводит его для
  пароля, Ф12 кладёт в него код (Р7).
- **∅6 — окна свежести правки своих данных нет** ни ручкой, ни читателем (0): Ф1 §4.1
  переносит величину дословно, Ф11 §5.1 п.6 отдаёт действие владельцу — этой фазе.
- **∅7 — глаголов второго фактора нет** ни в контрактах, ни на HTTP-поверхности; у
  распорядителя нет глагола сброса **ни одного** способа (Ф1 §5.1 «Сироты», F4d-20).

```sh
# ДОМ: PRO-Robotech/kacho @ origin/main · единица — файл посадки, объявляющий второй фактор чужому компоненту
git grep -lE 'totp|lookup_secret' -- 'PRO-Robotech/kacho:deploy/' | wc -l                     # → 4
#   два шаблона настройки поставщика · гейт достижимости пола и его инъекция
# единица — прод-файл Go края либо общих пакетов, называющий имя способа литералом
git grep -lE '"totp"|"lookup_secret"' -- 'PRO-Robotech/kacho:gateway/**/*.go' 'PRO-Robotech/kacho:pkg/**/*.go' ':!*_test.go' | wc -l   # → 0
# единица — прод-файл консоли, называющий имя способа
git grep -lE 'totp|lookup_secret' -- 'PRO-Robotech/kacho:ui-future/**' ':!*.test.ts' ':!*.test.tsx' ':!ui-future/e2e/**' | wc -l   # → 3
#   StepUpModal.tsx (ведёт поток поставщика с aal=aal2) · step-up-methods.ts (объявление способов церемонии) · lib/kratos.ts (тип ответа поставщика)
```

- **∅8 — на крае второго фактора нет ничего**, и это верно: край не выводит и не
  проверяет (Ф11 Р7); его единственная работа здесь — ретрансляция (Р4).
- **∅9 — консоль ведёт церемонию ПОСТАВЩИКА** (`aal=aal2`), не нашу (Ф11 §5.1 п.4, Ф8).

```sh
# ДОМ: PRO-Robotech/kaname · пол каталога — единица — запись; предикат Ф11 §1.4, перемерен
python3 -c 'import json,collections as c; d=json.load(open("internal/apps/kaname/seed/embedded/permission_catalog.json")); print(len(d), c.Counter(e.get("required_acr_min","нет") for e in d))'
#   → 338 Counter({'1': 284, 'нет': 27, '2': 27})
# все 27 с полом «2» — глаголы этой службы (fqn начинается с kaname.cloud.iam.v1)
python3 -c 'import json; d=json.load(open("internal/apps/kaname/seed/embedded/permission_catalog.json")); print(sum(1 for e in d if e.get("required_acr_min")=="2" and e["fqn"].startswith("kaname.cloud.iam.v1")))'   # → 27
# условие mfa_fresh — цепь Ф11 §1.7, перемерено на ревизии измерения
git grep -n '"webauthn" in amr_claims' -- 'internal/authzmodel/**' | wc -l                     # → 1 (строка 1400)
git grep -nE 'define [a-z_]+: .*with mfa_fresh' -- internal/authzmodel/fga_model.fga | wc -l   # → 2
python3 -c 'import json; d=json.load(open("internal/apps/kaname/seed/embedded/permission_catalog.json")); print(sum(1 for e in d if e.get("required_relation") in ("ssh","console")))'   # → 0
```

### §1.2 Формы — механизм уже живёт в дереве, экземпляра для второго фактора нет

| # | форма | где живёт | чем держится | на что опирается |
|---|---|---|---|---|
| **Ф-а** | правило уровня и закрытый словарь способов; строки «пароль и второй фактор», «пароль»; перечень предъявимых уровней из провязанного | `internal/assurance/level.go` (`LevelOf`, `Methods`, `PresentableLevels`, `TOTPPresented`, `LookupSecretPresented`) | `TestLevelOf_WholeTableMatchesRuleR2` · `TestPresentableLevels_FollowsTheRuleOverWiredMethods` · гейты `TestAssuranceLevelHasOneWriterAndOneHome`, `TestAssuranceMethodVocabularyIsDeclaredOnce` | Ф12-11, 12, 15, 18, 34, 40: уровень — правилом, имена — словарём; второго объявления имён Ф12 не заводит |
| **Ф-б** | строка способа входа — пара (человек, вид), материал непуст, вид — закрытым ограничением схемы; материал не копируется, статистика по нему не собирается | миграция Ф2 П1 `internal/migrations/20260915111233_login_methods_live_in_their_own_rows.sql`, `internal/domain/login_method.go`, адаптер `internal/repo/kaname/pg/login_method_repo.go` | `TestLoginVerifierStaysInsideTheSchema` · гейты `internal/check/login_verifier_containment.go`, `login_verifier_flow.go` · `TestIntegration_LoginMethodRollbackDoesNotRaceItsWriter` | Ф12-02, 05, 39, 40: виды `totp` и `lookup_secret` — строки той же таблицы; словарь вида расширяется НОВОЙ миграцией; контейнер материала — тот же тип и тот же гейт, у которого объявленный набор ограничений (`lmDeclaredConstraints`, сегодня 4 имени, набор ЗАКРЫТ) и перечень разрешённых файлов (`AllowedFiles`, сегодня 2) расширяются **решением** этой приёмки (§4.1 п.1), а не проходят под ним |
| **Ф-в** | проверяющий, следующий за хранимым значением; закрытый словарь исходов типом; ёмкость одновременных проверок | `internal/passwordverify/` (7 файлов) | пробы пакета (ID-PW-1 §9) | Ф12-13, 33: код сверяется своим проверяющим той же дисциплины — исход типом, наружу один отказ; код по времени ёмкости не занимает (HMAC дёшев), запасной код занимает **ту же** ёмкость — медленный хеш той же дисциплины (Р6) |
| **Ф-г** | обёртка секрета для хранения в базе: AES-256-GCM, перечень ключей — первый оборачивает, все открывают; отказ не различает причину; ни байта материала в тексте отказа | `internal/keywrap/keywrap.go` (`New`, `Wrap`, `Unwrap`, `KeyCount`); читатели — `cmd/kaname/signing.go`, `internal/apps/kaname/config/authn.go` (ручка `authn.jwks-encryption-key-hex`) | пробы пакета; страж размера ключа | Ф12-35, 39: секрет кода по времени — этой обёрткой, **своим** перечнем ключей (Р2) |
| **Ф-д** | отказ старта на незаданной величине, текст называет ручку; строка требований посадки | `internal/apps/kaname/config/validate.go`, `lane_requirements.go` | `TestUnsetDomainRefusesTheStart` · `TestF4d10_EveryLaneRequirementRefusesTheStartOnItsOwnLane` | Ф12-35, 36: ключ обёртки и окно свежести — ручки без умолчания |
| **Ф-е** | страж «объявленный пол, который полосе нечем предъявить»; перечень предъявимых — правилом из провязанных способов | `internal/apps/kaname/config/lane_requirements.go` (`unreachableFloorsComplaint`), `cmd/kaname/laneposture.go` (`wiredSignInMethods` → `nil` на ревизии измерения; на голове линии после кода Ф3 та же функция — `loginLane.signInMethods()`, `cmd/kaname/loginlane.go`, отдаёт `password` — §12.1) | `TestF11_27_OwnLaneWithOnlyPasswordRefusesToStartNamingLevel1` · `TestF11_28_OwnLaneWithPasswordAndSecondFactorPassesTheFloorRow` (сегодня — на подставленном перечне, корень отдаёт `nil`) | Ф12-34: корень называет `totp` и `lookup_secret` провязанными, когда собран проверяющий |
| **Ф-ж** | управляемые часы на пути решения | `internal/presentedcred/reader.go` (поле `now`) | `TestKAN_VER_02_ExpiredIsRefused` | Ф12-04, 09, 10, 21, 22, 44: шаг кода, окно, срок неподтверждённого заведения, окно свежести — часами пробы |
| **Ф-з** | счётчик с клетками, заведёнными нулём до первого события | `internal/observability/metrics/expired_credential_sweep_recorder.go` | `TestExpiredCredentialSweep_CellsExistBeforeTheFirstPass` | Ф12-43 |
| **Ф-и** | реестр уборщиков одной фоновой петли — одна запись на предмет | `cmd/kaname/retention.go` | приёмка `retention-sweep-has-a-caller.md` | Ф12-44: неподтверждённые заведения после срока |
| **Ф-к** | событие аудита одной транзакцией с изменением; полезная нагрузка без личных данных | `internal/repo/kaname/pg/audit_session_revocation_repos.go` (`InsertTx`); гейт `internal/check/audit_payload_pii.go` | `TestAuditPayloadCarriesNoPersonalData` | Ф12-42 |
| **Ф-л** | отсечка субъекта `now` с причиной и актором из запроса администратора — принудительный выход | существующие писатели Ф3 §1.2 (`ForceLogout`); операция записи отсечки с причиной и актором записи, чей момент стоит, — Ф3 §4.1 п.17 (после Ф3) | пробы Ф3-16, Ф3-25 | Ф12-30: сброс распорядителем гасит все сессии человека тем же писателем и своей причиной |
| **Ф-м** | объектная проверка прав и пол «2» на административном глаголе над человеком (служба); сокрытие существования чужого — **на крае**: `NOT_FOUND` байт-идентичный промаху владельца из таблицы «тип → форма ответа», где `iam_user` → `User %s not found` | `UserService/Block`, `Unblock` — отношение `identity_suspender` на `iam_user`, `required_acr_min: "2"` (каталог); `internal/apps/kaname/api/user/set_blocked.go`; таблица края — `PRO-Robotech/kacho:gateway/internal/middleware/permission_denied_response.go` | пробы глагола; гейт паритета каталога; проба байт-идентичности края (`security-hardening.md` п.6) | Ф12-30: сброс второго фактора — тем же отношением, тем же полом, тем же типом `iam_user` в таблице края |
| **Ф-н** | фиксированный текст отказа на чужой причине — гейт дерева | `internal/check/refusal_text_is_fixed.go` | `TestRefusalTextOnForeignCauseLanesIsFixed` | Ф12-35: материал не открывается → текст без причины |
| **Ф-о** | потолок темпа: окна, объявляемые настройкой; все оси разом либо отказ старта | `internal/apps/kaname/config/config.go` (`RateLimitConfig`) | пробы настройки | Ф12-31: экземпляр по адресу и источнику — Ф3 Р10, сюда кладётся код |
| **Ф-п** | HTTP-слушатель формы в режиме `mutual`, допуск ровно края по имени службы из SAN; четыре глагола, признак формы, ретрансляция краем через полосу сессии | **Ф3** (Р2, Р12, Р16; §4.1 п.9, 18) — одобренная приёмка в стволе, в коде **∅3** | пробы Ф3 §9 | Ф12-38, 41: шесть глаголов — тем же слушателем, тем же перечнем ретрансляции, тем же перечнем видов формы |
| **Ф-р** | строка способа входа вида `password`, подтверждённость адреса, проверяющий пароля — в стволе с `#178` | `git ls-files internal/migrations/20260915111233_login_methods_live_in_their_own_rows.sql \| wc -l` → 1; `git ls-files 'internal/passwordverify/*.go' \| wc -l` → 7 | пробы Ф2 П1/П2 (ID-PW-1 §9) | «Дано» всякого сценария с паролем |
| **Ф-с** | объявление способов церемонии консоли и гейт достижимости пола, читающий три стороны — настройку поставщика, консоль, каталог | `PRO-Robotech/kacho:ui-future/shared/src/lib/step-up-methods.ts`; `PRO-Robotech/kacho:deploy/identity_second_factor_reachable_test.go` | `PRO-Robotech/kacho:StepUpModal.secondfactor.test.tsx` — по имени файла | §4.2: перечень консоли — второе объявление подмножества словаря (Ф11 §10 п.20); после Ф12 на посадке `own` его читатель — Ф8; посылка гейта достижимости («методы объявляет настройка поставщика») истекает с Ф10 — §11 |

```sh
# ДОМ: PRO-Robotech/kaname @ 4f6ab4be · единица — файл формы в стволе (Ф-п — приёмка, Ф-р сочтена своей строкой; здесь не считаются)
git ls-files internal/assurance/level.go internal/domain/login_method.go internal/repo/kaname/pg/login_method_repo.go \
  internal/keywrap/keywrap.go cmd/kaname/signing.go internal/apps/kaname/config/authn.go \
  internal/apps/kaname/config/validate.go internal/apps/kaname/config/lane_requirements.go cmd/kaname/laneposture.go \
  internal/presentedcred/reader.go internal/observability/metrics/expired_credential_sweep_recorder.go cmd/kaname/retention.go \
  internal/repo/kaname/pg/audit_session_revocation_repos.go internal/check/audit_payload_pii.go \
  internal/apps/kaname/api/user/set_blocked.go internal/check/refusal_text_is_fixed.go internal/apps/kaname/config/config.go \
  internal/check/login_verifier_containment.go internal/check/login_verifier_flow.go | wc -l
#   → 19
```

### §1.3 Что перепись изменила в замысле

Четыре «Тогда» первого наброска производителя не имели, и у каждого назван исход:

- «сессия после снятия последнего фактора **не выдаётся** уровнем «2»» (из F4d-51) —
  **переписано под производимое**: уровень — свойство предъявленного (Ф11 Р2), после снятия
  вход паролем даёт «1» **потому что кода не предъявлено**, а живая сессия, повышенная снятым
  фактором, уровня не теряет — понижение в пределах сессии невыразимо (Ф11 §7 инв. 5); что она
  теряет, решает Р9;
- «отказ по свежести — код и текст Ф11» — Ф11-33 отдаёт их приёмке действия; **названы здесь**
  (Р8): `PERMISSION_DENIED` / 403, `reason = SESSION_NOT_FRESH`;
- «ключ обёртки — та же ручка, что у подписного ключа» — **отвергнуто** (Р2): второй предмет
  под ручкой, названной первым, есть ручка, чьё имя уже её содержимого; заведён свой перечень;
- «условие `mfa_fresh` в объёме» (посылка задания) — **вынесено** до первого сценария (§0.4).

Вынесенных за границу «Тогда» — четыре (§4.2); заказанных у чужой задачи — три: экраны (Ф8),
браузерная проба церемонии (Ф8), правка перечня способов церемонии консоли под нашу посадку
(Ф8, §11).

---

## §2 Решения — ратифицировать явно, ревьюер проверяет каждое

Каждое решение названо с обеими сторонами цены и с отвергнутым исходом. Ни одно не
пересказывает решения соседей — на них стоит ссылка номером (§0.1).

### Р1. Второй фактор — строки хранилища способов входа Ф2 двух видов; у строки есть состояние, уровня в ней нет

Второй фактор человека — **одна** строка вида `totp` и **одна** строка вида `lookup_secret` в
хранилище способов входа (Ф-б): та же таблица, тот же ключ «человек, вид», тот же контейнер
материала, тот же гейт сдерживания — **с объявленным набором ограничений, расширенным решением
§4.1 п.1** (набор у гейта закрыт, и шапка миграции Ф2 П1 требует для нового ограничения
решения, а не молчаливого прохода). Словарь вида расширяется с `['password']` до
`['password', 'totp', 'lookup_secret']` **новой миграцией** (ban #5: применённую не правят) —
ограничение схемы и объявление домена вместе, одним изменением; имена — из словаря Ф11 Р8
дословно, и гейт словаря (`TestAssuranceMethodVocabularyIsDeclaredOnce`) судит ограничение
схемы как подмножество словаря.

**Состояние строки — то, что Ф11 Р1 и шапка миграции Ф2 П1 отдали этой фазе.** Значений два и
только два, закреплены ограничением базы: `pending` — секрет чеканен, первый код не предъявлен;
`active` — подтверждён. **Строка `pending` способом входа НЕ является**: её не читает ни правило
уровня, ни полоса входа, ни статус (Ф12-01: «не заведён»); она живёт не дольше окна свежести
(Р8) и снимается уборкой (Ф12-44). Строки `lookup_secret` бывают только `active`: они чеканятся
вместе с подтверждением (Р6). Уровня в строке нет и не будет (Ф11 Р1).

**Материал.** У `totp` — секрет, **обёрнутый** (Р2): его нельзя хранить хешем, HMAC требует
самого секрета. У `lookup_secret` — проверочный материал набора кодов: значение кода из него
не восстановимо (F4d-18: «повторный показ не выразим — у хранилища нет поля»), и **форма
материала названа** — медленный хеш дисциплины Ф-в под той же ёмкостью, что пароль (Р6; первая
редакция форму не называла, и ни одно «Тогда» о невосстановимости без неё не строилось).
Сверх материала строка `totp` несёт **последний принятый шаг** (Р5) — это состояние сверки, не
секрет.

**Отвергнуто:** отдельная таблица второго фактора. Она завела бы второй контейнер секрета
рядом с первым, и гейт сдерживания (Ф-б) судил бы одну таблицу из двух — либо требовал бы
второй копии разбора. Отвергнуто и «одна строка на код»: набор кодов чеканится и снимается как
целое, ключ «человек, вид» остаётся ключом Ф2 П1, а однократность каждого **и годность прочих
под конкуренцией** держатся сериализацией на строке набора (Р6) — то, что покупала бы строка на
код, куплено без второго ключа таблицы и второго предмета у гейта сдерживания.

**Наблюдаемое:** Ф12-01, 02, 05, 39, 40.

### Р2. Секрет кода по времени хранится обёрткой Ф-г под СВОИМ перечнем ключей

Секрет — 20 байт случайности (RFC 4226 §4: не короче 128 бит; 160 — размер выхода SHA-1) —
кладётся в строку **обёрнутым** существующей обёрткой (Ф-г: AES-256-GCM, вектор свой у каждой
обёртки, подлинность вместе с сокрытием) под перечнем ключей, объявляемым **своей** ручкой
`authn.second-factor-encryption-key-hex`: перечень через запятую, первый оборачивает, все
открывают, ключ негодного размера — отказ старта с позицией. Незаданная ручка на посадке `own`
с провязанным вторым фактором — **отказ старта** с именем ручки (Ф-д); самоотчёт старта печатает
число ключей (как у Ф-г: «названо шесть ключей» иначе невидимо). **Ручка повторяет обе
половины существующей** (Н5 круга 1): у Ф-г перечень подаётся значением
(`authn.jwks-encryption-key-hex`) **либо** именем переменной окружения
(`authn.jwks-encryption-key-hex-env`, `internal/apps/kaname/config/authn.go`); здесь —
`authn.second-factor-encryption-key-hex` и `authn.second-factor-encryption-key-hex-env`, иначе
профиль, подающий секрет окружением, задать перечень не смог бы. «Не задана» (Ф12-35 «а») —
**ни одна из двух**.

**Почему свой перечень, а не ручка подписного ключа.** Ручка `authn.jwks-encryption-key-hex`
названа своим предметом — приватной половиной подписного ключа, и шапка пакета обёртки запрещает
**вторую ручку о том же предмете**. Предмет здесь другой: секретов много (по одному на
человека), их срок жизни — срок фактора, их радиус компрометации — вход людей, а не подпись
службы; смена одного ключа не обязана останавливать другой. Ручка, чьё имя уже её содержимого,
есть утверждение о настройке, которое лжёт с первого дня; переименовать существующую — ломающее
изменение для каждого профиля. Цена: второй секрет в профиле и вторая процедура смены —
механика у обеих одна (перечень), и самоотчёт печатает оба числа.

**Материал, который не открывается ни одним ключом перечня** (сменили перечень, не сохранив
прежний ключ; подмена в базе), — **не отказ предъявителя, а недоступность**: `UNAVAILABLE` /
503, фиксированный текст `second factor temporarily unavailable` (Ф-н), запись журнала уровня
ошибки и клетка счётчика; в счёт попыток (Р7) не идёт. `401` здесь лгал бы: человек предъявил
верный код, а служба его не смогла сверить, — и считал бы чужую ошибку попыткой подбора.

**Наблюдаемое:** Ф12-35, 39.

### Р3. Обязательность — свойство ДЕЙСТВИЯ, а не личности: на этой фазе завести фактор не обязан никто

Тело `#1281` требует решить, «обязателен ли второй фактор и для кого». Решение: **обязательность
объявляет пол каталога прав, и это единственный механизм**. Действие с полом «2» (сегодня — 27
записей, после Р10 — 28) отвергается сессии без второго фактора вызовом, называющим требуемый
и предъявленный уровень (Ф11 §3.0); действие с полом «1» и чтения — доступны паролем. Ни одна
личность **не обязана** завести фактор, чтобы войти, и вход паролем при заведённом факторе
даёт «1», а не отказ; восстановление при заведённом факторе кода **не требует** (Ф5 Д12 —
ответ: нет). Отсюда три следствия, каждое названо:

1. **одобренная Ф11 уже построила этот мир**: Ф11-07 (восстановление при заведённом факторе —
   «1»), Ф11-08 (сессия «1» входом паролем при заведённом факторе, код — внутри сессии), Ф11-11
   и Ф11-29 (сессия восстановления при заведённом факторе). Обязательность по личности сделала
   бы их «Дано» непостроимыми — против одобренного;
2. **посадка «своя» становится исполнимой без сброса у 80 перенесённых личностей** (тело
   `#1266`: `totp = 0`), а страж посадки уже требует от полосы **умения** предъявить «2»
   (Ф11 Р9) — не от каждого человека;
3. **цена названа и не спрятана**: человек, заведший фактор, паролем одним по-прежнему входит и
   делает всё с полом «1» — второй фактор защищает **действия**, а не вход; а владение ящиком
   почты и устройством доводит сессию восстановления до «2» через заданный пароль (Ф11 Р2 «Цена
   названа», Ф11-29). Кому нужна защита входа, а не действий, — нужна политика по личности.

**Отвергнуто:** (а) **обязателен для всех на входе** — противоречит трём одобренным «Дано»
выше и запирает на переводе стенда каждого, у кого фактора нет (всех); (б) **обязателен для
привилегированных** (`system_admin` на кластере, распорядители аккаунта) — требует точки
принуждения при выдаче права и при снятии фактора и таблицы «какое отношение → обязателен»,
второго места рядом с полом каталога, который **уже** закрывает шесть глаголов кластера полом «2»
под отношением `system_admin` (§1.1: `GrantAdmin`, `RevokeAdmin`, три глагола интерактивного
клиента, `Apply` модулей); (в) **политика аккаунта** («все члены предъявляют фактор при
каждой выдаче») — ресурс аккаунта со своим контрактом, консолью и владельцем — **отдельная
приёмка**, и это условие возврата (§4.2): её приход меняет «Дано» Ф11-07/08/11/29 добавлением
«политика выключена», и правится это в доме Ф11.

**Наблюдаемое:** Ф12-14 (вход паролем при заведённом факторе — «1»), Ф12-11 (с кодом — «2»),
Ф11-07 (восстановление — «1»).

### Р4. Поверхность — шесть глаголов семейства Ф3 на origin консоли и одно поле в форме входа

| глагол | путь · метод | сессия | вид признака формы | что делает |
|---|---|---|---|---|
| начать заведение | `POST /iam/v1/auth/second-factor/enroll` | живая, **свежая** (Р8) | `second-factor` | чеканит секрет, пишет строку `pending`, **один раз** возвращает секрет и `otpauth`-адрес |
| подтвердить заведение | `POST /iam/v1/auth/second-factor/confirm` | живая, свежая | `second-factor` | сверяет первый код с `pending`; на «совпал» — строка `active`, набор запасных кодов чеканен и **один раз** показан; предъявление (Р5) |
| снять фактор | `POST /iam/v1/auth/second-factor/remove` | живая; код в теле | `second-factor` | сверяет код; снимает `totp` и `lookup_secret`; следствия Р9 |
| перечеканить запасные коды | `POST /iam/v1/auth/second-factor/backup-codes` | живая; код в теле | `second-factor` | сверяет код; новый набор заменяет прежний целиком, показан один раз |
| состояние | `GET /iam/v1/auth/second-factor` | живая | — (чтение) | заведён ли; остаток запасных кодов |
| повышение внутри сессии (церемония) | `POST /iam/v1/auth/step-up` | живая | `step-up` | предъявление одного способа: `password` (ветвь Ф11), `totp`, `lookup_secret`; ответ называет достигнутый уровень и чего не хватает |
| вход (Ф3 Р2) | `POST /iam/v1/auth/login` | — | `login` | **новое необязательное поле** `secondFactor: {"method": "totp" \| "lookup_secret", "code"}` — сессия сразу «2» на исходном носителе (Ф11-02, Ф11 §3.0а) |

**Адрес церемонии повышения называется здесь, потому что не назвал никто.** Ф11 производит
«серверную операцию и её ответ» и обещает, что пробы Ф11-08 и Ф11-26 зовут церемонию **её
адресом**, — адреса не называя; Ф3 закрыла своё семейство четырьмя глаголами. Глагол один на
все способы, ветвь пароля — DoD Ф11 (Ф11-09, 13, 31), ветви кода — DoD Ф12; кто кодирует
церемонию первым, тот заводит глагол этой формой (§9). Второго адреса церемонии — на способ —
не заводится: консоль звала бы два адреса об одном предмете.

**Форма запроса** — JSON, camelCase (`api-conventions.md` §Naming); способ называется
**клиентом** полем `method` из словаря Ф11 Р8, а не выводится службой по длине кода: два
формата кода различимы длиной by construction (шесть цифр против десяти знаков), но вывод по
форме есть неявный второй способ выразить то же. `method` вне словаря либо способ, у которого
нет поля с этим именем (`password` у входа со вторым фактором; `webauthn` до Ф7), —
`INVALID_ARGUMENT` / 400 с именем поля; лишнее поле отвергается, не глотается
(`api-conventions.md` §«Принято-и-проигнорировано»). **«Фактор не заведён» — не форма, а
состояние**, и у него на **всех** глаголах, которые зовёт уже аутентифицированный, **один** код
и **один** токен: `FAILED_PRECONDITION` / 400, `reason = SECOND_FACTOR_NOT_ENROLLED`, текст
`second factor is not enrolled` — клиент ключуется на токен, а не на прозу. Перечень глаголов
**выводится матрицей ниже**, а не выписывается (круг 1, Б1: первая редакция называла четыре и
пропускала перечеканку); сегодня их четыре — `step-up`, `remove`, `backup-codes`,
`ResetSecondFactor` (Ф12-17, 29 «г»/«д», 25 «в», 30). **Вход с полем `secondFactor` —
единственный глагол, где состояние судится ДО аутентификации, и его ячейка — не токен состояния,
а отказ входа** (редакция 7, `kaname#257`): `401 authentication failed`, побайтово равный
неверному паролю (Ф3-02, Ф12-13 «е»), и та же попытка (Р7). Иначе код, приложенный к каждой
попытке, по коду ответа называл бы совпавший пароль у всякой личности без фактора — а без
фактора всякая свежезарегистрированная, — и решение об одном отказе (Ф1 Р3) отменялось бы на
самой массовой популяции. Что теряет держатель пароля, приславший код по ошибке, — один отказ
без следующего шага и одну попытку в окне: следующий шаг ему называет `GET second-factor` после
входа без кода (Ф12-14), а консоль (Ф8) кода у незаведённого не спрашивает.

**Матрица «глагол × состояние строки `totp`» — единое место, откуда читается исход каждой
ячейки** (круг 1, Б1). Строка `totp` у человека **одна** (ключ «человек, вид», Ф2 П1), состояние —
её столбец (Р1), поэтому состояний ровно три: строки **нет** · `pending` · `active`; строка
`lookup_secret` есть ровно при `active` (Р1, Ф12-40). У каждого глагола — **одно** предусловие о
состоянии, и ячейка, где оно не выполнено, отвечает токеном **этого предусловия**; токенов о
состоянии три, каждый — один факт, и ни один не зависит от расписания уборки:

| глагол | предусловие | строки **нет** | `pending` | `active` |
|---|---|---|---|---|
| `enroll` | нет `active` | `200`, новая `pending` (Ф12-05 в) | `200`, `pending` заменена (Ф12-05 б) | `409` `SECOND_FACTOR_ALREADY_ENROLLED` (Ф12-05 а) |
| `confirm` | есть `pending` в сроке | `400` `ENROLLMENT_NOT_PENDING` — заведение не начато (Ф12-04 в) | `200` → `active` (Ф12-02); срок истёк — `400` `ENROLLMENT_NOT_PENDING` (Ф12-04 а, б) | `409` `SECOND_FACTOR_ALREADY_ENROLLED` — подтверждать нечего (Ф12-07 б) |
| вход с `secondFactor` | `active` | `401` `authentication failed`, побайтово Ф3-02, попытка — **не** токен состояния (Ф12-13 е) | то же (Ф12-13 е) | сверка (Ф12-11…13) |
| `step-up` кодом | `active` | `400` `SECOND_FACTOR_NOT_ENROLLED` (Ф12-17) | то же (Ф12-17) | сверка (Ф12-15…18) |
| `remove` | `active` | `400` `SECOND_FACTOR_NOT_ENROLLED` (Ф12-29 г) | то же, `pending` **не тронута** (Ф12-29 д) | сверка, снятие (Ф12-28) |
| `backup-codes` | `active` | `400` `SECOND_FACTOR_NOT_ENROLLED` (Ф12-25 в) | то же (Ф12-25 в) | сверка, перечеканка (Ф12-25) |
| `ResetSecondFactor` | `active` | `400` `SECOND_FACTOR_NOT_ENROLLED` (Ф12-30) | то же, `pending` **не тронута** (Ф12-30, `V`) | снятие (Ф12-30) |
| `GET` состояния | — | `enrolled: false` (Ф12-28) | `enrolled: false`, `pendingUntil` (Ф12-01) | `enrolled: true`, остаток (Ф12-02) |

Строка `pending` под `remove` и под сбросом **не трогается**: оба глагола снимают заведённый
фактор, а незавершённое заведение снимает срок (Ф12-44) либо заменяет новый `enroll`
(Ф12-05 б) — второго снимающего у неё не заводится. Ячейка «`pending` → `SECOND_FACTOR_NOT_ENROLLED`»
— прямое следствие Р1: `pending` способом не является. У `confirm` три состояния без ожидающего
заведения — не начатое, истёкшее, снятое уборкой — дают **один** токен намеренно: различать их
значило бы отвечать по тому, успела ли уборка (Ф12-44), то есть по расписанию, а не по состоянию.

**Порядок проверок у глаголов семейства — один, и он несущий:** после проверок Ф3 (форма
запроса — 400 с именем поля; сессия — 401; признак — 403) судятся **свежесть** сессии, где она
требуется (Р8 — `SESSION_NOT_FRESH`), затем **состояние строки** (матрица), затем **срок**
`pending` (Ф12-04), и только затем **сверяется код**. Отказ по состоянию и по сроку код не
сверяет и попыткой не считается (Р7); при истёкших **обоих** окнах — свежести и заведения —
первым отвечает свежесть (Н6 круга 1): сессия судится раньше всего, что она собирается делать.
Оракула здесь нет: глаголы семейства зовёт **сам человек** под своей сессией, и своё состояние он
читает `GET`-ом. Полоса **входа** — единственная, где состояние судится **после** пароля, и
потому единственная, где исход по состоянию **наружу не выходит**: он остаётся тем же одним
отказом входа и той же попыткой (Ф12-13 «е», Р7), а различимость — внутрь, в клетку
`second-factor-not-enrolled` счётчика исходов входа и в журнал (Р11).

**Под сессией восстановления** все шесть глаголов идут **как под всякой**: требования сменить
пароль у сессии нет (Ф3 Р8 редакции 8, `kacho#2697` — восстановление задаёт пароль тем же
обращением, что предъявляет код, и выдаёт полноправную сессию), состояния «сессия с
требованием» не существует, и различия «что проходит под ним» у шести глаголов нет по
построению. Единственное различие двух сессий — множество предъявленного (`{recovery_code}`
против `{password}`, Ф11 Р8), и оно наблюдаемо уровнем, а не правом. Здесь стояла строка о
проходе шести путей под требованием (Н1 круга 1; носитель `R` Ф12-38) — снята вместе с
предметом редакцией 6.

Подтверждение у снятия и перечеканки —
`{"method", "code"}` в теле, как у смены пароля `currentPassword` (Ф3 Р6, F4d-51): не прислано —
400 с именем поля; не подошло — 401 `authentication failed`.

**Ответ церемонии** (`step-up`) и ответы `confirm`, `remove`, `backup-codes` несут `session` в
составе Ф3-01 (там `assuranceLevel` — уровень **после** предъявления) и объект
`assurance: {"level", "level2Reachable", "missingForLevel2"}`: `level2Reachable` — есть ли
среди **заведённых** у личности способов строка «2» правила; `missingForLevel2` — имена
способов из словаря, чьё предъявление в этой сессии её выполнит (пуст, когда «2» достигнут;
Ф11-11 требует назвать «чего не хватает»). Два поля, а не одно со значением `null`: отсутствие
пути к «2» и достигнутый «2» — разные факты, и каждому своё имя. После предъявления запасного
кода ответ несёт ещё `backupCodesRemaining` — и **только** тогда: поле, стоящее всегда, читалось
бы как факт и там, где кода не предъявляли.

**Ретрансляция и полоса края.** Шесть глаголов входят в **тот же** перечень путей, которым
край регистрирует ретрансляцию четырёх глаголов Ф3 (одно объявление, Ф3 §8 инв. 7), и в
`isPublicHTTPPath` рядом с ними (Ф3 §1.8, Р2: путей без записи каталога станет четырнадцать —
четыре существующих, четыре Ф3, шесть Ф12).
Исход «сессии нет» ретранслируется — судит служба: у пяти глаголов под сессией это
`UNAUTHENTICATED` / 401 `authentication failed` (как у смены пароля, Ф3-20 «в»), у `step-up` —
тот же. **Недоступность службы и отсечка на всех шести — как на смене пароля** (Ф3 Р7:
F4d-23 и F4d-22 на крае, не ретранслируются): каждый из шести меняет состояние **под сессией
носителя** — уровень, носитель, способы, момент последнего предъявления, — и запрос с
носителем, чью отсечку установить не удалось, до них не доходит; чтение состояния сюда же —
оно читает **эту** сессию. Положительный контроль различимости — выход при том же дублёре
ретранслируется (Ф3-17). Ретранслированный запрос несёт печенья и один `X-Forwarded-For` и не
несёт `Authorization` и пространства `x-kacho-` (Ф3-51 дословно; Ф12-38).

**Отвергнуто:** второе семейство путей (`/iam/v1/second-factor/…`) — разошлось бы с перечнем
`isPublicHTTPPath` и с перечнем ретрансляции написанием; глаголы суффиксом `:verb` — Ф3 Р2
выбрала подпуть ради одного написания семейства; статус второго фактора полем ответа `Resolve` —
состав Ф3 Р1 закрыт, а у края читателя этого поля нет.

### Р5. Код по времени: RFC 6238 в параметрах по умолчанию, окно ±1 шаг, повтор невозможен

Параметры — те, которые понимает каждое приложение-аутентификатор без настройки, и потому они
контракт: **SHA-1**, **6 цифр**, **шаг 30 с**, секрет в `otpauth`-адресе — base32 без
дополнения. `otpauth://totp/<домен>:<адрес>?secret=…&issuer=<домен>&algorithm=SHA1&digits=6&period=30`,
где `<домен>` — доменное имя посадки из существующей ручки `authn.domain` (`ResolveDomain`,
умолчания нет), а не вторая ручка «как подписывать»: человек узнаёт запись в приложении по
имени, под которым открывает консоль. Адрес показывается один раз — ответом `enroll` — и после
ни одним глаголом (Ф12-27).

**Окно расхождения часов — ±1 шаг, и величина — решение этой фазы**: тело `#1281` называет
«окно расхождения часов» предметом и **не называет** величины (перемерено:
`gh issue view 1281 -R PRO-Robotech/kacho --json body` — числа при слове «окно» нет), поэтому
±1 ратифицируется владельцем как решение Ф12 (§11), а не читается из задачи. Принимаются коды
шагов `t−1`, `t`, `t+1` по часам службы (Ф-ж); `t−2` и `t+2` — отказ. Шире — дешевле подбор,
у́же — отказ на исправном телефоне. **Повтор невозможен by construction**: строка `totp` помнит **последний принятый
шаг**, и код шага не старше него отвергается тем же отказом, что неверный, — «код уже был
использован» и «код не подошёл» снаружи неразличимы (F4d-17). Запись принятого шага —
**условная** («шаг старше последнего принятого») и она же — **арбитр под конкуренцией**: сверка
кода в памяти читает последний принятый шаг до записи, запись перепроверяет условием (CAS), и два
одновременных предъявления одного кода проходят **ровно одним** (Ф12-22). **Записывает шаг только
полный успех** (круг 1, Б3): на входе — когда сошлись **и** пароль, **и** код; внутри сессии —
когда код сошёлся и транзакция глагола зафиксирована. Сверка на неверном пароле (ниже) идёт **тем
же путём** — тот же HMAC, то же сравнение с последним принятым шагом — и её исход **не
записывается**: строка после неё не отличается от строки до неё, и код той же ступени с верным
паролем затем проходит (Ф12-13 «б» → Ф12-11). Иначе держатель секрета без пароля закрывал бы
владельцу текущую ступень, предъявляя её с любым паролем. Первая редакция связывала сверку и
запись «тем же оператором» и требовала сверки на неверном пароле — сложенные буквально, три
предложения велели записать; здесь «тем же оператором» — про успешный путь. **Последний принятый
шаг — свойство строки, то есть человека, а не сессии**: код, принятый в одной сессии, отвергается
из любой другой сессии и с любого другого устройства (Ф12-22 «в»); смена сессии ничего не
отвязывает — иначе повтор стоил бы ровно второй вход паролем. Отсюда порядок предъявления —
условие всякого сценария с несколькими кодами: принятый шаг закрывает младшие у строки, и
Ф12-21 предъявляет коды **по возрастанию** — а всякий сценарий, стоящий на Ф12-02, предъявляет
ступень **старше** шага подтверждения (§5 преамбула: часы пробы сдвинуты после каждого принятого
кода). Первая редакция считала `t` неподвижным от подтверждения до входа, и Ф12-11/15/25 краснели
бы на верном продукте повтором, а Ф12-21 утверждала «шага у строки ещё нет» после того, как
Ф12-02 его записала (круг 1, Б2).

**Сверка — постоянным временем** (Ф-в), и **выполняется и там, где пароль не сошёлся**
(Ф12-13, 33): иначе по времени ответа на «пароль неверен + код» и «пароль верен + код неверен»
предъявитель узнавал бы, верен ли пароль, будучи отвергнут в обоих. **И там, где секрета нет**
— личность без фактора, личность со строкой `pending`, — выполняется **холостая сверка** над
неизменным холостым секретом той же длины (ID-PW-1 PWV-06, полоса «материала нет»): иначе
время ответа на «неверный пароль + код» отличало бы «фактор заведён» от «не заведён» тому, кто
пароля не знает. Та же неразличимость — и **после** совпавшего пароля, телом и темпом: «не
заведён» на входе отвечает тем же отказом, что неверный код, и той же попыткой (Ф12-13 «е»,
Р7; редакция 7, `kaname#257`), а по времени все исходы после ворот частоты ждут одного потолка
огибающей (Ф3 Р17). Первая редакция называла холостую сверку только для пароля; здесь она
названа для обоих способов (запасной код — Р6).

**Отвергнуто:** SHA-256 и 8 цифр («сильнее») — не поддерживаются частью приложений, и человек
с таким приложением не заведёт фактор никогда; окно ±2 — вдвое дешевле подбор ради часов,
отстающих на минуту.

**Наблюдаемое:** Ф12-01, 21, 22, 33.

### Р6. Запасные коды — набор из десяти, каждый однократен, набор заменяется целиком; это НЕ код восстановления

Набор — **10** кодов по **10** знаков алфавита без визуально смешиваемых знаков (Crockford
base32; ≥ 48 бит случайности у каждого — подбор при потолке частоты Р7 неисполним), чеканится
при подтверждении заведения и при перечеканке, показывается **один раз** ответом и хранится
проверочным материалом (F4d-18) — форма ниже. Десять — контракт, а не ручка: у величины нет
владельца, кроме этой строки, и ручка «сколько кодов» стала бы значением, которое задают и не
читают.

**Материал набора — медленный хеш дисциплины Ф-в (ID-PW-1), и это решение с обеими сторонами
цены.** Строка набора несёт **одну соль на набор**, формат и параметры стоимости — **в самом
значении**, как у пароля, и по одному хешу на каждый ещё не потреблённый код. **Что писать — та
же настройка, что у пароля** (ID-PW-1 PWV-16; круг 1, Б4): формат и параметры чеканки набора
берутся из настройки «что писать», её область — записываемый формат перечня с параметрами
**между полом и потолком** его записи, обе границы включены (страж — `Declared.Validate`,
`internal/passwordverify/declared.go`; на голове линии ручка объявлена кодом Ф3 —
`authn.login.hasher-*`, §12.1), и набор чеканится **тем же хешером**, что пароль. **Как читать** —
по признаку значения (ID-PW-1 Р1, PWV-07); потолок при чтении — **запись перечня** формата
(ID-PW-1 Р2, `record.Ceiling`; это не ручка), выше него — «параметры вне потолка» (PWV-14).
Второй настройки «как дорого хешировать набор» не заводится — она была бы вторым местом об одной
цене. Первая редакция сверяла набор «не ниже ручки потолка»: при запрете «выше потолка» это
означало «ровно на потолке», и законный набор с параметрами на **полу** записи (нынешние §1.2
ID-PW-1) был бы находкой Ф12-39. Сверка предъявленного — **одно** вычисление медленного хеша с солью набора, затем
сравнение постоянным временем с **каждым** элементом, не останавливаясь на первом совпавшем.
Отсюда:

- одно предъявление запасного кода стоит ровно одно вычисление — столько же, сколько сверка
  пароля, — и занимает **ту же объявленную ёмкость** проверяющего (ID-PW-1 PWV-15,
  `password_verifier.capacity`): исчерпание — исход PWV-15, наружу тот же `401` (Ф3-30 форма),
  попыткой не считается (Р7, Ф12-32), клетка счётчика растёт. Код по времени ёмкости **не**
  занимает — HMAC дёшев;
- **полоса «материала нет»** — личность без набора либо со строкой `pending` — проходит то же
  вычисление над **неизменным холостым материалом** и занимает ту же ёмкость (ID-PW-1 PWV-06,
  PWV-15.4 форма): иначе время ответа выдавало бы, заведён ли фактор, тому, кто пароля не
  знает (Ф12-33). То же у кода по времени — холостой HMAC над неизменным холостым секретом (Р5);
- недоступность материала (Ф12-35, 503) набора **не касается**: у набора нет ключа.

**Отвергнуто — с ценой у каждого.** (а) **Быстрый хеш** (SHA-256 и подобные): десять знаков
Crockford — около 50 бит; держатель дампа базы восстанавливает код перебором **офлайн за часы**,
тогда как секрет `totp` в той же таблице обёрнут (Р2) ровно против дампа — набор был бы слабой
половиной. (б) **Обёртка Ф-г для набора** под перечнем ключей Р2: сверка дешева и 503 Ф12-35
распространилось бы на набор, но повторный показ кодов стал бы **выразим** — держался бы
решением, а не отсутствием поля, — а F4d-18 (одобрена, принята Д4) требует именно отсутствия
поля; это было бы второе отступление от F4d ради дешёвой сверки, цена которой уже уплачена
паролем и измерена (PWV-15). (в) **Соль на каждый код**: десять вычислений на одно
предъявление — десятикратная ёмкость; выигрыш — держатель дампа проверял бы кандидата против
одного хеша вместо десяти, то есть множитель 10 на переборе в ≥ 2⁴⁸ при цене медленного хеша —
ничто.

- каждый код **принимается один раз** (F4d-18): второе предъявление того же — отказ, побайтово
  равный неверному коду; прочие остаются годны;
- **однократность одного и годность разных — оба исхода под конкуренцией, один механизм**:
  чтение-изменение-запись набора **сериализуется на его строке** — замок строки берётся до
  сравнения и держится до записи (`data-integrity.md` §«Within-service инварианты», строка
  «сериализовать read-modify-write набора»); хеш предъявленного считается **до** замка, под
  замком — сравнение и **запись только при полном успехе** (круг 1, Б3 — как у Р5): на неверном
  пароле путь тот же — хеш, замок, сравнение, — а записи нет: код **не потреблён**, остаток
  прежний, и тот же код с верным паролем затем проходит (Ф12-13 «ж» → Ф12-12). Иначе держатель
  списка кодов без пароля потреблял бы их все, предъявляя с любым паролем, и оставлял владельца
  без второго фактора при утере устройства (путь Р10 через распорядителя). Второй из двух
  одновременных ждёт первого и читает уже
  переписанный набор: тот же код — его там нет, `401` (Ф12-24 а); другой код — есть, `200`
  (Ф12-24 б). При любом порядке фиксации;
- **остаток** — величина ответа: `GET second-factor` несёт `backupCodes: {"remaining", "total"}`,
  ответ, потребивший код, — `backupCodesRemaining` (Р4). **Предупреждение об исчерпании**
  (тело `#1281`) — работа консоли (Ф8) над этой величиной; служба даёт число, и при `0` —
  число, а не молчание;
- **перечеканка заменяет набор целиком**: прежние коды непригодны все, остаток — 10 (Ф12-25);
- запасной код — **второй фактор** (Ф11 Р2: «пароль и второй фактор (код по времени либо
  запасной код)»), и предъявляется он **вместе с паролем** — на входе либо внутри сессии,
  выданной паролем. **Кодом восстановления он не является**: тот доказывает владение ящиком и
  даёт «1» (`recovery_code`, Ф11 Р8); запасной код доказывает то же, что устройство, и без пароля
  сессии не выдаёт. Два слова — два предмета, и словарь их не смешивает.

**Отвергнуто:** «показать коды повторно тому, кто предъявил пароль» — у хранилища нет поля, из
которого показать (F4d-18); перечеканка без кода в теле — тогда набор перечеканивал бы всякий,
кто держит сессию «1» (например, по украденному носителю), и снимал бы прежние коды у
владельца. **Отвергнуто и о конкуренции — с ценой:** CAS по материалу без повтора (`WHERE
verifier = <прочитанное>`) — первая редакция называла его механизмом однократности, и у него не
была названа вторая сторона оси: два устройства предъявляют **два разных годных** кода
одновременно, второй видит изменённый материал, получает ноль строк — и годный код отвергается
как неверный **и считается попыткой подбора** (Р7); повтор CAS внутри службы — тот же исход
ценой цикла с числом кругов, у которого нет владельца; **строка на код** (`DELETE … RETURNING`
по строке кода) сняла бы конфликт разных кодов, но требует ключа шире «человек, вид» — второго
ключа у таблицы Ф2 П1 и второго предмета у гейта сдерживания, — тогда как сериализация на строке
набора даёт оба исхода без правки ключа (это и есть цена, которой не хватало Р1).

**Наблюдаемое:** Ф12-02, 12, 18, 23…27.

### Р7. Подбор ограничен ТЕМ ЖЕ счётом, что подбор пароля; неверный код — попытка

Неверное предъявление кода — на входе, в церемонии, при подтверждении, снятии и перечеканке —
**увеличивает счёт неверных предъявлений Ф3 Р10** по адресу (нормализованный ключ почты
личности; в церемонии — почта субъекта сессии) и по источнику (адрес из `X-Forwarded-For`
допущенного края). Исчерпание любого — `RESOURCE_EXHAUSTED` / 429, `reason = TOO_MANY_ATTEMPTS`,
`too many attempts; try again later`, `Retry-After` — Ф3-28 дословно. **Успешное предъявление
кода обнуляет счёт по адресу**, как успешный вход (Ф3 Р10). Считаются: код не сошёлся ·
повторён · предъявлен после срока неподтверждённого заведения — **нет**: последнее не
попытка подбора, а истёкшее заведение (Ф12-04); не считаются также отказы формы, отсутствующее
поле, отказ по свежести, всякий отказ по **состоянию строки** под сессией (матрица Р4: «не
заведён», «уже заведён», «ожидающего заведения нет» — код при них не сверяется), недоступность
материала (Р2), исчерпание ёмкости проверяющего на запасном коде (Р6, ID-PW-1 PWV-15.3).
**Исключение одно, и оно на входе** (редакция 7, `kaname#257`): код при незаведённом факторе
**после совпавшего пароля** — попытка, та же, что верный пароль с неверным кодом (Ф12-13 «а»,
«е»). Не считать её значило бы отвечать одним телом, но разным темпом: темп окна частоты
отличал бы совпавший пароль от несовпавшего — второй канал того же оракула. Цена держателю
пароля — одна попытка в окне за код, присланный по ошибке.

**Почему один счёт, а не второй с «N кодов».** Пароль и код — две половины одного входа, и
предъявитель, исчерпавший подбор одной, не получает свежего окна на другой. Две ручки «сколько
неверных кодов» рядом с «сколько неверных паролей» — второе место о темпе одного входа, и
разойдутся они там, где подбор идёт по обеим половинам сразу.

**Наблюдаемое:** Ф12-13 (е), 31, 32.

### Р8. Заведение и подтверждение — правка своих данных: окно свежести 15 мин от последнего предъявления; срок неподтверждённого заведения — то же окно

Одобренная Ф1 §4.1 требует свежести сессии 15 мин для правки своих данных, Ф11 Р6 отсчитывает
её от **момента последнего предъявления**, Ф11 §5.1 п.6 отдаёт окно **на действии** его
владельцу — подключение второго фактора этой фазе. Здесь: `enroll` и `confirm` требуют, чтобы с
момента последнего предъявления прошло не больше окна; иначе — `PERMISSION_DENIED` / 403,
`reason = SESSION_NOT_FRESH`, текст `re-authentication required: present a credential again`
(называет следующий шаг — церемонию `step-up` любым способом, Ф11 Р6: окно освежает и слабый
способ). Величина — **ручка без умолчания** (`authn.self-service-freshness`; дословный перенос
«15 мин» — в профилях, как «24 ч» у Ф3 Р3); незаданная на посадке `own` — отказ старта с именем
ручки. Это ответ на строку Ф11 §6 «правило заведения второго фактора»: фактор, заводимый из
сессии «1» без свежего первого предъявления, делал бы «2» не сильнее «1».

**Снятие и перечеканка окна не проверяют отдельно** — их свежесть есть **сам код в теле**:
предъявление, которое их подтверждает, и есть предъявление в смысле Ф11 Р6, и оно же
освежает окно. **Чтение состояния** свежести не требует.

**Срок неподтверждённого заведения — то же окно от момента `enroll`**: `confirm` после него —
`FAILED_PRECONDITION` / 400, `reason = ENROLLMENT_NOT_PENDING`, текст называет следующий шаг —
начать с `enroll`; токен **один** на все три состояния «ожидающего заведения нет» — истёкшее,
снятое уборкой, не начатое, — потому что наружу они неразличимы (Ф12-04, матрица Р4); при
истёкших обоих окнах первой судится свежесть сессии (Р4, порядок проверок; Н6 круга 1);
повторный `enroll` заменяет `pending` новым секретом в любой момент (человек,
потерявший экран с секретом, не ждёт истечения). Одна величина, два читателя — названы оба;
третьего окна («сколько живёт секрет») не заводится.

**Отвергнуто:** свежесть от момента аутентификации (F4d-36 — заменено Ф11 Р10 п.12); секрет
заведения, переданный клиенту и возвращаемый им в `confirm` (клиент выбирал бы секрет сам).

**Наблюдаемое:** Ф12-04, 09, 10 (= Ф11-33, Ф11-32), 36.

### Р9. Снятие фактора самим человеком — подтверждается кодом, гасит ПРОЧИЕ сессии, текущей уровня не понижает; последний фактор снять МОЖНО

`remove` под живой сессией с верным кодом (по времени либо запасным) снимает строки `totp` и
`lookup_secret` **одним исходом одной транзакции**: (1) обе строки сняты; (2) записи **всех
прочих** сессий человека сняты (форма Ф3 Р6 п.2 — как при смене пароля: устройство, которым
повышались чужие сессии, могло быть тем, ради чего снимают); (3) событие
`iam.user.second_factor_removed` (Р11). Ответ несёт новый носитель **текущей** сессии: сверка
кода — предъявление (Ф11 Р5), и множество предъявленного получает `totp` либо `lookup_secret`.
**Текущая сессия уровня не теряет**: уровень — факт о предъявленном (Ф11 Р2, монотонность),
понижение в пределах сессии невыразимо, и человек, только что доказавший владение устройством,
не выбрасывается из настроек, которые правит. Это ответ на строку Ф11 §6 «судьба уровня живых
сессий при снятии фактора». Вход паролем **после** снятия даёт «1» — кода предъявить нечего;
вход с кодом — тот же `401`, что на неверный код, и попытка (Ф12-13 «е»): состояние наружу не
выходит.

**Последний (и единственный) фактор снять можно — отступление от одной строки F4d-51, и оно
названо (§3 Д5).** F4d-51 отвергала снятие последнего `FAILED_PRECONDITION`, чтобы человек «не
обнаружил потерю 32 записей на первом чувствительном глаголе». Здесь обязательность — свойство
действия (Р3), и запрет делал бы фактор **необратимо обязательным с момента заведения**: смена
телефона при ключе «один `totp` на человека» (F4d-14) без снятия невыразима вовсе, и всякий
сменивший устройство шёл бы к распорядителю (Р10). Что человек теряет, ему говорит **ответ**
(`assurance.level2Reachable: false` в следующем ответе церемонии) и консоль (Ф8) — до глагола,
а не на нём.

**Отвергнуто:** снятие без кода, по одному лишь уровню «2» сессии, — сессия «2» живёт 24 ч, и
держатель её копии снял бы фактор владельцу; снятие с гашением **и текущей** сессии — понижение
уровня чужим путём и выброс из настроек.

**Наблюдаемое:** Ф12-28, 29.

### Р10. Сброс второго фактора распорядителем — административный глагол службы с полом «2»; сирота F4d-20 закрывается в части второго фактора

Человек, утративший устройство **и** запасные коды, входит паролем (Р3) и остаётся на «1» без
пути к «2»: снятие требует кода (Р9). Оставить его без пути — фаза, неполная в своих границах
(ban #14); одобренный сценарий такого сброса — F4d-20 — сирота без производителя (Ф1 §5.1).
Поэтому глагол заводится здесь: **`UserService/ResetSecondFactor`** в контракте
`proto/kaname/cloud/iam/v1/` — отношение `identity_suspender` на `iam_user` и `required_acr_min:
"2"` (те же, что у `Block`/`Unblock`, Ф-м), объектная проверка прав с сокрытием существования:
чужой аккаунт — `NOT_FOUND` байт-идентичный промаху `User <id> not found`. Исход — одна
транзакция: строка `totp` (`active`) и строка `lookup_secret` сняты; **все** сессии человека покрыты
отсечкой `now` существующим писателем принудительного выхода (Ф-л) с причиной
`second-factor-reset` и актором-распорядителем — держатель украденного устройства мог держать
и сессию; событие `iam.user.second_factor_reset` с обоими акторами (Р11). Человек, у которого
фактора нет — строки `totp` нет либо она только `pending`, — `FAILED_PRECONDITION` / 400
`reason = SECOND_FACTOR_NOT_ENROLLED`; строка `pending` при этом **не трогается** и сессии не
гасятся (матрица Р4): сбрасывать нечего, а незавершённое заведение человек заменяет сам новым
`enroll` (Ф12-05 б) либо его снимает срок (Ф12-44).

**Что этот глагол НЕ делает:** не сбрасывает пароль (F4d-20 остаётся сиротой в этой части —
Ф5 по предмету, Ф1 §5.1), не заводит фактора за человека и не снимает блокировку. Через край он
маршрутизируется на внешнем слушателе как всякий глагол `UserService`, и запись каталога у него
своя — число записей с полом «2» становится **28** (Ф11-27: число контрактом не является).

**Отвергнуто:** переиспользовать `ForceLogout` с побочным снятием фактора — глагол одного
предмета получил бы второй; глагол на `InternalUserService` — распорядитель аккаунта зовёт его
через край, а `Internal*` на внешний слушатель не маршрутизируется (ban #6).

**Наблюдаемое:** Ф12-30.

### Р11. Журнал, наблюдаемость, уборка — тем же изменением, что миграция

- **события аудита** (Ф-к, одной транзакцией с изменением): `iam.user.second_factor_enrolled`
  (на `confirm`), `iam.user.second_factor_removed` (на `remove`), `iam.user.backup_codes_regenerated`,
  `iam.user.second_factor_reset` (актор и субъект — разные личности). Полезная нагрузка —
  субъект, идентификатор сессии, вид способа; без адреса, имени, секрета и кодов (гейт Ф-к);
- **журнал повышения** Ф11-14 получает записи со способами `totp` и `lookup_secret` — Ф11
  производит журнал, Ф12 — два имени в нём; **входа с кодом** журнал повышения не касается:
  это выдача, её событие — `iam.session.issued` Ф3 Р14 с множеством способов;
- **счётчики с нулевыми клетками до первого события** (Ф-з): предъявления кода по способу ×
  исходу (сошёлся · не сошёлся · повторён · материал не открывается · ёмкость исчерпана) · запасных кодов
  потреблено · заведений начато · подтверждено · истекло · снятий · сбросов · **отказы второго
  фактора по виду** (не заведён · уже заведён · ожидающего заведения нет · сессия несвежа ·
  материал недоступен — отказы под сессией и у сброса, попыткой не считаются) · **исходы входа**:
  закрытый перечень Ф3-48 расширяется здесь двумя клетками — `second-factor-refused` (пароль
  сошёлся, отказано по полю `secondFactor`: не сошёлся, повторён, потреблён — исход в клетках
  предъявления) и `second-factor-not-enrolled` (пароль сошёлся, фактора нет либо строка
  `pending`; наружу — тот же отказ входа, Ф12-13 «е»; редакция 8, `kaname#257`);
- **уборка** (Ф-и): строки `pending`, чей срок истёк, снимает существующая петля одной записью
  реестра; `active` не тронуты; перепись печатает «осмотрено N · снято M».

### Р12. Включается посадкой; консоль и край — потребители

- **служба:** корень композиции собирает проверяющий кода и объявляет `totp` и `lookup_secret`
  провязанными (`wiredSignInMethods` на ревизии измерения; на голове линии после кода Ф3 —
  `loginLane.signInMethods()`, §12.1), когда собраны обёртка секрета (Р2), хранилище способов
  (Ф2) и сессия (Ф3); страж посадки выводит предъявимые уровни «1», «2» правилом (Ф11 Р9) —
  Ф11-28 исполняется здесь. На посадке `external` семейство Р4 не регистрируется, ресет
  распорядителя не провязан: под ней второй фактор ведёт поставщик (Ф3 Р15);
- **край:** только перечень ретрансляции (Р4); ни одной строки, читающей код или уровень;
- **консоль:** экраны заведения, показа секрета и кодов, окна повышения, предупреждения об
  исчерпании — Ф8; объявление способов церемонии (`step-up-methods.ts`) на нашей посадке —
  `totp` и `lookup_secret` — Ф8 тем же изменением (§4.2, §11).

---

## §3 Дельта к одобренным соседям — обход по свойству, исход у каждого расхождения

Обход — по двум признакам на двух языках (второй нужен потому, что предикат на одном
недобирает молча — `testing.md` §«Предикат по ДВУЯЗЫЧНОМУ корпусу»): документы, называющие
второй фактор либо код по времени либо запасные коды **словом**, и документы, называющие фазу
**номером** (`Ф12`, `#1281`). Ведомостями соседей (второй вход обхода — по ссылке): Ф1 §5.1 →
F4d; Ф11 §5.3 → IAM-INT-1, F4e; Ф5 §3 → F4e.

```sh
# ДОМ: PRO-Robotech/kaname · единица — файл приёмки этого дома, называющий предмет либо фазу (без этого документа)
git grep -lP 'второй фактор|второго фактора|второму фактору|запасн\S+ код|код по времени|totp|lookup_secret' -- 'docs/engineering/acceptance/*.md' | grep -vc second-factor-totp   # → 6
git grep -lE 'Ф12|#1281' -- 'docs/engineering/acceptance/*.md' | grep -vc second-factor-totp                                                                        # → 6
#   оба признака дают ОДНО множество: Ф1 · ID-PW-1 · Ф4 · Ф5 · Ф11 · Ф3
# ДОМ: PRO-Robotech/kacho-workspace @ origin/main · единица — файл приёмки, называющий второй фактор словом (обе формы записи)
git grep -lE 'второй фактор|второго фактора|second factor|totp|lookup_secret' -- 'PRO-Robotech/kacho-workspace:docs/specs/*acceptance*.md' | wc -l   # → 6
#   F4d, F4e, IAM-INT-1 — одобрены и решают предмет (F4e — только уровень, заменённый Ф11 Р10); 3.2 и 3.3 — черновики; ID-MAIL-1 — цитирует F4e («письмо уровня не даёт»)
```

| № | сосед | что говорит | исход здесь | довод |
|---|---|---|---|---|
| **Д1** | F4d-14 | два способа сосуществуют строками; второй того же вида — `ALREADY_EXISTS` / 409 от базы (23505); третий вид не требует правки строк | **принято с интерпретацией**: Ф12-05 | ключ «человек, вид» — миграция Ф2 П1; исход F4d-14 — 409 от базы под конкуренцией, не проверкой перед вставкой — держится дословно; **устройство** («нарушение уникальности», 23505) в «Тогда» не называется: с состоянием строки (Р1) заведение при `pending` **заменяет** строку тем же оператором, который при `active` даёт 409, и SQLSTATE нарушения уникальности этот оператор не производит. Требование к исходу — §7 инв. 1 |
| **Д2** | F4d-15 | гонка двух заведений одного вида — проходит ровно одно | **принято с интерпретацией**: гонка судится на **активации** (два `confirm` одного `pending` — ровно один, Ф12-07); два `enroll` — два перезапуска, побеждает поздний, и это не два способа | строка `pending` способом не является (Р1): «заведён» — только `active`; человек, потерявший экран с секретом, не ждёт истечения (Р8) |
| **Д3** | F4d-17 | пароль и код → «2»; остановившийся после пароля → «1»; повтор кода → 401 тем же текстом | **принято**: Ф12-11, 14, 22 | Ф11-02 и Р5 |
| **Д4** | F4d-18 | запасной код даёт «2» (в прочтении Ф11 Р10 — «пароль, затем запасной код»); однократность; прочие годны; хранится материалом; показ один раз — уровень P | **принято**: Ф12-12, 18, 23, 27; половина P (экран) — Ф8 | Ф11 Р10 п.2, Р6 здесь |
| **Д5** | F4d-51 | снятие требует пола «2» и подтверждения текущим способом, отказов два (400 / 401); **последний фактор снять нельзя** — `FAILED_PRECONDITION`; после снятия «2» не выдаётся | **принято** в части подтверждения кодом и пары отказов (Ф12-28, 29); **отступление** в части «последний нельзя» — снять можно (Р9); «после снятия «2» не выдаётся» — переписано: вход даёт «1» потому, что кода нет, живая сессия уровня не теряет (Ф11 §7 инв. 5) | «Дано» F4d-51 — «два заведённых вторых фактора» — у Ф12 производителя нет: фактор один (`totp`), запасные коды — его спутник, не второй фактор; запрет делал бы фактор необратимо обязательным с заведения и запирал смену устройства (Р9). Пол «2» у снятия выполнен by construction — код в теле есть предъявление |
| **Д6** | F4d-20 | сброс способов входа распорядителем | **закрывается в части второго фактора** (Р10, Ф12-30); в части пароля остаётся сиротой — Ф1 §5.1 | без пути «утратил устройство и коды» фаза неполна в своих границах |
| **Д7** | F4d-32, F4d-20 (форма отказа по полу 403 / `authz.step_up`) | — | не предмет: заменено Ф11 Р10 п.9 (401 · `code` 16); Ф12 переносит форму Ф11 §4.1 дословно | — |
| **Д8** | F4d Р4, Р6; F4e Р1, Р2 (второй фактор → «2» сам по себе; максимум по видам) | — | не предмет: заменено Ф11 Р2, Р10 п.2, п.11; Ф12 следует Ф11 — без пароля код сессии не выдаёт | — |
| **Д9** | IAM-INT-1 Р5 | второй фактор заводится без браузера | **принято**: всё семейство Р4 — запросы к слушателю формы; Ф12-08 | без этого 27 записей каталога не проверяются автоматически никогда |
| **Д10** | IAM-INT-1-21, -22 | посев заводит фактор и предъявляет; `T1` (пароль) — 401 на полу «2», `T2` — 200 | **сходятся** на полосе `own`: Ф11-25/26 в DoD Ф12 (Ф12-20); форма отказа — та же (401, `acr_values`) | сценарии IAM-INT-1 — полоса поставщика; на нашей полосе исход тот же |
| **Д11** | Ф11 §6 «правило заведения второго фактора и ключа» | Ф12 обязана ответить своей приёмкой | **отвечено** Р8: свежесть от последнего предъявления + первый код | — |
| **Д12** | Ф11 §6 «судьба уровня живых сессий при снятии фактора» | Ф12 (`#1281`, «отключение с подтверждением») | **отвечено** Р9: прочие сессии гаснут, текущая уровня не теряет; сброс распорядителем гасит все (Р10) | — |
| **Д13** | Ф11 §6 «восстановление при заведённом втором факторе»; Ф5 Д12, §4.2; Ф4 Д11 | требовать ли фактор в самом восстановлении — Ф12 | **отвечено** Р3: не требуется; «Дано» Ф11-29 не меняется; строка Ф5 §4.2 остаётся с ответом, её предикат возврата («если в объёме Ф12 окажется полоса восстановления») не наступил | обязательность — действием |
| **Д14** | Ф11 §1.7, §6 первая строка; `kaname#133` | `mfa_fresh` — не Ф12 | **не предмет, назван** (§0.4, §4.2) | посылка задания опровергнута одобренным соседом |
| **Д15** | Ф3 §4.2 «второй фактор, повышение, церемония → `#1281`» | — | **производится здесь**: Р4, Р5, Ф12-11…20 | — |
| **Д16** | Ф3 Р10 (счёт неверных предъявлений пароля; успешный вход обнуляет) | — | **принято с расширением** Р7: неверный код — в тот же счёт; успешное предъявление кода обнуляет так же | пароль и код — две половины одного входа |
| **Д17** | Ф3 Р12, Ф3-35 (закрытый перечень видов формы, одно объявление) | — | **принято**: виды `second-factor` и `step-up` — в тот же перечень (Ф12-41) | — |
| **Д18** | Ф3 Р14 (события, счётчики, уборка) | — | **принято**: Р11 — новые события той же очереди, клетки, запись реестра | — |
| **Д19** | Ф3 Р2, Р7, Р16, Ф3-51 (ретрансляция; недоступность и отсечка на смене пароля не ретранслируются); Ф3 Р8 редакции 8 — требования сменить пароль у сессии нет | — | **принято**: шесть глаголов — в тот же перечень; ветка недоступности и отсечки — как у смены пароля; под сессией восстановления — как под всякой (Р4; носитель `R` Ф12-38 снят редакцией 6 вместе с предметом, `kacho#2697`) | все шесть меняют состояние под сессией носителя; различие сессий — множество предъявленного, не право |
| **Д20** | Ф3-09 («момент последнего предъявления читается окном свежести у владельцев действий — Ф12, Ф7») | — | **принято**: Р8, Ф12-09/10 | — |
| **Д21** | Ф3-44, Ф3 Р15 («1» предъявим паролем; «2» — Ф12) | — | **принято**: Ф12-34 | — |
| **Д22** | Ф1 §6 (второй фактор — Ф12), Ф1 §5.1 карта (F4d-14/17/18/51 → Ф12) | — | **принято**: Д1, Д3, Д4, Д5 | — |
| **Д23** | ID-PW-1 §4.2 (`#1281` — иных видов способа ни один Given не заводит) | — | **принято**: проверяющий кода — свой, дисциплина та же (Ф-в); проверяющего пароля не трогает | — |
| **Д24** | Ф11 Р8 (словарь один, объявлен вместе с правилом; ограничение схемы — подмножество словаря) | — | **принято**: Р1 — имена дословно; расширение ограничения схемы под гейтом словаря (Ф12-40) | — |
| **Д25** | Ф11 §10 п.20, Н54 (перечень способов церемонии консоли — второе объявление подмножества словаря) | — | **не предмет Ф12, назван**: на посадке `own` перечень — `totp`, `lookup_secret`; правка — Ф8 (§4.2, §11) | — |
| **Д26** | Ф2 П1 (шапка миграции: «состояние — PRO-Robotech/kacho#1281»); F4d-13 в части «состояние» | — | **отвечено** Р1: `pending` · `active`, ограничением базы | — |
| **Д27** | Ф11-27 («число записей контрактом не является») | — | **принято**: после Р10 — 28 | — |
| **Д28** | ID-PW-1 Р1, Р2, PWV-16 (настройка «что писать» — формат между полом и потолком записи; потолок чтения — запись перечня) | — | **принято**: Р6 — набор пишется тем же хешером по той же настройке; Ф12-39 сверяет с полом записи, не с «ручкой потолка» (круг 1, Б4) | первая редакция называла порогом потолок, и законный набор на полу записи был бы находкой |
| **Д29** | Ф1 Р3, Ф3-02 (отказ входа один на все причины, побайтово; различимость — внутрь, в счётчик и журнал) | — | **принято с расширением** на состояние второго фактора (редакция 7, `kaname#257`): код при незаведённом факторе после совпавшего пароля — тот же `401` тем же телом и та же попытка (Ф12-13 «е», Р4, Р7); токен состояния остаётся у глаголов под сессией и у сброса | редакции 4…6 держали здесь `400` с токеном и оправдывали различимость тем, что она наступает «после совпавшего пароля», — то есть называли совпавший пароль всякому, кто приложил код; найдено имитацией клиента (ban #18) |

**С одобренными соседями нет ни одного расхождения без исхода.** Единица сводки — **строка
таблицы**, каждая стоит ровно в одной категории, и категории суммируются в **29**:

| категория | строк | какие |
|---|---:|---|
| принято (у Д16 — с расширением на код при том же механизме, у Д29 — на состояние фактора на входе) | 15 | Д3, Д4, Д9, Д16, Д17, Д18, Д19, Д20, Д21, Д22, Д23, Д24, Д27, Д28, Д29 |
| принято с интерпретацией | 2 | Д1, Д2 |
| отступление с доводом и ценой | 1 | Д5 |
| закрытие сироты | 1 | Д6 |
| не предмет Ф12, назван с носителем | 4 | Д7, Д8, Д14, Д25 |
| строка соседа с условием возврата «Ф12» — отвечена решением | 4 | Д11, Д12, Д13, Д26 |
| строка соседа, чьё требование здесь производится | 2 | Д10, Д15 |

Д28 добавлена кругом 1 (Б4): сумма 27 → 28, категория «принято» 13 → 14 — единица счёта прежняя;
Д29 добавлена редакцией 7 (`kaname#257`): 28 → 29, «принято» 14 → 15.
Прежняя сводка называла «принято — 19 … всего 26» при 27 строках: число «19» не давала ни
одна единица счёта, Д14 стояла в таблице как «принято», а в сводке как «не предмет», четыре
«отвечено» и два «производится» не попадали никуда. Каждое отступление и интерпретация видны
владельцу в событии одобрения этого документа.

---

## §4 Объём и границы

### §4.1 В объёме

1. **Хранилище**: новая миграция — словарь вида `['password','totp','lookup_secret']`,
   состояние строки `pending` · `active` ограничением базы, «`pending` — только у `totp`»
   ограничением базы (строки `password` и `lookup_secret` — только `active`; Н4 круга 1: три
   ограничения без этого допускали `password` в `pending`), последний принятый шаг; тип материала и гейт сдерживания — те же (Ф-б);
   домен — словарь видов, совпадающий со схемой. **Решение, которого требует шапка миграции Ф2
   П1 («ограничений сверх объявленных четырёх … нельзя без решения»):** объявленный набор
   гейта `TestLoginVerifierStaysInsideTheSchema` (`lmDeclaredConstraints`, сегодня четыре имени,
   набор закрыт — его инъекция «ограничение сверх объявленных, материала не касающееся» есть
   ровно проверка по `kind`/`state`) **расширяется именами ограничений этой миграции тем же
   изменением**, с повторной инъекцией тем же дефектом (`testing.md` §«Гейт на класс» п. 8):
   зелёный гейт означает «чужих ограничений ноль при объявленных N», а не «набор не менялся».
   Ограничение, заведённое миграцией и не внесённое в перечень, — находка гейта, и это его
   работа, а не помеха (Ф12-39). То же у статического гейта сдерживания
   (`internal/check/login_verifier_containment.go`): выход материала разрешён **файлу**, и
   проверяющий кода — HMAC над секретом и хеш набора — зовёт `Reveal` по существу; перечень
   разрешённых файлов (сегодня два: адаптер хранилища и `internal/passwordverify/verifier.go`)
   **расширяется файлом проверяющего кода с названной причиной** — одним файлом, как у пароля:
   разбор, сверка и вычисление стоят в нём целиком, наружу уходят только исход и числа. Файл
   без предмета гейт уже отвергает («разрешение без предмета»), поэтому разрешение вперёд
   невыразимо.
2. **Обёртка секрета** под своим перечнем ключей (Р2): ручка, страж старта, самоотчёт числа
   ключей; неоткрываемый материал — 503 громко.
3. **Проверяющий кода**: RFC 6238 в параметрах Р5, окно ±1 шаг, последний принятый шаг CAS,
   постоянное время, холостая сверка при отсутствующем секрете; **проверяющий запасного кода** —
   медленный хеш дисциплины Ф-в (соль на набор; формат и параметры в значении — по настройке
   «что писать» ID-PW-1 PWV-16, между полом и потолком записи; потолок чтения — запись перечня
   Р2) под **той же** ёмкостью PWV-15, холостая полоса «материала нет», однократность одного и
   годность разных — сериализацией на строке набора (Р6).
4. **Шесть глаголов** Р4 на слушателе формы Ф3; **поле** `secondFactor` в форме входа.
5. **Церемония `step-up`** — глагол один на три ветви; ветвь пароля — Ф11 (§9 п.2).
6. **Запасные коды**: чеканка при подтверждении, перечеканка, остаток в ответах, показ один раз.
7. **Снятие** самим человеком (Р9) и **сброс распорядителем** (Р10): новый RPC `UserService/ResetSecondFactor`,
   запись каталога (`identity_suspender`, пол «2»), маршрут края, регенерация каталога разрешений.
8. **Подбор**: неверный код в счёт Ф3 Р10; успешный код обнуляет.
9. **Свежесть правки своих данных**: ручка окна, отказ `SESSION_NOT_FRESH`, срок `pending`.
10. **Посадка**: провязка `totp`/`lookup_secret` в корне → Ф11-28; `external` без семейства.
11. **Край** (`PRO-Robotech/kacho`, `gateway/`): шесть путей в перечне ретрансляции и в
    `isPublicHTTPPath`; ветка недоступности и отсечки как у смены пароля; маршрут `ResetSecondFactor`.
12. **Защита форм**: виды `second-factor`, `step-up` в закрытом перечне.
13. **Журнал, счётчики, уборка** (Р11).
14. **Восемь сценариев Ф11** под их номерами и три «последней из Ф5 и Ф12» — при условии
    §6.1.

### §4.2 За границей — с носителем и предикатом возврата по каждой строке

| вынесено | сценариев здесь | почему не здесь | носитель |
|---|---:|---|---|
| условие модели `mfa_fresh`, доводы способов и момента, различение «3»/«2» его исходом | 0 | Ф11 §1.7, §6: носителем не может быть Ф12 — фактор даёт не выше «2» и не `webauthn`; посылка задания опровергнута (§0.4) | `kaname#133`, OPEN |
| ключ доступа как второй фактор (`webauthn` в церемонии) и как первый (вход без пароля) | 0 | словарь Ф11 Р8 имя несёт, производителя — нет; строки правила «утверждение ключа» здесь не наблюдаются | `#1273` (Ф7), `#1282` (Ф13), OPEN |
| экраны: заведение (QR из `otpauth`), показ и перечеканка кодов, окно повышения на **нашей** церемонии, предупреждение об исчерпании; перечень способов церемонии консоли под посадку `own` | 0 | Ф8: «все потоки рисует консоль»; сегодня окно ведёт поток поставщика (∅9); служба даёт остаток числом (Р6), предупреждение — консоль | `#1274`, OPEN; перечень — §11 |
| политика обязательности **по личности** либо по аккаунту («каждая выдача сессии требует фактора») | 0 | Р3: ресурс аккаунта со своим контрактом и владельцем; её приход меняет «Дано» Ф11-07/08/11/29 | **держателя нет** — условие возврата: решение владельца, заводящее политику своей приёмкой; до него обязательность — пол каталога |
| сброс **пароля** распорядителем (вторая половина F4d-20) | 0 | по предмету — восстановление (Ф1 §5.1 «Сироты»: Ф5); здесь закрыта половина о втором факторе | сирота; Ф5 `#1271` по предмету |
| пол «2» на новых глаголах платформы, величины полов, пол «3» | 0 | смена пола — своя приёмка (`sub-phase-SEC-acr-stepup-refinement-acceptance.md` воркспейса); здесь только запись нового глагола Р10 | — |
| снятие компонента поставщика, его настройка второго фактора (4 файла посадки), гейт достижимости пола на её стороне | 0 | до Ф10 посадка `external` ведёт второй фактор поставщиком; посылка гейта истекает с Ф10 (§11) | `#1276`, OPEN |
| ёмкость проверяющего для кода **по времени** (ID-PW-1 PWV-15) | 0 | HMAC-SHA1 одного кода дешевле хеша пароля на порядки; потолок темпа Р7 — единственный ограничитель кода по времени, и это решение. **Запасного кода строка не касается**: его сверка — медленный хеш, и она **в объёме** под той же ёмкостью (Р6, §4.1 п.3, Ф12-32, Ф12-39) — первая редакция выносила ёмкость целиком доводом, который к набору не относится | — |
| уровень «2» на полосе предъявителя и в токенах | 0 | Ф11 §6: не полоса сессии | `#1276` |

Каждая строка — осознанное сужение объёма (ban #14): фаза production-complete в своих
границах, отложенное названо, посчитано и снабжено носителем либо названо безносительным вслух.

### §4.3 Что делает сценарий НЕИСПОЛНИМЫМ, а что нет

| зависимость | состояние | делает ли сценарий неисполнимым |
|---|---|---|
| Ф3 `#1269` — сессия, носитель, полоса входа, признак формы, слушатель формы, счёт частоты | приёмка APPROVED в стволе (`#178`); кода в дереве **нет** (∅3, ∅5) | **да — «Дано» каждого сценария с сессией не построить**, и это порядок работ (§9): Ф12 кодируется **после** Ф3. До того исполнимы только Ф12-39 (миграция и обёртка — интеграционный на базе), Ф12-40 (гейт словаря) и правило Ф-а |
| Ф2 `#1268` — строка способа входа, проверяющий пароля | в стволе (§1.2 Ф-р, `#178`) | **нет**: «Дано» с паролем строится посевом строки П1 |
| Ф11 `#1280` — правило, страж | в стволе (`#163`); церемония — ждёт Ф3 | **нет**: правило даёт «2» на `{password, totp}` сегодня; церемония заводится Ф11 либо здесь (§9 п.2) |
| Ф5 `#1271` | APPROVED, `blocked` по Ф3 | **нет**: «Дано» Ф11-07/11/29 (сессия восстановления) строится посевом записи сессии с множеством `{recovery_code}` (Ф11 §3.0а, Ф3 Р1: глаголы выдачи различаются множеством) — и это то же «Дано», что у Ф5-24 (Ф3-23 снят Ф3 редакцией 8 вместе с предметом; его близнец — Ф5-24) |
| Ф8 `#1274` — экраны | OPEN | **нет**: пробы идут запросами к слушателю службы (Ф3 §9 форма); браузерная — DoD Ф8 |
| стенд посадки `own` с краем — для Ф11-25/26 (Ф12-20) | «не выполнилось» до посадки Ф3 у края | **не делает неисполнимым**: третья категория, не вердикт |

```sh
# ДОМ: PRO-Robotech/kaname, рабочая копия ветки документа. Единица — НОМЕР задачи.
# Своя (#1281) из счёта исключена. Непустой вывод — находка: §6 назвал строителя «Дано», о котором §4.3 молчит.
D=docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md
comm -13 \
  <(awk '/^### §4.3 /,/^## §5 /'    "$D" | grep -oE '#[0-9]{4}' | sort -u) \
  <(awk '/^## §6 /,/^### §6.1 /'   "$D" | grep -oE '#[0-9]{4}' | sort -u | grep -v '#1281')
#   → пусто
```

---

## §5 Сценарии

Обозначения: **Ф12-NN** — номер сценария; производитель каждого и чем строится «Дано» — §6.
Уровень при идентификаторе: **E** — наблюдаемо снаружи, запросом к полосе либо к краю; **I** —
изнутри процесса (исход транзакции, счётчик, отказ старта, ответ внутреннего RPC); **P** —
свойство дерева, проверяемое переписью. «Уровень L» и «глагол с полом L» — определение Ф11
§3.0 дословно. «Личность с паролем» — строка способа вида `password` с материалом (Ф2 П1);
«живая сессия «1»» — выдана входом паролем (Ф3-01) и не тронута; «свежая» — с момента
последнего предъявления прошло меньше окна Р8. Все моменты — часами пробы (Ф-ж); `t` — текущая
ступень кода по часам службы (30 с, Р5); ε — величина пробы, печатается. **Ступень после
принятого кода — сдвинута** (круг 1, Б2): подтверждение Ф12-02 записывает принятый шаг в строку
(Р5), и код той же ступени отвергается повтором; поэтому во всяком сценарии, чьё «Дано» стоит на
Ф12-02 либо на другом принятом коде, часы пробы сдвинуты после последнего принятого не меньше
чем на **одну ступень**, `t` — ступень **после** сдвига, и `t` старше последнего принятого шага
строки (`t > t₀`, где `t₀` — шаг подтверждения Ф12-02 либо последний принятый в самом сценарии);
где сценарию нужен зазор больше, он назван в «Дано» (Ф12-21: `t ≥ t₀ + 3`). В живом времени
(Ф12-08) то же даёт код ступени `t₀ + 1`: он в окне ±1 и старше принятого. Сценарии Ф11, исполняемые здесь, **не
переписываются** — у каждого назван производящий его сценарий Ф12 (§6.1). Тексты отказов
названы потому, что они контракт; каждый выбран так, чтобы не раскрывать ничего (§7 инв. 3).

### Полоса «заведение»

**Ф12-01 — начало заведения: секрет показан один раз, строка `pending`, фактор ещё не заведён** · E + I
**Given** личность с паролем, второго фактора нет; живая свежая сессия «1»; признак формы
`second-factor` (Ф3-35 — вид из перечня Р4)
**When** `POST /iam/v1/auth/second-factor/enroll` с телом `{"csrfToken": <признак>}`
**Then** `200`; тело `{"secret": <base32 без дополнения, 32 знака — 20 байт>, "otpauthUri":
"otpauth://totp/<домен>:<адрес>?secret=<тот же>&issuer=<домен>&algorithm=SHA1&digits=6&period=30",
"expiresAt": <момент enroll плюс окно Р8, до секунды>}`; `<домен>` — значение ручки `authn.domain`
**And** уровень I: строка `totp` человека в состоянии `pending`; строки `lookup_secret` **нет**
**And** `GET /iam/v1/auth/second-factor` → `200`, `{"totp": {"enrolled": false, "pendingUntil":
<expiresAt>}}` — ключа `backupCodes` **нет**: заведён — только `active` (Р1)
**And** вход паролем **с кодом** от этого секрета — отказ Ф12-13 «е», побайтово равный отказу на
неверный пароль (строка `pending` не способ — Р1; наружу состояние не выходит); вход паролем
без кода — «1», как до заведения
**And** положительный контроль полосы: без него Ф12-03…05 зеленели бы на глаголе, отвергающем всё

**Ф12-02 — подтверждение первым кодом: фактор заведён, запасные коды показаны один раз, это предъявление** · E + I
**Given** сессия и секрет Ф12-01; свежесть в силе; код ступени `t` от этого секрета
**When** `POST /iam/v1/auth/second-factor/confirm` с `{"code": <код>, "csrfToken"}`
**Then** `200`; тело `{"backupCodes": [<10 кодов по 10 знаков Crockford base32>], "session":
{…как Ф3-01, "assuranceLevel": "2"…}, "assurance": {"level": "2", "level2Reachable": true,
"missingForLevel2": []}}`; `Set-Cookie: kaname_session=<новое значение>` — прежний носитель
даёт «сессии нет» (Ф11 Р5); момент аутентификации и `expiresAt` прежние (Ф1-53)
**And** уровень I: строка `totp` — `active`; строка `lookup_secret` — есть, материал не равен
ни одному коду ответа (F4d-18); **последний принятый шаг строки — `t`** (далее `t₀`: всякий
сценарий, стоящий на этом, предъявляет ступень старше — §5 преамбула); множество предъявленного
сессии — `{password, totp}`; момент последнего предъявления сдвинут на момент сверки (Ф11 Р6)
**And** глагол с полом «2» по новому носителю проходит **следующим же** запросом (Ф11-08
форма: окна, где действует прежний уровень, нет)
**And** `GET /iam/v1/auth/second-factor` → `{"totp": {"enrolled": true, "confirmedAt": <момент>},
"backupCodes": {"remaining": 10, "total": 10}}`
**And** событие `iam.user.second_factor_enrolled` в очереди аудита (Ф12-42); запись журнала
повышения: до «1», после «2», способ `totp` (Ф11-14)
**And** близнец — вход паролем без кода **после** заведения даёт «1» (Ф12-14): заведённый, но не
предъявленный фактор уровня не даёт (Ф11 Р4)

**Ф12-03 — подтверждение неверным кодом: отказ, `pending` остаётся, попытка сосчитана** · E + I
**Given** сессия и секрет Ф12-01; код, не совпадающий ни с одним из шагов `t−1…t+1`
**When** `confirm` с ним
**Then** `UNAUTHENTICATED` / **401**, текст `authentication failed` — тот же, что на входе
(Ф3-02); строка `totp` — по-прежнему `pending`, строки `lookup_secret` нет; носитель **не**
перевыпущен, уровень «1»; счёт неверных предъявлений по адресу и источнику вырос на единицу
(Р7)
**And** положительный контроль — Ф12-02 тем же секретом после этого отказа проходит; различие
ровно одно — код

**Ф12-04 — подтверждение без ожидающего заведения: истёкшее, снятое уборкой, не начатое — один отказ** · E
**Given** (а) сессия и секрет Ф12-01; часы пробы сдвинуты на окно Р8 плюс ε **от момента
`enroll`**; внутри сессии за это время предъявлен пароль (`step-up`, ветвь Ф11-09) — сессия
**свежа**, и единственное, что истекло, — заведение; (б) то же, но истёкшая строка `pending` уже
снята проходом уборки (Ф12-44) до `confirm`; (в) личность с паролем, у которой строки `totp`
**нет** — `enroll` не звали; сессия свежа
**When** `confirm` с верным кодом ступени `t` от секрета (а)/(б); в (в) — с любым кодом
**Then** все три — `FAILED_PRECONDITION` / **400**, `reason = ENROLLMENT_NOT_PENDING`, текст
`no pending enrollment: begin with enroll`; **тела трёх ответов побайтово равны**: истёкшее
заведение, снятое уборкой и не начатое **снаружи неразличимы** — иначе ответ зависел бы от того,
успела ли уборка, то есть от расписания, а не от состояния (круг 1, Б1: `confirm` без `pending`
— ячейка матрицы Р4); код в (а)/(б) **не сверяется** — состояние строки и срок судятся **раньше**
кода (Р4, порядок проверок), в счёт попыток отказ **не** идёт (Р7); истёкшую строку (а)
снимает уборка (Ф12-44) либо заменяет новый `enroll` (Ф12-05 б) — подтвердить её нельзя ни
при каком коде
**And** положительный контроль — новый `enroll` выдаёт новый секрет, и `confirm` кодом от **него**
проходит; код от прежнего секрета — Ф12-03
**And** близнец по оси свежести — Ф12-09: там истекло окно **сессии**, здесь — заведения; оба
отказа разные по коду и тексту, потому что следующий шаг у них разный; при истёкших **обоих**
первым отвечает свежесть — `SESSION_NOT_FRESH` (Н6 круга 1; Р4, порядок проверок): проба на
обоих истёкших получает ровно его, а не любой из двух

**Ф12-05 — второе заведение: при `active` — 409, при `pending` — замена; оба исхода — один оператор базы** · E + I
**Given** (а) личность с заведённым (`active`) фактором Ф12-02; (б) личность со строкой `pending`
Ф12-01; (в) личность без строки `totp`; у всех сессии свежие
**When** `enroll` у каждой — и у каждой **два одновременных** `enroll`
**Then** (а) — `ALREADY_EXISTS` / **409**, `reason = SECOND_FACTOR_ALREADY_ENROLLED`, текст
`second factor is already enrolled`; строка не изменена; (б) — `200`, **новый** секрет; прежний
`pending` заменён: `confirm` кодом от прежнего секрета — Ф12-03, от нового — Ф12-02; (в) —
`200` (Ф12-01)
**And** уровень I, конкуренция: у (а) оба — 409, ни один ничего не записал; у (б) и (в) —
**оба** проходят, строка `pending` одна, её секрет — секрет **позднего** ответа (два перезапуска,
не два способа — Д2); при любом порядке фиксации. Исход держится **одним оператором базы под
ключом «человек, вид»** (F4d-14 в части исхода): тот же оператор даёт (а) ноль изменённых
строк и (б)/(в) одну — проверки перед вставкой нет
**And** отрицательный контроль: реализация «прочитал строку, затем вставил либо заменил» при двух
одновременных первых `enroll` (в) даёт второму отказ уникальности, доехавший до клиента как
409 либо 500, вместо замены — обязана краснеть; при (а) она же под чередованием способна
записать две строки — тоже краснеет
**And** положительный контроль — (в) и Ф12-01

**Ф12-06 — форма запросов семейства: отсутствующее поле называется, лишнее отвергается** · E
**Given** живая свежая сессия; годный признак
**When** `enroll` с полем, которого форма не объявляет; `confirm` без `code`; `step-up` с
`method` вне словаря Ф11 Р8 и с `method: "password"` без поля `password`; `remove` без `method`
**Then** каждый — `INVALID_ARGUMENT` / **400**, текст называет **поле**; состояние не
изменено; в счёт частоты не идёт; лишнее поле отвергается, а не глотается
(`api-conventions.md` §«Принято-и-проигнорировано»)
**And** положительный контроль — полные формы проходят (Ф12-01, 02, 15, 28)

**Ф12-07 — гонка двух подтверждений одного заведения: активируется ровно одно; второе — «уже заведён», как и последовательное** · I
**Given** секрет Ф12-01; (а) два запроса `confirm` с верным кодом одной ступени **одновременно**;
(б) после успешного Ф12-02 — второй `confirm` той же личности **последовательно**, со свежим
кодом ступени `t₀ + 1` (в окне и старше принятого)
**When** в (а) оба доходят до записи; в (б) — второй после фиксации первого
**Then** (а) — проходит **ровно один**: одна строка `active`, один набор запасных кодов, одна
запись журнала повышения; второй — `ALREADY_EXISTS` / **409**, `reason =
SECOND_FACTOR_ALREADY_ENROLLED`, текст `second factor is already enrolled` — **тот же отказ, что у
(б)** и у `enroll` при `active` (Ф12-05 а): проигравший гонку неотличим от опоздавшего; при
любом порядке фиксации; (б) — тот же 409: при `active` подтверждать **нечего**, код **не
сверяется** (состояние судится раньше кода — Р4, матрица), и в счёт попыток отказ не идёт (Р7).
Первая редакция отвечала проигравшему `401` («шаг уже принят»): один факт получал два кода в
зависимости от чередования, а второй `confirm` при `active` не был описан вовсе (круг 1, Б1)
**And** отрицательных контроля два: реализация «прочитал состояние, затем записал» при обратном
порядке даёт две записи — обязана краснеть; реализация, отвечающая проигравшему (а) `401`, —
краснеет: его код был верен, и попытка сосчитана ложно
**And** положительный контроль — два последовательных `confirm` **разных** личностей проходят
оба

**Ф12-08 — фактор заводится и предъявляется без браузера** · E
**Given** личность с паролем; клиент — набор запросов (`tests/newman/` этого дома), без браузера
**When** посев проходит цепочку: признак → вход → признак `second-factor` → `enroll` → код,
вычисленный посевом из `secret` → `confirm`; затем выход и вход паролем **с кодом ступени
`t₀ + 1`** (Ф12-11): посев **не ждёт** границы ступени — следующая ступень в окне ±1 и старше
шага, принятого подтверждением (Р5), и это сказано в посеве, а не оставлено часам (круг 1, Б2:
код той же ступени, что при `confirm`, отвергается повтором на верном продукте)
**Then** каждый шаг проходит; последний вход даёт «2» — IAM-INT-1 Р5 на нашей полосе: без
этого 27 записей каталога не проверялись бы автоматически никогда
**And** отрицательная пара — тот же вход без кода даёт «1» (Ф12-14)

### Полоса «свежесть правки своих данных» — Ф11-32 и Ф11-33 исполняются здесь

**Ф12-09 — заведение из несвежей сессии отвергается; отказ называет следующий шаг** · E (= **Ф11-33**)
**Given** живая сессия «1», выданная входом паролем; у личности заведён только пароль;
предъявления внутри сессии не было; с момента выдачи прошло окно Р8 плюс ε (часами пробы)
**When** `enroll` с годным признаком
**Then** `PERMISSION_DENIED` / **403**, `reason = SESSION_NOT_FRESH`, текст `re-authentication
required: present a credential again`; строки `pending` нет; счёт частоты не тронут; носитель
цел и годен
**And** это **Ф11-33** дословно: «отказ по свежести; второй фактор не заведён»; код и текст —
здесь, потому что Ф11-33 отдала их приёмке действия
**And** без этого близнеца Ф12-10 зеленел бы на продукте, у которого окна нет вовсе

**Ф12-10 — предъявление внутри сессии освежает окно; уровень не меняется** · E (= **Ф11-32**)
**Given** та же сессия Ф12-09
**When** внутри неё предъявлен пароль (`step-up`, `{"method": "password", "password", "csrfToken"}`
— ветвь Ф11-09), затем по **новому** носителю — `enroll`
**Then** `enroll` — `200` (Ф12-01): отсчёт окна идёт от момента последнего предъявления (Ф11 Р6);
уровень остаётся «1» — предъявление окно освежает, уровня не поднимает; **единственное**
отличие от Ф12-09 — предъявление внутри сессии
**And** это **Ф11-32** дословно

### Полоса «предъявление на входе»

**Ф12-11 — вход паролем и кодом по времени: сессия «2» на исходном носителе** · E + I (→ **Ф11-02**)
**Given** личность с паролем и заведённым фактором (Ф12-02); признак `login`; код ступени `t`
(`t > t₀` — §5 преамбула)
**When** `POST /iam/v1/auth/login` с `{"email", "password": <верный>, "secondFactor": {"method":
"totp", "code": <код>}, "csrfToken"}`
**Then** `200`; тело как Ф3-01 с `session.assuranceLevel: "2"`; `Set-Cookie: kaname_session` —
**один**, это выдача, а не перевыпуск; глагол с полом «2» по этому носителю проходит — **Ф11-02**
**And** уровень I: множество предъявленного `{password, totp}`, уровень «2» **на исходном
носителе** (Ф11 §3.0а: подменное состояние Ф11 после Ф12 производит вход); последний принятый
шаг строки `totp` = `t`; событие `iam.session.issued` несёт оба способа (Ф3 Р14)
**And** положительный контроль полосы: без него Ф12-13 зеленел бы на входе, отвергающем всякий код

**Ф12-12 — вход паролем и запасным кодом: «2», код потреблён** · E + I
**Given** та же личность; один из десяти кодов Ф12-02
**When** вход с `"secondFactor": {"method": "lookup_secret", "code": <код>}`
**Then** `200`, `assuranceLevel: "2"`; множество `{password, lookup_secret}`; тот же код на
следующем входе — `401` Ф12-13; `GET second-factor` → `remaining: 9`
**And** близнец — Ф12-11: различие одно — способ

**Ф12-13 — отказы входа со вторым фактором: один текст, побайтово, и что от чего отличимо** · E
**Given** личность `A` с паролем и фактором (Ф12-02); личность `B` с паролем без фактора;
личность `C` со строкой `pending` (Ф12-01); годный признак
**When** (а) `A`: верный пароль, неверный код; (б) `A`: неверный пароль, верный код; (в) `A`:
неверный пароль, неверный код; (г) `A`: верный пароль, код, уже принятый Ф12-11 (тот же шаг);
(д) `A`: верный пароль, `method: "lookup_secret"` с кодом, уже потреблённым Ф12-12; (е) `B` и
`C`: верный пароль и любой код с `method: "totp"`; (ж) `A`: **неверный** пароль,
`method: "lookup_secret"` с **годным** кодом №2 из набора Ф12-02
**Then** (а)…(ж) — **все семь** — `UNAUTHENTICATED` / **401**, текст `authentication failed`,
**без** `Set-Cookie`; **тела и коды всех ответов побайтово равны** между собой и ответу на
неверный пароль без кода (Ф3-02) — Ф1-02, Ф1-03. (е) отвечает так с редакции 7 (`kaname#257`):
редакции 4…6 давали здесь `FAILED_PRECONDITION` / 400 `SECOND_FACTOR_NOT_ENROLLED` — токен
церемонии (Ф12-17), снятия (Ф12-29 «г») и сброса (Ф12-30) — и доказывали, что держателю пароля
он сообщает лишь то, что тот прочтёт статусом; **не** держателю он сообщал, совпал ли пароль, —
у всякой личности без фактора, то есть у всякой свежезарегистрированной, — и один отказ на все
причины (Ф1 Р3) отменялся на самой массовой популяции (найдено имитацией клиента, ban #18).
«Не заведён» остаётся состоянием, а не формой (Р1, Р4): в церемонии, снятии, перечеканке и
сбросе его токен — как был; на входе состояние **наружу не выходит**
**And** (а), (в), (г), (д), (е) увеличили счёт неверных предъявлений по адресу и источнику на
единицу каждый — (е) **тоже**, иначе темп окна отличал бы совпавший пароль от несовпавшего
(Р7); (б) — тоже (неверный пароль, Ф3 Р10)
**And** различимость (е) — **только внутрь**: клетка `second-factor-not-enrolled` исходов входа
(Р11) выросла на единицу на каждом обращении (е) — у `B` и у `C`; клетка отказов второго
фактора `not-enrolled` (Р11) — **нет**: она считает отказы под сессией (Ф12-17), и рост её на
входе делал бы счётчик вторым местом об одном предмете; в журнале — тот же исход. Положительный
близнец (е):
та же личность `B`, тот же пароль, **без** поля `secondFactor` — `200`, сессия «1» (Ф12-14 без
фактора). Отрицательный контроль: реализация, отдающая на (е) любой код, кроме `401`, тело,
отличное от (в), либо не считающая (е) попыткой, — обязана краснеть на этих утверждениях
**And** **строка после (б) и (ж) не изменена** (круг 1, Б3): сверка кода на неверном пароле
выполняется ради времени (Ф12-33) и **не записывает ничего** (Р5, Р6) — последний принятый шаг
`totp` прежний, и Ф12-11 **тем же кодом той же ступени**, что в (б), затем проходит; набор после
(ж) — остаток прежний (`GET second-factor` из живой сессии `A` → `remaining` не изменился), и код
№2 с верным паролем затем проходит (Ф12-12). Отрицательный контроль: реализация, записывающая шаг
либо потребляющая код при несошедшемся пароле, даёт `401` на этих двух положительных контролях
и обязана краснеть — иначе держатель секрета либо списка кодов без пароля закрывал бы владельцу
ступень и потреблял коды, предъявляя их с любым паролем
**And** (ж) — тоже попытка (неверный пароль, Ф3 Р10), как (б)
**And** положительный контроль — Ф12-11 на `A` после (а) проходит (F4d-16 форма)

**Ф12-14 — вход паролем без кода при заведённом факторе: сессия «1», а не отказ** · E (Р3)
**Given** личность с паролем и заведённым фактором
**When** `POST /iam/v1/auth/login` без поля `secondFactor`
**Then** `200`, `session.assuranceLevel: "1"`; глагол с полом «1» проходит, с полом «2» —
отказ вызовом, называющим «1» и «2» (Ф11 §3.0); `GET second-factor` → `enrolled: true` — консоль
из этого узнаёт, что предложить код (Ф8)
**And** это «Дано» Ф11-08 («сессия уровня «1»; у личности заведён второй фактор») — производит
его этот сценарий; F4d-17 «остановившийся после пароля — «1»»
**And** близнец — Ф12-11: различие одно — поле `secondFactor`

### Полоса «предъявление внутри сессии»

**Ф12-15 — церемония: код по времени внутри сессии «1» поднимает до «2»** · E + I (→ **Ф11-08**)
**Given** сессия Ф12-14 («1», фактор заведён); признак `step-up`; код ступени `t` (`t > t₀`
— §5 преамбула)
**When** `POST /iam/v1/auth/step-up` с `{"method": "totp", "code": <код>, "csrfToken"}`
**Then** `200`; тело `{"session": {…"assuranceLevel": "2"…}, "assurance": {"level": "2",
"level2Reachable": true, "missingForLevel2": []}}`; `Set-Cookie: kaname_session=<новое>`;
**следующий же** запрос к глаголу с полом «2» по новому носителю проходит; прежний носитель
получает отказ, **побайтово равный** отказу по несуществующей сессии (Ф3-10); запись журнала
повышения: «1» → «2», `totp` — всё это **Ф11-08** дословно
**And** момент аутентификации не сдвинут, `expiresAt` прежний (Ф11 Р6, Ф1-53); момент
последнего предъявления — момент сверки
**And** положительный контроль полосы для Ф12-16, 17

**Ф12-16 — неверный код в церемонии: отказ без последствий** · E (→ **Ф11-30**)
**Given** сессия Ф12-14; код, не совпадающий с `t−1…t+1`
**When** `step-up` с ним
**Then** `UNAUTHENTICATED` / **401** `authentication failed`; уровень «1», носитель прежний и
**годен**: неудачное предъявление сессию не гасит и не понижает — **Ф11-30**; счёт попыток
вырос (Р7)
**And** близнец — Ф12-15, отличие — код

**Ф12-17 — церемония кодом при незаведённом факторе: отказ называет следующий шаг** · E
**Given** сессия личности `B` без фактора (Ф12-13); и сессия личности `C` со строкой `pending`
**When** `step-up` с `method: "totp"` либо `"lookup_secret"` и любым кодом
**Then** `FAILED_PRECONDITION` / **400**, `reason = SECOND_FACTOR_NOT_ENROLLED`, текст `second
factor is not enrolled`; в счёт попыток не идёт; уровень и носитель прежние
**And** близнец — Ф12-15: различие одно — заведён ли фактор

**Ф12-18 — церемония запасным кодом: «2», остаток в ответе, код потреблён** · E + I
**Given** сессия Ф12-14; запасной код Ф12-02
**When** `step-up` с `{"method": "lookup_secret", "code"}`
**Then** `200`, `assurance.level: "2"`, **и** поле `backupCodesRemaining: 9` — оно есть только в
ответе, потребившем код (Р4); тот же код второй раз — `401` Ф12-16; прочие девять годны (F4d-18)
**And** близнец — Ф12-15: различие одно — способ

**Ф12-19 — ответ церемонии называет достигнутый уровень и чего не хватает — на трёх сессиях** · E (→ **Ф11-11**, **Ф11-29**)
**Given** (а) сессия восстановления личности с паролем и фактором — множество `{recovery_code}`,
новый пароль задан (Ф1-27; «Дано» Ф11-11, строится посевом записи — §4.3); (б) сессия «1»
входом паролем той же личности; (в) сессия «1» личности **без** фактора
**When** в (а) предъявлен **только** код по времени; в (б) — то же; в (в) — пароль
**Then** (а) — `200`, `assurance: {"level": "1", "level2Reachable": true, "missingForLevel2":
["password"]}`: уровень остаётся «1», недостающее названо — **Ф11-11**; (б) — `level: "2"`,
`missingForLevel2: []` (Ф12-15); (в) — `level: "1"`, `level2Reachable: false`,
`missingForLevel2: []` — путь к «2» не в предъявлении, а в заведении
**And** в (а) затем предъявлен **заданный** пароль, потом код — `level: "2"` — **Ф11-29**:
сверка заданного — предъявление; цена названа Ф11 Р2 и Р3 здесь
**And** `missingForLevel2` не пуст только при `level2Reachable: true` и `level` ниже «2» — два
поля, два факта (Р4)

**Ф12-20 — через край на посадке `own`: пол «2» отвергает пароль и проходит после нашей церемонии** · E (→ **Ф11-25**, **Ф11-26**)
**Given** стенд посадки `own` у службы и у края (Ф3-44, Ф3-45); личность с паролем и
заведённым фактором; вход паролем через край (ретрансляция Ф3-01)
**When** запрос к глаголу с полом «2» через край; затем `step-up` кодом через край
(ретранслировано, Ф12-38); затем тот же запрос по новому носителю
**Then** первый — отказ **401** · `code` 16 · `WWW-Authenticate` с `acr_values="2"` и «presented
ACR 1» — **Ф11-25**; третий — проходит — **Ф11-26**: предикат `#1280` в написанном виде, там,
где поставщику взять его неоткуда; форма отказа — IAM-INT-1 Р9, Ф11 §4.1 дословно
**And** глагол с полом «1» проходит по обоим носителям — ступень не плата за рутину
(IAM-INT-1-22)

### Полоса «окно часов и повтор»

**Ф12-21 — окно расхождения часов — ±1 шаг** · E
**Given** сессия Ф12-14; часы службы — часами пробы; после подтверждения Ф12-02 (принятый шаг
`t₀`) часы сдвинуты не меньше чем на **три** ступени — `t ≥ t₀ + 3`, чтобы и `t−2` был старше
принятого и отказ на нём был **окном**, а не повтором (круг 1, Б2: первая редакция держала `t`
неподвижным от подтверждения и утверждала «шага у строки ещё нет» после того, как Ф12-02 его
записала, — `t−1` и `t` краснели бы повтором на верном продукте); ступень `t` неподвижна на весь
сценарий; коды ступеней `t−2`, `t−1`, `t`, `t+1`, `t+2` вычислены пробой из секрета
**When** каждый предъявлен `step-up` **в этой же сессии, в порядке возрастания шага**: `t−2`,
`t−1`, `t`, `t+1`, `t+2`. Порядок — условие, а не удобство: принятый шаг закрывает младшие у
**строки** (Р5, Ф12-22), и отдельная сессия от этого не спасала бы — первая редакция сценария
велела предъявлять «в отдельной свежей сессии», считая защиту от повтора свойством сессии, и
расходилась с Р1/Р5
**Then** `t−2` — `401` `authentication failed`: за окном, и он старше принятого `t₀`, поэтому
отвергнут окном, а не шагом; `t−1`, `t`, `t+1` — `200` (каждый — предъявление, носитель
перевыпущен, уровень «2» остаётся «2» — Ф11 Р5); `t+2` — `401`: отвергнут **окном**, а не шагом
(последний принятый — `t+1`, и `t+2` старше него)
**And** обе стороны оси названы: у́же окно — красное на `t−1`; шире — зелёное на `t+2`
**And** отрицательный контроль **самой пробы** (круг 1, Б2): часы **не** сдвинуты после Ф12-02
(`t = t₀`) — `t−1` и `t` дают `401` повтором на **верном** продукте, и проба, требующая на них
`200`, красна ложно; сдвиг на одну-две ступени (`t₀ + 1`, `t₀ + 2`) — `t−2` отвергается ещё и
повтором, и отказ на нём окно не доказывает
**And** отрицательный контроль порядка — те же пять кодов **по убыванию** дают `200` только на
`t+1`: это Ф12-22, а не окно; проба, зеленеющая на убывающем порядке для `t−1`, читает окно
из сессии, а не из строки

**Ф12-22 — повтор невозможен: принятый шаг закрывает себя и младшие — у строки, не у сессии** · E + I
**Given** сессия `S1` Ф12-14; код шага `t` принят в ней (Ф12-15); `S2` — другая живая сессия «1»
той же личности (другое устройство), в окне ±1 от `t`
**When** (а) тот же код предъявлен снова по новому носителю `S1`; затем код шага `t−1` в `S1`;
(б) затем код шага `t+1` в `S1`; (в) код шага `t` — тот, что принят в `S1`, — предъявлен из
`S2`
**Then** (а) — оба `401` `authentication failed` — тем же текстом, что неверный код (F4d-17:
«уже использован» и «не подошёл» неразличимы); (б) — `200`; (в) — `401`: последний принятый шаг
принадлежит **строке**, и сессия `S2` его не обнуляет; уровень `S2` — «1», носитель цел и
годен (Ф11-30 форма)
**And** уровень I: последний принятый шаг строки после (б) — `t+1`, после (в) — не изменён;
затем часы сдвинуты на ступень (текущая — `t+1`, круг 1, Б2: без сдвига `t+2` отвергался бы
окном, а не арбитром), и два одновременных предъявления кода ступени `t+2` — в окне и старше
принятого — проходят **ровно одним**: запись условная («шаг старше последнего принятого») и
она же арбитр (Р5, CAS);
отрицательных контроля два: реализация «сверил, потом записал» пропускает оба одновременных и
обязана краснеть; реализация, хранящая последний принятый шаг **в записи сессии**, пропускает
(в) и обязана краснеть

### Полоса «запасные коды»

**Ф12-23 — каждый код принимается один раз; прочие годны** · E + I
**Given** набор Ф12-02 (десять); сессия «1»
**When** предъявлены по очереди код №3 (дважды), затем №7
**Then** №3 первый раз — `200`, второй — `401`; №7 — `200`; остаток после — 8; F4d-18
**And** уровень I: материал строки набора после потребления не равен материалу до — код снят из
набора, а не помечен в памяти процесса; после перезапуска процесса №3 по-прежнему `401`

**Ф12-24 — гонка запасных кодов: один код — ровно один; два разных — оба** · I
**Given** набор Ф12-02; две живые сессии одной личности (два устройства)
**When** (а) два `step-up` с кодом №5 **одновременно**; (б) на свежем наборе — два `step-up` с
кодами №5 и №6 **одновременно**; в обоих случаях оба доходят до записи
**Then** (а) — ровно один `200`, другой `401` `authentication failed`; остаток 9 (не 8); счёт
попыток по адресу вырос на единицу; (б) — **оба** `200`, остаток 8, счёт попыток **не тронут**;
и там и там — при любом порядке фиксации и любом чередовании
**And** отрицательных контроля два, по одному на исход: реализация «прочитал набор, сверил,
переписал» без сериализации при чередовании (а) даёт два `200` и остаток 8 — обязана краснеть;
реализация «сравнил материал с прочитанным до записи, при расхождении — отказ» (CAS по
материалу без повтора) при (б) отвергает годный код №6 как неверный и считает его попыткой —
обязана краснеть
**And** положительный контроль — Ф12-23

**Ф12-25 — перечеканка: новый набор из десяти, прежние непригодны все** · E + I
**Given** набор Ф12-02 с потреблёнными №3 и №7; сессия живая (свежесть не требуется — Р8);
признак `second-factor`; (в) личность `B` без фактора и личность `C` со строкой `pending`
(Ф12-13), у каждой живая сессия
**When** `POST /iam/v1/auth/second-factor/backup-codes` с `{"method": "totp", "code": <код
ступени t, старше принятого>, "csrfToken"}`; затем — с `{"method": "lookup_secret", "code":
<код №1 старого набора>}`; (в) `backup-codes` у `B` и у `C` с любым `method` и любым кодом
**Then** первый — `200`, тело `{"backupCodes": [<10 новых>], "session": {…}, "assurance":
{…"level": "2"…}}`, носитель перевыпущен (предъявление); `remaining: 10`; **все** десять
прежних — `401` при предъявлении; событие `iam.user.backup_codes_regenerated`; второй запрос —
`401`: старый код непригоден (и был бы потреблён, будь он годен); (в) — `FAILED_PRECONDITION` /
**400**, `reason = SECOND_FACTOR_NOT_ENROLLED`, текст `second factor is not enrolled` — тот же
токен, что в церемонии, снятии и сбросе (у всех глаголов под сессией и у сброса; на входе факт
наружу не выходит — Ф12-13 «е»): перечеканка без заведённого фактора — «не
заведён» (матрица Р4; круг 1, Б1: `backup-codes` без фактора); код **не сверяется**, набора нет
как не было, `pending` у `C` не тронута, в счёт попыток не идёт
**And** перечеканка без `code` — `400` с именем поля; с неверным — `401` `authentication
failed`, набор не тронут — пара отказов Ф3 Р6
**And** близнец — набор до перечеканки: №1 старого набора до неё проходил (Ф12-23)

**Ф12-26 — исчерпание: остаток ноль назван числом, код по времени по-прежнему работает** · E
**Given** набор Ф12-02; сессия
**When** потреблены все десять по очереди; затем предъявлен одиннадцатый (любой из десяти);
затем код по времени
**Then** ответ, потребивший десятый, несёт `backupCodesRemaining: 0`; `GET second-factor` →
`remaining: 0, total: 10`; одиннадцатый — `401`; код по времени — `200`
**And** предупреждение человеку — работа консоли над числом (Ф8, Р6); служба **не** молчит на
нуле и **не** перечеканивает сама

**Ф12-27 — секрет и коды показываются один раз и ниоткуда не читаются** · E + I
**Given** заведённый фактор Ф12-02
**When** читаются `GET second-factor`, ответ `Resolve` (Ф3-09), «кто я», журнал аудита, журнал
повышения, `/metrics`, тела всех ответов семейства, кроме `enroll` и `confirm`/`backup-codes` в
момент чеканки
**Then** ни один не несёт ни секрета, ни кода, ни `otpauthUri`; остаток — только число
**And** уровень I: строка `totp` несёт секрет **обёрнутым** (Ф12-39), строка набора — материал,
из которого код не восстановим; проба, спросившая хранилище напрямую, значения кода не получает
**And** положительный контроль различимости — ответы `enroll` и `confirm` несут их (Ф12-01, 02)

### Полоса «снятие и сброс»

**Ф12-28 — снятие кодом: строки сняты, прочие сессии погашены, текущая жива и уровня не теряет** · E + I
**Given** личность с фактором; три её сессии `S1` (текущая, «1» входом паролем), `S2`, `S3`
(другие устройства; `S3` — «2» после церемонии); признак `second-factor` под `S1`
**When** `POST /iam/v1/auth/second-factor/remove` с `{"method": "totp", "code": <код>,
"csrfToken"}` и носителем `S1`
**Then** `200`; тело `{"session": {…"assuranceLevel": "2"…}}` — сверка кода есть предъявление,
`S1` теперь «2» на **новом** носителе; строк `totp` и `lookup_secret` нет; `GET second-factor` →
`enrolled: false`; записи `S2` и `S3` **сняты** (их носители — «сессии нет»); `S1` жива, момент
аутентификации и `expiresAt` прежние; событие `iam.user.second_factor_removed` — всё одним
исходом одной транзакции (частичного не бывает: при подставном отказе любой из записей ничего
не изменено)
**And** `S1` держит «2» до срока либо отзыва (Ф11 §7 инв. 5) — пол «2» по ней проходит и после
снятия; вход паролем на другом устройстве — «1» (Ф12-14 без фактора); вход с кодом —
Ф12-13 «е»; `step-up` кодом — Ф12-17
**And** новое заведение после снятия проходит (Ф12-01) — смена устройства без распорядителя
(Р9, Д5)

**Ф12-29 — отказы снятия: без кода, с неверным кодом, без фактора, при незавершённом заведении** · E
**Given** личность с фактором и живой сессией; годный признак
**When** (а) `remove` без `code`; (б) с неверным кодом; (в) с кодом, уже принятым в этой сессии
(повтор); (г) `remove` у личности без фактора — строки `totp` нет; (д) `remove` у личности со
строкой только `pending` (Ф12-01), с любым кодом
**Then** (а) — `INVALID_ARGUMENT` / **400**, текст называет поле `code`; (б), (в) —
`UNAUTHENTICATED` / **401** `authentication failed`, сосчитаны попыткой; (г), (д) —
`FAILED_PRECONDITION` / **400**, `reason = SECOND_FACTOR_NOT_ENROLLED`, тела побайтово равны:
`pending` — не способ (Р1), и в (д) строка `pending` **не тронута** — `remove` снимает заведённый
фактор, незавершённое заведение снимает срок либо заменяет новый `enroll` (матрица Р4; круг 1,
Б1); код в (г)/(д) не сверяется, попыткой не считается; ни в одном случае строки не сняты,
сессии не тронуты, носитель не перевыпущен — пара отказов F4d-51 в части подтверждения
**And** положительный контроль — Ф12-28

**Ф12-30 — сброс распорядителем: фактор снят, все сессии человека погашены, чужой аккаунт — промах** · E + I
**Given** человек `U` с фактором и двумя живыми сессиями («1» и «2»); человек `V` того же
аккаунта со строкой только `pending` (Ф12-01) и живой сессией; распорядитель `D` их аккаунта с
`identity_suspender` на `iam_user` `U` и `V`, сессия `D` — «2»; распорядитель `D'` **чужого**
аккаунта, тоже «2»; распорядитель `D''` своего аккаунта с сессией «1»
**When** `D` зовёт `UserService/ResetSecondFactor` для `U` через край; затем `D'` — тот же вызов;
затем `D''`; затем `D` — для `V`
**Then** `D` — `200`; уровень I: строка `totp` (`active`) и строка `lookup_secret` у `U` сняты; отсечка
`U` записана моментом `now` с причиной `second-factor-reset` и актором `D` (Ф-л) — оба
носителя `U` на следующем предъявлении получают отказ по отсечке (F4d-22 на крае); событие
`iam.user.second_factor_reset` с субъектом `U` и актором `D`; `D'` — `NOT_FOUND` / **404**
текстом `User <id> not found`, **байт-идентичным** промаху по несуществующему `<id>`
(сокрытие существования, Ф-м); `D''` — отказ по полу: 401 · `code` 16 · `acr_values="2"`
(запись каталога с `required_acr_min: "2"`)
**And** `U` входит паролем — «1» (фактора нет), заводит фактор заново (Ф12-01); повторный
сброс у `U` без фактора — `FAILED_PRECONDITION` / 400 `SECOND_FACTOR_NOT_ENROLLED`
**And** уровень P: запись каталога `kaname.cloud.iam.v1.UserService/ResetSecondFactor` есть в
обеих побайтовых копиях каталога (гейт паритета), число записей с полом «2» — **28**; RPC
маршрутизируется краем на внешнем слушателе как всякий глагол `UserService`
**And** `D` для `V` — `FAILED_PRECONDITION` / **400** `SECOND_FACTOR_NOT_ENROLLED`, побайтово
равный повторному сбросу у `U`: строка только `pending` — фактора нет (Р1); `pending` у `V` **не
тронута**, сессия `V` не гашена, события нет — сбрасывать нечего, незавершённое заведение `V`
заменяет сам либо его снимает срок (матрица Р4; круг 1, Б1: `ResetSecondFactor` при `pending`)
**And** положительный контроль области: `D` на своём аккаунте проходит — без него отказ `D'`
был бы неотличим от «глагол не работает ни для кого» (F4d-20 форма)

### Полоса «подбор»

**Ф12-31 — N неверных кодов → отказ по частоте; счёт общий с паролем; по источнику; обнуление** · E
**Given** `N_адрес`, `T_адрес`, `N_источник`, `T_источник` объявлены профилем (Ф3 Р10);
личность с фактором и сессией «1»
**When** (а) `N` неверных кодов в `step-up`, затем `N + 1`-й **верный**; (б) в свежем окне:
`N − 1` неверных **паролей** на входе, затем один неверный **код** в церемонии (`N`-й), затем
верный код (`N + 1`-й); (в) с одного источника по одному неверному коду на `N` разных личностей,
затем ещё один; (г) после окна — верный код; (д) при `N ≥ 2` (проба печатает `N`): `N − 1`
неверных, верный, снова `N − 1` неверных, верный
**Then** (а) — `N + 1`-й — `RESOURCE_EXHAUSTED` / **429**, `reason = TOO_MANY_ATTEMPTS`, `too many
attempts; try again later`, `Retry-After` — секунды до конца окна, **и верный код в этом окне
не сверяется** (уровень остаётся «1»); (б) — `N`-й — `401`, `N + 1`-й — `429`: пароль и код —
один счёт по адресу (Ф3-28 форма); (в) — последний — `429` по источнику (Ф3-29); (г) —
`200`; (д) — оба верных — `200`: первый обнулил счёт по адресу, и вторая серия начала счёт
заново; без обнуления неверных накопилось бы `2N − 2 ≥ N`, и второй верный получал бы `429` —
Р7: успешное предъявление обнуляет
**And** те же четыре величины, тот же отказ старта при незаданной (Ф3-28); новой ручки нет
**And** положительный контроль — верный код ниже предела проходит (Ф12-15)

**Ф12-32 — что попыткой НЕ считается** · E + I
**Given** обе величины `N` объявлены; личность с фактором
**When** `N − 1` неверных кодов; затем отказ формы (Ф12-06), отказ признака (Ф3-36), отказ по
свежести (Ф12-09), `SECOND_FACTOR_NOT_ENROLLED` (у другой личности, Ф12-17), `ENROLLMENT_NOT_PENDING`
(Ф12-04), недоступность материала (Ф12-35), исчерпание ёмкости проверяющего на предъявлении
**запасного** кода (ID-PW-1 PWV-15 — подставной блокирующий проверяющий, Ф3-30 форма; наружу —
тот же `401`); затем верный код
**Then** ни один из семи не увеличил счёт — верный код проходит **без** отказа по частоте
**And** при занятой ёмкости предъявление запасного кода личностью **без** набора и личностью со
строкой `pending` занимает ту же ёмкость: исход «ёмкость исчерпана» тем же исходом и в то же
время (PWV-15.4 форма), не мгновенный `SECOND_FACTOR_NOT_ENROLLED`; код **по времени** при
занятой ёмкости сверяется — ёмкости он не занимает (Р6)
**And** клетки счётчиков по каждому из семи исходов выросли на единицу (Ф12-43) — различимость
внутрь, не наружу

**Ф12-33 — отказы неразличимы по времени — и у личности без фактора тоже** · E (измерительный — Ф1-48 форма)
**Given** личность `A` с фактором; личность `B` без фактора; личность `C` со строкой `pending`
(Ф12-13); классы на `A`: (1) неверный пароль без кода; (2) неверный пароль с кодом `totp`;
(3) верный пароль с неверным кодом; (4) верный пароль с повторённым кодом; в церемонии: (5)
неверный код; (6) повторённый код; **на `B` и `C` — неверный пароль с кодом**: (7) `B`,
`method: "totp"`; (8) `C`, `method: "totp"`; и по запасному коду: (9) `A`, неверный пароль,
`method: "lookup_secret"`; (10) `B`, неверный пароль, `method: "lookup_secret"`; **и после
совпавшего пароля у личности без фактора** (редакция 8, `kaname#257`): (11) `B`, **верный**
пароль, `method: "totp"`
**When** классы чередуются, медианы и размах — критерий Ф1-48; проба печатает `N` и `T` и не
получает ни одного отказа по частоте (PWV-03.5 форма) и ни одного — по ёмкости (Р6)
**Then** (1), (2), (3) попарно неразличимы; (5) и (6) неразличимы; **(2), (7), (8) попарно
неразличимы** — тот, кто пароля не знает, не узнаёт по времени, заведён ли фактор и есть ли
строка `pending`: у `B` и `C` секрета нет, и сверка идёт над холостым секретом (Р5); **(9) и (10)
неразличимы** — у `B` набора нет, и хеш считается над холостым материалом набора той же
стоимости (Р6); **(7) и (11) неразличимы** — у `B` совпавший пароль не отличим от несовпавшего
ни телом (Ф12-13 «е»), ни временем: обе полосы кончаются записью попытки и ждут одного потолка
огибающей (Ф3 Р17); сверка кода выполняется и на неверном пароле (Р5), сравнение — постоянным
временем. Пары **между** способами (`totp` против `lookup_secret`) не сравниваются: способ
называет клиент, и разница стоимости HMAC и медленного хеша оракулом не является
**And** отрицательных контроля два: реализация, пропускающая сверку кода при неверном пароле,
даёт различимую пару (2)/(3) — обязана краснеть; реализация, пропускающая сверку при
**отсутствии** строки `active` (без холостой полосы), даёт различимую пару (2)/(7) либо (9)/(10)
— обязана краснеть; третий — редакции 8: реализация, отвечающая на (11) до потолка огибающей
(отказом по состоянию вне ожидания), даёт различимую пару (7)/(11) — обязана краснеть

### Полоса «посадка, ключ, ручки»

**Ф12-34 — посадка `own` с провязанным вторым фактором: предъявимых уровней два** · I (→ **Ф11-28**)
**Given** профиль `authn.identityProvider=own` с хранилищем способов (Ф2), сессией (Ф3),
обёрткой секрета Р2 и проверяющим кода, собранными корнем; каталог требует «2» (28 записей)
**When** процесс поднимается
**Then** страж посадки требование «объявленный пол предъявим» **проходит**; самоотчёт старта
называет `lane_presentable_acrs=2` — **Ф11-28**; `wiredSignInMethods` корня называет `password`,
`totp`, `lookup_secret` (наблюдение, а не литерал; Ф11 Р9)
**And** близнец — Ф11-27 (в DoD Ф11): корень без проверяющего кода — отказ старта, текст
называет число записей с «2» и перечень «1»
**And** отрицательный контроль: корень, объявивший `totp` провязанным при **несобранной**
обёртке Р2, — отказ старта ручкой обёртки (Ф12-35), а не старт с фактором, который нечем открыть

**Ф12-35 — ключ обёртки секретов: ручка без умолчания; смена ключей перечнем; неоткрываемый материал — 503 громко** · I + E
**Given** профиль `own` с провязанным вторым фактором
**When** (а) ручка `authn.second-factor-encryption-key-hex` не задана — **ни значением, ни
именем переменной окружения** (`…-key-hex-env`, обе половины Р2; Н5 круга 1); (б) задана ключом не в 32
байта на второй позиции из двух; (в) задана одним ключом `K1` — фактор заведён; затем процесс
перезапущен с перечнем `K2,K1`; затем с перечнем `K2` **без** `K1`
**Then** (а) — отказ старта с именем ручки; (б) — отказ старта с позицией `#2 of 2`, без единого
байта значения; (в) — под `K2,K1` код сверяется (`200`), самоотчёт старта печатает число ключей
2; под `K2` без `K1` предъявление кода — `UNAVAILABLE` / **503**, текст `second factor temporarily
unavailable`, запись журнала уровня ошибки, клетка счётчика «материал не открывается» выросла;
в счёт попыток — **не** идёт; `401` здесь **не** производится (Р2)
**And** положительный контроль — новое заведение под `K2` оборачивает `K2` и открывается им
**And** отрицательный контроль (б): реализация, усекающая либо растягивающая ключ до 32 байт, стартует и обязана краснеть

**Ф12-36 — окно свежести правки своих данных — ручка без умолчания** · I
**Given** профиль `own`
**When** ручка `authn.self-service-freshness` не задана; задана `15m`
**Then** первое — отказ старта с именем ручки (Ф-д); второе — старт, и Ф12-09/10 читают эту
величину: смена значения в профиле меняет исход Ф12-09 без пересборки (Ф1 Р7)
**And** дословный перенос «15 мин» Ф1 §4.1 — в профилях обоих чартов, не в построении

**Ф12-37 — посадка `external` семейства не несёт** · I
**Given** профиль `external`
**When** процесс поднимается; вызваны шесть путей Р4 и `ResetSecondFactor`
**Then** шесть путей — `404` на адресе консоли (не зарегистрированы), `ResetSecondFactor` —
не провязан; страж посадки второго фактора не требует (`external` ведёт его поставщиком);
положительный контроль — под `own` все семь отвечают (Ф3-44 форма)

### Полоса «край»

**Ф12-38 — край ретранслирует шесть глаголов через полосу сессии; недоступность и отсечка — как на смене пароля** · E
**Given** край посадки `own` с дублёром службы, считающим ретрансляции и читающим заголовки
(Ф3-51 форма); носители: `A` — живая сессия; `B` — сессия, покрытая отсечкой принудительного
выхода; `C` — снятая выходом. Носителя «сессия восстановления с требованием сменить пароль»
здесь **нет** и быть не может: поле снято с контракта (`kacho#2697`, `kaname#201`), и сессия
восстановления на крае неотличима от `A` (Ф3 Р8 редакции 8) — своей половины ей не полагается
**When** каждый из шести глаголов Р4 с носителем `A`, `B`, `C`; затем с `A` при дублёре,
отвечающем `UNAVAILABLE` на `Resolve`; затем `A` с приложенными заголовками `x-kacho-principal-*`
чужого субъекта
**Then** `A` — ретранслировано 6, запрос несёт `Cookie` и **один** `X-Forwarded-For`, **ноль**
заголовков пространства `x-kacho-` в обеих формах написания и без `Authorization`; `B` — отказ
F4d-22 на крае, носитель гасится, ретранслировано 0; `C` — ретранслировано 6 («сессии нет»
судит служба: `401` `authentication failed`); недоступность — F4d-23 на крае, носитель цел,
ретранслировано 0 — как у смены пароля (Ф3-20 «д»), потому что каждый из шести меняет
состояние под сессией носителя; чужие заголовки — исход побайтово равен исходу без них
**And** положительный контроль различимости — выход при том же дублёре ретранслируется (Ф3-17)
**And** уровень P: шесть путей стоят в **том же** объявлении, что четыре пути Ф3, и в
`isPublicHTTPPath` — второе объявление перечня — находка гейта Ф3 §8 инв. 7

### Полоса «хранилище и словарь»

**Ф12-39 — секрет в базе не читается без ключа; материал не копируется; гейт сдерживания видит новые виды** · I + P
**Given** заведённый фактор под ключом `K1`
**When** строка `totp` прочитана из базы напрямую; попытка `Unwrap` без `K1`; проба гейта
сдерживания на дереве после Ф12
**Then** значение материала не равно секрету ответа `enroll` и не содержит его подстрокой;
без `K1` — отказ `ErrUnwrap`, без байта материала в тексте; `TestLoginVerifierStaysInsideTheSchema`
проходит на таблице с тремя видами **при объявленном наборе, расширенном именами ограничений
миграции Ф12** (§4.1 п.1): перепись печатает «ограничений объявленных N из N, чужих 0», где N —
четыре прежних плюс ограничения этой миграции; копий материала в других отношениях 0,
механизмов, исполняющихся при записи, 0, статистика по колонке не собирается; гейты
`internal/check/login_verifier_containment.go` и `login_verifier_flow.go` судят материал
`totp` и `lookup_secret` тем же типом: разрешённых файлов **три** (адаптер, проверяющий
пароля, проверяющий кода — каждый с причиной и с предметом), выход материала вне них — находка
**And** строка набора: материал объявляет **формат, названный настройкой «что писать»**
(ID-PW-1 PWV-16), параметры стоимости **не ниже пола и не выше потолка записи этого формата**
(ID-PW-1 Р2; ровно на полу — законно: это нынешние параметры §1.2 ID-PW-1) и соль набора;
потолок читатель судит по записи перечня (PWV-14), не по ручке; ни один элемент материала не
равен ни одному коду ответа `confirm` и не содержит его подстрокой; потребление кода меняет
материал (Ф12-23)
**And** отрицательные контроли: строка `totp`, положенная **открытым** секретом, — находка
интеграционной пробы («материал равен секрету»); строка набора, положенная **открытыми** кодами,
**быстрым хешем** (признака формата из перечня в значении нет) либо объявленным форматом с
параметрами **ниже пола** его записи, — та же находка на строке набора; **законный близнец** —
набор, чеканенный ровно на полу записи, молчит (круг 1, Б4: первая редакция сверяла «не ниже
ручки потолка», то есть «ровно на потолке», и краснела бы на нём); второй контейнер секрета (своя
таблица) — находка гейта сдерживания на
втором имени таблицы; ограничение миграции Ф12, **не внесённое** в объявленный набор, — находка
«чужое ограничение» с его именем (инъекция гейта повторена на трёх видах — `testing.md` §«Гейт
на класс» п. 8); разрешение файлу проверяющего кода **без** обращения к выходу — находка
«разрешение без предмета»

**Ф12-40 — словарь вида: схема, домен и словарь Ф11 сходятся; `pending` — не способ** · P + I
**Given** дерево после миграции Ф12
**When** гейт `TestAssuranceMethodVocabularyIsDeclaredOnce`; вставка строки с видом вне
`['password','totp','lookup_secret']`; вставка `totp` с состоянием вне `['pending','active']`;
вставка `lookup_secret` с `pending` **и** вставка `password` с `pending` (одно ограничение:
`pending` — только у `totp`, Н4 круга 1) — три ограничения схемы, каждое **объявлено** гейту
сдерживания поимённо (§4.1 п.1)
**Then** гейт: объявлений перечня имён в прод-коде службы **1** (правило), ограничений схемы,
сверенных со словарём, — 1, имя вне словаря — 0; все четыре вставки отвергает **база** — отказом
ограничения схемы, не проверкой в коде; `LoginMethodKinds()` домена — три вида, побайтово равные ограничению
**And** уровень I: строка `pending` не входит в множество, по которому правило считает уровень,
и не делает `enrolled: true` (Ф12-01)
**And** близнец — вставка `active` `totp` и `active` `lookup_secret` проходит

### Полоса «защита форм»

**Ф12-41 — виды `second-factor` и `step-up` в том же перечне; чужой вид отвергается** · E
**Given** живая сессия; признаки видов `login`, `logout`, `password`, `second-factor`, `step-up`
под одним контекстом
**When** каждый из пяти глаголов, меняющих состояние (`enroll`, `confirm`, `remove`,
`backup-codes`, `step-up`) — с признаком **своего** вида; затем `enroll` с признаком `step-up`;
`step-up` с признаком `password`; `confirm` без признака
**Then** пять — проходят (Ф1-42 форма); два чужих — `PERMISSION_DENIED` / **403**
`FORM_TOKEN_REJECTED` `form token rejected`; без признака — `INVALID_ARGUMENT` / 400 с именем
поля `csrfToken`; состояние не изменено — Ф1-39, Ф1-40
**And** `GET /iam/v1/auth/csrf?form=second-factor` и `?form=step-up` выдают признаки; `form`
вне перечня — 400 (Ф3-35); перечень видов — **одно** объявление (Ф3 §8 инв. 7): второе — находка

### Полоса «журнал, счётчики, уборка»

**Ф12-42 — события аудита одной транзакцией, без личных данных и без секретов** · I
**Given** подтверждение (Ф12-02), снятие (Ф12-28), перечеканка (Ф12-25), сброс (Ф12-30)
**When** каждый выполнен; затем — с подставным отказом записи очереди аудита
**Then** в очереди — `iam.user.second_factor_enrolled`, `iam.user.second_factor_removed`,
`iam.user.backup_codes_regenerated`, `iam.user.second_factor_reset` (субъект и актор различны);
каждое несёт субъект, идентификатор сессии, вид способа; **ни адреса, ни имени, ни секрета, ни
кода** (гейт Ф-к); при отказе записи очереди изменение **не** зафиксировано (Ф-к)
**And** предъявления `totp` и `lookup_secret` внутри сессии — записи журнала повышения Ф11-14 с
этими именами; вход с кодом — событие `iam.session.issued` с обоими способами (Ф3-47)

**Ф12-43 — счётчики с нулевыми клетками до первого события** · I
**Given** только что поднятый процесс
**When** читается `/metrics`
**Then** клетки существуют с нулём: предъявления кода по способу × исходу (сошёлся · не сошёлся
· повторён · материал не открывается · ёмкость исчерпана — последняя только у `lookup_secret`,
Р6) · запасных кодов потреблено · заведений начато · подтверждено · истекло · снятий · сбросов ·
отказы второго фактора по виду (пять клеток Р11) · исходы входа `second-factor-refused` и
`second-factor-not-enrolled` рядом с исходами Ф3-48 (Р11)
**And** после одного события ровно одна клетка выросла на единицу — положительный контроль;
(е) Ф12-13 растит `second-factor-not-enrolled` и **не** растит `not-enrolled` отказов

**Ф12-44 — уборка снимает истёкшие `pending`, не трогает `active`** · I
**Given** три строки: `pending`, чей срок истёк на управляемых часах; `pending` в сроке; `active`
**When** проход уборки существующей петлёй (Ф-и)
**Then** снята ровно первая; две другие не тронуты; перепись печатает «осмотрено N · снято M»
**And** порог — функция предиката читателя: строка, которую `confirm` уже не примет ни при
каком коде, годна к снятию — и не раньше (приёмка `retention-sweep-has-a-caller.md` §2.2)

---

## §6 Ведомость: сценарий → производитель, род требуемого и чем строится «Дано»

Ведомость сверяется по **трём** осям (Ф1 §5, Ф3 §7): метка без строки и строка без метки —
одна и та же находка; третья — **пригодность**: названная фаза обязана производить требуемый
**род** (отказ · значение · запись · величина · перепись). Производитель «Ф12 `#1281`»
означает **эту** фазу; там, где строит чужой дом либо существующая форма, это названо.
**Производитель и строитель «Дано» — разные роли** (Ф5 §7): Ф3 и Ф2 строят «Дано» почти всякого
сценария и производителями здесь не являются.

| сценарий | род требуемого | производитель | чем строится «Дано» |
|---|---|---|---|
| **Ф12-01** | значение (секрет, адрес) + запись `pending` | **Ф12** `#1281` (глагол, обёртка Р2, строка Р1); домен адреса — существующая ручка `authn.domain` | сессия — Ф3 `#1269` (вход Ф3-01 на слушателе службы); пароль — Ф2 `#1268` (посев строки П1); признак — Ф3-35 |
| **Ф12-02** | значение + запись + перевыпуск | **Ф12** (сверка, активация, чеканка кодов); перевыпуск и журнал повышения — Ф11 `#1280` (церемония, §9 п.2); уровень — Ф-а | Ф12-01; код — вычислен пробой из секрета (RFC 6238, часы Ф-ж) |
| **Ф12-03** | отказ + неизменность | **Ф12**; текст — Ф3 Р2 (`authentication failed`); счёт — Ф3 Р10 | Ф12-01 |
| **Ф12-04** | отказ (один на три состояния) | **Ф12** (срок `pending` от окна Р8; порядок проверок Р4); ветвь пароля церемонии — Ф11 | (а) Ф12-01, часы Ф-ж; (б) — проход уборки Ф12-44; (в) — личность Ф2 без строки `totp` |
| **Ф12-05** | отказ (409) + запись (замена) — оба одним оператором под конкуренцией | **Ф12** (оператор заведения); ключ «человек, вид» — Ф2 `#1268` (П1) | Ф12-02, Ф12-01; (в) — личность Ф2 без строки `totp` |
| **Ф12-06** | отказ | **Ф12** | форма без поля — в пробе |
| **Ф12-07** | запись (ровно одна) + отказ 409 | **Ф12** (CAS; матрица Р4) | (а) Ф12-01, два запроса — в пробе; (б) Ф12-02 |
| **Ф12-08** | проход (цепочка) | **Ф12**; наборы `tests/newman/` этого дома — существующая форма | личность — Ф2; клиент набора — Ф3 §9 форма (сертификат края — Ф3 Р16) |
| **Ф12-09** | отказ | **Ф12** (окно на действии, ручка Р8); отсчёт от последнего предъявления — Ф11 Р6 (запись — Ф3 Р1) | сессия Ф3-01; часы Ф-ж |
| **Ф12-10** | проход | **Ф12** (окно на действии); сдвиг момента предъявлением — Ф11 (церемония, ветвь пароля) | Ф12-09; `step-up` паролем — Ф11 |
| **Ф12-11** | значение (сессия «2» на исходном носителе) | **Ф12** (поле `secondFactor`, сверка); строка «2» правила — Ф-а; выдача — Ф3 | Ф12-02 (фактор) |
| **Ф12-12** | значение + запись (потребление) | **Ф12** | Ф12-02 |
| **Ф12-13** | отказ, побайтово один — включая (е): состояние наружу не выходит; попытка на (е); неизменность строки после неполного успеха | **Ф12** (Р5, Р6: запись только при полном успехе; Р4, Р7 — (е), редакция 7); отказ входа — Ф3-02 (Ф-б Ф3 §5.2) | личности `A`, `B`, `C` — Ф2 посев + Ф12-01/02; набор `A` — Ф12-02 |
| **Ф12-14** | значение («1») | **Ф3** (вход паролем); что фактор не обязателен — **Ф12** Р3 | Ф12-02 |
| **Ф12-15** | значение + ответ + перевыпуск + запись | **Ф12** (ветвь `totp` церемонии, форма ответа Р4); перевыпуск, журнал повышения — Ф11 | Ф12-14 |
| **Ф12-16** | отказ + неизменность | **Ф12** | Ф12-14 |
| **Ф12-17** | отказ | **Ф12** | сессии `B`, `C` — Ф3-01 + Ф12-01 |
| **Ф12-18** | значение + величина (`backupCodesRemaining`) | **Ф12** | Ф12-14, Ф12-02 |
| **Ф12-19** | ответ (три состояния полей) | **Ф12** (поля `assurance`); уровень — Ф-а | (а) — посев записи сессии с множеством `{recovery_code}` и заданным паролем (Ф11 §3.0а, §4.3); (б) — Ф12-14; (в) — Ф3-01 без фактора |
| **Ф12-20** | отказ + проход через край | **Ф12** (церемония кодом, ретрансляция); полоса нашей сессии на крае — Ф3 (`#1269`, §4.1 п.9); форма отказа — Ф11 §4.1 (П1 там) | стенд `own` у службы и у края — Ф3-44/45; фактор — Ф12-02 |
| **Ф12-21** | проход / отказ по шагу | **Ф12** (окно Р5; порядок — Р5) | сессия — Ф3-01; часы Ф-ж; коды — пробой |
| **Ф12-22** | отказ + запись (ровно одна) + неизменность строки из другой сессии | **Ф12** (последний принятый шаг — у строки, CAS) | Ф12-15; `S2` — вторая Ф3-01 той же личности |
| **Ф12-23** | проход / отказ + запись | **Ф12** | Ф12-02 |
| **Ф12-24** | запись (ровно одна у одного кода; обе у разных) | **Ф12** (сериализация чтения-изменения-записи на строке набора) | Ф12-02; две сессии — Ф3-01 |
| **Ф12-25** | значение + запись + отказ (401, 400) | **Ф12** (матрица Р4 — (в)) | Ф12-23; (в) — личности `B`, `C` Ф12-13 |
| **Ф12-26** | величина (0) + проход | **Ф12** | Ф12-02 |
| **Ф12-27** | отсутствие поля (перепись ответов) | **Ф12**; `Resolve`, «кто я» — Ф3 | Ф12-02 |
| **Ф12-28** | запись (транзакция) + перевыпуск + отказ прочих | **Ф12** (снятие, транзакция); снятие записей прочих сессий — форма Ф3 Р6 п.2 | три сессии — Ф3-01 ×3; `S3` «2» — Ф12-15 |
| **Ф12-29** | отказ | **Ф12** (матрица Р4 — (г), (д)) | Ф12-02; (д) — Ф12-01 |
| **Ф12-30** | значение + запись + отказ (404, 401) + перепись | **Ф12** (RPC, каталог, транзакция); отсечка `now` — Ф-л (существующий писатель); сокрытие существования, пол — Ф-м; маршрут — край (`PRO-Robotech/kacho`, `gateway/`) | `U` — Ф12-02 + Ф3-01 ×2; `D`, `D'`, `D''` — существующие посевы распорядителей и выдача `identity_suspender` |
| **Ф12-31** | отказ (429) + величины | **Ф3** `#1269` (Р10 — экземпляр счёта); **Ф12** — код в счёт, обнуление кодом | профиль с четырьмя величинами — Ф3-28; личность — Ф12-02 |
| **Ф12-32** | неизменность счёта + клетки | **Ф12**; исчерпание ёмкости — Ф2 (PWV-15, подставной блокирующий проверяющий — Ф3-30 форма) | Ф12-06, 09, 17, 04, 35; отказ признака — Ф3-36; личности без набора и со строкой `pending` — Ф2 посев + Ф12-01 |
| **Ф12-33** | неразличимость по времени | **Ф12** (сверка на неверном пароле; холостая полоса «материала нет» у обоих способов — Р5, Р6; постоянное время); критерий — Ф1-48; форма холостой полосы — ID-PW-1 PWV-06 | личности `A`, `B`, `C` — Ф12-02, Ф2 посев, Ф12-01 |
| **Ф12-34** | величина (самоотчёт) + проход стража | **Ф12** (провязка `totp`, `lookup_secret` в корне); правило — Ф-а; страж — Ф-е | профиль `own` с Ф2, Ф3, обёрткой Р2 — проба корня композиции (Ф3-44 форма) |
| **Ф12-35** | отказ старта + проход + отказ 503 | **Ф12** (ручка, страж, исход материала); обёртка — Ф-г; текст без причины — Ф-н | Ф12-02 под `K1` |
| **Ф12-36** | отказ старта / проход | **Ф12** (ручка Р8); страж — Ф-д | профиль — в пробе |
| **Ф12-37** | отказ 404 / непровязка | **Ф12**; посадка — Ф3 Р15 | профиль `external` |
| **Ф12-38** | перепись ретрансляций + отказы F4d-22/23 | **Ф12** — шесть путей в объявлении; механизм — Ф3 (`#1269`, §4.1 п.9, Р7); дом — `PRO-Robotech/kacho`, `gateway/` | дублёр службы — Ф3-51 форма; носители `A`, `B`, `C` — Ф3-01, Ф3-25, Ф3-15 |
| **Ф12-39** | неизменность (материал) + перепись гейта | **Ф12** (миграция, обёртка); гейты сдерживания — Ф2 `#1268` (Ф-б) | Ф12-02 под `K1` |
| **Ф12-40** | перепись гейта + отказ базы | **Ф12** (миграция, домен); гейт словаря — Ф11 `#1280` (Ф-а) | дерево после миграции; вставки — в пробе на настоящей базе |
| **Ф12-41** | проход / отказ 403 / 400 | **Ф12** (два вида в перечне); механизм — Ф3 Р12 | сессия Ф3-01; признаки — Ф3-35 |
| **Ф12-42** | запись (транзакция) | **Ф12** (четыре события); очередь и гейт — Ф-к | Ф12-02, 25, 28, 30 |
| **Ф12-43** | величины (клетки) | **Ф12**; форма — Ф-з | поднятый процесс |
| **Ф12-44** | запись (снятие) + перепись | **Ф12** (запись реестра); петля — Ф-и | три строки — посевом; часы Ф-ж |

```sh
# ДОМ: PRO-Robotech/kaname · единица — различная метка сценария в §5; и она же — в ведомости §6
F=docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md
awk '/^## §5 /,/^## §6 /' "$F" | grep -oE '^\*\*Ф12-[0-9]+ —' | sort -u | wc -l           # → 44
awk '/^## §6 /,/^### §6.1 /' "$F" | grep -oE '^\| \*\*Ф12-[0-9]+\*\*' | sort -u | wc -l   # → 44
# то же по латинскому написанию — перепись на одной форме недобирала бы молча
awk '/^## §5 /,/^## §6 /' "$F" | grep -cE '\*\*F12-[0-9]+'                              # → 0
```

### §6.1 Ведомость соответствия с Ф11 §5: восемь строк «в DoD Ф12» и три «последняя из Ф5 и Ф12»

Ф11 §5.2: «Ф12 — 02, 08, 25, 26, 28, 30, 32, 33; … позже из Ф5 и Ф12 — 07, 11, 29». Каждая
строка отвечена сценарием этого документа, который её **производит**; «Тогда» Ф11 не
переписываются — исполняются пробами под номерами Ф11 (Ф11 §5: «несёт номер Ф11, а не номер
своей фазы»).

| строка Ф11 | что требует (род) | производит здесь | чем строится «Дано» Ф11 | исполняется |
|---|---|---|---|---|
| **Ф11-02** | вход паролем и кодом (по времени либо запасным) → «2» | Ф12-11, Ф12-12 | фактор — Ф12-02; сессия — Ф3 | **DoD Ф12** |
| **Ф11-08** | церемония кодом: «2», следующий запрос проходит, прежний носитель — как несуществующая сессия, журнал `totp` | Ф12-15 | сессия «1» с заведённым фактором — Ф12-14 | **DoD Ф12** |
| **Ф11-25** | посадка `own`, вход паролем, глагол с полом «2» через край — отказ, вызов «1»/«2» | Ф12-20 (первая половина) | стенд `own` у службы и у края — Ф3-44/45; фактор — Ф12-02 | **DoD Ф12** — «не выполнилось» до посадки Ф3 у края |
| **Ф11-26** | через нашу церемонию предъявлен второй фактор, тот же запрос проходит | Ф12-20 (вторая половина) | Ф11-25 | **DoD Ф12** |
| **Ф11-28** | провязан второй фактор — требование стража пройдено, самоотчёт «2» | Ф12-34 | корень с проверяющим кода | **DoD Ф12** |
| **Ф11-30** | неверный код внутри сессии — отказ, уровень и носитель прежние, носитель годен | Ф12-16 | Ф12-14 | **DoD Ф12** |
| **Ф11-32** | предъявление освежает окно правки своих данных; заведение проходит; уровень «1» | Ф12-10 | Ф12-09 | **DoD Ф12** |
| **Ф11-33** | без предъявления — отказ по свежести; код и текст — приёмки действия | Ф12-09 (код и текст названы) | сессия Ф3-01, часы Ф-ж | **DoD Ф12** |
| **Ф11-07** | восстановление при заведённом факторе → «1» | Ф12 производит **фактор** в «Дано» (Ф12-02) и решение Р3 (фактор в восстановлении не требуется); значение «1» — Ф11 (Ф-а), выдача восстановлением — Ф5 | адрес — записью (Ф11 §3.0а); фактор — Ф12-02; восстановление — Ф5 `#1271` | **последняя из Ф5 и Ф12** |
| **Ф11-11** | в сессии восстановления код даёт «1» и ответ называет недостающее — пароль | Ф12-19 (а) — поля `assurance` | сессия восстановления — Ф5, до неё посевом записи; фактор — Ф12-02 | **последняя из Ф5 и Ф12** |
| **Ф11-29** | заданный пароль предъявлен, затем код → «2» | Ф12-19 (а, второе «И») | Ф11-11 | **последняя из Ф5 и Ф12** |

```sh
# ДОМ: PRO-Robotech/kaname · единица — номер сценария Ф11, названный Ф11 §5.2 фазе Ф12 и «последней из Ф5 и Ф12» — против строк таблицы выше
F11=docs/engineering/acceptance/assurance-level-is-declared-by-our-session.md
F=docs/engineering/acceptance/second-factor-totp-and-recovery-codes.md
grep -oE 'Ф12 — [0-9, ]+' "$F11" | head -1 | sed 's/.*— //' | grep -oE '[0-9]+' | wc -l                # → 8 (без sed «12» из «Ф12» считалось бы девятым)
grep -oE 'позже из Ф5 и Ф12 — [0-9, ]+' "$F11" | sed 's/.*— //' | grep -oE '[0-9]+' | wc -l          # → 3
awk '/^### §6.1 /,/^### §6.2 /' "$F" | grep -oE '^\| \*\*Ф11-[0-9]+\*\*' | sort -u | wc -l              # → 11
```

### §6.2 Строки соседей, называющие Ф12, — и что их производит здесь

| сосед | строка | здесь |
|---|---|---|
| Ф3 §4.2 | «второй фактор, повышение, церемония → `#1281`» | Р4, Р5; Ф12-11…20 |
| Ф3-09 | «момент последнего предъявления читается окном свежести правки своих данных у владельцев действий (Ф11 §5.1 п.6 — Ф12, Ф7)» | Р8; Ф12-09/10 |
| Ф3-44, Ф3 Р15 | «уровень «1» предъявим паролем; «2» — Ф12» | Ф12-34 |
| Ф3 Р10, Р12, Р14, Р2/Р7 | счёт, виды формы, события, ретрансляция — как механизмы | Ф12-31, 41, 42, 38 — экземпляры в тех же объявлениях |
| Ф11 §5.2 | 8 + 3 строки | §6.1 |
| Ф11 §6 | три строки с условием возврата «Ф12» | Р8 (Д11), Р9 (Д12), Р3 (Д13) |
| Ф11 §1.7, §6 | `mfa_fresh` — не Ф12 | §0.4, §4.2 (Д14) |
| Ф1 §6; Ф1 §5.1 карта F4d-14/17/18/51 | второй фактор — Ф12 | Д1, Д3, Д4, Д5 |
| Ф5 Д12, §4.2, §12 п.7; Ф4 Д11 | требование фактора в восстановлении — Ф12 | Р3, Д13: **нет**; строка Ф5 остаётся с ответом |
| ID-PW-1 §4.2 | `#1281` — иных видов способа Given не заводит | Д23 |
| Ф2 П1 (шапка миграции) | «состояние — kacho#1281» | Р1 (Д26) |
| F4d Р4, §9.1 п.6, F4d-14/15/17/18/51/20 | способы, снятие, сброс | Д1…Д6 |
| IAM-INT-1 Р5, -21, -22 | без браузера; ступень открывает набор | Д9, Д10; Ф12-08, 20 |

---

## §7 Инварианты (MUST hold — для ревьюера)

1. **Каждое «Тогда» описывает наблюдаемое**, а не устройство: ни один сценарий §5 не называет
   таблицу, колонку, функцию или библиотеку продукта как требование; формы §1.2 — координаты
   **существующего**. Слова «CAS» и «одним оператором» в сценариях — требование к **исходу под
   конкуренцией** (ровно один проходит при любом порядке — либо, где так решено, оба), а не к
   тексту запроса; **код состояния SQL (23505, 23514) — устройство**, и в «Тогда» §5 он не
   стоит ни разу: предикат — `grep -c '23505\|23514'` по §5 → 0. Первая редакция это
   нарушала в Ф12-05 (а) и Ф12-40 — и в Ф12-05 нарушение было не стилем, а ошибкой: под ключом
   «человек, вид» без состояния (Ф2 П1) вставка при живой строке `pending` давала бы то же
   нарушение уникальности, что при `active`, и три «Тогда» сценария одним механизмом не
   строились.
2. **Каждое отрицание стоит в паре с положительным близнецом, и близнец назван для КАЖДОЙ
   полосы §5** (единица — сценарий полосы, чьё «Тогда» несёт отказ либо негодность); где пара
   одно-фактная, назван отличающий факт:

   | полоса | отрицание | близнец | отличающий факт |
   |---|---|---|---|
   | заведение | Ф12-03, 04 (а, б, в), 05 (а), 06, 07 (б) | **Ф12-01, 02**, 05 (б, в) | код · ожидающее заведение есть · `active` есть · поле формы |
   | заведение, гонка | Ф12-07 (второй) | Ф12-07 (первый; разные личности) | одновременность |
   | свежесть | Ф12-09 | **Ф12-10** | предъявление внутри сессии |
   | вход | Ф12-13 (а…ж), 14 («2» не выдан) | **Ф12-11, 12** | код · пароль · фактор заведён · поле `secondFactor` |
   | церемония | Ф12-16, 17, 19 (а, в) | **Ф12-15, 18** | код · заведён ли · множество сессии |
   | край | Ф12-20 (первая половина) | Ф12-20 (вторая) | предъявлен второй фактор |
   | окно и повтор | Ф12-21 (`t±2`, убывающий порядок), 22 (а, в) | Ф12-21 (`t−1…t+1`), 22 (б) | шаг · порядок · принятый шаг · сессия |
   | запасные коды | Ф12-23 (второй раз), 24 (а — второй), 25 (старые, в), 26 (одиннадцатый), 27 | Ф12-23 (первый, №7), 24 (б — оба), 25 (новые), 26 (код по времени), 27 (ответы `enroll`/`confirm`) | потреблён · тот же код против разных · перечеканен · фактор заведён · остаток 0 · момент чеканки |
   | снятие и сброс | Ф12-29 (а…д), 30 (`D'`, `D''`, повтор, `V`) | **Ф12-28, 30 (`D`)** | код · аккаунт · пол · фактор есть (не `pending`) |
   | подбор | Ф12-31 (а…в) | Ф12-31 (г, д), **Ф12-32** | предел · окно · род отказа |
   | время | Ф12-33 — измерительный; отрицание — контроль внутри | — | сверка на неверном пароле |
   | посадка | Ф12-35 (а, б, `K2` без `K1`), 36, 37 | Ф12-34, 35 (`K2,K1`), 36 (`15m`), 37 (`own`) | ручка · перечень ключей · посадка |
   | край | Ф12-38 (`B`, недоступность) | Ф12-38 (`A`, `C`); выход при дублёре — Ф3-17 | отсечка · глагол |
   | хранилище, словарь | Ф12-39 (без `K1`), 40 (три вставки) | Ф12-39 (с `K1`), 40 (две вставки) | ключ · значение вне словаря |
   | формы | Ф12-41 (чужой вид, без признака) | Ф12-41 (пять своих) | вид |
   | журнал | Ф12-42 (отказ очереди) | Ф12-42 (четыре события) | подставной отказ |

3. **Ни один отказ не является оракулом** — по телу и по времени: семь отказов входа Ф12-13
   (а…ж) побайтово равны отказу на неверный пароль; «не подошёл», «повторён», «потреблён» — один
   текст (Ф12-16, 22, 23); отказы формы не различают чужой вид и чужой контекст (Ф3-36);
   Ф12-33 — критерий Ф1-48, **включая полосу «материала нет»**: личность без фактора и со
   строкой `pending` отвечает на «неверный пароль + код» тем же временем, что личность с
   фактором (Р5, Р6 — холостая сверка); (е) Ф12-13 — тем же телом и той же попыткой, что
   неверный код: различимости отказов входа **наружу нет ни одной** (редакция 7, `kaname#257`;
   редакции 4…6 называли (е) «единственной обоснованной» — обоснование держало держателя пароля
   и не держало постороннего). Различимость по состоянию — только у глаголов под сессией и у
   сброса, где вызывающий уже аутентифицирован.
4. **Недоступность — не «да» и не «попытка»** (Ф12-35): материал, который не открывается, даёт
   503 фиксированным текстом, счётчик и журнал; `401` не производится, счёт попыток не растёт.
5. **Транзакций в документе ПЯТЬ, и у каждой назван частичный исход, которого не бывает:**
   подтверждение (Ф12-07: строка `active` + набор + журнал), снятие (Ф12-28: две строки +
   прочие сессии + событие), перечеканка (Ф12-25: новый набор + событие; Н3 круга 1 — первая
   редакция её не считала), сброс (Ф12-30: строки + отсечка + событие), событие аудита с
   изменением (Ф12-42 — частичный исход всех четырёх с событием: при отказе записи очереди
   изменение не зафиксировано).
6. **Ни одна величина не подставляется молча** (Ф1 §7 инв. 4): перечень ключей обёртки, окно
   свежести — ручки без умолчания, отказ старта с именем; четыре величины частоты — Ф3;
   умолчания в коде нет ни у одной. Величины-контракты (шесть цифр, 30 с, ±1 шаг, десять кодов
   по десять знаков, 20 байт секрета) ручками **не являются** и названы решением (Р5, Р6).
7. **Объявление одно у каждого предмета:** словарь способов и правило (Ф-а — Ф12 не заводит
   второго имени), словарь видов строки (схема = домен, Ф12-40), перечень видов формы (Ф12-41),
   перечень путей ретрансляции и `isPublicHTTPPath` (Ф12-38), перечень ключей обёртки (одна
   ручка), окно свежести (одна ручка, два читателя названы — Р8). Второе объявление — находка
   гейта.
8. **Уровень — только правилом Ф-а**: ни один глагол этой фазы не пишет уровень иначе, чем
   вызовом правила на множестве предъявленного (гейт `TestAssuranceLevelHasOneWriterAndOneHome`);
   в строке способа уровня нет (Ф11 Р1, Ф12-40).
9. **Предъявление кода — предъявление в смысле Ф11**: всякое успешное (вход с кодом — выдача;
   церемония, подтверждение, снятие, перечеканка — внутри сессии) кладёт способ в множество,
   перевыпускает носитель (кроме выдачи — там носитель первый) и двигает момент последнего
   предъявления; момент аутентификации не двигает ничто (Ф12-02, 15, 28).
10. **Секрет и коды не покидают хранилище иначе, чем ответом, их отчеканившим** (Ф12-27,
    39): материал `totp` обёрнут, `lookup_secret` — невосстановим **формой** (медленный хеш под
    потолком ID-PW-1, Р6), а не обещанием; гейты сдерживания Ф-б судят оба вида тем же типом.
11. **Механизм личности на поверхности — ОДИН, носитель** (Ф3 §8 инв. 12, Ф12-38): пространство
    `x-kacho-` не ретранслируется, чужие заголовки принципала исхода не меняют.
12. **Ни одна фаза не разрешена вперёд** (§0.2): сценариев Ф7, Ф8, Ф10, Ф13 и политики по
    личности здесь нет; §4.2 называет каждую с носителем либо без него вслух.
13. **С одобренными соседями нет ни одного расхождения без исхода** (§3): интерпретаций — две
    (Д1, Д2), отступление — одно (Д5), все с доводом и ценой; «не предмет, назван» — четыре.
14. **Один факт — один код и один `reason`, где бы факт ни встречался** (ban #18: «два ресурса
    решают одну задачу по-разному» — находка): «фактор не заведён» — `FAILED_PRECONDITION` /
    `SECOND_FACTOR_NOT_ENROLLED` в церемонии, снятии, перечеканке и сбросе (Ф12-17, 29
    «г»/«д», 25 «в», 30) — везде, где вызывающий уже аутентифицирован; **на входе факт наружу
    не выходит** (Ф12-13 «е»: `401`, редакция 7, `kaname#257`) — не второй код у одного факта,
    а решение не сообщать факт тому, кто не доказал права его знать (инв. 3); «ожидающего
    заведения нет» — `ENROLLMENT_NOT_PENDING` на
    истёкшем, снятом уборкой и не начатом (Ф12-04); «сессия несвежа» — `SESSION_NOT_FRESH`
    (Ф12-09); «уже заведён» — `SECOND_FACTOR_ALREADY_ENROLLED` у `enroll` и у `confirm`,
    последовательного и проигравшего гонку (Ф12-05, Ф12-07). **Перечень мест каждого факта
    выводится матрицей Р4**, а не выписывается (круг 1, Б1: выписанный перечень «четыре»
    пропускал перечеканку, и `grep -c` этого не видел by construction). Предикат — по матрице,
    а не по счёту вхождений: каждая ячейка с отказом называет сценарий, и названный сценарий
    несёт токен ячейки — `SECOND_FACTOR_NOT_ENROLLED` в Ф12-17, 25, 29, 30 (четыре глагола;
    ячейка входа несёт отказ входа, Ф12-13 «е»),
    `ENROLLMENT_NOT_PENDING` в Ф12-04, `SECOND_FACTOR_ALREADY_ENROLLED` в Ф12-05, 07;
    `grep -c 'ENROLLMENT_EXPIRED'` по §5 → 0 (снятый токен не пережил своей замены); ни одно
    «Тогда» о состоянии строки не несёт `INVALID_ARGUMENT` и не зависит от расписания уборки.

---

## §8 Чем каждая группа краснеет ДО кода (строгий TDD, ban #12)

Здесь — **вид** доказательства, а не код. Красное до кода обязано быть **честным**
(`change-graph.md` §5): испытуемого нет либо объявленной возможности в нём нет — красный;
не поднятый стенд, не влитая Ф3, сломанная фикстура — «не выполнилось».

| группа | уровень | чем краснеет до кода |
|---|---|---|
| Ф12-01…08 заведение | сквозной **на собственном слушателе полосы службы**, без края и без браузера (дом пробы — репозиторий предмета, `e2e-flow.md` §7а; форма — наборы `tests/newman/` этого дома, клиент предъявляет сертификат края — Ф3 Р16) + интеграционный на настоящей базе (строка `pending`/`active`, набор, гонки Ф12-05 и Ф12-07 двумя транзакциями; матрица Р4 — `confirm` без строки и при `active`, Ф12-04 в / Ф12-07 б) | адреса нет — `404`, проба отвечает «не выполнилось», а не «прошло»; словарь вида не знает `totp` — вставка отвергнута базой (23514): красное Ф12-01 уровня I; проигравший гонку получает `401` вместо `409` — Ф12-07 (а) красный |
| Ф12-09, 10 свежесть | интеграционный на действии `enroll`, часы пробы управляемые; оба момента строятся согласованно (Ф11 §3.0а) | ручки окна нет — страж не отказывает (Ф12-36 красный); окна нет — `enroll` проходит на несвежей сессии: Ф12-09 красный |
| Ф12-11…14 вход с кодом | сквозной на слушателе службы; Ф12-13 — **все семь** ответов побайтово (форма Ф3-02), (е) — с положительным близнецом (та же личность без поля → «1») и счётом попытки; Ф12-11 уровня I читает запись: множество и уровень на исходном носителе; Ф12-13 (б), (ж) — интеграционный на строку после неполного успеха | поля `secondFactor` нет — 400 «лишнее поле» (Ф3-05): Ф12-11 красный; на реализации, глотающей поле, — «1» вместо «2», тоже красный; реализация, записывающая шаг либо потребляющая код на неверном пароле, — красная на положительном контроле Ф12-13 (Б3 круга 1); реализация, отдающая на (е) `400` с токеном состояния либо не считающая (е) попыткой, — красная на Ф12-13 (е) (редакция 7) |
| Ф12-15…19 церемония | сквозной + интеграционный (носитель до и после, журнал повышения, поля `assurance`); Ф12-19 (а) — «Дано» посевом записи сессии с `{recovery_code}` | адреса церемонии нет; на церемонии только с ветвью пароля (Ф11 DoD) — `method: "totp"` даёт 400: Ф12-15 красный |
| Ф12-20 край, посадка `own` | сквозной **через край** на стенде `own` (`tests/newman/` дома платформы — связка, `e2e-flow.md` §7а) | стенд не поднят — «не выполнилось»; поднят без ретрансляции шести путей — `404` на `step-up` через край: красный |
| Ф12-21, 22 окно и повтор | интеграционный, часы пробы; коды вычисляет проба (RFC 6238 из stdlib пробы, не из прод-кода — иначе проба и продукт делили бы одну ошибку); Ф12-22 — две транзакции и вторая сессия; часы пробы **сдвинуты после подтверждения** (§5 преамбула: Ф12-21 — на три ступени) | окна нет — `t−1` красный либо `t+2` зелёный; шага нет — повтор проходит: красный; шаг в записи сессии — Ф12-22 (в) проходит из `S2`: красный; **часы не сдвинуты после подтверждения — Ф12-21 красна на `t−1` и `t` ЛОЖНО** (отрицательный контроль самой пробы, Б2 круга 1: красное здесь — дефект пробы, не продукта) |
| Ф12-23…27 запасные коды | интеграционный (материал до/после, гонка Ф12-24 — обе половины, перезапуск процесса Ф12-23) + сквозной (ответы, остаток) | набора нет; на реализации «пометить в памяти» — Ф12-23 после перезапуска зелёный на повторе: красный; на CAS по материалу без повтора — Ф12-24 (б) даёт `401` годному коду: красный; набор быстрым хешем либо ниже пола записи объявленного формата — Ф12-39 красный; набор ровно на полу записи — Ф12-39 зелёный (законный близнец, Б4 круга 1) |
| Ф12-28, 29 снятие | сквозной + интеграционный на транзакцию с подставным отказом каждой из трёх записей (форма Ф3-16) | глагола нет; на реализации без снятия прочих сессий — `S2` жива: красный |
| Ф12-30 сброс | интеграционный на границе службы (RPC, отсечка, событие) + проба края с дублёром (маршрут, 404 байт-идентичный, 401 по полу); уровень P — гейт паритета каталога и перепись «записей с полом «2» — 28» | RPC не объявлен — `buf` красный; записи каталога нет — `catalog: no entry` fail-closed |
| Ф12-31…33 подбор и время | сквозной + декларативный по профилю (Ф3-28 форма); Ф12-33 — измерительный (Ф1 §8, строка Ф1-48…51) на трёх личностях | счёт не знает кода — `N` неверных кодов не дают 429: красный; сверка не выполняется на неверном пароле — пара (2)/(3) различима: красный; холостой полосы нет — пара (2)/(7) различима: красный |
| Ф12-34…37 посадка и ручки | проба корня композиции (форма Ф3-44, `TestF11_28_OwnLaneWithPasswordAndSecondFactorPassesTheFloorRow` — сегодня на подставленном перечне) + декларативный по объявлению профиля (Ф-д) + интеграционный на обёртку (два перечня ключей) | корень отдаёт `nil` — самоотчёт «1»: Ф12-34 красный на **наблюдении** корня; ручки нет — старт без неё: красный; `K2` без `K1` даёт 401 либо 500 вместо 503: красный |
| Ф12-38 край | пробы края с дублёром службы, считающим ретрансляции и читающим заголовки (Ф3-51 форма); объявление путей — перепись | ретрансляции нет — «не выполнилось»; путь вне объявления Ф3 — второе объявление: красный гейта Ф3 §8 инв. 7 |
| Ф12-39, 40 хранилище и словарь | интеграционный на настоящей базе (материал ≠ секрет; четыре вставки отвергнуты тремя ограничениями схемы; `TestLoginVerifierStaysInsideTheSchema` на трёх видах при расширенном объявленном наборе) + гейты `TestAssuranceMethodVocabularyIsDeclaredOnce`, `internal/check/login_verifier_containment.go` (три разрешённых файла) с инъекцией в обе стороны **заново** | миграции нет — вставка `totp` отвергнута; открытый секрет в строке — красный интеграционной; второй контейнер — красный гейта; **миграция без правки объявленного набора — красный гейта на своём же ограничении** (и это не помеха, а его предмет); проверяющий кода вне разрешённых файлов — красный статического гейта |
| Ф12-41 формы | сквозной на слушателе службы | вида `second-factor` нет — `GET csrf?form=second-factor` 400: красный |
| Ф12-42…44 журнал, счётчики, уборка | интеграционный на настоящей базе; `TestAuditPayloadCarriesNoPersonalData` на новых событиях; часы пробы | событий нет; клеток нет; записи реестра нет — `pending` живёт вечно: Ф12-44 красный |

**Дом пробы — репозиторий её предмета** (`e2e-flow.md` §7а): сценарии службы гоняются на её
собственном слушателе; сценарии края (Ф12-20, 38, половина Ф12-30) — в `PRO-Robotech/kacho`
против дублёра службы; связка «консоль → край → служба» — после Ф8. **Браузерная проба
заведения и церемонии — DoD Ф8**, не этой фазы.

**Инъекция в обе стороны обязательна для каждого гейта** (`testing.md` §«Гейт на класс»):
внесённый дефект краснеет и называет координату; законный близнец молчит; перепись печатает
объём осмотренного; пустой обход — отказ. Для гейтов, которые Ф12 **расширяет** (сдерживание
материала, словарь), инъекция прогоняется **заново** тем же дефектом (`testing.md` §«Гейт на
класс» п. 8): совпадение переписи способности падать не доказывает.

**Три исхода, а не два.** Прогон без условия (нет базы, нет стенда, нет Ф3 в дереве) — «не
выполнилось»: не вычитается из вердикта и не зачитывается в успех.

---

## §9 Порядок работ после APPROVED и DoD

1. **Ф3 — первой** (§4.3): без сессии, носителя, признака формы и счёта частоты «Дано» этой
   фазы не строится; кодировать Ф12 **после** вливания кода Ф3. До того исполнимы только
   миграция, обёртка, проверяющий кода как пакет и гейты (Ф12-39, 40) — их можно вести
   параллельно, не сдавая.
2. **Церемония `step-up` — один глагол**: ветвь пароля — DoD Ф11 (Ф11-09, 13, 31, после Ф3), ветви
   кода — DoD Ф12. Кто кодирует первым, заводит глагол формой Р4; второй добавляет ветви.
   Второго адреса не заводится.
3. **Служба**, одной пачкой в порядке: миграция (словарь вида, состояние, последний шаг) →
   обёртка и ручка → проверяющий кода и набора → `enroll`/`confirm` → поле `secondFactor` входа
   → ветви церемонии → `remove`/`backup-codes`/статус → `ResetSecondFactor` (контракт, каталог,
   регенерация) → счёт, свежесть, стражи → события, клетки, реестр уборки — **падающие пробы до
   кода** по §8.
4. **Край** (`PRO-Robotech/kacho`, `gateway/`): шесть путей в объявлении ретрансляции и в
   `isPublicHTTPPath`, ветка недоступности/отсечки — как у смены пароля; маршрут
   `ResetSecondFactor`; копия каталога.
5. **Ф11**: после вливания Ф12 её восемь сценариев исполняются в DoD `#1280` под номерами
   Ф11; три «последней из» — когда влиты и Ф5, и Ф12.
6. **Ф8** переводит консоль: заведение (QR из `otpauthUri`), коды, окно повышения на нашу
   церемонию, предупреждение об исчерпании, перечень `step-up-methods.ts` под `own`; и только
   тогда — браузерная проба.

**DoD фазы Ф12:**

- вердикт существует **записью по отпечатку** (форма Ф1 §9 п.1) и внешним событием в
  `kacho#1281`;
- каждый из **44** сценариев исполнен по §8 — числом «исполнено N из 44», третья категория
  названа отдельно (стенд `own` у края для Ф12-20);
- **восемь сценариев Ф11** исполнены пробами **Ф11** под их номерами — как написаны, без правки
  «Тогда» (§6.1); три «последней из Ф5 и Ф12» — если Ф12 влита последней, иначе названы
  ждущими Ф5;
- гейты, которые Ф12 расширяет, прогнаны с инъекцией заново (§8); `TestLoginVerifierStaysInsideTheSchema`
  зелен на трёх видах **при расширенном объявленном наборе** (перепись называет N объявленных, чужих 0
  — §4.1 п.1), `TestAssuranceMethodVocabularyIsDeclaredOnce` — на трёх видах; статический гейт
  сдерживания — с третьим разрешённым файлом и его причиной;
- предикаты дерева — и у каждого названо «сегодня»:

  ```sh
  # ДОМ: PRO-Robotech/kaname
  git grep -c "ARRAY\['password'::text\]" -- internal/migrations/20260915111233_login_methods_live_in_their_own_rows.sql   # сегодня 1 → 1 (применённая не правится; словарь расширяет НОВАЯ миграция)
  git grep -n -iA1 'func.*signInMethods' -- cmd/kaname/                                              # сегодня `wiredSignInMethods` → `return nil`; на голове линии `loginLane.signInMethods()` → `password` (§12.1) → перечень из провязанного, с `totp`, `lookup_secret`
  git grep -n 'hmac' -- '*.go' ':!*_test.go' | wc -l                                                 # сегодня 0 → ≥ 1 (проверяющий кода)
  # объявленный набор ограничений гейта сдерживания — единица: имя в перечне
  awk '/^var lmDeclaredConstraints = \[\]string\{/,/^\}/' internal/repo/kaname/pg/login_verifier_stays_inside_integration_test.go | grep -c '"user_login_methods_'   # сегодня 4 → 4 + число ограничений миграции Ф12
  # разрешённые файлы статического гейта — единица: путь в перечне
  awk '/AllowedFiles: map\[string\]string\{/,/^\t\t\},/' internal/check/login_verifier_containment_test.go | grep -cE '^\s*"internal/'   # сегодня 2 → 3 (проверяющий кода)
  python3 -c 'import json; d=json.load(open("internal/apps/kaname/seed/embedded/permission_catalog.json")); print(sum(1 for e in d if e.get("required_acr_min")=="2"))'   # сегодня 27 → 28
  ```

  Ноль справа без парного замера слева означал бы «перестали находить», а не «сняли»;
- контракт: новый RPC на публичном сервисе, `buf lint` / `buf breaking` зелёные, каталог
  разрешений перегенерирован в обе копии, `proto-api-reviewer`;
- миграция — новая, `db-architect-reviewer`; ограничения базы держат словарь вида и состояние
  (Ф12-40);
- корень композиции: `wiredSignInMethods` (на линии — `loginLane.signInMethods()`) — наблюдение;
  самоотчёт `lane_presentable_acrs=2` (Ф12-34);
- **два комментария правятся тем же изменением, что заводит их второй предмет** (Н2 круга 1;
  `architecture.md` §doc-truthfulness): шапка `internal/keywrap/keywrap.go` (§«Чей это ключ» —
  «второй ручки об этом предмете не заводится») называет **оба** предмета обёртки и оба перечня
  ключей и то, что предметы разные (Р2), — вместе с ручкой `authn.second-factor-encryption-key-hex`;
  комментарий у `identity_suspender` в `internal/authzmodel/fga_model.fga` называет **трёх**
  читателей — запрет, его снятие, сброс второго фактора (Р10) — вместе с записью каталога
  `ResetSecondFactor`;
- события, клетки, запись реестра уборки — в том же изменении, что миграция;
- записка задачи в хранилище записок воркспейса; `#1281` закрыта комментарием с коммитами
  обоих домов и выводом предикатов выше.

---

## §10 Границы текста этого документа

Оба репозитория **публичны** (`security-disclosure.md`). Здесь названы предмет, решения и
следствия для контракта; сборки «координата + условие обхода + следствие» нет ни в одном
месте: параметры кода (Р5) — стандарт, который печатает каждое приложение-аутентификатор;
окно ±1 шаг и правило повтора — публичная норма RFC 6238; тексты отказов названы потому, что
они контракт, и каждый выбран так, чтобы не раскрывать ничего (§7 инв. 3). Различимости отказов
входа документ не называет ни одной (редакция 7: прежняя единственная — Ф12-13 «е» — снята как
оракул совпавшего пароля, `kaname#257`); различимые отказы состояния названы только там, где
вызывающий уже аутентифицирован.

---

## §11 Что осталось решением, а не установленным фактом — и находки по дороге

**Решения, ратифицируемые вместе с APPROVED** (каждое — с отвергнутым исходом в §2): Р1
(состояние строки; `pending` — не способ), Р2 (свой перечень ключей), Р3 (обязательность —
действием; восстановление фактора не требует), Р4 (адрес церемонии и семейство; поле
`secondFactor`; способ называет клиент; ветка недоступности как у смены пароля; **матрица
«глагол × состояние строки» и порядок проверок «свежесть → состояние → срок → код»**; на входе
ячейка состояния — отказ входа, не токен (редакция 7, `kaname#257`); один токен
`ENROLLMENT_NOT_PENDING` на истёкшее, снятое уборкой и не начатое заведение; `pending` под
`remove` и сбросом не трогается), Р5 (параметры, окно ±1, повтор; **запись шага — только при
полном успехе**, сверка на неверном пароле холостая по исходу), Р6 (десять по десять; материал
набора — медленный хеш дисциплины Ф-в под ёмкостью пароля, соль на набор; **формат и параметры —
настройка «что писать» ID-PW-1 PWV-16, потолок чтения — запись перечня**; потребление —
только при полном успехе; однократность одного и годность разных — сериализацией на строке
набора; остаток числом; запасной код ≠ код восстановления), Р7
(один счёт с паролем; «не заведён» на входе после совпавшего пароля — попытка, редакция 7), Р8 (окно и срок `pending` — одна ручка), Р9 (последний снять можно;
прочие сессии гаснут, текущая нет — отступление Д5), Р10 (сброс распорядителем — новый RPC,
пол «2»).

**Числа, которые здесь НЕ перемерялись, и это сказано:** `totp = 0`, `lookup_secret = 0` в
живой базе 2026-08-25 (тело `#1281`; кластер чужой); 80 перенесённых личностей (тело `#1266`).

**Находки по дороге — задачами, в чужой предмет здесь не входят:**

| находка | где | задача |
|---|---|---|
| посылка задания «Ф12 добавляет окно свежести `mfa_fresh`» противоречит одобренной Ф11 §1.7/§6 — не дефект дерева, а расхождение задания с одобренным соседом; закрыто здесь (§0.4, Д14) | задание | правится в задании; предмет условия — `kaname#133` |
| гейт достижимости пола на стороне посадки читает **настройку поставщика** как источник «какие способы включены»; на посадке `own` источник — провязка корня (Ф12-34), и посылка гейта истекает с Ф10 | `PRO-Robotech/kacho:deploy/identity_second_factor_reachable_test.go` | `kacho#2691`; исполняется вместе с Ф10 `#1276` либо Ф8 `#1274` |
| перечень способов церемонии консоли — второе объявление подмножества словаря Ф11 Р8 (Ф11 §10 п.20, Н54); после Ф12 на `own` его читатель — наша церемония | `PRO-Robotech/kacho:ui-future/shared/src/lib/step-up-methods.ts` | Ф8 `#1274` — правка тем же изменением, что экран |
| одобренная F4d-51 расходится сама с собой: «после снятия последнего оставшегося … сессия «2» не выдаётся» при том же сценарии, где снятие последнего отвергается | воркспейс, F4d | `PRO-Robotech/kacho-workspace#660` — правка у владельца F4d; здесь строка заменена (Д5) |
| у распорядителя нет глагола сброса **пароля** человека (вторая половина F4d-20) — сирота Ф1 §5.1 | контракты службы | по предмету Ф5 `#1271`; здесь не заводится |
| тело `#1281` называет консоль с «единственным вхождением в комментарии» — прод-файлов три | тело задачи | правится комментарием при закрытии; предмета не меняет |

---

## §12 Круг 1 ревью: четыре блокирующих и восемь важных — исход по каждому, посылка перемерена

Запись круга 1 —
`docs/specs/reviews/second-factor-totp-and-recovery-codes/08b6e9f7adfc32cd859c1aff6f5c211c708ca84e0e857f915173d346b9b490d3.yaml`,
вердикт `CHANGES_REQUESTED` об отпечатке редакции 4 (`436d13b6`). Шапка не правлена: слово в ней
— статическая форма, вердикт о редакции 5 — соседняя запись по новому отпечатку
(`change-graph.md` §2). Посылки записи перемерены здесь **до** правки, в обе стороны: kaname
`origin/main` = `4f6ab4be` (ревизия измерения шапки — дрейф 0); kacho `origin/main` =
`a2981976c6` — тот же один коммит сверх объявленной `898754db1c`, что видел круг (Н8; три
предиката §1.1 дома платформы дают на нём объявленное 4 / 0 / 3 — замер круга, здесь не
повторялся: `deploy/` и `gateway/` того коммита второго фактора не касаются); воркспейс
`origin/main` = `3b43964b1a` (дрейф 0). ID-PW-1 на `4f6ab4be` прочитана построчно: Р2 (:412 —
потолок и пол **у записи перечня**, пол только у записываемой), PWV-16 (:1236 — «ровно на поле
… и ровно на потолке» проходит), `internal/passwordverify/declared.go` (`Declared.Validate` —
пол, потолок, только записываемый формат). Каждая из четырёх посылок круга **подтвердилась**;
ни одна не опровергнута. Чего круг не видел и что нашлось по дороге: у Ф12-22 два одновременных
кода ступени `t+2` при часах на `t` отвергались бы **окном**, а не арбитром (сдвиг часов назван);
у Р10 и Ф12-30 «`pending` снята» называла третью строку, которой при ключе «человек, вид» не
бывает (состояние — столбец одной строки).

| # | род (по записи круга) | исход | где |
|---|---|---|---|
| **Б1** | матрица «глагол × состояние строки `totp`» неполна: у `confirm` без `pending` и при `active`, у `backup-codes` без фактора, у `remove` при `pending` и у `ResetSecondFactor` при `pending`-only «Тогда» нет; перечень «четыре глагола» выписан и неполон | **матрица в Р4** — единое место, 8 глаголов × 3 состояния, у каждой ячейки код, `reason` и сценарий; три токена о состоянии, каждый — одно предусловие: `ENROLLMENT_NOT_PENDING` (`confirm` без ожидающего заведения: не начатое, истёкшее, снятое уборкой — **один** токен, потому что наружу они неразличимы, и ответ не вправе зависеть от расписания уборки — прежний `ENROLLMENT_EXPIRED` снят), `SECOND_FACTOR_ALREADY_ENROLLED` (`enroll` и `confirm` при `active` — и проигравший гонку Ф12-07 получает его же, а не `401`: один факт получал два кода в зависимости от чередования), `SECOND_FACTOR_NOT_ENROLLED` (пять глаголов — `backup-codes` добавлен; строка `pending` под `remove` и сбросом **не трогается**, второго снимающего у неё нет). Порядок проверок назван: свежесть → состояние → срок → код; отказ по состоянию код не сверяет и попыткой не считается. Перечень мест в §7 инв. 14 **выводится матрицей** | Р4 (матрица, порядок проверок), Р7, Р8, Р10, Ф12-04 (а, б, в), Ф12-07 (а, б), Ф12-25 (в), Ф12-29 (г, д), Ф12-30 (`V`), Ф12-32, §6, §7 инв. 2 и 14, §8, §11 |
| **Б2** | «Дано» с кодом ступени `t` после Ф12-02 не строится при неподвижных часах: подтверждение записывает шаг, и код той же ступени отвергается повтором; Ф12-21 утверждала «шага у строки ещё нет», Ф12-08 велела идти цепочкой без паузы | **§5 преамбула**: после каждого принятого кода часы пробы сдвинуты не меньше чем на ступень, `t` — ступень **после** сдвига, `t > t₀` (шаг подтверждения); Ф12-02 называет записанный `t₀`; Ф12-11, 15, 25 — `t > t₀`; **Ф12-21** — `t ≥ t₀ + 3`, чтобы и `t−2` был старше принятого и отказ на нём был окном, «шага ещё нет» снято, добавлен отрицательный контроль **самой пробы** (часы не сдвинуты — `t−1`, `t` красны ложно); **Ф12-08** — посев предъявляет ступень `t₀ + 1` (в окне, старше принятого), не ждёт границы; **Ф12-22** — часы сдвинуты на ступень перед двумя одновременными `t+2` (иначе отказ окном, не арбитром); §8 строка «окно и повтор» — отрицательный контроль пробы; Р5 — фраза о ступени старше принятого | §5 преамбула, Р5, Ф12-02, 08, 11, 15, 21, 22, 25, §8 |
| **Б3** | побочное действие НЕУДАВШЕГОСЯ предъявления на строку не названо; три предложения Р5/Р6, сложенные буквально, велели записать шаг и потребить код при неверном пароле — держатель списка кодов без пароля потреблял бы их все | **решение: состояние строки меняет только полный успех** — на входе сошлись и пароль, и код; внутри сессии код сошёлся и транзакция глагола зафиксирована. Сверка на неверном пароле идёт **тем же путём** (тот же HMAC либо хеш, тот же замок, то же сравнение — ради времени, Ф12-33) и **не записывает**: шаг прежний, код не потреблён. «Тем же оператором» — про успешный путь; запись условная («старше принятого») остаётся арбитром гонки. Порядок «сверка кода — только после пароля» отвергнут: он дал бы оракул пароля по времени (Ф12-33 классы 2/3), который Р5 закрывает намеренно; выбран порядок «сверить всё, записать только при полном успехе» — и это названо решением с доводом. Ф12-13 — «И» о строке после (б), новый подпункт (ж) «неверный пароль + годный запасной код → 401, код не потреблён, остаток прежний», оба с положительным контролем (тот же код затем с верным паролем проходит) и отрицательным (реализация, записывающая на неверном пароле, краснеет) | Р5, Р6, Ф12-13 (б, ж), §6, §8 строка «вход с кодом» |
| **Б4** | Ф12-39 сверяла параметры набора с «ручкой потолка» ID-PW-1 Р2; одобренная PWV-16 чеканит между полом и потолком записи по настройке «что писать»; производитель формата не назван | **решение: набор чеканится тем же хешером, что пароль, по той же настройке «что писать»** (PWV-16 — формат и параметры между полом и потолком записи, обе границы включены); читается по признаку значения (Р1, PWV-07); потолок при чтении — **запись перечня** (Р2, `record.Ceiling`), не ручка; второй настройки «как дорого хешировать набор» не заводится. Порог Ф12-39 — **не ниже пола записи объявленного формата** и не выше потолка; отрицательный контроль — ниже пола либо формат вне перечня; **законный близнец** — набор ровно на полу, молчит. Строка ID-PW-1 Р1/Р2/PWV-16 добавлена в §0.1 и в §3 (Д28, сумма 27 → 28) | Р6, §0.1, §3 Д28, §4.1 п.3, Ф12-39, §8, §11 |
| Н1 | шесть новых путей без записи каталога проходят под требованием сменить пароль (Ф3 Р8), следствие не названо | названо в Р4 («под требованием сменить пароль» — проходят, как смена пароля; таблица Ф3 Р8 иллюстрирует перечень на день Ф3, перечень выводится) и в Д19; Ф12-38 получил носитель `R` (сессия восстановления с требованием) — ретранслировано 6, судит служба | Р4, §3 Д19, Ф12-38 |
| Н2 | шапка `keywrap` после Ф12 лжёт о своём предмете; комментарий `identity_suspender` не называет третьего читателя | §9 DoD: оба комментария правятся **тем же изменением**, что заводит вторую ручку и глагол | §9 |
| Н3 | транзакций пять, инвариант считал четыре | §7 инв. 5 — перечеканка добавлена (Ф12-25: новый набор + событие) | §7 инв. 5 |
| Н4 | три ограничения схемы допускали `password` в `pending` | третье ограничение обобщено: «`pending` — только у `totp`» — покрывает `password` и `lookup_secret` одним предикатом; Ф12-40 — четыре вставки отвергнуты тремя ограничениями | §4.1 п.1, Ф12-40, §8 |
| Н5 | у существующей ручки обёртки две половины (значение / имя переменной окружения), новая обязана повторить обе | Р2 — `authn.second-factor-encryption-key-hex` и `…-env`; Ф12-35 (а) — «не задана» = ни одна из двух | Р2, Ф12-35 |
| Н6 | порядок проверок у `confirm` при истёкших обоих окнах не назван | Р4 (порядок проверок): свежесть первой; Р8; Ф12-04 последнее «И» — проба на обоих истёкших получает ровно `SESSION_NOT_FRESH` | Р4, Р8, Ф12-04 |
| Н7 | «52 таблицы» — 52 объявления, различных 51 | комментарий §1.1 назван по единице: объявления `CREATE TABLE`, `kaname.limits` объявлена дважды | §1.1 |
| Н8 | ревизия платформы сдвинулась на один коммит за время круга | перемерена перед этим кругом: `a2981976c6`, тот же один коммит; предикаты §1.1 дома платформы на нём дал круг (4 / 0 / 3); шапка не правится — слово в ней статическая форма, ревизия названа здесь и в §12.1 | §12 (эта строка) |

### §12.1 Дрейф линии `release/iam-lines`: голова ушла вперёд ствола, и что это меняет

База PR — накопительная линия `release/iam-lines`; на момент круга 1 она **равнялась** стволу
(`4f6ab4be`, запись круга: «дерево ветки без документа побайтово равно `origin/main` =
`origin/release/iam-lines`»). К этому кругу голова линии — `9a1e24b1`: два коммита сверх ствола —
`c0eb17c5` (**код Ф3**: полоса входа паролем, наша сессия, отзыв и выход, `#179`) и `9a1e24b1`
(MRW-1 S1, `#169`); `origin/main` не двигался. Ревизия измерения документа остаётся `4f6ab4be`
(шапка не правится), а предикаты §1.1 прогнаны **заново на голове линии** — и названо, какие
сдвинулись и что из этого следует:

```sh
# ДОМ: PRO-Robotech/kaname · R=origin/release/iam-lines (9a1e24b1) · та же единица, что у строки §1.1
git grep -lE '"totp"|"lookup_secret"' origin/release/iam-lines -- '*.go' ':!*_test.go' | wc -l    # 2 → 2 (∅ словаря — прежнее)
git grep -n 'hmac' origin/release/iam-lines -- '*.go' ':!*_test.go' | wc -l                       # 0 → 2: ОБА — признак формы (form_token.go, SHA-256), НЕ проверяющий кода — ∅1 держится
git ls-tree -r origin/release/iam-lines --name-only | grep -c 'internal/migrations/.*\.sql'       # 21 → 22: human_session_is_our_record — сессия Ф3 (∅3 на линии ЗАКРЫТА)
git grep -lE 'csrf|kaname_form|kaname_session' origin/release/iam-lines -- 'internal/**/*.go' ':!*_test.go' | wc -l   # 0 → 2 (∅3 закрыта кодом Ф3)
git grep -lniE 'failed.?(login|sign.?in|attempt)|attempts?.?(per|window)' origin/release/iam-lines -- 'internal/**/*.go' ':!*_test.go' | wc -l   # 0 → 1; счёт Ф3 Р10 — internal/apps/kaname/api/humansession/rate.go (∅5 на линии закрыта)
git grep -niE 'freshness|свежест' origin/release/iam-lines -- 'internal/apps/kaname/config/*.go' ':!*_test.go' | wc -l   # 0 → 0 (∅6 держится)
git grep -n -iA1 'func.*signInMethods' origin/release/iam-lines -- cmd/kaname/                       # wiredSignInMethods → nil  ⇒  loginLane.signInMethods() → [password] (loginlane.go:81)
git grep -c "ARRAY\['password'::text\]" origin/release/iam-lines -- internal/migrations/20260915111233_login_methods_live_in_their_own_rows.sql   # 1 → 1 (∅2 держится)
```

| строка §1.1 / §1.2 | на `4f6ab4be` (шапка) | на голове линии `9a1e24b1` | следствие для документа |
|---|---|---|---|
| ∅1 проверяющего кода нет | `hmac` 0 | 2 — оба в признаке формы Ф3 (`form_token.go`), не сверка кода | **держится**: единица «HMAC в прод-коде» стала грубее предмета; предикат DoD §9 читать как «HMAC над секретом второго фактора», а не любой HMAC |
| ∅2 хранилища секрета нет | `['password']`, домен `password` | то же | держится |
| ∅3 сессии нет | 0 файлов | 2 файла; таблица `human_sessions` | **закрыта кодом Ф3**: «Дано» с сессией на линии строимо; §4.3 первая строка и §9 п.1 («Ф3 — первой») на линии **выполнены**, порядок работ не меняется — он уже пройден |
| ∅5 счёта неверных предъявлений нет | 0 | `humansession/rate.go` — счёт Ф3 Р10 | **закрыта**: Р7 кладёт код в существующий счёт |
| ∅6 окна свежести нет | 0 | 0 | держится — производит эта фаза |
| ∅7 глаголов второго фактора нет | — | — | держится |
| Ф-е, Р12, Ф12-34: `wiredSignInMethods` → `nil` | есть | функции с этим именем нет: `loginLane.signInMethods()` отдаёт `password` при поднятой полосе (`cmd/kaname/loginlane.go`) | имя названо в обоих видах у Ф-е, Р12, §9; **предмет тот же** — перечень провязанного, из которого страж выводит уровни; Ф12-34 требует в нём `totp`, `lookup_secret` |
| Р6, §4.1 п.3: настройка «что писать» | читатель Р2 (`declared.go`) есть, ручки нет | ручка объявлена: `authn.login.hasher-format` / `-memory` / `-iterations` / `-parallelism` (`internal/apps/kaname/config/login_lane.go`) | набор чеканится по ней — производитель есть на линии |
| §1.2 — 19 файлов форм | 19 | 19 | держится |
| §1.1 «21 файл, 52 объявления» | 21 / 52 | 22 / 55 | комментарий стоит при своей ревизии; сдвиг назван здесь |

Что это **не** меняет: ни одно решение Р1…Р12, ни один сценарий; §1 описывает ревизию,
названную шапкой, и остаётся верен о ней. Гейты дома (`go test ./internal/check/`) автором
редакции 5 прогнаны и на дереве ветки, и на **сведённом** с головой линии дереве
(`git merge-tree --write-tree`) — вывод обоих прогонов в описании PR `#185`. Кодирование по этой
приёмке идёт от линии, где Ф3 уже есть: «не выполнилось» из-за отсутствия сессии (§4.3, §8) на
линии не наступает.
