<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка: посевная идентичность называет СВОЮ службу, а не платформу

- **Статус:** ✅ APPROVED — круг 2 (`acceptance-reviewer`, ревизия ревью
  `69d9ba3d`). Блокирующих нет. Проверяющий воспроизвёл своим прогоном перепись
  §0.1 (все пять вёдер дословно), отказ предиката кодом 2 на беспредметности в
  обе стороны, три ограничения §0.3 (пять координат свода · четыре деривации ·
  три записи `const` схемы), исход КАЖДОГО держателя §6 и пару RED→GREEN на
  настоящем документе. Четыре важных замечания — закрыты правкой после вердикта
  (строка ниже). Предыдущий вердикт — CHANGES REQUESTED (круг 1, та же роль):
  три блокирующих, все одного рода — **держатели и координаты не перемерены при
  переносе ревизии измерения с `d496c5a4` на `3178ed7c65`**. Ошибка автора, не
  документа: строка ревизии была сдвинута, а привязанное к ней оставлено. §6
  объявлял снятыми два гейта, которые живут здесь и зелены; третьим называл
  держателем гейт, который отсюда SKIP-ает by construction; §5 нёс одну
  координату, пережившую ревизию. Круг 2 подтвердил, что род круга 1 не
  повторился ни разу: координат со строкой **14, разрешились 14, устарело 0**
- **⚠️ Правка ПОСЛЕ вердикта (2026-09-13, волна 3): ЗАМЕР, а не редактура, и он
  опроверг предикат задачи `PRO-Robotech/kacho#2554` целиком.** Держатель чужой
  ведомости прогнан (`PRO-Robotech/kacho@8d70f599`, ЗЕЛЁН) и печатает по полосе «имя
  объекта» **10 · 5 ф.** при 638 · 226, которые называет предикат задачи; поверхность,
  которую он обходит, дерева службы больше не содержит (0 файлов). Значит обе ветви
  предиката адресуются величинам, которых нет, и свидетелю, который этого дерева не
  читает, — §0.2 врезка, §7-П2, §7-П4. Тем же замером закрыт `PRO-Robotech/kacho#2574`:
  пометка `kacho:latent` из полосы долга ушла вместе с поверхностью, правок требуется
  НОЛЬ, а запись в ведомость решённого остаться заводить нельзя (§7-П5). Пообъектный
  разбор ведра «живого» **опроверг предикат П2 второй раз** — врезка «ЧТО СТОИТ В
  ВЕДРЕ ЖИВОГО» §7; замена предиката вынесена кругу, а не внесена задним числом, как
  и в первый раз. **Ни один сценарий, вердикт, производитель и признак готовности не
  тронут**; §8 шаг 7 получил ЧЕТВЁРТОЕ место признака, которого в нём не было. Числа
  §0 пересняты на новой базе линии (`61053691`) — она сменилась вливанием волны 2, а
  не правкой текста
- **⚠️ Правка ПОСЛЕ вердикта (2026-09-13, шаги 2–5 §8 ИСПОЛНЕНЫ; П2 ЗАМЕНЁН, П6
  ИСПОЛНЕН): круга требует, и не одного замечания ради.** Посажены классы B и C
  целиком: имена двух встроенных ролей (`20260913143007_…`), пин четырёх
  идентификаторов литералом (`bootstrap_token/ids.go`), окно двух написаний у
  трёх читателей (`domain.SeedIdentityWindow`, применитель манифестов,
  опубликованная схема) и миграция перевода аккаунта, служебной записи и трёх
  посевных ЗНАЧЕНИЙ (`20260913144108_…`, П6 вместе с шагом 5, как велит §8).
  **Предикат П2 переписан** — прежний был недостижим by construction, и замер
  показал, что он двигался в сторону, ОБРАТНУЮ цели (§7-П2). **§0.5 исправлен
  по факту:** стенд поднимается, и все сценарии классов A · B · C сняты ОПЫТОМ
  на живой Postgres, а не объявлены заказанными. Что осталось незакрытым и
  почему — §7 (П1, П3, П4, П5) и §8 шаги 6–7
- **⚠️ Правка ПОСЛЕ вердикта (2026-09-13, шаг 1 §8 ИСПОЛНЕН): §0 перемерен, и
  замер ОПРОВЕРГ предикат П2 — это НЕ editorial, и круга он требует.** Класс A
  посажен (`20260913114722_cluster_name_leaves_the_platform_brand.sql`,
  `kacho-root` → `root`); арендатор в базе видит объявленное имя, три пробы
  утверждают это, якорь не сдвинулся. Перепись §0.1 при этом **выросла**, а не
  упала: `130 · 56 ф.` → `133 · 57 ф.`. Причина не в дереве, а в ЕДИНИЦЕ СЧЁТА —
  предикат считает написание **всюду, включая код, который его снимает**:
  переводящая миграция обязана назвать прежнее написание, чтобы найти строку, а
  проба — чтобы утверждать, что его больше нет. Пофайлово по `kacho-root`:
  свод **1** (правке не подлежит, ban #5) · переводящая миграция **3** · её
  проба **1**. **Следствие несущее: предикат П2 («перепись даёт 0») недостижим
  by construction** — ноль наступит только если снять миграцию перехода и её
  пробу, то есть отменить сам переход. Предикат обязан либо считать вне цепочки
  миграций и проб перевода, либо судить БАЗУ, а не текст. Здесь он **не
  переписан**: замена предиката готовности — предмет решения, а не правки, и
  вносить её задним числом под чужой вердикт нельзя. Вынесено находкой для
  следующего круга
- **⚠️ Правка ПОСЛЕ вердикта (2026-09-13, круг 2): закрыты четыре важных
  замечания, ни одно не трогает решение.** Снята величина объёма обхода из
  ячейки §6 — она попадала в объявленную слепую зону собственного гейта (он
  судит блок §0, а не прозу вокруг) и уже разошлась на три файла; осталось то,
  что гейт держит. Число проб инъекции приведено к предикату (семь, не восемь).
  §8 получил место для П6 и назвал асимметрию с П3. Ни один сценарий, вердикт,
  производитель, признак готовности и ни одно число §0 не тронуты — перепись
  §0.1 после правки печатает те же пять вёдер, и это держит
  `TestSeedIdentityCensusMatchesItsAcceptance`
- **⚠️ Правка ПОСЛЕ объявления состояния (2026-09-13, `kaname#11`): координата держателя названа своим
  ДОМОМ.** Служба вынесена из монорепо (`kacho#2598`), приёмка уехала вместе с ней, а
  гейты дерева остались судить своё дерево. Пролёт, целиком состоявший из имени пробы,
  читался адресом В ЭТОМ репозитории, где такой функции нет: следующий шёл по нему, не
  находил ничего и делал вывод О ДЕРЕВЕ, а не о документе. Там, где стояло одно имя,
  теперь стоит дом — `PRO-Robotech/kacho:` (гейт жив в монорепо) ·
  `PRO-Robotech/corelib:` (уехал в общий фундамент) · `PRO-Robotech/kacho@d941344bd9:`
  (снят самим вынесением — `PRO-Robotech/kacho@0cc1cd54c3`). **Следствие для читателя,
  и оно несущее:** зелёный такого гейта о дереве `PRO-Robotech/kaname` не говорит
  НИЧЕГО — он судит ЧУЖОЕ дерево, и держателя у названного класса здесь может не быть
  вовсе. Дельта правки — только приставка дома: ни один сценарий, вердикт,
  производитель, признак готовности и ни одно число не тронуты. Держит форму гейт
  `TestAcceptanceProbeCoordinateResolves` (`internal/check`) — чужие дома он считает
  переписью и печатает их, а приставку, домом не являющуюся, роняет
- **Правка ПОСЛЕ объявления состояния:** документ правлен позже строки состояния —
  предикат посевной идентичности переехал сюда вместе со службой, и вместе с ним
  перемерены числа §0. Форма объявления здесь именно такая, а не «после вердикта»:
  вердикт **не вынесен**, переносить нечего, и утверждать о переносе значило бы
  объявить прочитанной ревизию, которую никто не читал. Строка обязана правиться
  тем же изменением, что и тело: она есть единственное место, где видно, что
  состояние объявлено РАНЬШЕ содержимого
- **Ревизия измерения:** `83ddd08a` — **база линии**, то есть ствол службы, от которого
  ветвится это изменение. Числа §0 сняты на дереве «эта база ПЛЮС дифф настоящего
  изменения»; перемерить их — применить дифф запроса слияния к названной ревизии.
  **Коммит полосы здесь НЕ называется, и это решение, а не удобство.** Работа едет в
  ствол СХЛОПЫВАНИЕМ, поэтому коммит ветки предком ствола не становится никогда: датировка
  им верна ровно до вливания и лжёт сразу после — числа оказываются привязаны к точке,
  которой в истории нет. Цена измерена: ствол службы был красен по гейту датировки **два
  вливания подряд** (`ba43277c`, `c56218b6`), и оба запроса слияния несли зелёный
  обязательный контекст — на ветке хеш полосы был предком рабочей вершины. База же
  остаётся предком ствола и после схлопывания, поэтому форма переживает посадку.
  Держит это `internal/check` `TestMeasurementRevisionsAreDatedByHashAndBelongToThisHistory`:
  он судит вхождение относительно СТВОЛА (`origin/main`), а не рабочей вершины, и потому
  даёт один вердикт до и после вливания.
  Прежние редакции называли `3620b3301d`, `aea786bdbc` и `dd2d07ab47` — все три коммиты
  полос, все три непригодны по причине выше. Прежняя **база** — `c56218b6` — непригодной
  не была и не стала: она сменилась потому, что дифф, ради которого её называли, ПОСАЖЕН
  (`61053691`, волна 2), и числа §0 теперь снимаются на новой базе плюс дифф ЭТОГО
  изменения. База сменяется вливанием, а не правкой текста, — поэтому строка одна и
  перемеряется вместе с ней весь §0
- **Прежние ревизии измерения названы, а не стёрты, — и одна из них УЖЕ
  разошлась с деревом.** `PRO-Robotech/kacho@b3300a0b3` и `PRO-Robotech/kacho@7cd4cc8355`
  сняты в **монорепо** `PRO-Robotech/kacho`; как объекты в этом репозитории они **не существуют** —
  деревья экспорта не делят историю. Ревизия `PRO-Robotech/kaname@d496c5a4` (прежняя
  точка ветвления) существует и резолвится, но её числа предикат уже **не
  воспроизводит**: ствол ушёл вперёд, и §0.1 ниже называет обе величины и то,
  чем они разошлись. Это и есть та цена, ради которой строка ревизии одна:
  число без своей ревизии неотличимо от числа, снятого неизвестно когда
- **Задача:** `PRO-Robotech/kacho#2554`, часть эпика `#2076` (Р3, разрез полосы
  «имя объекта»), линия `release:standalone-iam`
- **Тип изменения:** ЛОМАЮЩЕЕ для оператора платформы — имя аккаунта, которое
  пишут манифесты пяти чужих продуктов, меняется. Для арендатора службы —
  НЕ ломающее: адресация идёт по идентификатору, а он не двигается (§2.2)
- **Сервис:** `kaname` — предмет; затрагивает `internal/migrations`
  (новая миграция), `internal/apps/kaname/{seed,api/bootstrap_token}`,
  `internal/repo/kaname/pg`, `schema` — пути ЭТОГО репозитория
  (`PRO-Robotech/kaname`); вне службы, в МОНОРЕПО `PRO-Robotech/kacho` —
  `services/{compute,nlb,registry,storage,vpc}/manifest.yaml` (по 5 вхождений).
  Этим изменением в дерево кладётся ОДИН файл — перепись §0.1 рядом с документом
- **Предмет приёмки — ВЫБОР** (какие из восьми объектов переименовываются, каким
  написанием, и что взамен пиннится литералом). Реализация заводится задачами-
  преемниками §7, а не этой под-фазой
- **Формат:** Given-When-Then. Рядом с документом лежит ОДИН исполняемый файл —
  `seed-identity-names-its-own-service.py`, перепись §0.1: он производит числа §0.2
  и не решает ничего. Кода предмета здесь нет по построению

---

## 0. Перепись производителей — ДО единого сценария

Раздел стоит первым намеренно: три круга соседних приёмок этой линии были
отвергнуты по одному классу — «Тогда» без производителя. Предмет проверки
перенесён во ВХОД работы (`multi-agent-flow.md` §«Класс, отвергший работу
ДВАЖДЫ, переезжает в форму задания»).

### 0.1. Единица счёта названа, и она НЕ совпадает с единицей задачи

Задача называет **120 вхождений в 50 файлах** и объявляет единицей находку
полосы `laneObjectName` держателя ведомости остатка. Здесь единица **другая**, и
это сказано вслух, потому что иначе два числа об одном предмете разошлись бы
молча:

> **вхождение литерала посевной идентичности** в отслеживаемом файле, счёт от
> длинного написания к короткому (`kacho-system.admin` не пересчитывается
> вторично как `kacho-system`), с адъюдикацией по КОНТЕКСТУ СТРОКИ.

Предикат ПОСАЖЕН и зовётся —
`docs/engineering/acceptance/seed-identity-names-its-own-service.py`
(`python3` над `git ls-files -z`, обход всего дерева; `--rev <ревизия>` мерит
названную ревизию, без него — рабочее дерево). Перепись печатает объём
осмотренного и ОТКАЗЫВАЕТ кодом 2 на беспредметности — пустой обход либо
нерезолвящаяся ревизия. Числа ниже сняты им, и любое из них воспроизводится
одной командой. **Отказы по объявлению поверхности из этого репозитория
СНЯТЫ** — перечень `KanameSurface`, который они стерегли, читался из
монорепо и здесь недостижим; см. врезку «ВЕНКА ПЕРЕЕХАЛА» ниже.

**Поверхность Kaname он не выписывает, а ЧИТАЕТ из дерева** — `KanameSurface`
(`internal/repohygiene/kanamenameresidue.go`), где она объявлена один раз для
обхода, переписи и инъекции. Копия перечня здесь была бы вторым местом об одном
предмете и разошлась бы молча; не прочиталось объявление — отказ, а не умолчание.
**Это верно для ПЕРВОЙ редакции предиката** (монорепо, ревизии `7cd4cc8355`/
`b3300a0b3`) и НЕ верно с этой правки — см. врезку «ВЕНКА ПЕРЕЕХАЛА» ниже:
чтение из монорепо снято, эта абзацная мысль остаётся историей выбора, а не
описанием текущего кода.

> [!warning] Посадка предиката ОПРОВЕРГЛА два числа, которые он же должен был
> производить, — и это находка посадки, а не правка задним числом
> Пока предиката не было, числа §0.2 не имел кто произвести: шесть из них
> ссылались на перепись, которой не существовало ни в дереве, ни в тексте. Первый
> же прогон посаженного разошёлся с двумя из шести вёдер: **поверхность**
> 135 · 55 ф. → **132 · 56 ф.**, **ns** 40 · 15 ф. → **39 · 15 ф.**; остальные
> четыре сошлись дословно. Разошедшееся исправлено ПО ПРЕДИКАТУ, а не наоборот:
> подгонка правил под записанный ответ дала бы инструмент, который согласен
> **by construction** и потому не доказывает ничего.
>
> Чем расхождение НЕ объясняется — проверено, а не предположено: литерал
> `kacho-bootstrap-grant` в перечень не входит законно (единственное вхождение —
> проза соседней приёмки о том, что имя УШЛО, то есть называет снятое); граница
> «поверхность против пространства имён» и есть спорное место (§0.4), и прежний
> прогон провёл её иначе. Точное правило того прогона из текста не
> восстанавливается: он не сохранился — ровно поэтому предикат теперь посажен.
>
> **Что из этого следует вперёд:** производитель у чисел §0.2 отныне один, и он
> зовётся. Расходятся числа и предикат — верен предикат.

**Мой замер не воспроизводит 120 и не опровергает его**: величины меряют разное.
Держатель считает вхождения сегмента `kacho`, разложенные по полосам ПОСЛЕ
границ и после прощения ведомостью; я считаю литералы целиком.

**Проверено, что расхождение — от единицы, а не от движения дерева.** Тот же
предикат прогнан на вершине разреза `7cd4cc8355` и на `b3300a0b3` — семь коммитов
между ними, пять по поверхности, — и **все шесть вёдер совпали дословно**:

```
$ .../seed-identity-names-its-own-service.py --rev <ревизия>

                       7cd4cc8355        b3300a0b3
ПРЕДМЕТ: поверхность   132 ·  56 ф.      132 ·  56 ф.
ПРЕДМЕТ: манифесты      25 ·   5 ф.       25 ·   5 ф.
ПРЕДМЕТ: прочее          2 ·   2 ф.        2 ·   2 ф.
остаётся: ns            39 ·  15 ф.       39 ·  15 ф.
остаётся: зонт          31 ·  16 ф.       31 ·  16 ф.
граница: приёмки        40 ·   6 ф.       40 ·   6 ф.
ПРЕДМЕТ всего          159 ·  63 ф.      159 ·  63 ф.
```

**Оговорка, без которой числа солгут при первом же прогоне.** Обход идёт по
`git ls-files`, то есть по ОТСЛЕЖИВАЕМЫМ файлам, и на момент замера этот документ
отслеживаемым не был — как и предикат рядом с ним. Как только посажены оба,
ведро границы растёт с 40 · 6 ф. до **105 · 8 ф.** — это их собственный текст, и
место ему именно там: вердикт приёмки привязан к отпечатку, поэтому её текст
границей и объявлен, а предикат лежит под тем же каталогом. **Ни одно из вёдер
ПРЕДМЕТА при этом не меняется** — 132 · 25 · 2, и это проверено прогоном по
рабочему дереву, а не выведено рассуждением.

> [!note] Две величины этого абзаца сняты в РАЗНЫХ деревьях, и без приписки
> предложение читается так, будто в одном
> `40 · 6 ф.` — монорепо, `7cd4cc8355`, где каталог приёмок лежал под
> `services/iam/docs/…`. `105 · 8 ф.` — уже ЭТОТ репозиторий, ревизия
> `d496c5a4`. На ревизии измерения (`3178ed7c65`) предикат даёт по тому же
> ведру **107 · 8 ф.**: прибавили два вхождения правки `aeaa77e3`, назвавшей
> координатам их дом. Довод абзаца («текст приёмки есть граница, а не остаток»)
> от величины не зависит и верен при любой из трёх.

**Ведро зонта тоже не двигается, и это стоило порядка вёдер.** Граница судится
РАНЬШЕ принадлежности литерала зонту: проза приёмки о личности, которую выдаёт
зонт, есть текст приёмки, а не остаток имени. На ревизии замера оба порядка дают
дословно одно (зонт 31 · 16 ф., граница 40 · 6 ф.), поэтому выбор ничего не
переписал; после посадки только этот порядок оставляет зонт неподвижным.

> [!important] ВЕНКА ПЕРЕЕХАЛА (2026-09-11, задача #2554) — предикат больше НЕ читает из монорепо, и три ведра стали короче
> Служба выделена в отдельный репозиторий `PRO-Robotech/kaname`. Держатель
> `KanameSurface` (`internal/repohygiene/kanamenameresidue.go`), из которого
> предикат §0.1 читал перечень поверхности, живёт **только в монорепо**
> `PRO-Robotech/kacho` — в этом репозитории такого файла нет. Прогон предиката
> из этого репозитория **отказывал кодом 2 на каждом заходе**, а не давал
> устаревшее число: посылка задачи «в монорепо её ноль файлов» была неверна на
> момент заведения (там ещё 2542 отслеживаемых файла службы), но верна для
> ВЕНКИ работы — предикат и приёмка обязаны жить там, где теперь код.
>
> **Предикат переписан**, а не подменена координата: `surface_roots()` снят
> целиком, ведро «поверхность» слито с «прочее» в одно «ПРЕДМЕТ» (различавший
> их вопрос — «это ещё каталог службы, или соседний код монорепо» — здесь не
> имеет предмета, соседнего кода в этом дереве нет by construction), `ACCEPTANCE_DIR`
> лишился приставки `services/iam/`. Числа воспроизведены СНОВА, не перенесены:
>
> ```
>                        7cd4cc8355 (монорепо)    3620b3301d (kaname, ревизия измерения)
> ПРЕДМЕТ: живой         159 (132+25+2) · 63 ф.   107 · 45 ф.
> ПРЕДМЕТ: свод и миграции  — (ведра не было)      28 ·  6 ф.
> ПРЕДМЕТ: манифесты       25 ·  5 ф.               0 ·  0 ф.  (см. ниже)
> остаётся: ns             39 · 15 ф.              13 ·  2 ф.
> остаётся: зонт           31 · 16 ф.               9 ·  5 ф.
> граница: приёмки         40 ·  6 ф.             109 ·  8 ф.  (растёт с каждой правкой этого текста — самоссылка, см. §0.1 выше)
> ```
>
> **Числа ПЕРЕСНЯТЫ на базе линии**, и датировка переведена на неё же. Коммит
> полосы (`3620b3301d`) предком ствола не станет никогда — здесь вливают
> схлопыванием, — поэтому колонка ниже названа **базой**: она остаётся координатой
> и после посадки. Величины не перенесены, а получены прогоном предиката на дереве
> «база плюс дифф этого изменения»; шесть вёдер из шести сошлись с колонкой полосы
> дословно, то есть переезд датировки чисел не сдвинул.
>
> ```
>                           83ddd08a (база линии, ревизия измерения)
> ПРЕДМЕТ: живой            112 · 45 ф.
> ПРЕДМЕТ: свод и миграции   28 · 6 ф.
> ПРЕДМЕТ: манифесты          0 · 0 ф.
> остаётся: ns               13 · 2 ф.
> остаётся: зонт              9 · 5 ф.
> граница: приёмки          110 · 8 ф.
> ```
>
> **База сменилась на `83ddd08a`, и живое ведро выросло на ПЯТЬ — рост объяснён
> единицей счёта, а не движением предмета** (линия `batch-probes-ci-stand`,
> 2026-09-16). Прежняя база — `61053691`; её числа воспроизводятся на ней до сих
> пор. Пять вхождений прибавила ПРОЗА, разбирающая сам перевод: сверка паритета
> посева судила НАПИСАНИЕ вместо строки (`kacho-system` объявленное против
> `system` живого — 30 находок из 30 на дереве платформы), и её починка обязана
> назвать оба написания дословно, иначе читатель не поймёт, что именно
> приводится к чему. Файлов при этом СТОЛЬКО ЖЕ — 45, — то есть новых мест
> предмет не занял: выросло число вхождений в местах, уже сосчитанных.
>
> Это та же самоссылка, что у ведра приёмок, только в прод-дереве: текст,
> объясняющий перевод, обязан называть переводимое. Предикат перехода (§7-П2)
> от неё не страдает — он спрашивает пару «живой = 0 ПРИ своде != 0», а не
> убывание от замера к замеру.
>
> **База сменилась, шесть вёдер из шести — нет, и это сказано числом, а не
> умолчанием** (волна 3, 2026-09-13). Прежняя база — `c56218b6`; дифф, ради которого
> её называли, посажен вливанием `61053691`, поэтому дерево «прежняя база ПЛЮС дифф»
> и дерево «новая база» есть одно и то же дерево, и величины обязаны были совпасть.
> Они совпали дословно — прогон предиката на рабочем дереве после переноса датировки
> даёт ту же шестёрку. **Совпадение здесь — проверка, а не совпадение:** разойдись
> хоть одно ведро, это означало бы, что вливание сдвинуло предмет, о котором вердикт
> уже вынесен.
>
> **Ведро ПРЕДМЕТА разрезано НАДВОЕ, и это следствие замера, а не уточнение
> формы.** После посадки классов A, B и C перепись единого ведра дала **159 ·
> 63 ф.** против 133 до них: предикат готовности («перепись даёт 0») двигался в
> сторону, ОБРАТНУЮ цели — каждый выполненный шаг перехода его увеличивал.
> Причина не в дереве, а в единице счёта: применённая миграция обязана назвать
> прежнее написание, чтобы найти строку, и правке она не подлежит (ban #5).
> Разрез идёт по ЕДИНСТВЕННОМУ механическому признаку, который делает вхождение
> вечным, — пути применённой миграции. Разбор и новый предикат — §7-П2.
>
> **Левый столбец перемеру не подлежит и разрезу тоже:** он снят в монорепо на
> `7cd4cc8355`, где ведра «свод и миграции» не существовало. Раскладывать его
> задним числом значило бы объявить прочитанным то, чего никто не читал.
>
> **Правый столбец перемерен ТРИЖДЫ за 2026-09-13; все прежние значения названы,
> а не затёрты.** Третий перемер — после посадки классов B и C и разреза ведра:
> `133 · 57 ф.` (одно ведро) → `107 · 45 ф.` живого при `28 · 6 ф.` замёрзшего
> (после разреза `131 · 57 ф.`; до `107` живое опустил перевод прозы прод-кода и
> трёх проб, искавших роли прежним именем).
> Второй — после посадки класса A: `130 · 56 ф.` → `133 · 57 ф.`, и рост
> объяснён единицей счёта, а не движением предмета. Первый перемер — ниже.
>
> **Правый столбец перемерен 2026-09-13; прежнее значение названо, а не затёрто.**
> Первая редакция сняла `130 · 54 ф.` на ревизии `d496c5a4` — она была верна
> тогда и воспроизводится там до сих пор (`--rev d496c5a4`). На ревизии
> измерения предикат даёт `130 · 56 ф.`: вхождений столько же, файлов на два
> больше. Разрешает это не выбор правдоподобного, а **пофайловая разность**,
> снятая тем же предикатом по обеим ревизиям, — и она сходится без остатка:
>
> | файл | было | стало |
> |---|---:|---:|
> | `internal/apps/kaname/seed/permissions.go` | 7 | **5** |
> | `internal/check/module_identity_seeded_only_by_baseline.go` | — | **1** |
> | `proto/kaname/cloud/iam/v1/fga_model.fga` | — | **1** |
>
> `−2 +1 +1 = 0` вхождений и `+2` файла. Оба прибавившихся завела соседняя
> полоса (`PR #49`, «служба стоит на своих ногах»): гейт посева модульных
> личностей и проекцию модели прав в каталог контракта. **Ни одно из трёх
> движений предметом этой приёмки не является** — они называют имя, а не
> заводят его, — но перечислены поимённо, потому что иначе «то же число при
> других файлах» читается как ошибка замера.
>
> **Ни одна из трёх просевших строк не значит «снято» — все три значат «этот
> предикат больше не видит соседей».** Проверено грепом по монорепо, а не
> предположено: hits вне `services/iam` дают ns **28** и зонт **22** — почти
> точно закрывают разницу (28+13=41≈39, 22+9=31 — дословно). Они живут в
> `deploy/helm/umbrella/{values*.yaml,templates/bootstrap-operator-certificate.yaml}`
> — умбрелла-чарт монорепо, где `kacho-system` называет K8s-namespace, в
> который сервисы ВСЕЙ платформы стучатся, а `kacho-bootstrap-operator` —
> личность, которую тот же чарт выдаёт сертификатом. Это не код службы, это
> общая инфраструктура платформы, и она **вне всякого репозитория службы**.
>
> **Манифесты пяти чужих продуктов теперь НЕ измеримы этой приёмкой вовсе.**
> Раньше предикат гонялся из монорепо и попутно видел
> `services/{compute,nlb,registry,storage,vpc}/manifest.yaml` — теперь их в
> дереве нет физически. Величина осталась в §0.2/§0.3/§7-П3 равной **25 в 5
> файлах** как ИСТОРИЧЕСКИЙ факт монорепо на момент разреза; предикат этого
> репозитория её больше не производит и не должен — это чужой предмет,
> измеримый только оттуда, откуда доставляют манифесты (П3).
>
> **ПРЕДМЕТ 132→130 — двумя единицами, а не сдвигом дерева.** Один хит
> (`git grep -c … proto/kaname/cloud/iam proto/corelib/authz` в монорепо → 1)
> жил вне `services/iam` и здесь недостижим тем же способом, что манифесты.
> Второй — не восстановлен: файлы `services/iam` и `kaname`-репозитория,
> которые я сверил построчно (`config.go`), побайтово совпали, значит это не
> редактирование, а различие адъюдикации на границе; не гонится далее — цена
> непойманной единицы здесь меньше цены нового прогона.
>
> Заголовки §0.2/§0.3 ниже **не переписаны**: они верны для ревизии, на которой
> измерены (монорепо, `7cd4cc8355`/`b3300a0b3`), и остаются историческим
> свидетельством трёх ограничений. Предикат рядом с документом с этой правки
> производит только то, что видно ИЗ ЭТОГО репозитория — граница, зонт, ns,
> манифесты(=0, не измеримо) и ПРЕДМЕТ(=130) — и печатает это явно, а не молчит.

### 0.2. Что в дереве ЕСТЬ — с командой на каждую строку

| факт | число | предикат |
|---|---:|---|
| объектов, разобранных ПООБЪЕКТНО | 8 | §0.4 |
| из них заводит САМА служба | **6** | §0.4 |
| из них заводит ЗОНТ платформы | **2** | §0.4 |
| вхождений ПРЕДМЕТА на поверхности `kaname` | **132** в **56** ф. | перепись §0.1 на ревизии МОНОРЕПО, ведро «поверхность» — **сегодняшний предикат такого ведра не печатает** (врезка ВЕНКА выше) |
| вхождений ПРЕДМЕТА — манифесты чужих продуктов | **25** в **5** ф. | перепись §0.1, ведро «манифесты»; **отсюда НЕ измеримо** — величина историческая (врезка ВЕНКА) |
| вхождений ПРЕДМЕТА — держатели гейтов платформы | **2** в **2** ф. | перепись §0.1 на ревизии МОНОРЕПО, ведро «прочее» — **сегодняшний предикат такого ведра не печатает** (врезка ВЕНКА выше) |
| **вхождений ПРЕДМЕТА всего (монорепо, `7cd4cc8355`)** | **159** в **63** ф. | перепись §0.1 на ревизии МОНОРЕПО |
| **ЖИВОЕ написание — ведро, обязанное дойти до нуля** | **107** в **45** ф. | перепись §0.1, ведро «ПРЕДМЕТ: живой» (§7-П2) |
| **ЗАМЁРЗШЕЕ написание — ноль недостижим by construction** | **28** в **6** ф. | перепись §0.1, ведро «ПРЕДМЕТ: свод и миграции»; ban #5 |
| остаётся: пространство имён кластера (предмет `#2553`) | **39** в **15** ф. | перепись §0.1, ведро «ns» (строки с `/ns/`, `namespace`, `.svc`) |
| остаётся: личность, которую выдаёт ЗОНТ | **31** в **16** ф. | перепись §0.1, ведро «зонт» (`kacho-bootstrap-operator`, `-seeder`) |
| граница: одобренная приёмка (вердикт привязан к отпечатку) | **40** в **6** ф. | перепись §0.1, ведро «граница» (путь под каталогом приёмок) |

**Производитель у ВСЕХ строк таблицы один — перепись §0.1, и это правка по
находке рецензии.** Ячейка манифестов прежде ссылалась на
`git grep -c 'kacho-system' -- '*manifest.yaml'`. Число она называла верное, а
производителя — нет: дословно этот предикат даёт **26 в 6 файлах**. Лишнее —
`manifest.yaml:295`, где имя роли стоит в КОММЕНТАРИИ, а не в посеве;
единица §0.1 адъюдицирует по контексту строки и потому его не считает, а `grep`
контекста не знает. Строка таблицы, ссылающаяся на предикат, чей вывод от неё
отличается, читается как расхождение — хотя расходились не числа, а адресация.

**Восемь объектов §0.4 — НЕ те же восемь строк, что в таблице признака задачи.**
Задача считает написаниями и держит `kacho-system.*` и `kacho-elsewhere`
отдельными строками; здесь единица — **объект**, поэтому первое есть прозаическая
запись тех же двух ролей, а второе — синтетика фикстуры, которая следует за
настоящим именем механически. Взамен пообъектно назван `kacho-bootstrap-seeder`,
которого перечень задачи не несёт вовсе. Расхождение названо, а не сглажено.

**Два вхождения «держателей гейтов платформы» названы поимённо**, иначе ведро
читается как неизвестность: `internal/repohygiene/kanamenameresidue.go` и
`internal/repohygiene/moduleserviceaccounthasacomponent_injection_test.go` —
**координаты МОНОРЕПО**, оба про `kacho-bootstrap-admin`, оба в прозе и
фикстуре, оба следуют за именем механически. Бывшее ведро «прочее», которому
они принадлежали, из этого репозитория недостижимо (§0.1 «ВЕНКА ПЕРЕЕХАЛА»).

Держатель ведомости остатка на той же ревизии печатал по своей полосе (**в
монорепо**, командой `go test ./internal/repohygiene/ -run
TestKanameSurfaceNameResidueMatchesItsLedgers -v`): осмотрено **1118** ·
найдено **638** · файлов **226** · прощено **17**. Ведомость **чужая** — её
вёл `#2553` (**закрыт** 2026-09-10), эта приёмка её не правит и из этого
репозитория прогнать не может — держателя здесь нет.

> [!important] ЭТИ ЧЕТЫРЕ ЧИСЛА — ИСТОРИЯ, и сегодня та же полоса печатает другое
> (перемерено 2026-09-13, волна 3)
> Держатель `PRO-Robotech/kacho@8d70f599:TestKanameSurfaceNameResidueMatchesItsLedgers`
> прогнан в рабочей копии монорепо (`go test ./internal/repohygiene/ -count=1 -run …
> -v`, вершина на два коммита позади своего ствола) и **ЗЕЛЁН**. По полосе «имя
> объекта» он печатает **осмотрено 46 · найдено 10 · файлов 5 · прощено 0**; весь
> долг ведомости — **189 вхождений на 8 полосах**.
>
> **Причина — не работа полос, а уход предмета из поверхности.** `KanameSurface`
> после withdrawal-волны состоит из подчарта зонта, каталога контракта и словаря
> аннотаций; дерева службы в нём нет — предикат в дереве монорепо, по одному на
> каталог: `git ls-tree -r origin/main --name-only -- <каталог> | wc -l` даёт **0** и
> для `services/iam`, и для `proto/kaname/cloud/iam`.
> Шапка самой ведомости это и называет: «имя объекта 192/85 → 10/5 … снижение с 1499
> до 189 переездом службы работой полос НЕ ЯВЛЯЕТСЯ».
>
> **Следствие несущее, и оно про ПРЕДИКАТ задачи `PRO-Robotech/kacho#2554`.** Обе его
> ветви — «полоса падает 638 → 518, файлов 226 → 184» и «те же 120 вхождений стоят в
> `KanameNameResidueStay` точным числом» — называют величины, которых в дереве больше
> **нет ни одной**, и адресуются ведомости, чей обход этого репозитория **не читает**.
> Переименование, посаженное здесь, двигает названную полосу на **ноль** by
> construction. Свидетель, у которого производитель есть ЗДЕСЬ, ровно один — перепись
> §0.1 и её пара §7-П2. Разбор — §7-П2 и §7-П5.

### 0.3. Замер посылок задачи — В ОБЕ СТОРОНЫ

Все три ограничения задачи **подтвердились**, и каждое — сильнее, чем заявлено.

**О1 — ban #5, имена посеяны ПРИМЕНЁННОЙ миграцией. ПОДТВЕРЖДЕНО.**
`internal/migrations/0001_initial.sql`: аккаунт — 3210, кластер —
3425, роли — 3785 и 3787, служебная учётка — 3843. Все пять координат прочитаны
дословно. Править файл нельзя; нужна новая миграция, и она обязана перевести
живые строки, а не только объявить написание.

**О2 — ban #15, идентификатор ВЫВОДИТСЯ из имени. ПОДТВЕРЖДЕНО ВЧЕТВЕРО.**
Задача называет одну деривацию; их **четыре**, и одна живёт в Go, а не только в
SQL:

```
acc1a18042d81fb438d6 = 'acc' || substr(md5('kacho-system'),1,17)
usr1a18042d81fb438d6 = 'usr' || substr(md5('kacho-system'),1,17)
svab91854890de887e6d = 'sva' || substr(md5('kacho-bootstrap-admin'),1,17)
soc_db27d17291ff453b6 = 'soc_' || substr(md5('kacho-bootstrap-soc'),1,17)
```

Проверено вычислением: `printf 'kacho-system' | md5sum | cut -c1-17` даёт
`1a18042d81fb438d6` — дословно суффикс посеянной строки. Формула объявлена
ОДИН раз (`internal/domain/derived_id.go`, `DerivedIDSuffix`) и
зовётся из `internal/apps/kaname/api/bootstrap_token/ids.go`
(`DeriveIdentity`) и `internal/authzguard/fgaproxy.go`. Единственность
держит гейт `TestDeterministicIDDerivationIsDeclaredOnce`, а не комментарий.

**Граница О2 названа, иначе норма была бы ложью.** Деривации подчиняются НЕ ВСЕ
восемь: у ролей `kacho-system.admin` и `kacho-system.viewer` идентификаторы
**рукописные** (`rol000000000sysadmin`, `rol000000000sysviewer`) и пиненные
`internal/domain/constants_extended.go`. У них смена имени
идентификатор не двигает **by construction** — и это делает роли самым дешёвым
из трёх классов §2.1, а не самым дорогим.

**О3 — межпродуктовая цитата. ПОДТВЕРЖДЕНО, и механизм у неё жёстче цитаты.**
Схема манифеста (`schema/module-manifest.schema.json`) пинит имя
аккаунта тремя записями `"const": "kacho-system"` — строки 406, 433, 557. Цитируют
его шесть манифестов: `compute`, `nlb`, `registry`, `storage`, `vpc` — по **5**
вхождений каждый, `iam` — **1**, и это **комментарий**, а не посев.

Жёстче цитаты потому, что согласие двух объявлений держит **гейт**:
`TestSchemaConstIsNotContradictedByTheTreeManifests`
(`internal/manifest/schemaconst_internal_test.go`). Он краснеет на
ЛЮБОЙ односторонней правке — сменишь `const`, не сменив манифесты, либо наоборот.
То есть переход обязан быть либо атомарным по шести файлам, либо выраженным
окном, принимающим оба написания; «сделаем в два коммита» отвергается гейтом, а
не вкусом.

### 0.4. Критерий разреза, применённый ПООБЪЕКТНО

Вопрос критерия дословен: *поставь Kaname одну, без Kachō, — объект, который это
имя называет, кто его заведёт?*

| объект | кто заводит | координата | вердикт |
|---|---|---|---|
| аккаунт `kacho-system` | служба, миграция | `0001_initial.sql:3210` | **ПРЕДМЕТ** |
| роль `kacho-system.admin` | служба, миграция | `0001_initial.sql:3785` | **ПРЕДМЕТ** |
| роль `kacho-system.viewer` | служба, миграция | `0001_initial.sql:3787` | **ПРЕДМЕТ** |
| учётка `kacho-bootstrap-admin` | служба, миграция | `0001_initial.sql:3843` | **ПРЕДМЕТ** |
| посевная строка `kacho-bootstrap-soc` | служба, код | `bootstrap_token/ids.go:26` | **ПРЕДМЕТ** |
| кластер `kacho-root` (ИМЯ) | служба, миграция | `0001_initial.sql:3425` | **ПРЕДМЕТ** |
| `kacho-bootstrap-operator` | **ЗОНТ**, чарт выдаёт сертификат | `deploy/helm/umbrella/templates/bootstrap-operator-certificate.yaml` | остаётся |
| `kacho-bootstrap-seeder` | **ЗОНТ**, пример в комментарии | `internal/apps/kaname/config/config.go:491` | остаётся |

**Пограничный случай перемерен, а не принят умолчанием.** Написание
`kacho-system` бывает и аккаунтом, и пространством имён кластера. Разбор строк
дал: пространством имён — **40** вхождений в **15** файлах (все несут `/ns/`,
`namespace` либо `.svc`), аккаунтом — остальные. Первое принадлежит `#2553` и
здесь не трогается.

**Довод, из которого вердикт по учётке НЕ мой:** его уже вынесла применённая
миграция `20260909202745_module_identities_leave_the_baseline.sql`. Она сняла из
свода пять служебных учёток модулей платформы и **назвала поимённо две
оставшиеся**, у каждой со своим доводом; про первую сказано дословно:
`kacho-bootstrap-admin` — «личность САМОЙ службы». То есть принадлежность
объекта службе установлена не этой приёмкой, а решением, уже посаженным в ствол.

### 0.5. Граница замера — чего я НЕ мерил

- ~~**стенд не поднимался.**~~ **ОПРОВЕРГНУТО 2026-09-13 опытом, а не
  рассуждением.** Прежняя редакция объявляла, что предел ядра занят чужими
  кластерами, и относила все сценарии полосы миграции (§4.1) к заказанным. На
  ревизии измерения стенд поднимается: `go test ./internal/migrations/ -count=1`
  проходит цепочку из тринадцати миграций в контейнере Postgres за ~3 с на
  пробу. Ограничение было верно для своей смены и **пережило её**; следующему,
  кто прочтёт «стенд не поднимался», надлежит перемерить, а не поверить. Все
  сценарии классов A · B · C сняты ОПЫТОМ — координаты держателей в §6;
- **не мерил Hydra.** Существует ли клиент с идентификатором
  `kacho-bootstrap-admin` у развёрнутого провайдера, я не спрашивал. Отсюда
  осторожный вердикт §2.3: имя клиента остаётся, а переход у провайдера — свой
  предмет;
- **не адъюдицировал ведомость остатка построчно.** Расхождение 132 и 120 названо
  различием единиц (§0.1) и на этом остановлено: сведение двух единиц — работа
  владельца ведомости, а не этой приёмки;
- **не мерил, сколько стоит окно у пяти чужих продуктов.** Их манифесты правит не
  Kaname; цена названа числом вхождений (25 в 5 файлах), а не человеко-временем;
- **предикат §0.1 НЕ ВИДИТ бренда, записанного ПРОЗОЙ, — и это его слепая зона,
  а не отсутствие предмета.** Он ищет дефисные литералы `kacho-…`; посевное
  ЗНАЧЕНИЕ, называющее платформу словом или доменом, под образец не подпадает ни
  при каком входе. Таких значений в своде **три**, все в применённой миграции, и
  все видны оператору установки без платформы:

  | строка | колонка | значение |
  |---|---|---|
  | `0001_initial.sql:3880` | `users.email` | `system@kacho.local` |
  | `0001_initial.sql:3880` | `users.display_name` | `Kacho System (module SA owner)` |
  | `0001_initial.sql:3425` | `clusters.description` | `Root cluster for Kachō control plane` |

  Предикат: `grep -oE "'[^']*Kach[oō][^']*'" internal/migrations/*.sql` → две
  строки; `grep -oE "'[^']*kacho\.local[^']*'" internal/migrations/*.sql` → одна.
  Читателей в прод-коде у всех трёх **ноль** (`git grep -l 'system@kacho.local'`
  → свод и один помощник проб).

  **Почему это названо, а не молчится.** Почта якорного пользователя несёт
  глобальную уникальность (`users_identity_email_uniq`), то есть её перевод —
  такой же предмет миграции, как и имя аккаунта, чьим владельцем этот
  пользователь является. Переименовать аккаунт в `system`, оставив его
  владельца `Kacho System` с почтой `system@kacho.local`, значит выполнить
  переход ровно наполовину — в той половине, которую оператор видит первой.
  Решение §2 этой приёмки об этих трёх значениях **не высказывается**: его
  предмет — восемь ИМЁН. Предмет значений вынесен П6.

---

## 1. Предмет

### 1.1. Что неверно

Арендатор, развернувший **только** службу доступа, получает базу, в которой:

- аккаунт, куда сложена вся его системная идентичность, назван именем продукта,
  которого он не ставил;
- встроенная роль супер-доступа и роль пола каталога названы **производными от
  того же имени** — `kacho-system.admin`, `kacho-system.viewer`;
- служебная учётка, под которой чеканится его первый неинтерактивный токен,
  названа `kacho-bootstrap-admin`;
- корневой кластер назван `kacho-root`.

Ни один из шести объектов не требует платформы для своего существования: все
шесть заводит цепочка миграций самой службы, и заводит **безусловно**.

### 1.2. Почему это дефект, а не уборка

Довод не эстетический, и он уже принят стволом — на уровень ниже. Применённая
миграция `20260909202745` сняла из свода служебные учётки пяти модулей платформы
ровно по этому признаку, и признак назван в ней дословно: *обязана ли эта строка
существовать у арендатора, который развернул продукт у себя?* Для личности чужого
модуля ответ — нет.

Здесь тот же вопрос задан на уровень выше — не про **жильцов** системного
аккаунта, а про **его собственное имя**. Ответ тот же: имя платформы у объекта,
который платформы не требует, есть данные окружения в применённой миграции.

### 1.3. Второе следствие — то, ради которого предмет решается СЕЙЧАС

Имя аккаунта — не косметика: это **живой ключ разрешения на пути применения
манифеста**. `internal/repo/kaname/pg/module_seed_writer.go` резолвит
аккаунт запросом `SELECT id FROM kaname.accounts WHERE name = $1` и на промахе
**отказывает** («аккаунт %q не резолвится»), а не проходит молча.

Значит у имени есть два свойства сразу, и они тянут в разные стороны: сменить его
дёшево в базе (одна строка, `UNIQUE(name)`), и дорого в контракте (пять чужих
продуктов пишут его в своих манифестах, а согласие держит гейт §0.3). Чем дольше
имя стоит, тем больше манифестов его цитируют — цена перехода растёт линейно по
числу модулей платформы, и растёт молча.

---

## 2. Решение

### 2.1. Восемь объектов — ТРИ класса, и разделяет их не важность, а деривация

Класс определяется одним вопросом: **выводится ли из имени внешне-адресуемая
координата** (ban #15)?

| класс | объекты | что с ними | почему |
|---|---|---|---|
| **A. имя ни к чему не привязано** | кластер `kacho-root` | переименовать, отдельной миграцией, БЕЗ окна | идентификатор якоря уже `cluster_root`; читателей имени в прод-коде **ноль**, единственный — одна интеграционная проба |
| **B. имя — ключ диспетчеризации, идентификатор пиненный** | роли `kacho-system.admin`, `kacho-system.viewer` | переименовать С ОКНОМ у читателя имени | идентификаторы рукописные и пиненные (§0.3), смена имени их не двигает; но имя читает `PermissionsForRole` разбором `switch` |
| **C. имя — АРГУМЕНТ деривации идентификатора** | аккаунт `kacho-system`, учётка `kacho-bootstrap-admin`, посевная строка `kacho-bootstrap-soc` | переименовать, ЗАМЕНИВ деривацию ПИНОМ, с окном у применителя манифестов | ban #15 запрещает двигать идентификатор; значит формула перестаёт его находить, и её место занимает литерал |

**Разделение — и есть решение.** Прежде эти восемь считались одной работой, и
именно поэтому она выглядела неподъёмной: самый дорогой класс задавал цену всем.

### 2.2. Идентификатор ПИННИТСЯ литералом — формула остаётся, но не для этих трёх

Для класса C переход выглядит так, и ни один шаг не изобретается — у каждого есть
образец в этом же дереве:

1. **четыре идентификатора становятся объявленными литералами.** Образец —
   `internal/domain/constants_extended.go`, где `SystemAdminRoleID` и
   `SystemViewerRoleID` уже стоят литералами по той же причине: «id неизменяем на
   всю жизнь ресурса (ban #15), а применённую миграцию править нельзя (ban #5)»;
2. **формула НЕ снимается.** `DerivedIDSuffix` продолжает выводить идентификаторы
   служебных учёток модулей из их имён (`authzguard`, `module_seed_writer`) — там
   имя и идентификатор двигаются вместе, и деривация верна. Единственность
   объявления, которую держит `TestDeterministicIDDerivationIsDeclaredOnce`, не
   затрагивается;
3. **`DeriveIdentity` перестаёт вычислять три посевные строки и начинает их
   называть.** Комментарий «MUST stay byte-identical to migration's md5 arguments»
   становится неправдой в момент переименования, и снимается тем же изменением —
   иначе следующий читатель починит код под неверный комментарий
   (`architecture.md` §doc-truthfulness);
4. **закрытый перечень посеянного пополняется.** Образец —
   `internal/domain/seeded_ids.go` с гейтом предмета
   `TestSeededIDExceptionsHaveASubject`: каждая запись обязана быть найдена
   посевом миграции И отвергнута чеканной формой; запись, которой нечего прощать,
   — находка.

**Что при этом НЕ происходит: идентификатор не двигается ни на один символ.**
Именно это, а не сохранение имени, требует ban #15.

### 2.3. Написание берётся НЕЙТРАЛЬНОЕ, а не имя службы

Соблазн — подставить имя продукта: `kaname-system`, `kaname-root`. Он отвергнут, и
отвергнут прецедентом недельной давности, а не вкусом.

Якорь кластера переехал с написания, несшего имя платформы, на **`cluster_root`**,
и миграция `20260906214500_cluster_anchor_moves_to_its_declared_spelling.sql`
называет довод дословно: якорь именует **УСТАНОВКУ**, а не службу, и «у
стороннего оператора ею не владеет ни платформа, ни служба, поэтому различитель в
имени лжёт в обе стороны».

> [!note] Прежнее написание якоря здесь названо ПРОЗОЙ, и это не стилистика
> Первая редакция этого абзаца цитировала его пролётом кода — и держатель
> `PRO-Robotech/kacho:TestClusterAnchorBeyondGoMatchesItsDeclaration` справедливо покраснел: цитата
> мёртвой координаты читается как живое утверждение, «служба спросит про один
> объект, а этот файл называет другой — молча». Исходов было два — прозаическая
> запись либо строка в ведомости держателя с точным числом; выбран первый, потому
> что предмет здесь и есть разбор прошлого решения, а не координата, по которой
> кто-то пойдёт. Тот же выбор связывает и всякую будущую правку этого документа.

Тот же довод покрывает шесть объектов этой приёмки целиком: системный аккаунт,
корневой кластер и встроенные роли принадлежат **установке**. Отсюда написания:

| было | становится | форма имени |
|---|---|---|
| `kacho-system` | `system` | ✓ `accounts_name_check` (RFC 1123) |
| `kacho-system.admin` | `system.admin` | первый сегмент `system` — НЕ член `authzmap.CatalogSeedModules` |
| `kacho-system.viewer` | `system.viewer` | то же |
| `kacho-bootstrap-admin` | `bootstrap-admin` | ✓ `service_accounts_name_check` |
| `kacho-bootstrap-soc` | `bootstrap-soc` | посевная строка, не имя строки БД |
| `kacho-root` | `root` | ✓ форма имени кластера |

**Три посевных ЗНАЧЕНИЯ (П6) берут написания по ТОМУ ЖЕ доводу — решение
2026-09-13, вместе с шагом 5.** Прежде решение §2 о них не высказывалось (его
предмет — восемь ИМЁН), и §7-П6 оставлял выбор преемнику. Выбор сделан здесь,
потому что §8 кладёт их в ту же миграцию:

| строка | было | становится | почему это написание |
|---|---|---|---|
| `users.email` | `system@kacho.local` | `system@system.invalid` | домен `.invalid` зарезервирован RFC 6761 и не разрешается НИКОГДА. Строка не почтовый ящик — и говорит это сама; прежняя несла бренд и при этом выглядела адресом, по которому кому-то можно написать. Форму проверяет `users_email_check` (`^…@…\.…$`), глобальную единственность — `users_identity_email_uniq` |
| `users.display_name` | `Kacho System (module SA owner)` | `System (module SA owner)` | снят ровно бренд; назначение строки («владелец служебных записей модулей») остаётся сказанным |
| `clusters.description` | `Root cluster for Kachō control plane` | `Root cluster of this installation` | тот же довод, что у имени кластера: объект принадлежит УСТАНОВКЕ, и у стороннего оператора ею не владеет ни платформа, ни служба |

**Отвергнуто — имя службы в почте** (`system@kaname.local`): §2.3 отвергает имя
службы по всему разрезу, и почта не исключение. **Отвергнуто — оставить как
есть:** назвать аккаунт `system`, оставив его владельца `Kacho System` с почтой
`system@kacho.local`, значит выполнить переход ровно НАПОЛОВИНУ — в той
половине, которую оператор видит первой.

**Форма нового имени роли допустима, и это проверено ограничением, а не на глаз.**
`roles_custom_name_check` (`0001_initial.sql:2503`) требует формы
`^[a-z][a-z0-9_]{0,40}$` **только от НЕсистемных** ролей: у системной проверка
выполняется первым дизъюнктом `is_system`. Поэтому точка в `system.admin`
законна ровно так же, как сегодня законна в `kacho-system.admin`.

**Членство первого сегмента проверено, а не предположено:** словарь имён модулей
закрыт шестью значениями — `iam`, `vpc`, `compute`, `loadbalancer`, `registry`,
`storage` (`schema/module-manifest.schema.json:17`), и ровно эти
шесть объявляют манифесты дерева — команда **монорепо**
(`grep -h '^module:' services/*/manifest.yaml | sort -u`, перепроверено
2026-09-11 на `origin/main` этой правкой — те же шесть значений);
`system` в нём нет. Это существенно: по признаку членства
`internal/apps/kaname/moduleroles/reconcile.go` решает, ждёт ли роль
манифеста модуля. Роль с первым сегментом-модулем ждала бы манифеста, которого не
существует, — **вечно**.

> [!note] Предикат членства взят у ЖИВОГО каталога, а не у координаты, которой нет
> Описание того же ключа схемы отсылает к `domain.IsKnownModule` как к канону
> набора. Координата в этой форме **не резолвится**, и это перемерено в обе
> стороны, а не выведено из одного отрицания:
>
> ```
> git grep -n 'func IsKnownModule' -- internal/domain                    → пусто, rc=1
> git grep -c 'domain\.IsKnownModule('  -- .                             → пусто, rc=1
> git grep -c 'func (.*) IsKnownModule' -- . ':!*_test.go' ':!docs/**'    → 3 файла
> ```
>
> То есть **пакетной функции нет** — её снял `#1927` вместе с литералом перечня,
> — а имя живёт **методом** интерфейса `domain.ModuleSet`, и реализуют его три
> места, читающие ЖИВЫЕ строки каталога. Сама схема свой предикат истечения
> объявила: «перечень истечёт вместе с `#1927` … предмет проверки меняется с
> „член платформенного набора“ на „объявлен каталогом“». Предикат сработал,
> перечень остался. Это **не предмет** этой приёмки; названо находкой по дороге,
> чтобы следующий не пошёл за координатой и не сделал вывод о дереве.
>
> Здесь поэтому названы schema-`enum` (шесть значений, сверено командой
> **монорепо** `grep -h '^module:' services/*/manifest.yaml | sort -u`) и
> `authzmap.CatalogSeedModules`, выводящий набор из состава каталога, — а не та
> координата.

**Оговорка, которую нельзя опустить.** Написание `system` в схеме уже стояло и
было заменено на `kacho-system` задачей `#1954`. Заменено по верной причине —
`const` объявлял невалидным каждый существующий манифест, — но причина была в
**схеме, разошедшейся с деревом**, а не в написании. Здесь возврат к `system`
идёт вместе с переводом самих манифестов, то есть без того расхождения. Это
названо прямо, чтобы следующий не прочёл возврат как отмену `#1954`.

### 2.4. Окно двух написаний — у ЧИТАТЕЛЯ, а не у строки

`accounts_name_unique UNIQUE (name)` глобален: двух аккаунтов с двумя написаниями
не бывает **by construction**. Значит окно живёт там, где имя ЧИТАЮТ:

- **применитель манифестов** принимает оба написания и резолвит их в одну строку
  (`WHERE name = ANY(...)` вместо `= $1`);
- **схема манифеста** объявляет два допустимых значения вместо одного `const`, и
  гейт согласия §0.3 продолжает держать обе стороны;
- **`PermissionsForRole`** принимает оба имени каждой из двух ролей.

**Окно ИСТЕКАЕТ САМО, и это часть решения, а не обещание.** Каждая запись окна —
послабление с предметом: когда в дереве не остаётся ни одного манифеста с прежним
написанием, запись **нечего прощать**, и это находка, а не тишина. Образец
механизма — ведомость послаблений гейта координат проб, которая сегодня пуста и
объявляет пустоту своей целью, а не поломкой.

### 2.5. Что решение НЕ решает — названо, а не умолчано

- **идентификатор клиента OAuth `kacho-bootstrap-admin` НЕ переименовывается этой
  приёмкой.** Он живёт у внешнего провайдера, а не в нашей базе; переход требует
  окна **у провайдера**, и его цена не измерена (§0.5). Имя остаётся, предмет
  выносится задачей-преемником;
- **пространство имён кластера не трогается** — 40 вхождений в 15 файлах, предмет
  `#2553`;
- **личности, которые выдаёт зонт, не трогаются** — 31 вхождение в 16 файлах;
- **схемные функции с приставкой платформы** не трогаются — их **семь**, и
  проверка меток (`kaname.kacho_labels_valid`) среди них
  (`KanameFoundationSchemaFunctions`, `internal/repohygiene/kanamenameresidue.go` —
  координата **монорепо**).
  Держатель ведомости остатка уже относит их к границе с доводом «решено
  остаться: один шаблон на шесть владельцев, заведена применённой миграцией» —
  число владельцев здесь **не перемерялось**, оно приведено как классификация
  держателя, а не как мой замер;
- **ведомость остатка не правится этой приёмкой.** Её ведёт `#2553`; строка полосы
  сдвинется, когда обе стороны сойдутся, и назовёт её владелец ведомости;
- **пометка `kacho:latent` ОСТАЁТСЯ, и это решение, а не умолчание** (`#2574`).
  Два держателя требуют сегодня противоположного: полоса «имя объекта» ведомости
  остатка (`PRO-Robotech/kacho:internal/repohygiene/kanamenameresidue.go`) числит
  пометку остатком, то есть требует её снять, а гейт написания
  (`PRO-Robotech/kacho:internal/repohygiene/latentmarkerspelling_test.go`) посажен
  с доводом, что она обязана остаться. Расхождение разрешается **в сторону гейта**,
  и доводов два, каждый самостоятельно достаточен:

  | довод | предикат |
  |---|---|
  | **критерий разреза эпика** (`#2076`) — увидит ли оператор, не открывая исходников? **Нет**: пометка есть комментарий внутри файла модели прав, её читает проба дерева и никто больше. Внутренняя координата по критерию **не переименовывается** | `git grep -n 'kacho:latent' -- . ':!docs/**'` → 7 вхождений в 3 файлах, из них 2 — сама пометка в блоке типа, 5 — распознаватель и объяснение |
  | **критерий владельца предмета** (`#2574`) — поставь службу одну, без платформы: кто заведёт объект, который имя называет? Пометка стоит на типе `vpc_address_pool`, домен **VPC**; её производителем стал бы манифест модуля платформы | `grep -n -B2 'kacho:latent — cluster-anchored' internal/authzmodel/fga_model.fga` → двумя строками выше `type vpc_address_pool` |

  **Распознавателей у пометки ТРИ, и один из них живёт здесь** —
  `internal/authzmap/verb_type_materializable_test.go` (`latentTypeMarker`);
  два других в монорепо. Решение «остаётся» не требует правки ни одного из трёх:
  оно требует **записи в перечне решённого остаться**, и эта запись принадлежит
  ведомости, то есть монорепо. Что из этого исполнимо отсюда, а что нет —
  §7-П5.

---

## 3. Отвергнутые альтернативы — с причиной, а не с предпочтением

**А1. Оставить всё как есть, записав 159 вхождений решённым остаться.**
Отвергнуто. Это законный исход (задача называет его прямо), и он был бы верен,
принадлежи объекты платформе. Но принадлежность уже установлена **стволом**:
применённая миграция `20260909202745` называет `kacho-bootstrap-admin` личностью
самой службы. Записать её имя «решённым остаться» значило бы завести два места об
одном предмете, из которых верно одно.

**А2. Переименовать, пересчитав идентификаторы по новому имени.** Отвергнуто
прямым запретом: ban #15, «операции смены id НЕ существует». Цена наблюдаемая:
`access_bindings_role_fk` и выданные права ссылаются на прежние идентификаторы —
пересчёт превратил бы их в висячие ссылки, а для арендатора это отказ в доступе,
которого у него не отзывали.

**А3. Снять деривацию целиком и пиннить ВСЕ идентификаторы.** Отвергнуто
замером: формула нужна там, где имя и идентификатор двигаются **вместе** —
служебные учётки модулей заводятся применителем манифеста, и их идентификатор
выводится из имени в момент вставки. Пин потребовал бы перечня, стареющего с
каждым новым модулем платформы, то есть ровно того, что корпус запрещает.

**А4. Сделать переход одним коммитом по шести файлам, без окна.** Отвергнуто
устройством доставки: манифесты чужих продуктов доставляет ОПЕРАТОР каталогом
доставки, а не наша сборка. Установка, где доставлен манифест прежней редакции, а
служба уже переведена, получила бы отказ применения на каждом модуле — то есть
поломку у того, кто ничего не менял.

**А5. Переименовать во `что-то с именем службы` (`kaname-system`).** Отвергнуто
прецедентом §2.3: объект принадлежит УСТАНОВКЕ, и различитель в имени лжёт в обе
стороны. Прецедент недельной давности, посажен в ствол, и заводить второе решение
об одной форме значит развести их молча.

---

## 4. Сценарии

Форма — Given-When-Then. У каждого «Тогда» назван производитель (§5).

### 4.1. Полоса миграции — перевод живых строк, три исхода

**KAN-SEED-1-01. Свежая установка рождается с объявленными именами.**
- **Дано:** пустая база; применяется вся цепочка миграций службы
- **Когда:** прогон завершился
- **Тогда:** `kaname.accounts` несёт ровно одну строку системного аккаунта с
  именем `system`; её `id` — **дословно** `acc1a18042d81fb438d6`, тот же литерал,
  что посеян `0001_initial.sql`. Роли называются `system.admin` и `system.viewer`,
  их `id` — `rol000000000sysadmin` и `rol000000000sysviewer`. Служебная учётка —
  `bootstrap-admin`, `id` — `svab91854890de887e6d`. Кластер — `root`, якорь —
  `cluster_root`

**KAN-SEED-1-02. Установка с прежними именами переводится, идентификаторы не двигаются.**
- **Дано:** база, поднятая ДО перехода: имена прежние, идентификаторы посеянные
- **Когда:** применяется новая миграция перевода
- **Тогда:** все шесть имён стали объявленными; **ни один идентификатор не
  изменился** — сверка литералов до и после дословная; перепись печатает число
  переведённых мест

**KAN-SEED-1-03. Повторное применение проходит молча (положительный контроль к -04).**
- **Дано:** база, уже переведённая
- **Когда:** миграция применяется снова
- **Тогда:** переводить нечего, миграция проходит, отказа нет ни одного. Без
  этого сценария отрицание `-04` зеленело бы на миграции, отвергающей всякое
  состояние

**KAN-SEED-1-04. ТРЕТЬЕ написание — ОТКАЗ, а не тишина.**
- **Дано:** база, в которой системный аккаунт назван написанием, не равным ни
  прежнему, ни объявленному
- **Когда:** миграция применяется
- **Тогда:** отказ; текст называет **оба** значения — найденное и объявленное, —
  иначе оператор не узнает, какое из двух менять. Молчание здесь означало бы
  «оставим базу с именем, которого код не знает», то есть отказ применения
  каждого манифеста модуля

**KAN-SEED-1-05. Перепись остатка прежнего написания печатает ОБЕ величины.**
- **Дано:** база после перевода
- **Когда:** спрашивается остаток прежнего написания по схеме
- **Тогда:** осмотрено мест — число; найдено — ноль. Одно число скрывает ровно
  тот случай, ради которого перепись заведена: переписчик, разошедшийся со
  схемой, вернул бы ноль тронутых и ноль остатка одинаково молча

### 4.2. Полоса применителя манифестов — окно двух написаний

**KAN-SEED-1-06. Манифест с ПРЕЖНИМ написанием применяется, пока окно открыто.**
- **Дано:** переведённая база; манифест модуля, объявляющий `seed.serviceAccounts[].account: kacho-system`
- **Когда:** манифест применяется
- **Тогда:** служебная учётка модуля заведена в единственном системном аккаунте;
  отказа нет. Это и есть предмет окна: оператор, доставивший манифест прежней
  редакции, не получает поломку

**KAN-SEED-1-07. Манифест с ОБЪЯВЛЕННЫМ написанием применяется (положительный контроль).**
- **Дано:** та же база; манифест объявляет `account: system`
- **Когда:** манифест применяется
- **Тогда:** тот же исход, та же строка. Без этого сценария `-06` доказывал бы
  лишь, что применитель принимает что угодно

**KAN-SEED-1-08. ТРЕТЬЕ написание отвергается, и отказ называет имя.**
- **Дано:** та же база; манифест объявляет аккаунт, которого нет
- **Когда:** манифест применяется
- **Тогда:** отказ; текст называет **имя, которое не резолвилось**. Вставка,
  чей `SELECT` не дал строк, прошла бы нулём затронутых строк и выглядела бы
  применённой — ровно тот класс, ради которого отказ и заведён

**KAN-SEED-1-09. Окно ИСТЕКАЕТ САМО: записи, которой нечего прощать, быть не должно.**
- **Дано:** дерево, в котором ни один манифест не несёт прежнего написания
- **Когда:** прогоняется гейт окна
- **Тогда:** находка — «прежнее написание больше не встречается, снимите запись
  окна». Послабление без предмета не истекает никогда и потому переживает свою
  причину

### 4.3. Полоса ролей — имя как ключ диспетчеризации

**KAN-SEED-1-10. Право супер-роли по ОБЪЯВЛЕННОМУ имени — прежнее.**
- **Дано:** реестр прав
- **Когда:** спрашивается право роли `system.admin`
- **Тогда:** `["*.*.*.*"]` — дословно то же, что сегодня отдаётся имени
  `kacho-system.admin`

**KAN-SEED-1-11. То же по ПРЕЖНЕМУ имени, пока окно открыто.**
- **Дано:** тот же реестр
- **Когда:** спрашивается право роли `kacho-system.admin`
- **Тогда:** тот же ответ. Иначе установка, переведённая раньше своих читателей,
  теряет супер-доступ — и теряет его молча, потому что разбор `switch` на
  неизвестном имени возвращает пустое право, а не отказ

**KAN-SEED-1-12. Первый сегмент нового имени НЕ член набора модулей.**
- **Дано:** роль `system.admin` без модуля-владельца
- **Когда:** сверка ролей ищет расхождения
- **Тогда:** роль **не** числится ждущей манифеста модуля. Признак — членство в
  наборе модулей платформы, а не число сегментов; `system` в наборе нет.
  Утверждение о НЕИЗМЕННОСТИ: сегодня то же верно для `kacho-system`

**KAN-SEED-1-13. Идентификаторы ролей не сдвинулись ни на символ.**
- **Дано:** база после перевода
- **Когда:** читаются обе системные роли
- **Тогда:** `rol000000000sysadmin` и `rol000000000sysviewer` — дословно.
  Утверждение о неизменности: смена имени роли идентификатора не двигает, потому
  что он рукописный, и это единственная причина, по которой класс B дешевле C

### 4.4. Полоса неизменного — что НЕ должно сдвинуться

**KAN-SEED-1-14. Идентификатор аккаунта и владельца — прежние литералы.**
- **Дано:** база после перевода
- **Когда:** читаются системный аккаунт и его пользователь-владелец
- **Тогда:** `acc1a18042d81fb438d6` и `usr1a18042d81fb438d6`. При этом
  `substr(md5('system'),1,17)` этим значениям **не равен** — и именно поэтому
  деривация заменяется пином (§2.2), а не «поправляется»

**KAN-SEED-1-15. Идентификаторы учётки и отображения OAuth — прежние литералы.**
- **Дано:** та же база
- **Когда:** читаются служебная учётка и её отображение на клиента провайдера
- **Тогда:** `svab91854890de887e6d` и `soc_db27d17291ff453b6`. Отображение —
  один к одному, и его идентификатор служит ключом ключа подписи у провайдера:
  сдвинув его, мы отозвали бы подпись, которую сами же и выпустили

**KAN-SEED-1-16. Идентификатор клиента у провайдера ОСТАЁТСЯ прежним.**
- **Дано:** та же база и живой провайдер
- **Когда:** чеканится неинтерактивный токен
- **Тогда:** клиент называется `kacho-bootstrap-admin` — то же, что сегодня.
  Утверждение о НЕИЗМЕННОСТИ и одновременно объявление остатка: переход у
  провайдера — предмет задачи-преемника (§7), а не побочный эффект этой

**KAN-SEED-1-17. Пространство имён кластера и личность зонта не тронуты.**
- **Дано:** дерево после перевода
- **Когда:** считаются вхождения по обеим границам
- **Тогда:** пространство имён — **40** в **15** файлах, личность зонта — **31**
  в **16**; оба числа не изменились. Граница с `#2553` и с зонтом доказывается
  числом, а не намерением

### 4.5. Полоса витрины остатка

**KAN-SEED-1-18. Снятое видно СВОИМ предикатом, а не чужой ведомостью.**
- **Дано:** дерево после перевода
- **Когда:** прогоняется предикат §0.1
- **Тогда:** вхождений предмета — **0** при непустом объёме осмотренного;
  вхождений на границах — прежние. Строка полосы чужой ведомости при этом
  сдвинется своим изменением, у которого свой владелец: до перевода
  платформенной стороны она не отличит снятое здесь от вечного

---

## 5. Сценарий → производитель

Раздел и есть предикат проверяемости: рецензент идёт по нему и отдельно по
каждому сценарию, которого в нём нет.

| сценарий | производитель «Тогда» | есть в дереве |
|---|---|---|
| `-01` | миграции перевода (`20260913114722` · `20260913143007` · `20260913144108`) + пиннутые литералы `ids.go` | ✅ **производится**; снято опытом — `TestClusterName_SeedNoLongerNamesThePlatform`, `TestSystemRoleNames_SeedNoLongerNamesThePlatform`, `TestSeedIdentity_NamesNoLongerNameThePlatform` |
| `-02` | те же миграции: адресуют строку ИДЕНТИФИКАТОРОМ, имя читают по факту | ✅ **производится**; `TestSeedIdentity_PinnedIDsStillAddressTheirRows`, `TestSystemRoleNames_GrantsDoNotMoveWithTheName` |
| `-03`, `-04` | три исхода в теле каждой миграции: прежнее → перевод, объявленное → молча, ТРЕТЬЕ → `RAISE EXCEPTION` | ✅ **производится обеими ветвями, снято опытом.** Накат цепочки на свежую базу проходит ТОЛЬКО ветвь «прежнее → перевод»: значения там прежние, повторного наката goose не делает, третьего написания между сводом и переводом взяться неоткуда. Поэтому тело наката исполняется НАПРЯМУЮ поверх применённой цепочки — `TestSeedIdentity_SecondApplicationIsSilent` (`-03`), `TestSeedIdentity_ThirdSpellingIsRefused` и `TestSystemRoleNames_ThirdSpellingIsRefused` (`-04`, по одной на каждую миграцию: тела разные). Способность упасть доказана инъекцией в тело миграции, по одной на ветвь: снятый отказ краснеет ТОЛЬКО пробой третьего написания, отказ на объявленном — ТОЛЬКО пробой повтора; близнец в обоих прогонах зелен |
| `-05` | перепись §0.1, ведро «ПРЕДМЕТ: живой» + ведро «ПРЕДМЕТ: свод и миграции» | ✅ **производится ОБЕИМИ величинами** после разреза (§7-П2); печатается всегда, а не при находке |
| `-06`, `-07` | `moduleSeedWriter.resolveBySpellings` — резолв по окну с явной проверкой кардинальности | ✅ **производится**; `TestModuleSeedApplier_WindowResolvesBothSpellingsToOneRow` на живой Postgres |
| `-08` | та же ветвь отказа: текст называет присланное имя | ✅ **производится**; `TestModuleSeedApplier_WindowDoesNotWidenToAThirdName` утверждает и отказ, и то, что он называет имя |
| `-09` | гейт истечения окна | ⛔ **НЕТ держателя, и это сказано прямо** (§6). Окно снимается шагом 7 §8, который блокирован П3; гейт, краснеющий раньше П3, краснел бы на верном состоянии |
| `-10` | `PermissionRegistry.PermissionsForRole` через `domain.SeedIdentityDeclared` | ✅ **производится**; `TestPermissionsForRole_WindowAcceptsBothSpellings` сверяет РАВЕНСТВО двух ответов |
| `-11` | тот же разбор, вторая ветвь окна | ✅ **производится** тем же прогоном; контроль — `_ThirdSpellingIsNotAccepted` |
| `-12` | `authzmap.CatalogSeedModules` + сверка `moduleroles/reconcile.go` | ✅ производится; объявленное имя `system.*` первым сегментом в набор модулей не входит |
| `-13` | `domain.SystemAdminRoleID`, `domain.SystemViewerRoleID` (`constants_extended.go`) | ✅ **производится**; `TestSystemRoleNames_GrantsDoNotMoveWithTheName` адресует строку этими литералами после перевода |
| `-14` | пин `pinnedBootstrapSvaID` · `pinnedSystemOwnerUserID` + посев свода | ✅ **производится**; `TestDeriveIdentity_IDsArePinnedToTheAppliedBaseline` (источник ожидаемого — текст свода, а не второй расчёт) |
| `-15` | пин `pinnedBootstrapSocID`; строку отображения заводит путь запроса, свод её не сеет | ✅ **производится**; форма утверждается той же пробой, адресация — `TestSeedIdentity_PinnedIDsStillAddressTheirRows` |
| `-16` | `bootstrapClientID` (`ids.go`) | ✅ — утверждается НЕИЗМЕННОСТЬ; написание остаётся прежним намеренно (П1) |
| `-17` | границы переписи §0.1: вёдра «остаётся: ns» и «остаётся: зонт» | ✅ **производится**; после всех трёх классов обе величины не сдвинулись (13 · 2 ф. и 9 · 5 ф.) |
| `-18` | предикат §0.1 — `…/acceptance/seed-identity-names-its-own-service.py` | ✅ в дереве, зовётся гейтом `TestSeedIdentityCensusMatchesItsAcceptance` (перепись приёмки, не гейт: сам ничего не роняет) |

**Перепись держателей после посадки классов A · B · C, и она печатает ОБЕ
величины:** сценариев **18** · производится целиком **17** · без держателя **1**
(`-09` — гейт истечения окна). Остаток назван поимённо выше и за исполненный не
выдан. Прежняя редакция этой строки называла **16 и 2** и утверждала, что `-03`
снят прогоном цепочки, — неверно: цепочка ветвь повтора не проходит вовсе, и
держатель у `-03` появился только отдельной пробой.

**Ни один сценарий не остался без строки.** Утверждения о **неизменности**
(`-13`, `-16`, `-17`) существуют затем, чтобы переход не сдвинул того, чего не
должен, и краснеют, если сдвинет.

---

## 6. Чем держится

| свойство | держит | предикат |
|---|---|---|
| идентификаторы не двигаются | пин литералом + закрытый перечень посеянного с гейтом предмета `TestSeededIDExceptionsHaveASubject` (**этот репозиторий**, `internal/check`) | сверка литерала до и после |
| формула деривации объявлена один раз | `TestDeterministicIDDerivationIsDeclaredOnce` (**этот репозиторий**, `internal/check/derived_id_single_source_test.go`) | `go test ./internal/check/ -count=1 -run TestDeterministicIDDerivationIsDeclaredOnce` → PASS, перепись «не-тестовых файлов Go разобрано 798 · объявлений импорта 4574 · импортов `crypto/md5` найдено 1» |
| схема и манифесты не расходятся | `TestSchemaConstIsNotContradictedByTheTreeManifests` (`internal/manifest`) — **вердикта ОТСЮДА не выносит**, см. абзац ниже | `go test ./internal/manifest/ -count=1 -run TestSchemaConstIsNotContradictedByTheTreeManifests -v` → **SKIP**, «УСЛОВИЕ НЕ СОЗДАНО: дерева платформы рядом с модулем нет» |
| выдача не переезжает между ролями | `TestNoMigrationMovesGrantsBetweenRoles` (**этот репозиторий**, `internal/migrations/grant_removal_trace_test.go`) | `go test ./internal/migrations/ -count=1 -short -run TestNoMigrationMovesGrantsBetweenRoles` → PASS, перепись «файлов корпуса 10 · операторов, называющих таблицу выдач 39 · переставляющих `role_id` 0» |
| окно истекает само | **этого держателя по-прежнему НЕТ, и это сказано прямо** | обход дерева на прежнее написание; предикат готовности к снятию — ведро «ПРЕДМЕТ: живой» переписи §0.1 (§8 шаг 7). Сегодня окно держится ПОРЯДКОМ РАБОТ, а не проверкой: гейт, краснеющий раньше П3, краснел бы на верном состоянии |
| имя кластера перешло, якорь не сдвинулся | `TestClusterName_SeedNoLongerNamesThePlatform` · `_AnchorDoesNotMoveWithTheName` · `_IsAcceptedByItsOwnConstraint` (**этот репозиторий**, `internal/migrations`) | `go test ./internal/migrations/ -count=1 -run TestClusterName_` → **PASS ×3** на живой Postgres (цепочка из 13 миграций) |
| имена ролей перешли, выдача не двинулась | `TestSystemRoleNames_SeedNoLongerNamesThePlatform` · `_GrantsDoNotMoveWithTheName` · `_AreAcceptedByTheirOwnConstraints` (**этот репозиторий**, `internal/migrations`) | `go test ./internal/migrations/ -count=1 -run TestSystemRoleNames_` → **PASS ×3**; третья утверждает ОБЕ стороны ограничения формы (объявленное имя НЕсистемной форме не отвечает — значит дизъюнкт `is_system` здесь и решает) |
| имена аккаунта и учётки перешли, пиннутые координаты адресуют строки | `TestSeedIdentity_NamesNoLongerNameThePlatform` · `_PinnedIDsStillAddressTheirRows` (**этот репозиторий**, `internal/migrations`) | `go test ./internal/migrations/ -count=1 -run TestSeedIdentity_` → **PASS**; вторая спрашивает не «как называется строка», а «адресует ли пиннутый идентификатор строку с объявленным именем» — это и есть ban #15, снятый опытом |
| три посевных ЗНАЧЕНИЯ не называют платформу (П6) | `TestSeedIdentity_SeededProseNoLongerNamesThePlatform` (**этот репозиторий**, `internal/migrations`) | то же → **PASS**, перепись «значений прочитано 3 · образцов бренда 3». Ищет ОБРАЗЕЦ (`Kacho` · `Kachō` · `kacho`), а не три известные строки: перечень пережил бы появление четвёртой молча |
| идентификаторы пиннуты, а не выведены из имени | `TestDeriveIdentity_IDsArePinnedToTheAppliedBaseline` + законный близнец `_FormulaStaysWhereNameAndIDMoveTogether` (**этот репозиторий**, `internal/apps/kaname/api/bootstrap_token`) | `go test ./internal/apps/kaname/api/bootstrap_token/ -short -count=1 -run TestDeriveIdentity_` → **PASS**, перепись «свода прочитано 322351 байт · пиннутых идентификаторов сверено 2». Способность упасть доказана инъекцией: возврат формулы на объявленном написании даёт красное с координатой |
| второй и третий исход миграций перевода | `TestSeedIdentity_SecondApplicationIsSilent` · `TestSeedIdentity_ThirdSpellingIsRefused` · `TestSystemRoleNames_ThirdSpellingIsRefused` (**этот репозиторий**, `internal/migrations`) | `go test ./internal/migrations/ -count=1 -run 'SecondApplication\|ThirdSpelling'` → **PASS ×3** на живой Postgres. Тело наката исполняется напрямую поверх цепочки: сама цепочка эти ветви не проходит. Инъекция в тело по одной на ветвь краснеет только своей пробой |
| окно резолвит оба написания в ОДНУ строку | `TestModuleSeedApplier_WindowResolvesBothSpellingsToOneRow` + положительный контроль `_WindowDoesNotWidenToAThirdName` (**этот репозиторий**, `internal/repo/kaname/pg`) | `go test ./internal/repo/kaname/pg/ -count=1 -run TestModuleSeedApplier` → **PASS ×5** на живой Postgres |
| разбор прав принимает оба написания роли | `TestPermissionsForRole_WindowAcceptsBothSpellings` + контроль `_ThirdSpellingIsNotAccepted` (**этот репозиторий**, `internal/apps/kaname/seed`) | `go test ./internal/apps/kaname/seed/ -short -count=1 -run TestPermissionsForRole_` → **PASS**. Сверяется РАВЕНСТВО двух ответов, а не непустота каждого |
| схема принимает оба написания, и распознаватель их ВИДИТ | `TestSchemaConstAuditAcceptsEitherSpellingOfAWindow` · `_RedensOnAValueOutsideTheWindow` · `TestSchemaWalkReadsTheEnumForm` · `_NamesAnEnumItCannotJudge` (**этот репозиторий**, `internal/manifest`) | `go test ./internal/manifest/ -short -count=1 -run TestSchema` → **PASS**. Замер расширения распознавателя: требований к значению стало **14** вместо **7** при неизменной полосе прежней формы — то есть семь требований схемы были ВНЕ наблюдения (`testing.md` §«Гейт на класс», п. 7) |
| остаток виден числом | предикат §0.1, посажен рядом с приёмкой (`seed-identity-names-its-own-service.py`) | прогон печатает ПЯТЬ вёдер из этого репозитория (шестое, «манифесты чужих продуктов», отсюда не измеримо) и объём осмотренного; отказ кодом 2 на беспредметности доказан инъекцией в обе стороны |
| числа §0 не расходятся с этим предикатом МОЛЧА | `TestSeedIdentityCensusMatchesItsAcceptance` (**этот репозиторий**, `internal/check/seed_identity_census_test.go`) — зовёт предикат и сверяет его вывод с объявлением по КАЖДОМУ ведру | `go test ./internal/check/ -count=1 -run TestSeedIdentityCensusMatchesItsAcceptance -v` → PASS; перепись печатается прогоном и здесь НЕ переписывается — объём обхода растёт с каждым файлом дерева, и вписанная величина разошлась бы молча (она попадает в объявленную слепую зону самого гейта: он судит блок §0, а не прозу вокруг). Способность упасть доказана инъекцией в обе стороны — `grep -c '^func TestSeedCensusInjection' internal/check/seed_identity_census_injection_test.go` → **7**, из них одна идёт настоящим входом |

> [!important] Здесь стояло, что двух держателей НЕТ, — и это было неправдой
> о собственном дереве
> Прежняя редакция объявляла `TestDeterministicIDDerivationIsDeclaredOnce` и
> `TestNoMigrationMovesGrantsBetweenRoles` снятыми withdrawal-волной, свойство —
> держащимся «ТОЛЬКО прочтением кода», а заведение держателя — предметом
> ОТДЕЛЬНОЙ задачи. Все три утверждения ложны: оба гейта живут **в этом
> репозитории**, оба зелены, и завёл их сюда `c865e539` («гейты службы, снятые из
> монорепо — 33 держателя переехали»). Команды и переписи стоят в таблице выше.
>
> **Цена этой ошибки была не формальной, и назвать её надо прямо.** Объявленная
> «отдельная задача» БЕСПРЕДМЕТНА, а исполнитель, пошедший по ней, завёл бы
> ВТОРОЙ держатель одного свойства — ровно то, что этот документ запрещает
> соседним абзацем. Отдельно: §2.2 п. 2 того же документа трактовал тот же гейт
> ЖИВЫМ («единственность объявления, **которую держит** …»), то есть два места об
> одном предмете уже разошлись, и неверным было §6.
>
> **Класс назван, потому что он повторится.** Утверждение «держателя нет» было
> верно на ревизии, когда писалось, и пережило свой предмет: гейты переехали
> отдельной волной, а строка §6 осталась. Держателя у самой строки нет —
> `TestAcceptanceProbeCoordinateResolves` судит ДОМ координаты, а не то, жив ли
> названный ею гейт. Проверяется единственным способом: **прогнать каждый гейт,
> названный §6, и вписать его исход**, а не его имя.

**Один из шести держателей ОТСЮДА вердикта не выносит, и это сказано строкой
таблицы, а не подразумевается.** `TestSchemaConstIsNotContradictedByTheTreeManifests`
сверяет объявление схемы с манифестами модулей по образцу `services/*/manifest.yaml`
(`internal/manifest/schemaconst_internal_test.go`, `treeManifestGlob`). Каталога
`services/` в этом репозитории нет (`ls -d services` → отказ), ручки, включающей
полосу, у гейта нет вовсе, — значит здесь он **всегда** SKIP, «условие не
создано». Сам гейт при этом построен верно: он отличает несозданное условие от
находки и не выдаёт первое за второе.

**Что из этого следует для §8 шага 4** — того самого, где окно заводится в схеме
и в применителе: прогон пакета напечатает `ok`, и принять это за «два объявления
сверены» **нельзя** — сверено не было ничего. Вердикт по этой оси выносится
ТОЛЬКО из монорепо, где лежат пять манифестов; отсюда — третья категория, и в
успех она не засчитывается.

**Чего НЕ держит ничто, и это сказано прямо:** правильность выбранного написания.
«`system` лучше, чем `kacho-system`» машинного предиката не имеет — держится
доводом §2.3 и прецедентом якоря кластера, то есть решением, а не проверкой.

---

## 7. Остаток и задачи-преемники

| # | предмет | почему не здесь | предикат снятия |
|---|---|---|---|
| П1 | переход идентификатора клиента у внешнего провайдера | цена не измерена: живой провайдер не спрашивался (§0.5) | клиент у провайдера отвечает на объявленное написание, прежнее снято |
| П2 | строка полосы «имя объекта» в ведомости остатка | ведомость чужая; предпосылка ИЗМЕНИЛАСЬ дважды с заведения — `#2553` **закрыт** (2026-09-10, платформенная сторона переехала в границы, ведомость 638→193), затем withdrawal-волна вычистила `services/iam` из монорепо, и предмет службы стал этим ведомостям НЕВИДИМ вовсе. **ПЕРЕМЕРЕНО 2026-09-13 (волна 3), и невидимость подтверждена прогоном, а не выведена:** держатель зелён, полоса «имя объекта» печатает 10 · 5 ф. при прощено 0, весь долг — 189 на 8 полосах, поверхность службы в индексе монорепо — 0 файлов (врезка §0.2). Значит **П2 закрывается не правкой ведомости, а её неприменимостью**: пункт остаётся только ради второй, здешней половины — пары величин справа | **ПАРА величин, а не одна** (предикат переписан 2026-09-13, разбор ниже): перепись §0.1, позванная ЗДЕСЬ, даёт по ведру **«ПРЕДМЕТ: живой» 0 вхождений** И по ведру **«ПРЕДМЕТ: свод и миграции» НЕ ноль**, при непустом объёме осмотренного (`python3 docs/engineering/acceptance/seed-identity-names-its-own-service.py`; «прочитано 0» — отказ кодом 2, а не снятие). **И ЭТА ПАРА НЕДОСТИЖИМА КАК НАПИСАНА — измерено 2026-09-13, врезка «ЧТО СТОИТ В ВЕДРЕ ЖИВОГО» ниже:** ноля не даст ни один порядок работ, пока в ведре стоят 9 вхождений цитаты прежнего написания как ИСТОРИИ и 1 координата у внешнего провайдера. Замена предиката — **предмет отдельного круга**, а не правки задним числом (прецедент этого же документа: так же поступили с прежним П2); вынесено находкой |
| П3 | перевод пяти манифестов чужих продуктов | их правит не Kaname; окно §2.4 существует именно затем, чтобы перевод шёл своим порядком | вхождений прежнего написания в манифестах дерева — 0 |
| П6 | ✅ **ИСПОЛНЕН 2026-09-13** — три посевных ЗНАЧЕНИЯ, называвших платформу прозой | переведены ТОЙ ЖЕ миграцией, что имена аккаунта и учётки (`20260913144108_…`), как и велит §8: они принадлежат тем же двум строкам свода, и разносить их по двум миграциям значило бы дважды трогать одну строку ради одного перехода | предикат ИСПОЛНЕН и снят опытом, а не чтением: `TestSeedIdentity_SeededProseNoLongerNamesThePlatform` читает три значения из живой базы после всей цепочки и ищет в них образцы `Kacho` · `Kachō` · `kacho`; перепись печатает «значений прочитано 3 · образцов бренда 3», то есть «ноль находок» отличимо от «ноль прочитанного». Написания и довод — §2.3 |
| П4 | сведение двух единиц счёта (**132** монорепо `7cd4cc8355` · **130** здесь `3178ed7c65` · **120** задачи) | работа владельца ведомости, а не этой приёмки (§0.5). **ПЕРЕМЕРЕНО 2026-09-13 (волна 3): сводить стало НЕЧЕГО, и это не исполнение пункта, а исчезновение его предмета.** Величина монорепо производилась обходом поверхности, в которой дерева службы больше нет (0 файлов), — значит вторая единица счёта об ЭТОМ предмете не производится вовсе, ни верной, ни неверной | обе величины получены одним предикатом — **исполнимой осталась одна**: перепись §0.1 зовётся файлом и мерит это дерево. Пункт снимается вместе с ведомостью, а не сведением: сводить единицу с величиной, у которой нет производителя, значит сравнивать число с памятью о числе |
| П5 | запись пометки `kacho:latent` в перечень решённого остаться (`#2574`) | **ПЕРЕМЕРЕНО 2026-09-13 — из этого репозитория НЕИСПОЛНИМО, и это установлено, а не предположено.** Оба держателя расхождения живут в `PRO-Robotech/kacho`: ведомость остатка (`internal/repohygiene/kanamenameresidue.go`) и гейт написания (`internal/repohygiene/latentmarkerspelling_test.go`). Предикат отсутствия копии здесь: `ls internal/repohygiene` → отказа нет каталога; `git ls-files \| grep -c 'kanamenameresidue\|latentmarkerspelling'` → **0**. Решение и оба его довода приняты §2.5. **ПЕРЕМЕРЕНО 2026-09-13 (волна 3): правок требуется НОЛЬ, а не две, и это установлено прогоном.** `kacho:latent` в дереве монорепо — **7 вхождений в 5 файлах**, все вне `KanameSurface`: `internal/repohygiene` (2 ф.) и `services/vpc` (3 ф.). Обход ведомости их не видит, полоса «имя объекта» стоит на 10 · 5 ф. при прощено 0, держатель зелён — то есть **ветвь предиката «снята из полосы долга» ВЫПОЛНЕНА**, и выполнена не правкой, а уходом предмета из поверхности. **Запись в `KanameNameResidueStay` теперь заводить НЕЛЬЗЯ, и это довод, а не пропуск:** ведомость решённого остаться истекает сама — «запись, которой нечего прощать, есть находка» (её собственная шапка), — а запись о вхождениях, которых обход не находит, есть послабление без предмета и покраснела бы первым же самоистечением. «Висит в обеих ролях» больше не наступает: у ведомости предмета нет, у гейта написания он жив (`services/vpc/manifest.yaml` в монорепо) | **ПЕРЕПИСАН ПО ЗАМЕРУ:** полоса «имя объекта» пометку не числит (10 · 5 ф., прощено 0, держатель зелён), гейт написания её стережёт, записи в `KanameNameResidueStay` **нет и не заводится**; ни один из трёх распознавателей не тронут. Прежняя формулировка требовала записи в ведомости — она отозвана тем же замером, что закрыл первую половину: требовать запись о невидимом вхождении значит заводить послабление без предмета. **Здешняя половина предиката ВЫПОЛНЕНА и перемерена:** `git grep -n 'kacho:latent' -- . ':!docs/**'` → **7 вхождений в 3 файлах** (два — сама пометка в блоке типа канонической модели и её побайтовой копии, пять — распознаватель и объяснения); пометка стоит на типе `vpc_address_pool` (`grep -n -B3 'kacho:latent' internal/authzmodel/fga_model.fga` → двумя строками выше `type vpc_address_pool`), то есть предпосылка решения §2.5 держится, и трогать здесь нечего |

> [!important] ЧТО СТОИТ В ВЕДРЕ ЖИВОГО — разобрано ПООБЪЕКТНО 2026-09-13, и разбор
> ОПРОВЕРГ предикат П2 второй раз
> Ведро «ПРЕДМЕТ: живой» объявлено тем, «что дерево сегодня ПИШЕТ или ЧИТАЕТ как
> значение», и обязано дойти до нуля. Классификация всех его вхождений на базе
> линии — тем же предикатом, с адъюдикацией по виду строки:
>
> | класс | вх. | ф. | чем снимается |
> |---|---:|---:|---|
> | проба и фикстура перехода | 86 | 35 | шаг 7 §8 (перевод на ОБРАЗЕЦ бренда) |
> | опубликованная схема, `enum` из пары | 6 | 1 | шаг 7 §8 |
> | окно двух написаний | 5 | 1 | шаг 7 §8 |
> | **цитата прежнего написания как ИСТОРИИ** | **9** | **7** | **ничем** |
> | **координата у ВНЕШНЕГО провайдера** | **1** | **1** | **П1, не шаг 7** |
> | живое ЗНАЧЕНИЕ прод-кода ВНЕ трёх мест выше | **0** | 0 | — снято классами A · B · C |
>
> **Последняя строка и есть исход полосы:** живого значения имени платформы вне
> трёх объявленных мест в прод-коде **не осталось ни одного**. Именно это полоса
> и должна была дать, и это её проверяемый результат — в отличие от числа ведра,
> которое к нулю не идёт.
>
> **Две строки жирным делают ноль недостижимым, и ни одна не названа ни в П2, ни в
> §8.** Девять вхождений цитаты — комментарии прод-кода (7 · 6 ф.) и проза
> `docs/engineering/architecture/known-divergences.md` (2 · 1 ф.) вида «свод посеял
> её под именем …»: снять оттуда прежнее написание значит сделать запись ЛОЖНОЙ —
> тот же довод, по которому вечно замёрзшим объявлен свод (ban #5). Одно вхождение
> — `bootstrapClientID`, идентификатор клиента у внешнего провайдера: его написание
> §2.4 прямо объявляет неизменным, а переход — предметом П1, чья цена не измерена.
>
> **Почему это записано находкой, а не исправлено здесь.** Правка ведра есть замена
> ПРЕДИКАТА ГОТОВНОСТИ, а не текста; этот же документ уже отказался вносить такую
> замену задним числом под чужой вердикт (первая врезка шапки) и вынес её кругу.
> Второй раз поступать иначе значило бы, что довод действовал ровно один раз.
> **Форма замены названа, чтобы круг не начинался с нуля:** разрез идёт по тому же
> механическому признаку, что уже применён к своду, — по признаку, делающему
> вхождение вечным. У свода это путь применённой миграции; здесь — строка, которая
> не может быть значением (комментарий, проза вне кодового блока), и координата,
> принадлежащая третьей стороне.

**Ни один пункт не есть отсрочка (ban #11):** у каждого назван предмет, владелец
и предикат снятия. Пункт без предиката был бы маркером, за которым никто не
отвечает.

---

## 8. Порядок работ после APPROVED

**Состояние каждого шага названо ЗДЕСЬ, а не выводится из прозы.** Шагов семь, а
не восемь: число проверяется перечнем ниже.

1. ✅ **класс A** (2026-09-13) — миграция переименования кластера
   (`20260913114722_…`). Самостоятельна, окна не требует, читателей имени в
   прод-коде ноль;
2. ✅ **класс B** (2026-09-13) — окно в разборе прав
   (`PermissionsForRole` через `domain.SeedIdentityDeclared`), затем миграция
   переименования двух ролей (`20260913143007_…`). Идентификаторы рукописные и
   пиненные, поэтому перевод не тронул ни одной выдачи — снято опытом
   (`TestSystemRoleNames_GrantsDoNotMoveWithTheName`);
3. ✅ **класс C, шаг 1** (2026-09-13) — пин идентификаторов литералами взамен
   деривации, БЕЗ переименования (`bootstrap_token/ids.go`). Изменение
   самостоятельно зелено и ничего не двинуло;
4. ✅ **класс C, шаг 2** (2026-09-13) — окно двух написаний у применителя
   манифестов (`moduleSeedWriter.resolveBySpellings`) и в схеме (`enum` из пары).
   Распознаватель схемы научен форме `enum` ТЕМ ЖЕ изменением — иначе требование
   ушло бы из-под наблюдения молча;
5. ✅ **класс C, шаг 3** (2026-09-13) — миграция перевода имён аккаунта и
   учётки, и вместе с ней П6 (`20260913144108_…`);
6. ⛔ **класс C, шаг 4** — перевод пяти манифестов чужих продуктов (П3).
   **ИЗ ЭТОГО РЕПОЗИТОРИЯ НЕИСПОЛНИМ by construction:** пять `manifest.yaml`
   лежат в `PRO-Robotech/kacho`, в этом дереве их нет физически, и предикат §0.1
   их не измеряет вовсе (ведро «манифесты» печатает `0 · 0 ф.` со строкой «НЕ
   измеримо»). Окно §2.4 существует ровно затем, чтобы перевод шёл своим
   порядком: до него манифест с прежним написанием резолвится в ту же строку;
7. ⛔ **закрытие окна** — БЛОКИРОВАНО шагом 6 и требует ТРЁХ действий, а не
   одного. Снять объявление окна (`internal/domain/seed_identity_window.go`) ·
   свернуть `enum` схемы обратно в `const` объявленного написания · перевести
   пробы перехода с литерала прежнего написания на ОБРАЗЕЦ бренда (образец уже
   написан — `TestSeedIdentity_SeededProseNoLongerNamesThePlatform`). Признак
   готовности к шагу — ведро «ПРЕДМЕТ: живой» переписи §0.1 состоит ТОЛЬКО из
   этих трёх мест и фикстур соседних предметов.
   **ЧЕТВЁРТОЕ МЕСТО НАЗВАНО ЗДЕСЬ ЯВНО, и шаг его НЕ снимает** (перемерено
   2026-09-13): `bootstrapClientID` — координата у внешнего провайдера, её предмет
   П1. Прежняя редакция признака говорила «только из этих трёх мест», и по ней
   исполнивший шаг 7 читал бы остаток как свою недоделку. Пятое — девять вхождений
   цитаты прежнего написания как истории — не снимается вовсе; обе величины и их
   довод — врезка «ЧТО СТОИТ В ВЕДРЕ ЖИВОГО» §7.

**Место П6 в этом порядке — рядом с шагом 5, и это решение, а не пропуск.**
ИСПОЛНЕНО вместе с шагом 5; написания и их довод — §2.3. Три
посевных ЗНАЧЕНИЯ (§0.5) переводятся ТОЙ ЖЕ миграцией, что имена аккаунта и
учётки: они принадлежат тем же двум строкам свода (`users` — почта и
отображаемое имя владельца аккаунта, `clusters` — описание), и разносить их по
двум миграциям значило бы дважды трогать одну строку ради одного перехода.
Отдельной задачей П6 остаётся потому, что решение §2 о них не высказывается —
его предмет восемь ИМЁН, — а не потому, что работа откладывается.

**Асимметрия с П3 названа, чтобы её не приняли за недосмотр:** П3 стоит
отдельным шагом 6, потому что его правит НЕ Kaname (манифесты пяти чужих
продуктов), и его нельзя внести тем же изменением. П6 правит Kaname, и он
вносится вместе с шагом 5.

Шаги 3 и 4 переставлять нельзя: окно, открытое до пина, приняло бы имя, из
которого идентификатор не выводится, при живой деривации — то есть завело бы
строку, которую формула не находит.
