<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка: отзыв клиентом назван в журнале семейства своей причиной

- **Статус:** DRAFT — редакция 1. Кругов ревью не было: вердикта нет, санкции на код нет
  (ban #1). Вердикт выносит `acceptance-reviewer`
- **Статическая форма:** DRAFT. Слово выше вписано и не означает вердикта. Действующее
  одобрение выводится из записи ревью по отпечатку редакции и из внешнего события, на
  которое запись ссылается (`change-graph.md` §2 «Вердикт привязан к ОТПЕЧАТКУ, а не к
  документу»). Слово DRAFT после ревью не правится. Номер редакции в строке статуса
  правится тем же изменением, что и тело
- **История ревью:** только дописывается строками вида «дата · круг · вердикт · SHA-256
  редакции · путь записи»; прежние строки не правятся и не удаляются. Строк пока нет
- **Журнал редакций:** только дописывается
  - **ред. 1 · 2026-09-26** — первая редакция
- **Дата:** 2026-09-26
- **Задача:** `PRO-Robotech/kaname#406`. Волна `#366`, эпик `#357`, цель линии —
  `PRO-Robotech/kacho#2564`. Приёмка держит миграцию слова (`#406`), условие 2 сборки
  церемонии (`#407`) и через неё задачи `#315` и `#318`
- **Источник объёма:** тело `#406` (ПРЕДМЕТ, ПРЕДИКАТ СНЯТИЯ пп. 1–4, DoD) и его
  комментарий 2026-09-25, п. 2. Спеки `00..04` у службы нет. Доводы о следствии изложены в
  задаче и здесь не повторяются (§1.4)
- **Ревизия измерения:** `cbbac984` — ствол службы, от которого ветвятся ветка эпика и её
  волны. Числа §0 сняты на дереве «этот ствол ПЛЮС дифф ветки волны `366` на момент
  записи». Коммит ветки волны здесь не называется: гейт датировки судит вхождение ревизии в
  историю ствола, а вершина волны станет её предком только после вливания эпика
- **Фундамент:** `corelib` `v1.10.0-rc.2` (пин `go.mod:8`). Тело задачи называет `rc.1`;
  словарь причин в обеих версиях один и тот же, три значения (`oauthceremony/ports.go:320`
  в `rc.1`, `:342` в `rc.2`). Ссылки на пробы фундамента связаны ревизией тега,
  `PRO-Robotech/corelib@c8b7650112`: в стволе фундамента пакета церемонии ещё нет
- **Тип изменения:** добавляет значение в закрытый словарь, которое служба пишет в свою
  запись семейства. Проводной контракт не меняется: ни `proto`, ни ответ предъявлению
- **Сервис:** `kaname`. Затрагиваются `internal/migrations/`, `internal/domain/`,
  `internal/ceremonyport/` (пробы и шапка пакета), `internal/repo/kaname/pg/` (пробы) и
  `docs/engineering/components/31-deployment.md`
- **Предшественники:** `#396` (адаптеры портов церемонии: путь отзыва клиентом не исполнен
  и вынесен сюда находкой круга 1) и `#404` (сузил то же ограничение, снял
  `consent-withdrawn`; влит в ветку эпика вместе с волной `#358`)

---

## §0. Перепись производителей — до сценариев

Каждое «Тогда» ниже опирается на производителя из этой переписи. Производитель, которого в
дереве ещё нет, помечен «заводит это изменение». Где факт получен опытом, а не командой, так
и сказано: опыт шёл на выгрузке дерева с черновой правкой, и в запрос она не входит.

### 0.1. Словарь фундамента: три причины, написание — контракт

Фундамент объявляет словарь закрытым: `code-replay`, `refresh-replay`, `client-revoke`
(`oauthceremony/ports.go:336`, перечень `:341-343`). Написание причины — контракт со службой:
служба сопрягает его со своим словарём по значению (там же, `:318-320`). Закрытость держит
`PRO-Robotech/corelib@c8b7650112:TestRevocationReasonDictionaryIsClosed`.

### 0.2. Кто ДЕРЖИТ словарь службы

Словарь держат два места.

**Домен.** `internal/domain/oauth_ceremony.go`: тип `:109`, значения `:112-121`, перечень
`FamilyRevocationReasons()` `:126-131`, проверка `Validate` `:135-143`. Значений **пять**:
`code-replay`, `refresh-replay`, `logout`, `session-ended`, `client-removed`.

**База.** Ограничение `token_families_revoked_reason_ck`. Определений три, действует второе:

```sh
git grep -n 'CONSTRAINT token_families_revoked_reason_ck CHECK\|ADD CONSTRAINT token_families_revoked_reason_ck' -- 'internal/migrations/*.sql'
# → 20260920175117_authorization_code_is_our_record.sql:209   шесть значений, с consent-withdrawn
# → 20260923225650_consent_leaves_the_schema.sql:64           накат #404: пять значений
# → 20260923225650_consent_leaves_the_schema.sql:84           откат #404: шесть значений
```

Слова `client-revoke` нет ни там, ни там:

```sh
git grep -n "'client-revoke'" -- internal/migrations | wc -l        # → 0
git grep -n '"client-revoke"' -- internal/domain | wc -l            # → 0
git grep -n '"client-revoke"' -- '*.go' ':!*_test.go' | wc -l       # → 0
```

Совпадение двух мест в обе стороны уже держится. Направление «домен шире базы» ловит проба
живой схемы `TestIntegration_RevocationVocabularyAgreesWithTheDomain`
(`internal/migrations/consent_leaves_the_schema_integration_test.go:202`). Сравнение в обе
стороны доказано инъекцией на соседе `TestRevocationVocabularyComparator_SeesBothDirections`
(`:224`). То, что сверка читает живой каталог, а не молчит, показывает откат `#404`: на
откаченной базе сверка обязана найти расхождение (`:349-354`).

Колонку держат ещё два ограничения той же таблицы, и на них стоит решение Р4:

```sh
git grep -n 'token_families_revoked_pair_ck\|token_families_live_pair_ck' -- 'internal/migrations/*.sql'
# → 20260920175117_…sql:204   revoked_pair_ck: (revoked_at IS NULL) = (revoked_reason IS NULL)
# → 20260920175117_…sql:207   live_pair_ck:    live = (revoked_at IS NULL)
# (и строка 108 — комментарий)
```

### 0.3. Кто СОПРЯГАЕТ и кто ПИШЕТ

Адаптер порта отзыва `ceremonyport.Grants` сопрягает причину фундамента со словом службы
**по значению**: `FamilyReasonOf` (`internal/ceremonyport/grants.go:93-102`) превращает
причину в слово и спрашивает `Validate` домена. Слова нет — отказ до записи (`:72-76`),
слово есть — зовётся писатель (`:77`). Своего литерала у адаптера нет, и заводить его не
нужно: слово появится в сопряжении, как только его объявит домен.

Писателей отзыва семейства в не-тестовом дереве три места вызова и один оператор сессий:

```sh
git grep -n '\.RevokeFamily(' -- '*.go' ':!*_test.go'
# → internal/ceremonyport/grants.go:77                     слово из сопряжения (любая причина фундамента)
# → internal/repo/kaname/pg/oauth_ceremony_repo.go:597     code-replay, своя запись повтора кода
# → internal/repo/kaname/pg/oauth_ceremony_repo.go:704     refresh-replay, своя запись повтора токена
```

Снятие сессии пишет `session-ended` через `revokeFamiliesOfSessionsTx`
(`human_session_repo.go:450`, `:562`). Значит, `client-revoke` после изменения пишет ровно
одно место — `grants.go:77`.

Оператор писателя — `UPDATE … WHERE id = $1 AND revoked_at IS NULL`
(`internal/repo/kaname/pg/oauth_ceremony_repo.go:746-749`), и `RevokeFamily` проверяет слово
доменом до базы (`:734`). Первая причина остаётся: второй отзыв того же семейства трогает
ноль строк. Держит это `TestOAuthCeremonyWritersKeepTheirOutcomeUnderEitherDefault`
(случай «RevokeFamily»: `code-replay`, затем `refresh-replay`).

Перепись ссылок на константы словаря в не-тестовом Go (нужна §2, строка о `#339`):

| значение | ссылок `domain.<константа>` вне проб | писатель |
|---|---|---|
| `code-replay` | 1 | `oauth_ceremony_repo.go:597` и сопряжение адаптера |
| `refresh-replay` | 1 | `oauth_ceremony_repo.go:704` и сопряжение адаптера |
| `session-ended` | 2 | снятие сессии |
| `logout` | 0 | нет (`#339`) |
| `client-removed` | 0 | нет (`#339`) |

```sh
for v in FamilyRevokedByCodeReplay FamilyRevokedByRefreshReplay FamilyRevokedByLogout \
         FamilyRevokedBySessionEnd FamilyRevokedByClientRemoval; do
  echo "$v $(git grep -h "domain\.$v" -- '*.go' ':!*_test.go' | wc -l)"; done
```

### 0.4. Кто ЗОВЁТ отзыв клиентом

Отзыв клиентом (RFC 7009) исполняет операция `Ceremony.Revoke` фундамента. Причину
`client-revoke` она называет для всей операции сразу (`oauthceremony/ceremony.go:1009`), и
причина, названная операцией, побеждает замеченный повтор (`oauthceremony/notes.go:265-277`).
Путей, по которым эта причина доходит до порта, три:

| путь | кто отзывает | координата в фундаменте | проба фундамента |
|---|---|---|---|
| живой токен обновления | движок: снимает оба вида артефактов гранта | `internal/oauth2/handler/oauth2/revocation.go:58-60` | `PRO-Robotech/corelib@c8b7650112:TestClientRevocationNamesClientRevokeToThePort` |
| живой токен доступа | движок, тем же кодом | там же | та же, подслучай токена доступа |
| обёрнутый токен обновления | церемония, только по просьбе клиента, которому выдан грант | `oauthceremony/ceremony.go:1037` | `PRO-Robotech/corelib@c8b7650112:TestRevokingAStaleRefreshTokenRevokesTheFamily` |

Клиент, которому грант не выдан, отзыва живым токеном не получает: движок отвечает
`unauthorized_client` (`internal/oauth2/handler/oauth2/revocation.go:54-56`). В фундаменте этот
случай держит только унитарная проба обработчика движка на подставных хранилищах, и только
для токена обновления (`PRO-Robotech/corelib@c8b7650112:TestRevokeToken`, случай «token was
issued to another client»). Сквозь операцию церемонии его не судит ни одна проба пакета
церемонии `rc.2`: другой клиент там заводится только на путях обмена. Сквозным держателем
становится KN-FRV-04.

**Производственного вызывающего у отзыва клиентом нет.** Церемония не собрана ни в одном
композиционном корне, и маршрута отзыва служба не монтирует:

```sh
git grep -n 'oauthceremony\|ceremonyport' -- cmd/ ':!*_test.go' | wc -l   # → 0
```

Сборка — предмет `#407`, край — `PRO-Robotech/kacho#2796`. Все сценарии этой приёмки
поэтому идут сквозь настоящую церемонию фундамента на стенде пробы K1
(`internal/ceremonyport/revocation_reaches_presentation_test.go`, `newFlowRig` `:339`), а не
через провод.

### 0.5. Кто ЧИТАЕТ причину

В продукте её не читает никто:

```sh
git grep -n 'revoked_reason' -- '*.go' ':!*_test.go'
# → 5 строк: 2 в домене (комментарий :105, текст отказа :141), 2 оператора SET
#   (oauth_ceremony_repo.go:748, :832), 1 комментарий (:757). Ни одного SELECT.
git grep -c -i 'revoked_reason' -- proto/        # → пусто: на проводе поля нет
```

Места предъявления судят живость семейства, а не причину:
`familyRevokedOfIssuanceSQL` — `SELECT family_live IS NOT TRUE FROM kaname.access_tokens
WHERE jti = $1` (`internal/repo/kaname/pg/oauth_ceremony_access_token.go:66`). Отсюда
следствие: новое слово меняет только журнал семейства. Ответ предъявителю оно изменить не
может. Держит это KN-FRV-06: там все три поверхности отказывают одинаково на каждой причине.

### 0.6. Что наблюдается сегодня — опытом

Опыт шёл на выгрузке дерева. Черновая проба сквозь стенд K1, затем тот же набор с
объявленным в домене словом, затем с черновой миграцией. В запрос ничего из этого не входит.

| что | без слова (дерево как есть) | слово в домене, миграции нет | слово и миграция |
|---|---|---|---|
| отзыв живым токеном обновления | отказ `temporarily_unavailable`, семейство живо | успех, причина `client-revoke` | то же |
| отзыв живым токеном доступа | отказ `temporarily_unavailable`, семейство живо | успех, причина `client-revoke` | то же |
| отзыв обёрнутым токеном обновления | отказ `server_error`, семейство живо | успех, причина `client-revoke` | то же |
| отзыв чужим клиентом, оба вида | `unauthorized_client`, семейство живо | то же | то же |
| прямой отзыв словом на настоящей базе (KN-FRV-06) | — | отказ базы, подслучай красный (`family_revocation_through_three_surfaces_integration_test.go:357`) | зелёный |
| сверка словарей (KN-FRV-08) | зелёная, 5 = 5 | красная: «`client-revoke`: домен объявляет, база отвергает» (`consent_leaves_the_schema_integration_test.go:211`), перепись 5 против 6 | зелёная, 6 = 6 |

Стенд K1 хранит записи в памяти, и схему службы он не знает. Поэтому в средней колонке пути
через церемонию уже зелены, а красные — пробы на настоящей базе. Красных деревьев два, и
§6 п.5 называет оба.

### 0.7. Пробы, которые истекают сами

Три пробы заведены так, чтобы покраснеть, когда слово появится. Опытом подтверждено: все три
красны на дереве со словом в домене.

1. Запись ожидания `reasonsAwaitingAServiceWord` (`internal/ceremonyport/grants_test.go:63-65`)
   называет владельцем эту задачу. `TestGrants_EveryCeremonyReasonHasAFamilyWordOrANamedSubject`
   краснеет на `:99`: «причина получила слово службы, а запись ожидания стоит».
2. Подслучай «причина фундамента без слова службы» в `TestGrants_RefuseBeforeAnyWrite`
   (`:178`) подаёт `client-revoke` и ждёт отказа до записи. Краснеет на `:186`.
3. Проба отказа отзыва клиентом (функция в
   `internal/ceremonyport/revocation_reaches_presentation_test.go:596`; её имя здесь не
   ставится координатой, потому что изменение её снимает) краснеет на первом утверждении
   (`:598`) и велит перевести подслучай на полное утверждение K1.

### 0.8. Соседи, чьё утверждение меняется по построению

`TestIntegration_ConsentLeavingRollsBackToTheSameStructure` откатывает базу с головы до версии
перед миграцией `#404` и требует на откаченной базе ровно ОДНОГО расхождения словарей —
`consent-withdrawn` (`consent_leaves_the_schema_integration_test.go:353-354`). Новая миграция
стоит выше, и этот откат проходит и через неё. После изменения расхождений на откаченной базе
**два**: `consent-withdrawn` (база шире домена) и `client-revoke` (домен шире базы). Опытом:
проба красна на `:353`, и оба расхождения в находке названы.

Проб, откатывающих базу с головы, в дереве 16 файлов. Все они проходят через раздел отката
новой миграции:

```sh
git grep -l 'goose.DownTo(\|goose.Down(\|goose.Reset(' -- '*_test.go' | wc -l   # → 16
git grep -n 'client-revoke' -- '*_test.go' | wc -l                               # → 3: два комментария и текст утверждения в ceremonyport; строк с этой причиной не кладёт ни одна
```

### 0.9. Фикстуры, которыми строится «Дано»

- **Стенд K1** — `newFlowRig` (`revocation_reaches_presentation_test.go:339`): настоящая
  церемония фундамента над настоящими адаптерами, настоящее место предъявления
  (`tokenintrospecthttp`, путь `/internal/tokens/introspect`), хранилища в памяти. Семейство
  заводит `freshFamily` (`:467`: код → обмен → пара токенов). Ключ семейства даёт `familyOf`
  (`:478`), причину отзыва — `isRevoked` хранилища семейств (`:150`). Хранилище судит
  причину ТЕМ ЖЕ кодом, что настоящее (`:99-104`, `Validate` домена).
- **Второй клиент** заводится тем же способом, что клиент стенда (`:351-365`): запись
  регистрации в справочнике клиентов и проверочное значение секрета, начеканенное хешером
  `floorHasher`. Опытом: такой клиент доказывает себя в операции отзыва.
- **Сцена схемы** — `acScene` (`internal/migrations/authorization_code_schema_integration_test.go:118`):
  аккаунт, человек, сессия, клиент и живое семейство. **Свёртка носителя сессии выводится из
  ДЛИНЫ метки** (`:143`, `acDigest(len(tag)*7919+1)`). Две сцены с метками одной длины в
  одной базе сталкиваются на уникальности носителя, и вторая не заводится: измерено опытом.
  Поэтому сцены одной базы заводятся метками РАЗНОЙ длины.
- **Версии цепочки** — `versionsOf` (там же, `:62`): версия файла и версия перед ним, выведенные
  из цепочки, а не выписанные числом.
- **Отказ базы с именем ограничения** — `requirePgRefusal`
  (`internal/migrations/login_method_schema_integration_test.go:144`).
- **Слой доступа на настоящей базе** — сцена и стенд семейств пробы трёх поверхностей
  (`family_revocation_through_three_surfaces_integration_test.go`: `catalogPool`,
  `ceremonyScene`, `newFamilyRig`, `familyReason` `:224`).

### 0.10. Утверждения дерева, которые станут ложью

Четыре места говорят «отзыв клиентом ждёт слова и отказывает»:

```sh
git grep -n 'ждёт слова' -- internal/ceremonyport docs/engineering/components/31-deployment.md
# → docs/engineering/components/31-deployment.md:508
# → internal/ceremonyport/doc.go:37
```

К ним добавляются комментарий записи ожидания (`grants_test.go:50-62`) и шапка пробы отказа
(`revocation_reaches_presentation_test.go:587-595`). Абзац описания развёртывания
(`31-deployment.md:500-508`) перечисляет поводы отзыва семейства без отзыва клиентом.

---

## §1. Предмет

### 1.1. Что входит в объём

1. Слово `client-revoke` — шестое значение закрытого словаря причин отзыва семейства — в
   домене (`FamilyRevocationReasons()`) и в ограничении `token_families_revoked_reason_ck`.
   Оба места меняются одним изменением.
2. Отзыв клиентом сквозь церемонию фундамента отзывает семейство этим словом. Сегодня он
   отказывает операцией (§0.6). Три пути §0.4, каждый своим сценарием (KN-FRV-01…03).
3. Отзыв, открывшись, открыт только клиенту, которому грант выдан (KN-FRV-04).
4. Слово не обходит прежних свойств записи семейства: первая причина остаётся (KN-FRV-05),
   отзыв доходит до всех трёх мест предъявления (KN-FRV-06).
5. Схема: принятие и отказ (07), сверка словарей (08), накат (09), откат (10).
6. Сопряжение словарей фундамента и службы полно, запись ожидания пуста (11).
7. Пробы, истекающие сами (§0.7), сосед `#404` (§0.8) и четыре утверждения дерева (§0.10)
   переводятся тем же изменением.

### 1.2. Почему именно это слово

Написание берётся у фундамента дословно: адаптер сопрягает словари по значению, и любое
другое написание оставило бы причину без слова (§0.1, §0.3). Подставить соседнее слово
тоже нельзя: журнал семейства записал бы чужое событие (тело задачи, ПРИЗНАК).

### 1.3. Почему сейчас и что это даёт соседям

Сборка церемонии в композиционном корне (`#407`, условие 2) требует слова до сборки: без
него собранный отзыв клиентом отказывал бы. Обратного заполнения нет, и оно не нужно:
ограничение до сих пор отвергало слово, значит строк с ним нет ни в одной базе. А
производственного вызывающего нет до `#407` (§0.4), так что до сборки слово в базу не
попадёт.

### 1.4. Почему доводы — ссылкой

Следствие отказа отзыва изложено в задаче. Репозиторий публичен (`security-disclosure.md`
§«Публичные артефакты»), и приёмка — не место для этого разбора. Предмет выражен сценариями.

---

## §2. Что НЕ входит

- **Два значения без писателя, `logout` и `client-removed`,** — предмет `#339`. Это изменение
  их не трогает, `#339` не закрывает и от неё не зависит. Для будущего гейта `#339`: у
  `client-revoke` не будет ни одной ссылки на константу домена вне проб, а писатель есть. Это
  сопряжение адаптера по значению (`grants.go:93-102` → `:77`), и держит его
  `TestGrants_EveryCeremonyReasonHasAFamilyWordOrANamedSubject`. Перепись писателей по имени
  константы (§0.3) насчитала бы у слова ноль. Значит, гейт `#339` обязан засчитывать писателем
  сопряжение по значению, иначе он назовёт беспризорным слово, у которого писатель есть
- **Сборка церемонии в композиционном корне, маршрут отзыва и край** — `#407` и
  `PRO-Robotech/kacho#2796`. Приёмка закрывает только условие 2 `#407`. Условия 1, 3, 4 и 5 —
  её собственные
- **Величины темпа (`#315`) и потолки сроков (`#318`)** — не трогаются. Обе ждут `#407`
- **Обратное направление сверки словаря СНЯТИЯ СЕССИИ** (`#386`) — предмет другого ограничения,
  `human_sessions_ended_reason_check`. У словаря семейств оба направления уже держатся (§0.2).
  Общие у `#386` и этого изменения только каталоги
- **Словарь фундамента** — не меняется. Три причины, пин `v1.10.0-rc.2`
- **Проводной контракт** — ни `proto`, ни REST, ни ответ предъявлению. Причину не выводит
  наружу ни одно чтение (§0.5)
- **Отсечка субъекта.** Отзыв клиентом отсечку человека не пишет. Проба трёх поверхностей это
  утверждает (`requireNoSubjectCutoff`), и изменение этого не меняет
- **Отзыв, запрошенный чужим клиентом прежним токеном обновления,** — поведение фундамента
  (`ceremony.go:1037`). Это изменение его не трогает и не утверждает

---

## §3. Решения

### Р1. Значение — `client-revoke`, дословно у фундамента

Довод — §1.2. Форма — строчные буквы через дефис, как у пяти соседей.

### Р2. Писатель — сопряжение по значению; второго литерала нет

Слово объявляется в домене один раз. Адаптер своего литерала не заводит (§0.3), писатели
отзыва семейства — тоже. Держит это п.3 §6 как проверка посадки. Держателем в дереве служит
сопряжение: `TestGrants_EveryCeremonyReasonHasAFamilyWordOrANamedSubject` сравнивает причину
и слово по значению (`grants_test.go:105`). Второй литерал того же слова в писателе прошёл бы
`Validate`, но разойтись с первым не смог бы: оба значения — одна строка. Поэтому отдельного
разборщика дерева не заводится, и это сказано прямо.

### Р3. Домен и база — одним изменением

Словарь домена и ограничение базы не расходятся ни в одной посадке (`#339`, п.3). Пробы
красны до правки (ban #12), и промежуточные коммиты ряда это допускают. Вливание в ветку волны
приносит обе половины сразу.

### Р4. Откат ОТКАЗЫВАЕТ, пока лежит семейство со словом `client-revoke`

Откат ставит ограничение из пяти значений с проверкой лежащих строк. Семейство с новым словом
отвергает его кодом `23514`, и откат исполняется одной транзакцией: версия не сдвигается,
строки не трогаются. Выбор держится на трёх доводах.

- **Снять причину нельзя.** Причина и отметка отзыва — пара (`revoked_pair_ck`), отметка и
  живость — тоже (`live_pair_ck`, §0.2). Снять причину значит оживить семейство, и выданные по
  нему токены снова принимались бы при предъявлении (§0.5).
- **Переписать в другое слово нельзя.** Журнал записал бы чужое событие (§1.2). Слова «как
  было до изменения» для того же акта тоже нет: до изменения этот акт не записывался вовсе, он
  отказывал (§0.6).
- **Форма взята у соседа.** Накат `#404` так же отказывает, пока лежит семейство со
  снимаемой причиной (шапка `20260923225650_consent_leaves_the_schema.sql:31-35`), и держит
  это `TestIntegration_ConsentLeavingRefusesAFamilyCarryingTheWithdrawnReason`.

Выполнимым откат становится, когда такие семейства уходят вместе со своей сессией. Уборка
удаляет записи сессий по сроку (`human_session_repo.go:246-252`), а семейство уходит
каскадом (`token_families_session_fk`, `20260920175117_…sql:196`). Так же оно уходит при
удалении человека и клиента. Держит это KN-FRV-10.

### Р5. Накат лежащих строк не трогает

Миграция только расширяет ограничение. Операторов данных в накате нет, и каждая лежащая
строка удовлетворяет обоим определениям. Держит это KN-FRV-09.

### Р6. Перечень нового ограничения — пять значений `#404` плюс `client-revoke`

`#404` влит, поэтому его миграция — предшественница новой, и `consent-withdrawn` в перечень не
возвращается. Откат новой миграции восстанавливает ровно определение наката `#404`
(`20260923225650_…sql:64-67`). Порядок значений в перечне контрактом не является: сверка
сортирует обе стороны (`consent_leaves_the_schema_integration_test.go:152`, `:162`).

### Р7. Сосед `#404` называет оба расхождения, а не ослабляется

Утверждение `TestIntegration_ConsentLeavingRollsBackToTheSameStructure` о числе расхождений на
откаченной базе меняется по построению (§0.8). Новое утверждение называет оба расхождения
поимённо, каждое со своей стороной. «Непусто» и «хотя бы одно» исходом не являются: такая
сверка зеленела бы и на базе, откаченной не туда.

---

## §4. Сценарии

Сценарии 01–04 идут сквозь стенд K1 (§0.9), 05–06 — через слой доступа на настоящей базе,
07–10 — через схему, 11 — через сопряжение словарей. ID сценария (`KN-FRV-NN`) входит в имя
заводимой пробы или подслучая. Исключения — 06, 08 и 11: их держат существующие пробы. У
каждого сценария назван производитель «Тогда». У отрицательного и различающего сценария
названа дельта против близнеца, и она равна ровно одному факту.

«Отзыв клиентом» ниже — операция отзыва церемонии фундамента (RFC 7009 §2.1) с полями:
`token`, `token_type_hint`, идентификатор клиента и его секрет, способ доказательства
`client_secret_basic`. «Предъявлен» — `POST /internal/tokens/introspect` авторитету отзыва
стенда, форма `token=<токен доступа>`, ответ `200` и поле `active`.

### KN-FRV-01 · отзыв клиентом живым токеном обновления отзывает семейство словом `client-revoke`

> **Дано** стенд K1. Семейство A и семейство B получены одной и той же церемонией одним и тем
> же клиентом C (`freshFamily`). Токены доступа обоих, предъявленные, приняты (`active: true`)
> **Когда** C просит отзыва:
>   - `token` = токен обновления семейства A
>   - `token_type_hint` = `refresh_token`
>   - клиент C доказан своим секретом
>
> **Тогда** операция отзыва отвечает успехом (ошибки нет)
> **И** семейство A отозвано, причина — `client-revoke`
> **И** токен доступа семейства A, предъявленный, отвергнут (`active: false`)
> **И** обмен токена обновления семейства A отвергнут (`invalid_grant`)
> **И** выпуск нового токена доступа в семейство A не состоится: отказ
> `domain.ErrAccessTokenFamilyNotLive`, токена нет. Токен отозванного семейства не переживает
> отзыва ни при каком `iat`: выпущенный раньше отвергнут, а позже выпуска нет
> **И** токен доступа семейства B, предъявленный, принят (`active: true`)

**Дельта против B — один факт:** о семействе A клиент попросил отзыва, о B — нет. Без B «A
отвергнут» было бы истинно и у места предъявления, которое отвергает всё.

**Производители:** причину называет фундамент (§0.4, первая строка таблицы). Слово даёт
сопряжение адаптера (`grants.go:93-102`) из объявления домена (заводит это изменение). Отметку
ставит хранилище семейств стенда тем же `Validate`, что настоящее. Отказ предъявлению даёт
запись выпуска (решение К10, `#319`). Отказ позднему выпуску на стенде даёт хранилище семейств
(`revocation_reaches_presentation_test.go:118-129`), у службы — ключ записи выпуска на живость
семейства (`20260923231545_access_token_belongs_to_its_family.sql:80`). Тот же отказ для
повтора кода уже утверждает подслучай «выпуск после отметки отзыва не состоится» в
`TestK1_RevokedFamilyIsRefusedWhereTheAccessTokenIsPresented`. Причина отзыва оба ключа не
читают, поэтому слово обойти их не может.

**Красный до правки:** на дереве без слова операция отказывает (`temporarily_unavailable`,
§0.6), и первое «Тогда» красно. Подслучай ложится рядом с двумя соседними путями
`TestK1_RevokedFamilyIsRefusedWhereTheAccessTokenIsPresented`. Проба отказа (§0.7 п.3)
снимается тем же изменением: это и есть её «перевести на полное утверждение K1».

### KN-FRV-02 · тот же отзыв токеном доступа

> **Дано** то же, что в KN-FRV-01, на свежем стенде
> **Когда** C просит отзыва:
>   - `token` = токен доступа семейства A
>   - `token_type_hint` = `access_token`
>   - клиент C доказан своим секретом
>
> **Тогда** операция отзыва отвечает успехом
> **И** семейство A отозвано, причина — `client-revoke`
> **И** токен доступа семейства A, предъявленный, отвергнут
> **И** обмен токена обновления семейства A отвергнут (`invalid_grant`)
> **И** токен доступа семейства B принят

**Дельта против KN-FRV-01 — один факт:** вид предъявленного токена. На этом пути грант
находится через адаптер службы: движок спрашивает опознание токена доступа
(`AccessTokens.IdentifyAccessToken`, `oauthceremony/strategy.go:223-233`). Ответ «не наш»
дал бы успех без действия: движок не нашёл бы гранта и ответил бы успехом по RFC 7009 §2.2.
Такой исход отличает от верного только «семейство A отозвано», и на KN-FRV-01 этот путь не
исполняется вовсе. «Тогда» об обмене токена обновления утверждает, что семейство снято
целиком: движок зовёт оба метода порта при любом виде токена (`revocation.go:58-60`).

**Производители:** опознание токена доступа адаптером (K3, `#396`); остальное — как в
KN-FRV-01. Путь — §0.4, вторая строка.

**Красный до правки:** как в KN-FRV-01, `temporarily_unavailable`.

### KN-FRV-03 · отзыв обёрнутым токеном обновления называет причиной просьбу клиента, а не повтор

> **Дано** стенд K1, семейства A и B как в KN-FRV-01. Токен обновления семейства A обёрнут
> законным обменом, и пара оборота принята при предъявлении
> **Когда** C просит отзыва:
>   - `token` = ПРЕЖНИЙ, уже обёрнутый токен обновления семейства A
>   - `token_type_hint` = `refresh_token`
>   - клиент C доказан своим секретом
>
> **Тогда** операция отзыва отвечает успехом
> **И** семейство A отозвано, причина — `client-revoke`, а не `refresh-replay`
> **И** оба токена доступа семейства A — выданный кодом и выданный оборотом — при
> предъявлении отвергнуты
> **И** токен доступа семейства B принят

**Дельта против KN-FRV-01 — один факт:** предъявленный токен обновления уже обёрнут.
Сценарий отделяет церемонию, которая называет порту причину операции, от той, что назвала
бы замеченный повтор. На KN-FRV-01 повтора нет, и там зелены обе. Отзывает здесь не движок,
а сама церемония, и только по просьбе клиента, которому выдан грант (§0.4, третья строка).

**Производители:** отзыв исполняет церемония (`ceremony.go:1037`). Причину называет ведомость
операции (`notes.go:265-277`, названная операцией побеждает). Слово — как в KN-FRV-01.
Различение, которого не видит 01, держит фундамент: у службы производитель тот же, что у 01.
Служба утверждает его сквозь свои адаптеры, потому что причина ложится в её журнал.

**Красный до правки:** операция отказывает (`server_error`, §0.6), семейство живо.

### KN-FRV-04 · чужой клиент семейство не отзывает (близнец KN-FRV-01)

> **Дано** стенд K1, семейство A клиента C. Второй клиент X заведён так же, как C (§0.9), и
> доказывает себя своим секретом
> **Когда** X просит отзыва:
>   - `token` = токен обновления семейства A, `token_type_hint` = `refresh_token`
>   - в отдельном вызове `token` = токен доступа семейства A, `token_type_hint` = `access_token`
>
> **Тогда** каждый из двух вызовов отвечает отказом `unauthorized_client`
> **И** семейство A не отозвано: причины нет, отметки нет
> **И** токен доступа семейства A, предъявленный, принят
> **Когда** затем C просит отзыва токеном обновления семейства A
> **Тогда** семейство A отозвано, причина — `client-revoke`

**Дельта против KN-FRV-01 — один факт:** кто просит отзыва (вызов токеном доступа — против
KN-FRV-02, с той же дельтой). Слово открывает путь, которого
прежде не было ни у кого (§0.6). Сценарий утверждает, что путь открыт только клиенту,
которому грант выдан (RFC 7009 §2.1). Последняя пара «Когда/Тогда» — положительный контроль
на том же стенде: без неё отказ X был бы неотличим от стенда, где отзыв не работает ни у кого.

**Производители:** проверка «токен выдан спрашивающему» в движке (`revocation.go:54-56`);
слово — как в KN-FRV-01. Своей проверки служба не заводит: вторая копия правила разошлась бы
с первой молча и на своих пробах была бы зелена.

**Красный до правки:** половина с X зелена и до правки, и после: свойство держит фундамент
(§0.6). Контрольная половина с C красна до правки. Способность половины X упасть внутри этого
дерева не показать: проверка стоит в фундаменте. Её показывает близнец — тот же запрос от C в
том же прогоне отзывает семейство.

### KN-FRV-05 · первая причина остаётся, в каком бы порядке ни пришли повтор и просьба клиента

> **Дано** база на голове миграций, слой доступа на ней. Два живых семейства P и Q в двух
> сценах (§0.9)
> **Когда** 05/1: `RevokeFamily(P, code-replay)`, затем `RevokeFamily(P, client-revoke)`
> **Тогда** первый вызов отмечает одну строку, второй — ноль строк и без ошибки
> **И** у P причина `code-replay`
> **Когда** 05/2: `RevokeFamily(Q, client-revoke)`, затем `RevokeFamily(Q, code-replay)`
> **Тогда** первый вызов отмечает одну строку, второй — ноль строк и без ошибки
> **И** у Q причина `client-revoke`

**Дельта 05/1 против 05/2 — один факт:** порядок двух отзывов. Журнал называет того, кто
отозвал первым. Второй причину не переписывает, иначе запись называла бы того, кто пришёл
позже.

**Производитель:** условие `revoked_at IS NULL` в операторе писателя
(`oauth_ceremony_repo.go:749`), без изменений. Одновременную пару того же свойства уже держит
`TestOAuthCeremonyWritersKeepTheirOutcomeUnderEitherDefault` (§0.3). Этот сценарий утверждает,
что новое слово свойства не обходит.

**Красный до правки** снимается на дереве Т1 (§6 п.5): 05/2 красна, база отвергает первый
вызов кодом `23514`. А 05/1 там зелена: второй вызов не находит строки для правки, и
ограничению проверять нечего. (На дереве без слова отказал бы уже домен, в `RevokeFamily` до
базы, `:734`, но проба, называющая слово константой домена, там не соберётся.)
**Способность упасть** показывается инъекцией (§6 п.12): без условия `revoked_at IS NULL`
05/1 краснеет, причина переписана.

### KN-FRV-06 · прямой отзыв словом `client-revoke` доходит до всех трёх мест предъявления

> **Дано** база на голове миграций. Два семейства A и B одного человека и клиента, у каждого
> токен доступа, принятый всеми тремя поверхностями: `IsRevoked` службы отзыва, авторитетом
> отзыва и читателем предъявленного удостоверения
> **Когда** семейство A отзывается писателем слоя доступа со словом `client-revoke`
> **Тогда** у A причина `client-revoke`
> **И** все три поверхности отвергают ранее принятый токен семейства A
> **И** отсечки по субъекту нет
> **И** токен семейства B всеми тремя принят

**Дельта против B — один факт:** отзыв семейства A.

**Производитель:** проба `TestLINE_A_1_21_FamilyRevocationReachesEveryPresentationSurface`
перебирает `domain.FamilyRevocationReasons()`
(`family_revocation_through_three_surfaces_integration_test.go:350`). Подслучай «прямой
отзыв семейства: client-revoke» появляется в ней сам, как только слово объявлено доменом.
Новой пробы не нужно. Отказ дают записи выпуска (§0.5), принятие новой причины — новая
миграция.

**Красный до правки:** на дереве «слово в домене, миграции нет» подслучай красен на `:357`:
база отвергает причину (§0.6). Остальные девять подслучаев там зелены. Они — контроль того,
что красное пришло от слова, а не от стенда.

### KN-FRV-07 · база принимает `client-revoke` и отвергает близкое написание

> **Дано** база на голове миграций и одно живое семейство (`acScene`). Каждая проба ниже идёт
> в своей транзакции, которая откатывается, так что семейство остаётся живым для следующей
> **Когда** семейство отзывается оператором слоя доступа с причиной `client-revoke`
> **Тогда** база принимает
> **Когда** та же строка тем же оператором с причиной `client_revoke`
> **Тогда** база отвергает: SQLSTATE `23514`, ограничение `token_families_revoked_reason_ck`
> **Когда** та же строка с каждым из `code-replay`, `refresh-replay`, `logout`,
> `session-ended`, `client-removed`
> **Тогда** база принимает все пять
> **Когда** та же строка с `consent-withdrawn`
> **Тогда** база отвергает тем же ограничением: снятое `#404` не возвращается

**Дельта между принятием и отказом — один факт:** написание причины. Строка одна, оператор
один. `client_revoke` выбрано намеренно: оно отличается одним знаком, и значит отказ
утверждает дословное написание, контракт с фундаментом (§0.1). Отказ произвольному слову
утверждал бы только, что словарь закрыт.

**Производитель:** новая миграция (заводит это изменение). Форма — соседа
`TestIntegration_ConsentWithdrawnLeavesTheRevocationVocabulary`: оператор `revokeFamilySQL` и
`requirePgRefusal`. Опытом с черновой миграцией все четыре «Тогда» подтверждены.

**Красный до правки:** на голове без новой миграции первое «Тогда» красно: база отвергает
`client-revoke` тем же ограничением. Это и есть п.4 предиката задачи: «вставка
`client-revoke` отвергается».

### KN-FRV-08 · словари домена и базы совпадают в обе стороны

> **Дано** база на голове миграций и перечень домена
> **Тогда** каждое значение перечня есть в определении `token_families_revoked_reason_ck`
> **И** каждое значение определения есть в перечне
> **И** каждое значение определения принимается живой строкой
> **И** проба печатает перепись: у базы 6, у домена 6, поимённо

**Производитель:** существующая `TestIntegration_RevocationVocabularyAgreesWithTheDomain`,
без правки. Перечень домена и новая миграция заводит это изменение.

**Способность упасть — в обе стороны, и обе уже исполнены.** «Домен шире базы» — естественный
красный на дереве «слово в домене, миграции нет» (`:211`, §0.6). С него начинается
реализация. «База шире домена» доказано инъекцией на сравнении
(`TestRevocationVocabularyComparator_SeesBothDirections`) и живым откатом KN-FRV-10/2.

### KN-FRV-09 · накат не трогает лежащие строки

> **Дано** база, накатанная до версии перед новой миграцией (`versionsOf`). На ней шесть сцен
> с метками разной длины (§0.9). Пять семейств отозваны по одному каждым значением прежнего
> словаря, шестое живо
> **Когда** накат до версии новой миграции
> **Тогда** версия базы равна версии новой миграции
> **И** у всех шести `revoked_at`, `revoked_reason` и `live` те же, что до наката

Первое «Тогда» несущее. Без него сценарий зеленел бы и там, где новой миграции нет: строки
остались бы нетронутыми по пустой причине.

**Производитель:** раздел наката новой миграции (заводит это изменение), операторов данных в
нём нет (Р5). Приём наката до версии в каталоге уже есть:
`TestIntegration_ConsentLeavingRefusesAFamilyCarryingTheWithdrawnReason`.

**Способность упасть по второму «Тогда»** показывается инъекцией, потому что верное дерево её
не даёт: оператор в накате, переписывающий одну причину в `client-revoke`, обязан сделать
сценарий красным и назвать строку (§6 п.12).

### KN-FRV-10 · откат отказывает, пока лежит семейство со словом; без него — откатывается и сходится

> **Дано** 10/1: база на голове миграций, три сцены с метками разной длины. Семейство A
> отозвано с `client-revoke`, семейство B — с `code-replay`, семейство C живо
> **Когда** откат до версии перед новой миграцией
> **Тогда** откат отвергнут: SQLSTATE `23514`, ограничение `token_families_revoked_reason_ck`
> **И** версия базы — по-прежнему версия новой миграции
> **И** у A `revoked_reason = 'client-revoke'` и `live = false`: семейство не ожило
> **И** B и C те же, что до попытки

> **Дано** 10/2: та же база, но у A причина заменена прямым оператором на `code-replay`
> **Когда** откат до версии перед новой миграцией
> **Тогда** откат проходит
> **И** определение ограничения равно определению наката `#404` (пять значений)
> **И** отзыв живого семейства C с `client-revoke` отвергается тем же ограничением
> **И** сверка словарей на откаченной базе находит ровно одно расхождение:
> «`client-revoke`: домен объявляет, база отвергает»
> **Когда** повторный накат
> **Тогда** отзыв C с `client-revoke` снова принимается, и сверка словарей пуста

**Дельта 10/1 против 10/2 — один факт:** причина семейства A. Строка та же, база та же.

**Производитель:** раздел отката новой миграции (заводит это изменение). Решение — Р4. Форма —
соседа `TestIntegration_ConsentLeavingRefusesAFamilyCarryingTheWithdrawnReason`: там так же
отказывает накат, и близнец так же отличается одной причиной у той же строки. Опытом с
черновой миграцией подтверждено каждое «Тогда» обеих половин: отказ `23514` с именем
ограничения, версия не сдвинута, A не ожило, после замены причины откат прошёл и сверка
назвала одно расхождение.

**Способность упасть** показывается инъекциями (§6 п.12). Откат, переводящий
`client-revoke` в другое слово, проходит, и 10/1 красна: отказа нет, у A чужая причина. Откат,
ставящий ограничение без проверки лежащих строк, тоже проходит, и 10/1 красна: отказа нет, а
база держит строку, которой её ограничение не допускает.

### KN-FRV-11 · сопряжение словарей полно, ожидающих нет

> **Дано** словарь фундамента (три причины) и словарь домена (шесть значений)
> **Тогда** каждая причина фундамента сопряжена со словом службы того же написания
> **И** запись ожидания `reasonsAwaitingAServiceWord` пуста
> **И** проба сопряжения печатает «причин фундамента 3 · сопряжено 3 · ожидают слова 0»
> **И** оба метода порта отзывают семейство с `client-revoke`: у
> `TestGrants_RevokeTheFamilyWithTheCeremonyReason` подслучаев шесть (два метода на три
> причины), а не четыре, как сегодня
> **И** «причина не названа» и «слово службы вне словаря фундамента» по-прежнему отвергаются
> до записи: писатель не зван

**Производитель:** `FamilyReasonOf` (`grants.go:93-102`) над объявлением домена (заводит это
изменение). Держатели — `TestGrants_EveryCeremonyReasonHasAFamilyWordOrANamedSubject`,
`TestGrants_RevokeTheFamilyWithTheCeremonyReason`, `TestGrants_RefuseBeforeAnyWrite`.

**Что снимается и почему это не ослабление.** Подслучай «причина фундамента без слова
службы» (§0.7 п.2) снимается, потому что его входа больше нет: в закрытом словаре фундамента
все три причины сопряжены. Ветвь отказа `grants.go:72-76` остаётся под пробой: её исполняют
два прочих подслучая, `""` и `session-ended` (`grants_test.go:176-177`). Сам класс «причина
фундамента без слова» держит статическая проба сопряжения. Новая причина фундамента без
слова и без записи ожидания делает её красной (`grants_test.go:100-102`). Вторую сторону
самоистечения — запись ожидания при сопряжённой причине — опыт показал красной (§0.7 п.1).

---

## §5. Сценарий → производитель

| ID | что производит «Тогда» | координата в дереве | чем измерено |
|---|---|---|---|
| KN-FRV-01 | успех отзыва; причина `client-revoke`; отказ предъявлению и обмену; отказ позднему выпуску; B принят | фундамент `ceremony.go:1009`, движок `revocation.go:58-60`; `internal/ceremonyport/grants.go:93-102` → `:77`; объявление домена (заводится); запись выпуска стенда `revocation_reaches_presentation_test.go:118-129` | §0.4; §0.6 опыт; `PRO-Robotech/corelib@c8b7650112:TestClientRevocationNamesClientRevokeToThePort` |
| KN-FRV-02 | то же по токену доступа; семейство снято целиком | движок `revocation.go:58-60` (оба метода) и то же | §0.6 опыт; подслучай токена доступа той же пробы фундамента |
| KN-FRV-03 | причина `client-revoke`, а не `refresh-replay`; пара оборота отвергнута | `ceremony.go:1037`; `notes.go:265-277` | §0.6 опыт; `PRO-Robotech/corelib@c8b7650112:TestRevokingAStaleRefreshTokenRevokesTheFamily` |
| KN-FRV-04 | `unauthorized_client`, семейство живо; контроль C отзывает | движок `revocation.go:54-56` | §0.6 опыт (обе половины) |
| KN-FRV-05 | первая причина остаётся в обоих порядках | `internal/repo/kaname/pg/oauth_ceremony_repo.go:749` (`revoked_at IS NULL`); проверка домена `:734` | §0.3; `TestOAuthCeremonyWritersKeepTheirOutcomeUnderEitherDefault` |
| KN-FRV-06 | три поверхности отвергают, B принят, отсечки нет | перебор `family_revocation_through_three_surfaces_integration_test.go:350`; новая миграция; `oauth_ceremony_access_token.go:66` | §0.6 опыт: красный `:357` без миграции, зелёный с ней |
| KN-FRV-07 | принятие `client-revoke` и пяти, отказ `client_revoke` и `consent-withdrawn` | новая миграция в `internal/migrations/` (заводится) | §0.2; опыт с черновой миграцией |
| KN-FRV-08 | совпадение словарей, перепись 6 = 6 | перечень домена + новая миграция; `consent_leaves_the_schema_integration_test.go:202` | §0.6 опыт: красный `:211` без миграции |
| KN-FRV-09 | лежащие строки не тронуты, версия новая | раздел наката новой миграции (заводится) | Р5; приём наката до версии — соседняя проба `#404` |
| KN-FRV-10 | отказ отката при строке со словом; близнец откатывается, сверка называет одно расхождение, повторный накат сходится | раздел отката новой миграции (заводится) | Р4; опыт с черновой миграцией, обе половины |
| KN-FRV-11 | сопряжение 3 из 3, ожидающих 0, шесть подслучаев порта | `internal/ceremonyport/grants.go:93-102`; объявление домена (заводится) | §0.7 опыт; `grants_test.go:99`, `:186` |

Сценариев без производителя — **ноль**. У каждого «заводит это изменение» названа координата,
куда производитель ляжет, и сценарий, который красен, пока его нет. Два различения держит
фундамент, и способности упасть внутри этого дерева у них не показать: половина X сценария
KN-FRV-04 и различение «причина операции, а не повтор» в KN-FRV-03. Оба названы в самих
сценариях. У 04 назван близнец, который не даёт половине X стать пустой проверкой. У 03
свой красный на Т0 (§6 п.5).

---

## §6. Признак готовности (DoD)

Каждый пункт имеет объективное «пройдено / не пройдено».

1. **Миграция НОВАЯ** (ban #5). Ни одна применённая миграция не тронута:
   `git diff --name-status origin/366...HEAD -- 'internal/migrations/*.sql'` даёт ровно одну
   строку, и она с пометкой `A`. Версия старше всякой миграции волны на момент вливания,
   `TestNewMigrationOutranksEveryAppliedOne` зелёный. В файле оба маркера мигратора,
   `-- +goose Up` и `-- +goose Down`. Шапка миграции называет санкцией эту приёмку (путь и
   событие одобрения) и решение Р4 об откате.
2. **Слово в схеме — в одном файле:**
   ```sh
   git grep -l "'client-revoke'::text" -- 'internal/migrations/*.sql'   # → ровно один файл, новая миграция (сейчас 0)
   ```
3. **Слово объявлено один раз:**
   ```sh
   git grep -n '"client-revoke"' -- '*.go' ':!*_test.go'
   # → ровно 1 строка, в internal/domain/oauth_ceremony.go (сейчас 0)
   ```
4. **Истекающие пробы переведены** (§0.7):
   ```sh
   git grep -n 'RevocationClientRevoke' -- internal/ceremonyport/grants_test.go        # → 0 (сейчас 2: :64, :178)
   git grep -n 'RefusesLoudlyWhileTheWordIsMissing' -- internal/ceremonyport           # → 0 (сейчас 2: grants_test.go:62, revocation_reaches_presentation_test.go:596)
   ```
   `TestGrants_EveryCeremonyReasonHasAFamilyWordOrANamedSubject` зелёный и печатает
   «сопряжено 3 · ожидают слова 0».
5. **Падающие пробы — до правки** (ban #12). Красных деревьев два, потому что стенд K1 схемы
   не знает (§0.6).
   - **Дерево Т0 — как есть**, слова нет нигде. На нём красны KN-FRV-01, 02, 03 и контрольная
     половина 04: пробам стенда K1 хватает причины фундамента, и собираются они без
     объявления домена. Зелёной обязана быть половина X сценария 04.
   - **Дерево Т1 — слово объявлено доменом, миграции нет.** На нём красны 05/2, подслучай
     `client-revoke` сценария 06 (`:357`), 07, 08 (`:211`), а также 09 и 10 по отсутствию файла
     предмета (`versionsOf`). Красны и три истекающие пробы §0.7, которые переводятся этим
     изменением. Зелёными на Т1 обязаны быть 01–04 (стенд K1 базы не касается), 05/1 и девять
     прочих подслучаев 06: контроль, красный на Т1, судил бы не то, что ему приписано.

   Красный каждой пробы прочитан и назван в описании запроса причиной, а не только числом.
6. **Все сценарии KN-FRV-01…11 зелены одновременно**, на одном дереве.
7. **Сосед `#404` называет оба расхождения** (Р7). На дереве вливания
   `TestIntegration_ConsentLeavingRollsBackToTheSameStructure` зелёный. Его утверждение на
   откаченной базе требует ровно двух расхождений и называет каждое:
   `consent-withdrawn` — база шире домена, `client-revoke` — домен шире базы. Без правки он
   красен на `consent_leaves_the_schema_integration_test.go:353` (§0.8).
8. **Утверждения, ставшие ложью, сняты тем же изменением** (§0.10):
   ```sh
   git grep -n 'ждёт слова' -- internal/ceremonyport docs/engineering/components/31-deployment.md   # → 0 (сейчас 2: 31-deployment.md:508, doc.go:37)
   ```
   Шапка пакета `internal/ceremonyport/doc.go` называет отзыв клиентом исполненным наравне с
   двумя путями повтора. Абзац `31-deployment.md:500-508` называет отзыв клиентом среди
   поводов отзыва семейства. Работающей сборку он не объявляет: сборка — `#407`.
9. **Пробы отката зелёные.** Все 16 файлов проб, откатывающих базу с головы (§0.8), зелёные
   на дереве вливания: раздел отката новой миграции проходит на каждой их базе.
10. **Проводной контракт не тронут:** `git diff --stat origin/366...HEAD -- proto/ pkg/api/` →
    пусто.
11. **Сквозной пробы newman нет, и это не пропуск.** Маршрута отзыва служба не монтирует, и
    церемония не собрана ни в одном композиционном корне (§0.4, команда там же → 0).
    Сквозному кейсу утверждать нечего, пока нет `#407` и края `PRO-Robotech/kacho#2796`.
    Держатели — пробы Go: стенд K1 (01–04, 11), слой доступа на базе в контейнере (05, 06),
    схема (07–10).
12. **Способность упасть показана инъекциями, и каждый красный прочитан.** Правка
    производителя на рабочей копии в запрос не входит. Исход каждой инъекции назван в
    описании запроса вместе с фактом, по которому проба покраснела:
    - KN-FRV-05 — оператор писателя без условия `revoked_at IS NULL`: 05/1 красная, у P
      причина `client-revoke`;
    - KN-FRV-09 — оператор в накате, переписывающий одну лежащую причину в `client-revoke`:
      красная, строка названа;
    - KN-FRV-10 — откат, переводящий `client-revoke` в `logout`: 10/1 красная, отказа нет, у
      A чужая причина; откат, ставящий ограничение без проверки лежащих строк: 10/1 красная,
      откат прошёл;
    - KN-FRV-02 — порт выпуска, поданный церемонии, обёрнут так, что опознание токена
      доступа отвечает «не наш». Ключ семейства проба берёт мимо обёртки (`familyOf` зовёт
      адаптер стенда напрямую). Ожидаемый исход, выведенный чтением
      (`oauthceremony/strategy.go:223-243`, `revocation.go:44-52`): операция отвечает успехом,
      а 02 красная по «семейство A отозвано»; 01 на той же обёртке зелёная. Инъекция
      показывает, что 02 различает то, чего 01 не видит.
13. **Приёмки дерева судятся:** `TestAcceptanceProbeCoordinateResolves` и
    `TestAcceptancePathCoordinateResolves` зелёные на дереве вливания. Координаты этой
    приёмки резолвятся, в том числе после снятия пробы отказа (§0.7 п.3): её имя здесь не
    стоит координатой.
14. **Запрос вливается в ветку волны `366` через `git merge --no-ff`, а не в `main`.** Вывод
    предиката снятия `#406` (пп. 1–4) — комментарием в задаче при сдаче. Условие 2 `#407`
    отмечается там же ссылкой на коммит слияния.
