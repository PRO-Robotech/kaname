<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка MRW-1: группу пишущих кортежи объявляет манифест службы

- **Статус:** DRAFT — кругов ревью **ноль**, редакция 1 к первому кругу; вердикт выносит
  `acceptance-reviewer`. Санкции на объём этого документа нет (ban #1): ни одна одобренная
  приёмка не разрешает манифесту службы сеять — сегодня он не сеет вовсе (§1.1)
- **Задача:** `PRO-Robotech/kaname#106`, метки `area:iam`, `P1`, `release:standalone-iam`,
  `size:M`; состояние **OPEN** (`gh issue view 106 -R PRO-Robotech/kaname --json state`,
  2026-09-16). Решение владельца в её теле: **`module-relation-writers` описывается в YAML
  службы**
- **Сервис:** `kaname` — манифест службы, применитель посева, писатель строк посева, сверка
  паритета, миграции. Платформа (`PRO-Robotech/kacho`) участвует **одной** правкой — вступления
  в снимаемую группу уходят из пяти её манифестов (§6, стадия S2а); её код не трогается
- **Формат:** Given-When-Then, только markdown — кода здесь нет по построению
- **Публичный репозиторий:** оба репозитория публичны; документ написан так, чтобы выдерживать
  публикацию — §10
- **Ревизии измерения — по дому, и все замеры ниже сняты на них:** служба
  `PRO-Robotech/kaname@83ddd08a84` · платформа `PRO-Robotech/kacho@45d1719fe8` · фундамент
  `PRO-Robotech/corelib@1d25468c19` (тег `v1.8.0`, он же пин службы — `go.mod`) · воркспейс
  `PRO-Robotech/kacho-workspace@0290f73e94`. Координата без приставки дома — координата дерева
  службы; приставка `PRO-Robotech/kacho:` означает чужое дерево, и зелёный тамошнего гейта об
  этом дереве не говорит ничего
- **Предыдущих кругов НЕТ, и посылка задания об этом опровергнута замером** — §2.1

---

## §0 Обзор

Право «писать кортежи владения через службу» — поверхность **самой службы**
(`RegisterResource` / `UnregisterResource`). Аудиторию этой возможности сегодня объявляет не
владелец возможности, а **свод**: группу `module-relation-writers`, её выдачу `fga_writer` на
кластере и пятерых её членов заводит применённая миграция `0001_initial.sql`. Манифест службы
раздела посева не имеет вовсе, а пять манифестов модулей платформы лишь **вступают** в группу
и в своих шапках утверждают, что «эти группы заводит платформа», — чего не делает никто.

Документ закрепляет пять вещей.

1. **Группу и её выдачу объявляет манифест службы** — тот же раздел `seed`, что у модулей, та
   же форма, тот же применитель, тот же валидатор связности.
2. **Манифест службы приезжает применителю ВСТРОЕННЫМ В ОБРАЗ, а не доставкой.** Доставка
   собирается обходом дерева платформы, где службы больше нет ни одним файлом (§1.1), а копия
   её манифеста в чужом репозитории запрещена решением владельца (ban #20). Встроенность даёт
   и порядок: своё применяется **раньше** доставленного, поэтому вступление модуля находит
   группу заведённой.
3. **Идемпотентность выдачи держит ЕСТЕСТВЕННЫЙ КЛЮЧ живой строки, а не совпадение
   выведенного идентификатора.** Это измеренное опровержение предиката задачи: у живой строки
   `fga_writer` идентификатор **рукотворный**, выражение применителя даёт другой, и вторая
   строка не «ложится поверх», а **отвергается** частичным уникальным индексом — то есть
   применение обрывается и служба не поднимается (§2.2).
4. **`module-quota-readers` снимается целиком** — вступления из пяти манифестов платформы,
   членства и строка группы из базы службы, кортежи членства через очередь, со следом аудита.
   Её выдачу `quota_reader` отозвала миграция `20260914091500`; группа осталась пустой и
   ничего не открывает.
5. **Сверка паритета относит живую группу к модулю, ЧЕЙ МАНИФЕСТ ЕЁ ОБЪЯВЛЯЕТ.** Сегодня
   владелец выводится из имени вида `kacho-<служба>`; `module-relation-writers` такому имени не
   отвечает, поэтому объявление её манифестом службы дало бы **ложную находку** «группа
   ОБЪЯВЛЕНА и не живёт» (§1.5).

Чего документ **не** делает: не меняет модель прав, не трогает ни одного глагола, не расширяет
и не сужает круг тех, кто пишет кортежи. Состав получателей права `fga_writer` до и после —
те же пять служебных записей модулей платформы. Предмет здесь один: **кто объявляет**, а не
**кому дано**.

---

## §1 Перепись производителей — ДО сценариев

Класс, отвергавший работу этой линии больше двух раз, — «Тогда» без производителя. Поэтому
раздел стоит первым, а не последним: по каждому утверждению документа ниже назван либо
**живой производитель координатой**, либо **стадия с открытой задачей**.

### §1.1 Как манифест службы применяется сегодня — НИКАК

Три независимых предиката, и все три дают ноль.

```sh
# 1. раздела посева у манифеста службы нет
git grep -c '^seed:' -- manifest.yaml | wc -l               # → 0

# 2. встроенного в образ читателя своего манифеста нет
git grep -n 'go:embed manifest.yaml' -- ':!*_test.go'      # → пусто

# 3. чарт службы своего манифеста никуда не монтирует
git grep -n 'manifests' -- 'deploy/*.yaml'                 # → пусто
```

Четвёртый предикат — в чужом дереве, и он объясняет, **почему** доставка его не привозит:
производитель ConfigMap доставки обходит `services/*/manifest.yaml` **дерева платформы**, а
каталога службы там больше нет ни одним файлом.

```sh
# каталог службы в дереве платформы — снят вынесением (kacho#2598)
git ls-files -- 'PRO-Robotech/kacho:services/iam/**' | wc -l        # → 0

# манифесты, которые доставка собирает
git ls-files -- 'PRO-Robotech/kacho:services/*/manifest.yaml'       # → 5:
#   compute · nlb · registry · storage · vpc
```

Каталог обхода объявлен константой `servicesDir` в фундаменте
(`PRO-Robotech/corelib@v1.8.0:modulemanifest/producer/producer.go`, строки 55–58 и 247–260), и
ключ ConfigMap выводится из имени каталога службы.

**Следствие, несущее для §3:** сегодня у манифеста службы нет читателя **ни на одном** пути —
ни доставки, ни встроенности, ни монтирования. Значит раздел `seed`, дописанный в него без
второй половины работы, был бы объявлением без производителя — ровно тот класс, который
`api-conventions.md` §«Принято-и-проигнорировано» называет запрещённым.

### §1.2 Что ГОТОВО и работает — восемь производителей, каждый координатой

| # | утверждение, которое этот производитель держит | координата | предикат |
|---|---|---|---|
| П1 | форма `seed.groups[]` объявлена схемой и требует трёх ключей — `name`, `account`, `description` | `schema/module-manifest.schema.json` | `python3 -c "import json;s=json.load(open('schema/module-manifest.schema.json'));print(s['properties']['seed']['properties']['groups']['items']['required'])"` → `['name', 'account', 'description']` |
| П2 | `iam` — законное значение ключа `module`: набор закрыт и его содержит | тот же файл | `python3 -c "import json;print(json.load(open('schema/module-manifest.schema.json'))['properties']['module']['enum'])"` → шесть имён, `iam` первое |
| П3 | применитель умеет заводить группу и выдавать ей **отношение**; порядок внутри модуля — личности → группы → вступления → выдачи | `internal/apps/kaname/moduleseed/apply.go` | `git grep -n 'seed.Groups\|GrantRelation' -- internal/apps/kaname/moduleseed/apply.go` |
| П4 | строка группы кладётся **поверх живой, не меняя идентификатора**: конфликт по `groups_account_name_unique`, идентификатор участвует только во вставке | `internal/repo/kaname/pg/module_seed_writer.go:189–199` | `sed -n '189,199p' internal/repo/kaname/pg/module_seed_writer.go` |
| П5 | канон объявляет `cluster#fga_writer` принимающим `group#member`, то есть выдача группе на кластере **исполнима** | `proto/kaname/cloud/iam/v1/fga_model.fga:294` | `git grep -n 'define fga_writer' -- proto/kaname/cloud/iam/v1/fga_model.fga` → две строки: `:294` у `cluster`, `:954` у исторического якоря |
| П6 | валидатор связности требует, чтобы заведённую манифестом группу **он же и одарил** | `internal/manifest/linkage.go:63` | `git grep -n 'ErrGroupNeverGranted' -- internal/manifest` |
| П7 | выдача посева принимается **только** на кластерном singleton'е; вид получателя судится **каноном**, а не вторым перечнем | `internal/manifest/linkage.go:96`, `internal/manifest/relationgrant.go:186–202` | `git grep -n 'ErrBindingAnchor\|ErrRelationRecipientKind' -- internal/manifest` |
| П8 | сверка паритета судит **группы наравне** с личностями, вступлениями и выдачами | `internal/moduleseedparity/parity.go:241–244` | `git grep -n 'группа ЖИВЁТ и не объявлена' -- internal/moduleseedparity` |

**П8 — измеренное опровержение половины предиката задачи.** Предикат 4 задачи требует, чтобы
сверка «судила группы наравне с учётками». Она **уже** их судит: у `Compare` четыре вызова
`diffSet`, четвёртый — по группам, и обе его стороны доказаны инъекцией
(`TestInjectionSeedParityLiveGroupNotDeclaredIsAFinding`,
`TestInjectionSeedParityDeclaredGroupNotLiveIsAFinding`). Открытой остаётся вторая половина
предиката (шапка) и **не названная задачей** третья — атрибуция владельца (§1.5).

### §1.3 Что заводит эти строки СЕГОДНЯ — свод, поимённо

```sh
git grep -n 'module-quota-readers\|module-relation-writers' -- internal/migrations/0001_initial.sql
#   :3488  INSERT INTO kaname.groups … 'module-quota-readers'
#   :3489  INSERT INTO kaname.groups … 'module-relation-writers'
```

| строка свода | что заводит | число |
|---|---|---|
| `0001_initial.sql:3488` | группа `module-quota-readers`, идентификатор `grp1ed8897b56bb9106f` | 1 |
| `0001_initial.sql:3489` | группа `module-relation-writers`, идентификатор `grp258e6bbe9bbe45568` | 1 |
| `0001_initial.sql:3196` | выдача `fga_writer` группе на кластере, идентификатор `acb9e920fb038c7e4f74` | 1 |
| `0001_initial.sql:3190` | выдача `quota_reader` группе на кластере, идентификатор `acb1296746dc06ec9e25` | 1 |
| `0001_initial.sql:3472…` | членства — по **5** в каждой группе | 10 |

Предикат числа членств: `git grep -c "'grp1ed8897b56bb9106f', 'service_account'" --
internal/migrations/0001_initial.sql` → **5**; та же команда по `grp258e6bbe9bbe45568` → **5**;
всего строк `group_members` в своде — **10**.

Выдачу `quota_reader` **уже отозвала** миграция
`20260914091500_limit_reader_grant_is_revoked.sql` (kaname#59). Она же в своей шапке объявляет,
ПОЧЕМУ группа осталась: «НЕ снимается группа-получатель `module-quota-readers` … пустая группа
не даёт никому ничего, а производителя, который завёл бы её заново, нет — валидатор связности
требует, чтобы заведённую манифестом группу он же и одарил. Снятие сломало бы установку
платформы» (`sed -n '19,24p' internal/migrations/20260914091500_limit_reader_grant_is_revoked.sql`).

Тот же довод своими словами говорит соседняя миграция, и она же называет держателя, которого
не хватало: «объявить её посевом СЕГОДНЯ нельзя — валидатор связности требует, чтобы
заведённая манифестом группа была им же и одарена … а выдача этих групп принадлежит службе, не
модулю» (`sed -n '66,69p' internal/migrations/20260909202745_module_identities_leave_the_baseline.sql`,
раздел «ГРУППЫ НЕ СНИМАЮТСЯ»).

**Это и есть предмет задачи, названный обеими миграциями заранее:** держателя, которого не
хватало, зовут «раздел `seed` у манифеста службы».

### §1.4 Вступления модулей — что снимается со стороны платформы

```sh
git grep -n 'name: module-' -- 'PRO-Robotech/kacho:services/*/manifest.yaml'
#   по 2 строки в каждом из пяти манифестов: module-quota-readers и module-relation-writers
```

| модуль платформы | вступление в `module-quota-readers` | вступление в `module-relation-writers` |
|---|---|---|
| `compute` | `services/compute/manifest.yaml:268` | `:271` |
| `nlb` | `services/nlb/manifest.yaml:283` | `:286` |
| `registry` | `services/registry/manifest.yaml:188` | `:191` |
| `storage` | `services/storage/manifest.yaml:149` | `:152` |
| `vpc` | `services/vpc/manifest.yaml:490` | `:493` |

Снимается **левый столбец** — пять вступлений. Правый остаётся нетронутым: он и есть то
членство, ради которого группа существует.

Шапки этих же пяти манифестов несут утверждение, которое станет ложным правой половиной
работы и верным — левой: «`module-quota-readers` и `module-relation-writers` заводит
платформа». Предикат: `git grep -c 'заводит платформа' --
'PRO-Robotech/kacho:services/*/manifest.yaml'` → **1** в каждом из пяти.

### §1.5 Чего производителя НЕТ — пять пробелов, каждый со своей стадией

| # | «Тогда», у которого сегодня нет производителя | чем это видно | стадия |
|---|---|---|---|
| О1 | манифест службы доезжает до применителя | §1.1, три нуля | S1 |
| О2 | применитель применяет свой манифест **раньше** доставленных | `ApplyAll` идёт по перечню в порядке обхода, а обход лексический: `compute` < `iam`, то есть вступление `compute` встретилось бы **раньше** группы | S1 |
| О3 | выдача, чья живая строка заведена сводом, применяется **без второй строки и без отказа** | §2.2 — измеренное опровержение | S1 |
| О4 | живая группа относится к модулю, **объявившему** её | владелец выводится из имени `kacho-<служба>` (`grep -n 'seededNamePrefix' internal/moduleseedparity/parity_test.go` → `:98–100`, значение `"kacho-"`); `module-relation-writers` такому имени не отвечает и считается «без модуля-владельца» | S1 |
| О5 | `module-quota-readers` и её членства сняты | строки живы, §1.3 | S2 |

**О2 — механика, а не догадка.** `ApplyAll` перебирает манифесты в том порядке, в каком их
отдал читатель доставки (`internal/apps/kaname/moduleseed/apply.go`, цикл `for _, m := range
manifests`), а читатель строит перечень обходом `filepath.WalkDir`
(`internal/manifest/check.go:379–382`), который идёт по именам лексически. Группа заводится
внутри `applyOne` **своего** модуля, а вступление `compute` — внутри своего; между модулями
порядка нет никакого, кроме имени. Отказ был бы назван
(`groupID`: «группа %s/%s не резолвится»), но это всё равно отказ старта.

**О4 — цена названа числом.** Пока правило владельца прежнее, объявление группы манифестом
службы даёт сверке пару «объявлено 1 · живых по модулю `iam` 0» и **ложную находку** «группа
ОБЪЯВЛЕНА и не живёт». Ложная находка у сверки дороже отсутствующей: проверку, краснеющую на
верном дереве, снимают первой.

---

## §2 Опровержения посылок — замером, а не мнением

### §2.1 Посылка «есть предыдущие круги и последняя запись ревью» — ОПРОВЕРГНУТА

Задание велело прочитать последнюю запись ревью и закрыть её блокирующие. Ни записи, ни
документа, ни ветки не существует ни в одном дереве.

```sh
# ветки нет на origin службы
git ls-remote --heads origin 'refs/heads/acceptance/106-service-manifest-groups'   # → пусто

# документа нет НИ НА ОДНОЙ ветке origin (перебор всех 38 голов)
for b in $(git ls-remote --heads origin | awk '{print $2}' | sed 's|refs/heads/||'); do
  git cat-file -e "origin/$b:docs/engineering/acceptance/service-manifest-seeds-its-own-groups.md" \
    2>/dev/null && echo "НАЙДЕН на $b"
done                                                                               # → пусто

# каталога записей ревью для этого документа нет
ls docs/specs/reviews/                                                             # → два каталога,
#   login-session-and-credentials-are-our-contract · password-verifier-follows-the-stored-value
```

Вторая половина задания («НОВЫЙ документ») верна, первая — нет. Настоящая редакция — **первая**,
кругов ревью **ноль**, и блокирующих, которые можно было бы закрыть, не существовало.

### §2.2 Посылка задачи «применитель кладёт объявленное поверх живой строки, не меняя идентификатора» — ВЕРНА ДЛЯ ГРУППЫ И ЛОЖНА ДЛЯ ЕЁ ВЫДАЧИ

Предикат снятия 1 задачи утверждает это про обе строки разом. Замер разводит их.

**Для группы — верно** (П4). Конфликт разрешается по ограничению уникальности `(account_id,
name)`, идентификатор стоит только в списке значений вставки и на живую строку не переносится.
Это сказано и в шапке писателя: «У группы такой производной нет: живые группы несут случайные
идентификаторы. Поэтому её идемпотентность держит ограничение уникальности `(account_id,
name)`, а не совпадение идентификатора».

**Для выдачи — ложно.** Идентификатор выдачи применитель **выводит** выражением
(`internal/repo/kaname/pg/module_seed_writer.go`, раздел «Идентификатор выдачи ВЫВОДИТСЯ тем же
выражением, каким его вывела платформа»), и конфликт объявляет по `(id)`. У живой строки
`fga_writer` идентификатор **рукотворный** — она заведена сводом, а не тем выражением:

```sh
python3 - <<'PY'
import hashlib
def acb(user, form, kind, anchor):
    return 'acb' + hashlib.md5(f'system-grant:{user}:{form}:{kind}:{anchor}'.encode()).hexdigest()[:17]
print('живая строка свода          ', 'acb9e920fb038c7e4f74')
for anchor in ('cluster_root', 'cluster_kacho_root'):
    print(f'выражение применителя @{anchor:18}', acb('group:grp258e6bbe9bbe45568', 'fga_writer', 'cluster', anchor))
PY
#   живая строка свода           acb9e920fb038c7e4f74
#   выражение применителя @cluster_root       acbbcb7bb3d70aa9cc4b
#   выражение применителя @cluster_kacho_root acb7a84798bb6bfc448b
```

Ни одно из двух написаний якоря не даёт живого идентификатора — то есть расхождение не
следствие переименования якоря, а следствие того, что **свод чеканил идентификаторы рукой**.
Выражение применяет лишь миграция `20260909202745` и лишь к выдачам `system_viewer` служебным
записям (`sed -n '299,302p' internal/migrations/20260909202745_module_identities_leave_the_baseline.sql`).

**Что произойдёт на живой установке — не «вторая строка», а ОТКАЗ.** Вставка с другим
идентификатором наталкивается на частичный уникальный индекс по естественному ключу, которого
`ON CONFLICT (id)` не покрывает:

```sh
git grep -n 'access_bindings_active_relation_grant_uniq' -- internal/migrations
#   0001_initial.sql:4450  CREATE UNIQUE INDEX … (subject_type, subject_id, granted_relation,
#                          resource_type, resource_id) WHERE granted_relation <> '' AND revoked_at IS NULL
```

Индекс жив: снимающей его миграции в дереве нет ни одной (та же команда даёт единственное
объявление). Значит применение отвергается кодом `23505`, транзакция модуля откатывается,
`ApplyAll` прекращает работу и служба **не поднимается**. Предикат задачи «кортежи уже живых
установок не рвутся» выполняется случайно и по худшей причине: до кортежей дело не доходит.

**Замечание в пользу самого предиката.** Для двух живых выдач посева (`system_viewer` у `vpc`
и `compute`) он верен буквально: их идентификаторы **выведены** тем же выражением, конфликт по
`(id)` срабатывает, изменений ноль. Форм выдачи две; живых строк роль-формы в разделах `seed`
сегодня **ноль** — `git grep -c 'roleId:' -- 'PRO-Robotech/kacho:services/*/manifest.yaml'`
даёт 0 в каждом из пяти, и это состояние без предмета, а не исключение.

### §2.3 Посылка задачи «сверка судит группы наравне с учётками» — УЖЕ ВЕРНА

См. П8. Предикат 4 задачи в этой половине беспредметен; открытыми остаются шапка и атрибуция
владельца (О4). Атрибуция задачей **не названа** — это находка переписи, а не пересказ задачи.

### §2.4 Посылка задачи «шапка `parity.go` пережила предмет» — ВЕРНА, и расхождение внутри пакета

Шапка утверждает: «у выдачи есть ключ `roleId` и нет ни одного ключа для ОТНОШЕНИЯ, поэтому
выдача отношением необъявима ни при каком написании; группа необъявима по следствию» и
называет живой пробу предпосылки, которой в дереве нет.

```sh
sed -n '14,23p' internal/moduleseedparity/parity.go       # утверждение и имя пробы
git grep -n 'TestBindingFormStillCannotExpressARelationGrant' -- internal/moduleseedparity
#   parity.go:22        — шапка называет её живой
#   parity_test.go:153  — надгробие: «СНЯТА ВМЕСТЕ СО СВОИМ ПРЕДМЕТОМ (#1936)»
```

Ключ `grantedRelation` заведён 2026-09-04 (`git log -1 --format='%h %ad' --date=short -S
grantedRelation -- schema/` → `96e8e485 2026-09-04`) и объявлен схемой
(`schema/module-manifest.schema.json:486`). Тело того же пакета с шапкой **уже не согласно**:
`parity.go:124–137` — надгробие предиката `ExpressibleByForm`, снятого тем же изменением, а
`parity_test.go:145–151` говорит «Выведенных из-под вердикта строк больше НЕТ». Два места об
одном предмете внутри одного пакета, и верно то, которое **исполняется**.

---

## §3 Решения

### Р1. Группу и её выдачу объявляет манифест службы

Манифест службы (`manifest.yaml`, `module: iam`) получает раздел `seed` с двумя подразделами:

- `groups` — одна запись: `module-relation-writers` в аккаунте системных служебных записей, с
  назначением, взятым **дословно** у живой строки свода;
- `accessBindings` — одна запись: получатель `{type: group, name: module-relation-writers}`,
  `grantedRelation: fga_writer`, `scopeType: iam.cluster`, `scopeId: cluster_root`,
  `target: allInScope`.

Подразделов `serviceAccounts` и `joins` у него нет: своей служебной записи у службы доступа
не бывает, и вступать ей некуда. Это сказано **словом** в шапке манифеста, а не оставлено
умолчанием.

**Почему выдача обязана быть.** Её требует валидатор связности (П6): заведённая манифестом
группа, которую он же не одаривает, — объявление без следствия. Требование здесь не помеха, а
ровно то, что делает решение владельца исполнимым: аудиторию и выдачу объявляет один документ.

### Р2. Манифест службы приезжает применителю ВСТРОЕННЫМ В ОБРАЗ

Исходов рассматривалось два, и второй отвергнут по цене, а не по вкусу.

| исход | цена |
|---|---|
| **встроен в образ** (принят) | манифест едет там же, где миграции и канон модели; условие «группа заведена» не зависит ни от доставки, ни от платформы; порядок «своё раньше доставленного» получается by construction |
| доставкой платформы (отвергнут) | требует копии манифеста службы в дереве платформы — **запрещено** решением владельца 2026-09-13 (ban #20: файл, чей владелец — другой репозиторий, в этом дереве не лежит). Сверх того делает существование группы условием установки платформы, тогда как право принадлежит поверхности самой службы, и ломает порядок (О2) |

**Что из этого следует для самостоятельной установки.** Служба без платформы поднимается с
заведённой группой и выданным ей правом; членов у группы нет, и это верное состояние, а не
пробел: модулей нет — писать кортежи некому. Пустая группа не даёт никому ничего (тот же
довод, которым две применённые миграции оставили пустыми обе группы).

### Р3. Свой манифест применяется ПЕРВЫМ, и порядок объявлен, а не получен случайно

Применитель получает манифест службы **отдельным доводом**, а не подмешанным в перечень
доставки, и применяет его до перебора доставленных. Два следствия, и оба обязательны:

- вступление модуля всегда встречает группу заведённой — независимо от имени модуля и от
  порядка обхода каталога доставки;
- перепись применения различает «свой» и «доставленные» **числом**: «манифестов доставлено N ·
  свой применён да/нет». Одно число на оба разряда скрыло бы ровно тот случай, ради которого
  разделение и делается.

### Р4. Идемпотентность выдачи держит ЕСТЕСТВЕННЫЙ КЛЮЧ живой строки

Применитель, встретив живую выдачу **того же субъекта** того же **отношения** на том же
**якоре**, не заводит второй строки, не отказывает и не трогает кортеж — независимо от того,
каким идентификатором эта строка заведена. Наблюдаемо: применение проходит, перепись выдач
показывает «записано 0», журнал прав не получает ни одной строки, идентификатор живой строки
не меняется.

Исходы, рассмотренные и отвергнутые, с их ценой:

| исход | почему отвергнут |
|---|---|
| миграция **переклеивает** живую строку на выведенный идентификатор | невыразимо: внешний ключ `access_binding_emitted_tuples_binding_id_fkey` объявлен **без** `ON UPDATE CASCADE` (`git grep -n 'access_binding_emitted_tuples_binding_id_fkey' -- internal/migrations` → `0001_initial.sql:5354`), поэтому `UPDATE … SET id` отвергается ссылкой ребёнка. У соседнего ключа `access_binding_subjects_scope_fk` каскад обновления **есть** (`:5362`) — то есть половина детей переехала бы, а половина отвергла бы переезд |
| миграция **снимает и заводит заново** | выразимо и дорого: уносит ведомость эмитированного каскадом удаления, эмитирует пару «снятие + запись» в журнал прав на **каждой** установке и заводит окно, в котором право отсутствует. Платится это ради совпадения идентификатора, которого снаружи никто не адресует: выдача системная и защищена от удаления |
| применитель **молча пропускает** выдачу, если хоть какая-то строка у субъекта есть | отвергнут: «выдача уже есть» и «выдача есть, но другая» стали бы неразличимы, а второе — настоящая находка |

Решение действует **для формы отношения**. Для формы роли живых строк посева сегодня ноль
(§2.2), и её естественный ключ — другой индекс (`access_bindings_active_grant_uniq`,
`0001_initial.sql:4443`); требование к ней названо в §4 отдельным сценарием, чтобы тот же
класс не пришлось находить второй раз.

### Р5. Живая группа относится к модулю, ЧЕЙ МАНИФЕСТ ЕЁ ОБЪЯВЛЯЕТ

Правило владельца для групп меняется: имя вида `kacho-<служба>` остаётся правилом для
**служебных записей**, а группа относится к модулю **по объявлению**. Группа, которую не
объявляет ни один прочитанный манифест, владельца не имеет, вердикту не подлежит и считается
переписью отдельно — тем же разрядом «без модуля-владельца», что и сегодня.

**Почему не «по имени с приставкой `module-`».** Приставка не называет владельца: обе
сегодняшние группы её несут, а объявлять их будет один манифест из шести. Правило по имени
пришлось бы менять снова с первой же группой, заведённой другим модулем.

**Что это НЕ ослабляет.** Обе стороны вердикта остаются: «группа ОБЪЯВЛЕНА и не живёт»
срабатывает, если объявленной строки в базе нет; «группа ЖИВЁТ и не объявлена» срабатывает,
если живая строка объявлена **этим** модулем с другим назначением. Появление у группы
владельца **сужает** разряд «без владельца», а не переносит строки в молчание: обе величины
печатаются рядом.

### Р6. `module-quota-readers` снимается ДВУМЯ стадиями в разных репозиториях, и порядок несущий

Сначала вступления уходят из манифестов платформы (S2а), потом строки — миграцией службы
(S2б). Обратный порядок ломает установку: применитель, встретив вступление в снятую группу,
отказывает (`groupID`: «группа %s/%s не резолвится»), и служба не поднимается.

Между стадиями живёт состояние «группа есть, вступлений нет», в котором не ломается ничто ни
при какой комбинации версий: применитель не снимает строк, а пустая группа ничего не открывает.

**И здесь надо сказать точно, чем это держится, потому что соблазн назвать соседний гейт
велик.** «Применитель не снимает строк» у посева держится **шапкой пакета**
(`sed -n '28,31p' internal/apps/kaname/moduleseed/apply.go`), и машинного держателя у него
нет: гейт `TestMODRD15ApplierNeverDeletesARoleRow` судит применитель **ролей**
(`internal/apps/kaname/moduleroles`) и о посеве не утверждает ничего — предмет назван первой
же строкой его разбора (`sed -n '4,14p' internal/check/applier_never_deletes.go`). Перенос
того же разбора на посев — выразимая работа, и она **здесь не заказывается**: предмет у неё
свой.

**Цена состояния названа, а не умолчана:** в посадке, где читаются манифесты платформы, сверка
паритета между S2а и S2б даёт находку «вступление ЖИВЁТ и не объявлено» — по одной на каждый
из пяти модулей. В посадке самостоятельного клона её нет: чужие модули там **считаются**, а не
судятся (`sed -n '207,238p' internal/moduleseedparity/parity_test.go`), и это измеренное
свойство, а не надежда.

### Р7. Свод перестаёт быть источником истины, но `0001` не правится

Применённую миграцию не правят (ban #5). Из этого следует по каждой строке §1.3:

| строка свода | что с ней делает эта работа |
|---|---|
| группа `module-relation-writers` | **усыновляется**: применитель кладёт объявленное поверх неё по ограничению уникальности (П4), идентификатор не меняется |
| выдача `fga_writer` | **усыновляется** естественным ключом (Р4); новой миграции для неё не требуется |
| группа `module-quota-readers` и её 5 членств | **снимаются** новой миграцией (S2б) |
| выдача `quota_reader` | уже снята миграцией `20260914091500`; эта работа её не касается |
| 5 членств в `module-relation-writers` | не трогаются: их объявляют манифесты модулей, и они остаются |

---

## §4 Сценарии

Идентификаторы `MRW-01…MRW-18` трассируются в имена проб. Все «Дано» строятся домом,
названным в §5.

### S1 — манифест службы объявляет свою группу

**MRW-01 · чистая установка: группа и выдача заведены объявлением**

- **Дано** установка службы, чья база поднята применением всех миграций дерева, и манифест
  службы объявляет раздел `seed` по Р1
- **И** доставки манифестов платформы нет (`manifests.dir` не объявлен посадкой)
- **Когда** служба стартует
- **Тогда** в базе есть **ровно одна** группа с именем `module-relation-writers` в системном
  аккаунте, и её назначение равно объявленному манифестом
- **И** у неё есть **ровно одна** активная выдача отношения `fga_writer` на объекте
  `cluster:cluster_root`, с признаком системной и защитой от удаления
- **И** служба принимает трафик (стадия применения посева не отказала)

**MRW-02 · повторный старт ничего не меняет**

- **Дано** установка MRW-01 после успешного старта
- **Когда** служба перезапускается без единой правки манифеста
- **Тогда** перепись применения своего манифеста печатает «групп 0/1 · выдач 0/1»
  (записано/объявлено)
- **И** идентификатор строки группы и идентификатор строки выдачи те же, что до перезапуска
- **И** в журнал прав не добавлено ни одной строки

**MRW-03 · живая установка: рукотворный идентификатор усыновляется, а не отвергается**
*(положительный близнец MRW-02; отличается ОДНИМ фактом — строка выдачи заведена сводом
с идентификатором `acb9e920fb038c7e4f74`, а не выражением применителя)*

- **Дано** установка, чья база несёт живые строки свода §1.3, включая выдачу `fga_writer` с
  идентификатором `acb9e920fb038c7e4f74`
- **Когда** служба с манифестом Р1 стартует впервые
- **Тогда** применение посева **проходит** — отказа с кодом нарушения уникальности нет
- **И** строк активной выдачи отношения `fga_writer` группе на кластере в базе **одна**, и её
  идентификатор по-прежнему `acb9e920fb038c7e4f74`
- **И** ведомость эмитированного этой выдачи не изменилась, и в журнал прав не добавлено ни
  одной строки — то есть кортеж не перезаписан и не снят

**MRW-04 · своё применяется раньше доставленного**

- **Дано** установка, чья доставка несёт манифест `compute` со вступлением в
  `module-relation-writers`, и база, в которой этой группы ещё нет
- **Когда** служба стартует
- **Тогда** вступление `compute` записано, отказа «группа не резолвится» нет
- **И** перепись называет **раздельно**: свой манифест применён, доставленных манифестов —
  число

**MRW-05 · самостоятельная установка без платформы**
*(отрицательный близнец MRW-04 по ОДНОЙ оси — доставки нет вовсе)*

- **Дано** установка службы, чья посадка не объявляет каталог доставки
- **Когда** служба стартует
- **Тогда** группа и её выдача заведены — как в MRW-01
- **И** членов у группы ноль, и это не отказ: перепись печатает «доставленных манифестов 0»

**MRW-06 · группа без выдачи отвергается формой**

- **Дано** манифест службы, объявляющий группу `module-relation-writers` и **не** называющий
  её ни в одной выдаче
- **Когда** манифест разбирается судьёй формы
- **Тогда** разбор даёт находку рода `ErrGroupNeverGranted`, называющую путь `seed.groups[0]` и
  номер строки документа
- **И** служба с таким манифестом не стартует

**MRW-07 · положительный близнец MRW-06**
*(отличается ОДНИМ фактом — выдача названа)*

- **Дано** тот же манифест, дополненный выдачей `fga_writer` этой группе на кластере
- **Когда** манифест разбирается тем же судьёй
- **Тогда** находок ноль, и служба стартует

**MRW-08 · якорь вне кластерного singleton'а отвергается**

- **Дано** манифест службы, чья выдача объявлена с `scopeType: iam.account`
- **Когда** манифест разбирается судьёй формы
- **Тогда** находка рода `ErrBindingAnchor` с путём `seed.accessBindings[0]`
- **И** та же выдача с `scopeType: iam.cluster` находок не даёт *(обе стороны оси названы)*

**MRW-09 · отношение вне канона отвергается**

- **Дано** манифест службы, чья выдача называет отношение, которого канон у типа `cluster` не
  объявляет
- **Когда** манифест разбирается судьёй формы
- **Тогда** находка рода `ErrRelationNotDeclared`
- **И** та же выдача с `fga_writer` находок не даёт *(обе стороны названы)*

**MRW-10 · получатель, которого отношение не принимает, отвергается**

- **Дано** манифест службы, объявляющий получателем **группу** на отношении `system_viewer`
  кластера — канон принимает у него `user` и `service_account`, но не членство группы
- **Когда** манифест разбирается судьёй формы
- **Тогда** находка рода `ErrRelationRecipientKind`, и текст называет **принимаемые** виды
- **И** та же группа на `fga_writer` принимается *(обе стороны названы: отношение, принимающее
  членство, и отношение, его не принимающее)*

**MRW-11 · сверка паритета зелена на объявленной группе**

- **Дано** база, поднятая миграциями и приведённая применителем к манифесту Р1
- **Когда** прогоняется сверка посева
- **Тогда** находок ноль
- **И** перепись по подразделу групп печатает четыре величины, и «с владельцем» **не** ноль:
  живая `module-relation-writers` отнесена к модулю `iam`

**MRW-12 · объявленной группы нет в базе — находка остаётся**
*(отрицательный близнец MRW-11, одно отличие: строка группы удалена из базы до сверки)*

- **Дано** та же база, из которой строка группы удалена
- **Когда** прогоняется та же сверка
- **Тогда** находка «группа ОБЪЯВЛЕНА и не живёт», называющая модуль `iam` и файл манифеста

**MRW-13 · живая группа, которую не объявляет никто, владельца не получает**

- **Дано** база, в которой есть группа с именем, не объявленным ни одним прочитанным
  манифестом
- **Когда** прогоняется сверка
- **Тогда** находок по ней нет
- **И** перепись считает её в разряде «без модуля-владельца», и это число печатается

**MRW-14 · выдача роли усыновляется своим естественным ключом**

- **Дано** манифест, объявляющий выдачу **ролью** субъекту, у которого такая активная выдача в
  базе уже есть с другим идентификатором
- **Когда** применяется посев
- **Тогда** применение проходит, строк выдачи остаётся одна, изменений ноль
- **И** та же выдача, которой в базе нет, заводится, и перепись считает её записанной
  *(обе стороны оси «строка есть / строки нет» названы)*

### S2 — `module-quota-readers` снимается

**MRW-15 · вступлений в неё не объявляет ни один манифест платформы**

- **Дано** дерево платформы после стадии S2а
- **Когда** обходятся разделы `seed` всех её манифестов
- **Тогда** вступлений в `module-quota-readers` — **ноль**
- **И** вступлений в `module-relation-writers` — **пять**, по одному на модуль
  *(обе стороны названы: снимается одно, остаётся другое)*

**MRW-16 · строки снимаются, кортежи выводятся, след остаётся**

- **Дано** установка, чья база несёт группу `module-quota-readers` и **5** её членств
- **Когда** применяется миграция стадии S2б
- **Тогда** строки группы и всех её членств в базе нет
- **И** на каждое снятое членство журнал прав получил строку снятия кортежа членства
- **И** в очереди аудита есть запись о снятии, называющая арендатора, и её тип события — тот
  же, что производит штатный отзыв
- **И** перепись снятого напечатана числами

**MRW-17 · установка без предмета — перепись, а не отказ**
*(отрицательный близнец MRW-16, одно отличие: группы в базе нет)*

- **Дано** установка, поднятая после стадии S2б, где группы уже нет
- **Когда** та же миграция применяется повторно
- **Тогда** отказа нет, снято ноль, и перепись говорит это **словом**, а не молчанием

**MRW-18 · доставка, ещё объявляющая вступление в снятую группу, получает НАЗВАННЫЙ отказ**

- **Дано** установка после стадии S2б, чья доставка несёт манифест модуля со вступлением в
  `module-quota-readers` (платформа не обновлена)
- **Когда** служба стартует
- **Тогда** применение посева отказывает, и текст отказа называет **пару** (аккаунт, имя)
  ненайденной группы и **модуль**, чей манифест её называет
- **И** та же установка с доставкой после S2а стартует *(обе стороны названы)*

---

## §5 Конструируемость «Дано» — дом и одно-фактная дельта

| сценарий | дом «Дано» | ОДИН факт, которым он отличается от близнеца |
|---|---|---|
| MRW-01 | база из миграций дерева + манифест Р1 | — базовое состояние |
| MRW-02 | состояние после MRW-01 | второй старт |
| MRW-03 | то же, что MRW-02 | строка выдачи заведена сводом (рукотворный идентификатор) |
| MRW-04 | база + манифест Р1 + каталог доставки с одним манифестом модуля | доставка есть |
| MRW-05 | то же, без каталога доставки | доставки нет |
| MRW-06 | документ манифеста в памяти судьи формы | выдача не названа |
| MRW-07 | тот же документ | выдача названа |
| MRW-08 | тот же документ | `scopeType` = `iam.account` вместо `iam.cluster` |
| MRW-09 | тот же документ | имя отношения вне канона |
| MRW-10 | тот же документ | отношение `system_viewer` вместо `fga_writer` |
| MRW-11 | база, приведённая применителем к Р1 | — базовое состояние сверки |
| MRW-12 | та же база | строка группы удалена |
| MRW-13 | та же база | добавлена группа, не объявленная никем |
| MRW-14 | база + манифест с выдачей ролью | форма выдачи — роль, а не отношение |
| MRW-15 | дерево платформы после S2а | — перепись, состояние одно |
| MRW-16 | база до S2б | группа и членства есть |
| MRW-17 | база после S2б | группы нет |
| MRW-18 | база после S2б + доставка до S2а | доставка отстала |

**Чем каждый дом строится.** Состояния базы — поднимаемой в пробе базой службы, к которой
применяются миграции дерева; состояния манифеста — документом, поданным судье формы, без
файла в дереве; состояния доставки — каталогом, собранным пробой. Ни одно «Дано» не требует
состояния, которого схема не допускает: все строки, о которых идёт речь, схема уже держит
(группа, выдача формы отношения на кластере, членство), а живые их экземпляры в базе есть
сегодня (§1.3).

**Одно «Дано» требует оговорки.** MRW-03 просит живую строку с рукотворным идентификатором.
Она **не синтетика**: именно её заводит `0001_initial.sql:3196` на **каждой** установке,
включая чистую. То есть MRW-03 и MRW-01 описывают одну и ту же базу с разных сторон, и
различие между «чистой» и «живой» установкой здесь не в этой строке, а в том, применялся ли
посев прежде.

---

## §6 Стадии и признак готовности

Стадия — самостоятельный сдаваемый срез со своим признаком готовности.

### S1 — манифест службы сеет свою группу (репозиторий `PRO-Robotech/kaname`)

1. раздел `seed` у `manifest.yaml` службы: одна группа, одна выдача (Р1);
2. манифест встроен в образ и доезжает до применителя **раньше** доставленных (Р2, Р3);
3. идемпотентность выдачи по естественному ключу (Р4), **обе** формы;
4. атрибуция живой группы по объявлению (Р5), с переписью в обе стороны;
5. сценарии `MRW-01…MRW-14` — пробами; отрицания — с положительными близнецами;
6. шапки, приведённые к факту: `internal/moduleseedparity/parity.go` (§2.4) и раздел «РАЗДЕЛА
   `seed` У ЭТОГО МАНИФЕСТА НЕТ» в самом `manifest.yaml`.

**Признак готовности S1:** сверка посева зелена, перепись групп печатает «с владельцем» не
ноль; применение на базе с живыми строками свода проходит без отказа; ни одно из двух
утверждений п. 6 в дереве не встречается.

### S2а — вступления в снимаемую группу уходят из манифестов платформы (репозиторий `PRO-Robotech/kacho`)

Пять правок, по одной на манифест (§1.4, левый столбец), плюс шапки пяти манифестов, которые
после S1 утверждают неверное: группу заводит **служба**, а не платформа.

**Признак готовности S2а:** `git grep -c 'module-quota-readers' --
'PRO-Robotech/kacho:services/*/manifest.yaml'` → **0** во всех пяти;
`git grep -c 'module-relation-writers' -- 'PRO-Robotech/kacho:services/*/manifest.yaml'` →
**1** во всех пяти.

### S2б — строки снимаются миграцией службы (репозиторий `PRO-Robotech/kaname`)

Новая миграция: 5 членств, строка группы, снятие кортежей членства через журнал, след аудита,
перепись. Сценарии `MRW-16…MRW-18`.

**Признак готовности S2б:** в базе после применения нет ни группы, ни её членств; на каждое
снятое членство есть строка снятия в журнале; запись аудита одна на каждое снятие; повторное
применение даёт «снято 0» и не отказывает.

**Предикат допуска S2б — ВНЕШНИЙ, и это сказано прямо.** Стадия не применяется, пока S2а не
выпущена платформой и не доехала доставкой до установок. Машинной проверки у этого условия
нет: миграция исполняется в init-контейнере и каталога доставки не читает by construction.
Держится это **порядком работ**, а не гейтом; признак для ведущего линию — S2а влита в ствол
платформы и попала в выпуск.

---

## §7 Что документ НЕ решает — вынесено явно

| предмет | почему не здесь |
|---|---|
| состав получателей права `fga_writer` | не меняется: те же пять служебных записей. Изменение состава — выдача или отзыв, и у него своя поверхность |
| снятие группы `module-relation-writers` | она остаётся; предмет работы — **кто её объявляет** |
| имя системного аккаунта `kacho-system` в дереве службы | остаток бренда, `PRO-Robotech/kacho#2076`; манифест называет аккаунт **живым** написанием, и переименование — своя работа со своим окном написаний (`internal/domain/seed_identity_window.go`) |
| снятие строк модулей у применителя | применитель не снимает строк, и это объявленный остаток его шапки, а не забытая половина |
| исторический тип якоря `iam_fgaproxy` | живых фактов на нём нет, снимается вместе со своими применёнными миграциями — сказано в каноне (`sed -n '936,952p' proto/kaname/cloud/iam/v1/fga_model.fga`) |
| доставка манифеста службы соседям | службе он не нужен: свои типы она берёт из встроенного канона образа (`internal/authzmodel/authzmodel.go:67`) |

---

## §8 Ведомость «сценарий → производитель»

Строка называет либо **живого** производителя (координата, проверенная командой), либо
**стадию**, которая его заводит. Третьего состояния в ведомости нет.

| сценарий | производитель «Тогда» | состояние |
|---|---|---|
| MRW-01 | применитель (П3) + писатель группы (П4) + встроенный манифест | П3, П4 живы; встроенность — **S1** |
| MRW-02 | перепись `Report`/`Census` применителя (`internal/apps/kaname/moduleseed/apply.go`, типы `Report` и `Census`) | живой; разряд «свой манифест» — **S1** |
| MRW-03 | конфликт по естественному ключу | **S1** (Р4) |
| MRW-04 | порядок применения | **S1** (Р3) |
| MRW-05 | ветвь «доставка не объявлена» (`cmd/kaname/module_manifests.go:60–68`) | живой; заведение своей группы без доставки — **S1** |
| MRW-06 | `ErrGroupNeverGranted` (П6) | **живой** |
| MRW-07 | тот же судья на годном документе | **живой** |
| MRW-08 | `ErrBindingAnchor` (П7) | **живой** |
| MRW-09 | `ErrRelationNotDeclared` (`internal/manifest/linkage.go:81`) | **живой** |
| MRW-10 | `ErrRelationRecipientKind` (П7) + `AcceptsKind` (`internal/authzmodel/relationsubjects.go:145`) | **живой** |
| MRW-11 | четвёртый `diffSet` сверки (П8) + перепись `Subsection` | П8 жив; «с владельцем» не ноль — **S1** (Р5) |
| MRW-12 | тот же `diffSet`, сторона «объявлено и не живёт» | **живой**, доказан инъекцией |
| MRW-13 | разряд «без модуля-владельца» переписи | **живой**; правило владельца для групп — **S1** |
| MRW-14 | конфликт по `access_bindings_active_grant_uniq` (`0001_initial.sql:4443`) | индекс жив; применение по нему — **S1** |
| MRW-15 | обход разделов `seed` манифестов платформы | **S2а** |
| MRW-16 | новая миграция службы | **S2б** |
| MRW-17 | её перепись | **S2б** |
| MRW-18 | отказ резолва группы (`internal/repo/kaname/pg/module_seed_writer.go:380`) | текст, называющий пару, **жив**; называние модуля — **S1** |

Живых производителей — **7** из 18; остальные заводит названная стадия. Ни одного «Тогда» без
того или другого в документе нет.

---

## §9 Чем это будет держаться

| требование | чем | почему не иначе |
|---|---|---|
| форма манифеста службы | тот же судья формы и связности, что у модулей | второй судья для второго документа разошёлся бы с первым молча |
| порядок «своё раньше доставленного» | проба применителя, подающая манифест модуля со вступлением **и** свой манифест, и требующая, чтобы вступление легло | проба на одном манифесте о порядке не утверждает ничего |
| идемпотентность по естественному ключу | интеграционная проба на базе, где строка заведена **рукотворным** идентификатором | синтетическая строка с выведенным идентификатором зелена при любом устройстве конфликта |
| атрибуция группы | инъекция в обе стороны: объявленная группа находится по владельцу, не объявленная — считается | односторонняя проба зеленела бы на правиле, относящем к владельцу всё подряд |
| снятие `module-quota-readers` | проба миграции на базе с предметом **и** на базе без него | вторая половина отличает «снято ноль» от «не исполнилось» |
| шапки, приведённые к факту | вниманием круга ревью | предиката у «шапка согласна с телом пакета» нет; проверяемый признак — оба утверждения §2.4 в дереве не встречаются |

**Названо честно:** у последней строки машинного держателя нет, и он здесь не обещается.

---

## §10 Публичный репозиторий

Оба репозитория публичны. Документ называет **предмет**, **решение** и **следствие для
контракта** и не собирает в одном месте тройку «координата + условие, при котором защита не
действовала + следствие для постороннего».

Что названо и почему это безопасно: имена группы, отношения и якоря лежат в открытом своде и
в открытом каноне; состав получателей права работой **не меняется**; отказ, описанный в §2.2,
есть отказ **старта службы**, а не обход защиты — он закрывает установку, а не открывает её.
Идентификаторы строк (`grp…`, `acb…`) — идентификаторы **системных** строк, одинаковые на
каждой установке по построению свода, и адресовать ими снаружи нечего: обе строки защищены от
удаления и наружной поверхности не имеют.
