<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка MRW-1: группу пишущих кортежи объявляет манифест службы

- **Статус:** DRAFT — кругов ревью **ноль**, редакция 1 к первому кругу; вердикт выносит
  `acceptance-reviewer`. Санкции на объём этого документа нет (ban #1): ни одна одобренная
  приёмка не разрешает манифесту службы сеять — сегодня он не сеет вовсе (§1.1)
- **Редакция 3 — правка ПОСЛЕ объявления состояния (2026-09-16), ответ на круг 1.** Строка
  «Статус» выше оставлена словами редакции 1 намеренно: статическую форму шапки после ревью не
  правят (`change-graph.md` §«Вердикт привязан к ОТПЕЧАТКУ, а не к документу») — действующее
  состояние выводится из записи ревью, а не из слова в заголовке. Круг 1 вынес
  **CHANGES_REQUESTED** отпечатку `7feb33fd…1436`, то есть содержимому **редакции 2**
  (`3fc15570`, 1153 строки); запись лежит соседним файлом этого же дерева —
  `docs/specs/reviews/service-manifest-seeds-its-own-groups/7feb33fd9c2cb68be55291dc8be0ae3740ba0d744a3926e7e519b8fb50a61436.yaml`
  (коммит `87ada2cf`). Блокирующих **пять**, и **ни одно не опровергнуто**: каждое перемерено
  этой редакцией своим предикатом и подтвердилось. **История ревью ДОБАВЛЯЕТСЯ, а не
  переписывается** — запись круга 1 не правлена ни одной строкой. Что изменено и чем закрыт
  каждый пункт — **§11**
- **Редакция 4 — доводка по находкам оппонента (2026-09-16), ДО круга 2.** Записи ревью круга 2
  в дереве **нет**: `ls docs/specs/reviews/service-manifest-seeds-its-own-groups/` → один файл,
  отпечаток редакции 2. Значит четыре находки, по которым сделана эта редакция, — **не вердикт
  круга**, а данные, и каждая перемерена своим предикатом. Подтвердились **четыре из четырёх**,
  опровергнуто **ноль**; сверх них перемер класса добавил **пятую** координату, которой не
  называл никто. Что изменено и чем закрыт каждый пункт — **§12**
- **Редакций ЧЕТЫРЕ, и различают их отпечатки, а не слово в шапке.** Редакция 1 — `d66107b2`,
  766 строк, отпечаток `b4c62bdb…63f7`; редакция 2 — `3fc15570`, 1153 строки, отпечаток
  `7feb33fd…1436`, её и читал круг 1; редакция 3 — `36ec0069`, 1566 строк, отпечаток
  `ee55c22f…a009`; редакция 4 — эта. Предикат любой из них:
  `git show <ревизия>:docs/engineering/acceptance/service-manifest-seeds-its-own-groups.md | sha256sum`.
  Отпечаток нынешнего файла с записанным в круге 1 **не совпадает, и это не расхождение**:
  редакции 3 и 4 рождают содержимое, которого круг 1 не читал, и вердикт о нём не переносится
  (`change-graph.md` §2). Слова «прежняя редакция» в теле документа означают **редакцию 1**,
  если рядом не сказано иное
- **Задача:** `PRO-Robotech/kaname#106`, метки `area:iam`, `P1`, `release:standalone-iam`,
  `size:M`; состояние **OPEN** (`gh issue view 106 -R PRO-Robotech/kaname --json state`,
  2026-09-16). Решение владельца в её теле: **`module-relation-writers` описывается в YAML
  службы**
- **Сервис:** `kaname` — манифест службы, применитель посева, писатель строк посева, сверка
  паритета, миграции. Платформа (`PRO-Robotech/kacho`) участвует **одной** правкой — вступления
  в снимаемую группу уходят из пяти её манифестов (§6, стадия S2а); её код не трогается
- **Формат:** Given-When-Then, только markdown — кода здесь нет по построению
- **Публичный репозиторий:** оба репозитория публичны; документ написан так, чтобы выдерживать
  публикацию — §10
- **Ревизии измерения — по дому, и все замеры ниже сняты на них:** служба
  `PRO-Robotech/kaname@83ddd08a84` · платформа `PRO-Robotech/kacho@45d1719fe8` · фундамент
  `PRO-Robotech/corelib@1d25468c19` (тег `v1.8.0`, он же пин службы — `go.mod`) · воркспейс
  `PRO-Robotech/kacho-workspace@0290f73e94`. Координата без приставки дома — координата дерева
  службы; приставка `PRO-Robotech/kacho:` означает чужое дерево, и зелёный тамошнего гейта об
  этом дереве не говорит ничего
- **Круг 1 СОСТОЯЛСЯ и оборван не был — посылка задания круга 2 опровергнута замером.** Задание
  говорило «рецензия оборвана окном». Запись круга лежит в дереве целиком: вердикт, пять
  блокирующих, шесть незаблокировавших, перечень проверенного, раздел пределов и раздел
  `next_round` — оборванной она не является ни одним признаком. Что было верно для **первого**
  круга (записи ревью не существовало вовсе) и перестало быть верным вместе с ним — §2.1
- **А вот ВТОРОГО круга не было вовсе, и это другое утверждение.** Задание редакции 4 говорит
  «рецензия оборвана окном» про круг 2; замер: записей в каталоге **одна**, и её отпечаток —
  редакции 2. То есть у редакции 3 вердикта нет **никакого** — ни оборванного, ни вынесенного, —
  и четыре находки, по которым сделана редакция 4, пришли **не из записи**. Смешивать их с
  блокирующими круга 1 нельзя: у тех есть запись, у этих — нет (§2.1, §12)
- **Объявление о ревизиях выше остаётся верным и для редакции 3, и это ЗАМЕР, а не допущение.**
  Числа редакции 3 сняты на голове ветки `acceptance/106-service-manifest-groups`; от
  объявленной `83ddd08a84` она отличается **тремя** файлами, и все три — документация (две
  приёмки и запись круга 1): `git diff --stat 83ddd08a84..<голова ветки>`. Код, о котором
  документ утверждает, между этими ревизиями не двигался, поэтому датировкой остаётся
  `83ddd08a84` — она **в стволе**, и перемерить по ней можно; голова ветки в ствол ещё не
  влита, и датировать ею значило бы назвать читателю дерево, которого у него нет

---

## §0 Обзор

Право «писать кортежи владения через службу» — поверхность **самой службы**
(`RegisterResource` / `UnregisterResource`). Аудиторию этой возможности сегодня объявляет не
владелец возможности, а **свод**: группу `module-relation-writers`, её выдачу `fga_writer` на
кластере и пятерых её членов заводит применённая миграция `0001_initial.sql`. Манифест службы
раздела посева не имеет вовсе, а пять манифестов модулей платформы лишь **вступают** в группу
и в своих шапках утверждают, что «эти группы заводит платформа», — чего не делает никто.

Документ закрепляет семь вещей.

1. **Группу и её выдачу объявляет манифест службы** — тот же раздел `seed`, что у модулей, та
   же форма, тот же применитель, тот же валидатор связности.
2. **Манифест службы приезжает применителю ВСТРОЕННЫМ В ОБРАЗ, а не доставкой — и механизм
   назван вместе с его держателем.** Доставка собирается обходом дерева платформы, где службы
   больше нет ни одним файлом (§1.1), а копия её манифеста в чужом репозитории запрещена
   решением владельца (ban #20). Встраивание делается **второй копией в каталоге пакета**, и её
   побайтовое согласие с манифестом дерева держит цель сборки — та же цена и тот же приём, что
   у канона модели прав в этом же дереве (§2.6, Р2). Встроенность даёт и порядок: своё
   применяется **раньше** доставленного, поэтому вступление модуля находит группу заведённой.
3. **Манифест службы получает ОДИН потребитель из пяти, и по каждому названо почему.** Перечень
   доставки на пути старта читают пятеро (§2.7); подмешанный в него манифест достался бы всем
   разом — вплоть до второго писателя семи строк каталога и до раздела `roles:`, чей исход уже
   решён соседней APPROVED-приёмкой. Поэтому манифест едет применителю посева **отдельным
   доводом** (Р3).
4. **Форму субъекта FGA у получателя-ГРУППЫ применитель берёт У КАНОНА.** Получателем выдачи
   впервые становится группа, и канон принимает её только формой членства; писатель строк посева
   сегодня строит субъекта встроенным выражением, которое на группе с каноном расходится
   (§2.5). Форма берётся вызовом объявленного источника, а не третьей копией (Р1а).
5. **Идемпотентность выдачи держит ЕСТЕСТВЕННЫЙ КЛЮЧ живой строки, а не совпадение
   выведенного идентификатора.** Это измеренное опровержение предиката задачи: идентификатор
   живой строки `fga_writer` выведен тем же выражением, но при **прежнем написании якоря**, а
   якорь с тех пор переименован миграцией — и переименование идентификатора не касается. Значит
   выражение применителя даёт **другое** значение на каждой установке, включая чистую; вторая
   строка не «ложится поверх», а **отвергается** частичным уникальным индексом — применение
   обрывается и служба не поднимается (§2.2).
6. **`module-quota-readers` снимается целиком** — вступления из пяти манифестов платформы,
   членства и строка группы из базы службы, кортежи членства через очередь, со следом аудита.
   Её выдачу `quota_reader` отозвала миграция `20260914091500`; группа осталась пустой и
   ничего не открывает.
7. **Сверка паритета относит живую группу к модулю, ЧЕЙ МАНИФЕСТ ЕЁ ОБЪЯВЛЯЕТ.** Сегодня
   владелец выводится из имени вида `kacho-<служба>`; `module-relation-writers` такому имени не
   отвечает, поэтому объявление её манифестом службы дало бы **ложную находку** «группа
   ОБЪЯВЛЕНА и не живёт» (§1.5).

Чего документ **не** делает: не меняет модель прав, не трогает ни одного глагола, не расширяет
и не сужает круг тех, кто пишет кортежи. Состав получателей права `fga_writer` до и после —
те же пять служебных записей модулей платформы. Предмет здесь один: **кто объявляет**, а не
**кому дано**.

---

## §1 Перепись производителей — ДО сценариев

Класс, отвергавший работу этой линии больше двух раз, — «Тогда» без производителя. Поэтому
раздел стоит первым, а не последним: по каждому утверждению документа ниже назван либо
**живой производитель координатой**, либо **стадия с открытой задачей**.

### §1.1 Как манифест службы применяется сегодня — НИКАК

Три независимых предиката, и все три дают ноль.

```sh
# 1. раздела посева у манифеста службы нет
git grep -c '^seed:' -- manifest.yaml | wc -l               # → 0

# 2. встроенного в образ читателя своего манифеста нет
#    ОБЛАСТЬ СУЖЕНА ДО КОДА НАМЕРЕННО: без `-- '*.go'` предикат находит СОБСТВЕННУЮ
#    строку этого документа (и строку записи круга 1), то есть считает своё
#    объяснение — `testing.md` §«Гейт на класс», п. 4. Существо не меняется:
#    директив `go:embed` в прод-коде девять, ни одна не про манифест.
git grep -n 'go:embed manifest.yaml' -- '*.go' ':!*_test.go'   # → пусто
git grep -n 'go:embed.*manifest'     -- '*.go'                 # → пусто

# 3. чарт службы своего манифеста никуда не монтирует
git grep -n 'manifests' -- 'deploy/*.yaml'                 # → пусто
```

Четвёртый предикат — в чужом дереве, и он объясняет, **почему** доставка его не привозит:
производитель ConfigMap доставки обходит `services/*/manifest.yaml` **дерева платформы**, а
каталога службы там больше нет ни одним файлом.

Предикаты чужого дерева записаны ИСПОЛНИМОЙ формой — `git -C <копия платформы>` с прибитой
ревизией, — а не приставкой `PRO-Robotech/kacho:`: приставка объявлена соглашением шапки и
читается однозначно, но прогнать её как записано нельзя, и читатель переводит её сам.

```sh
K=45d1719fe8dd76728847f45e901bc5ded7ea993d   # ревизия платформы, объявленная шапкой

# каталог службы в дереве платформы — снят вынесением (kacho#2598)
git -C <копия платформы> ls-tree -r $K --name-only | grep -c '^services/iam/'      # → 0

# манифесты, которые доставка собирает
git -C <копия платформы> ls-tree -r $K --name-only \
  | grep -E '^services/[^/]+/manifest\.yaml$'                                      # → 5:
#   compute · nlb · registry · storage · vpc
```

Каталог обхода объявлен константой `servicesDir` в фундаменте
(`PRO-Robotech/corelib@v1.8.0:modulemanifest/producer/producer.go`, строки 55–58 и 247–260), и
ключ ConfigMap выводится из имени каталога службы.

**Следствие, несущее для §3:** сегодня у манифеста службы нет читателя **ни на одном** пути —
ни доставки, ни встроенности, ни монтирования. Значит раздел `seed`, дописанный в него без
второй половины работы, был бы объявлением без производителя — ровно тот класс, который
`api-conventions.md` §«Принято-и-проигнорировано» называет запрещённым.

### §1.2 Что ГОТОВО и работает — ОДИННАДЦАТЬ производителей, каждый координатой

| # | утверждение, которое этот производитель держит | координата | предикат |
|---|---|---|---|
| П1 | форма `seed.groups[]` объявлена схемой и требует трёх ключей — `name`, `account`, `description` | `schema/module-manifest.schema.json` | `python3 -c "import json;s=json.load(open('schema/module-manifest.schema.json'));print(s['properties']['seed']['properties']['groups']['items']['required'])"` → `['name', 'account', 'description']` |
| П2 | `iam` — законное значение ключа `module`: набор закрыт и его содержит | тот же файл | `python3 -c "import json;print(json.load(open('schema/module-manifest.schema.json'))['properties']['module']['enum'])"` → шесть имён, `iam` первое |
| П3 | применитель умеет заводить группу и выдавать ей **отношение**; порядок внутри модуля — личности → группы → вступления → выдачи | `internal/apps/kaname/moduleseed/apply.go` | `git grep -n 'seed.Groups\|GrantRelation' -- internal/apps/kaname/moduleseed/apply.go` |
| П4 | строка группы кладётся **поверх живой, не меняя идентификатора**: конфликт по `groups_account_name_unique`, идентификатор участвует только во вставке | `internal/repo/kaname/pg/module_seed_writer.go:189–199` | `sed -n '189,199p' internal/repo/kaname/pg/module_seed_writer.go` |
| П5 | канон объявляет `cluster#fga_writer` принимающим `group#member`, то есть выдача группе на кластере **исполнима** | `proto/kaname/cloud/iam/v1/fga_model.fga:294` | `git grep -n 'define fga_writer' -- proto/kaname/cloud/iam/v1/fga_model.fga` → две строки: `:294` у `cluster`, `:954` у исторического якоря |
| П6 | валидатор связности требует, чтобы заведённую манифестом группу **он же и одарил** | `internal/manifest/linkage.go:63` | `git grep -n 'ErrGroupNeverGranted' -- internal/manifest` |
| П7 | выдача посева принимается **только** на кластерном singleton'е; вид получателя судится **каноном**, а не вторым перечнем | `internal/manifest/linkage.go:96`, `internal/manifest/relationgrant.go:186–202` | `git grep -n 'ErrBindingAnchor\|ErrRelationRecipientKind' -- internal/manifest` |
| П8 | сверка паритета судит **группы наравне** с личностями, вступлениями и выдачами | `internal/moduleseedparity/parity.go:241–244` | `git grep -n 'группа ЖИВЁТ и не объявлена' -- internal/moduleseedparity` |
| П9 | форма субъекта FGA объявлена **одним** местом на всё дерево: у группы она несёт сигил членства, у служебной записи и человека — нет | `internal/domain/subject.go:22` | `git grep -n 'FGASubjectRef' -- '*.go' ':!*_test.go'` → **5** строк в **3** файлах; все три вызывающих — в `internal/apps/kaname/api/access_binding` |
| П10 | манифест службы **уже** опознан деревом как манифест модуля, а 19 вставок роли `iam` в применённых миграциях объявлены **отдельным предметом** | `internal/check/migration_not_a_writer_of_module_role_test.go` | `go test -count=1 -short -v -run TestMODRD23 ./internal/check/` → «манифестов модулей найдено 1 [iam] … исторический остаток … 19 … их переезд к применителю есть отдельный предмет» |
| П11 | строки каталога модуля `iam` (**7** ресурсов) сеет **миграция**, а не применитель каталога | `internal/migrations/0001_initial.sql` | `grep -c "INSERT INTO kaname.catalog_resource .*'iam'" internal/migrations/0001_initial.sql` → **7** |

**П8 — измеренное опровержение половины предиката задачи.** Предикат 4 задачи требует, чтобы
сверка «судила группы наравне с учётками». Она **уже** их судит: у `Compare` четыре вызова
`diffSet`, четвёртый — по группам, и обе его стороны доказаны инъекцией
(`TestInjectionSeedParityLiveGroupNotDeclaredIsAFinding`,
`TestInjectionSeedParityDeclaredGroupNotLiveIsAFinding`). Открытой остаётся вторая половина
предиката (шапка) и **не названная задачей** третья — атрибуция владельца (§1.5).

**П9 — производитель ЖИВ, но применитель посева его НЕ ЗОВЁТ, и это несущее различие.**
Форма субъекта объявлена ровно одним местом, чей собственный godoc называет себя «SINGLE
source of truth» и требует побайтовой симметрии у всех, кто её строит. Писатель строк посева
в число вызывающих не входит: `git grep -c 'FGASubjectRef' -- internal/repo/kaname/pg/` даёт
**0** файлов. Он строит форму **встроенным выражением**, и на получателе-ГРУППЕ это выражение
расходится с каноном — разбор и опыт в §2.5, пробел О6 в §1.5.

### §1.3 Что заводит эти строки СЕГОДНЯ — свод, поимённо

```sh
git grep -n 'module-quota-readers\|module-relation-writers' -- internal/migrations/0001_initial.sql
#   :3488  INSERT INTO kaname.groups … 'module-quota-readers'
#   :3489  INSERT INTO kaname.groups … 'module-relation-writers'
```

| строка свода | что заводит | число |
|---|---|---|
| `0001_initial.sql:3488` | группа `module-quota-readers`, идентификатор `grp1ed8897b56bb9106f` | 1 |
| `0001_initial.sql:3489` | группа `module-relation-writers`, идентификатор `grp258e6bbe9bbe45568` | 1 |
| `0001_initial.sql:3196` | выдача `fga_writer` группе на кластере, идентификатор `acb9e920fb038c7e4f74` | 1 |
| `0001_initial.sql:3190` | выдача `quota_reader` группе на кластере, идентификатор `acb1296746dc06ec9e25` | 1 |
| `0001_initial.sql:3472…` | членства — по **5** в каждой группе | 10 |

Предикат числа членств: `git grep -c "'grp1ed8897b56bb9106f', 'service_account'" --
internal/migrations/0001_initial.sql` → **5**; та же команда по `grp258e6bbe9bbe45568` → **5**;
всего строк `group_members` в своде — **10**.

Выдачу `quota_reader` **уже отозвала** миграция
`20260914091500_limit_reader_grant_is_revoked.sql` (kaname#59). Она же в своей шапке объявляет,
ПОЧЕМУ группа осталась: «НЕ снимается группа-получатель `module-quota-readers` … пустая группа
не даёт никому ничего, а производителя, который завёл бы её заново, нет — валидатор связности
требует, чтобы заведённую манифестом группу он же и одарил. Снятие сломало бы установку
платформы» (`sed -n '19,24p' internal/migrations/20260914091500_limit_reader_grant_is_revoked.sql`).

Тот же довод своими словами говорит соседняя миграция, и она же называет держателя, которого
не хватало: «объявить её посевом СЕГОДНЯ нельзя — валидатор связности требует, чтобы
заведённая манифестом группа была им же и одарена … а выдача этих групп принадлежит службе, не
модулю» (`sed -n '66,69p' internal/migrations/20260909202745_module_identities_leave_the_baseline.sql`,
раздел «ГРУППЫ НЕ СНИМАЮТСЯ»).

**Это и есть предмет задачи, названный обеими миграциями заранее:** держателя, которого не
хватало, зовут «раздел `seed` у манифеста службы».

### §1.4 Вступления модулей — что снимается со стороны платформы

```sh
K=45d1719fe8dd76728847f45e901bc5ded7ea993d
for m in compute nlb registry storage vpc; do
  printf -- '-- %s\n' "$m"
  git -C <копия платформы> show $K:services/$m/manifest.yaml | grep -n 'name: module-'
done
#   по 2 строки в каждом из пяти манифестов: module-quota-readers и module-relation-writers
```

| модуль платформы | вступление в `module-quota-readers` | вступление в `module-relation-writers` |
|---|---|---|
| `compute` | `services/compute/manifest.yaml:268` | `:271` |
| `nlb` | `services/nlb/manifest.yaml:283` | `:286` |
| `registry` | `services/registry/manifest.yaml:188` | `:191` |
| `storage` | `services/storage/manifest.yaml:149` | `:152` |
| `vpc` | `services/vpc/manifest.yaml:490` | `:493` |

Снимается **левый столбец** — пять вступлений. Правый остаётся нетронутым: он и есть то
членство, ради которого группа существует.

Шапки этих же пяти манифестов несут утверждение, которое станет ложным правой половиной
работы и верным — левой: «`module-quota-readers` и `module-relation-writers` заводит
платформа». Предикат —
`for m in compute nlb registry storage vpc; do git -C <копия платформы> show $K:services/$m/manifest.yaml | grep -c 'заводит платформа'; done`
→ **1** в каждом из пяти.

### §1.5 Чего производителя НЕТ — семь пробелов, каждый со своей стадией

| # | «Тогда», у которого сегодня нет производителя | чем это видно | стадия |
|---|---|---|---|
| О1 | манифест службы доезжает до применителя | §1.1, три нуля | S1 |
| О2 | применитель применяет свой манифест **раньше** доставленных | `ApplyAll` идёт по перечню в порядке обхода, а обход лексический: `compute` < `iam`, то есть вступление `compute` встретилось бы **раньше** группы | S1 |
| О3 | выдача, чья живая строка заведена сводом, применяется **без второй строки и без отказа** | §2.2 — измеренное опровержение | S1 |
| О4 | живая группа относится к модулю, **объявившему** её | владелец выводится из имени `kacho-<служба>` (`grep -n 'seededNamePrefix' internal/moduleseedparity/parity_test.go` → `:98–100`, значение `"kacho-"`); `module-relation-writers` такому имени не отвечает и считается «без модуля-владельца» | S1 |
| О5 | `module-quota-readers` и её членства сняты | строки живы, §1.3 | S2 |
| О6 | форма субъекта FGA у получателя-**группы** несёт сигил членства | применитель строит форму встроенным выражением и канона (П9) не зовёт; на группе формы расходятся — опыт §2.5 | S1 |
| О7 | манифест службы лежит в образе так, что его согласие с `manifest.yaml` дерева чем-то держится | ни встроенной копии, ни копирования в образ, ни держателя согласия — §2.6 | S1 |

**О2 — механика, а не догадка.** `ApplyAll` перебирает манифесты в том порядке, в каком их
отдал читатель доставки (`internal/apps/kaname/moduleseed/apply.go`, цикл `for _, m := range
manifests`), а читатель строит перечень обходом `filepath.WalkDir`
(`internal/manifest/check.go:379–382`), который идёт по именам лексически. Группа заводится
внутри `applyOne` **своего** модуля, а вступление `compute` — внутри своего; между модулями
порядка нет никакого, кроме имени. Отказ был бы назван
(`groupID`: «группа %s/%s не резолвится»), но это всё равно отказ старта.

**О4 — цена названа числом.** Пока правило владельца прежнее, объявление группы манифестом
службы даёт сверке пару «объявлено 1 · живых по модулю `iam` 0» и **ложную находку** «группа
ОБЪЯВЛЕНА и не живёт». Ложная находка у сверки дороже отсутствующей: проверку, краснеющую на
верном дереве, снимают первой.

**О6 — расхождение ровно на том виде получателя, который вводит Р1.** Живых выдач посева
сегодня две, и обе — служебным записям (§2.5), то есть ветвь получателя-группы у применителя
**не исполняется ни разу**. Дефект формы поэтому не красный и не зелёный: он невидим, пока
группа не станет получателем, — а получателем её делает ровно этот документ.

**О7 — исходов встроенности ТРИ, и у каждого своя цена.** Прежняя редакция называла исход
(«встроен в образ») и не называла механизма, тогда как оба механизма требуют того, чего
документ не объявлял: копии внутри каталога пакета с держателем её согласия — либо пути в
образе со стражем старта. Разбор — §2.6, решение — Р2.

---

## §2 Опровержения посылок — замером, а не мнением

### §2.1 Посылки о кругах — обе перемерены, и ответ у них РАЗНЫЙ

Здесь две посылки двух разных заданий, и смешивать их нельзя: первая была опровергнута и
осталась опровергнутой **для своего круга**, вторая опровергнута этой редакцией.

**Посылка задания круга 1 — «есть предыдущие круги и последняя запись ревью» — была
ОПРОВЕРГНУТА и остаётся таковой применительно к кругу 1.** На редакции 2 записи ревью не
существовало ни в одной ревизии, кругов было ноль, блокирующих не было. Так же независимо
перемерил и сам круг 1 — его запись называет это третьей категорией, а не «повторено и
сошлось».

**Посылка задания круга 2 — «рецензия оборвана окном» — ОПРОВЕРГНУТА ЗАМЕРОМ.** Запись круга 1
лежит в дереве целиком и оборванной не является ни одним признаком: у неё есть вердикт
(`CHANGES_REQUESTED`), пять блокирующих, шесть незаблокировавших, перечень проверенного,
раздел пределов и раздел `next_round`.

```sh
R=docs/specs/reviews/service-manifest-seeds-its-own-groups
ls $R                                                  # → один файл, наш отпечаток
python3 -c "import yaml,sys;d=yaml.safe_load(open('$R/7feb33fd9c2cb68be55291dc8be0ae3740ba0d744a3926e7e519b8fb50a61436.yaml'));\
print(d['verdict'], len(d['blocking']), len(d['non_blocking']), len(d['next_round']))"
#   → CHANGES_REQUESTED 5 6 5
sha256sum docs/engineering/acceptance/service-manifest-seeds-its-own-groups.md   # редакция 3
git show 3fc15570:docs/engineering/acceptance/service-manifest-seeds-its-own-groups.md \
  | sha256sum                                          # → 7feb33fd…1436, предмет круга 1
```

> [!warning] Предикат, которым §2.1 доказывала «кругов ноль», УМЕР ОТ СОБСТВЕННОГО КРУГА — и
> это третий экземпляр одного класса в одном разделе
> Здесь стоял перечень каталогов записей ревью, когда-либо заведённых
> (`git log --all --diff-filter=A --name-only --format= -- docs/specs/reviews | …`), с выводом
> «восемь имён, этого среди них нет». Сегодня та же команда даёт **десять** имён, и десятое —
> `service-manifest-seeds-its-own-groups`: её завёл круг 1, положив свою запись в дерево. То
> есть предикат опровергается тем самым событием, ради отрицания которого приводился.
>
> Отдельно: он ходит по `--all`, то есть по ЛОКАЛЬНО вытянутым ссылкам, и между машинами
> невоспроизводим — число у него не бывает устойчивым by construction.
>
> Двумя сутками раньше здесь же умерли ещё два предиката — «нет ветки» и «нет документа ни на
> одной голове origin»: оба перестали быть верными, как только редакция 1 уехала на origin.
>
> **Класс один и тот же:** утверждение об отсутствии, доказываемое состоянием дерева, которое
> собственная работа и меняет. Противоядие — прибитая ревизия либо отказ от величины: ниже
> отсутствие кругов **для редакции 2** доказывается не перечнем каталогов, а прямым чтением
> дерева на прибитой ревизии.

```sh
# кругов у редакции 2 не было — на ревизии, которую читал круг 1, каталога записей нет
git ls-tree -r 3fc15570 --name-only \
  | grep -c 'docs/specs/reviews/service-manifest-seeds-its-own-groups/'      # → 0
```

### §2.2 Посылка задачи «применитель кладёт объявленное поверх живой строки, не меняя идентификатора» — ВЕРНА ДЛЯ ГРУППЫ И ЛОЖНА ДЛЯ ЕЁ ВЫДАЧИ

Предикат снятия 1 задачи утверждает это про обе строки разом. Замер разводит их.

**Для группы — верно** (П4). Конфликт разрешается по ограничению уникальности `(account_id,
name)`, идентификатор стоит только в списке значений вставки и на живую строку не переносится.
Это сказано и в шапке писателя: «У группы такой производной нет: живые группы несут случайные
идентификаторы. Поэтому её идемпотентность держит ограничение уникальности `(account_id,
name)`, а не совпадение идентификатора».

**Для выдачи — ложно, и ПРИЧИНА не та, которую называла прежняя редакция.** Идентификатор
выдачи применитель **выводит** выражением
(`internal/repo/kaname/pg/module_seed_writer.go`, раздел «Идентификатор выдачи ВЫВОДИТСЯ тем же
выражением, каким его вывела платформа»), и конфликт объявляет по `(id)`. Замер разводит **два**
входа этого выражения — форму субъекта и написание якоря:

```sh
python3 - <<'PY'
import hashlib
def acb(user, form, kind, anchor):
    return 'acb' + hashlib.md5(f'system-grant:{user}:{form}:{kind}:{anchor}'.encode()).hexdigest()[:17]
for user in ('group:grp258e6bbe9bbe45568', 'group:grp258e6bbe9bbe45568#member'):
    for anchor in ('cluster_root', 'cluster_kacho_root'):
        print(f'{user:42} @{anchor:18} -> {acb(user, "fga_writer", "cluster", anchor)}')
print('живая строка свода', ' ' * 41, '-> acb9e920fb038c7e4f74')
PY
#   group:grp258e6bbe9bbe45568                 @cluster_root       -> acbbcb7bb3d70aa9cc4b
#   group:grp258e6bbe9bbe45568                 @cluster_kacho_root -> acb7a84798bb6bfc448b
#   group:grp258e6bbe9bbe45568#member          @cluster_root       -> acbc5d31947bd0d570e8
#   group:grp258e6bbe9bbe45568#member          @cluster_kacho_root -> acb9e920fb038c7e4f74
#   живая строка свода                                             -> acb9e920fb038c7e4f74
```

**Живой идентификатор ВЫВЕДЕН, а не отчеканен рукой: он равен выражению на форме с сигилом
членства при ПРЕЖНЕМ написании якоря.** Совпадение точное и случайным быть не может —
семнадцать шестнадцатеричных знаков.

> [!warning] Прежняя редакция этого раздела утверждала обратное, и её ОПЫТ БЫЛ СЛЕП ровно по §2.5
> Здесь стояло: «ни одно из двух написаний якоря не даёт живого идентификатора — расхождение не
> следствие переименования якоря, а следствие того, что свод чеканил идентификаторы рукой».
> Опыт подавал выражению **голый объект группы** — ту самую форму, которой канон не принимает, —
> и потому не мог получить живого значения **ни при каком** якоре. Замер с формой канона даёт
> его с первой попытки.
>
> Это не отдельная небрежность, а **второй экземпляр одного класса**: там, где форма субъекта
> строится не вызовом объявленного источника (Р1а), ошибается не только продукт, но и тот, кто
> его измеряет. Свою ошибку называю прямо: форма была построена руками, число получилось верным
> для другого входа, и из него выведена неверная причина — при верном следствии.

**Причина расхождения — ПЕРЕИМЕНОВАНИЕ ЯКОРЯ, и она названа самим писателем заранее.** Якорь
переехал `cluster_kacho_root` → `cluster_root` миграцией
`20260906214500_cluster_anchor_moves_to_its_declared_spelling.sql`; переписчик правит **значение
якоря** во всех местах схемы и требует нулевого остатка прежнего написания, но идентификатор
выдачи непрозрачен и переписи не подлежит. Шапка писателя ровно это и оговаривает: «якорь входит
в выражение своим ЖИВЫМ написанием. Установка, пережившая переименование якоря, получит на этой
выдаче другой идентификатор».

**Следствие СИЛЬНЕЕ прежнего, а не слабее.** Расхождение не принадлежит «старым установкам»:
свод сеет строку прежним написанием, а миграция переименования применяется **следом и везде**,
включая чистую установку. Значит несовпадение выведенного идентификатора с живым
**универсально и постоянно**, и решение Р4 (естественный ключ вместо совпадения идентификатора)
требуется не от случая, а by construction.

Выражение, помимо применителя, применяет лишь миграция `20260909202745` и лишь к выдачам
`system_viewer` служебным записям
(`sed -n '299,302p' internal/migrations/20260909202745_module_identities_leave_the_baseline.sql`),
и там оно подставляет **форму субъекта FGA** — довод, на который опирается Р1а.

**Что произойдёт на живой установке — не «вторая строка», а ОТКАЗ.** Вставка с другим
идентификатором наталкивается на частичный уникальный индекс по естественному ключу, которого
`ON CONFLICT (id)` не покрывает:

```sh
git grep -n 'access_bindings_active_relation_grant_uniq' -- internal/migrations
#   0001_initial.sql:4450  CREATE UNIQUE INDEX … (subject_type, subject_id, granted_relation,
#                          resource_type, resource_id) WHERE granted_relation <> '' AND revoked_at IS NULL
```

Индекс жив: снимающей его миграции в дереве нет ни одной (та же команда даёт единственное
объявление). Значит применение отвергается кодом `23505`, транзакция модуля откатывается,
`ApplyAll` прекращает работу и служба **не поднимается**. Предикат задачи «кортежи уже живых
установок не рвутся» выполняется случайно и по худшей причине: до кортежей дело не доходит.

**Замечание в пользу самого предиката.** Для двух живых выдач посева (`system_viewer` у `vpc`
и `compute`) он верен буквально: их идентификаторы **выведены** тем же выражением, конфликт по
`(id)` срабатывает, изменений ноль. Форм выдачи две; живых строк роль-формы в разделах `seed`
сегодня **ноль** —
`for m in compute nlb registry storage vpc; do git -C <копия платформы> show $K:services/$m/manifest.yaml | grep -c 'roleId:'; done`
даёт 0 в каждом из пяти, и это состояние без предмета, а не исключение.

### §2.3 Посылка задачи «сверка судит группы наравне с учётками» — УЖЕ ВЕРНА

См. П8. Предикат 4 задачи в этой половине беспредметен; открытыми остаются шапка и атрибуция
владельца (О4). Атрибуция задачей **не названа** — это находка переписи, а не пересказ задачи.

### §2.4 Посылка задачи «шапка `parity.go` пережила предмет» — ВЕРНА, и расхождение внутри пакета

Шапка утверждает: «у выдачи есть ключ `roleId` и нет ни одного ключа для ОТНОШЕНИЯ, поэтому
выдача отношением необъявима ни при каком написании; группа необъявима по следствию» и
называет живой пробу предпосылки, которой в дереве нет.

```sh
sed -n '14,23p' internal/moduleseedparity/parity.go       # утверждение и имя пробы
git grep -n 'TestBindingFormStillCannotExpressARelationGrant' -- internal/moduleseedparity
#   parity.go:22        — шапка называет её живой
#   parity_test.go:153  — надгробие: «СНЯТА ВМЕСТЕ СО СВОИМ ПРЕДМЕТОМ (#1936)»
```

Ключ `grantedRelation` заведён 2026-09-04 (`git log -1 --format='%h %ad' --date=short -S
grantedRelation -- schema/` → `96e8e485 2026-09-04`) и объявлен схемой
(`schema/module-manifest.schema.json:486`). Тело того же пакета с шапкой **уже не согласно**:
`parity.go:124–137` — надгробие предиката `ExpressibleByForm`, снятого тем же изменением, а
`parity_test.go:145–151` говорит «Выведенных из-под вердикта строк больше НЕТ». Два места об
одном предмете внутри одного пакета, и верно то, которое **исполняется**.

### §2.5 Находка «применитель строит форму субъекта FGA БЕЗ сигила членства» — ПОДТВЕРЖДЕНА опытом

Посылка проверена, и она верна. Форму субъекта объявляет **одно** место (П9); писатель строк
посева его не зовёт и строит форму встроенным выражением.

```sh
git grep -c 'FGASubjectRef' -- internal/repo/kaname/pg/     # → 0 файлов
git grep -n  'fgaUser :='   -- internal/repo/kaname/pg/module_seed_writer.go
#   :283  fgaUser := subjectType + ":" + subjectID
```

**Опыт — одно-фактный, прогнан во временном пакете дерева и снят за собой.** Сравниваются две
формы одного субъекта: канон и встроенное выражение применителя; различается только вид
получателя.

```
group            канон=group:<id>#member            применитель=group:<id>            совпали=false
service_account  канон=service_account:<id>         применитель=service_account:<id>  совпали=true
user             канон=user:<id>                    применитель=user:<id>             совпали=true
```

Расхождение — ровно на группе, то есть на том единственном виде получателя, который вводит Р1.
Служебная запись и человек — **законные близнецы**: на них формы совпадают, и проба, написанная
только на них, зеленела бы при любой реализации.

**Канон голый объект группы субъектом НЕ принимает** (П5):
`proto/kaname/cloud/iam/v1/fga_model.fga:294` — `define fga_writer: [service_account,
group#member]`. Правильную форму показывают живые строки **этой же выдачи**, заведённые сводом:
`0001_initial.sql:3162` (ведомость эмитированного), запись **24** журнала прав и прямой факт
отношения — все три несут `group:<id>#member`.

**Радиус — три места из ОДНОГО выражения**, и это надо сказать числом: `fgaUser` участвует в
производной идентификатора выдачи, в ведомости эмитированного и в полезной нагрузке журнала
прав (`git grep -n 'fgaUser' -- internal/repo/kaname/pg/module_seed_writer.go` → **4** строки:
объявление и три потребителя). Значит неверная форма расходится по трём предметам сразу, а не
по одному.

**Почему это не ловится сегодня ничем.** Получателей-групп в разделах `seed` манифестов
платформы **ноль**, и предикат обязан идти ПОМОДУЛЬНО:

```sh
K=45d1719fe8dd76728847f45e901bc5ded7ea993d
for m in compute nlb registry storage vpc; do
  printf -- '-- %s\n' "$m"
  git -C <копия платформы> show $K:services/$m/manifest.yaml \
    | awk '/accessBindings:/{f=1} f&&/type:/'
done
#   -- compute:  - {type: serviceAccount, name: kacho-compute}
#   -- nlb:      (пусто)
#   -- registry: (пусто)
#   -- storage:  (пусто)
#   -- vpc:      - {type: serviceAccount, name: kacho-vpc}
```

Итого **две** строки, обе `{type: serviceAccount, …}`. Ветвь получателя-группы **не исполняется
ни разу**, поэтому её дефект не красный и не зелёный — он невидим.

> [!note] Прежняя запись этого предиката подавала пять манифестов ОДНИМ ПОТОКОМ и давала ТРИ
> строки, третья — не о получателе
> Флаг `f` в `awk` не сбрасывается на границе файлов, а границы в склеенном потоке нет: после
> `compute` он остаётся взведённым и печатает `- {name: parent, type: registry_registry}` из
> `registry` — строку каталога ресурсов, к получателям выдачи отношения не имеющую. Существо
> утверждения от этого не менялось (получателей-групп ноль), неверным было объявленное число.
> Помодульный прогон границу файла имеет by construction.

**Что из этого следует для документа.** Ни одно «Тогда» прежней редакции не утверждало о
**форме** записываемого субъекта, поэтому MRW-01 и MRW-03 были зелены при неверной форме: оба
судят наличие строки выдачи и её идентификатор, а не то, что уезжает в журнал прав. Решение —
Р1а, сценарии — MRW-20 и MRW-21, ведомость — §8, держатель — §9.

### §2.6 Находка «Р2 объявляет ИСХОД, а не МЕХАНИЗМ» — ПОДТВЕРЖДЕНА замером

Прежняя редакция говорила «манифест встроен в образ» и механизма не называла. Оба механизма
требуют того, чего документ не объявлял, и третий исход не рассматривался вовсе.

**Встраивание директивой требует ВТОРОЙ копии внутри каталога пакета.** Пакета в корне дерева
нет (`ls *.go` в корне — «No such file or directory»), а директива встраивания родительского
каталога не принимает. Опыт (временный пакет, снят за собой):

```
//go:embed ../../manifest.yaml  →  invalid pattern syntax
```

**Файла в образе тоже нет.** Вторая ступень сборки наполняется двумя двоичными, сертификатами и
файлом провенанса; `grep -n 'manifest' Dockerfile` не даёт **ни одной** строки.

**Прецедент встраивания в этом же дереве показывает недостающую половину, и состоит он из
ПАРЫ артефактов РАЗНОГО РОДА.** Канон модели прав лежит **двумя** копиями —
`proto/kaname/cloud/iam/v1/fga_model.fga` и `internal/authzmodel/fga_model.fga`, — их отпечатки
совпадают (`md5sum proto/kaname/cloud/iam/v1/fga_model.fga internal/authzmodel/fga_model.fga` →
одно значение на обе), вторая встроена директивой (`internal/authzmodel/authzmodel.go:67`).
Держат эту пару **два разных артефакта**, и путать их нельзя:

| род | артефакт | что делает |
|---|---|---|
| **порождает** копию | `make fga-model-embed` (`Makefile:718`, переменные `:713–714`) | копирует канон в `internal/authzmodel/fga_model.fga` и тут же сверяет `cmp` |
| **держит** равенство | `internal/authzmodel` `TestEmbeddedModelIsByteIdenticalToCanonical` (`identity_test.go:24`) | сверяет ДВЕ копии побайтово и роняет прогон на расхождении |

То же говорит сам Makefile: «Равенство держит гейт `internal/authzmodel`
TestEmbeddedModelIsByteIdenticalToCanonical» (`sed -n '710,713p' Makefile`); и третье место
дерева — `internal/authzmap/fga_model_drift_test.go:19–23`.

> [!warning] Прежняя редакция называла держателем `make model-canon-check` — у этой цели ДРУГОЙ
> ПРЕДМЕТ, и подмена ломала бы признак готовности S1
> Шапка цели говорит о себе сама (`sed -n '349,352p' Makefile`): «блоки модели доступов
> сверяются с манифестами модулей ПОБАЙТОВО (задача #1089). Цель обходит закрытый набор
> модулей, **порождает блоки типов из манифеста каждого** и сравнивает их с каноном …,
> прочитанным ИЗ ДЕРЕВА». То есть она сверяет ПОРОЖДЁННОЕ из манифестов с каноном, а второго
> операнда пары — встроенной копии — не читает вовсе: `grep -n 'authzmodel'
> tools/model-canon-check.sh` → пусто.
>
> **Цена подмены измерима, а не теоретична.** Признак готовности S1 требует: «правка
> `manifest.yaml` без пересборки копии роняет цель». У цели, построенной «по образцу
> `model-canon-check`», такого свойства нет **by construction** — она порождает одну сторону из
> другой и о второй копии не знает. Держатель выглядел бы поставленным, две копии расходились
> бы молча, и расхождение проявилось бы первым стартом.
>
> Прогон, которым это проверено: `go test -count=1 -short -run
> TestEmbeddedModelIsByteIdenticalToCanonical ./internal/authzmodel/` → `ok`.

Ровно этой пары — порождающей цели И держащей пробы — у прежней редакции не было ни в §6, ни
в §9.

**Третий исход прежде не рассматривался, и отвергнуть его доводом ban #20 нельзя:** свой чарт
службы (`deploy/templates/configmap.yaml`) вправе смонтировать **свой** манифест в каталог
доставки, и копии в чужом репозитории это не требует. Его цена — другая, и она названа в Р2.

### §2.7 Находка «судьба манифеста решена для ОДНОГО потребителя из пяти» — ПОДТВЕРЖДЕНА замером

На пути старта перечень манифестов читают **пять** потребителей, а прежняя редакция решала
судьбу манифеста службы для одного — применителя посева.

```sh
grep -c 'deliveredManifests' cmd/kaname/serve.go     # → 6: одно порождение и ПЯТЬ потребителей
grep -n  'deliveredManifests' cmd/kaname/serve.go
#   :291  порождение — чтение доставки
#   :309  опора паритета каталога
#   :335  композиция модели прав
#   :359  применитель каталога
#   :441  применитель ролей модуля
#   :457  применитель посева
```

Манифест службы при этом **не пуст**: `python3 -c "import yaml,sys; d=yaml.safe_load(open('manifest.yaml'));
print(d['module'], len(d['resources']), len(d['roles']), 'seed' in d)"` → `iam 7 19 False` — семь
ресурсов и девятнадцать ролей, и эти разделы четверо остальных потребителей применяют.

**Исход по каждому — в Р3.** Три из пяти измерены опытом (временный пакет, снят за собой):
композиция модели с манифестом службы в перечне даёт `manifests=1 resources=7 canon=32
composed=[] reaffirmed=[account iam_access_binding iam_group iam_role iam_service_account
iam_user project]` — ни одного **нового** типа; опора доставки даёт `added:[]` — ни одной строки
сверх образа; применитель каталога стал бы **вторым** писателем семи строк, которые сеет
миграция (П11).

**У раздела `roles:` исход уже решён соседней приёмкой, и его надо ЦИТИРОВАТЬ, а не решать
заново.** `docs/engineering/acceptance/roles-come-as-data-not-migrations.md` (APPROVED)
классифицирует роли по владельцу имени и говорит про `iam` прямо: «такой же модуль, а не
исключение… переезжают к применителю на общих основаниях, когда появится манифест `iam`». Тот же
исход держит **живой** гейт (П10): его перепись называет манифест `iam` найденным, а девятнадцать
вставок роли в применённых миграциях — историческим остатком, чей «переезд к применителю есть
**отдельный предмет**». Значит эта работа раздел `roles:` не трогает, и так сказано в Р3.

### §2.8 Находка «у оси „живая строка есть / строки нет“ названа одна сторона» — ПОДТВЕРЖДЕНА замером

Все четыре сценария формы **отношения** прежней редакции (MRW-01, 02, 03, 05) описывают мир, где
строка выдачи в базе уже есть, и §5 говорит это дословно: строку заводит `0001_initial.sql:3196`
на **каждой** установке, включая чистую. Снимающей её миграции в дереве нет ни одной:
`git grep -ln 'fga_writer' -- 'internal/migrations/*.sql'` → **один** файл, `0001_initial.sql`.

Следствие: **пишущая ветвь применителя, ради которой Р1 и заводится, не утверждалась ни одним
«Тогда»**. Для формы **роли** документ требование выполнял (MRW-14 называет обе стороны), для
формы отношения — нет. Тот же пробел делал невидимой и находку §2.5: форма записываемого
субъекта не проверялась, потому что **записи** не требовал ни один сценарий.

Мир «строки нет» конструируем тем же приёмом, каким документ уже пользуется в MRW-12: строка
удаляется из базы до применения. Сценарий — MRW-19, дом и дельта — §5.

### §2.9 Что в РЕДАКЦИИ 1 было МОИМ промахом — названо прямо

Раздел о **редакции 1** (`d66107b2`) и о находках, снятых редакцией 2; промахи редакции 2,
названные кругом 1, разобраны отдельно — §11; промахи **редакции 3**, названные оппонентом уже
после круга 1, — §2.10…§2.13 и §12. Все четыре находки подтверждены замером, ни одна
не опровергнута, и это надо сказать первым.
Разночтением не была ни одна: три — пропуск одного рода, четвёртая — неверная причина при верном
следствии.

**Три пропуска одного рода: документ называл ИСХОД там, где обязан был назвать МЕХАНИЗМ и его
радиус.** Р2 объявляла встроенность, не называя, чем она делается и чем держится (§2.6); Р2/Р3
решали судьбу манифеста для одного потребителя, не измерив, что их пять (§2.7); §4 не требовала
**записи** ни одним «Тогда», отчего форма записываемого не проверялась ничем (§2.8).

**Четвёртая — неверная ПРИЧИНА при верном следствии, и она же экземпляр того класса, который
сама и обнажает.** §2.2 утверждала, что идентификатор живой выдачи отчеканен рукой; замер
показывает, что он **выведен** тем же выражением при прежнем написании якоря. Ошибиться удалось
потому, что опыт строил форму субъекта **руками** — ровно как это делает применитель, — и потому
не мог попасть в живое значение ни при каком якоре. Следствие §2.2 (конфликт по идентификатору
не срабатывает, применение отвергается) от этого не изменилось и стало сильнее: расхождение
универсально, а не свойственно старым установкам.

**Урок, который дороже трёх пропусков:** форма, построенная руками вместо вызова объявленного
источника, ошибается **дважды** — в продукте и в том, кто продукт измеряет. Это и есть довод в
пользу Р1а, полученный на себе.

Отдельно — число в §8. Прежняя редакция писала «живых производителей 7 из 18» и **единицы счёта
не называла**, поэтому проверить его читатель не мог. В настоящей редакции число сопровождается
перечнем строк, к которым относится.

### §2.10 Находка «пишущая ветвь применителя для ГРУППЫ не утверждается ни одним „Тогда“» — ПОДТВЕРЖДЕНА замером

§2.8 нашла этот класс для **выдачи** и закрыла его сценариями MRW-19…MRW-21. Для **группы**
половина осталась открытой, и признак готовности S1 её не требовал.

**Строка группы жива на КАЖДОЙ установке, включая чистую.** Её заводит `0001_initial.sql:3489`
(`grp258e6bbe9bbe45568`), а снимающей её миграции в дереве нет ни одной:

```sh
grep -rn 'DELETE FROM kaname.groups' internal/migrations/*.sql      # → пусто
sed -n '3489p' internal/migrations/0001_initial.sql                 # → INSERT … 'module-relation-writers'
```

**Значит «Тогда» MRW-01, MRW-04 и MRW-05 прежней редакции выполнялись СВОДОМ** — они утверждают
**состояние** («в базе есть ровно одна группа … её назначение равно объявленному»), а состояние
это верно и тогда, когда применитель раздела `seed.groups` не читает вовсе.

**Ветвь, чеканящая новый идентификатор, не исполнялась ни в одном сценарии.** `UpsertGroup`
(`internal/repo/kaname/pg/module_seed_writer.go:182–200`) кладёт строку через
`ids.NewID(domain.PrefixGroup)` с конфликтом `ON CONFLICT ON CONSTRAINT groups_account_name_unique
DO UPDATE … WHERE kaname.groups.description IS DISTINCT FROM EXCLUDED.description`. Р1 требует
брать назначение у свода **дословно**, поэтому и обновление молчит: `RowsAffected()==0`, и
MRW-02 честно предсказывает «групп **0**/1». Пишущей стороны у оси не было ни одной.

**Предикат находки — по самому документу:**

```sh
grep -n 'записано' docs/engineering/acceptance/service-manifest-seeds-its-own-groups.md
#   редакция 3: «записано 1» встречается ТРИЖДЫ, и все три — о выдаче (MRW-19 дважды и
#   признак готовности S1). О группе — ни разу
```

**Закрыто** сценарием **MRW-23** (§4), домом в §5, строкой в §8 и признаком готовности S1,
который теперь называет **обе** половины. Дом строится тем же приёмом, что у MRW-12, и порядок
в нём диктует схема, а не второй факт: строка группы, числящаяся субъектом выдачи, прямому
удалению не поддаётся (триггер `groups_subject_ref_before_delete_trg`), а её членства уходят
каскадом (`group_members_group_fk … ON DELETE CASCADE`, `0001_initial.sql:5458`).

### §2.11 Находка «перечень шапок объявлен ЗАКРЫТЫМ и неполон» — ПОДТВЕРЖДЕНА, и радиус перемерен: координат ШЕСТЬ

Круг 1 закрыл Б3 тем, что перечень §6 п. 8 стал **закрытым** и назвал четыре координаты. Закрытым
он стал **неполным**, и это хуже открытого: находка, которой в нём нет, следующим кругом не
ищется by construction — ровно тот класс, который §9 сам и называет («перечень, неполный на
предмет собственного решения, есть гарантия того, что утверждение переживёт свой предмет»).

**Пятая координата названа находкой, шестую дал перемер класса.** Чинится класс, а не экземпляр
(`testing.md` §«Гейт на класс», п. 1), поэтому перечень пересобран обходом шапок обоих пакетов,
которых касается S1, а не дополнен одной строкой.

| # | координата | что в ней ложно после S1 | предикат |
|---|---|---|---|
| 5 | `internal/repo/kaname/pg/module_seed_writer.go:256–270` (godoc `grant`) | «Установка, пережившая переименование якоря, получит на этой выдаче другой идентификатор — выдача системная, защищена от удаления и по идентификатору снаружи не адресуется, **поэтому цена названа и принята**». Цена названа НЕ ТА: §2.2 измеряет, что другой идентификатор не «принимается», а отвергается частичным уникальным индексом `access_bindings_active_relation_grant_uniq` кодом `23505` — транзакция откатывается и служба не поднимается. После Р4 ложным становится и первое: применитель находит живую строку естественным ключом и второго идентификатора не выводит вовсе | `git grep -n 'цена названа и принята' -- internal/repo` → **1** строка; после S1 → пусто |
| 6 | `internal/apps/kaname/moduleseed/apply.go:14–19` (шапка пакета) | «Теперь строки заводит применитель, а **условием служит ДОСТАВКА манифеста**: каталог доставки кладёт зонтичный чарт платформы и не кладёт чарт самостоятельной службы». После Р3 условием доставки связаны только **доставленные** манифесты: свой применяется отдельным доводом и **без** доставки — это ровно то, что утверждает MRW-05 («доставленных манифестов 0», группа и выдача заведены) | `git grep -n 'условием служит ДОСТАВКА' -- internal` → **1** строка; после S1 → пусто либо строка, различающая два разряда |

**Что перемер НЕ нашёл — сказано, чтобы следующий не искал заново.** Шапка файла писателя
(`module_seed_writer.go:18–29`, «Идентификаторы ВЫВОДЯТСЯ выражением базы») остаётся **верной**:
она о производной служебной записи (`'sva' || …`) и о том, что у группы такой производной нет, —
Р4 и Р1а ни того, ни другого не отменяют. Godoc `subjectRef` (`:339`) и `ownerOfBindingSubject`
(`parity_test.go:528`) нейтральны: они называют, ЧТО делают, а не по какому правилу. Godoc
`readLiveBindings` (`parity_test.go:482–484`) говорит «относит их к модулю-владельцу по
СУБЪЕКТУ» и правила имени не называет — Р5 меняет правило, а не субъекта, и шапка остаётся
верной.

**Шапки применённых миграций в перечень НЕ входят, и это не умолчание.** Две из них говорят о
том же предмете: `20260909202745` — «объявить её посевом **СЕГОДНЯ** нельзя — валидатор
связности требует, чтобы заведённая манифестом группа была им же и одарена … а выдача этих
групп принадлежит службе, не модулю» (`sed -n '66,69p'`), `20260914091500` — «производителя,
который завёл бы её заново, **нет**» (`sed -n '19,24p'`). После S1 обе перестают быть верными
для `module-relation-writers`, но применённую миграцию не правят (ban #5); первая датирована
словом «сегодня» и истекает сама, вторая говорит о `module-quota-readers`, которую S2б
снимает вместе с предметом оговорки. Перечень §6 п. 8 — про шапки, **приводимые к факту**;
эти к нему не приводятся ни при каком исходе.

### §2.12 Находка «два APPROVED-соседа не названы ни разу» — ПОДТВЕРЖДЕНА замером

```sh
D=docs/engineering/acceptance/service-manifest-seeds-its-own-groups.md
grep -c 'module-manifest-relation-grant'  $D      # редакция 3 → 0
grep -c 'module-manifest-seed-contract'   $D      # редакция 3 → 0
grep -c 'roles-come-as-data-not-migrations' $D    # → 4
```

Оба соседа — APPROVED, оба **резервируют предмет**, которого касаются решения этого документа,
и ни один не был назван. Исход по каждому — §3 (Р5 и Р3) и §7; здесь — замер.

**Сосед 1 — `module-manifest-relation-grant.md` (APPROVED, круг 1, 2026-09-03).** Его §5 говорит
о живых строках без модуля-владельца: «у них нет модуля, чей манифест мог бы их объявить …
Их предмет — принадлежность платформе, а не модулю, и он открыт отдельно», а §3.7 — «обе живые
[группы] — платформенные, без модуля-владельца». Его перепись считает их разрядом «без
модуля-владельца»: группы — объявлено 0 · живых 2 · без владельца 2; выдачи — живых 8 · без
владельца 6 — числа **его** замера 2026-09-03 на дереве до выноса службы, здесь не перемерены:
довод о разряде от них не зависит, зависит только счёт «сколько уйдёт», и он назван по
именам строк, а не по разности чисел.

**Посылка соседа ИСТЕКАЕТ вместе с Р1, и это истечение, а не отмена.** Сосед говорит «нет модуля,
чей манифест мог бы их объявить» — и это было верно ровно до появления манифеста службы с
разделом `seed`. Р1 заводит именно его, поэтому у **двух** строк из разряда владелец появляется:
группа `module-relation-writers` и её выдача `fga_writer`. Остальные остаются там же, и их
предмет — по-прежнему предмет соседа, а не этой работы.

**Владельца у зарезервированного предмета сегодня НЕТ, и это перемерено по обоим трекерам**
(осмотрено 48 открытых задач в `kaname` и 235 в `kacho`):

```sh
gh issue list -R PRO-Robotech/kaname --state open --limit 300 --json number,title,body \
  -q '.[]|select((.title+" "+.body)|test("без модуля-владельца|принадлежность платформе"))|.number'
gh issue list -R PRO-Robotech/kacho  --state open --limit 600 --json number,title,body \
  -q '.[]|select((.title+" "+.body)|test("без модуля-владельца|принадлежность платформе"))|.number'
#   обе → пусто
```

Эта работа предмета соседа не заказывает и не закрывает: она **сужает** его на две строки и
говорит это числом (§7).

**Сосед 2 — `module-manifest-seed-contract.md` (APPROVED).** Его §4 выносит за скобки «порядок
установки модулей, выводимый из `joins`» с доводом «валидатор порядка не может покраснеть ни при
каком входе — это гейт на нулевой популяции» и предикатом заведения: «манифестов в дереве стало
**≥2**, и хотя бы один объявляет группу, в которую вступает другой». Преемника называет
`module-manifest-resources-roles-deprecated.md:830` — **`#1088`**, и он **`CLOSED`**
(`gh issue view 1088 -R PRO-Robotech/kacho --json state` → `CLOSED`).

**Предикат заведения наступает вместе с Р1 — и Р3 отвечает на него, а не откладывает.** Замер
популяции сегодня:

```sh
git ls-files | grep -cE '(^|/)manifest\.yaml$'                    # служба → 1 (в корне, не в `*/`)
K=45d1719fe8dd76728847f45e901bc5ded7ea993d
git -C <копия платформы> ls-tree -r $K --name-only | grep -cE '(^|/)manifest\.yaml$'   # → 5
for m in compute nlb registry storage vpc; do
  git -C <копия платформы> show $K:services/$m/manifest.yaml \
    | python3 -c "import sys,yaml;s=(yaml.safe_load(sys.stdin).get('seed') or {});print(len(s.get('groups') or []), len(s.get('joins') or []))"
done
#   → «0 2» во всех пяти: ни один доставленный манифест не объявляет НИ ОДНОЙ группы
```

Единица счёта названа, потому что предикат соседа (`git ls-files | grep -cE '/manifest\.yaml$'`)
в дереве службы даёт **0**: манифест лежит в корне и слэша в пути не имеет. Считать надо то, что
видит применитель, а это **1 свой + 5 доставленных = 6**.

**Следствие, несущее для Р3.** Группу объявляет **ровно один** манифест — свой; все вступления
идут в неё. Значит порядок, выводимый из `joins`, для существующей популяции сводится к одному
правилу «своё раньше доставленного», и его Р3 **решает**, а не откладывает. Порядок **между
доставленными** остаётся нерешённым, и его популяция — **ноль** объявленных групп на пять
манифестов: это состояние без предмета, а не отсрочка. Предикат возврата предмета назван в §7.

### §2.13 Находка «„Дано“ MRW-04 не строится домом §5» — ПОДТВЕРЖДЕНА замером

«Дано» MRW-04 просит «базу, в которой этой группы ещё нет». Дом §5 редакции 3 — «база + манифест
Р1 + каталог доставки с одним манифестом модуля», дельта «доставка есть». Снятия строки в доме
нет, а на базе, которую дом строит, группа **жива из свода** (§2.10) — значит `groupID` резолвит
её при любом порядке обхода, и «Тогда» MRW-04 («вступление `compute` записано, отказа „группа не
резолвится“ нет») выполняется **независимо от Р3**. Единственный сценарий решения Р3 о порядке
оставался без различающего мира.

**Исход — правка ДОМА, а не «Дано»** (первая из двух ветвей, названных находкой): дом MRW-04 и
его близнеца MRW-05 получает снятие строки группы вместе с её выдачей, дельта между ними
остаётся **одна** — «доставка есть / доставки нет». Побочно это чинит и MRW-05: на снятой строке
его «Тогда» («группа и её выдача заведены») перестаёт быть утверждением о своде и становится
утверждением о применителе.

---

## §3 Решения

### Р1. Группу и её выдачу объявляет манифест службы

Манифест службы (`manifest.yaml`, `module: iam`) получает раздел `seed` с двумя подразделами:

- `groups` — одна запись: `module-relation-writers` в аккаунте системных служебных записей, с
  назначением, взятым **дословно** у живой строки свода;
- `accessBindings` — одна запись: получатель `{type: group, name: module-relation-writers}`,
  `grantedRelation: fga_writer`, `scopeType: iam.cluster`, `scopeId: cluster_root`,
  `target: allInScope`.

Подразделов `serviceAccounts` и `joins` у него нет: своей служебной записи у службы доступа
не бывает, и вступать ей некуда. Это сказано **словом** в шапке манифеста, а не оставлено
умолчанием.

**Почему выдача обязана быть.** Её требует валидатор связности (П6): заведённая манифестом
группа, которую он же не одаривает, — объявление без следствия. Требование здесь не помеха, а
ровно то, что делает решение владельца исполнимым: аудиторию и выдачу объявляет один документ.

### Р1а. Форму субъекта FGA применитель БЕРЁТ У КАНОНА, а не строит третьей копией

Получателем выдачи Р1 впервые становится **группа**, и с этим в контракт входит форма, которой
прежде не было ни у одной живой выдачи посева: субъектом кортежа выступает не объект группы, а
её **членство**. Канон принимает только эту форму (П5), а живые строки этой же выдачи, заведённые
сводом, её и несут (§2.5).

**Решение.** Писатель строк посева строит субъект FGA **вызовом** объявленного источника формы
(П9) — того же, которым её строят проекция выдачи и реконсайлер, — и своего выражения не держит.
Наблюдаемо: у получателя-**группы** ведомость эмитированного и журнал прав получают субъекта с
сигилом членства; у получателя-**служебной записи** форма остаётся прежней побайтово.

**Почему вызов, а не «дописать сигил на месте».** Третья копия формы разошлась бы с первыми
двумя молча и ровно там, где расхождение дороже всего: субъект выдачи и субъект отзыва стали бы
разными, то есть отзыв не снял бы того, что выдача завела. Это сказано в godoc самого источника
требованием побайтовой симметрии — и здесь оно не пересказывается, а исполняется.

**Радиус решения — три предмета из одного выражения, а не один** (§2.5): производная
идентификатора выдачи, ведомость эмитированного, полезная нагрузка журнала прав. Значит проба
обязана утверждать **все три**, а не только последнюю: идентификатор, выведенный из неверной
формы, не совпал бы с идентификатором, выведенным из верной, и Р4 разрешал бы его естественным
ключом молча.

**Производная идентификатора берёт ИМЕННО эту форму, и это не выбор документа, а живой
прецедент.** Выражение, которое применитель повторяет, объявлено применённой миграцией, и её
довод — форма субъекта FGA: `sed -n '299,302p'
internal/migrations/20260909202745_module_identities_leave_the_baseline.sql` — там подставлено
`'system-grant:service_account:' || sa_id`. На служебной записи форма субъекта FGA и пара
«тип:идентификатор» совпадают, поэтому прецедент не различает их **by construction**; различает
их ровно получатель-группа, и различие решается в пользу формы FGA — той, которую принимает
канон.

**Чего решение НЕ делает.** Оно не меняет формы для служебной записи и человека — там канон и
выражение применителя совпадают уже сегодня (§2.5), и это законный близнец всякой пробы на эту
ось, а не «заодно исправленное».

### Р2. Манифест службы приезжает применителю ВСТРОЕННЫМ В ОБРАЗ

Исходов **три**, и каждый отвергнут или принят по названной цене, а не по вкусу. Прежняя
редакция называла исход и не называла механизма — разбор этого промаха в §2.6.

| исход | механизм | цена |
|---|---|---|
| **встроен в образ ВТОРОЙ КОПИЕЙ в каталоге пакета-носителя** (принят) | копия `manifest.yaml` в каталоге пакета-носителя плюс директива встраивания; копию **порождает цель сборки**, а побайтовое равенство её с манифестом дерева **держит проба** — два артефакта разного рода, по образцу пары `make fga-model-embed` + `TestEmbeddedModelIsByteIdenticalToCanonical` (§2.6) | вторая копия в дереве — и она **не** копия чужого репозитория (ban #20 про чужого владельца, а владелец здесь свой). Цена копии — держатель: без него две копии разойдутся молча |
| файлом в образе (отвергнут) | копирование `manifest.yaml` во вторую ступень сборки плюс ручка пути плюс отказ старта при её незаданности | цена выше при том же результате: заводит **ручку посадки** там, где величина не имеет законного разброса, и страж, который обязан отличать «путь не задан» от «файл не читается». Копия в дереве при этом всё равно не нужна — но и согласие держать нечем: сравнивать в образе не с чем |
| свой чарт монтирует свой манифест в каталог доставки (отвергнут) | `deploy/templates/configmap.yaml` кладёт манифест службы в тот же каталог, откуда читается доставка | копии в чужом репозитории не требует, и **доводом ban #20 не отвергается** — отвергается двумя другими. Первая: манифест попадает в **перечень доставки**, то есть ко всем пяти потребителям сразу (§2.7, Р3), а решение Р3 требует обратного. Вторая: условие «группа заведена» становится условием **посадки** — установка, чей оператор каталог не объявил, поднимется без группы, и это тот же пробел, что у доставки платформой |
| доставкой платформы (отвергнут) | ConfigMap зонтичного чарта платформы | требует копии манифеста службы в дереве платформы — **запрещено** решением владельца 2026-09-13 (ban #20: файл, чей владелец — другой репозиторий, в этом дереве не лежит). Сверх того делает существование группы условием установки платформы, тогда как право принадлежит поверхности самой службы, и ломает порядок (О2) |

**Почему директива, а не путь в образе.** Директива даёт величину, у которой нет незаданного
состояния: манифест либо встроен и собран вместе с двоичным, либо сборка не состоялась. Путь в
образе — это ручка, а всякая ручка требует стража старта и различения «не задана» от «не
читается»; за то же самое платится дважды.

**Цена принятого исхода названа, а не умолчана: копий становится ДВЕ.** Ровно та же цена уже
принята в этом дереве у канона модели прав — две побайтово равные копии, вторая встроена
директивой (§2.6). Поэтому и здесь **держателей обязательно ДВА, и они разного рода**:

- **порождает** копию — цель сборки, по образцу `make fga-model-embed` (`Makefile:718`): она
  копирует `manifest.yaml` в каталог пакета-носителя и тут же сверяет `cmp`. Цель **не
  является** держателем равенства: она его восстанавливает;
- **держит** равенство — **проба**, сверяющая обе копии побайтово и роняющая прогон на
  расхождении, по образцу `TestEmbeddedModelIsByteIdenticalToCanonical`
  (`internal/authzmodel/identity_test.go:24`). Признак готовности S1 («правка `manifest.yaml`
  без пересборки копии роняет») исполняет **она**, а не цель.

`make model-canon-check` держателем этой пары **не является и в образец не годится** — у него
другой предмет: он порождает блоки типов из манифестов модулей и сверяет их с каноном, а
встроенную копию не читает вовсе (разбор и предикаты — §2.6). Строки держателей — §9, пункт
S1 — §6.

**Пакет-носитель назван, потому что имя определяет ЧИТАТЕЛЯ, а не раскладку.** Копия лежит в
каталоге **своего** пакета `internal/` — отдельного носителя манифеста службы, а не внутри
применителя посева (`internal/apps/kaname/moduleseed`). Два довода, и оба из устройства
дерева:

- директива встраивания привязывает копию к каталогу пакета и родительского каталога не
  принимает (§2.6, опыт), поэтому «копия рядом с читателем» означает **каталог конкретного
  пакета**, и он обязан быть назван;
- применитель посева — слой use-case, и файлов он не читает: манифест ему приходит **готовым
  доводом** (Р3). Носителем поэтому служит пакет, который импортирует **композиционный
  корень** (`cmd/kaname/serve.go`), — ровно так устроен и прецедент: `internal/authzmodel`
  несёт копию и отдаёт её потребителям, сам ничьим потребителем не будучи.


**Что из этого следует для самостоятельной установки.** Служба без платформы поднимается с
заведённой группой и выданным ей правом; членов у группы нет, и это верное состояние, а не
пробел: модулей нет — писать кортежи некому. Пустая группа не даёт никому ничего (тот же
довод, которым две применённые миграции оставили пустыми обе группы).

### Р3. Свой манифест применяется ПЕРВЫМ, и порядок объявлен, а не получен случайно

Применитель получает манифест службы **отдельным доводом**, а не подмешанным в перечень
доставки, и применяет его до перебора доставленных. Два следствия, и оба обязательны:

- вступление модуля всегда встречает группу заведённой — независимо от имени модуля и от
  порядка обхода каталога доставки;
- перепись применения различает «свой» и «доставленные» **числом**: «манифестов доставлено N ·
  свой применён да/нет». Одно число на оба разряда скрыло бы ровно тот случай, ради которого
  разделение и делается.

**«Отдельным доводом» — не деталь провязки, а ГРАНИЦА РАДИУСА.** Перечень доставки читают
**пять** потребителей (§2.7), и подмешанный в него манифест службы достался бы всем пятерым
разом. Отдельный довод делает получателем **одного** — и это решение, за которое отвечает
таблица ниже.

#### Исход по КАЖДОМУ из пяти потребителей перечня

Порядок строк — порядок вызовов на пути старта (`grep -n 'deliveredManifests'
cmd/kaname/serve.go`).

| потребитель | получает ли манифест службы в S1 | почему именно так |
|---|---|---|
| опора паритета каталога (`:309`) | **нет** | безвреден и бесполезен: опыт даёт `added:[]` — ни одной строки сверх образа (§2.7). Ребро, ничего не меняющее, — это ребро, которое некому проверить: его зелёное неотличимо от его отсутствия |
| композиция модели прав (`:335`) | **нет** | опыт даёт `composed=[] reaffirmed=[7 типов]` — ни одного **нового** типа: свои семь служба и так берёт из встроенного канона образа (§7). Ребро добавило бы второй источник тех же типов, не добавив ни одного |
| применитель каталога (`:359`) | **нет** | завёл бы **второго писателя** семи строк `catalog_resource` модуля `iam`, которые сеет миграция (П11). Два места об одном предмете расходятся молча — ровно тот класс, который называет шапка гейта ролей |
| применитель ролей модуля (`:441`) | **нет** | предмет **адъюдицирован переписью живого гейта** (П10) как исторический остаток применённых миграций, правке не подлежащий (ban #5); классификацию держит APPROVED-сосед `roles-come-as-data-not-migrations.md` §3.4. Открытой задачи у него нет, и это сказано словом ниже — вместе с предикатом, по которому он перестанет быть остатком |
| применитель посева (`:457`) | **ДА** | единственный получатель: предмет работы — раздел `seed`, и он читается только здесь |

**Что это НЕ откладывает, и здесь ПРЕЖНИЙ ДОВОД БЫЛ ЛОЖЕН — опровергнут замером круга 1.**
Прежняя редакция снимала с четвёртой строки ban #11 словами «предмет принадлежит названной
чужой работе, **и она открыта**». Открытой работы нет ни в одном из двух трекеров:

```sh
gh issue list -R PRO-Robotech/kaname --state open --limit 200 --json number,title,body \
  -q '.[]|select((.title+" "+.body)|test("применител(ю|ь|я) ролей|переезд.*рол|19 ролей"))|.number'
gh issue list -R PRO-Robotech/kacho  --state open --limit 500 --json number,title,body \
  -q '.[]|select((.title+" "+.body)|test("применител(ю|ь|я) ролей|19 ролей|roles-come-as-data"))|.number'
#   первая даёт одну задачу — #111 «держатели: 12 гейтов … живут в монорепо», к переезду ролей
#   отношения не имеющую; вторая — одну задачу #1896 о координатах в приёмках. Предмета нет
gh issue view 1091 -R PRO-Robotech/kacho --json state -q .state        # → CLOSED
```

Довод переписан **третьим** из исходов, названных кругом 1, и слова «открыта» в нём больше нет:

- **предмет адъюдицирован**, а не отложен: перепись живого гейта `TestMODRD23` называет
  девятнадцать вставок роли `iam` в **уже применённых** миграциях историческим остатком,
  правке не подлежащим (ban #5 — применённую миграцию не правят). Адъюдикация — исход, а не
  отсрочка: «решим потом» здесь нет ни одной строкой;
- **предикат прекращения остатка назван и он МАШИННЫЙ, а не «когда дойдут руки»**: остаток
  перестаёт быть остатком ровно тогда, когда перепись гейта печатает по нему **ноль**, —

  ```sh
  go test -count=1 -short -run TestMODRD23 -v ./internal/check/ \
    | grep 'исторический остаток'
  #   сегодня: «… вставок роли модуля с манифестом в УЖЕ ПРИМЕНЁННЫХ миграциях 19 —
  #   правке не подлежат (ban #5), их переезд к применителю есть отдельный предмет»
  ```

  Ноль он напечатает не раньше, чем СОЙДУТСЯ ДВЕ вещи: новая миграция снимет эти строки **и**
  у раздела `roles:` появится читатель. Ни того, ни другого эта работа не делает и не обещает;
- **условие соседней APPROVED-приёмки при этом УЖЕ НАСТУПИЛО, и это надо сказать прямо**:
  `roles-come-as-data-not-migrations.md` §3.4 говорит «переезжают к применителю на общих
  основаниях, когда появится манифест `iam`», а манифест в дереве есть (П10). Наступившее
  условие **не превращает** остаток в отсрочку этой работы: приёмка-сосед объявляет **правило
  классификации**, а не срок, и её собственный держатель — тот же гейт, который остаток и
  считает. Работу, которую это условие открывает, заводит её владелец своим изменением; здесь
  она не заказывается и не обещается.

Остальные три строки «нет» не откладывают ничего по другой причине: у них исход — «ребра не
заводим», и ребра, которого нет, откладывать нечего.

**Порядок, который Р3 решает, — и порядок, который зарезервирован соседом.** APPROVED-приёмка
`module-manifest-seed-contract.md` §4 вынесла за скобки «порядок установки модулей, выводимый из
`joins`» как гейт на нулевой популяции и назвала предикат заведения преемника: «манифестов ≥2, и
хотя бы один объявляет группу, в которую вступает другой». Р1 создаёт именно это состояние, и
Р3 на него **отвечает**, а не откладывает: единственный манифест, объявляющий группу, — свой, все
пять доставленных лишь вступают (`0 2` у каждого, §2.12), поэтому «своё раньше доставленного» и
есть весь порядок, выводимый из `joins` сегодня. Порядок **между доставленными** Р3 не решает —
там популяция ноль, и предикат возврата предмета назван в §7. Преемник, за которым сосед
оставлял предмет, — `#1088` — закрыт; довод «работа открыта» здесь не используется (тот же
класс, что Б5 круга 1).

**Признак нарушения этого решения — механический:** манифест службы оказался в перечне,
который читают пятеро. Проверяется тем, что довод применителя посева отличен от перечня
доставки, а не тем, что кто-то помнит про это правило.

### Р4. Идемпотентность выдачи держит ЕСТЕСТВЕННЫЙ КЛЮЧ живой строки

Применитель, встретив живую выдачу **того же субъекта** того же **отношения** на том же
**якоре**, не заводит второй строки, не отказывает и не трогает кортеж — независимо от того,
каким идентификатором эта строка заведена. Наблюдаемо: применение проходит, перепись выдач
показывает «записано 0», журнал прав не получает ни одной строки, идентификатор живой строки
не меняется.

Исходы, рассмотренные и отвергнутые, с их ценой:

| исход | почему отвергнут |
|---|---|
| миграция **переклеивает** живую строку на выведенный идентификатор | невыразимо: внешний ключ `access_binding_emitted_tuples_binding_id_fkey` объявлен **без** `ON UPDATE CASCADE` (`git grep -n 'access_binding_emitted_tuples_binding_id_fkey' -- internal/migrations` → `0001_initial.sql:5354`), поэтому `UPDATE … SET id` отвергается ссылкой ребёнка. У соседнего ключа `access_binding_subjects_scope_fk` каскад обновления **есть** (`:5362`) — то есть половина детей переехала бы, а половина отвергла бы переезд |
| миграция **снимает и заводит заново** | выразимо и дорого: уносит ведомость эмитированного каскадом удаления, эмитирует пару «снятие + запись» в журнал прав на **каждой** установке и заводит окно, в котором право отсутствует. Платится это ради совпадения идентификатора, которого снаружи никто не адресует: выдача системная и защищена от удаления |
| применитель **молча пропускает** выдачу, если хоть какая-то строка у субъекта есть | отвергнут: «выдача уже есть» и «выдача есть, но другая» стали бы неразличимы, а второе — настоящая находка |

Решение действует **для формы отношения**. Для формы роли живых строк посева сегодня ноль
(§2.2), и её естественный ключ — другой индекс (`access_bindings_active_grant_uniq`,
`0001_initial.sql:4443`); требование к ней названо в §4 отдельным сценарием, чтобы тот же
класс не пришлось находить второй раз.

### Р5. Живая группа относится к модулю, ЧЕЙ МАНИФЕСТ ЕЁ ОБЪЯВЛЯЕТ

Правило владельца для групп меняется: имя вида `kacho-<служба>` остаётся правилом для
**служебных записей**, а группа относится к модулю **по объявлению**. Группа, которую не
объявляет ни один прочитанный манифест, владельца не имеет, вердикту не подлежит и считается
переписью отдельно — тем же разрядом «без модуля-владельца», что и сегодня.

**Почему не «по имени с приставкой `module-`».** Приставка не называет владельца: обе
сегодняшние группы её несут, а объявлять их будет один манифест из шести. Правило по имени
пришлось бы менять снова с первой же группой, заведённой другим модулем.

**КЛЮЧ атрибуции — УЗКАЯ пара (аккаунт, имя); ключ сравнения остаётся ТРОЙКОЙ с назначением.**
Это не деталь реализации, а несущее различие, и без него обе стороны вердикта гаснут разом.
Ключ сравнения объявлен сегодня: `keysOfGroup` (`internal/moduleseedparity/parity.go:301`)
склеивает `Account \x00 Name \x00 Description`. Если тем же тройным ключом относить живую
строку к модулю, то группа, чьё живое назначение разошлось с объявленным, ни одному модулю не
достанется — уйдёт в разряд «без модуля-владельца», и **обе** стороны подраздела промолчат:
«ЖИВЁТ и не объявлена» не сработает (строки нет у модуля), «ОБЪЯВЛЕНА и не живёт» — тоже
(живой у модуля нет, но и находка о ней адресована бы никому). Перепись при этом напечатала бы
«с владельцем 0», и это выглядело бы исправной работой, потому что MRW-13 ровно такое состояние
объявляет законным. Поэтому: **относим по паре, сравниваем по тройке** — тогда дрейф назначения
даёт находку, а не молчание.

**Правило по объявлению распространяется и на ВЫДАЧУ, чей субъект — эта группа.** Это второе
следствие Р5, и без него MRW-11 («находок ноль») недостижим **by construction**: сегодня
владелец живой выдачи выводится из ИМЕНИ её субъекта тем же правилом `kacho-<служба>`, а имя
`module-relation-writers` ему не отвечает. Замер (временный опыт в пакете проб, снят за собой):

```
ownerOfSeededName("module-relation-writers")                        -> owner="" ok=false
ownerOfSeededName("kacho-vpc")                                      -> owner="vpc" ok=true
ownerOfBindingSubject("group", "", "module-relation-writers")       -> owner="" ok=false
ownerOfBindingSubject("service_account", "kacho-vpc", "")           -> owner="vpc" ok=true
```

То есть живая выдача `fga_writer` группе сегодня принадлежит **никому** (разряд «без
модуля-владельца»). Объяви её манифест `iam` — и у объявленной выдачи не окажется живой пары:
сверка выдаст **ложную** находку «выдача ОБЪЯВЛЕНА и не живёт» на верном дереве. Это ровно то,
чего опасается О4, но в подразделе **выдач**, а не групп, и прежняя редакция этого не называла.
Решение: выдача, чей субъект — группа, относится к модулю по **объявлению этой группы** —
той же паре (аккаунт, имя). Служебная запись как субъект правила не меняет: там имя
`kacho-<служба>` остаётся и работает.

**Довод godoc против второго правила назван и ОТВЕДЁН, а не обойдён молчанием.** Шапка
`ownerOfSeededName` (`internal/moduleseedparity/parity_test.go:392–397`) говорит: «Правило ОДНО
на служебную запись и на группу … Второе правило для второго предмета разошлось бы с первым
молча». Довод верен для того, о чём он написан, — и здесь не о том. Расходиться молча могут
**два правила об ОДНОМ предмете**: тогда у одной строки два ответа, и какой из них действует,
не решал никто. Здесь предметы **разные**: служебная запись относится к модулю по своему имени
(его назначает сам модуль, `kacho-<служба>`), группа — по тому, **кто её объявил**. У одной
строки по-прежнему **ровно один** ответ, и вопрос «какое правило применить» решается видом
строки, а не совпадением. Разойтись им не на чем by construction — а вот **одно** правило на
два предмета уже расходится с деревом: имя `module-relation-writers` его не удовлетворяет, и
владельцем группы сегодня не является никто.

**Что это делает с классификацией APPROVED-соседа — сказано, а не умолчано.**
`module-manifest-relation-grant.md` §5 и §3.7 относят обе живые группы и шесть живых выдач к
разряду «без модуля-владельца» и объявляют их предмет открытым отдельно — с посылкой «у них
нет модуля, чей манифест мог бы их объявить». Р5 эту классификацию **не отменяет**: она
**истекает** для двух строк вместе с Р1, потому что Р1 заводит ровно тот манифест, которого
посылка не видела. Группа `module-relation-writers` и её выдача `fga_writer` получают владельца
`iam`; остальные строки разряда остаются его предметом — исход и числа в §7, замер в §2.12.

**Что это НЕ ослабляет.** Обе стороны вердикта остаются: «группа ОБЪЯВЛЕНА и не живёт»
срабатывает, если объявленной строки в базе нет (MRW-12); «группа ЖИВЁТ и не объявлена»
срабатывает, если живая строка объявлена **этим** модулем с другим назначением (MRW-22).
Появление у группы владельца **сужает** разряд «без владельца», а не переносит строки в
молчание: обе величины печатаются рядом (`Subsection`, `parity.go:176–179` — «объявлено · живых
· без модуля-владельца · с владельцем»).

### Р6. `module-quota-readers` снимается ДВУМЯ стадиями в разных репозиториях, и порядок несущий

Сначала вступления уходят из манифестов платформы (S2а), потом строки — миграцией службы
(S2б). Обратный порядок ломает установку: применитель, встретив вступление в снятую группу,
отказывает (`groupID`: «группа %s/%s не резолвится»), и служба не поднимается.

Между стадиями живёт состояние «группа есть, вступлений нет», в котором не ломается ничто ни
при какой комбинации версий: применитель не снимает строк, а пустая группа ничего не открывает.

**И здесь надо сказать точно, чем это держится, потому что соблазн назвать соседний гейт
велик.** «Применитель не снимает строк» у посева держится **шапкой пакета**
(`sed -n '28,31p' internal/apps/kaname/moduleseed/apply.go`), и машинного держателя у него
нет: гейт `TestMODRD15ApplierNeverDeletesARoleRow` судит применитель **ролей**
(`internal/apps/kaname/moduleroles`) и о посеве не утверждает ничего — предмет назван первой
же строкой его разбора (`sed -n '4,14p' internal/check/applier_never_deletes.go`). Перенос
того же разбора на посев — выразимая работа, и она **здесь не заказывается**: предмет у неё
свой.

**Цена состояния названа, а не умолчана:** в посадке, где читаются манифесты платформы, сверка
паритета между S2а и S2б даёт находку «вступление ЖИВЁТ и не объявлено» — по одной на каждый
из пяти модулей. В посадке самостоятельного клона её нет: чужие модули там **считаются**, а не
судятся (`sed -n '207,238p' internal/moduleseedparity/parity_test.go`), и это измеренное
свойство, а не надежда.

### Р7. Свод перестаёт быть источником истины, но `0001` не правится

Применённую миграцию не правят (ban #5). Из этого следует по каждой строке §1.3:

| строка свода | что с ней делает эта работа |
|---|---|
| группа `module-relation-writers` | **усыновляется**: применитель кладёт объявленное поверх неё по ограничению уникальности (П4), идентификатор не меняется |
| выдача `fga_writer` | **усыновляется** естественным ключом (Р4); новой миграции для неё не требуется |
| группа `module-quota-readers` и её 5 членств | **снимаются** новой миграцией (S2б) |
| выдача `quota_reader` | уже снята миграцией `20260914091500`; эта работа её не касается |
| 5 членств в `module-relation-writers` | не трогаются: их объявляют манифесты модулей, и они остаются |

---

## §4 Сценарии

Идентификаторы `MRW-01…MRW-23` трассируются в имена проб. Все «Дано» строятся домом,
названным в §5. Сценарии `MRW-19…MRW-21` и `MRW-23` стоят в конце стадии S1, к которой
принадлежат, а `MRW-22` — рядом со своим близнецом `MRW-13`, потому что его предмет — сверка:
номер здесь трассировочный якорь, а не порядок чтения.

### S1 — манифест службы объявляет свою группу

**MRW-01 · чистая установка: группа и выдача заведены объявлением**

- **Дано** установка службы, чья база поднята применением всех миграций дерева, и манифест
  службы объявляет раздел `seed` по Р1
- **И** доставки манифестов платформы нет (`manifests.dir` не объявлен посадкой)
- **Когда** служба стартует
- **Тогда** в базе есть **ровно одна** группа с именем `module-relation-writers` в системном
  аккаунте, и её назначение равно объявленному манифестом
- **И** у неё есть **ровно одна** активная выдача отношения `fga_writer` на объекте
  `cluster:cluster_root`, с признаком системной и защитой от удаления
- **И** служба принимает трафик (стадия применения посева не отказала)

**MRW-02 · повторный старт ничего не меняет**

- **Дано** установка MRW-01 после успешного старта
- **Когда** служба перезапускается без единой правки манифеста
- **Тогда** перепись применения своего манифеста печатает «групп 0/1 · выдач 0/1»
  (записано/объявлено)
- **И** идентификатор строки группы и идентификатор строки выдачи те же, что до перезапуска
- **И** в журнал прав не добавлено ни одной строки

**MRW-03 · идентификатор, выведенный при ПРЕЖНЕМ якоре, усыновляется, а не отвергается**
*(положительный близнец MRW-02; отличается ОДНИМ фактом — идентификатор живой строки выведен при
написании якоря `cluster_kacho_root`, тогда как выражение применителя берёт якорь живой,
`cluster_root`, и даёт другое значение)*

- **Дано** установка, чья база несёт живые строки свода §1.3, включая выдачу `fga_writer` с
  идентификатором `acb9e920fb038c7e4f74`
- **Когда** служба с манифестом Р1 стартует впервые
- **Тогда** применение посева **проходит** — отказа с кодом нарушения уникальности нет
- **И** строк активной выдачи отношения `fga_writer` группе на кластере в базе **одна**, и её
  идентификатор по-прежнему `acb9e920fb038c7e4f74`
- **И** ведомость эмитированного этой выдачи не изменилась, и в журнал прав не добавлено ни
  одной строки — то есть кортеж не перезаписан и не снят

**MRW-04 · своё применяется раньше доставленного**

- **Дано** установка, из базы которой **снята** строка группы `module-relation-writers` —
  вместе с её выдачей, потому что иного порядка схема не допускает (§5), — и чья доставка несёт
  манифест `compute` со вступлением в эту группу
- **Когда** служба стартует
- **Тогда** вступление `compute` записано, отказа «группа не резолвится» нет
- **И** перепись называет **раздельно**: свой манифест применён, доставленных манифестов —
  число

> Снятие строки здесь **несущее**, а не аккуратность: на базе миграций дерева группа жива из
> свода (§2.10), и `groupID` резолвит её при любом порядке обхода — «Тогда» выполнялось бы
> **независимо от Р3**, то есть сценарий не мог бы упасть на неверном порядке.

**MRW-05 · самостоятельная установка без платформы**
*(отрицательный близнец MRW-04 по ОДНОЙ оси — доставки нет вовсе)*

- **Дано** та же база, что у MRW-04 — со снятой строкой группы и её выдачей, — и установка,
  чья посадка не объявляет каталог доставки
- **Когда** служба стартует
- **Тогда** группа и её выдача заведены **применителем**: строк по одной, назначение и якорь
  равны объявленным манифестом
- **И** членов у группы ноль, и это не отказ: перепись печатает «доставленных манифестов 0»

**MRW-06 · группа без выдачи отвергается формой**

- **Дано** манифест службы, объявляющий группу `module-relation-writers` и **не** называющий
  её ни в одной выдаче
- **Когда** манифест разбирается судьёй формы
- **Тогда** разбор даёт находку рода `ErrGroupNeverGranted`, называющую путь `seed.groups[0]` и
  номер строки документа
- **И** служба с таким манифестом не стартует

**MRW-07 · положительный близнец MRW-06**
*(отличается ОДНИМ фактом — выдача названа)*

- **Дано** тот же манифест, дополненный выдачей `fga_writer` этой группе на кластере
- **Когда** манифест разбирается тем же судьёй
- **Тогда** находок ноль, и служба стартует

**MRW-08 · якорь вне кластерного singleton'а отвергается**

- **Дано** манифест службы, чья выдача объявлена с `scopeType: iam.account`
- **Когда** манифест разбирается судьёй формы
- **Тогда** находка рода `ErrBindingAnchor` с путём `seed.accessBindings[0]`
- **И** та же выдача с `scopeType: iam.cluster` находок не даёт *(обе стороны оси названы)*

**MRW-09 · отношение вне канона отвергается**

- **Дано** манифест службы, чья выдача называет отношение, которого канон у типа `cluster` не
  объявляет
- **Когда** манифест разбирается судьёй формы
- **Тогда** находка рода `ErrRelationNotDeclared`
- **И** та же выдача с `fga_writer` находок не даёт *(обе стороны названы)*

**MRW-10 · получатель, которого отношение не принимает, отвергается**

- **Дано** манифест службы, объявляющий получателем **группу** на отношении `system_viewer`
  кластера — канон принимает у него `user` и `service_account`, но не членство группы
- **Когда** манифест разбирается судьёй формы
- **Тогда** находка рода `ErrRelationRecipientKind`, и текст называет **принимаемые** виды
- **И** та же группа на `fga_writer` принимается *(обе стороны названы: отношение, принимающее
  членство, и отношение, его не принимающее)*

**MRW-11 · сверка паритета зелена на объявленной группе**

- **Дано** база, поднятая миграциями и приведённая применителем к манифесту Р1
- **Когда** прогоняется сверка посева
- **Тогда** находок ноль
- **И** перепись по подразделу групп печатает четыре величины, и «с владельцем» **не** ноль:
  живая `module-relation-writers` отнесена к модулю `iam`

**MRW-12 · объявленной группы нет в базе — находка остаётся**
*(отрицательный близнец MRW-11, ОДНО отличие мира: объявленной строки группы в базе нет)*

- **Дано** та же база, из которой строка группы снята — **вместе с её выдачей**, потому что
  иного порядка схема не допускает: снятие строки группы, пока она числится субъектом выдачи,
  отвергается триггером `groups_subject_ref_before_delete_trg` кодом `23503`
  (`0001_initial.sql:5171`, функция `principal_not_referenced_as_subject`, `:1302–1319`).
  Зависимое снятие — **не второй факт мира**, а порядок, который схема диктует для одного
- **Когда** прогоняется та же сверка
- **Тогда** находка «группа ОБЪЯВЛЕНА и не живёт», называющая модуль `iam` и файл манифеста
- **И** находок по подразделу групп ровно **одна** — вторая сторона («ЖИВЁТ и не объявлена») не
  срабатывает: живой строки нет вовсе
- **И** рядом приходит находка подраздела **выдач** — «выдача ОБЪЯВЛЕНА и не живёт»: живой
  выдачи не стало вместе с группой. Её ждут **вместе** с первой; сценарий, требующий ровно
  одной находки на весь прогон, был бы красным на верном поведении

**MRW-13 · живая группа, которую не объявляет никто, владельца не получает**

- **Дано** база, в которой есть группа с именем, не объявленным ни одним прочитанным
  манифестом
- **Когда** прогоняется сверка
- **Тогда** находок по ней нет
- **И** перепись считает её в разряде «без модуля-владельца», и это число печатается

**MRW-22 · живая группа объявлена этим модулем с ДРУГИМ назначением — находка остаётся**
*(отрицательный близнец MRW-11, ОДИН факт отличия — назначение живой строки разошлось с
объявленным; MRW-12 отличается от MRW-11 другим фактом — отсутствием живой строки, поэтому это
не он)*

- **Дано** база MRW-11 — приведённая применителем к манифесту Р1, — в которой назначение живой
  строки группы `module-relation-writers` изменено и с объявленным манифестом не совпадает
- **Когда** прогоняется та же сверка
- **Тогда** находок по подразделу групп **две**, и обе называют модуль `iam` и файл манифеста:
  «группа ЖИВЁТ и не объявлена» — с ЖИВЫМ назначением, «группа ОБЪЯВЛЕНА и не живёт» — с
  ОБЪЯВЛЕННЫМ
- **И** перепись по подразделу групп печатает «с владельцем» **не ноль**: живая строка отнесена
  к модулю `iam` **по паре (аккаунт, имя)**, а не по тройке с назначением — иначе она ушла бы в
  разряд «без модуля-владельца» и обе находки погасли бы разом (Р5)
- **И** на той же базе **без** правки назначения (MRW-11) находок ноль *(обе стороны оси
  «назначение сошлось / разошлось» названы)*

**MRW-14 · выдача роли усыновляется своим естественным ключом**

- **Дано** манифест, объявляющий выдачу **ролью** субъекту, у которого такая активная выдача в
  базе уже есть с другим идентификатором
- **Когда** применяется посев
- **Тогда** применение проходит, строк выдачи остаётся одна, изменений ноль
- **И** та же выдача, которой в базе нет, заводится, и перепись считает её записанной
  *(обе стороны оси «строка есть / строки нет» названы)*

**MRW-19 · выдачи отношением в базе НЕТ — применитель её ЗАВОДИТ**
*(близнец MRW-03 по ОДНОЙ оси «строка есть / строки нет»; отличается одним фактом — строка
выдачи удалена из базы до применения)*

- **Дано** установка, чья база поднята миграциями дерева и из которой **удалена** строка активной
  выдачи отношения `fga_writer` группе `module-relation-writers` на `cluster:cluster_root`
  вместе с её дочерним субъектом и ведомостью эмитированного
- **И** строка самой группы на месте
- **Когда** служба с манифестом Р1 стартует
- **Тогда** применение проходит, и в базе есть **ровно одна** активная выдача отношения
  `fga_writer` этой группе на `cluster:cluster_root`
- **И** перепись выдач печатает «записано **1**» — то есть строку завёл применитель, а не
  миграция
- **И** журнал прав получил **ровно одну НОВУЮ** строку записи кортежа (строки, оставшиеся от
  свода, не считаются: они старше применения), а ведомость эмитированного — ровно одну строку
- **И** на той же базе **без** удаления строки (MRW-03) та же перепись печатает «записано **0**»,
  и журнал не получает ничего *(обе стороны оси названы)*

**MRW-20 · форма субъекта у получателя-ГРУППЫ несёт СИГИЛ ЧЛЕНСТВА**

- **Дано** мир MRW-19 — строки выдачи в базе нет, применитель её заводит
- **Когда** служба с манифестом Р1 стартует
- **Тогда** субъект FGA, записанный в ведомость эмитированного, равен субъекту, который отдаёт
  объявленный источник формы (П9) для пары (`group`, идентификатор группы), — то есть несёт
  сигил членства, а не голый объект группы
- **И** полезная нагрузка строки журнала прав несёт **того же** субъекта побайтово
- **И** идентификатор заведённой выдачи выведен из **этой же** формы: он равен идентификатору,
  который даёт выражение применителя на субъекте с сигилом, и **не равен** тому, что оно даёт на
  голом объекте группы
- **И** записанный субъект совпадает **дословно** с тем, что свод пишет для этой же выдачи
  своим литералом (`0001_initial.sql:3162` и запись **24** журнала прав) — то есть применитель и
  свод адресуют одного субъекта, а не двух

**MRW-21 · положительный близнец MRW-20 на служебной записи**
*(отличается ОДНИМ фактом — вид получателя: служебная запись вместо группы)*

- **Дано** доставленный манифест модуля, чья выдача посева адресована **служебной записи**, и
  база, в которой этой выдачи нет
- **Когда** применяется посев
- **Тогда** субъект FGA, записанный в ведомость эмитированного и в журнал прав, равен
  `service_account:<идентификатор>` — **без** сигила членства
- **И** он совпадает с тем, что отдаёт объявленный источник формы для той же пары *(обе стороны
  оси «вид получателя» названы: на группе формы расходились, на служебной записи совпадают)*

**MRW-23 · строки ГРУППЫ в базе НЕТ — применитель её ЗАВОДИТ**
*(близнец MRW-02 по ОДНОЙ оси «строка есть / строки нет»; отличается одним фактом — строка
группы удалена из базы до применения. От MRW-19 отличается предметом: там снята выдача, здесь —
группа; их «Тогда» говорят о разных подразделах переписи)*

- **Дано** установка, чья база поднята миграциями дерева и из которой **удалена** строка группы
  `module-relation-writers` — вместе с её выдачей `fga_writer`, потому что снятие группы,
  числящейся субъектом выдачи, схема отвергает (§5), и вместе с её членствами, которые уходят
  каскадом внешнего ключа
- **И** доставки манифестов платформы нет
- **Когда** служба с манифестом Р1 стартует
- **Тогда** применение проходит, и в базе есть **ровно одна** группа
  `module-relation-writers` в системном аккаунте, чьё назначение равно объявленному манифестом
- **И** перепись групп печатает «записано **1**» — то есть строку завёл применитель, а не
  миграция
- **И** идентификатор этой строки **отличен** от снятого `grp258e6bbe9bbe45568`: он отчеканен
  генератором продукта, а не выведен из имени, — иначе «записано 1» могло бы означать
  восстановление прежней строки, а не работу пишущей ветви
- **И** на той же базе **без** удаления строки (MRW-02) та же перепись печатает «записано
  **0**», и идентификатор живой строки не меняется *(обе стороны оси названы)*

### S2 — `module-quota-readers` снимается

**MRW-15 · вступлений в неё не объявляет ни один манифест платформы**

- **Дано** дерево платформы после стадии S2а
- **Когда** обходятся разделы `seed` всех её манифестов
- **Тогда** вступлений в `module-quota-readers` — **ноль**
- **И** вступлений в `module-relation-writers` — **пять**, по одному на модуль
  *(обе стороны названы: снимается одно, остаётся другое)*

**MRW-16 · строки снимаются, кортежи выводятся, след остаётся**

- **Дано** установка, чья база несёт группу `module-quota-readers` и **5** её членств
- **Когда** применяется миграция стадии S2б
- **Тогда** строки группы и всех её членств в базе нет
- **И** на каждое снятое членство журнал прав получил строку снятия кортежа членства
- **И** в очереди аудита есть запись о снятии, называющая арендатора, и её тип события — тот
  же, что производит штатный отзыв
- **И** перепись снятого напечатана числами

**MRW-17 · установка без предмета — перепись, а не отказ**
*(отрицательный близнец MRW-16, одно отличие: группы в базе нет)*

- **Дано** установка, поднятая после стадии S2б, где группы уже нет
- **Когда** та же миграция применяется повторно
- **Тогда** отказа нет, снято ноль, и перепись говорит это **словом**, а не молчанием

**MRW-18 · доставка, ещё объявляющая вступление в снятую группу, получает НАЗВАННЫЙ отказ**

- **Дано** установка после стадии S2б, чья доставка несёт манифест модуля со вступлением в
  `module-quota-readers` (платформа не обновлена)
- **Когда** служба стартует
- **Тогда** применение посева отказывает, и текст отказа называет **пару** (аккаунт, имя)
  ненайденной группы и **модуль**, чей манифест её называет
- **И** та же установка с доставкой после S2а стартует *(обе стороны названы)*

---

## §5 Конструируемость «Дано» — дом и одно-фактная дельта

| сценарий | дом «Дано» | ОДИН факт, которым он отличается от близнеца |
|---|---|---|
| MRW-01 | база из миграций дерева + манифест Р1 | — базовое состояние |
| MRW-02 | состояние после MRW-01 | второй старт |
| MRW-03 | то же, что MRW-02 | идентификатор живой строки выведен при прежнем написании якоря |
| MRW-04 | база со **снятой** строкой группы (и её выдачей — порядок диктует схема) + манифест Р1 + каталог доставки с одним манифестом модуля | доставка есть |
| MRW-05 | то же, без каталога доставки | доставки нет |
| MRW-06 | документ манифеста в памяти судьи формы | выдача не названа |
| MRW-07 | тот же документ | выдача названа |
| MRW-08 | тот же документ | `scopeType` = `iam.account` вместо `iam.cluster` |
| MRW-09 | тот же документ | имя отношения вне канона |
| MRW-10 | тот же документ | отношение `system_viewer` вместо `fga_writer` |
| MRW-11 | база, приведённая применителем к Р1 | — базовое состояние сверки |
| MRW-12 | та же база | объявленной строки группы нет (снимается вместе со своей выдачей — иначе триггер, см. ниже) |
| MRW-13 | та же база | добавлена группа, не объявленная никем |
| MRW-14 | база + манифест с выдачей ролью | форма выдачи — роль, а не отношение |
| MRW-15 | дерево платформы после S2а | — перепись, состояние одно |
| MRW-16 | база до S2б | группа и членства есть |
| MRW-17 | база после S2б | группы нет |
| MRW-18 | база после S2б + доставка до S2а | доставка отстала |
| MRW-19 | та же база, что у MRW-03 | строка выдачи удалена до применения |
| MRW-20 | тот же мир, что у MRW-19 | — базовое состояние пары «вид получателя» |
| MRW-21 | база, в которой этой выдачи НЕТ, + доставленный манифест модуля с выдачей служебной записи | вид получателя — служебная запись, а не группа |
| MRW-22 | база MRW-11 + прямая правка назначения живой строки группы | назначение живой строки разошлось с объявленным |
| MRW-23 | та же база, что у MRW-02, со снятой строкой группы (и её выдачей — порядок диктует схема) | строки группы нет до применения |

**Чем каждый дом строится.** Состояния базы — поднимаемой в пробе базой службы, к которой
применяются миграции дерева; состояния манифеста — документом, поданным судье формы, без
файла в дереве; состояния доставки — каталогом, собранным пробой. Ни одно «Дано» не требует
состояния, которого схема не допускает: все строки, о которых идёт речь, схема уже держит
(группа, выдача формы отношения на кластере, членство), а живые их экземпляры в базе есть
сегодня (§1.3).

**Дом MRW-19 строится СНЯТИЕМ, и снятие выразимо.** Мир «строки выдачи нет» получается тем же
приёмом, каким документ уже пользуется в MRW-12, — удалением строки из базы до применения.
Защита от удаления у этой строки стоит **колонкой**, а не триггером (`0001_initial.sql:1614`,
`deletion_protection boolean`), то есть держится прикладным слоем и прямому удалению фикстурой
не препятствует; дочерние строки уходят с ней сами — внешний ключ ведомости эмитированного
объявлен с каскадным удалением (`0001_initial.sql:5354`). Схема при этом не допускает **лишь**
второй активной строки той же выдачи, а ноль строк допускает: частичный уникальный индекс
судит дубль, а не отсутствие.

**Дом MRW-12 несёт ПОРЯДОК, который диктует схема, а не второй факт мира.** Строку группы,
числящуюся субъектом выдачи, прямое удаление не берёт: триггер
`groups_subject_ref_before_delete_trg` (`0001_initial.sql:5171`) отвергает его кодом `23503`
через `principal_not_referenced_as_subject` (`:1302–1319`). Значит выдача снимается первой, и
дочерняя строка субъекта уходит с ней сама — внешний ключ `access_binding_subjects_scope_fk`
объявлен с `ON DELETE CASCADE` (`:5362`). Дельта против MRW-11 остаётся **одна** — объявленной
группы в базе нет; но следствие этого порядка наблюдаемо, и потому названо в «Тогда» MRW-12
отдельной строкой: приходит **и** находка подраздела выдач. Скрыть её было бы хуже, чем
назвать: проба, требующая ровно одной находки, покраснела бы на верном поведении.

**Дом MRW-23, MRW-04 и MRW-05 — ОДИН, и строится он СНЯТИЕМ СТРОКИ ГРУППЫ.** Три сценария
делят один мир и утверждают о нём разное: MRW-23 — пишущую ветвь группы (ось «строка есть /
строки нет», близнец MRW-02), MRW-04 и MRW-05 — порядок и доставку (ось «доставка есть /
доставки нет», близнецы друг другу). Так же устроены MRW-19 и MRW-20, делящие мир снятой
выдачи.

**Порядок внутри дома диктует схема, а не второй факт мира** — тот же довод, что у MRW-12.
Строку группы, числящуюся субъектом выдачи, прямое удаление не берёт: триггер
`groups_subject_ref_before_delete_trg` (`0001_initial.sql:5171`) отвергает его кодом `23503`
через `principal_not_referenced_as_subject` (`:1302–1319`). Значит выдача снимается первой,
дочерняя строка субъекта уходит с ней каскадом (`access_binding_subjects_scope_fk`, `:5362`), а
пять членств уходят каскадом внешнего ключа группы
(`group_members_group_fk … ON DELETE CASCADE`, `0001_initial.sql:5458`). Ни одно из трёх снятий
не является вторым фактом: это единственный порядок, которым схема допускает первый.

**Наблюдаемое следствие названо в «Тогда», а не скрыто.** В мире без доставки (MRW-05, MRW-23)
членов у группы после применения **ноль** — вступления приезжают доставкой, а её нет; это ровно
то, что MRW-05 и утверждает. Скрыть было бы хуже, чем назвать: проба, ждущая пяти членов,
покраснела бы на верном поведении.

**Почему этот дом ОБЯЗАТЕЛЕН, а не удобен.** На базе миграций дерева строка группы жива из свода
и снимающей её миграции нет ни одной (§2.10). Значит «Тогда», говорящее о **состоянии** группы,
на такой базе истинно независимо от применителя, а «Тогда» о **записи** — беспредметно. Дом со
снятой строкой — единственный, в котором обе стороны оси различимы.

**Дом MRW-22 строится ПРАВКОЙ, и правка выразима.** Назначение живой строки меняется прямым
`UPDATE kaname.groups SET description = …`: ограничение на колонке одно — длина не больше 256
(`groups_description_check`, `0001_initial.sql:1999`), запрещающего триггера нет, а тот, что
есть на обновлении, лишь пишет строку в журнал ресурса
(`20260914090000_resource_journal_serves_the_subscription.sql:311`). Применитель это состояние
не «чинит» по дороге: сверка прогоняется **после** применения, а применитель кладёт объявленное
назначение конфликтом `DO UPDATE SET description` — поэтому правка делается между применением и
сверкой, как и удаление строки в MRW-12.

**Дом MRW-21 обязан называть «выдачи в базе нет», и это НЕСУЩЕЕ.** Без этого факта «Тогда»
беспредметно: писатель объявляет конфликт `ON CONFLICT (id) DO NOTHING` и на живой строке
возвращает `RowsAffected()==0`, то есть **ни** ведомость эмитированного, **ни** журнал прав не
получают ничего (`internal/repo/kaname/pg/module_seed_writer.go:300–334`) — утверждать о форме
записанного субъекта было бы не о чем. Прежняя редакция называла в доме только вид получателя.

**Одно «Дано» требует оговорки.** MRW-03 просит живую строку, чей идентификатор выведен при
прежнем написании якоря. Она **не синтетика**: именно её заводит `0001_initial.sql:3196` на
**каждой** установке, включая чистую, а якорь переименовывается следом — тоже на каждой. То есть MRW-03 и MRW-01 описывают одну и ту же базу с разных сторон, и
различие между «чистой» и «живой» установкой здесь не в этой строке, а в том, применялся ли
посев прежде.

---

## §6 Стадии и признак готовности

Стадия — самостоятельный сдаваемый срез со своим признаком готовности.

### S1 — манифест службы сеет свою группу (репозиторий `PRO-Robotech/kaname`)

1. раздел `seed` у `manifest.yaml` службы: одна группа, одна выдача (Р1);
2. **форма субъекта FGA берётся вызовом объявленного источника** (Р1а): писатель строк посева
   своего выражения формы не держит;
3. манифест встроен в образ **второй копией в каталоге пакета-носителя** `internal/`, и
   держателей у копии **два, разного рода** (Р2): копию **порождает цель сборки** по образцу
   `make fga-model-embed` (`Makefile:718`), а побайтовое равенство её с `manifest.yaml` дерева
   **держит проба** по образцу `TestEmbeddedModelIsByteIdenticalToCanonical`
   (`internal/authzmodel/identity_test.go:24`). `make model-canon-check` образцом **не
   служит** — у него другой предмет (§2.6);
4. манифест доезжает до применителя посева **отдельным доводом**, а не перечнем доставки, и
   применяется **раньше** доставленных (Р3); остальные четыре потребителя перечня его **не
   получают** — по строке исхода на каждого в таблице Р3;
5. идемпотентность выдачи по естественному ключу (Р4), **обе** формы;
6. атрибуция по объявлению (Р5), с переписью в обе стороны, и она касается **двух**
   подразделов: живой **группы** — по паре (аккаунт, имя) объявленной группы, и живой
   **выдачи**, чей субъект эта группа, — по объявлению той же группы. Второе обязательно:
   без него объявленная манифестом выдача не находит живой пары и сверка даёт ложную находку
   «выдача ОБЪЯВЛЕНА и не живёт» на верном дереве (замер — Р5);
7. сценарии `MRW-01…MRW-14` и `MRW-19…MRW-23` — пробами; отрицания — с положительными
   близнецами;
8. шапки, приведённые к факту, — **шесть координат**, и перечень закрытый. Он пересобран
   обходом шапок **обоих** пакетов, которых касается S1, а не дополнен одной строкой: закрытый
   перечень, неполный на предмет собственного решения, хуже открытого — находка, которой в нём
   нет, следующим кругом не ищется by construction (§2.11):
   1. `internal/moduleseedparity/parity.go:13–23` (§2.4);
   2. godoc `ownerOfSeededName` `internal/moduleseedparity/parity_test.go:392–397` («Правило
      ОДНО на служебную запись и на группу … Второе правило … разошлось бы с первым молча» —
      после Р5 ложно, и Р5 отвечает на этот довод прямо);
   3. godoc `readLiveGroups` `internal/moduleseedparity/parity_test.go:445–452` («относит их к
      модулю-владельцу ТЕМ ЖЕ правилом имени … Сегодня ни одна живая группа этому правилу не
      отвечает … Поэтому „объявлено групп 0“ — верное объявление» — после Р5 ложно целиком);
   4. раздел «РАЗДЕЛА `seed` У ЭТОГО МАНИФЕСТА НЕТ» в самом `manifest.yaml`;
   5. godoc `grant` `internal/repo/kaname/pg/module_seed_writer.go:256–270` («… получит на этой
      выдаче другой идентификатор — … поэтому **цена названа и принята**») — цена названа не та:
      §2.2 измеряет отказ `23505` и несостоявшийся старт, а после Р4 применитель второго
      идентификатора не выводит вовсе. Предикат: `git grep -n 'цена названа и принята' --
      internal/repo` → после S1 пусто;
   6. шапка пакета `internal/apps/kaname/moduleseed/apply.go:14–19` («условием служит ДОСТАВКА
      манифеста») — после Р3 доставкой связаны только **доставленные** манифесты, свой едет
      отдельным доводом и применяется без неё (MRW-05). Предикат:
      `git grep -n 'условием служит ДОСТАВКА' -- internal` → после S1 пусто либо строка,
      различающая два разряда.

**Признак готовности S1:** сверка посева зелена, перепись **обоих** тронутых подразделов —
групп и выдач — печатает «с владельцем» не ноль; применение на базе с живыми строками свода
проходит без отказа; применение на базе, из которой строка **выдачи** удалена, печатает
«выдач записано 1» и кладёт субъекта **с сигилом членства**, а на служебной записи — **без**
него; применение на базе, из которой удалена строка **группы** (вместе с её выдачей — порядок
диктует схема), печатает «групп записано 1» и чеканит идентификатор, отличный от снятого, а на
той же базе без удаления — «групп записано 0» (обе половины оси, а не одна: строка группы жива
из свода на каждой установке, поэтому «есть ровно одна группа» истинно и при применителе,
который раздела `seed.groups` не читает, — §2.10); **проба** побайтового равенства встроенной
копии зелена, и правка `manifest.yaml` **без пересборки копии её роняет** (цель сборки этого
признака не исполняет — она копию восстанавливает, а не сверяет две); довод применителя посева
отличен от перечня доставки; ни одно из **шести** утверждений п. 8 в дереве не встречается.

### S2а — вступления в снимаемую группу уходят из манифестов платформы (репозиторий `PRO-Robotech/kacho`)

Пять правок, по одной на манифест (§1.4, левый столбец), плюс шапки пяти манифестов, которые
после S1 утверждают неверное: группу заводит **служба**, а не платформа.

**Признак готовности S2а** (предикат исполнимый, по копии платформы):
`for m in compute nlb registry storage vpc; do git -C <копия платформы> show origin/main:services/$m/manifest.yaml | grep -c 'module-quota-readers'; done`
→ **0** во всех пяти; та же команда по `module-relation-writers` → **1** во всех пяти.

### S2б — строки снимаются миграцией службы (репозиторий `PRO-Robotech/kaname`)

Новая миграция: 5 членств, строка группы, снятие кортежей членства через журнал, след аудита,
перепись. Сценарии `MRW-16…MRW-18`.

**Признак готовности S2б:** в базе после применения нет ни группы, ни её членств; на каждое
снятое членство есть строка снятия в журнале; запись аудита одна на каждое снятие; повторное
применение даёт «снято 0» и не отказывает.

**Предикат допуска S2б — ВНЕШНИЙ, и это сказано прямо.** Стадия не применяется, пока S2а не
выпущена платформой и не доехала доставкой до установок. Машинной проверки у этого условия
нет: миграция исполняется в init-контейнере и каталога доставки не читает by construction.
Держится это **порядком работ**, а не гейтом; признак для ведущего линию — S2а влита в ствол
платформы и попала в выпуск.

---

## §7 Что документ НЕ решает — вынесено явно

| предмет | почему не здесь |
|---|---|
| состав получателей права `fga_writer` | не меняется: те же пять служебных записей. Изменение состава — выдача или отзыв, и у него своя поверхность |
| снятие группы `module-relation-writers` | она остаётся; предмет работы — **кто её объявляет** |
| имя системного аккаунта `kacho-system` в дереве службы | остаток бренда, `PRO-Robotech/kacho#2076`; манифест называет аккаунт **живым** написанием, и переименование — своя работа со своим окном написаний (`internal/domain/seed_identity_window.go`) |
| снятие строк модулей у применителя | применитель не снимает строк, и это объявленный остаток его шапки, а не забытая половина |
| переезд **девятнадцати** ролей `iam` от миграций к применителю ролей | предмет **адъюдицирован**, а не отложен: перепись живого гейта (П10) объявляет их историческим остатком применённых миграций, правке не подлежащим (ban #5); классификацию держит APPROVED-приёмка `roles-come-as-data-not-migrations.md` §3.4. **Открытой задачи у него нет — перемерено по двум трекерам**, и предикат прекращения остатка назван машинным (Р3, «Что это НЕ откладывает»). Здесь раздел `roles:` манифеста службы не читает никто — строка исхода в Р3 |
| строки каталога модуля `iam` | их сеет миграция (П11), и второго писателя эта работа не заводит: применитель каталога манифеста службы не получает — строка исхода в Р3 |
| исторический тип якоря `iam_fgaproxy` | живых фактов на нём нет, снимается вместе со своими применёнными миграциями — сказано в каноне (`sed -n '936,952p' proto/kaname/cloud/iam/v1/fga_model.fga`) |
| доставка манифеста службы соседям | службе он не нужен: свои типы она берёт из встроенного канона образа (`internal/authzmodel/authzmodel.go:67`) |
| живые строки **без модуля-владельца**, остающиеся такими после Р5 | предмет зарезервирован APPROVED-соседом `module-manifest-relation-grant.md` §5 («их предмет — принадлежность платформе, а не модулю, и он открыт отдельно»). Эта работа его **сужает**, а не закрывает: из разряда уходят **две** строки — группа `module-relation-writers` и её выдача `fga_writer`, — потому что посылка соседа «нет модуля, чей манифест мог бы их объявить» истекает вместе с Р1 ровно для них. Остальные остаются в разряде, и открытой задачи у них **нет** — перемерено по обоим трекерам (§2.12). Задачу эта работа не заводит по условию задания; перепись «без модуля-владельца» после S1 печатает их число, и это единственное, что здесь обещается |
| порядок применения **между доставленными** манифестами, выводимый из `joins` | зарезервирован APPROVED-соседом `module-manifest-seed-contract.md` §4 за преемником с предикатом «манифестов ≥2, и хотя бы один объявляет группу, в которую вступает другой»; преемник, названный `module-manifest-resources-roles-deprecated.md:830`, — `#1088`, **`CLOSED`**. Предикат заведения наступает вместе с Р1, и Р3 отвечает на него для **всей существующей популяции**: группу объявляет ровно один манифест — свой, — а «своё раньше доставленного» есть единственный порядок, который из `joins` сегодня выводится (§2.12). Между доставленными порядок **не решён**, и это состояние без предмета, а не отсрочка: доставленных манифестов, объявляющих группу, — **ноль из пяти**. Предмет вернётся, когда доставленный манифест объявит группу, в которую вступает другой доставленный: `for m in …; do git -C <копия платформы> show origin/main:services/$m/manifest.yaml \| python3 -c "…len(seed.groups)…"; done` → не ноль |

---

## §8 Ведомость «сценарий → производитель»

Строка называет либо **живого** производителя (координата, проверенная командой), либо
**стадию**, которая его заводит. Третьего состояния в ведомости нет.

| сценарий | производитель «Тогда» | состояние |
|---|---|---|
| MRW-01 | применитель (П3) + писатель группы (П4) + встроенный манифест | П3, П4 живы; встроенность — **S1** |
| MRW-02 | перепись `Report`/`Census` применителя (`internal/apps/kaname/moduleseed/apply.go`, типы `Report` и `Census`) | живой; разряд «свой манифест» — **S1** |
| MRW-03 | конфликт по естественному ключу | **S1** (Р4) |
| MRW-04 | порядок применения — на базе со снятой строкой группы, иначе «Тогда» истинно при любом порядке (§2.13) | **S1** (Р3) |
| MRW-05 | ветвь «доставка не объявлена» (`cmd/kaname/module_manifests.go:60–68`) + пишущая ветвь группы (`UpsertGroup`, `module_seed_writer.go:182–200`) | ветвь и писатель живы; заведение своей группы без доставки — **S1** |
| MRW-06 | `ErrGroupNeverGranted` (П6) | **живой** |
| MRW-07 | тот же судья на годном документе | **живой** |
| MRW-08 | `ErrBindingAnchor` (П7) | **живой** |
| MRW-09 | `ErrRelationNotDeclared` (`internal/manifest/linkage.go:81`) | **живой** |
| MRW-10 | `ErrRelationRecipientKind` (П7) + `AcceptsKind` (`internal/authzmodel/relationsubjects.go:145`) | **живой** |
| MRW-11 | четвёртый `diffSet` сверки (П8) + перепись `Subsection` (`parity.go:176–179`) | П8 жив; «с владельцем» не ноль у групп **и у выдач** — **S1** (Р5) |
| MRW-12 | тот же `diffSet`, сторона «объявлено и не живёт» — в обоих подразделах | **живой**, доказан инъекцией (`TestInjectionSeedParityDeclaredGroupNotLiveIsAFinding`) |
| MRW-13 | разряд «без модуля-владельца» переписи | **живой**; правило владельца для групп — **S1** |
| MRW-14 | конфликт по `access_bindings_active_grant_uniq` (`0001_initial.sql:4443`) | индекс жив; применение по нему — **S1** |
| MRW-15 | обход разделов `seed` манифестов платформы | **S2а** |
| MRW-16 | новая миграция службы | **S2б** |
| MRW-17 | её перепись | **S2б** |
| MRW-18 | отказ резолва группы (`internal/repo/kaname/pg/module_seed_writer.go:380`) | текст, называющий пару, **жив**; называние модуля — **S1** |
| MRW-19 | пишущая ветвь выдачи (`internal/repo/kaname/pg/module_seed_writer.go`, `grant`) + перепись `Report`/`Census` | ветвь записи и перепись **живы**; доезд манифеста своим доводом — **S1** |
| MRW-20 | объявленный источник формы субъекта (П9) | источник **жив**; его вызов писателем посева — **S1** (Р1а) |
| MRW-21 | тот же писатель на служебной записи (`fgaUser` в `grant`) | **живой**: на этом виде получателя форма совпадает с каноном уже сегодня (§2.5) |
| MRW-22 | тот же четвёртый `diffSet`, **обе** стороны сразу — ключ сравнения тройной (`keysOfGroup`, `parity.go:301`) | обе стороны **живы** и доказаны инъекцией; атрибуция по паре (аккаунт, имя) — **S1** (Р5) |
| MRW-23 | пишущая ветвь группы — `UpsertGroup` с `ids.NewID(domain.PrefixGroup)` (`internal/repo/kaname/pg/module_seed_writer.go:182–200`) + перепись `Report`/`Census`, разряд групп | ветвь записи и перепись **живы**; доезд своего манифеста отдельным доводом — **S1** (Р3) |

**Единица счёта названа, а строки перечислены** — прежняя запись этого числа единицы не
называла, и проверить его читатель не мог. Строк, у которых производитель «Тогда» **жив без
остатка**, — **7** из **23**: `MRW-06`, `MRW-07`, `MRW-08`, `MRW-09`, `MRW-10`, `MRW-12`,
`MRW-21`. Остальные шестнадцать несут либо названную стадию, либо пару «живое + стадия», и в
колонке состояния это различие стоит дословно. Ни одного «Тогда» без того или другого в
документе нет.

**Что изменилось против редакции 2 — названо числом, а не словом «дополнено».** Сценариев было
21, стало **22** в редакции 3 (прибавился `MRW-22`, блокирующее Б4 круга 1) и **23** в
редакции 4 (прибавился `MRW-23`, находка §2.10). Число «живых без остатка» осталось **7**:
`MRW-22` и `MRW-23` несут пару «живое + стадия», как и `MRW-11` с `MRW-19`, — обе находки
`MRW-22` производит живой `diffSet`, а атрибуция приходит стадией; пишущую ветвь `MRW-23`
исполняет живой `UpsertGroup`, а свой манифест доезжает до него стадией.

---

## §9 Чем это будет держаться

| требование | чем | почему не иначе |
|---|---|---|
| форма манифеста службы | тот же судья формы и связности, что у модулей | второй судья для второго документа разошёлся бы с первым молча |
| форма субъекта FGA у получателя-группы | проба, утверждающая **три** предмета одного выражения: субъекта в ведомости эмитированного, субъекта в журнале прав и производную идентификатора выдачи — рядом с близнецом на служебной записи | проба на одном предмете зелена при неверной форме в двух других; проба без близнеца зелена при реализации, приписывающей сигил всем видам получателя |
| согласие встроенной копии манифеста с деревом | **пара разного рода**: копию **порождает** цель сборки по образцу `make fga-model-embed` (`Makefile:718`), равенство двух копий **держит проба** по образцу `internal/authzmodel` `TestEmbeddedModelIsByteIdenticalToCanonical` (`identity_test.go:24`) | вниманием две копии не держатся: они расходятся в тот день, когда правят одну, и расхождение молчит до первого старта. **Одной цели мало:** цель копию ВОССТАНАВЛИВАЕТ, а не сверяет две — признак «правка манифеста без пересборки роняет» исполняет проба. И образцом здесь **не** `make model-canon-check`: он порождает блоки из манифестов модулей и о второй копии не знает вовсе (§2.6), поэтому цель, построенная по нему, свойства не имела бы by construction |
| «манифест службы не в перечне доставки» | проба провязки композиционного корня: довод применителя посева отличен от перечня, который читают пятеро | проба на исходе применения о **радиусе** не утверждает ничего — она зелена и тогда, когда манифест достался всем пятерым |
| порядок «своё раньше доставленного» | проба применителя на базе, из которой строка группы **снята** (вместе с выдачей — порядок диктует схема), подающая манифест модуля со вступлением **и** свой манифест, и требующая, чтобы вступление легло | проба на одном манифесте о порядке не утверждает ничего; проба на базе миграций дерева — тоже: группа там жива из свода, и вступление ложится при любом порядке (§2.13) |
| пишущая ветвь **группы** | интеграционная проба на базе со снятой строкой группы, требующая «групп записано 1» и идентификатора, отличного от снятого, рядом с близнецом на нетронутой базе — «записано 0» и прежний идентификатор | утверждение о **состоянии** группы истинно от свода при применителе, не читающем `seed.groups`; различает стороны только запись (§2.10) |
| идемпотентность по естественному ключу | интеграционная проба на базе, поднятой **всеми** миграциями дерева, — то есть на живой строке, чей идентификатор выведен при прежнем якоре | синтетическая строка, чей идентификатор выведен при ЖИВОМ якоре, зелена при любом устройстве конфликта: она совпадает с тем, что посчитает применитель |
| атрибуция группы и её выдачи | проба, исполняющая **саму атрибуцию** на живой базе, — сегодня это `TestModuleManifestDeclaresTheSeedTheLiveBaseHolds` (`internal/moduleseedparity/parity_test.go:103`), где живое читается из базы и относится к модулю; плюс инъекция в обе стороны | **инъекции `Compare` этого не держат, и это измерено:** обе живые инъекции строят `ModuleState` рукой (`parity_injection_test.go:235–252` — `LiveGroup: …` подаётся готовым), то есть атрибуцию не исполняют вовсе и зелены при ЛЮБОМ её правиле. Атрибуция живёт в чтении живой базы (`readLiveGroups` → `ownerOfSeededName`, `readLiveBindings` → `ownerOfBindingSubject`), и держать её обязана проба, которая через них проходит. Односторонняя проба к тому же зеленела бы на правиле, относящем к владельцу всё подряд |
| снятие `module-quota-readers` | проба миграции на базе с предметом **и** на базе без него | вторая половина отличает «снято ноль» от «не исполнилось» |
| шапки, приведённые к факту | вниманием круга ревью | предиката у «шапка согласна с телом пакета» нет; проверяемый признак — **шесть** координат §6 п. 8, и ни одно из их утверждений в дереве не встречается: `parity.go:13–23` (§2.4), godoc `ownerOfSeededName` `parity_test.go:392–397`, godoc `readLiveGroups` `parity_test.go:445–452`, раздел «РАЗДЕЛА `seed` У ЭТОГО МАНИФЕСТА НЕТ», godoc `grant` `module_seed_writer.go:256–270`, шапка пакета `moduleseed/apply.go:14–19`. Вторая, третья, пятая и шестая ложны именно от решений ЭТОГО документа (Р5, Р4, Р3) — и потому перечисляются поимённо: перечень, неполный на предмет собственного решения, есть гарантия того, что утверждение переживёт свой предмет. Редакция 3 объявила перечень закрытым на четырёх и была неполна на пятой; норма из этого: закрытый перечень собирается **обходом** шапок затронутых пакетов, а не памятью о найденном (§2.11) |

**Названо честно:** у последней строки машинного держателя нет, и он здесь не обещается.

---

## §10 Публичный репозиторий

Оба репозитория публичны. Документ называет **предмет**, **решение** и **следствие для
контракта** и не собирает в одном месте тройку «координата + условие, при котором защита не
действовала + следствие для постороннего».

Что названо и почему это безопасно: имена группы, отношения и якоря лежат в открытом своде и
в открытом каноне; состав получателей права работой **не меняется**; отказ, описанный в §2.2,
есть отказ **старта службы**, а не обход защиты — он закрывает установку, а не открывает её.
Идентификаторы строк (`grp…`, `acb…`) — идентификаторы **системных** строк, одинаковые на
каждой установке по построению свода, и адресовать ими снаружи нечего: обе строки защищены от
удаления и наружной поверхности не имеют.

---

## §11 Ответ на круг 1 — чем закрыт каждый пункт записи

Запись круга —
`docs/specs/reviews/service-manifest-seeds-its-own-groups/7feb33fd9c2cb68be55291dc8be0ae3740ba0d744a3926e7e519b8fb50a61436.yaml`
(коммит `87ada2cf`, вердикт `CHANGES_REQUESTED`, роль `acceptance-reviewer`). Предмет вердикта
— **редакция 2** (`3fc15570`, отпечаток `7feb33fd…1436`). Запись **не правлена ни одной
строкой**: история ревью добавляется, а не переписывается.

**Блокирующих пять, опровергнуто НОЛЬ.** Каждое перемерено этой редакцией своим предикатом —
не прочитано, а прогнано, — и каждое подтвердилось. Сказано первым, потому что «перемерил и
сошлось» иначе неотличимо от «не перемерял».

### §11.1 Блокирующие

| # | предмет | чем закрыт | предикат, прогнанный заново |
|---|---|---|---|
| **Б1** | требуемый гейт красен, и единственная находка называет этот документ | шапка получила **две** строки редакции: «Редакция 3 — правка ПОСЛЕ объявления состояния» и перечень трёх редакций с отпечатками. Строка «Статус» **не правлена** — см. §11.2 | `go test -count=1 -short ./internal/check/` — было: код 1, `TestAcceptanceEditedAfterItsVerdictSaysSo`, перепись «правлено после объявления состояния 33 · из них с записью о правке 32». Стало: код 0 |
| **Б2** | держателем побайтового согласия назван `make model-canon-check` — у цели другой предмет | §2.6 называет прецедент **парой разного рода**: порождает `make fga-model-embed` (`Makefile:718`), держит `TestEmbeddedModelIsByteIdenticalToCanonical` (`identity_test.go:24`). Р2, §6 п. 3 и §9 переписаны под пару; `make model-canon-check` назван тем, что он есть | `sed -n '349,352p' Makefile` (цель обходит модули и порождает блоки из их манифестов) · `grep -n 'authzmodel' tools/model-canon-check.sh` → пусто · `sed -n '710,713p' Makefile` («Равенство держит гейт `internal/authzmodel` …») · `go test -count=1 -short -run TestEmbeddedModelIsByteIdenticalToCanonical ./internal/authzmodel/` → `ok` |
| **Б3** | Р5 делает ложными две шапки `parity_test.go`, а §6 п. 8 их не называет | §6 п. 8 стал **закрытым перечнем из четырёх** координат, обе godoc названы с номерами строк и цитатой; §9 повторяет их в проверяемом признаке. В Р5 довод godoc **отведён прямо**: правила о РАЗНЫХ предметах разойтись молча не могут, а одно правило на два предмета уже разошлось с деревом | `sed -n '392,397p' internal/moduleseedparity/parity_test.go` · `sed -n '445,452p' internal/moduleseedparity/parity_test.go` — обе шапки на месте и после Р5 ложны |
| **Б4** | сторона «группа ЖИВЁТ и не объявлена» без сценария; инъекции строят `ModuleState` рукой; ключ атрибуции не назван | (1) Р5 называет ключ: относим по **паре** (аккаунт, имя), сравниваем по **тройке** с назначением. (2) Заведён **MRW-22** — дом и дельта в §5, строка в §8. (3) §9 называет держателем пробу, исполняющую **саму атрибуцию**, а не инъекции `Compare` | `sed -n '235,252p' internal/moduleseedparity/parity_injection_test.go` — `LiveGroup` подаётся готовым, атрибуции нет · опыт на `Compare` (временный пакет, снят за собой): дрейф назначения → находок **2**, близнец без дрейфа → **0**, MRW-12 (живой строки нет) → **1** |
| **Б5** | Р3 снимает ban #11 доводом «работа открыта» — открытой работы нет | довод переписан **третьим** исходом записи: предмет **адъюдицирован** переписью живого гейта как исторический остаток применённых миграций (ban #5), и назван **машинный предикат** прекращения остатка. Слова «открыта» в Р3 больше нет; §7 приведён к тому же | перебор открытых задач обоих трекеров по заголовкам и телам → в `kaname` одна задача о держателях гейтов (`#111`), к переезду ролей отношения не имеющая; в `kacho` одна о координатах в приёмках (`#1896`); `gh issue view 1091 -R PRO-Robotech/kacho --json state` → `CLOSED` |

### §11.2 Б1 закрыт НЕ ТОЙ ФОРМОЙ, которую предписала запись, — и это сказано прямо

Запись требовала **двух** половин: привести строку 8 к факту **и** внести запись о правке.
Внесена вторая половина; **строка 8 не правлена**, и вот почему — чтобы круг 2 судил решение, а
не обнаружил его сам:

- **связывающее указание круга 2** — «шапку со статусом не правь». Указание исполнено буквально;
- **норма корпуса на стороне того же исхода**: статическая форма вердикта в шапке после ревью не
  правится, действующее состояние выводится из записи (`change-graph.md` §«Вердикт привязан к
  ОТПЕЧАТКУ, а не к документу»);
- **прецедент этого же дома, три круга подряд** — `password-verifier-follows-the-stored-value.md`:
  строка «Статус» оставлена словами редакции 1 намеренно, а каждая следующая редакция получает
  СВОЮ строку. Предикат: `sed -n '7,20p'
  docs/engineering/acceptance/password-verifier-follows-the-stored-value.md`;
- **опасение записи («только запись оставит шапку ложной») закрыто не молчанием, а СОСЕДНЕЙ
  СТРОКОЙ**: «редакция 1 к первому кругу» больше не читается как действующее состояние —
  следующая же строка шапки называет редакцию, круг, вердикт, отпечаток и координату записи, а
  третья перечисляет все три редакции с отпечатками и предикатом. Читатель не может унести
  «кругов ноль», прочитав шапку подряд.

**Половина замечания, которую это НЕ закрывает, названа честно:** прочитавший **только** строку
8 и не прочитавший следующей унесёт неверное. Цена этого меньше цены нарушения связывающего
указания и нормы о статической форме, но она есть. Если круг 2 решит иначе — правка строки 8
одна строка, и запись круга 1 останется верной о своей редакции в обоих случаях.

**Отдельно о гейте:** он погас бы и от одной правки строки 8 (тогда отметки строки и файла
совпали бы и ветвь сравнения не исполнилась бы вовсе). Выбранная форма **сильнее**: она гасит
находку записью, которая переживёт и следующую правку тела — когда отметка строки 8 снова
окажется старше отметки файла.

### §11.3 Незаблокировавшие — все шесть закрыты

| # | предмет | что сделано |
|---|---|---|
| **В1** | предикат §1.1 совпадал с собственным объяснением | область сужена до `-- '*.go' ':!*_test.go'`; рядом второй предикат `go:embed.*manifest` и объяснение, почему сужение несущее |
| **В2** | перечень каталогов записей давал другое число и ходил по `--all` | предикат **снят**: он опровергается собственным кругом (сегодня даёт **10** имён, и десятое — наше). Отсутствие кругов у редакции 2 доказывается теперь чтением дерева на прибитой ревизии: `git ls-tree -r 3fc15570 --name-only \| grep -c 'docs/specs/reviews/service-manifest-seeds-its-own-groups/'` → 0 |
| **В3** | `awk` не сбрасывал флаг на границе файлов и давал три строки | предикат переписан **помодульно**, вывод приведён построчно; прежняя ошибка названа врезкой вместе с механикой |
| **В4** | дом MRW-21 не называл «выдачи в базе нет» | дом в §5 приведён, и добавлен абзац: без этого факта «Тогда» беспредметно — `ON CONFLICT (id) DO NOTHING` даёт `RowsAffected()==0`, и ни ведомость, ни журнал не получают ничего (`module_seed_writer.go:300–334`) |
| **В5** | Р2 не называла пакет-носитель второй копии | назван: собственный пакет `internal/`, который импортирует композиционный корень, — **не** каталог применителя посева. Два довода: директива не принимает родительского каталога; применитель — слой use-case и файлов не читает (Р3) |
| **В6** | приставка `PRO-Robotech/kacho:` делает предикаты неисполнимыми | все предикаты чужого дерева переписаны формой `git -C <копия платформы> show <ревизия>:<путь>` с прибитой `K=45d1719fe8…`; приставка осталась только там, где объясняется само соглашение |

### §11.4 Что нашла ЭТА редакция сверх записи — две находки, обе о собственном документе

**Первая — и она несущая: MRW-11 («находок ноль») был НЕДОСТИЖИМ даже после Р1 и Р5 в прежнем
их виде.** Р5 меняла правило владельца для **групп** и молчала о **выдаче**, чей субъект —
группа. Между тем владелец живой выдачи выводится из имени её субъекта **тем же** правилом
`kacho-<служба>`, и имя `module-relation-writers` ему не отвечает. Замер (временный опыт в
пакете проб, снят за собой):

```
ownerOfSeededName("module-relation-writers")                  -> owner="" ok=false
ownerOfBindingSubject("group", "", "module-relation-writers") -> owner="" ok=false
ownerOfBindingSubject("service_account", "kacho-vpc", "")     -> owner="vpc" ok=true
```

Значит объявленная манифестом выдача не нашла бы живой пары, и сверка дала бы **ложную**
находку «выдача ОБЪЯВЛЕНА и не живёт» на верном дереве — то самое, чего О4 опасается для групп,
но в подразделе выдач. Правило по объявлению распространено на выдачу (Р5), признак готовности
S1 теперь требует «с владельцем не ноль» у **обоих** подразделов.

**Вторая — дом MRW-12 требовал второго факта, и схема его диктует.** Прямое снятие строки
группы отвергается триггером `groups_subject_ref_before_delete_trg` (`0001_initial.sql:5171`)
кодом `23503`, пока группа числится субъектом выдачи. Дом приведён: выдача снимается первой,
дочерняя строка субъекта уходит каскадом (`access_binding_subjects_scope_fk`, `:5362`), а
наблюдаемое следствие — вторая находка, в подразделе выдач, — названо в «Тогда» MRW-12. Скрыть
её было бы хуже, чем назвать: проба, требующая ровно одной находки, покраснела бы на верном
поведении.

**Плюс одно число, пережившее свой предмет:** заголовок §1.2 говорил «восемь производителей»
при одиннадцати строках таблицы (`grep -c '^| П' <документ>` → 11). Приведён к факту.

### §11.5 Чего РЕДАКЦИЯ 3 НЕ делала

- **не трогает ни одного решения Р1, Р1а, Р4, Р6, Р7** и ни одного сценария, кроме `MRW-12`
  (дом и вторая находка) и нового `MRW-22`. Круг 1 назвал одиннадцать производителей §1.2 живыми,
  все четыре опровержения §2.1…§2.4 и обе находки §2.5, §2.8 верными — правка сверх блокирующих
  размывает, что проверять кругу 2;
- **не правит запись круга 1** и не правит строку «Статус» (§11.2);
- **не заводит задач** — по условию задания круга 2; находки §11.4 закрыты в самом документе,
  предметов вне него они не создают;
- **вердикта на себя не выносит.** Отпечаток редакции 3 с записанным в круге 1 не совпадает, и
  одобрения у документа нет: санкции на объём §4 по-прежнему НЕТ (ban #1). Одобрение, когда до
  него дойдёт, потребует **внешнего события** — эпоха post-cutover, и политика называет
  единственного, кто выпускает effective approval приёмки; агент в роли им не является.

---

## §12 Ответ на находки оппонента — доводка к кругу 2

**Записи ревью круга 2 в дереве нет** (`ls docs/specs/reviews/service-manifest-seeds-its-own-groups/`
→ один файл, отпечаток редакции 2), и посылка задания «рецензия оборвана окном» опровергнута
замером тем же способом, что и в §2.1: оборвать можно то, что началось, а у редакции 3 нет ни
оборванной записи, ни вынесенной. Четыре находки, по которым сделана редакция 4, пришли **не из
записи** и вердиктом не являются — это **данные**, и как данные они перемерены. Подтвердились
**четыре из четырёх**, опровергнуто **ноль**. Сверх них перемер класса по первой находке дал
**пятую** координату, которой не называл никто (§2.11, п. 6).

Сказано первым, потому что «перемерил и сошлось» иначе неотличимо от «не перемерял».

### §12.1 По каждой находке — посылка, замер, исход

| # | посылка находки | замер, прогнанный заново | исход в документе |
|---|---|---|---|
| **1** | §6 п. 8 объявляет перечень шапок закрытым на четырёх координатах; пятая — godoc `grant` — сделана ложной решением Р4 и в перечень не попала | `sed -n '256,270p' internal/repo/kaname/pg/module_seed_writer.go` — шапка кончается «цена названа и принята»; `python3` по выражению применителя: `#member` @ `cluster_root` → `acbc5d31947bd0d570e8`, @ `cluster_kacho_root` → `acb9e920fb038c7e4f74` = живая строка; контроль `quota_reader` → `acb1296746dc06ec9e25` сошёлся с §1.3; `git grep -n 'цена названа и принята' -- internal/repo` → **1** строка | **подтверждена.** Перечень пересобран **обходом** шапок обоих пакетов S1, а не дополнен строкой: координат **шесть**, шестая — шапка пакета `moduleseed/apply.go:14–19` («условием служит ДОСТАВКА манифеста», после Р3 ложно для своего манифеста). Признак готовности S1 прогоняется по шести; что перемер **не** нашёл, названо (§2.11) |
| **2** | пишущая ветвь применителя для **группы** не утверждается ни одним «Тогда»; §2.8 закрыла класс для выдачи и оставила половину | `grep -rn 'DELETE FROM kaname.groups' internal/migrations/*.sql` → пусто; `sed -n '3489p' 0001_initial.sql` → строка группы; `sed -n '182,200p' module_seed_writer.go` — `DO UPDATE … WHERE description IS DISTINCT FROM`; `grep -n 'записано' <документ>` → три вхождения «записано 1», все о выдаче | **подтверждена.** Заведён **MRW-23** — близнец MRW-02 по оси «строка есть / строки нет», обе стороны названы; дом в §5 (снятие вместе с выдачей — порядок диктует триггер `groups_subject_ref_before_delete_trg`; членства — каскад `group_members_group_fk`, `0001_initial.sql:5458`); строка в §8; признак готовности S1 называет **обе** половины; §9 получил строку держателя. «Живых без остатка» — **7 из 23** |
| **3** | Р5 отменяет классификацию APPROVED-соседа `module-manifest-relation-grant.md` (§5, §3.7), и сосед не назван ни разу | `grep -c 'module-manifest-relation-grant' <документ>` → **0**; `sed -n '885,900p'` и `sed -n '457,461p'` соседа — цитаты подтверждены дословно; открытых задач с образцом «без модуля-владельца\|принадлежность платформе» → **0** при 48 осмотренных в `kaname` и 235 в `kacho` | **подтверждена.** Сосед назван в Р5, §2.12 и §7. Исход — **истечение, а не отмена**: посылка соседа «нет модуля, чей манифест мог бы их объявить» перестаёт быть верной для **двух** строк вместе с Р1 (группа и её выдача), остальные остаются в разряде и остаются предметом соседа; владельца у него нет, и задачу эта работа не заводит по условию задания — числа названы в §7 |
| **4** | (1) «Дано» MRW-04 требует базы без группы, а §5 её не строит; на построенном доме сценарий не может упасть. (2) Р3 решает предмет, зарезервированный `module-manifest-seed-contract.md` §4 за преемником `#1088`, который закрыт | (1) §5 редакции 3, строка MRW-04 — снятия нет; группа жива из свода (замер находки 2). (2) `sed -n '1043p'` соседа — оговорка и предикат «≥2 и хотя бы один объявляет группу, в которую вступает другой»; `sed -n '830p' module-manifest-resources-roles-deprecated.md` → преемник `#1088`; `gh issue view 1088 -R PRO-Robotech/kacho --json state` → `CLOSED`; популяция: свой манифест **1** (в корне — предикат соседа по `*/manifest.yaml` даёт **0**, единица счёта названа), доставленных **5**, из них объявляющих группу — **0** (`0 2` у каждого) | **подтверждена в обеих частях.** (1) Правлен **дом**, не «Дано»: MRW-04 и MRW-05 получили базу со снятой строкой группы, дельта между ними осталась одна — «доставка есть / доставки нет»; побочно «Тогда» MRW-05 стало утверждением о применителе, а не о своде (§2.13). (2) Сосед назван в Р3, §2.12 и §7. Исход — Р3 **замещает** зарезервированный предмет для всей существующей популяции (группу объявляет ровно один манифест, свой) и **не решает** порядка между доставленными, чья популяция ноль объявленных групп из пяти; предикат возврата предмета назван в §7; довод «работа открыта» не используется — преемник закрыт, и это сказано |

### §12.2 Что нашла ЭТА редакция сверх находок — одна координата и одна норма

**Шестая координата перечня §6 п. 8** — шапка пакета применителя (`moduleseed/apply.go:14–19`):
«строки заводит применитель, а условием служит ДОСТАВКА манифеста». Она верна для доставленных
манифестов и становится ложной для своего — MRW-05 утверждает ровно обратное (доставленных 0,
группа и выдача заведены). Нашлась не находкой, а **обходом**: перечень, объявленный закрытым,
пересобирался по шапкам обоих пакетов, которых касается S1, и обход дал на одну координату
больше, чем назвала находка.

**Норма, выведенная на себе:** закрытый перечень собирается **обходом** предмета, а не памятью о
найденном. Редакция 3 закрыла перечень на четырёх и была неполна на пятой; редакция 4 обошла
шапки и нашла шестую. Что обход **не** нашёл, названо в §2.11 поимённо, чтобы следующий круг
не искал заново: шапка файла писателя, godoc `subjectRef`, `ownerOfBindingSubject`,
`readLiveBindings` — остаются верными, и по каждому сказано почему.

### §12.3 Чего редакция 4 НЕ делает

- **не трогает ни одного решения Р1, Р1а, Р2, Р4, Р6, Р7** и ни одного сценария, кроме `MRW-04`
  и `MRW-05` (дом — §2.13) и нового `MRW-23`. Р3 и Р5 получили по абзацу о соседях, а решений
  не сменили: «своё раньше доставленного» и «относим по паре, сравниваем по тройке» стоят как
  стояли;
- **не правит запись круга 1** и не правит строку «Статус» (§11.2 — довод тот же);
- **не заводит задач** — по условию задания; предметы, оставшиеся без владельца после двух
  соседей (§7, две последние строки), названы числом и предикатом, а не обещаны;
- **не переносит на себя вердикта круга 1 и не объявляет круга 2.** Отпечаток редакции 4 с
  записанным в круге 1 не совпадает, записи о редакциях 3 и 4 в дереве нет; одобрения у
  документа нет, санкции на объём §4 — тоже (ban #1).
