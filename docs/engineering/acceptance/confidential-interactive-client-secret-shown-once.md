<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка: конфиденциальный интерактивный клиент своего реестра получает секрет один раз

> **Статус:** DRAFT
> **Статическая форма:** DRAFT; действующий вердикт выводится из внешнего события и записи ревью
> (`change-graph.md` §2 «Вердикт привязан к ОТПЕЧАТКУ, а не к документу»). Шапка после ревью не правится.
> **История review:** (только дописывается строкой: дата · круг · вердикт · SHA-256 редакции · путь записи)
> 2026-09-26 · круг 1 · CHANGES_REQUESTED · `f1786b6a376cdec76ae8dfa10e4d13cbf133f0db444a088d6096ed13d2f8abac` ·
> `docs/specs/reviews/confidential-interactive-client-secret-shown-once/f1786b6a376cdec76ae8dfa10e4d13cbf133f0db444a088d6096ed13d2f8abac.yaml`
> **Дата:** 2026-09-26
> **Редакция:** 2 (круг 2). Документ правлен ПОСЛЕ вердикта круга 1 (CHANGES_REQUESTED на редакцию 1,
> отпечаток `f1786b6a…`); строка состояния не тронута; что и по какой находке изменено — §9.
> **Задача:** `PRO-Robotech/kaname#405` (волна-3 `#366`, эпик службы `#357`); эпик платформы `PRO-Robotech/kacho#2564`
> **Дом:** `PRO-Robotech/kaname`, `docs/engineering/acceptance/` — предмет внутри одной службы:
> исполнитель заведения, контракт ресурса, хранение проверочного значения и зачистка — всё в службе.
> Кросс-репо следствие одно — порядок пина края платформы (Р8), и оно названо, а не перенесено.
> **Опора, противоречить которой нельзя:** одобренная приёмка LINE-A-1,
> `PRO-Robotech/kacho-workspace:docs/specs/sub-phase-LINE-A-1-own-authorization-endpoint-and-code-acceptance.md`
> на ветке воркспейса `771` @ `PRO-Robotech/kacho-workspace@27eb0775` (так её называет тело `#405`; на
> стволе воркспейса @ `PRO-Robotech/kacho-workspace@6eddaa5e` документа ещё нет),
> редакция 3, sha256 `b755886fd3a9cbf4070598ad3d921bb92d53d7f4186832a2c34f1d8ca197a7ec` (APPROVED, круг 3,
> запись ревью — одноимённый каталог `docs/specs/reviews/` той же ветки, файл по тому же отпечатку):
> решение **Р3** и **строка 3** таблицы решений её §6 («интерактивный клиент — конфиденциальный; PKCE
> S256 обязателен вдобавок, не вместо»), сценарии **LINE-A-1-10** и **LINE-A-1-12**.
> **База сверки:** ветка волны `366` @ `fc9f5aff1` (она же вершина эпика `357`) — от неё ветвится `405`.
> Церемония `authorization_code` провязана в композиционном корне только на ветке сборки `425` @
> `685f8ad17` (запрос `#426` в `366`, открыт); факты о ней помечены «ветка 425». Ни одна из двух
> ревизий ещё не предок ствола службы, а гейт датировки судит вхождение относительно ствола
> (`internal/check/docs_measurement_dating_test.go`); предком ствола база станет вливанием каскада
> слиянием без схлопывания. До того ревизия названа этой строкой, и у каждого числа §1 стоит команда
> пересчёта на ней.
> **Формат:** Given-When-Then; кода не содержит.
>
> **Нормативка — по названию раздела, без пересказа:**
> - `api-conventions.md` — «Форма ресурса», «Error-format», «Принято-и-проигнорировано», «update_mask
>   discipline», «Пустое значение обязано означать «пусто»», «Поле контракта не выходит наружу, пока
>   КРАЙ не пересобран»
> - `security.md` — «Internal-vs-external (ban #6)», «Инфра-чувствительные данные»
> - `security-hardening.md` — «Hardening-инварианты» п. 1–2, «Части, исправные по отдельности, не
>   сходятся в целое»
> - `security-disclosure.md` — «Публичные артефакты»
> - `data-integrity.md` — «Within-service инварианты — ТОЛЬКО на DB-уровне», «Данные СТЕНДА заводятся
>   посевом, а не миграцией»
> - `00-kacho-core.md` — ban #1, #5, #6, #9, #10, #11, #12, #14, #15
> - прецедент одноразового секрета — приёмка BAT-1,
>   `PRO-Robotech/kacho-workspace:docs/specs/sub-phase-BAT-1-basic-access-token-acceptance.md`, §4.3
>   (четыре пути записи, «не попадает» против «стирается позже», пометка поля и перечень подметальщика)

---

## Обзор

Одобренная LINE-A-1 решила (Р3), что интерактивный клиент нашей церемонии **конфиденциальный** и
доказывает себя на обмене кода. Собственный реестр посадки `own` сегодня заводит клиента
**публичным** (способ `none`), и секрета ему не выдаёт: у ответа `Create` нет поля, которое могло
бы его нести. Поэтому положительная половина LINE-A-1-12 («верная клиентская аутентификация →
200») на клиенте, которого завёл продукт, невыполнима — предъявлять нечего, — а отрицательная
половина не наблюдается вовсе: публичный клиент на обмене себя не доказывает.

Эта приёмка задаёт наблюдаемый контракт того, чего не хватает: на посадке `own` заведение выдаёт
клиенту секрет **ровно один раз** — в ответе самого вызова `Create`; секрет не живёт больше нигде
(ни в строке операции, ни в строке ресурса, ни в журнале), его нет в `Get` и `List`; снятие клиента
не оставляет годного материала; выданный секрет проходит обмен кода — это и есть положительный
близнец LINE-A-1-12.

---

## §1 Перепись производителей — ДО сценариев

База — `366` @ `fc9f5aff1`, если не сказано «ветка 425». Команды — `git grep` по ревизии, их вывод
перемерен этой редакцией, а не взят из возврата полосы.

| # | что в дереве | координата | что оно делает сегодня | чем измерено |
|---|---|---|---|---|
| 1 | способ, который объявляет собственный исполнитель заведения | `internal/repo/kaname/pg/own_interactive_client_provider.go` (константа — строка 94, ответ `Register` — строка 145) | объявляет `none`; шапка файла называет публичность решением, принятым вопреки Р3 | `git grep -n ownPublicClientAuthMethod origin/366 -- internal/` → 3 строки |
| 2 | способ адаптера внешнего поставщика | `internal/clients/hydra_interactive_clients.go` (строка 126) | объявляет `none` | `git grep -n 'TokenEndpointAuthMethod: "none"' origin/366 -- internal/clients/hydra_interactive_clients.go` → 1 |
| 3 | словарь способов и связь с материалом | `internal/migrations/20260923230023_interactive_client_declares_how_it_authenticates.sql` | принимает `none`, `client_secret_basic`, `client_secret_post`; материал секрета лежит только у способа секретом; обратной связи «способ секретом ⟹ материал в строке» нет намеренно (клиент внешнего поставщика) | чтение миграции: ограничения `interactive_clients_auth_method_ck`, `interactive_clients_secret_verifier_method_ck` |
| 4 | колонка проверочного значения | `internal/migrations/20260920175118_interactive_client_carries_its_secret_verifier.sql` | argon2id разметкой PHC, момент установки стоит ровно при непустом значении; секрета в открытом виде не хранит | ограничения `interactive_clients_secret_verifier_form_ck`, `interactive_clients_secret_verifier_stamp_ck` |
| 5 | писатель проверочного значения | `internal/repo/kaname/pg/oauth_ceremony_repo.go` (`SetClientSecretVerifier`) | объявлен, прод-вызывающих **ноль**; единственный писатель строки `InteractiveClientRepo.Insert` (`internal/repo/kaname/pg/interactive_client_repo.go`) колонку не называет | `git grep -n SetClientSecretVerifier origin/366 -- '*.go' ':!*_test.go'` → 3 строки: объявление и два комментария |
| 6 | кто решает, публичен ли клиент для церемонии | ветка 425: internal/repo/kaname/pg/oauth_ceremony_vaults.go, `LookupClient` | публичность = способ строки равен `none`; публичный клиент на обмене себя не доказывает (`PRO-Robotech/corelib:oauthceremony/ports.go`, порт `ClientSecretVerifier`: «публичного клиента точки токена и отзыва не сверяют») | чтение файла на ветке 425 (`git show` по ревизии `685f8ad17`), присваивание признака публичности в `LookupClient` |
| 7 | сверка секрета на обмене | `internal/ceremonyport/client_secrets.go` (`ClientSecrets`) | есть; неизвестный клиент, клиент без секрета и неверный секрет дают один вердикт, вычисленный против приманки того же класса | чтение файла; провязка в корне — только ветка 425: cmd/kaname/ceremony.go (`NewClientSecrets`) |
| 8 | поле под секрет в контракте ресурса | `proto/kaname/cloud/iam/v1/interactive_client.proto`, `proto/kaname/cloud/iam/v1/internal_interactive_client_service.proto` | нет ни в запросе `Create`, ни в `InteractiveClient`; ответ операции `Create` — `InteractiveClient` | `git grep -n -i secret origin/366 -- proto/kaname/cloud/iam/v1/interactive_client.proto proto/kaname/cloud/iam/v1/internal_interactive_client_service.proto` → 0 |
| 9 | тело ответа, записываемое в строку операции | `internal/apps/kaname/api/interactive_client/usecases.go` (строка 290) | `Create` пишет в строку операции ровно то тело, что вернёт вызывающему: секрет, положенный в ответ без разделения, осел бы в строке | чтение: `MarkDone(ctx, op.ID, resp)` и `op.Response = resp` |
| 10 | образец «тело для строки без секрета, тело для вызывающего с ним» | `internal/apps/kaname/api/sa_keys/usecases.go` (`issueSecretSync`) | есть, для вида `SECRET` ключа служебной учётки | чтение функции |
| 11 | пометка поля-носителя и её читатели | `proto/kaname/cloud/iam/v1/secret_options.proto`; перечень подметальщика `cmd/kaname/secret_backstop.go`; гейт `TestBAT1_73_EverySecretBearingOperationResponseFieldIsInTheSweeperLedger` | помечено 7 полей; в перечне подметальщика 2 типа ответа (ключ служебной учётки, токен пользователя) | `git grep -n 'secret_bearing) = true' origin/366 -- proto \| wc -l` → 7; `git grep -n 'ResponseType: ' origin/366 -- cmd/kaname/secret_backstop.go` → 2 записи перечня |
| 12 | снятие материала при снятии клиента | `internal/repo/kaname/pg/own_interactive_client_provider.go` (`Deregister`) + удаление строки глаголом `Delete` | есть: строка уходит первой, снятие идемпотентно на её отсутствии; держит `TestIntegration_OwnInteractiveClientDeregistrationLeavesNoSecretBehind` — и та же проба закрепляет способ `none` | чтение пробы, `require.Equal(t, "none", …)` |
| 13 | аудит и лента подписки ресурса | `internal/apps/kaname/api/interactive_client/` | нет ни того, ни другого: путей записи у выдачи три — ответ, строка операции, строка ресурса — плюс журнал службы | `git grep -n -i audit origin/366 -- internal/apps/kaname/api/interactive_client/ \| wc -l` → 0 |
| 14 | читатели тела ответа операции `Create` | служба, Go: `internal/apps/kaname/api/interactive_client/name_canon_test.go` (строка 33); служба, сквозной набор: `tests/newman/cases/iam-interactive-client.py`; платформа: `PRO-Robotech/kacho:tests/authz-fixtures/prodseed_ceremony.py` | проба службы разбирает тело целиком как `InteractiveClient` — при смене типа ответа она переписывается тем же изменением; набор и посев читают `metadata.interactiveClientId` и затем `Get`, тела `response` не разбирают — только факт его наличия | Go: `git grep -n 'GetResponse().UnmarshalTo' origin/366 -- internal/apps/kaname/api/interactive_client/` → 1 (на ветке 425 — тот же 1); перепись шире — все файлы Go вне сгенерированного кода, которые разбирают `Any` (`UnmarshalTo`, `UnmarshalNew`) и называют `InteractiveClient`, — тот же один файл на обеих ревизиях; newman: `grep -n 'j\.response' tests/newman/cases/iam-interactive-client.py tests/newman/scripts/gen.py` → 1 (проверка наличия); в `prodseed_ceremony.py` обращений к `response` 0 |
| 15 | край платформы, проксирующий ресурс | `PRO-Robotech/kacho:gateway/internal/restmux/mux.go` (`RegisterInternalInteractiveClientServiceHandlerFromEndpoint`) при пине службы `v0.4.1-0.20260917085954-16b5cadead2a` | переводит ответ в JSON своим реестром типов: тип ответа, которого нет в пине, край не разберёт | чтение `PRO-Robotech/kacho:gateway/internal/restmux/mux.go` на `origin/main` платформы @ `1d42a6728bf` (строки 829–840) и строки пина службы в `PRO-Robotech/kacho:go.mod` (строка 120) |
| 16 | ссылки посева на `#405` как на отсутствующего производителя | ветка 425: `.github/scripts/newman-suite-debt.py`, tests/newman/cases/kaname-authorization-code.py, tests/newman/environments/local.postman_environment.template.json | 8 строк в 3 файлах: «производителя конфиденциального клиента у продукта нет (kaname#405), поэтому это посев» | `git grep -n 'kaname#405' origin/425 -- tests .github \| wc -l` → 8; из них называют производителя отсутствующим — все 8: `git grep -n -e 'нет (kaname#405)' -e 'ПУБЛИЧНЫМ (kaname#405)' origin/425 -- tests .github \| wc -l` → 8, на `origin/366` → 0 |
| 17 | посадка стенда | `deploy/values.prod.yaml` | `authn.identityProvider: external`; посадка `own` поднимается заданием `chart-own` | чтение values |
| 18 | как набор церемонии предъявляет «секрета нет» | ветка 425: tests/newman/cases/kaname-authorization-code.py, случай IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED | шаг exchange-without-client-secret шлёт форму без заголовка `Authorization` и с полем `client_id` клиента, ждёт **401** / `invalid_client`; его близнец exchange-fresh-right-secret — заголовок Basic верной пары | `git grep -n 'body_client_id=True' origin/425 -- tests \| wc -l` → 1 (единственное попадание — этот шаг); на `origin/366` → 0 |
| 19 | кто различает «клиент не назван» и «клиент не доказал себя» | движок церемонии фундамента `PRO-Robotech/corelib:internal/oauth2/client_authentication.go` при пине `v1.10.0-rc.2` (`clientCredentialsFromRequest`, `clientCredentialsFromRequestBody`) | нет ни заголовка Basic, ни поля `client_id` — отказ формы `invalid_request`; клиент назван полем формы, секрета нет — сверка секрета конфиденциального клиента и `invalid_client` | чтение обеих функций в кэше модулей; исходы подтверждены черновой пробой круга 1 над `Ceremony.Exchange` (запись ревью, находка 1) |

**Посылка задания — подтверждена по признаку, область уточнена.** Признак (реестр `own` заводит
клиента публичным вопреки Р3) подтверждён строками 1, 3, 6. Красная проба полосы — TestOwnInteractiveClientRegistrationDeclaresAConfidentialClient в
`internal/repo/kaname/pg/` на ветке задачи `405` (на origin не отправлена) — названа прозой: в
дереве этой ветки её нет. Уточнение: возврат полосы
ссылался на «LINE-A-1 §7 п.1», по тексту утверждение «регистрация клиента в контракте не меняется»
стоит в §10 п.1 (строки 740–741 редакции `b755886f`); отношение к нему — Р7.

---

## §2 Решения (ратифицировать явно — ревьюер проверяет каждое)

**Р1. На посадке `own` клиент конфиденциален: способ — `client_secret_basic`.** Основание — Р3 и
строка 3 §6 LINE-A-1. Из двух способов секретом, которые знает схема (§1 строка 3), взят тот, что
RFC 6749 §2.3.1 обязывает поддерживать всякий сервер авторизации (`client_secret_post` там же назван
не рекомендуемым), и тот, которым предъявляет секрет набор церемонии на ветке 425. Подписанное
утверждение, которое Р3 допускает («либо»), не берётся: церемония фундамента его отвергает, а у
строки нет колонки ключа (§3). Доводы шапки исполнителя заведения против конфиденциальности
разобраны так: (1) «секрет в браузерном коде не секрет» верно для клиента, обмен которого идёт в
браузере, — из решения владельца Р3 следует, что обмен конфиденциального клиента идёт на серверной
стороне потребителя, а перевод консоли на нашу церемонию — LINE-A-2 (§3); (2) PKCE по Р3 обязателен
**вдобавок**, а не вместо; (3) расхождение посадок по выходному полю способа есть не молчаливая
разница, а ровно то, что поле и называет (Р4, IC-SECRET-11).

**Р2. Секрет чеканит служба; поля для него во входе нет.** Запрос `Create` поля секрета не несёт, и
это сказано в комментарии сообщения запроса — так же, как там уже сказано об отсутствии `audiences`
(величина, назначаемая выдачей, во входе не принимается). Стойкость — не меньше **128 бит** из
криптографически стойкого источника: паритет с базовым удостоверением платформы
(`PRO-Robotech/corelib:credsecret/credsecret.go`, 130 бит) и с полом `state` LINE-A-1 Р13 (128 бит);
форма записи — предмет замысла. Секрет **невосстановим**: утрата ответа означает снятие клиента и
новое заведение (новый `clientId`), и это названная цена, а не пробел (§3).

**Р3. Секрет существует ровно в одном месте — в ответе самого вызова `Create`.** Заведение
завершается на пути запроса (как сегодня, §1 строка 9: `done = true` в ответе вызова; ban #9 не
нарушен — предмет закоммичен). Путей записи у выдачи три, и по каждому названо, что верно:

| путь записи | что обязано быть верно |
|---|---|
| строка ресурса | несёт проверочное значение argon2id (§1 строка 4), секрета — нет |
| строка операции (тело ответа) | поля секрета **не несёт ни в какой момент**: в строку пишется тело без секрета, вызывающему отдаётся тело с ним (образец — §1 строка 10) |
| журнал службы | секрета не несёт; корреляция — по `clientId` и `id` ресурса |

Форма «не попадает» взята вместо «стирается позже» по доводу BAT-1 §4.3.1: секрет долгоживущий, и
отложенное затирание превратило бы «показан один раз» в «показан ещё столько-то». Перечень
подметальщика называет поле **бэкстопом** против будущего второго пути записи (BAT-1 §4.3.2).

**Р4. Носитель — отдельное сообщение ответа `Create`; у ресурса поля под секрет нет вовсе.** Ответ
операции `Create` становится `CreateInteractiveClientResponse` из двух полей: `interactiveClient`
(ресурс, та же проекция, что `Get`) и `clientSecret`, помеченное `(kaname.cloud.iam.v1.secret_bearing)
= true`. Сообщение `InteractiveClient` поля секрета **не получает**, поэтому `Get`, `List`, `Update` и
`Delete` секрета не несут **по форме контракта**, а не по дисциплине проекции. Форма — паритет с
`IssueSAKeyResponse` (`proto/kaname/cloud/iam/v1/sa_key_service.proto`: ресурс плюс поля-носители).
Смысл пустого значения назван в комментарии поля (исход 3 правила «Пустое значение обязано означать
«пусто»»): `clientSecret` непуст **ровно** в ответе вызова `Create` клиента со способом секретом; в
ответе вызова клиента со способом `none` пуст — секрета у клиента нет; в ответе операции, прочитанном
`OperationService.Get`, пуст всегда — показ один раз. Признак «секрет у клиента есть» один —
`interactiveClient.tokenEndpointAuthMethod`, а не пустота поля. Смена типа ответа операции `Create`
объявляется ломающим изменением в коммите контракта. Разбирает тело ответа как ресурс ровно один
читатель — проба службы `internal/apps/kaname/api/interactive_client/name_canon_test.go` (§1
строка 14), и она переписывается под новый тип тем же изменением; сквозной набор службы и посев
платформы тела не разбирают.

**Р5. Проверочное значение кладётся тем же оператором вставки, что и строка клиента.** У строки
один писатель (§1 строка 5), и второго не заводится: отдельный вызов `SetClientSecretVerifier`
после вставки открыл бы окно «клиент со способом секретом есть, предъявить нечего», а при сбое между
двумя операторами — клиента, которого нельзя доказать никогда. Несостоявшаяся вставка (имя занято,
гонка) не оставляет ни строки, ни проверочного значения, и секрета никому не выдаёт: вызов отвечает
отказом синхронно, операции не возвращая, а строка его операции помечается ошибкой без `response`
(`internal/apps/kaname/api/interactive_client/usecases.go`, ветка отказа `Create`). Схема этого
не держит — строку со способом секретом без материала она принимает (так показал посев ревью
круга 1), — поэтому у одноместности записи свой держатель (IC-SECRET-03, гейт G).

**Р6. Снятие клиента не оставляет годного материала.** Материал живёт только колонкой строки
ресурса (Р3), строку уносит глагол `Delete`, исполнитель снятия идемпотентен на её отсутствии (§1
строка 12). После снятия прежняя пара (`clientId`, секрет) на обмене получает **тот же** отказ, что
неверный секрет и никогда не заводившийся `clientId` (§1 строка 7) — снятый клиент неотличим от
незаведённого.

**Р7. Отношение к LINE-A-1 — опора, а не правка.** Эта приёмка LINE-A-1 не редактирует и ни одного
её сценария не переутверждает. §10 п.1 LINE-A-1 («регистрация клиента в контракте не меняется
(регистрация у нас — LINE-A-2)») ограничивает дельту **самой** LINE-A-1; строка §4 LINE-A-1 относит
«перевод регистрации интерактивного клиента с провайдера к нам» к LINE-A-2. Здесь регистрация не
переводится: исполнитель заведения посадки `own` уже в дереве (задача `#313`, §1 строка 1), и
предмет — дать ему удостоверение, которого требует Р3. Приёмки LINE-A-2 нет ни на одной ветке
воркспейса (история путей docs/specs/*LINE-A-2* по всем ссылкам воркспейса пуста — `git log --all --oneline`
с этим образцом пути ничего не печатает), поэтому её объём не
затрагивается ни словом: снятие поверхности поставщика, перевод волны проб и посадка `own`-только
остаются за ней. «Given, сконструированный посевом» LINE-A-1 §1 остаётся законным; после посадки
этого предмета у того же Given появляется производитель-глагол.

**Р8. Порядок посадки края: сначала край знает тип, потом служба его отдаёт.** Край платформы
переводит ответ операции в JSON своим реестром типов (§1 строка 15). Ответ `Create` нового типа,
пришедший на край с прежним пином службы, край не разберёт — глагол сломается на REST, будучи
исправным в gRPC. Поэтому: (1) сообщение ответа посажено в контракт службы и край платформы
перепинен на ревизию, где оно есть (расширение, ничего не меняющее: служба ещё отвечает прежним
типом); (2) затем служба отвечает новым типом. Это пункт DoD, а не рекомендация
(`api-conventions.md` «Поле контракта не выходит наружу, пока КРАЙ не пересобран»).

---

## §3 Что НЕ входит (явно, каждое с причиной и местом)

| вне объёма | почему | где живёт / условие возврата |
|---|---|---|
| конфиденциальность клиентов посадки `external` | их церемонию проводит внешний поставщик, а не наш токен-эндпоинт; Р3 и LINE-A-1-12 — о нашем обмене. Поведение не меняется: способ `none`, секрет пуст (IC-SECRET-11) | путь поставщика замещается Линией A и снимается LINE-A-2 / `kacho#2564` |
| перевод регистрации к нам, снятие поверхности поставщика, перевод волны проб, посадка `own`-только | объём LINE-A-2 (LINE-A-1 §3, §4) | LINE-A-2; её приёмки нет |
| серверная половина обмена у консоли и CLI | конфиденциальный клиент не хранит секрет в браузере; как потребители проводят обмен — предмет их перевода на нашу церемонию | LINE-A-2 (перевод консоли); здесь названо как следствие Р3 |
| смена секрета без смены `clientId` (ротация) | глагола нет; утрата и компрометация закрываются снятием и новым заведением (Р2) | отдельный предмет службы; условие возврата — потребитель, не способный сменить `clientId` вместе с секретом |
| закрепление способа за записью клиента на обмене | церемония фундамента способ за записью не закрепляет (`PRO-Robotech/corelib:oauthceremony/translate.go`, абзац «Представления клиента С ЯВНЫМ СПОСОБОМ ДОКАЗАТЕЛЬСТВА … НЕТ»): секрет, предъявленный телом формы, тоже будет сверен. Объявленный способ говорит клиенту, как предъявлять, и им предъявляют пробы этой приёмки | предмет церемонии (LINE-A-1 / фундамент) |
| обмен, не называющий клиента вовсе: нет ни заголовка `Authorization`, ни поля `client_id` | это отказ формы до аутентификации клиента, а не половина LINE-A-1-12: движок фундамента не находит имени клиента ни в заголовке, ни в теле и отвечает **400** / `invalid_request` (§1 строка 19); к заведению и секрету отношения не имеет. Отрицательные ветки 06 поэтому называют клиента всегда | предмет церемонии — LINE-A-1, её отказы формы до того, как назван код (LINE-A-1-24) |
| подписанное утверждение клиента (`private_key_jwt`) | церемония фундамента утверждения клиента отвергает; колонки ключа у строки нет (§1 строка 3) | предмет, заводимый вместе с колонкой материала |
| цена сверки секрета на обмене (argon2id на каждое доказательство) и ёмкость проверяющего | свойство церемонии и проверяющего (§1 строка 7), а не заведения | LINE-A-1 |
| судьба уже выданных по клиенту токенов при его снятии | открытый вопрос 3 IAM-INT-1 («что наступает раньше»); здесь утверждается только секрет клиента | IAM-INT-1 §6 вопрос 3 |
| политика неизвестных ключей тела REST (фронт отбрасывает их молча) | свойство фронта всей платформы; поля секрета во входе нет (Р2), поэтому принятого-и-проигнорированного поля контракта здесь не возникает | фронт |
| шапка применённой миграции `20260923230023_…` («оба производителя объявляют none») | после посадки описывает прошлое; применённая миграция не правится (ban #5). Правду держат комментарии колонок и шапка исполнителя, которая переписывается (DoD п.3) | — |
| ось 2 гейта носителей (имя похоже на секрет ⇒ поле помечено) | живёт в дереве платформы и видит контракт службы через пин | `PRO-Robotech/kacho:TestBAT1_73_Axis2_NoUnmarkedSecretOnTheSurface`, при следующем пине службы |

Ни одна строка не делает существующий глагол недостижимым: посадка `external` не меняется,
на посадке `own` каждый глагол ресурса остаётся исполнимым.

---

## §4 Сценарии (Given-When-Then)

ID `IC-SECRET-NN` трассируется в имена integration-, newman- и гейтовых проб. Уровни: **I** —
integration (testcontainers, применённые миграции, композиционный корень посадки `own`); **E** —
сквозной на стенде; **G** — гейт по дереву. Отказ утверждает **пару** — HTTP-статус и `code`
`google.rpc.Status` (`api-conventions.md` «gRPC-код → HTTP-статус»).

**Общие Given — все конструируются посевом, ни одно не берётся со стенда на веру:**
- *посадка `own`* — I: композиционный корень выбирает собственный реестр (как
  `TestCompositionRoot_InteractiveClientCreateHasAnExecutorUnderOwnPosture`); E: стенд задания
  `chart-own`;
- *вызывающий* — машинный принципал `system_admin` на внутреннем листенере (посев учётки бутстрапа;
  пол `"2"` у `Create`/`Update`/`Delete` снимает только машинный принципал — довод IAM-INT-1, здесь
  не повторяется);
- *адрес возврата R* — `https://api.kacho.local/auth/callback`, то же значение, что `GOOD_REDIRECT`
  набора `tests/newman/cases/iam-interactive-client.py`; K заводится с `redirectUris = [R]` (01),
  то есть R — это `redirectUris[0]` заведения 01;
- *клиент K и секрет S* — K заведён глаголом `Create` сценария 01 в том же прогоне; S — значение
  `response.clientSecret` из ответа того вызова, сохранённое пробой как секретная величина (в отчёт
  прогона не уезжает);
- *код для K* — выдан точкой авторизации по LINE-A-1-02 для K с `redirect_uri = R`; сессия человека —
  посев ответа авторитета входа (I) либо вход полосы входа (E), как LINE-A-1 Р2; V — его
  `code_verifier`.

### Группа A — выдача на заведении

**ID: IC-SECRET-01** · I + E — заведение на посадке `own` выдаёт секрет в ответе `Create`
**Given** посадка `own`; вызывающий
**When** `POST /iam/v1/internal/interactiveClients` с payload:
  - `name` = `ic-secret-{{runId}}`
  - `redirectUris` = `[R]`
**Then** ответ — `Operation` с `done = true` и без `error`
**And** `response` — сообщение `CreateInteractiveClientResponse` (Р4)
**And** `response.interactiveClient`: `id` формы `ic-<17 знаков крокфорда>`, `clientId` непуст и
несёт приставку `oic-`, `tokenEndpointAuthMethod = client_secret_basic` (Р1), `grantTypes =
["authorization_code", "refresh_token"]`, `status = ACTIVE`, `createdAt` усечён до секунд
**And** `response.clientSecret` непуст
**And** последующий `GET /iam/v1/internal/interactiveClients/{id}` отдаёт те же `id`, `clientId`,
`tokenEndpointAuthMethod` — ответ описывает записанную строку
**And** это положительный контроль для 02, 03, 08, 09: без него их «секрета нет» зеленели бы на
заведении, которое секрета не выдало вовсе

**ID: IC-SECRET-02** · I — секрет не попадает в строку операции ни в какой момент (близнец 01)
**Given** K и S из 01; `id` операции того `Create`
**When** `OperationService.Get(id)` и строка `kaname.operations` этой операции читаются в пределах той
же секунды после ответа `Create` — раньше любого окна затирания, объявленного в дереве (120 с у выдачи
ключа служебной учётки и токена пользователя)
**Then** `response.interactiveClient` совпадает с пришедшим в ответе вызова (01)
**And** `response.clientSecret` пуст
**And** тело ответа в строке операции не содержит S ни как подстроку
**And** положительный контроль: то же тело содержит `id` клиента K — иначе «секрета нет» значило бы
«ответа нет»
**And** момент чтения назван в пробе числом: проба обязана краснеть, если тело кладётся в строку с
секретом и стирается позже, — это её предмет (паритет BAT-1-22), а не побочный случай
**And** близнец — 01: ответ самого вызова секрет несёт; отличие в одном факте — читается ответ вызова
либо операция

**ID: IC-SECRET-03** · I + G — строка реестра несёт проверочное значение этого секрета, а не секрет (близнец 01)
**Given** K и S из 01
**When** строка `kaname.interactive_clients` клиента K читается целиком сразу после ответа `Create`
**Then** ни одна колонка не содержит S ни как подстроку
**And** `secret_verifier` непуст и имеет форму argon2id PHC, `secret_verifier_set_at` задан
**And** положительный контроль: порт сверки (`internal/ceremonyport/client_secrets.go`) на S отвечает
«совпал», на S с одним изменённым знаком — «не совпал»: лежит проверочное значение **именно** этого
секрета
**And** наблюдаемое утверждение — о моменте ответа и после него: строка K несёт проверочное значение
уже при чтении сразу после ответа `Create` (второе And) и несёт его в каждом чтении проб 06–09, до
снятия (10)
**And** держатель одноместности записи (Р5) — гейт по дереву (G), потому что изнутри транзакции она не
видна, а схема строку со способом секретом без материала принимает: на пути заведения проверочное
значение и момент его установки кладёт тот же оператор вставки, что строку, и второго вызова записи
материала на этом пути нет. Гейт печатает объём осмотренного, краснеет на инъекции «вставка строки,
затем отдельная запись материала» с координатой вызова и молчит на дереве посадки

**ID: IC-SECRET-04** · I + E — несостоявшееся заведение секрета не выдаёт и материала не оставляет (близнец 01)
**Given** K заведён с `name = N` (01); S — его секрет
**When** второй `Create` с тем же `name = N`, прочие поля — как в 01
**Then** **409** / `ALREADY_EXISTS` синхронно: вызов отвечает отказом и операции не возвращает —
`clientSecret` в ответе нет ни в каком виде
**And** `List` с `filter = name="N"` отдаёт ровно одного клиента — K
**And** I: строка `kaname.operations`, заведённая этим вызовом (её описание называет имя N, а
метаданные — заранее выделенный `id`, отличный от `id` K), несёт `error` и не несёт `response`; в
реестре одна строка с именем N и одно проверочное значение — K
**And** S по-прежнему проходит обмен (06): отказ второго заведения не тронул материала первого
**And** близнец — 01 с новым именем; отличие в одном факте — имя занято

**ID: IC-SECRET-05** · I — конкурентные заведения одного имени: одна строка, один секрет (близнец: разные имена)
**Given** посадка `own`; вызывающий
**When** два `Create` с одним `name` доходят до вставки одновременно
**Then** ровно один завершается с `response` и непустым `clientSecret`; второй отвечает
`ALREADY_EXISTS` синхронно, операции не возвращая, и строка его операции несёт `error` без `response`
**And** в реестре одна строка этого имени, и её проверочное значение сверяется с секретом победителя
**And** исход держат уникальность имени на уровне базы и вставка строки вместе с проверочным значением
одним оператором (Р5), а не проверка-перед-вставкой (ban #10)
**And** близнец: два одновременных `Create` с **разными** именами оба завершаются с секретом, и
секреты различны

### Группа B — предъявление на обмене кода

**ID: IC-SECRET-06** · I + E — секрет из ответа `Create` проходит обмен: положительный близнец LINE-A-1-12
**Given** K и S из 01; код для K; V
**When** клиент обращается `POST /iam/v1/token` на поверхности выдачи:
  - `grant_type = authorization_code`
  - `code = <код>`
  - `redirect_uri = R`
  - `code_verifier = V`
  - заголовок `Authorization: Basic` от пары (`clientId` K, S) по RFC 6749 §2.3.1
**Then** **200** и предъявитель, как в LINE-A-1-10 (форма и утверждения предъявителя — предмет
LINE-A-1-10, здесь не переутверждаются)
**And** отрицательные близнецы — LINE-A-1-12 на **том же** клиенте. В каждой ветке клиент назван —
`clientId` K, — и меняется ровно доказательство:
  - (а) *доказательство не предъявлено*: заголовка `Authorization` нет; в форме, вдобавок к полям
    06, `client_id = clientId K` (RFC 6749 §3.2.1: клиент вправе назвать себя полем тела); секрета
    нет ни в заголовке, ни в форме → **401** / `invalid_client`. Имя клиента то же, что в 06: там его
    несёт заголовок вместе с доказательством, здесь — поле формы без доказательства; отличие ровно в
    одном факте — предъявлено ли доказательство (так же шлёт этот случай набор ветки 425, §1 строка 18)
  - (б) *доказательство неверно*: заголовок `Authorization: Basic` от пары (`clientId` K, S′), где в
    S′ изменён один знак S → **401** / `invalid_client`; отличие ровно в одном факте — значение
    секрета
**And** ветки, где клиент не назван вовсе (нет ни заголовка, ни `client_id`), здесь нет: это отказ
формы **400** / `invalid_request`, а не половина LINE-A-1-12 (§3, §1 строка 19)
**And** до этого предмета отрицательная половина на клиенте, заведённом глаголом, не наблюдалась:
клиент со способом `none` для церемонии публичен и себя не доказывает (§1 строка 6). После — обе
половины LINE-A-1-12 исполнимы на клиенте, которого завёл продукт, а не посев

**ID: IC-SECRET-07** · I — секрет связан со своим клиентом (близнец 06)
**Given** K1 и K2 из двух заведений по 01, их секреты S1 и S2; код для K1
**When** обмен кода K1 с `Authorization: Basic` от пары (`clientId` K1, S2)
**Then** **401** / `invalid_client`, побайтово тот же ответ, что на неверный секрет в LINE-A-1-12
**And** S1 ≠ S2 — два заведения не выдают одного секрета
**And** близнец: пара (`clientId` K1, S1) → **200** (06); отличие в одном факте — чей секрет предъявлен

### Группа C — секрета нет в чтениях и прочих ответах

**ID: IC-SECRET-08** · I + E — `Get` секрета не несёт (близнец 06)
**Given** K и S из 01
**When** `GET /iam/v1/internal/interactiveClients/{id K}`
**Then** **200**; ресурс с `tokenEndpointAuthMethod = client_secret_basic` и `clientId` K
**And** тело ответа не содержит S ни как подстроку; поля под секрет или проверочное значение у
сообщения ресурса нет (Р4)
**And** сверка — **по значению** S, а не образцом имени: образец `client_secret` совпадает с законным
значением способа `client_secret_basic` и краснел бы на верном ответе (так устроена нынешняя проверка
посадки `external` в `tests/newman/cases/iam-interactive-client.py` — там способ `none`, и образец
законен только там)
**And** положительный контроль: в теле есть `id` и `clientId` K, и S в тот же момент проходит обмен
(06) — секрет существует, его нет в ответе

**ID: IC-SECRET-09** · I + E — `List` и `Update` секрета не несут; маской его не задать и способ не переключить (близнец 08)
**Given** K и S из 01
**When** (а) `GET /iam/v1/internal/interactiveClients` с `filter = name="<имя K>"`; (б) `PATCH
…/{id K}` с `updateMask = description` и новым `description`; (в) `PATCH` с `updateMask =
clientSecret`; (г) `PATCH` с `updateMask = tokenEndpointAuthMethod`
**Then** (а) страница содержит K в той же проекции, что `Get` (08), и во всей странице нет S
**And** (б) операция `done` без `error`; `response` — ресурс с новым `description` и без S; S
по-прежнему проходит обмен (06)
**And** (в) **400** / `INVALID_ARGUMENT`; сообщение статуса — `invalid argument`, а текст `unknown
field in update_mask: client_secret` стоит в деталях `BadRequest`: `fieldViolations[0]` с `field =
update_mask` и этим `description` (`PRO-Robotech/corelib:validate/validate.go`, `UpdateMask`) — тем
же тоном, что любое неизвестное поле маски; проба судит текст по месту, а не по `message`; секрет не
меняется
**And** (г) **400** / `INVALID_ARGUMENT`; текст `token_endpoint_auth_method is immutable after
InteractiveClient.Create` стоит и сообщением статуса, и `description` нарушения поля `update_mask`
(`internal/apps/kaname/shared/errdetails.go`, `InvalidArg`) — способ нельзя перевести в `none` после
заведения
**And** близнец для (в) и (г) — (б): маска известного изменяемого поля проходит; отличие в одном факте
— путь маски

### Группа D — снятие клиента

**ID: IC-SECRET-10** · I + E — снятие не оставляет годного секрета (близнец 06)
**Given** K и S из 01; S проходит обмен (06); код для K, выданный до снятия
**When** `DELETE /iam/v1/internal/interactiveClients/{id K}`, операция доходит до `done` без `error`
**Then** `response` — эхо снятого ресурса без S
**And** обмен кода с парой (`clientId` K, S) → **401** / `invalid_client`, побайтово тот же ответ, что на
неверный секрет и что на `clientId`, которого служба не заводила никогда (Р6)
**And** `GET` по `id` K → **404** / `NOT_FOUND` `InteractiveClient <id> not found`
**And** I: строки с `clientId` K в реестре нет; справочник проверочных значений на `clientId` K отвечает
«клиента нет»; повторный `DELETE` даёт тот же исход, что первый (идемпотентность IAM-INT-1-09)
**And** близнец: тот же обмен до снятия → **200** (06); отличие в одном факте — клиент снят
**And** проба ставит вопрос сквозь обе стороны — сняли клиента → предъявили прежний секрет → получили
отказ, — а не двумя пробами по половине (`security-hardening.md` «Части, исправные по отдельности, не
сходятся в целое»)

### Группа E — посадка `external` и форма ответа

**ID: IC-SECRET-11** · E — на посадке `external` секрета нет, и отсутствие сказано способом (близнец 01)
**Given** стенд посадки `external` — объявлена значением `authn.identityProvider: external` в
`deploy/values.prod.yaml`
**When** `Create` с payload из 01
**Then** операция `done` без `error`; `response` — `CreateInteractiveClientResponse`;
`response.interactiveClient.tokenEndpointAuthMethod = none`; `response.clientSecret` пуст
**And** `Get` ресурса — как сегодня (IAM-IC-CR-CRUD-OK: способ `none`, секрета в ответе нет)
**And** пара с 01 и есть правило поля (Р4): `clientSecret` непуст тогда и только тогда, когда способ —
секретом; пустота в ответе вызова означает «секрета у клиента нет», и говорит это способ, а не пустота
**And** близнец — 01; отличие в одном факте — посадка

### Группа F — сопровождение секрета

**ID: IC-SECRET-12** · G + I — поле-носитель помечено, и подметальщик его знает (паритет BAT-1-73)
**Given** контракт, где `clientSecret` сообщения `CreateInteractiveClientResponse` помечен
`secret_bearing`, а аннотация операции `Create` называет это сообщение ответом
**When** исполняется гейт `TestBAT1_73_EverySecretBearingOperationResponseFieldIsInTheSweeperLedger`
**Then** пара (тип `CreateInteractiveClientResponse`, поле `client_secret`) стоит в перечне
подметальщика `cmd/kaname/secret_backstop.go`, и гейт зелен
**And** на дереве без этой записи при живой пометке гейт красен с именем типа и поля — прогон в обе
стороны, а не объявление
**And** законный близнец: непомеченное поле того же сообщения (`interactive_client`) в перечне не стоит,
и гейт молчит
**And** I: строка операции `Create`, в тело которой секрет положен в обход конструкции 02, подметается
бэкстопом — перечень не только объявлен, но и действует
**And** предел назван: поле-носитель, которое не пометили, эта ось не видит; его ловит ось 2 в дереве
платформы (§3)

**ID: IC-SECRET-13** · I — секрет не пишется в журнал службы, а внутренний отказ заведения его не эхает (близнецы: строка исхода обмена в журнале есть; отказ словаря — 04)
**Given** (а) захват журнала службы на прогон сценариев 01–10; (б) глагол `Create` посадки `own`
над настоящим реестром, у которого подменён только оператор вставки строки: он отказывает ошибкой
**вне словаря отказов** — без признака «не найдено / занято / недоступно / неверный вход» и без
статуса gRPC, с текстом-маркером M
**When** (а) захват читается целиком; (б) `Create` с payload из 01 и новым именем N′
**Then** (а) ни одна строка не содержит S ни как подстроку — ни на заведении, ни на отказе 04, ни на
обмене 06/07, ни на снятии 10 (`security-hardening.md` «Hardening-инварианты» п. 2)
**And** положительный контроль (а): в захвате есть строка исхода обмена 06 с `clientId` K (LINE-A-1-24 —
запись журнала на каждый исход обмена); иначе «секрета нет» значило бы «журнала нет»
**And** (б) **500** / `INTERNAL` с фиксированным текстом `internal error` (ветка по умолчанию
отображения отказов, `internal/apps/kaname/shared/errors.go`, `MapRepoErr`): ответ синхронный, не несёт
ни M, ни поля `clientSecret`; строки с именем N′ в реестре нет, проверочного значения — тоже (п. 1; Р5)
**And** близнец (б): та же подмена, отказывающая признаком «имя занято», → **409** / `ALREADY_EXISTS`
с текстом этой полосы (04); отличие в одном факте — вид отказа хранилища

---

## §5 Сценарий → производитель

| ID | что производит «Тогда» | производитель и координата | состояние на базе | исход |
|---|---|---|---|---|
| 01 | секрет в ответе `Create`, способ `client_secret_basic` | исполнитель заведения `own` (`internal/repo/kaname/pg/own_interactive_client_provider.go`), глагол `Create` (`internal/apps/kaname/api/interactive_client/usecases.go`), сообщение ответа (контракт) | исполнитель есть и объявляет `none`; чеканки секрета и сообщения ответа нет (§1 строки 1, 8) | заказан производитель — реализация `#405` после APPROVED |
| 02 | тело строки операции без секрета | разделение «записанное тело / показанное тело» в глаголе `Create`; образец — `internal/apps/kaname/api/sa_keys/usecases.go` | образец есть; у `Create` одно тело на оба (§1 строки 9, 10) | заказан |
| 03 | проверочное значение этого секрета в строке | вставка `InteractiveClientRepo.Insert` (`internal/repo/kaname/pg/interactive_client_repo.go`); хешер `internal/passwordverify/hasher.go`; форма колонки — миграция `20260920175118_…`; порт сверки `internal/ceremonyport/client_secrets.go` | хешер, форма, порт — есть; вставка колонку не называет (§1 строки 4, 5); гейта одноместности записи нет | вставка и гейт G заказаны — реализация `#405`; прочее посажено |
| 04, 05 | отказ без секрета, одна строка | уникальность имени в базе и путь ошибки `Create` (есть) + вставка вместе с проверочным значением (Р5) | уникальность и путь ошибки — есть | вставка заказана |
| 06, 07 | обмен с выданным секретом → 200; непредъявленный (клиент назван полем формы), искажённый и чужой → 401 | провязка церемонии (ветка 425: cmd/kaname/ceremony.go), публичность по способу (ветка 425: `LookupClient`), различение «не назван / не доказал» движком фундамента (§1 строка 19), сверка `ClientSecrets` (есть на базе) + способ секретом у исполнителя (01) | провязка — только на ветке 425 (запрос `#426` открыт) | способ — заказан; провязка — предмет `#423`, приезжает сведением с `425` |
| 08, 09 | `Get`/`List`/`Update` без секрета; маска | проекция `internal/apps/kaname/api/interactive_client/mapping.go` (полей секрета нет) и дисциплина маски в `usecases.go` (изменяемые и неизменяемые поля перечислены) | есть | держится формой сообщения ресурса (Р4); положительный контроль зависит от 01 и 06 |
| 10 | после снятия нет строки, нет материала, отказ неотличим | глагол `Delete` + `Deregister` (`internal/repo/kaname/pg/own_interactive_client_provider.go`) + приманка порта сверки | есть; `TestIntegration_OwnInteractiveClientDeregistrationLeavesNoSecretBehind` закрепляет при этом способ `none` | посажено; проба переписывается под способ секретом (DoD п.3) |
| 11 | `external`: способ `none`, секрет пуст | адаптер `internal/clients/hydra_interactive_clients.go` (есть) + сообщение ответа (01) | адаптер есть | сообщение заказано |
| 12 | поле в перечне подметальщика | опция `proto/kaname/cloud/iam/v1/secret_options.proto`, перечень `cmd/kaname/secret_backstop.go`, гейт `TestBAT1_73_EverySecretBearingOperationResponseFieldIsInTheSweeperLedger` | опция, перечень и гейт есть; записи для нового типа нет | запись заказана |
| 13 | (а) секрета нет в журнале; строка исхода обмена есть; (б) внутренний отказ заведения — `INTERNAL` фиксированным текстом, без строки и материала | (а) журнал заведения и обмена; запись исхода обмена — LINE-A-1-24 (ветка 425); (б) ветка отказа `Create` (`internal/apps/kaname/api/interactive_client/usecases.go`) и отображение отказов `internal/apps/kaname/shared/errors.go` (`MapRepoErr`) | (а) положительный контроль — только на ветке 425; (б) ветка отказа и фиксированный текст есть на базе | (а) приезжает сведением с `425`; (б) ветка отказа и текст посажены; «ни строки, ни материала» держит вставка вместе с проверочным значением (Р5) — заказана |

Ни одно «Тогда» не переписано под существующее и ни одно не снято: это приёмка до кода (ban #1), и
отсутствие производителя здесь ожидаемо. Производители, стоящие только на ветке 425, названы как
условие сведения, а не как посаженные.

---

## §6 Инварианты (MUST hold — для ревьюера)

1. **Конфиденциальность на `own` — по Р3 LINE-A-1**, способ `client_secret_basic` (Р1); PKCE
   остаётся обязательным вдобавок (LINE-A-1 Р3, её сценарии не меняются).
2. **Секрет чеканит служба; во входе поля нет** (Р2).
3. **Секрет существует ровно в одном месте — ответе вызова `Create`**; строка операции не несёт его
   ни в какой момент, строка ресурса несёт только проверочное значение, журнал — ничего (Р3; 02, 03, 13).
4. **У ресурса поля секрета нет**, поэтому `Get`/`List`/`Update`/`Delete` не несут его по форме
   контракта (Р4; 08, 09). Признак «секрет есть» — способ, а не пустота поля (11).
5. **Проверочное значение — тем же оператором, что строка**; несостоявшееся заведение не оставляет
   ничего и секрета не выдаёт (Р5; 04, 05, 13 (б); ban #10). Держатель одноместности — гейт G
   сценария 03, а не схема.
6. **Снятие не оставляет годного материала**; снятый клиент неотличим от незаведённого (Р6; 10).
7. **Выданный секрет проходит обмен; непредъявленный, искажённый и чужой — нет** (**401** /
   `invalid_client`): положительный близнец LINE-A-1-12 на клиенте, заведённом продуктом (06, 07). В
   каждой отрицательной ветке клиент назван — заголовком либо полем формы `client_id`, — и меняется
   ровно доказательство; обмен, не называющий клиента вовсе, — отказ формы `invalid_request`, а не
   половина LINE-A-1-12 (§3).
8. **Поле-носитель помечено и названо в перечне подметальщика** (12).
9. **Порядок посадки: край знает тип раньше, чем служба его отдаёт** (Р8).
10. **Поверхность не расширяется**: глаголы ресурса остаются `Internal*` на внутреннем листенере
    (ban #6), полы и отношения каталога не меняются.

---

## §7 DoD (одна стадия; «пройдено / не пройдено»)

1. **Контракт + regen** (`buf` зелёные): сообщение `CreateInteractiveClientResponse`
   (`interactive_client`, `client_secret` с пометкой `secret_bearing`); аннотация операции `Create`
   называет его ответом; комментарий поля называет смысл пустоты (Р4); комментарий запроса называет
   отсутствие поля секрета (Р2); комментарии `client_id`, `token_endpoint_auth_method` и глагола
   `Create` перестают говорить только о поставщике. Коммит контракта объявляет смену типа ответа
   ломающей; перепись читателей тела ответа (§1 строка 14) приложена командой: читатель, разбирающий
   тело как `InteractiveClient`, один — `internal/apps/kaname/api/interactive_client/name_canon_test.go`,
   — и он переписан под новый тип тем же изменением; после него та же перепись таких читателей даёт 0.
2. **Порядок края** (Р8): край платформы перепинен на ревизию службы с новым сообщением **до** того,
   как служба им отвечает; проверяется прогоном `Create` через край платформы на сведённой сборке.
3. **Исполнитель `own`** объявляет `client_secret_basic`, чеканит секрет (Р2) и проверочное значение
   тем же хешером, что пишет пароли; шапка `internal/repo/kaname/pg/own_interactive_client_provider.go`
   переписана под Р3; `TestIntegration_OwnInteractiveClientDeregistrationLeavesNoSecretBehind`
   закрепляет способ секретом вместо `none`; красная проба полосы 405 (§1, абзац о посылке)
   зеленеет тем же изменением.
4. **Вставка** (Р5): единственный писатель строки кладёт проверочное значение и момент его установки
   тем же оператором; второго писателя не заводится; миграции не нужно — схема уже выражает всё
   (§1 строки 3, 4); применённые миграции не правятся (ban #5). Гейт G сценария 03 зелен на дереве
   посадки, красен на инъекции «вставка, затем отдельная запись материала» и печатает объём
   осмотренного.
5. **Ответ** (Р3): тело для строки операции без секрета, тело вызывающему — с ним.
6. **Подметальщик**: запись нового типа в перечне `cmd/kaname/secret_backstop.go`; гейт 12 зелен с
   записью и красен без неё.
7. **Строгий TDD** (ban #12): падающая проба до кода на каждый сценарий §4. Уровни: integration на
   01–05, 07–10, 12, 13; гейт по дереву — 03 (одноместность записи) и 12; сквозь композиционный
   корень с провязанной церемонией — 06 (обе отрицательные ветки: (а) с `client_id` в форме, (б) с
   искажённым секретом в заголовке) и 07 (после сведения с `425`); newman на стенде посадки `own`
   (задание `chart-own`) — 01, 04, 06, 08, 09, 10;
   посадка `external` — 11 в `tests/newman/cases/iam-interactive-client.py` рядом с IAM-IC-CR-CRUD-OK.
   Новая коллекция получает запись в ведомости `.github/scripts/newman-suite-debt.py`; позиция,
   которую `chart-own` ещё не гоняет, остаётся записью долга с держателем, а не зелёным.
8. **Посев ветки 425**: строки, называющие `kaname#405` отсутствующим производителем (§1 строка 16),
   на сведённой ветке — 0:
   `git grep -n -e 'нет (kaname#405)' -e 'ПУБЛИЧНЫМ (kaname#405)' -- tests .github | wc -l` → 0
   (на ветке 425 @ `685f8ad17` → 8). Предикат считает только утверждение «производителя нет», а не
   любое упоминание задачи: запись долга п.7 и пробы, ссылающиеся на `#405`, его не трогают. Набор
   церемонии заводит своих клиентов глаголом `Create`, а не посевом.
9. **Документация службы** для арендатора: одноразовость секрета, его невосстановимость, способ
   предъявления, смысл пустого поля — без сравнений с чужими облаками (ban #2) и без сборки
   «координата + условие + следствие» (`security-disclosure.md`); trail задачи в записках службы:
   ресурс интерактивного клиента, RPC `InternalInteractiveClientService/Create`, ребро пина края.
10. **Ни одного маркера отсрочки** (ban #11); ни одного поля контракта, принятого и не прочитанного.

---

## §8 Вопросы владельцу

Ноль. Указание владельца 2026-09-26 — решения принимает исполнитель; каждое принятое решение названо
в §2 с основанием, каждое невзятое — в §3 с местом, где оно живёт.

---

## §9 Правки редакции 2 — по записи ревью круга 1

Запись — `docs/specs/reviews/confidential-interactive-client-secret-shown-once/f1786b6a376cdec76ae8dfa10e4d13cbf133f0db444a088d6096ed13d2f8abac.yaml`.
Каждый факт, на который опирается правка, перемерен этой редакцией на тех же ревизиях (§1), а не
взят из записи.

| находка | род | что изменено |
|---|---|---|
| 1 (блокирующая) | TWIN | IC-SECRET-06: отрицательная половина разведена на ветки (а) и (б). В (а) заголовка нет, клиент назван полем формы `client_id`, секрета нет → 401 `invalid_client`; в (б) искажён один знак секрета в заголовке. Ветки без имени клиента нет: это отказ формы 400 `invalid_request`, вынесен в §3. §6 п.7 и DoD п.7 согласованы; в §1 добавлены строки 18 (набор ветки 425 шлёт этот случай с `client_id` в форме) и 19 (движок фундамента различает «не назван» и «не доказал») |
| 2 | DoD | §1 строка 14, Р4 и DoD п.1: читатель тела ответа в Go назван — `name_canon_test.go`, строка 33, с командой переписи; он переписывается тем же изменением |
| 3 | DoD | DoD п.8: предикат сужен до строк, называющих производителя отсутствующим; на ветке 425 он даёт 8, на `366` — 0; §1 строка 16 несёт обе величины |
| 4 | FORMAT | IC-SECRET-09 (в): текст неизвестного поля маски назван в деталях `BadRequest`, а не в сообщении статуса; (г) — назван в обоих местах |
| 5 | FORMAT | IC-SECRET-04, 05 и Р5: отказ заведения синхронный, строка операции с `error` — уровень I; адрес возврата R задан значением и связан с `redirectUris[0]` заведения 01 |
| 6 | FORMAT | шапка, «Опора»: названа ветка воркспейса `771` и её ревизия; на стволе воркспейса документа нет |
| 7 | CONSTRUCTIBILITY | IC-SECRET-13: внутренний отказ получил свои Given и When (подмена вставки отказом вне словаря), фиксированный текст `internal error` и близнеца. IC-SECRET-03: «ни в какой момент» заменено наблюдаемым утверждением о моменте ответа, а одноместность записи получила держателя — гейт G; §5, §6 п.5 и DoD п.4 согласованы |
