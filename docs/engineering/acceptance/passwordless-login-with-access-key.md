<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка: вход без пароля — ключ доступа как первичный метод (iam Ф13)

- **Статус: редакция 4 — ВЕРДИКТА НА НЕЁ НЕТ.** Кругов ревью **три**; последний вердикт —
  `APPROVED`, и он принадлежит **редакции 3**, отпечатку `83347069…`, а не тексту, который вы
  читаете. Шапка редакций 1…3
  говорила «кругов ревью два, вердикт круга 2 — `CHANGES_REQUESTED`» — **на день редакции 4 это
  ложь о действующем вердикте**, и строка поэтому переписана, а не дополнена: круг 3 состоялся,
  его запись
  `docs/specs/reviews/passwordless-login-with-access-key/833470690aff167314d54fec332810f34c3aab2e4574a0e3e871ee9184841099.yaml`
  привязана к отпечатку **`83347069…`** (редакция 3), несёт `round: 3`, `verdict: APPROVED`,
  `effective_approval.issued: true`, `ban1_lifted: true`, `issued_at: 2026-09-17T05:24:39Z`, и
  **внешнее событие опубликовано** —
  `https://github.com/PRO-Robotech/kacho/issues/1282#issuecomment-5709230381`. Записи кругов 1
  (`449a08dd…`) и 2 (`305f0777…`) не тронуты и остаются тем, чем были: вердиктами о **своих**
  отпечатках
- **Токен `DRAFT` в записях ревью — поле `subject.static_status`, и оно НЕ есть вердикт.**
  Действующее одобрение выводится из записи по `sha256` содержимого **плюс** внешнего события
  (`change-graph.md` §«Вердикт привязан к ОТПЕЧАТКУ, а не к документу»), и по обоим оно **выдано —
  на редакцию 3**. Разница между «статической формой» и «действующим вердиктом» была объявлена
  ещё редакцией 1; ошибка редакций 1…3 в другом: шапка называла **номер круга и его вердикт**, а
  это утверждение о факте, а не статическая форма, и оно пережило свой предмет. Впредь строка
  состояния называет **последнюю запись ревью и её отпечаток**, а не память автора о круге
- **⚠️ Правка ПОСЛЕ APPROVED — это редакция 4, и вердикт круга 3 на неё НЕ переносится**
  (`change-graph.md` §2): она идёт **своим** кругом по своему отпечатку. Повод — не замечание
  рецензента, а **решение владельца**, принятое после разбора поверхности, которого в момент
  круга 3 не существовало: **рукоятка человека есть отдельное случайное значение, а не
  платформенный идентификатор** (Р3). Состав и основание — врезка «Что правит редакция 4» ниже;
  что редакция **не** трогает — там же, разделом «вне объёма». Решения Р1, Р2, Р4…Р15 по существу
  не тронуты, число сценариев прежнее — **32**, ни один сценарий не снят
- **Что правит редакция 4, и почему это не «передумали».** Предмет один — **Р3**, значение
  рукоятки. Прежнее решение («значение у ключей, заведённых Ф7, — платформенный `id` человека как
  байты») **было верным при том, что было известно**: оно опиралось на неизменяемость `id`
  (ban #15), на отсутствие в нём личных данных и на то, что другого своего значения у службы нет.
  Изменилось **знание**: разбор поверхности установил, куда рукоятка **уезжает** и что с ней после
  этого сделать нельзя. Рукоятка записывается **в аутентификатор** и в синхронизируемое хранилище
  держателя, возвращается в **каждом** утверждении обнаружимого удостоверения и **видна человеку**
  в интерфейсе управления удостоверениями операционной системы. **Отозвать её нечем — писать туда
  мы не можем.** Пока рукоятка есть наш идентификатор, развязать учётную запись с ним
  **невозможно**: строки у нас снимутся, а значение в чужом хранилище останется называть снятое.
  Решение — **отдельное случайное значение**: своя таблица, **шестьдесят четыре случайных байта на
  человека**, и **каскад удаления и есть механизм развязывания**. **Мест, где прежнее значение
  объявлено, ЧЕТЫРЕ, и четвёртое меняет форму починки:** контракт · публичная страница ·
  реализация · **комментарий влитой миграции**. Последний нельзя исправить на месте — влитую
  миграцию не правят (ban #5), — значит нужна **новая** миграция с новым `COMMENT ON COLUMN`;
  перечень с исходом по каждому — §4.4. Правятся: **Р3** (заголовок и
  два абзаца), §4.1 п. 4, «Дано» §5, **Ф13-23**, §11 п. 1 (названный там отвергнутый исход
  **принят** владельцем), и заведён раздел **«вне объёма редакции 4»**. Ни одного сценария не
  заведено и не снято — меняется **значение**, которое кладёт церемония, а не наблюдаемое:
  §11 п. 1 предсказывал это дословно («колонка та же, писатель тот же … сценарии не меняются»), и
  предсказание перемерено, а не принято на слово
- **Второе, что правит редакция 4, — утверждения о ДЕРЕВЕ, и повод тот же круг 8 у соседа.**
  Редакции 1…3 писали «код Ф7 не начат» и строили на этом две строки (шапка, §6 преамбула) и одну
  клетку §4.3. Утверждение **пережило свой предмет**: реализация Ф7 влита коммитом `39628487`
  2026-09-17, два отсутствия §1.1 (∅1 и ∅2) закрылись, ∅4 — собственное отсутствие Ф13 —
  **держится**. Правятся три места, все с числом и ревизией; **ни одно решение и ни один сценарий
  этим не тронуты**: предмет правки — утверждение о дереве, а не о контракте. Числа §1.1 и §1.2
  при этом остаются на **своих** объявленных ревизиях (`af0ca8f3` · `6acf8f19` · `94352d9c` ·
  `bbd25128`) и редакцией 4 **не переснимались** — они честны тем, что ревизию называют; сплошной
  их перемер на судимой ревизии — работа своего круга, и §4.4 её называет
- **На что редакция 4 опирается у соседа — и чего у этой опоры НЕТ.** Ф7 правится тем же поводом
  и в тот же день: её **редакция 12** заводит решение Р12 (признак обнаружимости хранится, три
  состояния) и переутверждает перечень колонок Р1 — двенадцать, рукоятка двенадцатая. **Вердикта
  на редакцию 12 нет**, как нет его и на эту: обе — правки после APPROVED, и каждая идёт **своим**
  кругом по своему отпечатку. Читать отсюда «Ф7 решила» **нельзя**; читать надо так: делта к
  **одобренной** Ф7 (`12e9608d…`) — это всё, что ниже, **кроме** двух ссылок на её редакцию 12
  (перемер предиката в Р3 и число колонок в Р15), и обе названы здесь поимённо. Если редакция 12
  Ф7 вердикта не получит, обе ссылки возвращаются к состоянию `12e9608d…`, а решение Р3 о значении
  рукоятки от этого **не зависит**: его дом — эта приёмка, и Ф7 его не принимает ни в какой
  редакции. Порядок кругов — решение оркестратора, а не этого документа
- **Правки ПОСЛЕ вердикта круга 2 — редакция 3, и вот её состав.** **Б3** — Ф13-32: «Дано» и
  «Тогда» приведены к арифметике границы Ф3-28 и собственного Ф13-04 — уже предъявлено
  `N_адрес` неверных (граница включена, `N_адрес`-я — 401), близнец (б) даёт 429 на
  `N_адрес + 1`-й; «ни одного 429» в (а) стало утверждением о счёте по адресу, а не о величине
  профиля: «Дано» несёт `N_источник ≥ 2·N_адрес` с выводом числа попыток по источнику
  (`2·N_адрес`: успешный вход попыткой не считается — Р9; редакция 2 в «И» считала `login`
  попыткой источника вопреки Р9 — исправлено тем же местом), и `N_адрес ≥ 2` — порог, при котором
  ветвь (а) различает реализации (**Н11**); §8 строка Ф13-32 сверена. **Решение `kacho#2698`
  принято** (оркестратор, комментарий 2026-09-17T03:24Z): восстановление **заводит** способ входа
  паролем при отсутствии строки тем же писателем ID-PW-1; заведение первого пароля из живой
  сессии (`kaname#213`) этим **не** открыто. Ф13-25 получает производителя — **Ф5** (`#1271`,
  редакция после ред. 3 и код в `recovery_complete.go` — заказаны решением, на день редакции
  **не написаны**: Р12 п. 3 называет предикаты) — и переводится из «условие не создано:
  производитель не назван» в «условие не создано: производитель Ф5 не посажен», как Ф13-13 с
  `kaname#208`; правятся §0, §0.2, §0.4 (две строки), §1.1 ∅7, §1.3 п. 4, Р7, **Р12** (заголовок и
  пп. 2–3), §3 Д4/Д24, §4.2, §4.3, Ф13-19 «И», полоса «утрата» и **Ф13-25** («Тогда» (2), три «И»),
  §6, §6.1, §7 инв. 14–15, §8, §9 п. 7, §11 п. 5 и таблица. **Н12** — Р15: строка о перечне
  ключей отсылает к Ф7 Р11 / §3.0, а не к Ф7-25; **Н13** — §4.1 п. 14 называет поля ответа
  браузера, которые снимает страница; **Н14** — линия перемерена (ниже); **Н15** — контроль §3
  сводки множествами; **Н16** — Ф13-30: подлежащее — прогон пары. **Число сценариев — 32**, метки
  §5 и ведомость §6 не изменились (`при условии — 2` те же); решения Р1…Р15 по существу не
  тронуты (Р12 — переписан под принятое решение, его требование к Ф13 прежнее). **Линия ушла
  вперёд, и это перемерено:** `release/iam-lines` на origin — `0cb0eddd` (одна строка
  `git ls-remote`), несёт ствол `94352d9c` предком и **Ф12 `#215` — код второго фактора**; на
  ней предикаты §1.1/§1.2 дают: ∅1 файлов, называющих предмет, 7 → **8** (`internal/domain/login_method.go`);
  ∅4 путей 7 → **13**, видов формы 6 → **8** (шесть глаголов второго фактора и `step-up`); ∅6
  контроль `ResetSecondFactor` 0 → **4** (Ф12 Р10 на линии есть — «Ф12 не начата» верно о стволе,
  не о линии); Ф-ж литерал `MethodPassword}` 1 → **0** — перечень провязанных способов
  `{password, totp, lookup_secret}`, самоотчёт «1», «2» (Ф13-14 это уже называет: «с Ф12 без
  ключа — «1», «2»»). **Ни одно ∅ Ф13 не закрылось**: ∅1…∅3, ∅5, ∅7 (503, фраза 2), ∅8 — те же;
  столбец «линия» таблиц §1.1/§1.2 остаётся о `6acf8f19 = 94352d9c`, сдвиг назван здесь и в §0.4,
  §4.3 строка Ф12 — «код на линии». Вердикт круга 2 относится к редакции 2 (`305f0777…`) и на
  редакцию 3 не переносится; записи кругов 1 и 2 не тронуты
- **Правки ПОСЛЕ вердикта круга 1 — редакция 2, и вот её состав:** **Б1** — снятый ключ у
  проверяющего неотличим от неизвестного удостоверения: решение **Р15** (оба исхода ревьюера
  названы, второй отвергнут с ценой по каждому месту Ф7), Ф13-06 «к» без устройства в «Когда»,
  клетки Р10 и Ф13-30 — одна вместо двух, §0.1 и §3 Д27 (принято), §7 инв. 13, §11 п. 6.
  **Б2** — ведомость дельты к Ф7 дополнена тремя дописками, которых редакция 1 не называла
  строкой: «Тогда» Ф7-21 (пара «с рукояткой · без» — Д28), число рядом с Ф7-24 (Д29), DoD Ф7
  §9.1 п. 6 (Д30); сводка §3 — «дописано — 7», §0.2 — перечень всех семи мест поимённо, §6.1,
  Р3, Р11, Ф13-24, §9 п. 1. **Н1…Н10** — ∅7: предикат даёт 2 строки, отказ один; Ф-г держат
  Ф3-28/29; край `#2689` **влит** стволом платформы (шапка, §1.2 Ф-з/Ф-и, §4.3, §6, §8, §9 п. 8,
  Ф13-28, §11); `access-key-revoked` — **седьмая** причина отзыва и своя константа носителя (Р8,
  Д12, §6.2); **Ф13-32** — обнуление счёта по адресу входом ключом (держатель Р9; §6, §7 инв. 10,
  §8, §9 п. 3 — сценариев 32); регистрация личности ключом — `kacho#2703` (§0.2, §4.2, §6.1);
  §6.1 отвечает строкам Ф7 §4.5 «кроме четырёх о своём», не «каждой»; Ф13-23 утверждает проход
  церемонии из сессии ключа явно — третье «Дано» Ф7 со строителем (§6.1); 73 коммита вместо 81;
  цена Р1 для клиента вне консоли (§4.1 п. 14). **Перемер посылки после схлопывания линии в
  ствол** (PR `#218`) — ревизии измерения ниже и §0.4.
  Вердикт круга 1 относится к редакции 1 (`449a08dd…`) и на редакцию 2 не переносится
  (`change-graph.md` §«Вердикт привязан к ОТПЕЧАТКУ»); редакция 1 — черновик целиком, правок до
  круга 1 не было
- **Задача:** `PRO-Robotech/kacho#1282` — «Ф13: вход без пароля — ключ доступа как первичный
  метод»; эпик `#1266`. Тело задачи: «Вход: Ф7 (#1273, серверная сторона WebAuthn) и Ф11»
- **Это ДЕЛЬТА к двум одобренным приёмкам этого дома, и обе читаются номером, а не
  пересказываются:**
  - **Ф7** — `access-keys-are-ours.md`, APPROVED круга 6: запись
    `docs/specs/reviews/access-keys-are-ours/12e9608d820a5fc13aa7e071f10fec9d958260703bbfff1b628f432d77b8e64d.yaml`,
    внешнее событие `https://github.com/PRO-Robotech/kacho/issues/1273#issuecomment-5703628145`
    (`issued: true`, `ban1_lifted: true`); отпечаток файла на линии равен `subject.sha256` записи
    (`sha256sum docs/engineering/acceptance/access-keys-are-ours.md` → `12e9608d…`).
    **⚠️ КОД Ф7 ВЛИТ, и редакции 1…3 писали здесь обратное.** Стояло: «Код Ф7 **не начат**
    (`#1273` OPEN; §1.1) — производители ключа, испытания, сверки утверждения и хранилища здесь
    **будущие**». Перемерено редакцией 4 на судимой ревизии `736c0de4`: реализация Ф7 влита в
    ствол коммитом **`39628487`** (2026-09-17), и **два отсутствия §1.1 закрылись** —
    `git grep -liE 'CredentialAssertion|VerifyAssertion|FinishAssertion|BeginAssertion|authenticatorData' $R -- '*.go' ':!*_test.go'`
    → **9** (было 0, ∅1), `… | grep -c 'AccessKeyService'` → **2** и миграций ключа → **2**
    (было 0 · 0, ∅2). Задача `#1273` при этом по-прежнему OPEN — открытость задачи и наличие
    кода это **разные** факты, и редакции 1…3 выводили второе из первого.
    **Предмет Ф13 закрытием не затронут:** её собственное отсутствие ∅4 держится —
    `git show $R:internal/handler/loginlanehttp/handler.go | grep -c 'access-key'` → **0** при
    тринадцати путях семейства. Ведомость §6 называет фазу-производителя тем же правилом: «чьё
    изменение делает исход верным», а не «написано ли уже». Регистрация и
    предъявление ключа, испытание и его однократность, привязка к имени доверяющей стороны и
    перечню происхождений, счётчик, перенос трёх ключей, потолок — **всё это Ф7**; её §4.5 отдаёт
    сюда ровно четыре вещи, и §6.1 здесь отвечает каждой строке той таблицы, **кроме четырёх, где
    Ф7 говорит о своём** (церемония · сверка · хранилище · перенос) — одиннадцати из пятнадцати;
  - **Ф3** — `login-lane-issues-our-session-and-logout-ends-it-server-side.md`, APPROVED круга 2:
    запись
    `docs/specs/reviews/login-lane-issues-our-session-and-logout-ends-it-server-side/1a7f6846d102068bd7023eab4527010b573a22f43a1c988416a4c5f63e1a1170.yaml`,
    внешнее событие `https://github.com/PRO-Robotech/kacho/issues/1269#issuecomment-5701542562`
    (`issued: true`); **код Ф3 в стволе** (§1.2). Её §4.2 отдаёт этой фазе строку «вход ключом
    доступа»; её поверхность (Р2), носитель (Р3), «сессии нет» на глаголах формы (Р7), частота
    (Р10), защита форм (Р12), журнал (Р14), допуск слушателя (Р16) здесь **не переизлагаются** —
    берутся номером и **переиспользуются** двумя новыми глаголами того же семейства
- **Над чем ещё стоит — четыре одобренные приёмки, каждая читается номером:** **Ф11**
  (`assurance-level-is-declared-by-our-session.md`, APPROVED круга 3, запись
  `docs/specs/reviews/assurance-level-is-declared-by-our-session/68c2cc6cda07e7ecfbd5efe209954748146970cfcbbd9fbc670a737527f3a324.yaml`,
  событие `https://github.com/PRO-Robotech/kacho/issues/1280#issuecomment-5690585262`;
  правило в стволе, `#163`; **три её сценария — Ф11-03, -04, -05 — исполняются в DoD этой
  фазы** по её §5.2); **Ф12** (`second-factor-totp-and-recovery-codes.md`, APPROVED круга 2,
  запись
  `docs/specs/reviews/second-factor-totp-and-recovery-codes/b71c3ff7777987e4bc48f9a06aee86f52155c3f8856e5352cd61eb6f408bc726.yaml`,
  событие `https://github.com/PRO-Robotech/kacho/issues/1281#issuecomment-5705441673`; её Р3
  решила обязательность второго фактора, и ось «второй фактор при входе ключом» здесь строится на
  её решении); **Ф4** (`registration-and-its-three-consequences.md`, APPROVED круга 1, событие
  `https://github.com/PRO-Robotech/kacho/issues/1270#issuecomment-5691996280`) и **Ф5**
  (`recovery-of-access.md`, APPROVED круга 1, событие
  `https://github.com/PRO-Robotech/kacho/issues/1271#issuecomment-5691996522`) — производители
  «Дано» о регистрации и о возврате доступа; **Ф1** (`login-session-and-credentials-are-our-contract.md`,
  APPROVED круга 8) — её §6 выносит беспарольный вход сюда одной строкой; **Ф2**
  (`password-verifier-follows-the-stored-value.md`, APPROVED круга 3) — хранилище способов входа
- **Ревизии измерения — дом службы, и их три, потому что ствол двигался под документом.**
  Редакция 1 мерила **ствол `af0ca8f3`** (`origin/main` `PRO-Robotech/kaname` на 2026-09-17 до
  02:19Z — слияние PR `#189`; на ней код Ф3, правило уровня Ф11, приёмки Ф3, Ф4, Ф5, Ф11) и
  **базу этого документа — голову линии `release/iam-lines` `6acf8f19`** (merge-base ветки с
  линией): **73** коммита сверх `af0ca8f3` (`git rev-list --count af0ca8f3..6acf8f19` → 73; без
  слияний 59; по первому родителю 7. Редакция 1 писала «81» — число головы линии `de41dbe0` в
  момент, когда тянулись ссылки, а не названной ревизии — Н9 круга 1). На `af0ca8f3` **нет**
  приёмок Ф7 и Ф12 и кода Ф4 и Ф5 (`internal/apps/kaname/api/registration/`,
  `internal/apps/kaname/api/humansession/recovery_complete.go`); на `6acf8f19` есть. Пара
  «ствол · линия» ниже снята на **этих** двух ревизиях.
  **Редакция 2 перемерила на стволе `94352d9c`** — `origin/main` после схлопывания линии в ствол
  (PR `#218`, MERGED 2026-09-17T02:19Z): его дерево **побайтово равно** дереву головы линии
  `bbd25128` (`git diff --stat origin/main bbd25128` → пусто); приёмки Ф7 (`12e9608d…`) и Ф12
  (`b71c3ff7…`) и код Ф4/Ф5 теперь **в стволе**; между `6acf8f19` и `bbd25128` ни одна приёмка
  и ни одна запись ревью не тронуты (`git diff --name-only 6acf8f19 bbd25128 -- docs/engineering/acceptance docs/specs/reviews`
  → 0 путей). Все предикаты §1.1, §1.2 и §3 прогнаны на `94352d9c` и `bbd25128` и дали столбец
  «линия» у каждого; единственная величина, сдвинувшаяся с `6acf8f19`, — число записей каталога
  с полом «2»: 28 → **29** (`UserService/ResendInvite`, линия приглашений; Ф13-14). Линия
  `release/iam-lines` на origin заведена заново на `bbd25128` и остаётся базой PR этого документа.
  *Редакция 3 (Н14 круга 2): голова линии на origin — `0cb0eddd` (`git ls-remote --heads origin
  refs/heads/release/iam-lines` → одна строка); между `bbd25128` и `0cb0eddd` — слияние
  `origin/main` (`1f51668f`, дерево то же) и Ф12 `#215`; база ветки и PR `#219` — по-прежнему
  линия, merge-base `6acf8f19`; что на линии сдвинулось — пункт редакции 3 выше*
- **Ревизия измерения — дерево платформы:** `PRO-Robotech/kacho@275240efdc` (`origin/main`).
  Край, консоль и объявление беспарольного входа в посадке живут там; **`275240efdc` и есть
  коммит слияния ветки края Ф3** (`issue-1269-login-lane-edge`, PR `#2689` — MERGED
  2026-09-17T01:08Z): ретрансляция семейства Ф3 — в стволе платформы, **четыре** пути; пятого и
  двух путей восстановления там нет (`kacho#2699`, `kacho#2701` OPEN). Редакция 1 называла ветку
  невлитой на той же ревизии — Н3 круга 1
- **Тип изменения:** ввод отсутствующего производителя — второй полосы входа. Для арендатора —
  **расширение, не ломающее**: вход паролем остаётся как есть (Ф3-01…05 дословно); ни одна
  величина посадки не добавляется; ни один пол каталога не меняется; форма отказа по полу —
  Ф11 §4.1 дословно
- **Сервис:** `kaname` — два глагола входа ключом на слушателе формы, обнаружение без имени,
  выдача сессии по утверждению, рукоятка ключа, судьба живых сессий при снятии ключа, частота,
  журнал, страж посадки. Край — **потребитель**: ретранслирует два новых глагола тем же
  механизмом, что семь глаголов Ф3, Ф4, Ф5, и ничего не решает (Ф11 Р7). Консоль — потребитель
  края; её экран — Ф8
- **Формат:** Given-When-Then, только markdown — кода здесь нет по построению
- **Публичный репозиторий:** документ написан так, чтобы выдерживать публикацию — §10

---

## §0 Обзор и границы

Сегодня беспарольный вход **объявлен и производится чужим компонентом**: посадка платформы
объявляет удостоверение ключа доступа **самостоятельным** способом входа (`passwordless: true`,
§1.1), три живых ключа заведены этим режимом (Ф7 §1.2), а в нашем дереве нет ни сверки
утверждения, ни строки ключа, ни глагола входа без пароля — ни в стволе, ни на линии (§1.1).
Ф7 решила, **чем** ключ заводится, сверяется, хранится и переносится, и в своём §4.5 назвала
предикатами, что она этой фазе даёт и чего не даёт. Ф3 решила, **какой поверхностью** живёт
вход, **чем** представлена сессия и **как** защищена форма, и её слушатель формы, сессия,
частота и признак формы **в стволе**. Ф11 решила, **что даёт** утверждение ключа — «2» либо
«3» по флагам этого утверждения, — и три её сценария ждут этой фазы.

Этот документ решает то, чего не решает ни одна из трёх: **какими глаголами** ключ предъявляется
там, где вызывающий ещё никто (Р1); **как** служба находит человека, не назвав его, и что при
этом сверяется (Р2, Р3); **что** выдаётся по утверждению и **какой** у выданного уровень (Р4);
**нужен ли второй фактор** при входе ключом — обе стороны оси (Р5); **как сосуществуют две
полосы входа** и что значит «личность без пароля вовсе» (Р6, Р7); **что происходит с живыми
сессиями**, когда ключ снят, и чем снятый ключ виден полосе входа (Р8, Р15); чем ограничен подбор и как ведётся журнал (Р9, Р10); **что
делать с ключом, который в этой полосе не появился** (Р11); и — то, что тело `#1282` требует
назвать и решить, — **что происходит с человеком, у которого ключ утрачен, а пароль не
заводился** (Р12). Последнее **решено** (`kacho#2698`, исход 1, оркестратор 2026-09-17): путь
возврата — восстановление по подтверждённому адресу, которое **заводит** способ входа паролем,
если строки нет; производитель — Ф5 (`#1271`) с писателем ID-PW-1, и на дереве он **ещё не
посажен** (§1.1 ∅7); сброс распорядителем путём не является — сирота без глагола (∅6,
`kacho#2702`). Сценарий пути написан (Ф13-25), его условие — посадка производителя, и это
названо, а не спрятано (§4.3, §11).

### §0.1 Что уже решили соседи — названо номером, не переизлагается

| где | что решено | что из этого здесь |
|---|---|---|
| **Ф7 §4.5** | шов с Ф13 назван предикатами в обе стороны: Ф13 — обнаружение ключа до названия человека, выдача сессии по утверждению, свой глагол входа со своей причиной освобождения, уровень после входа (исполняет), судьба перенесённого ключа, не появившегося в полосе; Ф7 — всё остальное о ключе | §6.1 отвечает каждой строке той таблицы, кроме четырёх, где Ф7 говорит о своём; Р1 — глагол свой и причины из словаря освобождений ему **не нужно**: он не публичный RPC, а глагол формы (Ф3 Р2) |
| **Ф7 Р9** | обнаруживаемое удостоверение **требуется** на регистрации — «полоса Ф13 без него невозможна by construction»; проверка пользователя при предъявлении — «предпочтительна»; значение, чей единственный читатель — Ф13, приходит её приёмкой | Р2: испытание входа без `allowCredentials`, требование проверки пользователя — тот же литерал; Р3: рукоятка ключа — величина, чей читатель здесь, и она решена здесь |
| **Ф7 Р11, §10 п. 26** | два глагола утверждения Ф7 освобождены причиной `SELF_SERVICE` со связыванием с **вызывающим**; глагол входа — свой, вызывающего у него нет; его причина — решение Ф13 | Р1: глагол входа не наследует ни `BeginAssertion`, ни `FinishAssertion` — он на слушателе формы, где записи каталога и причины освобождения не бывает (Ф3 Р2: «записи каталога у формы нет»); §3 Д1 |
| **Ф7 §3.0, §3.2** | «утверждение проходит» — ответ называет человека без отказа; **единый отказ аутентификации** — код и тело побайтово, время по Ф1-48…50; четырнадцать полос | Р7: единый отказ входа ключом **побайтово равен** отказу входа паролем Ф3-02 — один текст на семейство; полосы отказа здесь выведены предикатом §5 |
| **Ф7 Р1, Р10** | ключ — строка своей таблицы по идентификатору удостоверения, глобально уникальному; наружу ключ назван `id` формы `ak-…`, идентификатор удостоверения наружу не выходит; колонка без читателя не заводится, читатель приходит своей приёмкой | Р3: человека называет идентификатор удостоверения; рукоятка — колонка строки, у которой читатель здесь; событие выдачи сессии называет ключ его `id` (Р10) |
| **Ф7 Р5, Ф7-26, Ф7-24** | церемонии без сессии у Ф7 нет; снятие последнего способа входа отвергается; людей без пароля перенос считает числом | Р6, Р7: личность без пароля — первоклассная, её сессия строится **действием** этой фазы; Ф7-26 остаётся держателем «наш глагол человека не запирает» |
| **Ф7 Р8, Ф7-37, Ф7-27, §3.0** | снятие ключа **возвращает слот** — триггер учёта возвращает его на удалении строки, той же транзакцией; «ключ не снят» определён поведением — снятый ключ по-прежнему проходит; отказ снятия по `id` годной формы, которому не отвечает ни одна строка, побайтово равен отказу по чужому ключу; состояния «снят» в составе строки Р1 нет | Р15: после Ф7-25 строки нет — снятый ключ у проверяющего есть «удостоверение неизвестно», клетка счётчика одна, отдельного состояния Ф13 за Ф7 не заводит |
| **Ф7 §4.4 расх. 10, расх. 11; §10 п. 16, 20** | путь при утрате ключа решён F4d Р4 / F4e Р6 и Ф5 — за Ф13 только вход; регистрация личности ключом — своя приёмка, составляющая Ф4 и Ф7; обнаруживаемость перенесённых ключей не установлена | Р12: путь **объявлен** и **перемерен** — производителей нет (§1.1 ∅6, ∅7; `kacho#2698`, `kacho#2702`); §4.2: регистрации ключом здесь нет; Р11: перенесённый ключ без рукоятки либо необнаруживаемый — число и цена |
| **Ф11 Р2, Р3** | «3» — утверждение с проверкой пользователя ключом, не допускающим резервного копирования; «2» — любое утверждение ключа; различитель — флаги **этого** утверждения | Р4; Ф13-10…12 исполняют Ф11-03…05 под их номерами; §0.4 — тело `#1282` говорит «проверка присутствия», и это сведено к Р3 |
| **Ф11 Р1, Р5, Р6** | уровень — поле сессии, писатель один; копии — чтения записи; предъявление внутри сессии перевыпускает носитель, момент аутентификации неподвижен | Р4: выдача — одной операцией Ф3 с множеством `{webauthn}` и флагами; Ф13-13: копии читают запись (сегодня одна не читает — `kaname#208`) |
| **Ф11 Р4** | правило одно для всех производителей сессии; вход без пароля — по строкам ключа | Р4 |
| **Ф11 Р9, §5.1 п. 2** | страж посадки `own` считает предъявимые уровни правилом из провязанных способов; Ф11 — предусловие Ф13 | Ф13-14: провязка ключа даёт перечень «1», «2», «3» |
| **Ф11 §3.0** | «уровень L» наблюдается по исходу: пол пройден либо 401 · `code` 16 · `WWW-Authenticate`; «3» — в ответе службы краю о сессии | все «уровень L» ниже — определение Ф11 §3.0 дословно |
| **Ф12 Р3** | обязательность второго фактора — свойство **действия** (пол каталога), а не личности; на входе никто не обязан; вход паролем при заведённом факторе даёт «1» | Р5: при входе ключом второй фактор не требуется и не принимается; заведённый фактор ничего не меняет (Ф13-15) |
| **Ф12 Р4, Р9** | шесть глаголов семейства Ф3; поле `secondFactor` у входа паролем; церемония `step-up` одна на все способы; снятие фактора гасит прочие сессии, текущей уровня не понижает | Р5: у входа ключом поля `secondFactor` **нет** (Ф13-16); `step-up` внутри сессии ключа — Ф13-17; Р8 — снятие ключа гасит прочие сессии по образцу Р9 |
| **Ф3 Р1** | состав записи сессии закрыт: субъект · моменты · срок · уровень · множество предъявленного · подтверждённость адреса · требование сменить пароль | состав **не расширяется**; всё, что нужно входу ключом, — множество `{webauthn}` и уровень |
| **Ф3 Р2, Р16** | семейство `/iam/v1/auth/*` на origin консоли; край ретранслирует; слушатель формы допускает ровно край; текст отказа входа — один на все причины: `authentication failed` | Р1: два новых глагола — то же семейство, тот же слушатель, та же ретрансляция (Ф13-28); Р7 — тот же текст |
| **Ф3 Р7** | «сессии нет» на глаголах формы ретранслируется; отсечка — отказ на крае | Р1: на обоих глаголах входа ключом — как на входе паролем (Ф3-51 дословно) |
| **Ф3 Р10** | два счёта неверных предъявлений — по адресу и по источнику; успешный вход обнуляет счёт по адресу; отказ формы не считается | Р9: у полосы без имени адреса нет — считается **источник**; выдача испытания — попытка (Ф13-04); успешный вход ключом обнуляет счёт по адресу человека, как всякий вход |
| **Ф3 Р12, Ф3-35** | признак формы привязан к контексту и к виду из закрытого перечня; контекст сменяется выдачей сессии; виды дописывают фазы в **тот же** перечень | Р1: вид `access-key-login` — в тот же перечень одним объявлением (Ф13-27); испытание входа привязано к **контексту формы** (Р2) |
| **Ф3 Р14, Ф3-47** | события аудита в существующую очередь одной транзакцией; выдача сессии — событие `iam.session.issued` с именами способов; счётчики с нулевыми клетками | Р10: то же событие с `["webauthn"]` и `id` ключа; клетки исходов полосы ключа (Ф13-29, 31) |
| **Ф3 Р9** | пять причин отзыва — один отказ предъявителю; причина живёт в журнале; Ф12 Р10 добавила шестую | Р8: седьмая причина — снятие ключа, своей константой у носителя; отказ предъявителю тот же |
| **Ф1 §4.1, Ф1 Р3, Ф1-48…50** | 15 минут окна свежести; отказ входа один по телу и по времени | Р7: неразличимость по времени на полосе ключа — Ф13-06 «л» |
| **Ф5 Р6, Ф5-01…03, Ф5-23** | два пути возврата доступа сосуществуют; восстановление — код по почте и новый пароль | Р12: путь назван; его исполнимость у личности без пароля — `kacho#2698` |
| **Ф4 §1.2, Ф4-06** | полос регистрации у нашего объявления одна — пароль; `N = M` | §4.2: регистрации личности ключом здесь нет (Ф7 §4.4 расх. 11) |
| **Ф12 §0.2, Ф11 §0.2, Ф3 §0.2, Ф7 §0.2** | одобрение документа не разрешает ничего вне его ведомости | §0.2 здесь |

### §0.2 Что APPROVED этого документа разрешает — и чего не разрешает

**Разрешает:** кодировать объём §4.1 в фазе Ф13 `#1282` — и только то в ней, что ведомость §6
называет поимённо: два глагола входа ключом на слушателе формы, испытание входа и его
однократность, обнаружение по идентификатору удостоверения и сверку рукоятки, выдачу сессии по
утверждению с множеством `{webauthn}` и флагами, отказы — единый и формы, счёт по источнику,
обнуление счёта по адресу входом, снятие ключа как причину отзыва прочих сессий, событие и
клетки счётчиков, провязку ключа в перечень стража посадки, вид признака формы, две записи в закрытых перечнях Ф3 (виды формы,
пути ретрансляции) и ретрансляцию краем. Вместе с этим — исполнить в DoD Ф13 три сценария Ф11
под их номерами (Ф11-03, -04, -05) и сценарии Ф7, чьё «Дано» до этой фазы строилось записью
(Ф7-26 положительный контроль, Ф7-16 — §6.1).

**Не разрешает:** сервер церемонии, сверку утверждения, хранилище и перенос ключей (Ф7 `#1273`
— здесь они **переиспользуются**, а не заводятся: второй сверки в дереве не будет, §7 инв. 2);
второй фактор и его глаголы (Ф12 `#1281`); экран входа ключом и кнопку «войти ключом» в
консоли (Ф8 `#1274`); регистрацию **личности** ключом без пароля и без сессии (своя приёмка,
Ф7 §4.4 расх. 11 — `kacho#2703`, §4.2); заведение первого пароля из сессии (`kaname#213`); сброс способа
входа распорядителем (`kacho#2702`); восстановление у личности без пароля — решено `kacho#2698`,
производит Ф5 (`#1271`), не эта фаза;
снятие чужого компонента и его объявления `passwordless: true` (Ф10 `#1276`). У каждого свой
владелец и своя приёмка; разрешение, выданное вперёд, неотличимо от разрешения, выданного по
невнимательности.

**Ничего в одобренных соседях этот документ не заменяет.** Все расхождения §3 разрешены
принятием либо интерпретацией с названным исходом; мест, где Ф13 **дописывает** к одобренным
соседям, — **семь**, каждое стоит строкой §3 с доводом и ценой и видно владельцу в событии
одобрения этого документа. У **Ф7** — пять: рукоятка в строке ключа (Д2; Р3 — по правилу Ф7
Р1/Р9 о колонке с читателем), отзыв прочих сессий при снятии ключа глаголом Ф7 (Д3; Р8 — по
образцу Ф12 Р9), пара «строк с рукояткой · без» в «Тогда» переписи Ф7-21 (Д28; Р3, Р11), второе
число рядом с Ф7-24 под тем же условием объявления переноса состоявшимся (Д29; Р11), чтение
этой пары и числа в DoD Ф7 §9.1 п. 6 (Д30; §9 п. 1). У **Ф3** — два: седьмая причина отзыва
`access-key-revoked` в перечне Ф3 Р9 (Д12; Р8) и поле `access_key_id` в событии Ф3 Р14 (Д14;
Р10). Редакция 1 называла два места из семи — Б2 круга 1: три правки одобренной Ф7 стояли в
Р3, Р11, Ф13-24 и §9 п. 1 без строки ведомости, и одобрение покрыло бы меньше, чем Ф13 меняет.
Предикат: число строк §3 с исходом «дописано» равно **семи**, и перечень здесь совпадает с
ними поимённо.

### §0.3 Почему документ лежит здесь

Глаголы входа, испытание, сверка рукоятки, выдача сессии, счёт попыток — предмет `kaname`, и
лежат они рядом с кодом (`polyrepo.md` §«У приёмки домов ДВА»; Ф3 §0.3, Ф7 §0.3, Ф12 §0.3).
Край здесь потребитель ровно так же, как у Ф3, Ф4, Ф5 и Ф12: ретранслирует и не решает. Консоль
— потребитель края, и её экран — Ф8.

Координаты чужих домов пишутся приставкой `владелец/репозиторий@ревизия:` в прозе; в
пути-спецификацию команды приставка **не вставляется** (Ф7 §0.3: `git grep` примет её за часть
пути, найдёт ноль и промолчит) — команда о чужом дереве исполняется в клоне названного дома.

### §0.4 Перемер посылки задания и тела задачи — в обе стороны

Задание велело перемерить посылку первым делом. Исход по каждому утверждению:

| утверждение | исход | чем |
|---|---|---|
| задание: база ветвления и PR — `release/iam-lines` @ `6acf8f19`, впереди `main` на Ф4, Ф5, #181, #184, #106 S2б и приёмки Ф12/Ф7 | **подтвердилось на день редакции 1; устарело к редакции 2 — ниже** | `git ls-remote --heads origin refs/heads/release/iam-lines` → одна строка, `6acf8f19` (2026-09-17 до 02:19Z); сверх ствола `af0ca8f3` **73** коммита (редакция 1 писала 81 — Н9); приёмок Ф7/Ф12 и кода Ф4/Ф5 в стволе `af0ca8f3` нет, на линии есть (шапка) |
| задание круга 2: «база `release/iam-lines` `6acf8f19`, несёт APPROVED Ф7 и Ф12, впереди `main`» | **опровергнуто наполовину — ствол догнал линию** | линия схлопнута в ствол PR `#218` (`94352d9c`, MERGED 02:19Z; дерево = `bbd25128`); `origin/release/iam-lines` заведена заново на `bbd25128` — `git ls-remote --heads origin refs/heads/release/iam-lines` → одна строка, `bbd25128`; приёмки Ф7/Ф12 **теперь и в стволе**, их отпечатки равны записям ревью; PR `#219` GitHub закрыл при снятии базы и он переоткрыт на неё же. База ветки (merge-base с линией) — по-прежнему `6acf8f19` |
| задание круга 2: «PR `#2689` края OPEN, не влит» (унаследовано от редакции 1) | **опровергнуто** | `gh pr view 2689` → MERGED 2026-09-17T01:08Z, коммит слияния `275240efdc` — ревизия измерения платформы (Н3) |
| задание: «сервер WebAuthn Ф7 ещё НЕ реализован — приёмка одобрена, код `kacho#1273` не начат» | **подтвердилось** | §1.1 ∅1, ∅2: библиотеки в пине **0**, контракта ключа **0**, сверки утверждения **0** — на обеих ревизиях; `#1273` OPEN |
| задание: «производители Ф13 — будущие» | **подтвердилось наполовину, и половина несущая** | будущие — сверка, ключ, испытание (Ф7). **Существующие** — операция выдачи сессии, правило уровня с флагами ключа, слушатель формы с закрытыми перечнями, счёт по источнику, признак формы, событие выдачи, страж посадки (§1.2 Ф-а…Ф-и, все в стволе). Ф13 заводит своего мало (§1.3), и ведомость §6 у каждого «Тогда» называет фазу — Ф7 · Ф12 · Ф13 — либо существующую форму |
| тело `#1282`: «беспарольный вход объявлен в нашем конфиге внешнего провайдера (`passwordless: true`)» | **подтвердилось — с уточнением координаты** | §1.1: шаблон переехал вместе с чартом — `deploy/helm/umbrella/charts/kaname/templates/_kratos-identity.tpl` в дереве платформы (комментарий задачи от 2026-09-05 называл `charts/kacho-iam/`); объявление на месте — 1 строка `passwordless: true` |
| тело `#1282`: «уровень — «2», а при проверке **присутствия** пользователя — «3»» | **ОПРОВЕРГНУТО одобренной Ф11 Р3, и Ф11 §10 п. 3 предупредила об этом поимённо** | присутствие обязательно в каждом годном утверждении (Ф7-49: без бита присутствия — единый отказ), и «3» по присутствию сделало бы «2» недостижимым; различитель — проверка **пользователя** ключом, **не допускающим резервного копирования**. Этот документ читает предикат задачи через Ф11 Р3: «2» либо «3» **по факту проверки пользователя и признака резервного копирования** (Ф13-10…12); контракт задачи этим не меняется — уточняется по одобренному соседу, на которого само тело ссылается («см. Ф11») |
| тело `#1282`: «путь восстановления при утрате ключа объявлен и проверен пробой» — и Ф7 §4.5: «уже решён чужими приёмками» | **подтвердилось на уровне РЕШЕНИЙ и ОПРОВЕРГНУТО на уровне ПРОИЗВОДИТЕЛЕЙ** | решения есть: F4d Р4 (сброс распорядителем), Ф5 Р6 (самостоятельное восстановление). Производителей нет: сброс распорядителем — сирота F4d-20 без глагола и без фазы (§1.1 ∅6, `kacho#2702`); завершение восстановления у личности **без строки пароля** отвечает **503** и откатывается (§1.1 ∅7). Путь здесь **объявлен** (Р12) и его проба **написана** (Ф13-25). *Редакция 3:* исход **решён** — `kacho#2698` исход 1 (оркестратор, 2026-09-17T03:24Z): восстановление заводит способ; производитель — Ф5 (`#1271`, редакция после ред. 3 и код — заказаны решением, на `0cb0eddd` не посажены); проба исполнима после посадки — §4.3, §11 |
| Ф7 §4.4 расх. 10: «ни один из двух путей пароля не требует» | **опровергнуто для Ф5 на дереве; решением `kacho#2698` возвращается к истине после посадки** | `recovery_complete.go`: замещение строки пароля; строки нет — отказ (§1.1 ∅7). Довод Ф7 верен о **доказательстве** (владение ящиком), неверен о **производителе** на дереве сегодня; решение `#2698` (исход 1) делает доказательство и основанием заведения строки — тем же писателем ID-PW-1 |
| Ф7 §4.5: «глагол входа ключом — свой; его причина освобождения — решение Ф13 внутри закрытого словаря контракта либо расширение переписи» | **подтвердилось в первой половине, вторая беспредметна** | глагол свой (Р1); но он — глагол **формы** семейства Ф3, не публичный RPC: записи каталога и причины освобождения у него нет by construction, перепись публичных RPC его не видит (Ф3 Р2 дословно: «записи каталога у формы нет»). Ф7 писала о словаре, полагая глагол RPC; §3 Д1 |
| задание круга 3: «голова ветки `08088a53`, документ `305f0777…`, запись круга 2 — CHANGES_REQUESTED, одно блокирующее Б3» | **подтвердилось** | `git rev-parse origin/acceptance/1282-passwordless` → `08088a53`; `sha256sum` документа → `305f0777…`; запись `305f0777….yaml`: `verdict: CHANGES_REQUESTED`, `blocking: [Б3]`, `non_blocking: [Н11…Н16]` |
| задание круга 3: «линия `release/iam-lines@0cb0eddd` несёт `main@94352d9c` предком плюс Ф12 (`#215`)» | **подтвердилось — и сдвинуло столбец «линия»** | `git ls-remote` → одна строка `0cb0eddd`; `git merge-base --is-ancestor origin/main origin/release/iam-lines` → да; `git log origin/main..origin/release/iam-lines` — коммиты `#215` и `#221`; предикаты §1.1/§1.2 на `0cb0eddd`: ∅4 путей 13, видов 8; ∅6 `ResetSecondFactor` 4; Ф-ж `MethodPassword}` 0 (перечень `{password, totp, lookup_secret}`); ∅1 файлов 8; остальное — как на `94352d9c` (∅7: `есть · 2 · 1`) |
| задание круга 3: «решение `kacho#2698` принято оркестратором 2026-09-17: восстановление заводит способ входа паролем при отсутствии строки тем же писателем ID-PW-1; заведение первого пароля из живой сессии (`kaname#213`) этим не открывается» | **подтвердилось дословно** | `gh issue view 2698 -R PRO-Robotech/kacho --json comments` → один комментарий, 03:24:08Z, «исход 1 — восстановление ЗАВОДИТ способ входа паролем, если строки нет; писатель — тот же оператор ID-PW-1»; «Заведение первого пароля из живой сессии (kaname#213) — отдельный предмет и этим решением не открывается». Заказанное решением не написано: ID-PW-1 на стволе `2554fb6f`, Ф5 — `e889257f` (ствол) и `5b70cc58` (ветка `#214`), ни одна не несёт `2698`; `recovery_complete.go` на `0cb0eddd` — фраза 503 стоит (2) |
| задание круга 3 (Б3): «Ф3-28 и Ф13-04 дают 429 на `N + 1`-й; Ф13-32 (б) ждал 429 на `N`-й» | **подтвердилось; сверх того — второе расхождение в том же сценарии** | Ф3-28 (`1a7f6846`): «N неверных …, затем N + 1-я … N + 1-я по каждому — 429»; Ф13-04: «`N_источник − 1` уже; ещё один — 200; второй — 429»; Ф13-32 редакции 2: «`N_адрес − 1` уже … ещё один → 429» — на одну раньше. Второе: «И» Ф13-32 редакции 2 считало `login` попыткой источника — Р9 этого документа говорит обратное («успешный вход не считается; пара `begin` + успех — одна попытка»); исправлено тем же местом, число попыток по источнику выведено (`2·N_адрес`) |
| Ф7-26, Ф7-16: сессия человека без пароля «до Ф13 строится записью» | **подтвердилось; после Ф13 — действием** | §6.1: оба «Дано» получают строителя — вход ключом |

---

## §1 Перепись производителей — составлена ДО сценариев

Перепись сделана до единого сценария: класс «Тогда без производителя» отверг эту линию больше
двух раз, и он шаг работы, а не замечание ревью (`multi-agent-flow-orchestration.md` §14а).
Единица счёта названа у каждого предиката; дом — строкой над командой; каждый предикат прогнан
на **обеих** ревизиях шапки (ствол `af0ca8f3` и линия `6acf8f19`), и где числа разошлись —
названы оба; редакция 2 перемерила их на `94352d9c` (ствол после схлопывания линии) и
`bbd25128` — столбец «линия» у каждого (шапка). Команды исполняются в рабочей копии дома службы; `R` — ревизия, о которой
спрашивают.

### §1.1 Отсутствия — на них держится каждое «сегодня: ∅»

```sh
# ДОМ: PRO-Robotech/kaname · R=af0ca8f3 (ствол редакции 1) · R=6acf8f19 (линия) · перемер редакции 2: R=origin/main (94352d9c) и R=bbd25128 дают столбец «линия»
# ∅1 — сверки утверждения нет: ни библиотеки в пине, ни проверяющего в прод-дереве
git show $R:go.mod | grep -cE 'webauthn|fido'                                                    # → 0 · 0
git grep -liE 'webauthn|passkey' $R -- '*.go' ':!*_test.go' | wc -l                              # → 7 · 7 — семь файлов НАЗЫВАЮТ предмет
git grep -liE 'CredentialAssertion|VerifyAssertion|FinishAssertion|BeginAssertion|authenticatorData' $R -- '*.go' ':!*_test.go' | wc -l   # → 0 · 0
# КОНТРОЛЬ в обратную сторону — предикат находить умеет: по слову правила уровня
git grep -l 'MethodWebAuthn' $R -- internal/assurance/level.go | wc -l                           # → 1
# ∅2 — контракта ключа и его хранилища нет (Ф7 не начата)
git ls-tree -r $R --name-only | grep '\.proto$' | while read f; do git show $R:$f; done | grep -c 'AccessKeyService'   # → 0 · 0
git ls-tree -r $R --name-only | grep -cE 'internal/migrations/.*(access_key|webauthn|passkey)'   # → 0 · 0
# ∅4 — глаголов входа ключом нет: пути семейства и виды формы объявлены без них
git show $R:internal/handler/loginlanehttp/handler.go | grep -cE '^\s+Path[A-Za-z]+ += "/iam/v1/auth/'   # → 4 · 7 (Ф3 · Ф3+Ф4+Ф5)
git show $R:internal/handler/loginlanehttp/handler.go | grep -c 'access-key'                     # → 0 · 0
git show $R:internal/domain/human_session.go | grep -cE 'FormKind = "'                           # → 3 · 6
# ∅5 — запись сессии несёт ИМЕНА способов, флагов утверждения ключа в ней нет
git show $R:internal/migrations/20260916190000_human_session_is_our_record.sql | grep -c 'presented_methods *text\[\]'   # → 1 · 1
git grep -c 'KeyAssertion(false, true)' $R -- internal/apps/kaname/api/humansession/change_password.go   # → 1 · 1 — пересчёт копии от имён
# ∅6 — глагола сброса способа входа распорядителем нет (сирота F4d-20, часть пароля и ключа)
git ls-tree -r $R --name-only | grep '\.proto$' | while read f; do git show $R:$f; done | grep -cE 'rpc (Reset|Recover)[A-Za-z]*(Password|LoginMethod|SignIn|AccessKey)'   # → 0 · 0
# КОНТРОЛЬ той же формы — глагол сброса ВТОРОГО ФАКТОРА Ф12 Р10 предикат нашёл бы; его в дереве ещё нет:
git ls-tree -r $R --name-only | grep '\.proto$' | while read f; do git show $R:$f; done | grep -c 'ResetSecondFactor'   # → 0 · 0 (Ф12 не начата)
# ∅7 — завершение восстановления у личности БЕЗ строки пароля: замещение существующей строки
git cat-file -e $R:internal/apps/kaname/api/humansession/recovery_complete.go && echo есть || echo нет   # → нет · есть (код Ф5 — на линии)
git grep -n 'has no password sign-in method' 6acf8f19 -- internal/apps/kaname/api/humansession/recovery_complete.go | wc -l   # → 2 — фраза стоит в журнальной записи и в тексте ошибки (Н1 круга 1: редакция 1 писала 1)
git grep -n 'has no password sign-in method' 6acf8f19 -- internal/apps/kaname/api/humansession/recovery_complete.go | grep -c 'fmt.Errorf'   # → 1 — отказ один, откат; kacho#2698
```

| ∅ | чего нет | на стволе `af0ca8f3` | на линии `6acf8f19` (= ствол `94352d9c`) | кто производит |
|---|---|---|---|---|
| **∅1** | сверки утверждения ключа: библиотека, проверяющий подписи, происхождения, хэша имени, бита присутствия, счётчика | 0 | 0 | **Ф7** — §3.2 её; здесь **переиспользуется** (Р1, §7 инв. 2) |
| **∅2** | строки ключа, идентификатора удостоверения, открытого ключа, испытания и его срока | 0 | 0 | **Ф7** — Р1, Р2, Р9, Ф7-34 |
| **∅3** | испытания входа, привязанного не к сессии, а к контексту формы; обнаружения без имени | — | — | **Ф13** — Р2 |
| **∅4** | глаголов входа ключом; вида формы; двух путей в перечнях Ф3 | 4 пути · 3 вида | 7 путей · 6 видов | **Ф13** — Р1; край — DoD п. 8 |
| **∅5** | флагов утверждения в записи сессии; копия уровня в ответе смены пароля пересчитывается от имён с самыми слабыми флагами | 1 | 1 | латентно до «3»; с Ф13 наблюдаемо — **`kaname#208`** (Ф13-13) |
| **∅6** | глагола сброса способа входа распорядителем | 0 | 0 | сирота F4d-20 — **`kacho#2702`** (§4.2) |
| **∅7** | завершения восстановления у личности без строки пароля | кода Ф5 нет | отказ 503, откат (и на `0cb0eddd` — тот же) | **Ф5** `#1271` по **`kacho#2698`** (исход 1, 2026-09-17): заводит строку тем же писателем ID-PW-1; редакции Ф5 и ID-PW-1 заказаны, код не посажен (Р12, §4.3) |
| **∅8** | заведения первого пароля из сессии человеком без пароля | 0 | 0 | **`kaname#213`** (§4.2) |

**Что семь файлов «называют», названо, чтобы «7» не читалось как производитель.** Три — словарь
и правило уровня (`internal/assurance/level.go`, гейт словаря `internal/check/assurance_method_vocabulary.go`,
подстановка слова в предъявление в `change_password.go`); два — обогащение утверждений токена
и хук обновления **чужой** полосы (`internal/service/token_enrichment_service.go`,
`internal/handler/iamhooks/refresh_hook_handler.go` — уходят с Ф10); один — условие модели прав
`relverdict/condition.go` (`mfa_fresh`, не в силе — Ф11 §1.7); один — сгенерированный стаб
контракта. Сверки подписи нет ни в одном (третья строка ∅1).

**Объявление беспарольного входа живёт в посадке ПЛАТФОРМЫ, и координата уточнена.** В доме
`PRO-Robotech/kacho@275240efdc` шаблон чужого компонента объявляет удостоверение ключа доступа
самостоятельным способом входа — строка `passwordless: true` в
`deploy/helm/umbrella/charts/kaname/templates/_kratos-identity.tpl` (одна; комментарий рядом
оговаривает: перевести ключ во второй фактор — отдельное решение, и `passkey` с `webauthn`
разводятся разными удостоверениями). Комментарий задачи от 2026-09-05 называл каталог
`charts/kacho-iam/` — чарт переименован вместе с выносом службы. Предикат (в клоне дома платформы,
путей-спецификаций нет намеренно — §0.3): `git grep -l 'passwordless: true' origin/main | wc -l`
→ 1. Экрана входа, который **наш**, у консоли нет: страница входа снята `#1225`, консоль ходит
потоками поставщика (`ui-future/shared/src/lib/kratos.ts`), кодировщик церемонии остался
(`ui-future/shared/src/lib/webauthn.ts` — «Страница снята (#1225), кодировщик остался»);
файлов консоли, называющих предмет, — 6, и все шесть ведут поток **поставщика**. Экран — Ф8.

### §1.2 Формы — механизм уже живёт в дереве, экземпляра для входа ключом нет

```sh
# ДОМ: PRO-Robotech/kaname · R как выше
# Ф-а — операция выдачи сессии: одна, зовётся ИЗНУТРИ транзакции выдающего глагола
git grep -n 'IssueSession(' $R -- '*.go' ':!*_test.go' | grep -vc 'func IssueSession'           # → 1 · 3 (вход · вход, регистрация, восстановление)
# Ф-б — правило уровня с флагами ключа; конструктор предъявления ключа есть, вызывающих в проде — один, и это подстановка (∅5)
grep -c '{Level3, "ключ с проверкой пользователя' internal/assurance/level.go                    # → 1
grep -c '{Level2, "утверждение ключа доступа (любое)"' internal/assurance/level.go               # → 1
git grep -n 'KeyAssertion(' $R -- '*.go' ':!*_test.go' | grep -vc 'internal/assurance/level.go'  # → 1 · 1 — change_password.go, ∅5
# Ф-в — слушатель формы: пути одним объявлением, виды формы закрытым перечнем, допуск ровно края
git grep -n 'func Paths' $R -- internal/handler/loginlanehttp/handler.go | wc -l                 # → 1
git grep -n 'func FormKinds' $R -- internal/domain/human_session.go | wc -l                      # → 1
# Ф-г — счёт неверных предъявлений: два окна, по адресу и по источнику
git show $R:internal/apps/kaname/api/humansession/rate.go | grep -cE 'FailureBySource'           # → 2 · 2
# Ф-д — признак формы: контекст в печенье, признак по виду
git grep -l 'JudgeFormToken' $R -- 'internal/apps/kaname/api/humansession/*.go' ':!*_test.go' | wc -l   # → 1
# Ф-е — событие выдачи сессии с именами способов
git grep -c 'AuditSessionIssued' $R -- internal/apps/kaname/api/humansession/issue.go            # → 2 · 2 (объявление и употребление)
# Ф-ж — страж посадки own считает предъявимые уровни из провязанных способов; провязан один пароль
git show $R:cmd/kaname/loginlane.go | grep -c 'assurance.MethodPassword}'                       # → 1 · 1
```

| форма | что уже есть | где | чем держится | что Ф13 к ней добавляет |
|---|---|---|---|---|
| **Ф-а** | операция выдачи сессии: запись, память первой аутентификации, событие — одной транзакцией writer'а вызывающего; уровень назначает она из множества предъявленного | `internal/apps/kaname/api/humansession/issue.go` | пробы Ф3 §9, Ф4-04, Ф5-03 | четвёртого вызывающего — глагол входа ключом с `KeyAssertion(uv, be)` (Р4) |
| **Ф-б** | правило уровня Р2 как данные, строки «3» и «2» ключа; конструктор предъявления ключа с флагами | `internal/assurance/level.go` | `TestAssuranceMethodVocabularyIsDeclaredOnce`, гейт единственного писателя | **ничего**: правило не правится, словарь не растёт; флаги приходят из сверки Ф7 (Ф7-28…30) |
| **Ф-в** | слушатель формы: семь путей одним объявлением, шесть видов признака, допуск ровно края по SAN, `X-Forwarded-For` одним значением, отказы `google.rpc.Status` одной функцией | `internal/handler/loginlanehttp/handler.go`, `internal/domain/human_session.go` | пробы Ф3-05, Ф3-29, Ф3-51; гейт единственного объявления путей (Ф3 §8 инв. 7) | два пути и один вид — в те же перечни (Р1) |
| **Ф-г** | счёт неверных предъявлений по адресу и по источнику, окна и величины — профилем, отказ 429 `TOO_MANY_ATTEMPTS` | `internal/apps/kaname/api/humansession/rate.go` | Ф3-28 (адрес), Ф3-29 (источник) | полоса без адреса пишет **только источник**; выдача испытания — попытка; успех обнуляет счёт по адресу (Р9, Ф13-32) |
| **Ф-д** | признак формы, привязанный к контексту (`kaname_form`) и виду; контекст сменяется выдачей сессии | `internal/apps/kaname/api/humansession/form_token.go` | Ф3-35…39 | вид `access-key-login`; испытание входа привязано к **тому же контексту** (Р2) |
| **Ф-е** | событие `iam.session.issued` с `user_id`, `session_id`, `methods`; очередь и дренаж существующие | `issue.go`, Ф3 Р14 | `TestAuditPayloadCarriesNoPersonalData`, Ф3-47 | `methods: ["webauthn"]` и `id` ключа (Р10) |
| **Ф-ж** | страж посадки `own` считает предъявимые уровни правилом из провязанных способов; сегодня провязан пароль — перечень «1» | `cmd/kaname/loginlane.go`, Ф11 Р9 | Ф11-27/28, `TestF11_28_OwnLaneWithPasswordAndSecondFactorPassesTheFloorRow` | провязка ключа — перечень получает «2» и «3» (Ф13-14) |
| **Ф-з** | ретрансляция глаголов формы краем через полосу сессии; перечень путей у края — **своё** объявление | `PRO-Robotech/kacho@275240efdc:gateway/internal/middleware/login_lane_paths.go` (ствол платформы — коммит слияния PR `#2689`) — **четыре** пути при семи у службы: пятый и два восстановления едут `kacho#2699`, `kacho#2701` | пробы края Ф3-51, Ф12-38 | два пути — своей строкой DoD (п. 8), тем же способом, что `#2699`/`#2701` |
| **Ф-и** | ответ службы краю о сессии несёт уровень; край судит по оси «1», «2», «3» и ранжирует общим правилом | `pkg/api/kaname/cloud/iam/v1/human_session_service_grpc.pb.go`; у края — `own_session_assurance.go` того же ствола | Ф11-15…20 | ничего: «3» на оси уже принимается краем (Ф13-11 наблюдает это) |

### §1.3 Что перепись изменила в замысле

1. **Своего кода у Ф13 мало, и это следствие переписи, а не скромность.** Первая прикидка
   заводила «сервер входа ключом» рядом с сервером церемонии Ф7. На дереве каждый механизм,
   кроме сверки утверждения и самих глаголов, **уже есть**: сессию выдаёт Ф-а, уровень считает
   Ф-б, форму защищает Ф-д, попытки считает Ф-г, событие пишет Ф-е. Ф13 — два глагола формы,
   испытание с привязкой к контексту, сверка рукоятки и провязка (§4.1). Сверка утверждения —
   **та же**, что у глаголов Ф7: второй в дереве не будет (§7 инв. 2).
2. **Глагол входа — не RPC, и вопрос о причине освобождения снят по существу, а не решён.**
   Ф7 Р11 ждала от Ф13 причины из закрытого словаря `exempt_reason` либо расширения переписи
   публичных RPC. Перепись показала: вход паролем, регистрация и восстановление — глаголы
   **слушателя формы**, не публичные RPC; записи каталога у них нет by construction, перепись
   `publicauthzcensus` их не видит, а дверь на них — допуск ровно края, признак формы и счёт
   попыток (Ф3 Р2, Р16). Глагол входа ключом кладётся рядом с ними (Р1). Расширения переписи не
   требуется — §3 Д1.
3. **Запись сессии не несёт флагов утверждения (∅5), и это меняет одну вещь: копии уровня
   обязаны читать запись.** Ф11 Р1 говорит «каждое предъявление со своими флагами», схема
   хранит имена. Пересчёт от имён теряет «3» by construction; сегодня это невидимо — «3» не
   производит никто, — с Ф13 видимо на первой же смене пароля из сессии «3». Ф13 **не** заводит
   колонку флагов: читателя у неё нет — уровень записан и читается как есть (Ф11 Р1), а
   пересчёт копии есть второй путь вывода, который Ф11 Р1 отвергла. Предмет — `kaname#208`;
   сценарий — Ф13-13.
4. **Путь при утрате ключа — решён, производитель назван и не посажен, и это названо первым
   (§0).** Прежняя формулировка задания («объявлен и проверен пробой») исполнима наполовину:
   проба написана (Ф13-25), её условие — посадка производителя, назначенного решением
   `kacho#2698` (исход 1: восстановление заводит способ; Ф5 `#1271`, писатель ID-PW-1); вторая
   половина пути (сброс распорядителем) — сирота без фазы (`kacho#2702`) и путём не является.
   Ф13 не заводит ни глагола заведения пароля, ни глагола сброса: первый из живой сессии —
   предмет `kaname#213`, который решением `#2698` **не открыт** (там основание — сессия, здесь —
   владение ящиком); второй — решение эпика.
5. **Рукоятка — величина Ф7, чей единственный читатель здесь; поэтому решена здесь и
   становится колонкой строки ключа.** Ф7 не назвала, что церемония кладёт в `user.id`, и не
   хранит рукоятку — по своему же правилу «колонка без читателя не заводится, читатель приходит
   своей приёмкой» (Ф7 Р1, Р9). Читатель появился (Р3), и вместе с ним — колонка, писатели
   (церемония Ф7 и перенос Ф7-23) и сценарий, наблюдающий расхождение сохранённого с
   предъявленным (Ф13-06 «з»). Перенесённый ключ без рукоятки в источнике полосу входа не
   проходит, и таких ключей перепись называет числом (Ф13-24).

---

## §2 Решения — ратифицировать явно, ревьюер проверяет каждое

Каждое решение несёт, что отвергнуто и почему; там, где решение дописывает к глаголу соседа, это
сказано словом «дописывает» и стоит в §3 своей строкой.

### Р1. Поверхность — ДВА глагола семейства Ф3 на origin консоли; глагол входа не RPC и записи каталога не несёт

| глагол | путь · метод | сессия | вид признака формы | что делает |
|---|---|---|---|---|
| испытание входа | `POST /iam/v1/auth/access-key/begin` | — | `access-key-login` | выдаёт испытание, **не назвав человека**: привязано к контексту формы, одно на контекст, живёт срок испытания Ф7 |
| вход | `POST /iam/v1/auth/access-key/login` | — (выдаёт) | `access-key-login` | принимает утверждение; сверяет тем же проверяющим, что Ф7; находит человека по идентификатору удостоверения; сверяет рукоятку; выдаёт сессию — как `POST /iam/v1/auth/login` |

**Почему то же семейство, тот же слушатель и та же ретрансляция — довод Ф3 Р2 дословно и без
дополнений:** печенье обязано быть выдано на origin консоли, всё под `/iam/v1/` там раздача
отдаёт краю, написание подпутём — ради одного написания семейства в перечне освобождённых путей
края. Ф12 Р4 положила шесть глаголов сюда же тем же доводом. Второе семейство (`/iam/v1/access-key/…`)
и суффиксы `:verb` отвергнуты Ф12 Р4 и здесь не переоткрываются.

**Глагол входа — свой, как Ф7 §4.5 и требовала, и НЕ один из шести глаголов Ф7.**
`BeginAssertion` и `FinishAssertion` Ф7 связывают испытание и утверждение с **вызывающим** —
человеком, из чьей сессии предъявлено (Ф7 Р11, Ф7-06, Ф7-51, Ф7-55); у входа вызывающего нет
by construction, и наследовать это связывание он не может. Ф7 §10 п. 26 ждала от Ф13 причины
освобождения из закрытого словаря контракта либо расширения переписи публичных RPC — **вопрос
беспредметен**: глагол формы не есть публичный RPC. Дверь у него та же, что у входа паролем, и
названа: допуск слушателя ровно краю по SAN (Ф3 Р16), признак формы (Ф3 Р12), счёт по источнику
(Р9), единый отказ (Р7). Что глагол не попадает в перепись публичных RPC — свойство слушателя, а
не пропуск переписи: она обходит дескрипторы контракта, а у формы дескриптора нет.

**Два глагола, а не один, — потому что испытание обязано быть выдано ДО того, как аутентификатор
подпишет.** Норма требует свежего испытания от доверяющей стороны на каждое утверждение (Ф7 Р2:
«to avoid replay attacks»); объединить выдачу и приём в один запрос нельзя by construction.
Отвергнуто: **брать испытание у `BeginAssertion` Ф7** — оно привязано к сессии вызывающего
(Ф7-55), а у входа сессии нет; и **испытание без состояния** (подпись службы над случайным
значением и сроком) — однократность (Ф13-08) тогда нечем держать: повтор в пределах срока проходил
бы, против Ф7-53 на соседней полосе.

**Форма запроса и ответа — JSON, camelCase** (`api-conventions.md` §Naming). Ответ испытания —
объект `publicKey` в той форме, которую браузерный интерфейс принимает разбором из JSON без
переложения: `challenge` · `rpId` — имя доверяющей стороны Ф7 Р2 · `timeout` — срок испытания
Ф7 в миллисекундах · `userVerification: "preferred"` — литерал Ф7 Р9, тот же, что у испытания
предъявления Ф7-42 · `allowCredentials: []` — **пустой перечень**, и это не умолчание, а
предмет Р2. Запрос входа — `csrfToken` и `credential` с полями `id`, `rawId`, `type`
(`"public-key"`), `response.clientDataJSON`, `response.authenticatorData`, `response.signature`,
`response.userHandle` — **все обязательны**; поле вне этого перечня отвергается, не глотается
(`api-conventions.md` §«Принято-и-проигнорировано»; Ф3-05), и это относится к `secondFactor`,
`email`, `password` и к полям сериализации браузера, которых форма не объявляет
(`clientExtensionResults`, `authenticatorAttachment`) — их снимает консоль (Ф8), а не
принимает служба. **Цена этого для клиента вне нашей консоли названа** (ban #18, ось
«разработчик»): стандартная сериализация браузера (`PublicKeyCredential.toJSON()`) несёт оба
поля всегда, поэтому клиент, отправляющий ответ браузера как есть, получает 400 с именем поля на
первом же вызове; отказ называет поле и тем восстанавливает следующий шаг, а страница арендатора
(§4.1 п. 14) перечисляет, какие поля снять перед отправкой. Принимать поля и не читать —
запрещено (исход 2 из трёх, `api-conventions.md` §«Принято-и-проигнорировано»), и цена
отвергнутого исхода выше: клиент, чьё поле проглочено, уверен, что оно применено. Ответ входа — **побайтово та же форма**, что у входа паролем (Ф3-01): `user`,
`session` с `assuranceLevel`, `emailVerified`, `passwordChangeRequired: false`; `Set-Cookie`
носителя и **нового** признака формы.

**Отвергнуто:** отдельный слушатель либо порт для ключей (Ф3 Р2 — печенье не дошло бы до
консоли); RPC `AccessKeyService/Login` с записью каталога — потребовало бы вызывающего, которого
нет, и второго механизма выдачи печенья на крае (Ф3 Р2: край не переводит формы в RPC).

### Р2. Обнаружение БЕЗ имени: испытание не называет человека, `allowCredentials` пуст, испытание привязано к контексту формы и однократно

Полоса, ради которой Ф7 Р9 потребовала обнаруживаемое удостоверение, — вход, **не называющий
человека**: испытание выдаётся любому держателю контекста формы, аутентификатор сам предлагает
удостоверения, у которых имя доверяющей стороны совпадает, и человек выбирает. Отсюда три
свойства испытания входа, и каждое наблюдаемо:

- **`allowCredentials` пуст.** Непустой перечень требует знать человека до испытания, то есть
  спросить у него адрес; ответ службы на «адрес → перечень удостоверений» есть оракул существования
  адреса и наличия у него ключей (Ф1 §7 инв. 3), а маскировать его синтетическими идентификаторами —
  второй механизм ради полосы, которой у продукта нет. Отвергнуто: **вход по адресу и ключу**
  (identifier-first) как вторая форма той же полосы — две полосы одного механизма без решённого
  различия (`architecture.md` §«Параллельные полосы одного механизма»). Цена названа в Р11:
  перенесённый ключ, чьё удостоверение **не** обнаруживаемое, в эту полосу не попадёт;
- **привязано к контексту формы** (`kaname_form`, Ф3 Р12) — единственному состоянию, которое у
  анонимного вызывающего есть. Утверждение, собранное на испытании другого контекста, — единый
  отказ (Ф13-06 «ж»), как Ф7-55 на полосе сессии. **Одно живое испытание на контекст:** повторное
  `begin` замещает прежнее (Ф13-03) — так число живых испытаний ограничено числом контекстов, а не
  числом запросов;
- **однократно и срочно:** срок — **тот же литерал**, что у испытаний Ф7 (Ф7 §7 инв. 5: одна
  величина на все процедуры; третья процедура величины не заводит); потребляется **первым**
  предъявлением, каким бы ни был исход (Ф13-08): повторный успех — отказ (повтор), повторная
  попытка после отказа — отказ (испытание сгорело), новая — новое `begin`. Отвергнуто:
  оставлять испытание живым после отказа — оно позволяло бы перебирать подписи над одним
  испытанием в пределах его срока; счёт по источнику это ограничивает, но однократность делает
  перебор невыразимым, а не только дорогим.

**Требование проверки пользователя — «предпочтительна», и это литерал Ф7 Р9, не решение
здесь.** «Обязательна» отвергла бы утверждение ключа без проверки целиком — против Ф11 Р2
(«2» у любого утверждения ключа); «не требуется» гасила бы флаг у ключа, который проверку умеет,
— и «3» не достигалась бы (Ф7 Р9 дословно). Ф13-01 наблюдает равенство объявленному — тем же
способом, что Ф7-42.

**Дано «человек не назван» и дано «ключ обнаружен» — разные факты, и наблюдает их разное.**
Первый — в запросе `begin` нет ни одного поля, называющего личность (Ф13-01: форма из одного
поля `csrfToken`); второй — то, что аутентификатор **предложил** ключ, службе не наблюдаемо
вовсе: до неё доходит уже утверждение. Поэтому «ключ не появился в полосе» (Р11) не имеет
«Тогда» на нашей стороне, и это сказано, а не умолчано.

### Р3. Человека называет идентификатор удостоверения; рукоятка сверяется и хранится СО СТРОКОЙ ключа; её значение у ключей Ф7 — ОТДЕЛЬНОЕ СЛУЧАЙНОЕ значение человека, а не платформенный идентификатор

**Кого назвал вход — владельца строки ключа, найденной по идентификатору удостоверения.**
Идентификатор глобально уникален (Ф7 Р1), строка ключа несёт человека (Ф7 Р1), поэтому
утверждение, чья подпись сверилась открытым ключом строки, называет ровно одного человека без
второго вопроса. Это существо сверки Ф7 §3.2 без изменений; отличие от Ф7-06 одно и оно
несущее: там названный ответом человек обязан **равняться вызывающему**, здесь вызывающего нет,
и названный — это тот, **кому выдаётся сессия**.

**Рукоятка — обязательна в утверждении и сверяется со строкой.** Рукоятка (`user.id` церемонии
регистрации; аутентификатор возвращает её полем `userHandle` в каждом утверждении обнаруживаемого
удостоверения) — второе имя человека в удостоверении, и норма требует от полосы без названного
человека сверять его с записью, которой принадлежит удостоверение (Ф7 Р2 перемеряла норму по
тексту; здесь читается тот же §7.2, шаг о `userHandle`). Утверждение без поля — отказ формы с
именем поля (Ф3-05); утверждение с рукояткой, не равной сохранённой у строки, — **единый
отказ** (Ф13-06 «з»): различимый текст сказал бы предъявителю, что подпись сверена и ключ
признан, а не сходится лишь рукоятка, — тот же довод, что у Ф7-49 и Ф7-51.

**Рукоятка хранится со строкой ключа, и это дописывает к Ф7 Р1 по её собственному правилу.**
Ф7 Р1 отвергла колонку без читателя и назвала условие возврата: «появится читатель — колонка
возвращается как свойство … вместе со сценарием, наблюдающим расхождение хранимого признака с
предъявленным». Читатель — эта полоса; сценарий расхождения — Ф13-06 «з»; писателей два, и оба
Ф7: церемония регистрации кладёт в строку то, что положила в `user.id`, перенос (Ф7-23) — то, что
несёт источник. Перечень ключей человека рукоятку **не показывает**: читателя снаружи у неё нет.

**Где эта колонка названа у Ф7 — перемерено редакцией 4, и число изменилось.** Редакции 1…3
писали: «Ф7 значения `user.id` не называла ни одной строкой (перемерено: `grep -ci
'userhandle\|user\.id' docs/engineering/acceptance/access-keys-are-ours.md` → 0)». **На день
редакции 4 это не так**: редакция 12 приёмки Ф7 переутвердила перечень колонок строки ключа и
назвала рукоятку **двенадцатой** — но назвала её **только счётом**, с пометкой «не решение этого
документа», и отослала решение о значении сюда. Предикат перемерен на дереве редакции 12 и даёт
**4** (на `cbbac984`, то есть на редакции 11, он давал **0**); из четырёх попаданий **одно** —
строка перечня колонок Р1, остальные три — перемер §4.3, где та же величина названа **решённой
соседом**. То есть предикат ловит **перечисление** колонки и **отсылку** решения, а не само
решение, и это сказано здесь, чтобы «не ноль» не читалось как появление второго дома. **Решение о значении рукоятки
по-прежнему принадлежит этой приёмке, и другого дома у него нет.**

**Значение у ключей, заведённых Ф7, — ОТДЕЛЬНОЕ СЛУЧАЙНОЕ значение человека: шестьдесят четыре
случайных байта, своя таблица, одна строка на человека.** Это решение **редакции 4**, и оно
заменяет решение редакций 1…3 — «платформенный `id` человека (`usr-…`, F4d Р10) как байты».

**Прежнее решение было верным при том, что было известно, и его доводы названы дословно, а не
переписаны:** `id` неизменяем на всю жизнь личности (ban #15), не содержит ни адреса, ни имени
(норма запрещает личные данные в рукоятке) и один и тот же у всех ключей человека. Ни один из трёх
доводов ложным не оказался. **Изменилось знание — о том, КУДА рукоятка уезжает и что с ней после
этого можно сделать.** Разбор поверхности установил четыре факта, ни один из которых прежнему
решению известен не был:

1. рукоятка **записывается в аутентификатор** — она часть удостоверения, а не нашей строки;
2. у синхронизируемого удостоверения она уезжает в **синхронизируемое хранилище держателя**, то
   есть в чужую инфраструктуру, к которой у нас нет ни записи, ни чтения;
3. она возвращается в **каждом** утверждении обнаружимого удостоверения — то есть предъявляется
   снова и снова, а не лежит однажды записанной;
4. она **видна человеку** в интерфейсе управления удостоверениями операционной системы.

**Следствие, ради которого решение и принято: отозвать её нечем.** Писать в аутентификатор и в
чужое хранилище мы не можем — ни исправить значение, ни стереть его. Пока рукоятка есть **наш**
идентификатор человека, развязать учётную запись с ним **невозможно**: наши строки снимутся —
человек, ключи, привязки, — а значение в чужом хранилище **останется называть снятое**, и будет
предъявляться при каждом утверждении того удостоверения, которое человек у себя не удалил.

**Реализация решения названа целиком, и механизм развязывания — не обещание, а конструкция:**
отдельная таблица, **шестьдесят четыре случайных байта на человека**, **каскад удаления от строки
человека**. Каскад **и есть** механизм развязывания: снят человек — снято и значение, и ни одна
наша строка больше не связывает с ним то, что лежит в чужом хранилище. Байты берутся у источника
случайности, а не выводятся ни из чего нашего — производное от `id` или от адреса вернуло бы ровно
то свойство, от которого решение уходит.

**Шестьдесят четыре — не круглое число, а ВЕРХНЯЯ ГРАНИЦА нормы**, и взята она целиком намеренно:
норма WebAuthn отводит `user.id` от 1 до 64 байт, и значение во всю ширину не оставляет запаса,
который кто-нибудь позже примет за место для осмысленного содержимого. Ограничение уже стоит на
колонке рукоятки у строки ключа (`>= 1` и `<= 64` байт), то есть новое значение ложится в
существующую форму и не требует её расширять — проверено чтением схемы, а не предположено.

**Почему значение на ЧЕЛОВЕКА, а не на ключ.** Норма требует, чтобы удостоверения одного человека
у одной доверяющей стороны несли **одну** рукоятку: иначе аутентификатор считает их разными
учётными записями и показывает человеку несколько записей вместо одной. Значение на ключ
рассорило бы это представление, и цена была бы видна человеку, а не нам. Одна строка на человека
даёт то же, что давал `id`, — одинаковость у всех его ключей, — не отдавая наружу ничего, что
называло бы что-то ещё.

**Колонка рукоятки у строки ключа при этом остаётся, и два предмета не путаются.** В строке ключа
лежит то, что **несёт данное удостоверение**, — с ним и сверяется каждое утверждение (Ф13-06 «з»);
в своей таблице лежит то, что церемония **кладёт в `user.id` новых** регистраций. У человека с
перенесённым ключом и ключом Ф7 рукоятки по-прежнему **разные** — источник переноса нёс своё, — и
потому сверка идёт **со строкой ключа**, а не с человеком. Снять колонку у ключа и сверять с
человеком значило бы отказать перенесённым ключам всем разом.

**Существующих ключей, которым досталось бы прежнее значение, НОЛЬ — это измеренный факт, а не
допущение.** Замер на **живой посадке** (кластер `kacho`, база `kaname` на
`kacho-umbrella-pg-iam-0`, 2026-09-22): `select to_regclass('kaname.user_access_keys')` → пусто —
таблицы ключей **нет**; `select count(*) from public.goose_db_version where version_id in
(20260917220000, 20260917221000)` → `0` — обе миграции ключа **не применены**, голова применённых
`20260917210000`. Контроль в обратную сторону, без которого пустой ответ неотличим от чужой базы:
`select count(*) from kaname.users` → `1`; таблиц в схеме `kaname` — **55**, и **единица счёта
названа, потому что источники дают разные числа**: `select count(*) from pg_tables where
schemaname='kaname'` → **55**, а `information_schema.tables` по той же схеме → **56**, из них
`BASE TABLE` **55** и `VIEW` **1**. Взят `pg_tables`, то есть единица — **таблица**, не отношение. **Граница замера
названа:** измерена **одна** посадка и **один** момент.

**И сказано, что было бы, если бы их было не ноль** — потому что на другой посадке может быть
иначе, и решение не вправе этого умалчивать: новое значение кладётся **только новым**
регистрациям, а старые ключи проверяются **против своего** — против того, что лежит в их
собственной строке. Переписывать рукоятку у уже заведённого ключа нечем by construction (см.
четыре факта выше), и попытка это сделать сломала бы вход тем самым людям, ради которых перенос и
затевался. Механизм развязывания у таких ключей остаётся неполным, и это **цена**, а не
недосмотр: она названа здесь, чтобы её не приняли за свойство.

**Перенесённый ключ, чей источник рукоятки не несёт, полосу входа НЕ проходит — и это сказано
числом, а не умолчано.** Что именно источник несёт по каждому ключу, устанавливает перепись
Ф7-21; Ф13 **дописывает** к её «Тогда» одну пару — «ключей, чья строка получила рукоятку · без
рукоятки» — и к DoD Ф7 §9.1 п. 6 чтение этой пары (§3 Д28, Д30 — дописано, с ценой: одна
величина в выводе переписи и в приложении к DoD; Ф7-21 не переписывается), и ключ без рукоятки
в этой полосе получает единый отказ (Ф13-24), потому что сверить нечего, а принять без сверки значило бы принять поле и не смотреть
на него. Второй способ войти у такого человека — пароль (если есть) либо путь Р12. Отвергнуто:
пропускать сверку у строк без рукоятки — «принято-и-проигнорировано» ровно на тех ключах, о
которых известно меньше всего.

**Отвергнуто: не читать рукоятку вовсе и находить человека только по идентификатору
удостоверения.** По существу это безопасно — подпись доказывает владение названным ключом, а
владелец у строки один, — но поле пришлось бы либо снять с формы (тогда консоль вырезает из
ответа браузера обязательное поле нормы — форма продукта, требующая не следовать норме), либо
принять и не читать (запрещено). Сверка стоит одного сравнения и даёт сценарию расхождения предмет.

### Р4. По утверждению выдаётся НАША сессия одной операцией Ф3; множество предъявленного — `{webauthn}` с флагами этого утверждения; уровень — Ф11 Р2/Р3

Вход ключом зовёт **ту же операцию выдачи**, что вход паролем, регистрация и восстановление
(Ф-а), внутри своей транзакции, с предъявлением `KeyAssertion(userVerified, backupEligible)`,
где оба флага — то, что ключ сообщил **в этом** утверждении (Ф7-28…30 доносят их до правила;
Ф11 Р3 — различитель из этого утверждения, не из памяти о регистрации). Уровень назначает
операция правилом: «2» — любое утверждение; «3» — проверка пользователя выполнена и резервное
копирование не допускается. Ни строки правила, ни слова словаря Ф13 не добавляет (Ф-б).

**Что из состава записи Ф3 Р1 полоса заполняет иначе, чем вход паролем, — ровно одно поле:**
множество предъявленного — `{webauthn}` вместо `{password}`. Момент аутентификации, момент
последнего предъявления, срок 24 ч, подтверждённость адреса, `passwordChangeRequired: false` —
как у Ф3-01. Носитель, атрибуты печенья, смена контекста формы выдачей — Ф3 Р3, Р12 дословно.
«Один вход — одна запись»: вход ключом при живой сессии в том же браузере выдаёт новую запись,
прежняя живёт до срока либо отзыва (Ф1-13, Ф3-51 положительный контроль).

**Три сценария Ф11 исполняются здесь под своими номерами** (Ф11 §5.2: «Ф13 — 03, 04, 05»):
Ф13-10 = Ф11-03, Ф13-11 = Ф11-04, Ф13-12 = Ф11-05. Нумерация Ф13 дана, чтобы ведомость §6 несла
их производителя; утверждения — Ф11 дословно.

**Копии уровня читают запись, и Ф13 это наблюдает, потому что до неё наблюдать было нечем.**
Ф11 Р1: копии уровня — чтения записи, а не второе состояние. На дереве одна копия — ответ смены
пароля — уровень **пересчитывает** от имён способов с самыми слабыми флагами (§1.1 ∅5): для
сессии «3» ответ назвал бы «2» при записи «3». Ф13-13 требует «3» и в ответе смены пароля, и в
ответе церемонии повышения; производитель — `kaname#208`, не эта фаза (§4.3). Колонки флагов в
записи сессии Ф13 **не заводит**: у неё нет читателя — уровень выведен при выдаче и записан, а
пересчёт от записи есть второй путь вывода (Ф11 Р1, Р2 монотонность).

### Р5. Второй фактор при входе ключом НЕ требуется и НЕ принимается; внутри сессии ключа он предъявляется и уровня не меняет — обе стороны названы

| сторона оси | решение | почему |
|---|---|---|
| **вход ключом у человека с заведённым вторым фактором** | кода **не требуется**; сессия «2» либо «3» по флагам; глагол с полом «2» проходит (Ф13-15) | обязательность — свойство действия, не личности (Ф12 Р3); ключ **сам** производит «2» (Ф11 Р2), и требовать второго фактора ради «2» — класс «два правила об одном поле» (`api-conventions.md` §«Неисполнимая возможность»); человек, заведший фактор ради действий с полом «2», получает его ключом без кода |
| **поле `secondFactor` у глагола входа ключом** | **нет**: лишнее поле — отказ формы 400 (Ф13-16) | код к ключу ничего не прибавляет: правило Ф11 Р2 даёт `{webauthn, totp}` тот же уровень, что `{webauthn}`; поле, которое сверяют и не читают ни одним следствием, — «принято-и-проигнорировано» в чистом виде. Поле у входа паролем (Ф12 Р4) остаётся: там оно и есть производитель «2» |
| **предъявление второго фактора ВНУТРИ сессии, выданной ключом** | `POST /iam/v1/auth/step-up` с `totp` либо `lookup_secret` **проходит** как предъявление (Ф11 Р5: носитель перевыпущен, момент последнего предъявления сдвинут); уровень **не меняется** — «2» остаётся «2», «3» остаётся «3»; ответ церемонии называет достигнутый уровень и **пустой** перечень недостающего (Ф13-17) | монотонность Ф11 Р2: добавленное предъявление уровень не понижает, а строки выше «3» нет; окно свежести правки своих данных освежается — это и есть предмет предъявления внутри сессии (Ф11-32). Отвергнуто: отказ «фактор не нужен» — предъявление, которое проходит проверку, отвергать нельзя, и человеку, которому нужна свежесть, оно нужно |
| **предъявление пароля внутри сессии, выданной ключом** | проходит как Ф11-10: уровень «2»/«3» прежний, носитель перевыпущен (Ф13-17 «б») | множество накапливается: `{webauthn, password}` — по-прежнему строка ключа |

**Обратное направление — ключ как второй фактор при входе паролем — не предмет этой фазы**
(Ф12 §0.2 отдаёт его Ф7 и Ф13 одной строкой, §4.2 здесь): церемония `step-up` со способом
`webauthn` внутри сессии, выданной паролем, — глаголы утверждения Ф7 из сессии вызывающего
(Ф7-06), и что даёт правило в этом случае, Ф11 Р2 уже сказала («2» либо «3» по флагам).
Провязка `webauthn` в `step-up` — Ф12 Р4 говорит «`webauthn` до Ф7 — 400»; после Ф7 это её
ветвь, и здесь она не переписывается.

### Р6. Две полосы входа сосуществуют и не знают друг о друге; выбор полосы — у клиента

Человек с паролем и ключом входит любой полосой: паролем — сессия `{password}` уровня «1»
(Ф3-01), ключом — `{webauthn}` уровня «2»/«3». Ни одна полоса не отключает другую и не спрашивает
о ней: вход паролем при заведённых ключах даёт «1», как при заведённом втором факторе (Ф12 Р3);
вход ключом при заведённом пароле не требует пароля и не читает его строки. Две сессии, выданные
двумя полосами, — две записи (Ф1-13), и снятие одной не касается другой иначе, чем через
причины отзыва Ф3 Р9 и Р8 здесь.

**«Откат к паролю» — не механизм, а свойство двух независимых полос:** человек, чей ключ
недоступен здесь и сейчас (другое устройство, разряжен, забыт), входит паролем, если он у него
есть. Служба ни одного «переключателя» не несёт; консоль показывает обе кнопки — Ф8.

**Отвергнуто:** политика «ключ заведён — пароль отключён» (ломает откат и запирает при утрате
ключа — Р12); «вход ключом требует хотя бы одного пароля» (Ф7-24: людей без пароля источник
несёт, и после переноса они у нас есть — им запретить вход значит запереть их).

### Р7. Личность без пароля — первоклассная: входит ключом, единый отказ — тот же текст, что у пароля

Личность, у которой единственный способ входа — ключ (Ф7-24 считает таких на переносе; после
Ф13 их сессия строится **действием**, а не записью — §6.1 к Ф7-26, Ф7-16), входит ключом как
любая другая; полоса пароля отвечает ей единым отказом Ф3-02 (случай C — «личность без строки
способа входа»), и это не дефект, а вторая сторона оси Ф13-19.

**Единый отказ входа ключом — побайтово тот же, что единый отказ входа паролем:** `UNAUTHENTICATED`
/ **401**, текст `authentication failed`, без `Set-Cookie`, `details` — пустой массив (Ф3 Р2:
«текст отказа входа — один на все причины»; F4d-16). Метки, чьё «Тогда» называет единый отказ,
**выводятся** предикатом §5 (как §3.0 у Ф7): сегодня их **семь** — Ф13-03 · Ф13-06 (одиннадцать
ветвей «а»…«л») · Ф13-08 · Ф13-20 · Ф13-21 · Ф13-24 · Ф13-25. Отвергнуто: свой
текст для ключа («ключ не распознан») — различимость сказала бы предъявителю, что перед ним
полоса, у которой **есть** что распознавать, и разошлась бы с Ф7 §3.0 на соседней полосе того
же проверяющего.

**Неразличимость по времени** — критерий Ф1-48…50 своей строкой (как Ф1-51 у восстановления):
работа по неизвестному идентификатору удостоверения выполняется наравне с работой по известному
(Ф7-09 на полосе сессии; Ф13-06 «л» здесь). Ф13 своей величины не вводит: внесённое различие
и порог — Ф1 §3.9 правилом вывода.

**Что личность без пароля НЕ может — названо, а не умолчано:** завести пароль из сессии
(`kaname#213` — глагола нет: смена требует текущего) и — **до посадки Ф5 по `kacho#2698`** —
вернуть доступ самостоятельно при утрате всех ключей (восстановление на дереве требует строки;
решением оно её заводит). Оба — чужие производители; Ф13 их называет в §4.2 и в Р12.

### Р8. Снятие ключа гасит ПРОЧИЕ сессии человека, текущей уровня не понижает — по образцу Ф12 Р9; это дописывает к глаголу снятия Ф7

Ф7-25 наблюдает снятие ключа тем, что утверждение снятым ключом перестаёт проходить, и о живых
сессиях молчит — до Ф13 сессий, выданных ключом, не было. С Ф13 они есть, и вопрос «что с
сессией, выданной снятым ключом» обязан иметь ответ: ключ снимают чаще всего потому, что его
**лишились** (устройство утеряно либо украдено), и живая сессия на нём — держатель устройства.

**Решение — образец Ф12 Р9 дословно:** снятие ключа глаголом Ф7 (`AccessKeyService/Revoke`)
гасит **все прочие** сессии человека — снятием записей и отсечкой, как смена пароля (Ф3 Р6,
Ф1 Р4), с причиной отзыва **`access-key-revoked`** — **седьмой** в перечне причин: пять Ф3 Р9,
шестая — `second-factor-reset` Ф12 Р10, эта — вторая из наших; в дереве причины отзыва, которые
производит сама служба, — константы у носителя (`RevokeReasonLogout`,
`RevokeReasonPasswordChange` в `internal/domain/human_session.go`; прочие причины Ф3 Р9 —
состояние личности и причина из запроса администратора), и новая приходит **своей константой
туда же**, а не строкой в месте вызова; отказ предъявителю тот же, причина — в журнале и
счётчиках, — и с актором — самим человеком; **текущая** сессия
остаётся, её уровень не понижается (Ф11 Р2: понижение в пределах сессии невыразимо). Это
**дописывает** к «Тогда» Ф7-25 одну строку и ни одной не отменяет (§3 Д3); держатель — Ф13-21.

**Цена названа:** человек с тремя ключами, снимающий давно не используемый третий с ноутбука,
теряет сессию телефона. Отвергнуто: **гасить только сессии, выданные снятым ключом** — запись
сессии несёт множество предъявленного словами словаря (Ф3 Р1: состав закрыт), а не идентификатор
ключа; завести его значило бы расширить состав записи ради одного читателя и разойтись с Ф12,
которая при снятии фактора гасит все прочие; **не гасить ничего** — украденное устройство
оставалось бы вошедшим до срока сессии (24 ч), и «снять ключ» не означало бы «лишить доступа».

**Сброс ключа распорядителем** этим решением не заводится (`kacho#2702`); блокировка и
принудительный выход гасят сессии, выданные ключом, как всякие (Ф3 Р9 — пять причин, механизм
общий).

### Р9. Частота: полоса без адреса пишет ТОЛЬКО источник; выдача испытания — попытка; величины — окно Ф3 Р10, ручек не добавляется

У полосы входа ключом нет адреса до самой сверки — человек не назван (Р2), — поэтому из двух
счётов Ф3 Р10 ведётся **один: по источнику**, тем же окном `N_источник`/`T_источник` и тем же
отказом `RESOURCE_EXHAUSTED` / 429, `reason = TOO_MANY_ATTEMPTS`, `Retry-After`. Попыткой
считаются: **выдача испытания** (`begin`) и **каждый единый отказ входа** (Ф13-06); не
считаются: отказ формы (признак, отсутствующее поле — как Ф3-05), отказ по частоте сам по себе
и успешный вход. Успешный вход ключом **обнуляет счёт по адресу** человека — как всякий успешный
вход (Ф3 Р10: «успешный вход обнуляет счёт по адресу»; адрес после сверки известен); счёт по
источнику успех не трогает — как у Ф3. Держатель обнуления — **Ф13-32**: без него реализация, не
трогающая счёт по адресу, была бы зелена на всём §5 (Н5 круга 1).

**Почему выдача испытания — попытка.** Испытание — строка состояния со сроком; полоса без
адреса и без сессии выдаёт её любому, кто держит контекст формы, а контексты бесплатны. Не
считать `begin` значило бы дать анонимному источнику неограниченный рост хранилища испытаний в
пределах срока; ограничение «одно на контекст» (Р2) не спасает — контекст чеканится на каждый
запрос признака. Цена: у законного человека пара `begin` + успех стоит **одной** попытки
источника, пара `begin` + отказ — двух. Отвергнуто: **свой счёт для испытаний** — второе окно
об одном предмете с двумя величинами и двумя ручками там, где Ф3 Р10 уже требует четыре;
**пропуск `begin` из счёта** — рост без предела.

**Ручек посадки Ф13 не добавляет ни одной** (Ф7 Р2: три ручки ключа — имя доверяющей стороны,
перечень происхождений, перечень алгоритмов; Ф3 Р3, Р10: срок сессии, домен, четыре величины
частоты). Срок испытания — литерал Ф7. Страж старта поэтому новых ветвей не получает; что
полоса провязана — видно самоотчётом (Ф13-14).

### Р10. Журнал, наблюдаемость — существующее событие с именем способа и `id` ключа; клетки исходов полосы

**Событие выдачи сессии — то же** `iam.session.issued` (Ф-е), `methods: ["webauthn"]`, и сверх
состава Ф3 — `access_key_id` со значением `id` ключа (форма `ak-…`, Ф7 Р10 — адрес, которым
ключ назван наружу; идентификатор удостоверения в событие **не выходит**, как и в Ф7). Нового
вида события у входа ключом нет: вход есть выдача сессии, и различает входы поле `methods`
(Ф3 Р1: «глаголы выдачи различаются множеством предъявленного и своим событием»). Отказы входа
события не порождают — как у пароля (Ф3-47).

**Клетки счётчиков — по исходу полосы ключа, с нулевыми клетками** (Ф3 Р14): испытание выдано ·
вход состоялся · единый отказ (по причине, которая живёт в журнале службы, не в ответе: подпись ·
**удостоверение неизвестно** — сюда же попадает снятый ключ, Р15 · происхождение · хэш имени ·
присутствие · счётчик · испытание · рукоятка · блокировка) · отказ по частоте · хранилище не
ответило. Причина отказа **в ответе не различима** (Р7) и **в счётчике различима** — ровно как у
Ф3-48; у каждой клетки перечня есть исход, который её увеличивает (Ф13-30).

**Секрета в журнале нет by construction:** ни подписи, ни данных аутентификатора, ни носителя
событие не несёт (`TestAuditPayloadCarriesNoPersonalData` на новом поле).

### Р11. Перенесённый ключ, не появившийся в полосе, — число, названное переписью, а не умолчание; второй формы входа ради него не заводится

Ф7 §4.5 и §10 п. 16 отдали сюда: «перенесённый ключ может не появиться в полосе Ф13, и узнать
это заранее нечем». Причин две, и различаются они тем, где наступают:

| причина | где наступает | наблюдаемо ли нам | что делает Ф13 |
|---|---|---|---|
| удостоверение **не обнаруживаемое** — источник обнаруживаемости не несёт (Ф7-21 «не установлено») | в аутентификаторе: браузер ключа не предложит | **нет** — до службы ничего не доходит (Р2) | **ничего**: второй формы входа (по адресу, с `allowCredentials`) не заводит — Р2 отвергла её оракулом. Человек входит паролем, если есть; без пароля — Р12 |
| строка ключа **без рукоятки** — источник её не несёт | у нас: утверждение пришло, сверить нечего | **да** — единый отказ (Ф13-24) и число в переписи | перепись Ф7-21 печатает пару «строк с рукояткой · без» — **дописка к её «Тогда»** (Р3, §3 Д28); людей без пароля с таким ключом она называет **вторым числом рядом с Ф7-24** под тем же условием объявления переноса состоявшимся — **дописка к Ф7-24** (§3 Д29); оба числа печатаются ДО переноса |

**Цена названа числом, которого сегодня нет:** три живых ключа (число ЛИЧНОСТЕЙ — Ф7 §10 п. 3),
из них без пароля — Ф7-24, из них без рукоятки — перепись; произведение этих условий и есть
люди, которым после переноса войти нечем. Ф7-24 уже требует не объявлять перенос состоявшимся,
пока каждому из них не назван второй способ войти; Ф13 ничего к этому не добавляет и ничего
не снимает.

### Р12. Утрата единственного ключа у человека без пароля: путь возврата — восстановление по подтверждённому адресу, которое заводит пароль (Ф5 + `kacho#2698`, решено 2026-09-17); распорядитель — не путь, а сирота

Тело `#1282`: «Без ответа на это беспарольный вход — ловушка, а не возможность». Ответ в три
шага, и каждый со своим предикатом.

**1. Ловушкой полоса не становится ДЕЙСТВИЕМ этой фазы.** Ни один глагол Ф13 не снимает и не
заводит способа входа; снятие последнего способа отвергает Ф7-26 («завести другой способ прежде»);
снятие ключа при наличии пароля оставляет пароль (Р6). Утрата — событие вне продукта: устройство
потеряно. Ф13-22 — положительный контроль этого шага.

**2. Путь возврата ОБЪЯВЛЕН двумя одобренными соседями, и один из двух решением стал ПУТЁМ**
(Ф7 §4.4 расх. 10; Ф5 Р6): самостоятельное восстановление — код по почте и новый пароль
(Ф5-01…03), после чего человек входит паролем (Ф3-01), заводит новый ключ из сессии (Ф7-01) и
снимает утраченный (Ф7-25). Сброс распорядителем (F4d Р4) — вторая половина Ф5 Р6 — сирота
F4d-20 без глагола и без фазы (`kacho#2702`): сосуществование двух путей Ф5 Р6 не отменено, но
путём возврата, который наблюдает проба, он не является. Ф13 не заводит третьего пути.

**3. Производитель НАЗНАЧЕН решением и на дереве ЕЩЁ НЕ ПОСАЖЕН — обе половины названы.**
Завершение восстановления на дереве (`0cb0eddd`) замещает **существующую** строку пароля; у
личности без строки оно отвечает 503 и откатывается (§1.1 ∅7). Решение `kacho#2698` (оркестратор
2026-09-17T03:24Z; владелец делегировал решения 2026-09-16) — **исход 1**: восстановление
**заводит** способ входа паролем, если строки нет; писатель — тот же оператор ID-PW-1 (PWV-10),
расширенный на «нет строки → завести»; основание пароля — доказанное кодом владение
подтверждённым адресом (Ф5 Р5), то же, на котором Ф5 замещает существующий. Что решение
заказало и что из этого написано: новая редакция ID-PW-1 (PWV-10 дополняется заведением) —
**заказана, не написана** (`password-verifier-follows-the-stored-value.md` на стволе —
`2554fb6f`, о заведении молчит); редакция Ф5 после ред. 3 — строка «у личности без строки способа
код заводит её» в Р5/Ф5-03 — **заказана, не написана** (`recovery-of-access.md`: ствол `e889257f`,
ветка `#214` `5b70cc58` — ни одна не несёт `#2698`); реализация в `recovery_complete.go`
(Ф5 `#1271`, RED из предиката задачи → GREEN, текст журнала снимается) — **не посажена**
(`git grep -c 'has no password sign-in method' 0cb0eddd -- internal/apps/kaname/api/humansession/recovery_complete.go`
→ 2). Отвергнутый исход 2 (терминальный отказ «обратитесь к распорядителю») назван в решении с
доводом: держался бы на глаголе, которого нет (`kacho#2702`), и оставлял бы самообслуживание без
пути там, где он нужнее всего. Довод Ф13 редакций 1…2 — «ветвь „заводит“ — единственная без
нового производителя» — совпал с принятым.

**Что решение НЕ открывает — сказано там же и повторено здесь, чтобы не вывели.** Заведение
первого пароля **из живой сессии** (`kaname#213`) остаётся отдельным предметом: там основание —
сессия, а не владение ящиком, и он решается своей приёмкой. Ф13 глагола заведения пароля
по-прежнему не заводит (∅8).

**Что Ф13 требует и что производит Ф5 — разведено словами.** Ф13 **требует** одного: чтобы
проба пути (Ф13-25) была исполнима — личность без пароля, утратившая ключ, доходит до сессии и
до нового ключа. Шаг (2) этой пробы производит **Ф5** по `#2698`; Ф13 не правит ни ID-PW-1, ни Ф5
— их редакции живут своими документами и своими записями ревью.

**До посадки производителя:** Ф13-25 — красный **по третьей категории** («условие не создано:
производитель шага (2) — Ф5 по `kacho#2698` — не посажен», `change-graph.md` §5), не по
существу; DoD Ф13 (§9) называет это условием, а не вычитает из вердикта, — ровно как Ф13-13
ждёт `kaname#208`. Сама полоса входа от этого не зависит (§4.3): человек с ключом входит,
человек с ключом и паролем — обеими полосами, и ни один сценарий §5, кроме Ф13-25, «Дано» о
восстановлении не строит.

### Р13. Сверка утверждения — ОДНА на дерево: полоса входа зовёт проверяющего Ф7, а не свой

Подпись, происхождение, хэш имени доверяющей стороны, бит присутствия, счётчик, срок и
однократность испытания сверяются **тем же кодом**, что у `FinishAssertion` Ф7 (§3.2 её);
Ф13 добавляет к сверке ровно две вещи, которых у Ф7 нет: испытание берётся по контексту формы,
а не по сессии (Р2), и рукоятка сверяется со строкой (Р3). Второго проверяющего в дереве не
будет — ни копией, ни «упрощённой» версией для входа; держит это §7 инв. 2 и §8: каждая полоса
единого отказа Ф7 §3.2 воспроизведена на глаголе входа своим входом (Ф13-06), и отказ, на
котором полосы разошлись бы, — красное.

**Отвергнуто:** отдельный проверяющий «для входа» — две сверки одного утверждения расходятся
молча ровно там, где расхождение опасно (`architecture.md` §«Параллельные полосы одного
механизма»); и вызов `FinishAssertion` Ф7 **как RPC** изнутри слушателя формы — вызывающего у
него нет, а связывание с вызывающим — его дверь (Ф7 Р11).

### Р14. Включается провязкой ключа в перечень стража посадки `own`; экран, край и консоль — потребители

Провязка глагола входа ключом добавляет `webauthn` в перечень провязанных способов, из которого
страж посадки `own` считает предъявимые уровни правилом (Ф11 Р9, Ф-ж): с паролем — «1», с
паролем и ключом — «1», «2», «3» (ключ — в лучшем исходе флагов, Ф-б). Самоотчёт старта печатает
перечень (Ф13-14). Ручки провязки у полосы нет: она поднимается вместе со слушателем формы Ф3
на посадке `own` (Ф3 Р15) и без Ф7 не поднимается вовсе — сверять нечем (§4.3).

Край — две записи в его перечнях (пути ретрансляции, `isPublicHTTPPath`) тем же способом, что
`kacho#2699` и `kacho#2701` у Ф4/Ф5 (DoD п. 8); ветка полосы сессии на двух новых путях —
как на входе паролем: «сессии нет» ретранслируется, отсечка — отказ на крае (Ф3 Р7, Ф13-28).
Консоль — экран входа ключом и кнопка рядом с паролем — Ф8; до неё полоса наблюдается
сквозной пробой на слушателе службы и пробой края с дублёром (§8).

### Р15. Снятый ключ у проверяющего НЕОТЛИЧИМ от неизвестного удостоверения — строки нет; клетка счётчика одна, и «ключ снят» отдельным состоянием не существует

**Ф7 читается как одобрена, и она снятому ключу строки не оставляет.** Состав строки ключа
закрыт (Ф7 Р1 — **двенадцать** колонок, состояния среди них нет; число перемерено редакцией 4 по
переутверждённому перечню Р1 редакции 12 приёмки Ф7. Здесь стояло «восемь величин» — число,
которое не воспроизводится ни на одной редакции Ф7: её Р1 перечисляла десять своих колонок,
одиннадцатая — рукоятка — решена **этой** приёмкой, двенадцатая — признак обнаружимости — её
редакцией 12. Вывод абзаца от числа не зависит: он о том, что **состояния** среди колонок нет, и
это верно при любом их числе); снятие **возвращает слот** потолка,
и возвращает его триггер учёта **на удалении** строки в той же транзакции (Ф7 Р8, Ф7-37: «снятие
ключа возвращает слот: после Ф7-25 та же церемония проходит»); «ключ не снят» определён
поведением — «снятый ключ по-прежнему проходит» (Ф7 §3.0); отказ снятия по `id` годной формы,
**которому не отвечает ни одна строка**, побайтово равен отказу по чужому ключу (Ф7-27). Ни одна
строка Ф7 не говорит о надгробии, отметке снятия или «снятом, но хранимом» ключе (предикат:
`grep -n -i 'строка снимается\|строка снята\|надгроб\|revoked_at\|момент снятия'` по её §3.7 —
ничего о хранении снятого). Следовательно после Ф7-25 у проверяющего **нет строки** по этому
идентификатору удостоверения — снятый ключ и удостоверение, которого никогда не было, суть
**одно состояние by construction**, и полоса входа их не различает: ветвь Ф13-06 «к» приходит к
проверяющему как ветвь «б». Редакция 1 писала в «Когда» Ф13-06 «строка снята» — устройство,
против собственного §7 инв. 1, — и при этом держала клетку «ключ снят», которую при снятой
строке нечем увеличить (Б1 круга 1).

**Отсюда клетка одна.** Причина «удостоверение неизвестно» в счётчике Р10/Ф13-30 покрывает и
снятый ключ; клетки «ключ снят» **нет**: ни один исход не способен её увеличить, а клетка,
которую нечем увеличить, есть форма без содержания (`testing.md` §«Гейт на класс», п. 6). Ветвь
Ф13-06 «к» при этом **остаётся сценарием**: её «Дано» отличается от Ф13-05 одним фактом (ключ
снят глаголом Ф7-25), и она наблюдает то, чего ни один сценарий Ф7 не наблюдает, — что снятие
действует **на полосе входа**, а не только на полосе утверждения из сессии (Ф7-25); её «Тогда» —
единый отказ входа и клетка «удостоверение неизвестно» +1, ровно как у ветви «б».

**Отвергнуто — второй исход, названный ревьюером: Ф7 держит снятый ключ отмеченной строкой ради
различимой клетки.** Цена названа по каждому месту Ф7, которое пришлось бы дописать, и ни одного
читателя у различия нет:

| что дописывать у Ф7 | цена |
|---|---|
| Р1 — состав строки | девятая величина (отметка снятия) против правила Р1 «колонка без читателя не заводится»; читатель один — клетка |
| Р8, Ф7-37 — слот | возвращается не удалением, а отметкой: триггер учёта на **правке** строки, второй писатель слота |
| Ф7 Р11 / §3.0 — перечень ключей | обязан **не показывать** отмеченную строку — второе условие у чтения перечня (Ф7-25 о перечне молчит: его «Тогда» — «утверждение снятым ключом перестаёт проходить») |
| Ф7-27 — пара отказов | третья ветвь: снятие **уже снятого** по его `id` обязано быть побайтово равно двум прочим — иначе оракул «ключ был» |
| сосед `retention-sweep-has-a-caller.md` | у отмеченной строки **есть** момент, после которого она не меняет ничьего исхода, — таблица получает предмет уборки, которого у живых ключей нет (Ф7 Р8 выбрала потолок именно потому, что его нет) |

Выигрыш второго исхода — одна различимая клетка, у которой сегодня **нет читателя**: ни одно
«Тогда» §5 не строит действия на различии «снят» и «неизвестен», а живые сессии снятого ключа
гасит Ф13-21 без этого различия. **Предикат пересмотра:** появится читатель различия
(оповещение владельца «снятым ключом пытались войти», отчёт распорядителю) — отмеченная строка
приходит **вместе с ним, его приёмкой**, по правилу Ф7 Р1 о колонке с читателем; до него
различие не заводится. **Границу этого правила Ф7 назвала своей редакцией 12 (её Р12), и здесь
она не срабатывает:** правило не годится для факта, который сообщается **один раз** и задним
числом не восстанавливается, — такой факт заводят сразу, не дожидаясь читателя. Отметка снятия
таким фактом не является: строку пишем мы, и завести отметку можно в любой момент, задним числом
проставив её по журналу снятий. Поэтому исход здесь прежний, и он **перемерен**, а не унаследован.

**Чем это отличается от Ф12.** У второго фактора состояния строки названы Ф12 Р1 (`pending` ·
`active`), и клетки Ф12 по состоянию имеют производителя — там строка живёт в двух состояниях
**по своей приёмке**. У ключа состояние одно — строка есть либо её нет, — и Ф13 не вправе
завести второе за Ф7.

---

## §3 Дельта к одобренным соседям — обход по свойству, исход у каждого расхождения

Обход — по слову предмета в обоих написаниях (`testing.md` §«Предикат по ДВУЯЗЫЧНОМУ корпусу»),
по дому службы; воркспейсные приёмки (F4d, F4e) читаются **через** Ф7 §4.4 и Ф5 §1.5, которые
их уже разобрали, — второго разбора здесь нет.

```sh
# ДОМ: PRO-Robotech/kaname · R=origin/release/iam-lines (6acf8f19) · единица — ДОКУМЕНТ приёмки, называющий предмет
git grep -liE 'Ф13|1282|беспарол|passwordless|без пароля' $R -- 'docs/engineering/acceptance/*.md' | wc -l   # → 7
git grep -liE 'Ф13|1282|беспарол|passwordless|без пароля' $R -- 'docs/engineering/acceptance/*.md'
#   → access-keys-are-ours · assurance-level-… · login-lane-… · login-session-… · password-verifier-… ·
#     registration-… · second-factor-…  (recovery-of-access слова не несёт — она входит ПРЕДМЕТОМ, ниже)
# КОНТРОЛЬ: по слову, которого нет ни у кого, — 0
git grep -liE 'идентификатор-первым|identifier-first' $R -- 'docs/engineering/acceptance/*.md' | wc -l   # → 0
```

Семь документов по слову и восьмой — Ф5 — по **предмету**: путь возврата доступа, которым она
владеет вместе с F4d Р4 (Ф7 §4.4 расх. 10 называет обе); у каждого — строки, которые Ф13 либо
исполняет, либо с которыми расходится.
Исход у каждого расхождения — **принято**, **интерпретация** (с названным исходом) либо
**дописано** (к глаголу соседа — с доводом и ценой); **заменено** здесь нет ни одного.

| № | источник | что говорит | исход |
|---|---|---|---|
| **Д1** | Ф7 Р11, §10 п. 26, §4.5: «глагол входа ключом — свой … его причина — решение Ф13 внутри закрытого словаря контракта … либо расширение переписи её собственным изменением» | ждёт от глагола входа причины освобождения публичного RPC | **интерпретация**: глагол входа — глагол **формы** (Р1), как вход паролем; у формы нет ни записи каталога, ни причины (Ф3 Р2), перепись публичных RPC её не обходит. Требование Ф7 «свой глагол, не один из шести» исполнено дословно; вопрос о словаре беспредметен, и ни одно слово Ф7 не отменяется — она сама оговорила: «Чего решение НЕ утверждает: … что Ф13 обязана взять ту же причину» |
| **Д2** | Ф7 Р1: строка ключа несёт восемь величин, «колонка без читателя не заводится»; Ф7 Р9: «Ф13 заводит читателя — тогда признак приходит вместе с ним, её приёмкой» | перечень колонок закрыт условием возврата | **дописано по правилу самой Ф7** (Р3): рукоятка — колонка с читателем (полоса входа), писателями Ф7 (церемония, перенос) и сценарием расхождения (Ф13-06 «з»). Ф7 не правится: её Р1 остаётся верна о величинах, у которых читатель был на день её одобрения |
| **Д3** | Ф7-25: «Тогда» снятия — утверждение снятым ключом перестаёт проходить; о сессиях молчит | сессий, выданных ключом, на день Ф7 не было | **дописано** (Р8): снятие гасит прочие сессии причиной `access-key-revoked`; Ф7-25 не отменяется — его «Тогда» остаётся верным дословно, добавлена строка. Ведомость Ф7 §5 не трогается: держатель добавленной строки — Ф13-21 |
| **Д4** | Ф7 §4.4 расх. 10: «ни один из них [путей] пароля не требует … самостоятельное восстановление — тем более (Ф5: доказательство — владение почтовым ящиком)» | утверждает о производителе Ф5 то, чего на дереве нет | **интерпретация с перемером**: довод о **доказательстве** верен; о **производителе** — нет: завершение замещает существующую строку (§1.1 ∅7). Ф7 не правится (одобрена); расхождение отдано владельцу как развилка `kacho#2698` (Р12) — **решена** исходом 1 (2026-09-17): после посадки Ф5 довод Ф7 верен и о производителе |
| **Д5** | Ф7-26, Ф7-16: «сессия человеку без пароля до Ф13 строится записью» | ждёт строителя | **принято, исполнено**: после Ф13 «Дано» обоих строится действием — вход ключом; §6.1 |
| **Д6** | Ф7 §10 п. 16: «перенесённый ключ может не появиться в полосе Ф13 … что делать с таким ключом, решает Ф13 своим кругом» | передаёт решение | **принято, решено** (Р11): второй формы входа нет; необнаруживаемость нам не наблюдаема, отсутствие рукоятки — наблюдаемо и считается |
| **Д7** | Ф7 Р9: `userVerification` при предъявлении — «предпочтительна»; Ф7-42 наблюдает равенство объявленному | литерал контракта | **принято**: тот же литерал в испытании входа (Ф13-01); третьего объявления нет — оба испытания читают одно |
| **Д8** | Ф7 §7 инв. 5: срок испытания — одна величина на обе процедуры | две процедуры | **принято**: третья процедура (испытание входа) величины не заводит — читает ту же (Р2) |
| **Д9** | Ф3 Р2: семейство `/iam/v1/auth/*`, край ретранслирует, «текст отказа входа — один на все причины: `authentication failed`» | семейство и текст | **принято**: два глагола в семействе (Р1); тот же текст, побайтово равный Ф3-02 (Р7) |
| **Д10** | Ф3 Р12, Ф3-35: виды формы — закрытый перечень, «виды регистрации и восстановления дописывают Ф4 и Ф5 в тот же перечень» | перечень растёт фазами | **принято**: вид `access-key-login` — тем же способом (Ф13-27); один вид на два глагола, потому что это одна форма в двух запросах |
| **Д11** | Ф3 Р10: два счёта — по адресу и по источнику; «успешный вход обнуляет счёт по адресу» | оба счёта у входа паролем | **интерпретация** (Р9): у полосы без адреса — только источник; `begin` — попытка; успех обнуляет счёт по адресу человека, как всякий вход. Ф3 не правится: её счёт по адресу у пароля остаётся |
| **Д12** | Ф3 Р9: «пять причин отзыва — один отказ предъявителю»; Ф12 Р10 добавила `second-factor-reset` | перечень причин | **дописано** (Р8): `access-key-revoked` — седьмая в перечне, вторая наша после Ф12; своя константа у носителя; отказ предъявителю тот же |
| **Д13** | Ф3 Р1: состав записи сессии закрыт | закрыт | **принято**: Ф13 состав не расширяет — ни идентификатора ключа (Р8 отвергла), ни флагов (Р4 отвергла) |
| **Д14** | Ф3 Р14, Ф3-47: событие `iam.session.issued` с `methods`; «регистрация и восстановление своего события входа не дублируют» | состав события | **дописано** (Р10): поле `access_key_id`; вид события тот же |
| **Д15** | Ф11 Р3: «3» — проверка пользователя + не допускает резервного копирования; §10 п. 3: тела `#1266`, `#1280`, `#1282` говорят «присутствие», «при их закрытии свести к Р3, иначе предикат Ф13 будет утверждать то, чего правило не делает» | предупреждает поимённо | **принято**: предикат задачи читается через Р3 (§0.4); Ф13-10…12 — Ф11-03…05 дословно; «присутствие» здесь — сверка бита (Ф7-49), не различитель уровня |
| **Д16** | Ф11 Р1: «копии уровня — чтения записи, а не второе состояние»; Ф11 Р2: понижение в пределах сессии невыразимо | норма о копиях | **принято; на дереве не исполнено** (§1.1 ∅5, `kaname#208`): Ф13-13 — держатель; производитель — та задача, не эта фаза |
| **Д17** | Ф11 §5.2: «Ф13 — 03, 04, 05» | три сценария ждут этой фазы | **принято**: Ф13-10…12 исполняют их под номерами Ф11 в DoD Ф13 (§9) |
| **Д18** | Ф11 Р9: страж посадки считает предъявимые уровни правилом из провязанных способов | перечень выводится | **принято** (Р14, Ф13-14) |
| **Д19** | Ф12 Р3: обязательность — свойство действия, не личности; «вход паролем при заведённом факторе даёт «1», а не отказ» | ось обязательности | **принято** и продолжено на ключ (Р5): вход ключом при заведённом факторе — «2»/«3», кода не требует |
| **Д20** | Ф12 Р4: поле `secondFactor` у `POST /iam/v1/auth/login`; «`method` вне словаря либо способ, у которого нет поля с этим именем (… `webauthn` до Ф7) — 400» | поле у входа паролем | **принято**: у входа паролем поле остаётся; у входа ключом его **нет** (Р5) — поле без следствия. Ветвь `webauthn` церемонии `step-up` — не предмет этой фазы (§4.2) |
| **Д21** | Ф12 Р9: снятие фактора гасит прочие сессии, текущей уровня не понижает | образец | **принято как образец** (Р8) |
| **Д22** | Ф12 §0.2, §4.2: «ключи доступа как второй либо первый фактор — Ф7, Ф13» | передаёт | **принято**: первый фактор — здесь; второй — глаголы утверждения Ф7 из сессии (§4.2) |
| **Д23** | Ф4 §1.2, Ф4-06: полос регистрации у нашего объявления одна — пароль | одна полоса | **принято**: регистрации личности ключом здесь нет (§4.2, Ф7 §4.4 расх. 11) |
| **Д24** | Ф5 Р6: два пути возврата сосуществуют; Ф5-01…03: восстановление — код и новый пароль | путь назван | **принято** (Р12); исполнимость у личности без пароля — решена `kacho#2698` (исход 1), производит Ф5 после посадки |
| **Д25** | Ф1 §6: «беспарольный вход ключом доступа — 0 сценариев, своя фаза; здесь пароль — единственная объявленная полоса» | вынос | **принято, возврат исполнен**: полос две, и Ф1 не правится — её слова верны о её объёме |
| **Д26** | Ф2 (ID-PW-1) §5: «целиком о способе вида «пароль»; иных видов способа ни один Given не заводит» | граница | **принято**: строк способа `webauthn` не заводится (Ф7 Р1); писатель пароля один — потому `kaname#213` и `kacho#2698` не здесь |
| **Д27** | Ф7 Р1 (состав строки закрыт, состояния нет), Ф7 Р8 и Ф7-37 (снятие возвращает слот — на удалении строки, той же транзакцией), Ф7 §3.0 («ключ не снят» = снятый ключ по-прежнему проходит), Ф7-27 (отказ по `id` без строки побайтово равен отказу по чужому ключу) | снятого ключа как хранимого состояния у Ф7 нет | **принято** (Р15): у проверяющего снятый ключ и неизвестное удостоверение — одно состояние; клетка счётчика одна; Ф13-06 «к» наблюдает действие снятия на полосе входа и увеличивает клетку «удостоверение неизвестно». Отмеченная строка — отвергнута с ценой по каждому месту Ф7 (Р15) |
| **Д28** | Ф7-21 «Тогда»: перепись печатает закрытый перечень — объём осмотренного · покрытие алгоритмов · наибольшее число ключей у одного человека · обнаруживаемость «не установлено» отдельной строкой · столбец меток; рукоятки в перечне нет | перечень печатаемого закрыт | **дописано** (Р3, Р11): перепись печатает **пару** «строк ключа с рукояткой N · без рукоятки M» — величину, чей единственный читатель здесь (Ф13-24 P); Ф7-21 не переписывается — её «Тогда» остаётся верным дословно, добавлена одна пара. Цена: одна величина в выводе переписи (и в приложении к DoD Ф7 — Д30); держатель — Ф13-24 |
| **Д29** | Ф7-24: число людей, чей единственный способ входа — ключ, **непереносимый по Ф7-22** (алгоритм вне перечня); «перенос не объявляется состоявшимся, пока это число не ноль либо пока каждому из них не назван второй способ войти»; положительный контроль — человек с паролем в число не входит | считает одну причину запертости — алгоритм; ключ без рукоятки по Ф7-22 **переносим** | **дописано** (Р11): рядом с числом Ф7-24 перепись печатает **второе** число — людей без пароля, чей единственный ключ переносим, но **без рукоятки** (в полосу входа не попадёт — Ф13-24), и условие Ф7-24 об объявлении переноса состоявшимся действует на это число так же; положительный контроль тот же. Числа **не сливаются**: причина и производитель у них разные (Ф7-22 — алгоритм, Ф13-24 — рукоятка), и слитое число потеряло бы, что чинить. Цена: одно число и одно условие; Ф7-24 не переписывается — дописка живёт здесь |
| **Д30** | Ф7 §9.1 п. 6 (DoD): «перепись источника прогнана и её вывод приложен: людей, строк способа, ключей, покрытых и непокрытых, запертых, наибольшее число ключей у одного человека и его сверка с объявленной величиной» | перечень приложенного закрыт | **дописано** (§9 п. 1): приложенный вывод несёт пару Д28 и число Д29; исполняет исполнитель Ф7 по этой приёмке — вместе с колонкой рукоятки (Д2), чтобы перепись и перенос не переписывались вторым заходом. Цена: одна строка в приложении к DoD Ф7; DoD Ф7 не переписывается (одобрен) — дописка живёт здесь и связывает `#1273` через `#1282` |

**Сводка:** принято — 20, интерпретация — 3 (Д1, Д4, Д11), дописано — 7 (Д2, Д3, Д12, Д14,
Д28, Д29, Д30), заменено — 0; предикат:
`grep -cE '^\| \*\*Д[0-9]+\*\*' docs/engineering/acceptance/passwordless-login-with-access-key.md`
→ 30, и число строк с «**дописано**» в столбце исхода
(`grep -E '^\| \*\*Д[0-9]+\*\*' <док> | grep -c '\*\*дописано'`) → 7. Редакция 1 давала
26 и 4: три дописки к Ф7 (Ф7-21, Ф7-24, DoD §9.1 п. 6) стояли в Р3, Р11, Ф13-24 и §9 п. 1 без
строки ведомости — Б2 круга 1; контроль: `grep -E '^\| \*\*Д[0-9]+\*\*' <док> | grep -c
'Ф7-21\|Ф7-24\|DoD Ф7'` → **3**, было 0. Контроль — **множествами**, а не числом: голое `DoD`
даёт {Д17, Д28, Д30} (Д17 несёт «в DoD Ф13»), предикат выше — {Д28, Д29, Д30}; мощности равны
(3 и 3), различает состав (Н15 круга 2).

---

## §4 Объём и границы

### §4.1 В объёме

1. **Два глагола формы** на слушателе Ф3: испытание входа и вход (Р1); их отказы формы —
   отсутствующее поле, лишнее поле, признак (Ф3-05, Ф3 Р12) — теми же текстами.
2. **Испытание входа**: без имени, `allowCredentials` пуст, `userVerification: "preferred"`,
   срок — литерал Ф7, привязка к контексту формы, одно на контекст, однократность (Р2).
3. **Сверка утверждения — проверяющим Ф7** (Р13): подпись, происхождение из перечня, хэш имени
   доверяющей стороны, бит присутствия, счётчик, испытание; **плюс** рукоятка (Р3).
4. **Рукоятка**: колонка строки ключа; писатели — церемония и перенос Ф7; значение у ключей
   Ф7 — **отдельное случайное значение человека** (64 байта, своя таблица, одна строка на
   человека, каскад удаления от строки человека) — редакция 4; чтение вывода переписи Ф7-21 по
   величине «с рукояткой · без» (Р3, Р11).
5. **Выдача сессии** операцией Ф3 с `{webauthn}` и флагами; уровень — Ф11 (Р4); Ф11-03…05
   под номерами Ф13-10…12.
6. **Единый отказ** — текст и время Ф3-02 (Р7); полосы отказа выводятся предикатом §5.
7. **Второй фактор**: не требуется, не принимается полем, предъявляется внутри сессии без
   изменения уровня (Р5).
8. **Снятие ключа** — прочие сессии гаснут причиной `access-key-revoked`; дописка к глаголу
   Ф7 (Р8).
9. **Частота**: счёт по источнику, `begin` — попытка, успех обнуляет счёт по адресу (Р9).
10. **Журнал и счётчики**: `iam.session.issued` с `["webauthn"]` и `access_key_id`; клетки
    исходов полосы (Р10).
11. **Провязка**: перечень стража посадки `own` получает `webauthn` (Р14).
12. **Две записи в закрытых перечнях Ф3** (виды формы, пути) и **ретрансляция краем** двух путей
    — изменение платформы, едет с фазой (DoD п. 8).
13. **Пробы** (§8): сквозные на слушателе службы с подставным аутентификатором Ф7 (умеющим
    флаги, снятый бит, испытание по имени), интеграционные на транзакцию и однократность,
    измерительная по времени, декларативные по перечням, пробы края с дублёром; и **гейт дерева**
    на единственность проверяющего (§7 инв. 2).
14. **Страница арендатора** о входе ключом — дополнить существующую страницу входа документации
    службы, не заводить вторую (`kacho-docs-writer`); в том числе — какие поля ответа браузера
    (`clientExtensionResults`, `authenticatorAttachment`) снять перед отправкой, иначе цена Р1
    (400 с именем поля на первом же вызове) названа в решении и не доезжает до страницы (Н13
    круга 2).

### §4.2 За границей — с носителем и предикатом возврата по каждой строке

| вынесено | сценариев здесь | почему вне объёма | носитель · условие возврата |
|---|---:|---|---|
| сервер церемонии, сверка утверждения, строка ключа, перенос трёх ключей, потолок, снятие | 0 (переиспользуются) | предмет Ф7; здесь — вызывающий | `#1273` (Ф7), OPEN; DoD Ф13 наступает после её посадки (§4.3) |
| ключ как **второй** фактор: `step-up` со способом `webauthn` из сессии, выданной паролем | 0 | глаголы утверждения Ф7 из сессии вызывающего (Ф7-06); ветвь церемонии Ф12 Р4 | Ф7 `#1273` и Ф12 `#1281`; правило уже сказало исход (Ф11 Р2) |
| экран входа ключом, кнопка рядом с паролем, обработка испытания браузером, снятие полей сериализации | 0 | консоль — потребитель края | Ф8 `#1274`, OPEN |
| регистрация **личности** ключом — человека ещё нет, сессии нет | 0 | граница `#1282` дословно; носитель назван Ф7 §4.4 расх. 11 — своя приёмка, составляющая Ф4 и Ф7 | `kacho#2703` (OPEN, заведена кругом 1 ревью этого документа): два исхода — завести приёмку либо снять полосу из объявлений Ф4 §1.2 / Ф1-24, решение владельца; до неё нового человека одним ключом не завести (Ф7 §10 п. 20) |
| заведение **первого пароля** из сессии человеком без пароля | 0 | писатель строки пароля один — ID-PW-1 (Ф2) | `kaname#213`, OPEN |
| восстановление у личности без строки пароля | 1 — Ф13-25, **исполним после посадки производителя** | производитель — завершение Ф5, которое по `kacho#2698` (исход 1) **заводит** строку при её отсутствии тем же писателем ID-PW-1; редакции Ф5 и ID-PW-1 заказаны, код не посажен | `kacho#2698`, OPEN — решено 2026-09-17; §4.3, §11 |
| сброс способа входа распорядителем (F4d-20, часть пароля и ключа) | 0 | сирота без фазы (Ф1 §5.1); Ф12 Р10 закрыла только второй фактор | `kacho#2702`, OPEN |
| копия уровня в ответе смены пароля и церемонии читает запись | 1 — Ф13-13, **исполним после производителя** | код Ф3; дефект латентен до «3» | `kaname#208`, OPEN — §4.3 |
| признак обнаруживаемости в строке ключа | 0 | читателя нет: обнаруживаемость наблюдает аутентификатор, не служба (Р2, Р11) | появится читатель — своей приёмкой, как Ф7 Р9 предусмотрела |
| второй формы входа — по адресу с `allowCredentials` | 0 | оракул адреса и наличия ключей; вторая полоса одного механизма (Р2) | не возвращается; необнаруживаемый перенесённый ключ — Р11 |
| политика «ключ обязателен для…», «пароль отключён при ключе» | 0 | обязательность — полом действия (Ф12 Р3); откат к паролю — Р6 | решение о политике по личности/аккаунту — отдельная приёмка (Ф12 §4.2 та же строка) |
| условие модели `mfa_fresh` и «3» его исходом | 0 | не в силе ни на одной полосе (Ф11 §1.7, §6) | `kaname#133` |
| снятие чужого компонента и строки `passwordless: true` в посадке платформы | 0 | Ф10 | `#1276`, OPEN |
| токен человека, выпущенный из нашей сессии, и его уровень | 0 | полосы выдачи нет (Ф11 §6) | появление полосы — своей приёмкой |

Каждая строка — осознанное сужение объёма (ban #14): фаза production-complete в своих границах,
отложенное названо, посчитано и имеет адрес.

### §4.3 Что делает сценарий НЕИСПОЛНИМЫМ, а что нет — по каждой зависимости

| зависимость | состояние на линии | что без неё | исход для DoD |
|---|---|---|---|
| **Ф7 `#1273`** — сверка, строка ключа, церемония, перенос | приёмка APPROVED круга 6; **код ВЛИТ** в ствол коммитом `39628487` 2026-09-17 (перемер редакции 4; редакции 1…3 писали «кода нет») | **изменилось редакцией 4**: «Дано» с принятым ключом **строимо действием** — церемония Ф7 в дереве есть; сверять **есть чем** — проверяющий `internal/webauthnverify` влит и Р13 требует звать именно его. Не поднимается по-прежнему **полоса** (Р14): её глаголов в дереве ноль (∅4, перемерено) | **порядок «Ф7 — первой» ИСПОЛНЕН**, а не предстоит; отсюда сценарии §5 больше не «не выполнилось» по причине «Ф7 не посажена» — третью категорию им даёт теперь **только** отсутствие собственных глаголов Ф13 (∅4) и отдельно названные `kaname#208` и Ф5 |
| **Ф3** — слушатель, сессия, признак, частота | в стволе и на линии | — | «Дано» строимо |
| **Ф11** — правило с флагами | в стволе | — | строимо; Ф11-03…05 исполняются здесь |
| **Ф12 `#1281`** — второй фактор | приёмка APPROVED; код — **на линии** (`#215` влит в `release/iam-lines@0cb0eddd`), в стволе `94352d9c` нет | «Дано» Ф13-15, Ф13-17 («заведён второй фактор») на линии строимо **действием**; на стволе — **записью** строки `totp` `active` (Ф11 §3.0а, Ф12 Р1) | исполнимо записью на стволе, действием на линии |
| **`kaname#208`** — копия уровня читает запись | OPEN | Ф13-13 красный **по существу** на «3» | производитель — та задача; DoD Ф13 её называет условием (§9 п. 6) |
| **Ф5 `#1271` по `kacho#2698`** — восстановление у личности без пароля заводит строку | решено (исход 1, 2026-09-17); редакции Ф5 и ID-PW-1 заказаны, код не посажен — на `0cb0eddd` 503 | Ф13-25 — «не выполнилось» до посадки: производитель назван, не посажен | **условие DoD**, не вычитается и не зачитывается (§9 п. 7) — как Ф13-13 с `kaname#208` |
| **край `#2689`** — ретрансляция семейства | в стволе платформы (`275240efdc` — коммит слияния) | Ф13-28 против дублёра службы исполним на стволе после двух записей Ф13 (DoD п. 8); сквозная «консоль → край → служба» — после Ф8 | пробы края — в доме платформы (`e2e-flow.md` §7а) |
| **перенос трёх ключей** (Ф7-21…24) | вместе с Ф7 | Ф13-23, -24 «Дано» строятся переносом либо записью строки ключа с рукояткой / без | строимо записью до переноса |

### §4.4 ВНЕ ОБЪЁМА редакции 4 — перечень закрыт и проверяется чтением, а не обещанием

Редакция 4 меняет **одно** — значение рукоятки (Р3). Всё нижеперечисленное она **не трогает**, и
перечень назван затем, чтобы правка одного решения не читалась правкой документа:

| что НЕ трогает редакция 4 | чем это проверяется |
|---|---|
| **решения Р1, Р2, Р4…Р15** — ни одно не переоткрыто и не переформулировано по существу | §11 и §2 читаются целиком; правки редакции 4 стоят в Р3, §4.1 п. 4, §5 «Дано», Ф13-23 и §11 п. 1 — перечень закрыт |
| **число и состав сценариев** — их по-прежнему **32**, ни один не заведён, не снят и ни одно «Тогда» не ослаблено | метки §5 и ведомость §6 не изменились; Ф13-23 получил уточнение слов в «Дано» и «И», утверждение прежнее — **разность** двух рукояток и сверка каждой со своей строкой |
| **сверка рукоятки и её отказы** (Р3, первая половина): отсутствующее поле — отказ формы (Ф3-05), несовпадение — **единый отказ** (Ф13-06 «з») | оба отказа о **сверке**, а не о значении: они верны при любом исходе решения о значении, и редакция 4 это проверяет чтением Ф13-06 |
| **колонка рукоятки у строки ключа** и оба её писателя (церемония и перенос Ф7) | Р3, абзац «колонка рукоятки при этом остаётся»; Ф7 Р1 её перечисляет и решения о значении не принимает |
| **форма церемонии, словарь алгоритмов, формат открытого ключа** — предмет Ф7, и он не менялся | ни одна строка редакции 4 их не называет |
| **вход без пароля как предмет** — полоса, её отказы, частота, журнал, уровень | правка — только Р3; §0.2 и §4.2 не двигались |
| **Р11 этой приёмки** (перенесённый ключ, не появившийся в полосе; метка названа вместе с документом — у Ф7 под `Р11` стоит решение о записях каталога разрешений, предмет иной) — **не переутверждается**, хотя Ф7 редакцией 12 дала ему **читателя** | у Ф7 теперь хранится признак обнаружимости в трёх состояниях (её Р12), и «какие ключи полоса не покажет» стало известно **до** попытки входа, а не после. Это меняет **основание** Р11, а не её решение, и потому идёт **своим** кругом: перечень ключей, которые полоса не покажет, Р11 сегодня выводит из числа переписи, и число это остаётся верным. Носитель — эта приёмка, предмет — свой круг |
| **`user.id` в контракте** `proto/kaname/cloud/iam/v1/access_key_service.proto` и **на публичной странице** `docs/content/api/auth-lane.mdx` | оба называют значение рукоятки платформенным `id` человека — с редакцией 4 это **перестало быть верным**, и мест **два**, а не одно: второе найдено перемером круга 8 приёмки Ф7. Ни контракт, ни страницу приёмка **не правит**: оба — предмет исполнителя, и приёмка обязана назвать расхождение, а не устранить его |
| **реализация Ф7, уже влитая в ствол** (коммит `39628487`, 2026-09-17): `begin_registration.go` кладёт в рукоятку платформенный `id` | это **третье** место того же расхождения и самое дорогое — код. Редакция 4 его не правит и не может: приёмка пишет контракт, а не реализацию. Названо здесь, чтобы «дельта к одобренной Ф7» не читалась дельтой к **ненаписанному**: написано, влито, и решение Р3 идёт **после** |
| **ЧЕТВЁРТОЕ место — комментарий ВЛИТОЙ МИГРАЦИИ** `internal/migrations/20260917221000_access_keys_are_our_record.sql`: и шапка («у ключей Ф7 — платформенный `id` человека как байты»), и `COMMENT ON COLUMN … user_handle` говорят то же самое | **это меняет форму починки, и потому входит в объём приёмки, а не только в строку возврата.** Влитую миграцию **не правят** (ban #5) — значит комментарий нельзя исправить на месте: его меняет **новая миграция**, несущая новый `COMMENT ON COLUMN`. Отсюда миграций у этого изменения **две**, а не одна: колонка признака обнаружимости (Ф7 Р12) — и эта. Их можно свести в одну, и решает это исполнитель по ban #5, а не приёмка; приёмка требует лишь, чтобы **комментарий перестал лгать тем же изменением**, которым меняется значение |

**Чего редакция 4 не перемеряла, сказано прямо.** Числа §1.1 и §1.2 (перепись отсутствий и форм)
и числа §3 (дельта к соседям) сняты редакциями 2 и 3 на **своих объявленных** ревизиях и сплошь
этой редакцией **не переснимались**: они честны тем, что ревизию называют, а их сплошной перемер —
работа своего круга. Исключений **три**, и каждое взято потому, что редакция 4 **сама** ссылается
на вывод: предикат «Ф7 не называет `user.id`» (Р3) — вывод изменился, стало 4; отсутствия **∅1 и
∅2** — на судимой ревизии **закрыты** реализацией Ф7 (шапка), и три места, писавшие «код Ф7 не
начат», приведены к измеренному; отсутствие **∅4** — перемерено и **держится** (ноль глаголов
входа ключом при тринадцати путях семейства), потому что на нём стоит весь предмет этой приёмки.

---

## §5 Сценарии

Обозначения: **Ф13-NN** — номер сценария; производитель каждого — §6. Уровень при
идентификаторе: **E** — наблюдаемо снаружи, запросом к слушателю службы либо к краю; **I** —
изнутри процесса (исход транзакции, счётчик, самоотчёт старта, ответ внутреннего RPC); **P** —
свойство дерева, проверяемое переписью. Все моменты — часами пробы (форма Ф-д Ф3).

**Как строится «Дано» — названным домом, и подмена — только согласованным состоянием
(Ф11 §3.0а):**

- **«личность с принятым ключом»** — Ф7-01: человек с заведённым паролем, вошедший паролем,
  провёл церемонию регистрации; ключ обнаруживаемый (Ф7 Р9), рукоятка — **случайное значение
  этого человека** из его строки (Р3, редакция 4). До посадки Ф7 — «не выполнилось» (§4.3), записью не строится: строку ключа
  без открытого ключа, которым сверять, продукт не производит;
- **«личность без пароля, с ключом»** — переносом (Ф7-23: людей без пароля источник несёт,
  Ф7-24 считает) либо **записью** строки ключа с рукояткой и удалением строки способа «пароль»;
  её **сессия** — действием этой фазы (Ф13-05), и это первое, что Ф13 делает строимым
  действием (Ф7-26, Ф7-16 — §6.1);
- **«контекст формы»** — `GET /iam/v1/auth/csrf?form=access-key-login` (Ф3 Р12): печенье
  `kaname_form` и признак вида `access-key-login`;
- **«подставной аутентификатор»** — фикстура Ф7 §8: исполняет контракт настоящего тем же кодом,
  умеет отдать флаги проверки пользователя и резервного копирования по имени, снятый бит
  присутствия, утверждение над испытанием, которое ему назовут (невыданное, чужого контекста,
  просроченное — часы управляемые), и рукоятку по имени;
- **«уровень L»** — Ф11 §3.0 дословно: глагол с полом ≤ L проходит пол, с полом > L —
  401 · `code` 16 · `WWW-Authenticate` с `acr_values`; «3» — в ответе службы краю о сессии;
- **«единый отказ входа»** — Р7: 401 `authentication failed`, `details: []`, без `Set-Cookie`,
  побайтово равный ответу Ф3-02;
- **«заведён второй фактор»** — Ф12-02 после её посадки; до неё — записью строки `totp`
  `active` (Ф12 Р1, Ф11 §3.0а).

**Перечень полос единого отказа ВЫВОДИТСЯ предикатом, а не помнится** — та же форма, что
Ф7 §3.0, и предикат обязан сойтись с Р7 и §7 инв. 4:

```sh
# ДОМ: PRO-Robotech/kaname · единица — МЕТКА сценария, чьё «Тогда» называет единый отказ входа
python3 - <<'PRED'
import re, io
s = io.open('docs/engineering/acceptance/passwordless-login-with-access-key.md', encoding='utf-8').read()
sec = re.split(r'(?m)^## §6 ', re.split(r'(?m)^## §5 ', s)[1])[0]      # ЯКОРЬ ^ обязателен (Ф7 §3.0)
blocks = [b for b in re.split(r'(?m)^(?=\*\*Ф13-\d+)', sec) if re.match(r'\*\*Ф13-\d+', b)]
hits = []
for b in blocks:
    m = re.match(r'\*\*Ф13-(\d+)', b); i = b.find('**Then**')
    if i < 0: print('БЕЗ «ТОГДА»:', m.group(1)); continue
    if 'единый отказ входа' in b[i:]: hits.append(m.group(1))
print('меток осмотрено', len(blocks), '· с единым отказом входа в «Тогда»:', hits)
PRED
#   → меток осмотрено 32 · с единым отказом входа в «Тогда»: ['03', '06', '08', '20', '21', '24', '25']
#     Редакция 2 завела Ф13-32 (обнуление счёта по адресу) — метка без единого отказа: 31 → 32, семь — те же
#     Семь меток; у Ф13-06 — одиннадцать ветвей «а»…«л». Число обязано совпадать с Р7 и §7 инв. 4.
#     Первый прогон на редакции 1 дал восемь — Ф13-09 говорил «это НЕ единый отказ входа», и предикат
#     по слову отрицания не видит; фраза переписана без слова, сценарий не изменился
```
### Полоса «испытание входа»

**Ф13-01 — испытание выдаётся, не назвав человека; форма объявленная** · E

**Given** посадка `own` с провязанной полосой ключа (Р14); контекст формы получен; ни одной
личности в запросе не названо — «Дано» не содержит человека вовсе
**When** `POST /iam/v1/auth/access-key/begin` с телом `{"csrfToken": <признак>}` — и ничего
сверх: форма из одного поля
**Then** `200`; тело — `{"publicKey": {"challenge": <base64url>, "rpId": <имя доверяющей стороны
Ф7 Р2>, "timeout": <срок испытания Ф7 в мс>, "userVerification": "preferred",
"allowCredentials": []}}` (Р1, Р2)
**And** `allowCredentials` — **пустой массив**, а не отсутствующее поле: отсутствие и «пусто»
здесь разные факты, и клиент читает «никого не ограничивать» словом
**And** `userVerification` равен объявленному литералу Ф7 Р9 — тем же способом наблюдения, что
Ф7-42; «обязательна» либо «не требуется» — красное
**And** ответ **не** несёт `Set-Cookie: kaname_session` — испытание сессией не является; контекст
формы не сменён
**And** два запроса подряд дают два **разных** `challenge` (§7 инв. 5: не выводится ни из
контекста, ни из времени)
**And** положительный контроль полосы: без него Ф13-02…04 зеленели бы на глаголе, отвергающем
всё

**Ф13-02 — форма испытания: признак обязателен, вид свой, лишнее отвергается** · E

**Given** то же, что в Ф13-01
**When** (а) `begin` без `csrfToken`; (б) с признаком вида `login`; (в) с признаком, выданным
под контекстом, который **сменила** выдача сессии (Ф1-41: вход паролем в том же браузере между
`csrf` и `begin`); (г) с лишним полем `email`
**Then** (а) и (г) — `INVALID_ARGUMENT` / **400**, текст называет **поле**; (б) и (в) —
`PERMISSION_DENIED` / **403**, `reason = FORM_TOKEN_REJECTED`, текст `form token rejected`,
неразличимо между «чужой вид» и «чужой контекст» (Ф3 Р12)
**And** ни один из четырёх испытания не выдал и в счёт частоты (Р9) не пошёл
**And** положительный контроль — Ф13-01

**Ф13-03 — одно живое испытание на контекст: второе замещает первое** · E

**Given** личность с принятым ключом; один контекст формы; `begin` выдал испытание `C1`, затем
тот же контекст — `C2`
**When** утверждение собрано над `C1` и предъявлено; затем — над `C2`
**Then** над `C1` — **единый отказ входа** (испытание замещено — та же полоса, что «не
выдавалось»); над `C2` — `200`, сессия выдана (Ф13-05)
**And** внесённое различие — ровно одно: какое из двух испытаний подписано
**And** отказ над `C1` посчитан попыткой по источнику, испытание `C2` — не тронуто (сгорает
только предъявленное, Ф13-08)

**Ф13-04 — выдача испытания — попытка по источнику; отказ формы — нет** · E

**Given** профиль с `N_источник` (Ф3 Р10); один источник; `N_источник − 1` запросов `begin`
уже сделано
**When** ещё один `begin`; затем ещё один
**Then** первый — `200` (граница включена); второй — `RESOURCE_EXHAUSTED` / **429**,
`reason = TOO_MANY_ATTEMPTS`, `Retry-After` в секундах до конца окна (Ф3 Р10)
**And** положительный контроль внутри сценария: `N_источник` отказов **формы** (Ф13-02 «а»)
подряд из того же источника окна не исчерпывают — за ними `begin` проходит
**And** окно **по адресу** этим не затронуто: у полосы без имени адреса нет (Р9)

**Ф13-32 — успешный вход ключом обнуляет счёт неверных предъявлений по адресу человека; счёт по источнику не трогает** · E

**Given** личность с паролем и принятым ключом; профиль с `N_адрес ≥ 2` и
`N_источник ≥ 2·N_адрес` (Ф3 Р10); один источник; `N_адрес` неверных паролей по её адресу уже
предъявлено в окне — **граница включена**: `N_адрес`-я — 401, следующая была бы 429 (Ф3-28; та
же арифметика, что у Ф13-04 по источнику)
**When** (а) `begin`, затем вход ключом (Ф13-05), затем ещё `N_адрес − 1` неверных паролей по
тому же адресу; (б) близнец — та же серия **без** `begin` и входа ключом
**Then** (а) вход — `200`; все `N_адрес − 1` неверных после него — отказ Ф3-02 (401), **ни
одного** 429: счёт по адресу обнулён входом ключом, как всяким успешным входом (Ф3 Р10, Р9);
(б) — первая же неверная — `N_адрес + 1`-я по адресу: `RESOURCE_EXHAUSTED` / **429**
`TOO_MANY_ATTEMPTS` (Ф3-28), и каждая следующая — 429: без входа граница исчерпана
**And** внесённое различие — ровно одно: состоялся ли вход ключом между двумя сериями
**And** «ни одного 429» в (а) — утверждение о счёте **по адресу**, а не о величине профиля, и
«Дано» это обеспечивает: по источнику проба делает ровно `2·N_адрес` попыток — `N_адрес` неверных
«Дано», `begin` (Р9), `N_адрес − 1` неверных после; успешный вход попыткой **не** считается
(Р9: «пара `begin` + успех стоит одной попытки»), — и при `N_источник ≥ 2·N_адрес` граница по
источнику не перейдена (включена — Ф13-04)
**And** счёт **по источнику** входом не тронут: `begin` этой пробы посчитан попыткой источника,
как требует Р9, и после входа счёт не уменьшился (I — чтение счёта; близнец — Ф13-04)
**And** отвергнутая реализация названа: полоса, не трогающая счёт по адресу, зелена на всём §5,
кроме этого сценария (Н5 круга 1); различает её ветвь (а), и только при `N_адрес ≥ 2`: без
обнуления первая же неверная после входа была бы `N_адрес + 1`-й по адресу и дала 429 (Н11)

### Полоса «вход»

**Ф13-05 — вход без пароля: сессия выдана, носитель поставлен, пароль не предъявлен нигде** · E

**Given** личность с принятым ключом (Ф7-01); контекст формы получен; испытание выдано
(Ф13-01); подставной аутентификатор собирает утверждение с флагами «проверка пользователя не
выполнена, резервное копирование не допускается»
**When** `POST /iam/v1/auth/access-key/login` с телом `{"csrfToken": <признак>, "credential":
{"id", "rawId", "type": "public-key", "response": {"clientDataJSON", "authenticatorData",
"signature", "userHandle"}}}`
**Then** `200`; тело — **та же форма, что Ф3-01**: `{"user": {"id", "email", "displayName"},
"session": {"expiresAt", "assuranceLevel": "2", "emailVerified", "passwordChangeRequired":
false}}`; `assuranceLevel` — по правилу Ф11 над `{webauthn}` с флагами этого утверждения, а не
константа полосы
**And** ответ несёт `Set-Cookie: kaname_session=<значение>` с атрибутами Ф3-06 и **новый**
`kaname_form` (контекст сменён выдачей — Ф3-37); `expiresAt` — момент выдачи плюс 24 ч
**And** **ни один запрос полосы не несёт поля `password`** — предикат по телам обоих запросов
пробы; строка способа «пароль» человека **не читалась** (наблюдаемо у личности без пароля —
Ф13-19)
**And** множество предъявленного сессии — `{webauthn}`, момент последнего предъявления равен
моменту аутентификации (I — чтение записи)
**And** момент последнего успешного предъявления **ключа** сдвинулся и его счётчик судится
Ф7-17…19 — тем же проверяющим (Р13): полоса входа для ключа есть предъявление
**And** событие `iam.session.issued` в потоке аудита несёт `methods: ["webauthn"]` и
`access_key_id` формы `ak-…` (Р10); отказ Ф13-06 события не порождает
**And** положительный контроль полосы: без него Ф13-06 зеленел бы на входе, отвергающем всё

**Ф13-06 — единый отказ входа ОДИН на все причины, побайтово и по времени; одиннадцать ветвей** · E

**Given** то же, что в Ф13-05; сверх того для ветвей: второй человек с принятым ключом («и»,
«з»); личность, заблокированная администратором («и»); ключ, снятый Ф7-25 («к»)
**When** предъявляется утверждение, годное во всём, кроме **одного** факта:
(а) подпись не сверяется с открытым ключом строки;
(б) идентификатор удостоверения, которого в хранилище нет вовсе;
(в) происхождение вне объявленного перечня (Ф7 Р2);
(г) хэш имени доверяющей стороны не совпадает;
(д) бит присутствия не установлен;
(е) сообщённый счётчик не больше сохранённого у считающего ключа (Ф7-18);
(ж) испытание — не выдавалось · уже предъявлено · просрочено (часы пробы) · выдано в другом
контексте формы;
(з) `userHandle` не равен рукоятке строки — рукоятка другого человека либо случайные байты;
(и) владелец ключа **заблокирован**;
(к) ключ **снят** глаголом Ф7-25 — аутентификатор его ещё держит и подписывает
**Then** каждый — **единый отказ входа** (Р7): `UNAUTHENTICATED` / **401**, `authentication
failed`, `details: []`, без `Set-Cookie`; **тела и коды всех ветвей побайтово равны между собой
и равны ответу Ф3-02** — один текст на семейство
**And** (л) **время** неразличимо по критерию Ф1-48…50: работа по неизвестному идентификатору
(б) выполняется наравне с работой по известному (а); измерительная проба своей строкой, как
Ф1-51 у восстановления; своей величины Ф13 не вводит (Ф1 §3.9)
**And** состояние **не изменено** ни в одной ветви: сессия не выдана, момент последнего
предъявления ключа не сдвинут, счётчик не сдвинут, строка ключа та же; испытание **сгорело**
(Ф13-08)
**And** каждая ветвь — попытка по источнику (Р9); различимость причин — **только** клеткой
счётчика службы (Ф13-30) и её журналом, не ответом; ветви (б) и (к) при этом увеличивают **одну**
клетку — «удостоверение неизвестно»: снятого ключа у проверяющего нет by construction (Р15), и
различить их нечем
**And** ветви (а)…(е) и (ж) — те же входы, что Ф7-07…09, Ф7-10, Ф7-12, Ф7-49, Ф7-18, Ф7-52…55
на полосе сессии, и отказы обеих полос **побайтово равны**: сверка одна (Р13, §7 инв. 2);
ветви (з), (и), (к) — свои у полосы входа, и у каждой положительный близнец в Ф13-05 отличается
одним фактом
**And** положительный контроль (и): тот же человек, **разблокированный**, входит (Ф13-05);
блокировка отличима от (а) только тому, кто вправе читать состояние личности (Ф1-05)

**Ф13-07 — форма входа: отсутствующее поле называется, лишнее отвергается, ничего не считается** · E

**Given** то же, что в Ф13-05
**When** `login` без `credential`; без `response.userHandle`; без `response.signature`; без
`csrfToken`; с признаком чужого вида (`login`); с лишним полем — по одному: `secondFactor`,
`email`, `password`, `clientExtensionResults`
**Then** отсутствующее — `INVALID_ARGUMENT` / **400**, текст называет **поле**; чужой вид —
**403** `FORM_TOKEN_REJECTED`; лишнее — **400** с именем поля: отвергается, не глотается
(`api-conventions.md` §«Принято-и-проигнорировано»)
**And** сессия не выдана; испытание **не сгорело** — форма отвергнута до сверки; в счёт
частоты не пошло (Р9)
**And** `secondFactor` здесь — вторая сторона оси Р5, не оговорка формы: поле у входа паролем
законно (Ф12 Р4), у входа ключом — нет
**And** положительный контроль — полная форма проходит (Ф13-05)

**Ф13-08 — испытание однократно и сгорает первым предъявлением, каким бы ни был исход** · E

**Given** личность с принятым ключом; испытание `C` выдано
**When** (а) годное утверждение над `C` предъявлено дважды; (б) в новом контексте: над `C'`
предъявлено утверждение с негодной подписью, затем над **тем же** `C'` — годное; (в) затем
новое `begin` и годное утверждение над `C''`
**Then** (а) первое — `200`, второе — **единый отказ входа** (Ф7-53 на полосе сессии; здесь
повтор — та же полоса «ж»); (б) оба — единый отказ: второе не потому, что подпись негодна, а
потому, что `C'` сгорело; (в) — `200`
**And** внесённое различие в (б) ровно одно: **порядок** — годное после негодного; годное над
свежим испытанием проходит (в), то есть отвергнут был не ключ, а сгоревшее испытание
**And** отвергнутая реализация названа: испытание, живущее после отказа, зеленело бы на (а) и
(в) и краснело бы только на (б)

**Ф13-09 — вход ключом при живом носителе в браузере: новая запись, прежняя жива; отсечённый носитель — отказ края** · E

**Given** личность с паролем и принятым ключом; сессия `S1` выдана входом паролем, её носитель
в браузере; контекст формы получен под `S1`
**When** `begin` и `login` ключом с носителем `S1` в `Cookie` (через край — Ф13-28; на слушателе
службы — напрямую)
**Then** `200`; выдана **новая** запись `S2` с `{webauthn}` и своим уровнем; `Set-Cookie`
заменяет носитель в браузере; `S1` **жива** до срока либо отзыва (Ф1-13: один вход — одна
запись); её носитель, сохранённый вне браузера, по-прежнему годен и несёт «1»
**And** через край с носителем сессии, покрытой **отсечкой** принудительного выхода, — оба
глагола получают отказ **F4d-22 на крае**, до службы запрос не доходит (Ф3-51 дословно; Ф13-28)
— это отказ **края**, не отказ полосы входа, и в перечень §7 инв. 4 не входит
**And** положительный контроль различимости: с носителем сессии, **снятой выходом**, оба глагола
ретранслированы — «сессии нет» судит служба, и вход выдаёт новую запись (Ф3-51 положительный
контроль)

### Полоса «уровень» — Ф11-03, -04, -05 исполняются здесь под своими номерами

**Ф13-10 — Ф11-03: ключ без проверки пользователя, резервного копирования не допускает → «2»** · E

**Given** личность с принятым ключом; иных способов, кроме пароля, нет
**When** вход ключом; аутентификатор сообщил: проверка пользователя **не** выполнена, резервное
копирование **не** допускается — единственное отличие от Ф13-11
**Then** уровень «2» (Ф11 §3.0): глагол с полом «2» проходит пол; ответ службы краю о сессии
называет «2»; `session.assuranceLevel: "2"` в ответе входа
**And** положительный контроль пары с Ф13-11: без него «3» было бы неотличимо от «полоса всегда
отвечает «3»»

**Ф13-11 — Ф11-04: проверка пользователя выполнена, резервного копирования не допускает → «3»** · E

**Given** то же
**When** вход ключом; аутентификатор сообщил: проверка пользователя **выполнена**, резервное
копирование **не** допускается
**Then** уровень «3»: ответ службы краю о сессии называет «3»; глагол с полом «2» проходит
пол; `session.assuranceLevel: "3"`
**And** край принимает «3» как значение оси сессии и ранжирует общим правилом — пол «2»
пройден, а не отвергнут «значением вне оси» (Ф11-19 — отрицательный контроль: `aal3`
словаря поставщика край **не** переводит)
**And** **один и тот же ключ** даёт «2» в Ф13-10 и «3» здесь: уровень — свойство утверждения, а
не способа (Ф11 Р1); запись сессии несёт уровень, выведенный при выдаче, и он не меняется до
срока либо отзыва

**Ф13-12 — Ф11-05: проверка выполнена, резервное копирование допускается → «2», не «3»** · E

**Given** то же
**When** вход ключом; аутентификатор сообщил: проверка пользователя выполнена, резервное
копирование **допускается** — единственное отличие от Ф13-11
**Then** уровень «2», не «3»: ответ службы краю о сессии называет «2» (Ф11 Р3)
**And** ключ, выпущенный до появления флагов и сообщающий их нулями, читается как Ф13-10 —
«2» (Ф11 Р3, граница названа там)

**Ф13-13 — копии уровня читают запись: «3» остаётся «3» в ответе смены пароля и церемонии** · E

**Given** личность с паролем и принятым ключом; сессия `S` выдана входом ключом на уровне «3»
(Ф13-11); близнец — сессия `S'` того же человека на «2» (Ф13-10)
**When** (а) из `S` — `POST /iam/v1/auth/password` с верным текущим паролем и годным новым
(Ф3-19); (б) из `S` — `POST /iam/v1/auth/step-up` с `method: "password"` (Ф11-10 форма);
то же из `S'`
**Then** (а) ответ несёт `session.assuranceLevel: "3"` и новый носитель; (б) ответ церемонии
называет достигнутый уровень «3» и **пустой** перечень недостающего; из `S'` — «2» в обоих
**And** уровень **записи** `S` до и после — «3» (I): копия читает запись, а не пересчитывает
от имён способов (Ф11 Р1); понижение в пределах сессии невыразимо (Ф11 Р2)
**And** отвергнутая реализация названа: пересчёт от `{webauthn}` с подставленными слабыми
флагами даёт «2» при записи «3» — это дерево **сегодня** (§1.1 ∅5), и на нём этот сценарий
**красный по существу**; производитель — `kaname#208` (§4.3)

**Ф13-14 — страж посадки `own` считает предъявимые уровни с ключом: «1», «2», «3»** · I

**Given** посадка `own`; полоса формы провязана вместе с полосой ключа (Р14)
**When** старт службы
**Then** самоотчёт старта печатает перечень предъявимых уровней **«1», «2», «3»** — посчитанный
правилом Ф11 Р9 из провязанных способов (`password`, `webauthn`, ключ — в лучшем исходе
флагов), а не выписанный
**And** близнец: та же посадка **без** полосы ключа — перечень «1» (Ф3), с Ф12 без ключа —
«1», «2» (Ф12-34); каталог с полом «2» (28 записей на `6acf8f19`, **29** на `94352d9c` —
`UserService/ResendInvite` пришёл линией приглашений; число — ориентир, свойство — ниже) проходим
на посадке, где провязан
**хотя бы один** производитель «2»
**And** ручек посадки полоса ключа не добавляет (Р9): страж старта не получил ни одной новой
ветви — перепись его ветвей до и после Ф13 равна

### Полоса «второй фактор» — обе стороны оси

**Ф13-15 — вход ключом при заведённом втором факторе: кода не требуется, уровень по ключу** · E

**Given** личность с паролем, принятым ключом и **заведённым** вторым фактором (Ф12-02 либо
записью — §5 преамбула)
**When** вход ключом (Ф13-05), без кода
**Then** `200`; уровень «2» либо «3» по флагам — как без фактора; глагол с полом «2» проходит
пол **без** единого предъявления кода
**And** `GET /iam/v1/auth/second-factor` из этой сессии отвечает «заведён» (Ф12 Р4) —
состояние фактора и множество предъявленного `{webauthn}` — разные факты
**And** близнец с другой стороны оси — Ф12-14: тот же человек паролем входит на «1» и код
предъявляет внутри сессии либо полем у входа; ключ этого не требует, потому что сам производит
«2» (Ф12 Р3, Ф11 Р2)

**Ф13-16 — поле `secondFactor` у входа ключом не принимается** · E

**Given** то же
**When** `login` с полем `secondFactor: {"method": "totp", "code": <верный>}`
**Then** `INVALID_ARGUMENT` / **400**, текст называет поле `secondFactor`; сессия не выдана;
код **не потреблён** — тот же код после этого проходит в `step-up` (Ф13-17)
**And** положительный контроль — тот же запрос без поля проходит (Ф13-05)
**And** отвергнутая реализация названа: полоса, принимающая поле и сверяющая код, зеленела бы
на «2» и не имела бы ни одного наблюдаемого следствия сверки — ровно «принято-и-проигнорировано»

**Ф13-17 — предъявление внутри сессии, выданной ключом: проходит, носитель перевыпущен, уровень прежний** · E

**Given** сессия `S3` уровня «3» и сессия `S2` уровня «2», обе выданы входом ключом; у человека
заведены пароль и второй фактор
**When** внутри каждой — `POST /iam/v1/auth/step-up`: (а) `method: "totp"`, верный код;
(б) `method: "password"`, верный пароль; (в) `method: "totp"`, **неверный** код
**Then** (а), (б) — ответ церемонии называет достигнутый уровень **равным прежнему** («3» у
`S3`, «2» у `S2`) и пустой перечень недостающего; носитель **перевыпущен** — прежний получает
отказ, побайтово равный отказу по несуществующей сессии (Ф11 Р5, Ф11-09); момент последнего
предъявления сдвинут (окно свежести правки своих данных освежено — Ф11-32)
**And** (в) — отказ предъявления; уровень и носитель прежние, носитель **годен** (Ф11-30)
**And** множество предъявленного накопилось (`{webauthn, totp}`, `{webauthn, password}`) и
уровень не понизился и не повысился: строки правила выше «3» нет, а «2» ключа — не ниже
«пароль и второй фактор» (Ф11 Р2 монотонность)

### Полоса «две полосы входа — откат к паролю, личность без пароля»

**Ф13-18 — человек с паролем и ключом входит любой полосой; полосы друг о друге не знают** · E

**Given** личность с паролем и принятым ключом; два браузера
**When** в первом — вход паролем (Ф3-01); во втором — вход ключом (Ф13-05)
**Then** первая сессия — `{password}`, уровень «1»; вторая — `{webauthn}`, «2»/«3»; две записи
(Ф1-13); выход из одной не гасит другую (Ф3 Р4)
**And** вход паролем **не** требует ключа и не читает его строк; вход ключом не требует пароля
и не читает его строки (Ф13-19) — «откат к паролю» есть свойство двух независимых полос, а не
глагол (Р6)
**And** отрицательный контроль назван: реализация, отключающая пароль при заведённом ключе,
краснеет на первой половине; требующая пароль у входа ключом — на Ф13-19

**Ф13-19 — личность без пароля: ключом входит, паролем — единый отказ Ф3-02** · E

**Given** личность без пароля, с ключом (§5 преамбула: переносом либо записью)
**When** (а) вход ключом; (б) `POST /iam/v1/auth/login` с её адресом и любым паролем
**Then** (а) `200`, сессия выдана (Ф13-05) — **первое** действие, которым такая личность
получает сессию в нашем продукте (Ф7-26, Ф7-16: до Ф13 — записью); (б) — `401`
`authentication failed`, **побайтово равный** Ф3-02 (случай C — «личность без строки способа
входа»)
**And** обе стороны оси: (а) — ключ достаточен; (б) — пароля нет, и полоса пароля этого не
раскрывает
**And** что такая личность **не может** — названо, а не умолчано: завести пароль из сессии
(`kaname#213`) и — до посадки Ф5 по `kacho#2698` — восстановиться при утрате всех ключей
(Ф13-25); ни то, ни другое эта фаза не производит (§4.2)

**Ф13-20 — личность без ключей: полоса ключа отвечает единым отказом на любое утверждение** · E

**Given** личность с паролем, ключей нет; подставной аутентификатор, у которого есть ключ
**другой** личности
**When** над выданным испытанием предъявлено утверждение чужого ключа с `userHandle` этой
личности
**Then** **единый отказ входа** — сверяется рукоятка **строки ключа** (владелец — другой
человек), а не присланная (Ф13-06 «з»); сессия никому не выдана
**And** что аутентификатор человека без ключей ничего **не предлагает**, службе не наблюдаемо
(Р2) и здесь не утверждается

**Ф13-21 — снятие ключа гасит прочие сессии человека причиной `access-key-revoked`; текущая остаётся** · E

**Given** личность с паролем и двумя принятыми ключами `A`, `B`; сессия `S1` выдана входом
ключом `A` (уровень «3»), сессия `S2` — входом паролем; момент последнего предъявления в `S2` в
окне свежести (Ф7 Р5)
**When** из `S2` — снятие ключа `A` (`AccessKeyService/Revoke`, Ф7-25)
**Then** `S1` **снята**: её носитель получает отказ, побайтово равный отказу по несуществующей
сессии (Ф3-13), у края — отсечкой (Ф3 Р7); `S2` **жива**, её уровень «1» не изменён; утверждение
ключом `A` — единый отказ входа (Ф13-06 «к»), ключом `B` — вход проходит (Ф7-25)
**And** событие снятия ключа Ф7 и снятие сессий — **одним исходом** одной транзакции (форма
Ф3-16: подставной отказ любой из записей откатывает всё — ключ на месте, `S1` жива); причина
отзыва в журнале и счётчиках — `access-key-revoked`, актор — сам человек (Р8)
**And** положительный контроль: до снятия носитель `S1` годен и несёт «3»
**And** вторая ветвь оси, и её цена названа: из `S2` снят **не** ключ `A`, а `B` — `S1`
(выданная `A`) **тоже** снята: гаснут все прочие сессии, а не «сессии этого ключа» (Р8: запись
сессии ключа не называет, состав Ф3 Р1 закрыт); Ф12 Р9 — тот же образец
**And** сброс ключа **распорядителем** этим сценарием не наблюдается — глагола нет
(`kacho#2702`)

**Ф13-22 — наш глагол человека не запирает: последний ключ без второго способа не снимается** · E

**Given** (а) личность без пароля с одним ключом; (б) та же личность с двумя ключами
**When** (а) снятие единственного ключа; (б) снятие первого из двух
**Then** (а) отказ, называющий следующий шаг — завести другой способ входа прежде; ключ на месте,
вход им проходит (Ф7-26 — держатель Ф7, здесь положительный контроль полосы входа: «Дано»
построено действием Ф13-19); (б) ключ снят, вход **вторым** ключом проходит (Ф7-25 → Ф13-05)
**And** «другой способ входа» у личности без пароля — сегодня только второй ключ (Ф7-14):
пароль ей завести нечем (`kaname#213`). Текст отказа — Ф7-26, он называет **шаг**, а не
перечень способов; перечень доступных способов показывает консоль (Ф8)

### Полоса «перенесённый ключ»

**Ф13-23 — перенесённый ключ с рукояткой входит; рукоятка — та, что несёт источник** · E

**Given** личность **без пароля**, чей единственный ключ перенесён (Ф7-23); строка ключа несёт
рукоятку **источника** — не то значение, что кладёт церемония Ф7 (Р3); подставной аутентификатор
возвращает в `userHandle` ту же рукоятку
**When** вход ключом
**Then** `200` (Ф13-05); уровень по флагам; событие несёт `id` **перенесённой** строки
**And** положительный близнец Ф13-24; внесённое различие — наличие рукоятки в строке
**And** тот же человек — **без пароля** — из **этой** сессии, выданной ключом, проводит церемонию
**нового** ключа (глагол Ф7-01; её «Дано» там строится из сессии паролем, здесь — из сессии
ключа: Ф7 Р5 допускает церемонию из сессии человека, у которого уже есть способ входа, Ф7 Р11 —
пол «1», момент последнего предъявления — сам вход, окно свежести открыто): церемония
**проходит**, ключ принят; входит им с рукояткой, равной **случайному значению этого человека**
(Р3), — у одного человека две строки с **разными** рукоятками, и полоса сверяет каждую со **своей**
строкой. Сценарий от редакции 4 не изменился ни одним «Тогда»: он утверждает **разность** двух
рукояток и сверку каждой со своей строкой, а не то, чему равна вторая, — и потому держит решение
Р3 при **любом** его исходе. Это третье «Дано» Ф7, получающее строителя здесь (§6.1); до Ф13 его
не построить ничем

**Ф13-24 — перенесённый ключ без рукоятки в полосу входа не проходит; перепись называет число** · E · P

**Given** источник, где у одного ключа рукоятки нет; перенос (Ф7-21…23) прогнан
**When** (P) перепись прогоняется; (E) вход этим ключом
**Then** (P) перепись печатает **пару** — «строк ключа с рукояткой N · без рукоятки M» —
**дописка к «Тогда» Ф7-21** (§3 Д28) — рядом с числом людей без пароля Ф7-24;
(E) **единый отказ входа**: сверить нечего, и не сверять нельзя (Р3)
**And** ключ при этом **принят** в смысле Ф7 §3.0: утверждение им из сессии, выданной паролем
(Ф7-06), проходит — полоса сессии рукоятки не сверяет, вызывающий там назван; различие полос
названо, а не умолчано
**And** человек с таким ключом и **без пароля** — **отдельным** числом рядом с Ф7-24, не
растворённым в нём (у Ф7-24 причина — алгоритм, здесь — рукоятка; §3 Д29), и под тем же
условием: второй способ войти ему обязан быть назван до объявления переноса состоявшимся
(Ф7-24 «И», Р11); положительный контроль тот же — человек с таким ключом **и паролем** в это
число не входит

### Полоса «утрата единственного ключа» — производитель шага (2) — Ф5 по решению `kacho#2698`

**Ф13-25 — личность без пароля, утратившая ключ, возвращает доступ восстановлением и заводит новый ключ** · E — **исполним после посадки Ф5 по `kacho#2698`**

**Given** личность без пароля с одним ключом (Ф13-19); адрес подтверждён; устройство с ключом
утеряно — аутентификатора у пробы больше нет, строка ключа на месте
**When** (1) `POST /iam/v1/auth/recovery` с адресом (Ф5-01); (2) код из письма предъявлен в срок
с новым паролем — `POST /iam/v1/auth/recovery/complete` (Ф5-03); (3) вход этим паролем
(Ф3-01); (4) из этой сессии — церемония регистрации нового ключа (Ф7-01); (5) снятие утраченного
ключа по его `id` (Ф7-25)
**Then** (2) — сессия выдана (Ф1-27), **строка способа «пароль» заведена** — у человека, у
которого её не было: тем же писателем ID-PW-1, что замещает существующую (`kacho#2698`, исход 1;
производитель — Ф5 `#1271`); (3) — `200`, «1»; (4) — ключ принят; (5) — ключ снят, утверждение
им — единый отказ входа (Ф13-06 «к»); новым ключом человек входит (Ф13-05)
**And** сброс распорядителем (F4d Р4) путём возврата **не является** и здесь не наблюдается:
производителя нет (`kacho#2702`); Ф5 Р6 сосуществование двух путей не отменяет, проба наблюдает
тот, у которого производитель назначен
**And** **на дереве сегодня** (`0cb0eddd`) шаг (2) отвечает `503 request not performed; try again
later` и откатывается — код остаётся годным, строки нет (§1.1 ∅7). Это **не** красный по
существу, а «не выполнилось»: исход решён, производитель заказан и не посажен (Р12 п. 3). Проба
до посадки печатает «условие не создано: производитель шага (2) — Ф5 по `kacho#2698` — не
посажен» и в вердикт DoD не засчитывается ни зелёным, ни красным (§9 п. 7); после посадки —
сквозная по пяти шагам, честный красный до Ф7 (§8)
**And** отвергнутый исход 2 (`#2698`: терминальный отказ с текстом о распорядителе) сценарий
**не** переписывает: он держался бы на глаголе, которого нет (`kacho#2702`), и назван в решении
с ценой. Довод Ф13 к выбору — Р12 — совпал с принятым

### Полоса «защита форм, край, посадка, журнал, дерево»

**Ф13-26 — признак формы на входе: контекст, вид, смена контекста выдачей** · E

**Given** личность с принятым ключом; контекст формы `K1` и признак `T1` вида
`access-key-login`; испытание выдано под `K1`
**When** (а) `login` без `csrfToken`; (б) с признаком вида `password`; (в) после входа паролем
в том же браузере (контекст сменён на `K2`) — с `T1`; (г) с годным `T1` под `K1`
**Then** (а) — 400 с именем поля; (б), (в) — 403 `FORM_TOKEN_REJECTED`, неразличимо; (г) —
`200` (Ф13-05), и ответ несёт **новый** `kaname_form`: признак `T1` после выдачи негоден
(Ф1-41 на этой полосе)
**And** в (в) испытание, выданное под `K1`, для `K2` — чужого контекста: даже с годным для `K2`
признаком утверждение над ним — единый отказ (Ф13-06 «ж»)

**Ф13-27 — вид формы `access-key-login` в закрытом перечне одним объявлением** · E · P

**Given** посадка `own`
**When** `GET /iam/v1/auth/csrf?form=access-key-login`; затем `?form=access-key`
**Then** первый — `200` с признаком; второй — 400, текст называет поле `form` (Ф3-35)
**And** (P) перепись видов формы печатает **7** (шесть Ф3, Ф4, Ф5 плюс один); объявление одно
— второе написание вида где-либо в прод-дереве службы — находка гейта Ф3 §8 инв. 7

**Ф13-28 — край ретранслирует два глагола через полосу сессии; отсечка — на крае, «сессии нет» — службе** · E

**Given** край посадки `own` с дублёром службы, считающим ретрансляции и читающим заголовки
(Ф3-51 форма); носители: без носителя; `A` — живая сессия; `B` — сессия, покрытая отсечкой
принудительного выхода; `C` — снятая выходом; чужие заголовки `x-kacho-principal-*` в запросе
**When** каждый из двух глаголов Р1 — без носителя, с `A`, с `B`, с `C`; затем с приложенными
заголовками пространства `x-kacho-`
**Then** без носителя, `A`, `C` — ретранслировано 2 каждого: запрос несёт `Cookie` (где есть)
и **один** `X-Forwarded-For`, **ноль** заголовков пространства `x-kacho-` в обеих формах
написания, без `Authorization` (Ф3-51, Ф12-38); `B` — отказ **F4d-22 на крае**, носитель гасится,
ретранслировано 0
**And** оба пути стоят в перечне путей без записи каталога (`isPublicHTTPPath`) и в перечне
ретрансляции — **тем же** объявлением, что семь глаголов Ф3, Ф4, Ф5 (Ф3 §8 инв. 7); решение по
каталогу прав, пол уверенности и полоса привязки предъявителя на них **не** исполняются (Ф3 Р2)
**And** недоступность службы (`Resolve` отвечает `UNAVAILABLE`) на обоих глаголах даёт **тот
же** исход, что у `POST /iam/v1/auth/login` при том же дублёре и том же носителе — полосы
входа ведут себя одинаково, и это утверждается **парой**, а не значением
**And** ретрансляция семейства Ф3 — в стволе платформы (`#2689` влит, `275240efdc`); без двух
записей Ф13 (DoD п. 8) сценарий на стволе — красный **честный** (`404` на обоих путях при
дублёре, считающем ретрансляции), после них — исполним против дублёра

**Ф13-29 — журнал: событие выдачи несёт способ и `id` ключа; секрета нет; отказы событий не порождают** · I

**Given** вход Ф13-05 (успех) и отказ Ф13-06 «а»
**When** очередь аудита читается
**Then** ровно **одно** событие `iam.session.issued` — с `user_id`, `session_id`,
`methods: ["webauthn"]`, `access_key_id` формы `ak-…`; событие **без** адреса, имени, подписи,
данных аутентификатора, рукоятки, идентификатора удостоверения и значения носителя
(`TestAuditPayloadCarriesNoPersonalData` на новом поле — держатель); отказ события не породил
**And** событие пишется **той же транзакцией**, что запись сессии (Ф-а): подставной отказ
записи события откатывает выдачу — сессии нет (форма Ф4-04)

**Ф13-30 — счётчики: клетки исходов полосы ключа, нулевые печатаются** · I

**Given** прогоны Ф13-01, Ф13-05, Ф13-06 (каждая ветвь), Ф13-04
**When** читаются счётчики
**Then** клетки полосы: «испытание выдано» · «вход состоялся» · «единый отказ» по причине
(подпись · удостоверение неизвестно · происхождение · хэш имени · присутствие · счётчик ·
испытание · рукоятка · блокировка) · «отказ по частоте» · «хранилище не ответило»;
каждая клетка **существует до первого события** (нулевая печатается — Ф3 Р14); причина
отказа различима **здесь и только здесь** (Р7)
**And** клетки «ключ снят» **нет** (Р15): прогон Ф13-06 «к» увеличивает «удостоверение
неизвестно» — ровно как прогон «б», и это утверждается парой: прогон пары (б), (к) — клетка +2,
прочих клеток «единого отказа» эта пара не тронула; у каждой клетки перечня назван исход §5,
который её увеличивает, и клетки, которую не увеличивает ни один исход, в перечне нет
(`testing.md` §«Гейт на класс», п. 6)

**Ф13-31 — сверка утверждения в дереве ОДНА: второй проверяющий — находка** · P

**Given** прод-дерево службы после Ф7 и Ф13
**When** гейт дерева (`internal/check`, заводится Ф13 — §8) обходит прод-файлы
**Then** объявление сверки подписи утверждения ключа — **одно** (Ф7); полоса входа зовёт его,
а не копию; перепись печатает «файлов осмотрено N · объявлений сверки 1 · вызывающих 2» (глаголы
утверждения Ф7 и глагол входа)
**And** способность упасть доказана инъекцией в обе стороны: вторая сверка (копия функции под
другим именем в пакете полосы формы) — красное с координатой; законный близнец — обёртка,
**зовущая** первую, — молчит; пустой обход — отказ, а не «ноль находок»

---

## §6 Ведомость: сценарий → производитель, род требуемого и чем строится «Дано»

Столбцов четыре, как у Ф11 §5 и Ф12 §6: «Тогда производит» — фаза либо существующая форма §1.2,
чьё изменение делает исход верным; «род» — что требуется; «Дано строится» — кто производит
условие; «в DoD» — фаза, влитая последней из тех, что названы в «Дано». Фаза-производитель у
каждого «Тогда» названа правилом «**чьё** изменение делает исход верным»: **Ф7 · Ф12 · Ф13 ·
задача**; существующие формы — буквой из §1.2. Редакции 1…3 обосновывали это фразой «код Ф7 не
начат» — **она перестала быть верной** (шапка, перемер редакции 4), а правило от её падения не
двигается: код Ф7 влит, и строки с производителем **Ф7** от этого стали лишь исполнимее.

| сценарий | Тогда производит | род | Дано строится | в DoD |
|---|---|---|---|---|
| Ф13-01 | **Ф13** — глагол `begin`, форма ответа; литералы — Ф7 Р2, Р9 | ответ + значение | Ф3 (контекст), Ф13 (провязка) | Ф13 |
| Ф13-02 | Ф-д (признак формы) — **Ф3**; форма запроса — **Ф13** | отказ | Ф3 | Ф13 |
| Ф13-03 | **Ф13** — одно испытание на контекст | отказ + проход | Ф7 (ключ), Ф13 | Ф13 |
| Ф13-04 | Ф-г (счёт по источнику) — **Ф3**; `begin` как попытка — **Ф13** | отказ + проход | Ф3 | Ф13 |
| Ф13-05 | **Ф13** — глагол `login`, выдача через Ф-а; сверка — **Ф7**; событие — Ф-е с полем Ф13 | проход + значение + запись | Ф7 (Ф7-01), Ф3, Ф13 | Ф13 |
| Ф13-06 | сверка (а)…(ж) — **Ф7**; (з) рукоятка, (и), (к), единый текст — **Ф13**; (л) время — Ф7 + Ф13 | отказ + неизменность + время | Ф7, Ф13; (и) — блокировка существующим глаголом | Ф13 |
| Ф13-07 | **Ф13** — форма | отказ | Ф7, Ф13 | Ф13 |
| Ф13-08 | **Ф13** — однократность испытания входа | отказ + проход | Ф7, Ф13 | Ф13 |
| Ф13-09 | Ф-а (одна запись на вход) — **Ф3**; отсечка на крае — край Ф3 (`#2689`) | проход + отказ края | Ф3, Ф7, Ф13; край | Ф13 (край — в стволе платформы) |
| Ф13-10 | **Ф11** — строка «2» ключа (в стволе); флаги — **Ф7** (Ф7-28…30); выдача — **Ф13** | значение | Ф7, Ф13 | **Ф13** — Ф11-03 |
| Ф13-11 | **Ф11** — строка «3»; ответ службы краю — Ф3; край — ось «3» (`#2689`) | значение | Ф7, Ф13; край | **Ф13** — Ф11-04 |
| Ф13-12 | **Ф11** — различитель Р3 | значение | Ф7, Ф13 | **Ф13** — Ф11-05 |
| Ф13-13 | **`kaname#208`** — копия читает запись; «Дано» — Ф13 | значение | Ф7, Ф13; (б) церемония — Ф12 (адрес) | последняя из Ф13 и `#208`; (б) — и Ф12 |
| Ф13-14 | Ф-ж (страж, Ф11 Р9) — **Ф11**; провязка — **Ф13** | величина (самоотчёт) | Ф13 | Ф13 |
| Ф13-15 | **Ф12 Р3** (обязательность действием); выдача — **Ф13** | проход + значение | Ф7, Ф13; фактор — Ф12 либо записью | Ф13 (записью) / Ф12 |
| Ф13-16 | **Ф13** — форма без поля | отказ | Ф7, Ф13; фактор — записью | Ф13 |
| Ф13-17 | **Ф11 Р5**, церемония — **Ф12** (адрес и ветви); монотонность — Ф11 Р2 | значение + носитель | Ф7, Ф13, Ф12 | Ф12 |
| Ф13-18 | **Ф3** (вход паролем) + **Ф13** (вход ключом); Ф1-13 | значение + независимость | Ф7, Ф3, Ф13 | Ф13 |
| Ф13-19 | **Ф13** (а); **Ф3-02** (б) | проход + отказ | перенос Ф7-23 либо запись; Ф13 | Ф13 |
| Ф13-20 | **Ф13** — сверка рукоятки со строкой | отказ | Ф7, Ф13 | Ф13 |
| Ф13-21 | **Ф13** — дописка к глаголу снятия Ф7: отзыв прочих сессий, причина `access-key-revoked` | отказ + неизменность + запись | Ф7 (два ключа, снятие), Ф3 (сессии), Ф13 | Ф13 |
| Ф13-22 | **Ф7-26** (а), **Ф7-25** (б); «Дано» — Ф13 | отказ + проход | Ф7, Ф13 | Ф13 |
| Ф13-23 | перенос с рукояткой — **Ф7** (Ф7-23, столбец Р3); вход — **Ф13** | проход | Ф7 (перенос) либо запись | Ф13 |
| Ф13-24 | перепись — **Ф7-21** + чтение величины **Ф13**; отказ — **Ф13** | величина + отказ | перенос либо запись | Ф13 |
| Ф13-25 | (2) — **Ф5** `#1271` по `kacho#2698` (исход 1; редакция Ф5 и код — заказаны, не посажены); (1), (3)…(5) — Ф5, Ф3, Ф7 | проход + запись | Ф13 (личность без пароля с ключом), Ф5 | **условие** — §9 п. 7 |
| Ф13-26 | Ф-д — **Ф3**; смена контекста выдачей ключом — **Ф13** | отказ + проход | Ф7, Ф3, Ф13 | Ф13 |
| Ф13-27 | **Ф13** — вид в перечне Ф3 | проход + отказ + величина | Ф3 | Ф13 |
| Ф13-28 | край — ретрансляция (`#2689` + две записи **Ф13**) | ретрансляция + отказ края | край, дублёр | Ф13 (дом платформы) |
| Ф13-29 | Ф-е — **Ф3**; поле `access_key_id` — **Ф13** | запись | Ф7, Ф13 | Ф13 |
| Ф13-30 | **Ф13** — клетки | величина | Ф13 | Ф13 |
| Ф13-31 | **Ф13** — гейт дерева | свойство дерева | Ф7 + Ф13 в дереве | Ф13 |
| Ф13-32 | Ф-г (счёт по адресу, обнуление успехом) — **Ф3**; вход ключом как успех — **Ф13** | проход + отказ + величина | Ф3, Ф7, Ф13 | Ф13 |

**Число выводится, а не выписывается:** сценариев **32**, производитель «Тогда» назван у 32;
исполняются в DoD Ф13 — **29** (из них два — Ф13-09 в части края и Ф13-28 — пробы **дома
платформы** против дублёра, в стволе платформы после `#2689`); в DoD Ф12 — **1** (Ф13-17: «Дано» —
её церемония); при условии — **2**: Ф13-13 (`kaname#208`), Ф13-25 (Ф5 по `kacho#2698` — не посажена). 29 + 1 + 2 = 32.
Редакция 1 несла 31: Ф13-32 заведён редакцией 2 (Н5 круга 1).

```sh
# ДОМ: PRO-Robotech/kaname · единица — различная метка сценария в §5 / строка ведомости §6
F=docs/engineering/acceptance/passwordless-login-with-access-key.md
awk '/^## §5 /,/^## §6 /' "$F" | grep -oE '^\*\*Ф13-[0-9]+' | sort -u | wc -l                    # → 32
awk '/^## §6 /,/^### §6.1/' "$F" | grep -cE '^\| Ф13-[0-9]+ '                                      # → 32
# латинское написание — перепись на одной форме недобирала бы молча
awk '/^## §5 /,/^## §6 /' "$F" | grep -cE '^\*\*F13-[0-9]+'                                        # → 0
```

### §6.1 Ведомость соответствия с Ф7 §4.5: каждая строка шва — и что её производит здесь

| строка Ф7 §4.5 | чей | что здесь |
|---|---|---|
| обнаружение ключа до входа — кому какие ключи предлагать, не назвав человека | **Ф13** | Р2, Ф13-01: испытание без имени, `allowCredentials` пуст |
| требование обнаруживаемого удостоверения при регистрации | Ф7 — Р9, Ф7-40/41 | переиспользуется; Ф13 читателя признака не заводит (§4.2) |
| обнаруживаемость перенесённых ключей — не установлена | никому | Р11: не наблюдаемо нам; **рукоятка** — наблюдаемо (Ф13-24): Ф13 читает её переписью Ф7-21 — **дописано** (Д28); число людей без пароля с таким ключом — рядом с Ф7-24, дописано (Д29) |
| требование проверки пользователя на обеих церемониях | Ф7 — Р9 | тот же литерал в третьем испытании (Ф13-01); флаг читает Ф11 (Ф13-10…12) |
| выдача сессии по утверждению ключа | **Ф13** | Р4, Ф13-05 |
| множество предъявленного: что в него попадает | Ф11 — правило; Ф7 — что регистрация не попадает | `{webauthn}` с флагами — Ф13-05; накопление — Ф13-17 |
| путь при утрате ключа у человека без пароля — решён F4d Р4 / F4e Р6 и Ф5; за Ф13 только вход | чужие | Р12, Ф13-25: путь решён — восстановление (`kacho#2698`, исход 1), производитель Ф5 заказан и не посажен; распорядитель — сирота `kacho#2702`, не путь; §0.4 |
| уровень после входа ключом | Ф11, исполняет Ф13 | Ф13-10…12 = Ф11-03…05 |
| кто вызывающий у глаголов утверждения — у Ф13 вызывающего нет, свой глагол и своя причина | Ф7 / **Ф13** | Р1: глагол свой, глагол **формы**; причина беспредметна (§3 Д1) |
| потолок числа ключей человека | Ф7 — Р8 | не трогается |
| регистрация личности ключом без пароля | никому из трёх | §4.2: своя приёмка, носитель — `kacho#2703` |

Сверх таблицы Ф7 — **три** «Дано», получающие строителя здесь. Два Ф7 строила записью «до
Ф13»: **Ф7-26** (человек без пароля, «его сессия до Ф13 строится записью») и **Ф7-16** (в
редакциях 1…6 — человек без пароля) — после Ф13 оба строятся действием, Ф13-19. Третье —
**церемония Ф7-01 из сессии, выданной ключом, у человека без пароля**: такого «Дано» у Ф7 нет
(её Ф7-01 — из сессии паролем), Ф7 Р5 и Р11 его допускают, а строимым действием оно становится
с Ф13 — Ф13-23 утверждает проход церемонии явно (Н8 круга 1). Ведомость Ф7 §5 не правится
(одобрена), строители названы здесь.

### §6.2 Ведомость соответствия с Ф3: каждое решение Ф3, которое полоса ключа переиспользует

| решение Ф3 | что здесь |
|---|---|
| Р1 — состав записи сессии закрыт | не расширяется (Д13); множество `{webauthn}` — Ф13-05 |
| Р2 — семейство, ретрансляция, один текст отказа | два глагола в семействе (Р1); текст — Ф13-06 |
| Р3 — печенье, атрибуты, смена контекста выдачей | Ф13-05, Ф13-26 |
| Р7 — «сессии нет» ретранслируется, отсечка — край | Ф13-09, Ф13-28 |
| Р9 — причины отзыва, один отказ предъявителю | седьмая причина `access-key-revoked` (после шестой Ф12 Р10) — Ф13-21 |
| Р10 — два счёта, успех обнуляет счёт по адресу | источник, `begin` — попытка — Ф13-04; успех — Р9 |
| Р12, Ф3-35 — признак формы, виды | Ф13-02, Ф13-26, Ф13-27 |
| Р14 — событие выдачи, счётчики с нулевыми клетками | Ф13-29, Ф13-30 |
| Р16 — слушатель допускает ровно край | Ф13-28 (край) и на слушателе службы напрямую — те же пробы, что Ф3-29 (пир не край — 403) |
| Ф3-05 — форма: отсутствующее называется, лишнее отвергается | Ф13-02, Ф13-07 |
| Ф3-51 — глагол формы с отсечённым носителем | Ф13-09, Ф13-28 |

### §6.3 Строки Ф11 и Ф12, называющие эту фазу, — и что их производит здесь

| источник | строка | здесь |
|---|---|---|
| Ф11 §5.2 | «Ф13 — 03, 04, 05» | Ф13-10, -11, -12 в DoD Ф13 под номерами Ф11 |
| Ф11 §10 п. 3 | «тела … говорят «проверка присутствия» — при их закрытии свести к Р3» | §0.4; Ф13-06 «д» — присутствие как сверка, Ф13-11 — проверка пользователя как различитель |
| Ф11 §6 | «правило заведения второго фактора и ключа» — Ф12, Ф7 | не предмет: заведение ключа — Ф7 Р5, Р11 |
| Ф12 §0.2, §4.2 | «ключ доступа как второй либо первый фактор — Ф7, Ф13» | первый — здесь целиком; второй — §4.2 |
| Ф12 Р4 | «`webauthn` до Ф7 — 400» у `step-up` | не переписывается (§4.2) |

---

## §7 Инварианты (MUST hold — для ревьюера)

1. **Каждое «Тогда» описывает наблюдаемое**, а не устройство: ни один сценарий не называет
   таблицу, колонку, функцию или библиотеку; «рукоятка хранится со строкой» наблюдается
   расхождением (Ф13-06 «з») и переписью (Ф13-24), а не чтением схемы.
2. **Сверка утверждения в дереве ОДНА.** Полоса входа зовёт проверяющего Ф7; второе объявление
   — находка гейта (Ф13-31); отказы обеих полос на одном входе побайтово равны (Ф13-06).
3. **Пароль на полосе не предъявляется и не читается**: ни один запрос полосы не несёт поля
   `password` (Ф13-05, Ф13-07); личность без строки пароля входит (Ф13-19).
4. **Единый отказ входа — один текст на семейство**, побайтово равный Ф3-02, на **семи**
   метках: Ф13-03 · 06 · 08 · 20 · 21 · 24 · 25 — перечень выводится предикатом §5, а не
   помнится; отказ края (Ф13-09, Ф13-28 — F4d-22) и отказы формы (Ф13-02, Ф13-07) в него не
   входят.
5. **Испытание входа: без имени, одно на контекст, привязано к контексту, срок — литерал Ф7,
   однократно, сгорает первым предъявлением при любом исходе** (Ф13-01, 03, 08); значение
   испытания не выводится ни из контекста, ни из времени (Ф13-01).
6. **Уровень — Ф11 Р2/Р3 по флагам ЭТОГО утверждения**; Ф13 не добавляет ни строки правила, ни
   слова словаря (гейты Ф11 — зелены до и после); один ключ даёт «2» и «3» (Ф13-10/11); копии
   читают запись (Ф13-13).
7. **Второй фактор при входе ключом не требуется и не принимается; внутри сессии — проходит без
   изменения уровня** (Ф13-15…17) — обе стороны оси наблюдаются.
8. **Две полосы входа независимы** (Ф13-18, 19): ни одна не читает способа другой.
9. **Снятие ключа гасит прочие сессии одной транзакцией с событием снятия; текущая остаётся**
   (Ф13-21); **наш глагол человека не запирает** (Ф13-22 — Ф7-26).
10. **Частота: только источник; `begin` — попытка; отказ формы — нет; успех обнуляет счёт по
    адресу** (Ф13-04, 07, 32); ручек посадки не добавлено (Ф13-14).
11. **Состав записи сессии Ф3 Р1 не расширен**: ни идентификатора ключа, ни флагов (Д13).
12. **Две записи в закрытых перечнях Ф3 — одним объявлением каждая** (Ф13-27, Ф13-28); второе
    написание пути или вида — находка гейта Ф3 §8 инв. 7.
13. **Журнал: событие выдачи с `["webauthn"]` и `id` ключа, без секрета и без идентификатора
    удостоверения; отказы событий не порождают; клетки нулевые печатаются** (Ф13-29, 30);
    **снятый ключ у проверяющего — «удостоверение неизвестно», отдельной клетки нет** (Р15).
14. **Путь при утрате единственного ключа решён (`kacho#2698`) и до посадки производителя не
    производится, и это названо словами** — Р12, Ф13-25: «условие не создано: производитель Ф5
    не посажен», ни зелёное, ни красное.
15. **Три исхода у каждой пробы**: зелёный · красный · «не выполнилось» (Ф7 не влита, стенд не
    поднят, дублёр не собран, производитель другой фазы не посажен) — третий не вычитается и не
    зачитывается (§8, §9).
16. **Ни одна фаза не разрешена вперёд** (§0.2): сценариев Ф7, Ф8, Ф10, Ф12 и составной
    приёмки регистрации ключом здесь нет.

---

## §8 Чем каждая группа краснеет ДО кода (строгий TDD, ban #12)

Здесь — **вид** доказательства, а не код. Красное до кода обязано быть **честным**
(`change-graph.md` §5): испытуемого нет либо объявленной возможности в нём нет — красный; не
влитая Ф7, не поднятый стенд, не собранный дублёр, не посаженный производитель другой фазы
(Ф5 по `#2698`) — «не выполнилось».

| группа | уровень | чем краснеет до кода |
|---|---|---|
| Ф13-01…04 испытание | сквозной **на собственном слушателе полосы службы**, без края и без браузера (дом пробы — репозиторий предмета, `e2e-flow.md` §7а; клиент предъявляет сертификат края — Ф3 Р16) + интеграционный на настоящей базе (одно испытание на контекст — две выдачи, одна строка; срок — часы пробы) | адреса нет — `404`: проба отвечает «не выполнилось», а не «прошло»; `allowCredentials` отсутствует либо непуст — Ф13-01 красный; второе испытание не замещает первое — Ф13-03 красный на первой половине; `begin` не считается попыткой — Ф13-04 красный на границе |
| Ф13-32 счёт по адресу | сквозной на слушателе службы: серия неверных паролей до границы, вход ключом между сериями; уровня I — чтение счёта по источнику до и после | вход не обнуляет счёт по адресу — первая же неверная после входа оказывается `N_адрес + 1`-й по адресу и даёт 429: красный (при `N_адрес ≥ 2`); близнец без входа даёт 429 на той же попытке — положительный контроль внутри сценария; обнуление задело источник — красный на чтении счёта; 429 по источнику на верной реализации исключён «Дано» (`N_источник ≥ 2·N_адрес`) |
| Ф13-05…09 вход | сквозной на слушателе службы с подставным аутентификатором Ф7 §8 (флаги, снятый бит, испытание по имени, рукоятка по имени); Ф13-06 — одиннадцать ответов побайтово против ответа Ф3-02 (форма Ф3-02) и **против ответов Ф7-07…09/49/52…55** на том же входе; (л) — измерительный (Ф1 §8, строка Ф1-48…51) на трёх личностях; Ф13-05 уровня I читает запись | глагола нет; на реализации со своей сверкой — Ф13-06 (в), (г), (д) зелёные при красных Ф7-10/12/49 либо наоборот: расхождение полос — красное; отказ со своим текстом — красный на побайтовом равенстве; испытание, живущее после отказа, — Ф13-08 (б) красный; сверка рукоятки снята — Ф13-06 (з) и Ф13-20 красные |
| Ф13-10…12 уровень | сквозной + уровня I (чтение записи): множество и уровень; проба на границе службы — ответ службы краю о сессии (Ф11 §3.0, «3»); ответ входа `assuranceLevel` | флаги не доезжают — «2» вместо «3»: Ф13-11 красный (при зелёных Ф7-28…30 — дефект вызывающего, не сверки); константа полосы «2» — Ф13-11 красный, Ф13-10/12 **ложно зелёные** — потому пара обязательна |
| Ф13-13 копии | use-case-проба на сессии с записанным «3» (посевом — Ф11 §3.0а: множество `{webauthn}`, уровень «3» — согласованно, флаги в записи не хранятся, и это единственное согласованное состояние для этого множества) | **сегодня красный по существу**: ответ смены пароля называет «2» (`kaname#208`); близнец «2» зелёный — то есть красное различает предмет |
| Ф13-14 страж | проба корня композиции (форма Ф3-44, `TestF11_28_OwnLaneWithPasswordAndSecondFactorPassesTheFloorRow` — на подставленном перечне) | корень отдаёт `[password]` — самоотчёт «1»: красный на наблюдении корня |
| Ф13-15…17 второй фактор | сквозной; фактор — записью строки `totp` `active` до Ф12; Ф13-17 — после её церемонии | вход требует код при заведённом факторе — Ф13-15 красный; поле принято — Ф13-16 красный; уровень после `step-up` изменился — Ф13-17 красный; до Ф12 Ф13-17 — «не выполнилось» (адреса церемонии нет) |
| Ф13-18…22 две полосы, снятие | сквозной; Ф13-21 — интеграционный на транзакцию с подставным отказом каждой записи (форма Ф3-16); Ф13-19 (б) — семь ответов Ф3-02 плюс восьмой побайтово | вход ключом читает строку пароля — Ф13-19 (а) красный у личности без пароля; снятие не гасит прочих — Ф13-21 красный (`S1` жива); гасит и текущую — красный на `S2` |
| Ф13-23, 24 перенос | интеграционный: строка ключа с рукояткой / без — записью до переноса; перепись — прогон предполётной переписи Ф7 на синтетическом источнике с ключом без рукоятки | рукоятка не сверяется — Ф13-24 (E) зелёный там, где обязан быть красным: отрицательный контроль; перепись без величины — Ф13-24 (P) красный |
| Ф13-25 утрата | сквозной по пяти шагам; шаг (2) — Ф5-03 на личности без строки | **«не выполнилось»** до посадки Ф5 по `kacho#2698`: шаг (2) — 503; проба печатает «условие не создано: производитель шага (2) — Ф5 (`#2698`) — не посажен» — третья категория, не красное и не зелёное; после посадки шаг (2) — 200 и строка заведена, дальше красный честный до Ф7 |
| Ф13-26, 27 формы | сквозной на слушателе службы | вида `access-key-login` нет — `GET csrf` 400: красный; признак прежнего контекста после выдачи ключом годен — Ф13-26 (в) красный |
| Ф13-28 край | пробы края с дублёром службы, считающим ретрансляции и читающим заголовки (Ф3-51 форма); объявление путей — перепись | ретрансляция семейства в стволе платформы (`#2689`); без двух записей Ф13 — красный честный (`404` на обоих путях); путь вне объявления — второе объявление: красный гейта Ф3 §8 инв. 7 |
| Ф13-29, 30 журнал | интеграционный на настоящей базе; `TestAuditPayloadCarriesNoPersonalData` на новом поле; клетки — чтение счётчиков до и после | поля `access_key_id` нет; клетки нет до первого события — красный |
| Ф13-31 дерево | гейт `internal/check` с инъекцией в обе стороны: копия сверки под другим именем — красное с координатой; обёртка, зовущая первую, — молчит; пустой обход — отказ | до Ф7 обход даёт **ноль** объявлений сверки — гейт обязан ответить «предмета нет», а не «находок нет» (перепись печатает объём) |

**Дом пробы — репозиторий её предмета** (`e2e-flow.md` §7а): сценарии службы гоняются на её
собственном слушателе; сценарии края (Ф13-09 в части отсечки, Ф13-28) — в `PRO-Robotech/kacho`
против дублёра службы; связка «консоль → край → служба» и церемония живым браузером — после Ф8
(её DoD: браузерная проба входа ключом). **Флаги, бит присутствия и рукоятку** подаёт
подставной аутентификатор Ф7 §8 **тем же кодом**, что настоящий; фикстура не снисходительнее
продукта (`e2e-flow.md` §5).

**Инъекция в обе стороны обязательна для каждого гейта** (`testing.md` §«Гейт на класс»):
внесённый дефект краснеет и называет координату; законный близнец молчит; перепись печатает
объём осмотренного; пустой обход — отказ. Гейт Ф13-31 заводится **этой фазой** и до посадки
Ф7 отвечает «предмета нет» — третья категория, названная им самим.

**Три исхода, а не два.** Прогон без условия (нет Ф7, нет стенда, нет дублёра, не посажен
производитель Ф5 по `#2698`) — «не выполнилось»: не вычитается из вердикта и не зачитывается в
успех.

---

## §9 Порядок работ после APPROVED и DoD

1. **Ф7 — первой** (§4.3): без сверки, строки ключа и церемонии «Дано» ни одного сценария §5,
   кроме Ф13-27 и Ф13-28, не построить, и полоса не поднимается (Р14). Кодирование Ф13 идёт
   **после посадки Ф7 в линию**, от неё; рукоятка (Р3) — колонка и её писатели — заводится
   **вместе с Ф7** её исполнителем по этой приёмке (Д2), чтобы церемония и перенос не
   переписывались вторым заходом; DoD Ф7 §9.1 п. 6 получает чтение пары «с рукояткой · без» и
   числа рядом с Ф7-24 (Ф13-24 P) — §3 Д28…Д30, **дописано** с ценой, а не подразумевается.
2. **Разбор классов** — `class-exposure-analyst` на одобренном замысле, до первой строки кода.
3. **Строгий TDD (ban #12):** все **32** сценария существуют красными прежде кода, и красный
   честный (§8). Порядок первых красных: Ф13-27 (вид формы — исполним без Ф7), Ф13-31 (гейт —
   «предмета нет» до Ф7, красный на копии после), Ф13-01…04 (испытание), затем Ф13-05…09,
   Ф13-10…12, остальные.
4. **Реализация — в порядке слоёв Ф3**: вид формы и пути (Ф-в, Ф-д) → испытание входа с
   контекстом и однократностью (своя таблица, уборка существующей петлёй — Ф3 Р14) → глагол
   входа: проверяющий Ф7 + рукоятка + выдача Ф-а с `KeyAssertion` → счёт по источнику (Ф-г) →
   снятие ключа: отзыв прочих сессий (дописка к глаголу Ф7) → событие и клетки → провязка в
   перечень стража (Ф-ж) → страница арендатора.
5. **Гейты дома зелены:** `go test -count=1 -short ./internal/check/`; гейты Ф11 (словарь,
   единственный писатель уровня) — до и после без правки; гейт единственности путей и видов
   Ф3 §8 инв. 7 — с двумя новыми записями; гейт Ф13-31 — с инъекцией.
6. **Ф13-13 исполняется вместе с `kaname#208`**: если та задача влита раньше — сценарий в DoD
   Ф13; если нет — DoD Ф13 называет его **условием**, вердикт «не выполнилось» по третьей
   категории, и фаза production-complete в своих границах (ban #14): копия уровня — код Ф3, не
   этой фазы.
7. **Ф13-25 — условие DoD, названное словами:** до посадки Ф5 по `kacho#2698` (редакция Ф5 и
   код в `recovery_complete.go`) проба печатает «условие не создано: производитель шага (2) не
   посажен» и в вердикт не входит. Что Ф13 обязана сделать сама, сделано без условия: путь
   назван решённым (Р12), человек нашим глаголом не запирается (Ф13-22), страница арендатора
   называет путь возврата — восстановление по подтверждённому адресу — и не обещает сброса
   распорядителем (`kacho#2702` — сирота).
8. **Край (дом платформы):** две записи — пути в перечне ретрансляции и в `isPublicHTTPPath` —
   тем же способом, что `kacho#2699` и `kacho#2701`; пробы Ф13-28 и Ф13-09 (отсечка) с дублёром
   службы; едет **своей задачей платформы**, заведённой при открытии кода Ф13 (номер — в
   закрывающем комментарии `#1282`), базой — ствол платформы (`#2689` влит, `275240efdc`).
9. **Сквозная проба на слушателе службы** — вход ключом без пароля, наблюдаемый §5: Ф13-05,
   Ф13-06 (все ветви), Ф13-10…12, Ф13-19 — в наборе `tests/newman/` дома службы под
   сертификатом края (Ф3 Р16).
10. **Ф8 после Ф13:** экран входа ключом читает форму Р1 без переложения (`publicKey` — в
    браузерный интерфейс как есть; ответ браузера — в форму `credential`, лишние поля сняты).
11. **Trail:** записка `obsidian/kacho/KAC/issue-1282.md` в воркспейсе; `rpc/` службы — два
    глагола формы; `resources/` — строка ключа с рукояткой (Ф7) и причина отзыва
    `access-key-revoked`; закрытие `#1282` с артефактами и с исходом по каждому из трёх
    условий (п. 6, 7, 8).

**DoD фазы — production-complete в своих границах (ban #14):** отложенное вынесено §4.2 с
носителем; условия названы п. 6…8 словами и в вердикт не вычитаются.

---

## §10 Границы текста этого документа

Документ лежит в публичном репозитории и написан так, чтобы выдерживать публикацию
(`security-disclosure.md`): он называет предмет, решения и следствия для контракта. Он **не**
несёт сборки «координата + условие, при котором защита не действовала + следствие для
постороннего»: ∅5 (§1.1) назван предметом и задачей без рецепта; отказы полосы описаны
тем, что они **едины**, а не тем, как их различить; порядок сверок внутри проверяющего
контрактом не является (Ф7 §11) и здесь не описан. Величины частоты, срок испытания и
перечень происхождений — величины профиля и контракта, а не этого текста.

Чего документ **не утверждает**: что полоса работает в живом браузере (это Ф8 и её проба);
что перенесённые ключи появятся в полосе (Р11 — не наблюдаемо нам); что путь при утрате
ключа исполним сегодня (Р12 — нет, и сказано числом); что копии уровня сегодня читают
запись (§1.1 ∅5 — нет).

---

## §11 Что осталось решением, а не установленным фактом — и находки по дороге

1. **Рукоятка ключей Ф7 — отдельное СЛУЧАЙНОЕ значение человека — РЕШЕНИЕ, а не замер** (Р3,
   редакция 4). Замер — то, что читатель один и здесь; что у человека с ключом переноса и ключом
   Ф7 рукоятки разные; и **четыре факта разбора поверхности** (Р3): рукоятка записывается в
   аутентификатор, уезжает в синхронизируемое хранилище держателя, возвращается в каждом
   утверждении и видна в интерфейсе операционной системы — то есть **отозвать её нечем**.
   **Отвергнутый исход — платформенный `id` человека**, и он стоял здесь решением редакций 1…3:
   его цена названа — пока рукоятка есть наш идентификатор, развязать учётную запись с ним
   невозможно, строки снимутся, а значение в чужом хранилище останется называть снятое.
   **Предсказание редакций 1…3 перемерено и сбылось:** здесь стояло «ревьюер вправе отвергнуть в
   пользу случайной рукоятки … колонка та же, писатель тот же, меняется лишь то, что кладёт
   церемония; сценарии не меняются» — так и вышло, ни один сценарий редакцией 4 не тронут по
   «Тогда». Отличие принятого решения от предсказанного одно и оно названо: значение **на
   человека**, а не на ключ, потому что норма требует одной рукоятки у всех удостоверений одного
   человека у одной доверяющей стороны. Ревьюер вправе отвергнуть **и это** — в пользу значения на
   ключ: тогда правятся Р3 (абзац «почему на человека»), §4.1 п. 4 и §5 «Дано»; сценарии опять не
   меняются, а цена — несколько учётных записей в интерфейсе аутентификатора вместо одной.
2. **`begin` как попытка по источнику — решение с ценой** (Р9): пара `begin` + отказ стоит двух
   попыток. Отвергнутые исходы названы там же.
3. **Снятие ключа гасит ВСЕ прочие сессии, а не сессии этого ключа, — решение по образцу Ф12 Р9**
   (Р8); цена — сессия телефона при снятии ключа ноутбука.
4. **Второй формы входа (по адресу) не будет — решение** (Р2, Р11); цена — необнаруживаемый
   перенесённый ключ. Условие пересмотра: число людей без пароля с таким ключом после переноса
   (Ф7-24, Ф13-24) окажется ненулевым — тогда решение владельца, а не расширение полосы молча.
5. **Развилка `kacho#2698` — РЕШЕНА** оркестратором 2026-09-17 (владелец делегировал решения
   2026-09-16): исход 1 — восстановление заводит способ входа паролем при отсутствии строки, тем
   же писателем ID-PW-1; довод Ф13 редакций 1…2 (Р12) совпал с принятым. Заведение первого пароля
   из живой сессии (`kaname#213`) решением **не открыто** — так сказано в нём самом. Что из
   заказанного написано на день редакции 3 — Р12 п. 3: ничего; производитель назван, не посажен.
6. **Снятый ключ = неизвестное удостоверение — решение по чтению Ф7, а не замер** (Р15). Замер —
   то, что Ф7 хранимого состояния «снят» не называет ни одной строкой и возвращает слот на
   удалении. Условие пересмотра: читатель различия (оповещение, отчёт) — тогда отмеченная строка
   приходит его приёмкой с ценой, названной в Р15.

**Находки по дороге — заведены задачами, здесь не чинятся:**

| находка | признак | задача |
|---|---|---|
| копия уровня в ответе смены пароля пересчитывается от имён способов с самыми слабыми флагами; для «3» назовёт «2» (§1.1 ∅5) | `git grep -n 'KeyAssertion(false, true)' -- internal/apps/kaname/api/humansession/change_password.go` → 1 | `kaname#208` |
| человек без пароля не может завести первый пароль из сессии: смена требует текущего, восстановление — строки; глагола заведения нет (§1.1 ∅8) | предикаты в теле задачи | `kaname#213` |
| сброс способа входа распорядителем (F4d-20, часть пароля и ключа) — производителя нет ни в одной фазе; два одобренных документа на него опираются (§1.1 ∅6) | предикат в теле задачи | `kacho#2702` |
| завершение восстановления у личности без строки пароля отвечает 503 и откатывается (§1.1 ∅7) — известно; исход решён | — | `kacho#2698` (OPEN, до этой приёмки; решено 2026-09-17 — исход 1, производитель Ф5 заказан) |
| перечень путей ретрансляции у края отстаёт от службы: 4 против 7 (§1.2 Ф-з) — известно | `git show origin/main:gateway/internal/middleware/login_lane_paths.go \| grep -c '"/iam/v1/auth/'` в доме платформы → 4 (`#2689` влит) | `kacho#2699`, `kacho#2701` (OPEN, до этой приёмки) |
