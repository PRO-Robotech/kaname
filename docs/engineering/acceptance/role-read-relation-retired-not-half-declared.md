<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Приёмка: отношение чтения роли СНИМАЕТСЯ, а не остаётся полуобъявленным

- **Статус:** DRAFT — круг 3, ждёт `acceptance-reviewer`. Кода по §5 не писать до
  вердикта (ban #1). Автор вердикта себе не ставит.
- **Ревизия измерения:** `61053691` — **база линии**, то есть ствол службы, от которого
  ветвится это изменение. Числа §0 сняты на дереве «эта база ПЛЮС дифф настоящего
  изменения»; перемерить их — применить дифф запроса слияния к названной ревизии.
  **Коммит полосы здесь НЕ называется, и это решение, а не удобство.** Работа едет в
  ствол СХЛОПЫВАНИЕМ, поэтому коммит ветки предком ствола не становится никогда: датировка
  им верна ровно до вливания и лжёт сразу после — числа оказываются привязаны к точке,
  которой в истории нет. Цена измерена: ствол службы был красен по гейту датировки **два
  вливания подряд** (`ba43277c`, `c56218b6`), и оба запроса слияния несли зелёный
  обязательный контекст — на ветке хеш полосы был предком рабочей вершины. База же
  остаётся предком ствола и после схлопывания, поэтому форма переживает посадку.
  Держит это `internal/check` `TestMeasurementRevisionsAreDatedByHashAndBelongToThisHistory`:
  он судит вхождение относительно СТВОЛА (`origin/main`), а не рабочей вершины, и потому
  даёт один вердикт до и после вливания.
  Прежняя редакция называла `a18659d1` — коммит полосы `lane/w2-domain`, непригодный по
  причине выше; круг 3 перевёл координату с `c56218b6` на `61053691` — базу СВОЕЙ линии,
  и той же правкой снял вторую ревизию, стоявшую прозой в §7а
- **Тип изменения:** снятие объявленной и неисполнимой возможности. **Сужает
  публичное поле `closed_verbs` у ВСЕХ ресурсов платформы** (§2.4) — наблюдаемо
  арендатору дважды: в превью роли и в словаре редактора ролей. Проводного
  контракта не ломает: ни одно поле не снимается и ни один код не меняется
- **Сервис:** `kaname`; затрагивает `manifest.yaml`,
  `proto/kaname/cloud/iam/v1/fga_model.fga`, `internal/authzmap` (порождаемое),
  строки `kaname.catalog_verb`, две ведомости и текст посева
- **Задача:** `PRO-Robotech/kacho#1922`, п. 1 (пп. 2–3 закрыты `#2047`, перемерены
  дважды, предикат пуст)

> **Круг 1 (CHANGES REQUESTED) — восемь блокирующих, все адресованы.** Круг был
> сильнее круга автора: рецензент **симулировал** DoD пп. 1–3 в отдельной копии и
> прогнал набор, а не прочитал документ. Из восьми находок **две** — дефекты
> измерений автора (§2 грепал таблицу, которой в дереве нет; §4 называл механизм
> ключа неверно дважды), **три** — неполнота цены и DoD (Б1, Б3, Б4), **одна** —
> ломающее следствие, названное в документе обратным (Б2), **одна** — производитель,
> дающий пропуск вместо вердикта (Б5), **одна** — свойство, приписанное пробе,
> которого её тело не держит (Б8). Решение §3 кругом **не затронуто** и остаётся
> выбранным исходом. Что изменилось построчно — §8.1.

---

> [!note] Правлено ПОСЛЕ вердикта — датировка чисел, а не сами числа
> Изменена **одна строка шапки**: ревизия измерения переведена с коммита полосы на
> базу линии. Причина названа в самой строке — коммит ветки предком ствола не
> становится никогда, поэтому датировка им лжёт сразу после схлопывания, и ствол
> службы был красен по гейту датировки два вливания подряд.
>
> **Ни одно утверждение приёмки этой правкой не затронуто:** сценарии, числа §0 и
> перечень производителей те же. Величины, привязанные к ревизии, пересняты на
> новой координате и сошлись с прежними дословно — то есть переезд датировки их
> не сдвинул. Вердикт рецензента относится к содержанию, и оно не менялось.

## 1. Предмет

На типе `iam_role` объявлено отношение `v_get`, и **читателя у него нет ни
одного**. Право, выданное по нему, не даёт ничего: ни один путь запроса его не
спрашивает, ни одна запись каталога прав его не называет, а единичное чтение
роли энфорсится **другим** предикатом — тем же, которым фильтруется страница.

Это третья поверхность класса «право выдаётся и не даёт ничего» (родня `#1825`,
`#1827`): там были строка права и класс правила, здесь — **отношение ТИПА**.
Поэтому закрыть его правкой правила нельзя: подстановочное правило
`iam.role.admin` (`classes: ["*"]`) разворачивается в набор ТИПА и продолжает
называть снятый класс by construction.

**Чем это видно арендатору — ДВУМЯ поверхностями, а не одной.** Превью роли
(`effective_verbs`) с `#1994`/`#1816` читает живые строки каталога, то есть набор
глаголов ТИПА; держатель `iam.role.admin` видит там `get`. И публичное поле
`closed_verbs`, из которого редактор ролей строит выпадающий список, сегодня
несёт `get`, потому что его объявляют все 27 глагольных типов (§2.4). Обе
поверхности обещают то, чего не исполняет ни один путь, — ровно та находка,
которую ban #18 называет отдельно: **возможность объявлена и неисполнима**.

## 2. Измерение — обе стороны, предикаты повторяемы

### 2.1. Каталог прав: `v_get` на роли не назван ни одной записью

```sh
python3 -c "import json;d=json.load(open('internal/apps/kaname/seed/embedded/permission_catalog.json'));\
r=[e for e in d if (e.get('scope_extractor') or {}).get('object_type')=='iam_role'];\
print(len(d),[(e['fqn'].split('.')[-1],e.get('required_relation')) for e in r],\
len([e for e in d if e.get('required_relation')=='v_get']))"
```

| величина | значение |
|---|---:|
| записей каталога | **350** |
| из них со `scope_extractor.object_type = iam_role` | **3** — `Delete` (`v_delete`) · `ListOperations` (`v_list`) · `Update` (`v_update`) |
| записей с `required_relation = v_get` во всём каталоге | **27** |
| из них на `iam_role` | **0** |

### 2.2. Путь чтения службы объявляет то же и объявляет ЯВНО

`internal/authzfilter/visibility.go`, таблица `pageRelations`: у шести иных типов
`iam` предикат страницы `{v_get}`, у `iam_role` — `{viewer, v_list}`.

```sh
git grep -n "v_get" -- '*.go' ':!*_test.go' ':!pkg/api/*' | grep -i iam_role
# → internal/authzmap/tables_gen.go:43 — и только он
```

Единственное попадание — **порождённая** таблица набора глаголов типа, то есть
ПИСАТЕЛЬСКАЯ сторона: из неё эмитируются кортежи, которых не читает никто.

### 2.3. Дерево УЖЕ объявило это отношение мёртвым — ведомостью

Сильнейшее измерение §1, и в круге 1 документ его не использовал.
`internal/authzmap/verb_relation_has_reader_test.go` держит перечень пар «модель
объявляет, никто не спрашивает», и запись про роль там **уже стоит**:

```go
{Type: "iam_role", Relation: "v_get"}: "Get энфорсится предикатом страницы {viewer, v_list}, не v_get",
```

Ведомость самоистекает: «перечень, переживший свой предмет» — её собственная
находка. То есть снятие отношения ОБЯЗАНО снять и запись, и это не уборка, а
условие зелёного (см. DoD п. 8 и GWT-9).

### 2.4. `closed_verbs` — ПЕРЕСЕЧЕНИЕ наборов всех типов, и это ломающая половина

`internal/apps/kaname/api/permission_catalog/resource_verbs_test.go` объявляет
предмет дословно: «сужение набора у ОДНОГО типа вынимало глагол из словаря у ВСЕХ
остальных (замер: глагольных типов каталога 27, `update` объявляли все 27)».

Значит снятие `get` у `iam.role` вынимает `get` из общего словаря **платформы**:
`closed_verbs` становится `[list]` вместо `[get, list]`. Проверено симуляцией в
круге 1 — `TestListPermissionCatalog_ClosedVerbsAreTheCommonSetInCanonicalOrder`
краснеет с `closedVerbs=[list], want [get list]`.

**Круг 1 документа утверждал обратное** («контракта на проводе не ломает»; «право
`iam.roles.get` этим изменением не трогается»). Утверждение снято: поле публично,
сужается, и у изменения появляется вторая наблюдаемая арендатором половина.

### 2.5. Кто называет ресурс `role` — и каким полем

`manifest.yaml`, 19 ролей; по полю `rules[].classes`:

| роль | классы |
|---|---|
| `iam.role.admin` | `["*"]` — подстановка, разворачивается в набор типа |
| `iam.role.edit` | `[list, update]` |
| `iam.role.view` | `[list]` |

**Ни одна роль не называет `get` поимённо В ПОЛЕ `classes`** — единица счёта
названа намеренно. Легаси-колонка `permissions` у `iam.role.view` несёт
`["iam.role.*.read","iam.role.*.list","iam.role.*.get"]`, то есть **уже**
разошлась со своими же `rules` и называет снимаемый глагол. На эмиссию она не
влияет (читателей в реконсайлере нет) и наружу не отдаётся
(`internal/dto/toproto/role.go:115`: «Permissions intentionally omitted»), но
это ещё одно объявление снимаемого глагола — его место в GWT-9, а не в молчании.

### 2.6. Референты снятия — предикат круга 1 был ВАКУУМНЫМ

```sh
grep -c "CREATE TABLE kaname.role_rule_ref_verb" internal/migrations/0001_initial.sql   # → 0
```

Таблицы `role_rule_ref_verb` **не существует**: это имя ключа
(`role_rule_ref_verb_fk`) и двух CHECK-ов. Круг 1 грепал её и получал ноль
**by construction** — «ноль находок» был неотличим от «ноль прочитанного».

Настоящая таблица — `kaname.role_rule_ref` (строка 2723), INSERT-ов в посеве
**92**; про `iam/role` из них **четыре**:

```sh
grep "INSERT INTO kaname.role_rule_ref " internal/migrations/0001_initial.sql | grep "'iam', 'role'"
# ('rol672f…', 'iam', 'role', 'list',   true);
# ('rol672f…', 'iam', 'role', 'update', true);
# ('role6859…', 'iam', 'role', NULL,    true);
# ('rolee27b…', 'iam', 'role', 'list',  true);
```

**Ни одна не называет `get`.** Вывод круга 1 верен, негоден был предикат.

## 3. РЕШЕНИЕ: снять отношение с типа. Второй исход отвергнут

Задача называет два исхода, и оба правят контракт чтения. Выбран **второй**.

### 3.1. Почему не «дать читателя»

Перевести единичное чтение роли на `v_get` значит сломать объявленное свойство:
страница и единичное чтение решают ОДНИМ предикатом —

```
{role : Get(role) успешен} == {role : role ∈ List}
```

**Держится оно ПОСТРОЕНИЕМ, а не пробой**, и это уточнение круга 2: обе
поверхности зовут один резолвер — `role/get.go:110` и `role/list.go:326`, обе
`resolveVisibleRoleIDs`, — поэтому разойтись не могут by construction.

Круг 1 называл держателем `TestVisibleSet_RoleKeepsTheUnionItsOwnGetEnforces`.
**Неверно:** её тело — один поддельный проверяльщик, один вызов `VisibleSet` и
одно утверждение; `Get` в ней не зовётся ни разу. Она держит ОДНУ сторону
(страница включает роль при гранте `v_list`), а не паритет. Заголовок шире тела
(`testing.md` §«Гейт на класс» п. 6), и опираться на неё как на держателя паритета
нельзя.

Довод против второго исхода от этого не ослаб, а укрепился: разводить два
предиката там, где сегодня один вызов, — значит заводить второе место об одном
вопросе, расходящееся молча. Цена для арендатора наблюдаема: роль, видимая в
перечне, перестала бы открываться поимённо, и «перечень врёт» было бы
единственным, что он увидит.

### 3.2. Почему «снять» — и что снятие НЕ отнимает

Снятие не отнимает **доступа**: у отношения нет читателя, значит по нему ничего
не предоставлялось никогда. Снимается не право, а **обещание права** — и вместе
с ним сужается словарь предлагаемых глаголов (§2.4), что и есть цена.

Проверка пятью вопросами (ban #18):

| вопрос | ответ |
|---|---|
| **благо продукта** | выигрыш не в объёме работы: снимается утверждение, которое продукт делает и не исполняет |
| **конкурентоспособность** | предсказуемость контракта: превью роли и словарь редактора перестают называть глагол, которого не бывает |
| **перспективность** | **обратимо**: снятие помечает строку (`RetireVerb`), оживление — объявление того же глагола в манифесте, и `UpsertVerb` поднимает **ту же** строку (`ON CONFLICT … DO UPDATE SET retired_at=NULL, live=true`). Внешне-адресуемая координата не трогается (ban #15), миграции данных арендатора нет |
| **безопасность** | поверхность **сужается**: перестают материализоваться кортежи, которых не читает никто. Ни один путь решения о доступе не меняется |
| **производственность** | режима «только для стенда» нет; отложенного нет; снятие идёт тем же путём, которым каталог принимает всякое изменение манифеста |

## 4. ЦЕНА СНЯТИЯ — где премиса задачи опровергнута, а где подтверждена ДРУГИМ доводом

Тело `#1922` называет снятие «правкой канонической модели, раздела `resources`
манифеста и посева `catalog_verb`, **плюс новая миграция по живым строкам**», а
комментарий линии добавляет: «применённую править нельзя, ban #5».

### 4.1. ОПРОВЕРГНУТО: отзыв в рантайме — данные, а не миграция

Каталог модуля живёт данными (эпик `#1027`), и у снятия глагола есть
производитель, исполняющийся **на подъёме**:

```sh
git grep -n 'RetireVerb' -- internal/apps/kaname/modulecatalog/*.go ':!*_test.go'
# apply.go:184  RetireVerb(ctx, v catalog.VerbRow, reason string) (bool, error)
# apply.go:549  retired, terr := w.RetireVerb(ctx, v, reason)
```

План собирает корзину снятого из разности «что в базе» против «что объявлено
манифестом» (`modulecatalog/plan.go:180-192`, ветвь `gone.Verbs`); применитель
зовётся композиционным корнем до подъёма слушателей
(`cmd/kaname/serve.go:359`, `applyDeliveredManifests`, отказ фатален), и порядок
держит `module_catalog_apply_wiring_test.go`. Запись — `UPDATE kaname.catalog_verb
SET retired_at = now(), live = false, retired_reason = $4 … AND live`
(`internal/repo/kaname/pg/catalog_writer.go:339`): строка **помечается**, а не
удаляется.

### 4.2. ИСПРАВЛЕНО: механизм ключа круг 1 описал неверно ДВАЖДЫ

```sql
ADD CONSTRAINT role_rule_ref_verb_fk
  FOREIGN KEY (module, resource, verb, live)
  REFERENCES kaname.catalog_verb(module, resource, verb, live) DEFERRABLE;
```
(`internal/migrations/0001_initial.sql:5530`)

- `ON DELETE RESTRICT` у этого ключа **нет вовсе** — круг 1 называл действие,
  которого не существует;
- `live` входит в ключ **с обеих сторон**, поэтому пометка снятой **уничтожает**
  референт `('iam','role','get', true)`, а не сохраняет его. Вывод круга 1
  («строка помечается снятой, ПОЭТОМУ ключ держится») неверен как вывод.

**Ключ держится по двум другим причинам, и обе про ДАННЫЕ:** ни одна системная
строка `role_rule_ref` этот глагол не называет (§2.6, четыре строки, `get` среди
них нет), а арендаторские снимает `ResettleTenantProjections` (ветвь
`stale_verb`) **до** снятия. Системные она не трогает намеренно:
`catalogStaleRuleRefPredicate` объясняет, что манифест, снимающий то, что его же
роль называет, противоречит сам себе, и это обязано быть отвергнуто ключом.

### 4.3. ПОДТВЕРЖДЕНО ДРУГИМ ДОВОДОМ: миграция ВСЁ РАВНО нужна — из-за паритета посева

Круг 1 заключил «миграция не нужна». **Неверно**, и причина не та, что в теле
задачи. `internal/check` `TestIAMCT114_CatalogSeedMatchesTheLiteral` читает
**ТЕКСТ** посева и требует точного равенства множеств с литералом, выведенным из
манифеста:

```sh
go test -count=1 -short -v ./internal/check/ -run CatalogSeedMatchesTheLiteral
# осмотрено: литерал — модулей 6, ресурсов 27, пообъектных глаголов 109;
# прочитано строк — модуля 6, ресурса 30, глагола 135; … снятых ресурсов 3
```

Гейт **даёт вердикт в этом репозитории** (не пропуск). Снятие глагола из
манифеста делает его красным: строка 3300 `0001_initial.sql` продолжает сеять
`('iam','role','get', NULL, NULL, true, true)`, а литерал этого глагола больше не
называет. Отзыв в рантайме текста посева не меняет — значит красное не пройдёт
само.

**Прецедент формы в дереве есть, и он текстовый:** три снятых *ресурса* несут
`live=false` + `superseded_by` прямо в посеве (строки 3262–3264), и перепись
гейта их считает («снятых ресурсов 3»).

**Осложнение, которое надо назвать:** гейт читает **один** файл
(`catalogMigrationPath = …/0001_initial.sql`), а миграций в каталоге **13**
(`ls internal/migrations/*.sql | wc -l`). Значит новая миграция сама по себе его
не успокоит — ему надо научиться читать цепочку. Исходов три, и выбор входит в
DoD (п. 5): новая миграция **плюс** расширение гейта на весь каталог · поимённое
послабление с причиной и предикатом · отказ от снятия.

Ban #5 при этом не задет ни в одном из исходов: применённая миграция не правится.

### 4.4. Что осталось верным из цены круга 1

Правка манифеста тянет за собой канон модели. **Держатель назван точнее:** в
круге 1 им объявлялся `internal/modelrender` — симуляция круга 1 показала, что
он на этом расхождении **зелён**. Держат его `internal/authzmap`:
`TestDrift_TypeVerbSetsMatchModelExactly` («тип объявляет набор …, а модель
определяет у него …») и `TestVerbAxis_EmitterMatchesModel` — обе краснеют на
неправленой модели и зеленеют после правки. Вшитую копию держит
`internal/authzmodel` `TestEmbeddedModelIsByteIdenticalToCanonical`; правится
**только** канонический файл, копия порождается `make -C deploy fga-model-embed`.

## 5. Приёмочные сценарии (Given-When-Then)

### GWT-1 — отношение не объявлено ни манифестом, ни моделью

**Дано** дерево после изменения.
**Когда** читают объявление ресурса `role` в `manifest.yaml` и блок
`type iam_role` канонической модели.
**Тогда** ни одно из двух не называет `get`/`v_get`.
**И** порождённая таблица набора глаголов типа называет для `iam_role` ровно
`{v_delete, v_list, v_update}`.

```sh
git grep -n "v_get" -- '*.go' ':!*_test.go' ':!pkg/api/*' | grep -i iam_role   # пусто
```

### GWT-2 — модель сошлась с манифестом (положительный контроль)

**Дано** то же дерево.
**Когда** прогоняют `go test ./internal/authzmap/ ./internal/authzmodel/`.
**Тогда** `TestDrift_TypeVerbSetsMatchModelExactly`,
`TestVerbAxis_EmitterMatchesModel` и `TestEmbeddedModelIsByteIdenticalToCanonical`
**зелены** — вердиктом, а не пропуском.

Близнец несущий: без него «`v_get` пропал» неотличимо от «порождение сломалось и
не производит ничего».

> **Производитель назван ИМЕННО ЭТОТ, и вот почему.** Круг 1 называл здесь
> перегенерацию и `TestGeneratedTablesAreFresh`. В этом репозитории они не дают
> вердикта: `//go:generate … -root ../../../..` отсчитывался от координаты
> монорепо и уводит на два уровня **выше** корня, `GeneratedRelPath` называет
> `services/iam/internal/authzmap/tables_gen.go`, а гейт свежести уходит в
> **SKIP** («УСЛОВИЕ НЕ СОЗДАНО: дерева платформы рядом с модулем нет»). Пропуск
> — третья категория, и в зелёное он не зачитывается. Предмет заведён своей
> задачей `PRO-Robotech/kaname#61` и этой приёмкой **не чинится**; DoD п. 3
> называет, что делать до её закрытия.

### GWT-3 — соседние глаголы типа не задеты

**Дано** то же дерево.
**Когда** держатель `iam.role.edit` обновляет роль, а держатель `iam.role.view`
читает перечень.
**Тогда** оба действия проходят как прежде.

Отрицание GWT-1 без этого зеленело бы на типе, у которого отняли всё.

### GWT-4 — превью роли перестало обещать глагол, которого нет

**Дано** роль `iam.role.admin` (правило `classes: ["*"]`).
**Когда** арендатор читает её превью.
**Тогда** среди эффективных глаголов **нет** `get`.
**И** остальные три глагола названы — превью не опустело.

Производитель: `domain.Role.EffectiveVerbs` ← `internal/dto/toproto/role.go:85`
← `TypeVerbs`, провязанный в `role/integrity.go:124` из
`cat.Facts().RolePreviewLookup()`; помощник общий для обоих чтений, непровязанный
набор даёт отказ, а не словарь сборки. Второго производителя в дереве нет.

### GWT-5 — словарь редактора ролей сузился, и это СКАЗАНО

**Дано** публичный каталог разрешений.
**Когда** клиент читает `closed_verbs`.
**Тогда** поле равно `[list]` — `get` из общего словаря вышел.
**И** ресурс, чей набор не менялся (например `iam.group`), предлагает прежнее:
сужение коснулось словаря-пересечения, а не каждого набора.

**И** сужение **записано** в ведомость осознанных сужений
(`narrowedBelowThePreviousOffering`) с причиной и номером задачи — иначе
`TestCatalogResourceVerbs_DescribeTheTypesOwnSets` краснеет текстом «сужение
обязано быть решением с причиной, а не побочным эффектом чужой правки».

Это вторая наблюдаемая арендатором половина изменения, и в круге 1 её не было.

### GWT-6 — паритет чтения роли не изменился

**Дано** держатель гранта по селектору роли (`v_list` без яруса).
**Когда** он читает роль поимённо и перечнем.
**Тогда** оба чтения отвечают одинаково: роль видна и там, и там.

Держатель — **построение** (`get.go:110` и `list.go:326` зовут один резолвер),
а не проба: `TestVisibleSet_RoleKeepsTheUnionItsOwnGetEnforces` `Get` не зовёт
вовсе и о паритете не утверждает (§3.1). Сценарий требует пробы, **зовущей обе
поверхности**, — либо прямого объявления, что паритет держится построением и
пробы-держателя у него нет.

### GWT-7 — строка каталога СНЯТА, а не удалена

**Дано** база, где строка `('iam','role','get')` жива.
**Когда** применитель каталога применяет манифест без этого глагола.
**Тогда** строка помечена снятой (`live = false`, названа причина), и число строк
таблицы не уменьшилось.
**И** ни одна применённая миграция не правлена.

### GWT-8 — снятие ОБРАТИМО

**Дано** база, где глагол снят.
**Когда** манифест снова объявляет `get` у ресурса `role`.
**Тогда** `UpsertVerb` оживляет **ту же** строку
(`ON CONFLICT (module,resource,verb) DO UPDATE SET retired_at=NULL, live=true`),
а не заводит вторую.

Без этого сценария «обратимо» из §3.2 — обещание, а не свойство.

### GWT-9 — посев и литерал сошлись

**Дано** дерево после изменения.
**Когда** прогоняют `go test ./internal/check/ -run CatalogSeedMatchesTheLiteral`.
**Тогда** зелено, и перепись печатает объём осмотренного.

Это тот сценарий, которого в круге 1 не было вовсе, и он — единственный, чей
исход требует решения из §4.3.

### GWT-10 — кортежи снятого глагола перестают материализоваться

**Дано** держатель подстановочной роли `iam.role.admin`.
**Когда** проекция глаголов роли пересчитывается.
**Тогда** кортежей `v_get` на объектах типа `iam_role` не появляется.
**И** решения о доступе ни по одному глаголу не изменились: ни один путь запроса
`v_get` на этом типе не спрашивал и до изменения.

### GWT-11 — ведомости, объявлявшие мёртвое отношение, снятием ИСТЕКЛИ

**Дано** дерево после изменения.
**Когда** прогоняют `go test ./internal/authzmap/ ./internal/apps/kaname/api/permission_catalog/`.
**Тогда** зелено, и это означает, что **обе** ведомости приведены в соответствие:

| ведомость | что с ней стало |
|---|---|
| `declaredWithoutReader` (`internal/authzmap/verb_relation_has_reader_test.go`) | запись `{iam_role, v_get}` **снята**: типу больше нечего прощать, и перечень, переживший предмет, — её собственная находка |
| `narrowedBelowThePreviousOffering` (`…/permission_catalog/resource_verbs_test.go`) | запись про `iam.role` **добавлена** с причиной и номером `#1922` |

**И** перепись мест, объявлявших отношение прозой, названа **числом**, а не
словами «любое место дерева». Измеренные кандидаты круга 2: `manifest.yaml` (два
абзаца про `viewer ∪ v_list`), `internal/authzfilter/visibility.go` (комментарий
у строки `iam_role`), `internal/apps/kaname/api/role/list.go`, легаси-колонка
`permissions` у `iam.role.view` (§2.5) и приёмки `docs/engineering/acceptance/`
(`git grep -rln "v_get" -- '*.md'` — 10 и более).

Класс назван `testing.md` §«Гейт на класс» п. 9: негативное утверждение о снятом
предмете **замолкает**, а не краснеет, и заметить это нечем, кроме вопроса «а
бывает ли ещё вход, на котором оно падает».

## 6. Вне области

- **пп. 2–3 задачи `#1922`** — закрыты `#2047`, предикат пуст, перемерен дважды;
- **координаты генератора таблиц типов** — `kaname#61`: перегенерация в этом
  клоне неисполнима, гейт свежести уходит в пропуск. Приёмка это **обходит**
  (GWT-2 называет других производителей), но не чинит;
- **запись каталога прав `RoleService/Get`** — стоит на фильтруемой полосе и этим
  изменением не трогается: она про **глагол края**, а не про отношение типа.
  Круг 1 писал здесь «право `iam.roles.get` не трогается» — формулировка снята
  как двусмысленная: общий словарь `closed_verbs` трогается (§2.4, GWT-5);
- **форма превью роли** (глаголы одним множеством, без разбивки по ресурсам) —
  своя задача; здесь превью только перестаёт называть снятый глагол;
- **легаси-колонка `permissions`** — расходится со своими `rules` независимо от
  этого изменения (§2.5); приводится в соответствие своим предметом, здесь она
  только попадает в перепись GWT-11;
- **прочие типы** `iam`: у шести из семи предикат страницы `{v_get}`, и он живой;
- **`v_get` на типах других модулей** — 27 записей каталога, все с читателем.

## 7. Готовность (DoD)

1. `manifest.yaml`: у ресурса `role` объявлены `[list, update, delete]`;
2. `proto/kaname/cloud/iam/v1/fga_model.fga`: блок `type iam_role` не объявляет
   `v_get`; вшитая копия сведена — `make -C deploy fga-model-embed`, и
   `TestEmbeddedModelIsByteIdenticalToCanonical` зелён;
3. `internal/authzmap/tables_gen.go` приведён в соответствие. **Способ назван в
   изменении**: до закрытия `kaname#61` перегенерация в этом клоне неисполнима,
   поэтому правка ручная и обоснована ссылкой на `#61`, а вердикт о соответствии
   дают `TestDrift_TypeVerbSetsMatchModelExactly` и `TestVerbAxis_EmitterMatchesModel`
   (GWT-2), а не пропускающийся гейт свежести;
4. `TestListPermissionCatalog_ClosedVerbsAreTheCommonSetInCanonicalOrder` зелён
   на новом значении `[list]` (GWT-5);
5. паритет посева зелён (GWT-9) — исход §4.3 выбран и назван в изменении: новая
   миграция плюс расширение гейта на весь каталог миграций · поимённое
   послабление с причиной и предикатом · отказ от снятия;
6. запись про `iam.role` добавлена в `narrowedBelowThePreviousOffering` с
   причиной и номером задачи (GWT-5, GWT-11);
7. запись `{iam_role, v_get}` снята из `declaredWithoutReader` (GWT-11);
8. интеграционная проба: применитель **помечает** строку снятой и оживляет её
   обратно (GWT-7, GWT-8), число строк не меняется;
9. проба превью: `iam.role.admin` не называет `get` и называет остальные три
   (GWT-4, с положительным контролем);
10. паритет чтения роли (GWT-6): либо заведена проба, зовущая **обе** поверхности,
    либо в изменении прямо сказано, что паритет держится построением и
    пробы-держателя у него нет. `TestVisibleSet_RoleKeepsTheUnionItsOwnGetEnforces`
    держателем паритета **не называть** — её тело этого не держит;
11. перепись мест, объявлявших отношение прозой, названа числом (GWT-11);
12. ни одна применённая миграция не правлена (ban #5);
13. `go test ./... -short -count=1` — код возврата 0.

## 7а. Что уже измерено (числа с предикатом и ревизией)

Всё в §2 и §4 снято на базе `61053691` — той же, что названа шапкой; каждый
предикат приведён рядом со своим числом. Числа, которые изменятся к моменту
реализации: **350** записей каталога (тело задачи называло 346 — росло дважды),
**19** ролей манифеста, **13** миграций, **92** строки `role_rule_ref`. Все —
ориентир, а не гейт: свойство держат нули (`iam_role` среди `v_get` — 0;
читателей на пути запроса — 0; строк `role_rule_ref` с `get` — 0), и они от
роста не зависят.

> [!note] Здесь стояла ВТОРАЯ ревизия, и расходилась она с шапкой молча
> Круг 2 перевёл датировку шапки с коммита полосы на базу линии и **этот абзац не
> тронул**: шапка называла `c56218b6`, §7а — `a18659d1`, коммит ветки
> `lane/w2-domain`. Предикат `git merge-base --is-ancestor a18659d1 origin/main`
> отвечает отказом — координаты в этой истории **нет**.
>
> **Гейт датировки этого не нашёл, и это свойство его распознавателя, а не
> случайность.** Он судит строку ОБЪЯВЛЕНИЯ (три формы, перепись печатает их
> раздельно: «Ревизия измерения 41 · Замер на ревизии 5 · Ревизия замера 2»), а
> ревизия в §7а стояла **прозой** и попала в строку «процитировано прозой 4» —
> то есть была прочитана и не адъюдицирована. Класс — `testing.md` §«Гейт на
> класс» п. 7: форма, о которой распознаватель не знает, даёт не красное и не
> зелёное, а молчание.
>
> Числа пересняты на `61053691` целиком: **350 · 19 · 92** сошлись дословно,
> **миграций 13, а не 11** — правлено здесь и в §4.3 (довод §4.3 от величины не
> зависит: гейт читает один файл при любом их числе).

## 7б. Открытое — числом

- **эффект на уже материализованные кортежи** `v_get@iam_role:*` у сегодняшних
  держателей `iam.role.admin`: их число на стенде с данными арендатора **не
  измерено** — свежая схема пуста by construction. Предикат назван в GWT-10 через
  пересчёт проекции; величина остаётся вопросом к стенду, а не к дереву;
- **исход §4.3** (миграция против послабления) в приёмке **не выбран
  намеренно**: выбор зависит от того, готов ли исполняющий расширить гейт
  паритета на цепочку миграций, и это работа с известной ценой, а не вкус. DoD
  п. 5 требует, чтобы исход был назван **в изменении**;
- **разбивка превью по ресурсам** — отсутствует; поэтому «ни одного глагола по
  снятому ресурсу» выразимо только для разворота подстановки. Граница та же, что
  назвала `#1814`, и здесь она не расширяется;
- **проверка круга 2 не покрывает** поднятого стенда: интеграционные пробы
  применителя (GWT-7, GWT-8) и поведение на данных арендатора не исполнялись.

## 8. Ревью

Вердикт ставит `acceptance-reviewer` — единственный gate APPROVED, и не заказчик.
Автор своего вердикта не ставит.

### 8.1. Что изменил круг 2 — по каждому блокирующему круга 1

| # круга 1 | предмет | что сделано |
|---|---|---|
| **Б1** | паритет посева краснеет, исхода нет | §4.3 заведён целиком; GWT-9; DoD п. 5 требует назвать исход из трёх |
| **Б2** | `closed_verbs` — публичное поле, сужается у всех | §«Тип изменения», §1, §2.4, GWT-5, §6 переписаны; утверждение «контракта не ломает» снято |
| **Б3** | ведомость сужений не названа | §2.4, GWT-5, GWT-11, DoD п. 6 |
| **Б4** | ведомость мёртвых отношений уже несёт запись | §2.3 (как измерение §1), GWT-11, DoD п. 7 |
| **Б5** | производитель GWT-2 даёт пропуск, не вердикт | GWT-2 переписан на `authzmap`/`authzmodel`; врезка о `kaname#61`; DoD п. 3; §6 |
| **Б6** | предикат грепал несуществующую таблицу | §2.6: предикат заменён, вывод перемерен настоящей таблицей |
| **Б7** | механизм ключа описан неверно дважды | §4.2: ключ процитирован, оба вывода исправлены |
| **Б8** | пробе приписано свойство, которого нет | §3.1 и GWT-6: держатель — построение; DoD п. 10 прямо запрещает называть ту пробу держателем |

Неблокирующие Н1–Н5 адресованы: Н1 — §4.4 (держатель назван точно), Н2 — DoD п. 2
(команда названа), Н3 — GWT-11 (перепись числом), Н4/Н5 — §2.5 (единица счёта и
легаси-колонка).

### 8.1а. Что изменил круг 3 — САМОПРОВЕРКА автора, не ответ на вердикт

Вердикта по кругу 2 вынесено не было; круг 3 — перемер собственных чисел на базе
своей линии. Найдено **две** находки, обе в датировке и обе у автора:

| находка | предикат | что сделано |
|---|---|---|
| §7а называла **вторую** ревизию, расходящуюся с шапкой, и её нет в этой истории | `git merge-base --is-ancestor a18659d1 origin/main` → отказ | ревизия переведена на `61053691`, врезка объясняет, почему гейт молчал |
| число миграций **11** при **13** в дереве (§4.3 и §7а) | `ls internal/migrations/*.sql \| wc -l` → 13 | правлено в обоих местах; довод §4.3 от величины не зависит |

Перемерено и **сошлось дословно**: записей каталога 350 · `v_get` всего 27, на
`iam_role` 0 · записей со `scope_extractor=iam_role` 3 · ролей манифеста 19 ·
строк `role_rule_ref` 92, с `get` 0 · читатель `v_get` на роли — один
(`tables_gen.go:43`, писательская сторона) · `declaredWithoutReader` несёт запись
`{iam_role, v_get}` · паритет посева зелён вердиктом (перепись: живых ресурсов
27, снятых 3) · `catalogMigrationPath` — один файл, и он резолвится в этом клоне
(`RequirePath` снимает приставку модуля), то есть §4.3 верен и в самостоятельной
посадке.

### 8.2. На что смотреть в круге 3

1. §4.3 — новый несущий раздел. Проверить прогоном, что гейт паритета читает
   один файл, и что исходов действительно три;
2. §2.4 и GWT-5 — ломающая половина. Проверить, что `closed_verbs` строится
   пересечением и что положительный близнец в сценарии различает «сузился
   словарь» от «сузился набор каждого типа»;
3. GWT-2 и врезка — что названные производители дают **вердикт**, а не пропуск,
   на этом дереве;
4. §3.1 и GWT-6 — что довод против второго исхода больше нигде не опирается на
   пробу, которая его не держит.
