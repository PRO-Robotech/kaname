// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: AGPL-3.0-or-later

package check_test

import (
	"os"
	"testing"

	"github.com/PRO-Robotech/corelib/treecorpus"
	"github.com/stretchr/testify/require"

	"github.com/PRO-Robotech/kaname/internal/check"
	"github.com/PRO-Robotech/kaname/internal/testsupport/platformtree"
)

// loginVerifierSpec — объявление предмета. Живёт ЗДЕСЬ, в файле пробы: литерал
// имени таблицы в ядре гейта сделал бы гейт своей же находкой (см. шапку
// `login_verifier_containment.go`).
//
// Разрешённый файл сегодня ОДИН — адаптер хранилища, а не его пакет: пакет —
// 70 не-тестовых файлов, и разрешение каталогу пропускало вызов выхода в любом.
// Проверяющий пароля (часть П2 фазы Ф2) получит разрешение СВОЕМУ файлу вместе
// со своим кодом и своей причиной — не раньше: раньше оно было бы разрешением
// без предмета, и гейт это объявляет находкой.
//
// Потребители — ровно два оператора базы адаптера: запрос, называющий таблицу,
// и материал уходят в них аргументами. Ключ — как его печатает находка; вызов с
// материалом либо именем таблицы в любой другой функции либо другой вызов той же
// функции — находка, пока не объявлен здесь с причиной.
func loginVerifierSpec() check.LoginVerifierSpec {
	return check.LoginVerifierSpec{
		Accessor:      "Reveal",
		DeclRel:       "internal/domain/login_method.go",
		DeclType:      "LoginVerifier",
		Table:         "user_login_methods",
		TableOwnerRel: "internal/repo/kaname/pg/login_method_repo.go",
		AllowedFiles: map[string]string{
			"internal/repo/kaname/pg/login_method_repo.go": "адаптер хранилища кладёт материал в базу аргументом оператора",
			"internal/passwordverify/verifier.go": "проверяющий пароля сверяет предъявленное с хранимым — " +
				"материал и есть его предмет (часть П2 фазы Ф2). Разрешён ФАЙЛ, а не пакет: разбор, " +
				"сверка с потолком и вычисление стоят в нём целиком, наружу уходят только исход, " +
				"признак формата и числа параметров",
			"internal/repo/kaname/pg/oauth_ceremony_repo.go": "адаптер собственной церемонии OAuth кладёт " +
				"проверочное значение секрета интерактивного клиента в базу аргументом оператора " +
				"(kaname#313). Значение чеканит ТОТ ЖЕ производитель, что пароли " +
				"(passwordverify.Hasher), и тип у него тот же; разрешён ФАЙЛ, а не пакет, и " +
				"обращение к выходу в нём ОДНО — вставка материала в колонку " +
				"interactive_clients.secret_verifier",
			"internal/totpverify/verifier.go": "проверяющий кода по времени снимает обёртку с хранимого секрета " +
				"и считает над ним HMAC (Ф12 Р2, Р5, kacho#1281) — материал и есть его предмет. Разрешён ФАЙЛ: " +
				"снятие обёртки, вычисление по ступеням окна и сравнение постоянным временем стоят в нём " +
				"целиком, наружу уходят только исход и ступень принятого кода (число)",
		},
		OpaqueConsumers: map[string]string{
			// Вставка строки способа — ОДИН оператор на два пути (Ф4 `kacho#1270`):
			// глагол `Create` исполняет его пулом, регистрация — транзакцией
			// вызывающего (строка способа входа ложится одним исходом с зеркалом и
			// сессией). Исполнитель приходит параметром (`q`), поэтому вызов
			// ключуется по имени функции-оператора, а не по полю адаптера.
			"insertLoginMethod → q.QueryRow":        "оператор вставки строки способа: материал и запрос с именем таблицы — его аргументы; исполнитель — пул либо транзакция вызывающего",
			"LoginMethodRepo.Get → r.pool.QueryRow": "оператор чтения строки способа: запрос с именем таблицы — его аргумент",
			// Проверочное значение секрета интерактивного клиента (kaname#313):
			// материал уходит аргументом ОДНОГО оператора, исполняемого в
			// транзакции названного уровня изоляции (kaname#316). Снятие значения
			// — отдельный глагол, и материала он не несёт.
			"OAuthCeremonyRepo.SetClientSecretVerifier → tx.Exec": "оператор записи проверочного значения " +
				"секрета интерактивного клиента в своей транзакции: материал — его аргумент",
			// Перепись классов стоимости (решение kaname#188 по Ф3-31): класс
			// вычисляет база, наружу уходит значение БЕЗ соли и тела — признак
			// формата и числа параметров; признак вне перечня — пустой префикс.
			// Материал в аргументах не участвует: запрос с именем таблицы —
			// единственный предмет вызова.
			"LoginMethodRepo.PasswordCostClasses → r.pool.Query": "оператор переписи классов стоимости: запрос с именем " +
				"таблицы — его аргумент; наружу уходят префиксы классов (формат и числа параметров) и число строк, " +
				"соль и тело остаются в базе",
			// Замещение материала одним оператором (ID-PW-1 PWV-10, фаза Ф3
			// `kacho#1269`): транзакцию приносит вызывающий — полоса входа и смена
			// пароля кладут материал одним исходом с прочими записями, — поэтому
			// оператор идёт по `tx`, а не по пулу. Материал и запрос с именем
			// таблицы — его аргументы; наружу уходит только число строк.
			"replaceLoginVerifierTx → tx.Exec": "оператор замещения строки способа в транзакции вызывающего: " +
				"материал и запрос с именем таблицы — его аргументы, наружу уходит число затронутых строк",

			// Второй фактор (Ф12, kacho#1281): операторы над той же таблицей, все в
			// файле-владельце, транзакцию приносит вызывающий. Материал уходит
			// оператору аргументом у заведения и у набора; прочие несут только
			// имя таблицы и числа. Наружу — число строк, строка способа в типе
			// либо ничего.
			"upsertPendingTOTPTx → tx.Exec": "оператор заведения одним `INSERT … ON CONFLICT DO UPDATE … WHERE state <> 'active'`: " +
				"обёрнутый секрет и запрос с именем таблицы — его аргументы, наружу уходит число строк",
			"activateTOTPTx → tx.Exec": "CAS подтверждения: запрос с именем таблицы, момент заведения и принятый шаг — " +
				"аргументы; материала среди них нет, наружу уходит число строк",
			"replaceLookupSetTx → tx.Exec": "оператор набора запасных кодов целиком (вставка либо замена): материал набора и " +
				"запрос с именем таблицы — его аргументы",
			"lockLookupSetTx → tx.QueryRow": "чтение строки набора `FOR UPDATE` — замок на время транзакции; запрос с именем " +
				"таблицы — аргумент, материал возвращается в типе через общий разбор ряда",
			"consumeLookupElementTx → tx.Exec": "снятие одного элемента набора по значению, вычисленному проверяющим из " +
				"предъявленного кода; запрос с именем таблицы — аргумент, наружу уходит число строк",
			"recordAcceptedStepTx → tx.Exec": "условная запись принятого шага: запрос с именем таблицы и число шага — аргументы",
			"removeSecondFactorTx → tx.QueryRow": "снятие строк `totp` и `lookup_secret` одним оператором: запрос с именем " +
				"таблицы — аргумент, наружу уходит число снятых",
			"LoginMethodRepo.SweepExpiredEnrollments → r.pool.Exec": "уборка истёкших заведений (`pending` старше окна): запрос " +
				"с именем таблицы, окно и партия — аргументы, наружу уходит число снятых",

			// Проверяющий пароля: вызовы, которым материал отдан ПО СУЩЕСТВУ.
			// Объявлен ровно тот, кому гейт видит передачу предмета: объявление
			// без предмета он называет находкой, и правильно — разрешение,
			// которому нечего разрешать, неотличимо от прощённой утечки.
			"inspectAndCompare → strings.HasPrefix": "признак формата читается началом самого значения: " +
				"выбор проверяющего производен от хранимого значения, а не от настройки",
			"compareBcrypt → strconv.ParseUint": "стоимость разбирается из разметки ДО вычисления — " +
				"сверить её с потолком нужно раньше, чем библиотека начнёт считать; наружу уходит число",
			"compareBcrypt → bcrypt.CompareHashAndPassword": "сверка предъявленного пароля с хранимым " +
				"значением — предмет проверяющего; результат библиотеки материала не несёт",
			"compareArgon2id → argon2.IDKey": "вычисление argon2id над ПРЕДЪЯВЛЕННЫМ значением — сама сверка. " +
				"Предъявленное приходит выходом своего типа: секрет клиента церемонии отдаёт себя тем же " +
				"именем выхода (`oauthceremony.PresentedSecret`, kaname#410), и принимает выход только " +
				"`VerifyPresented` этого файла; наружу из вычисления уходит тело для сравнения постоянным " +
				"временем, а не значение",
			"compareArgon2id → strings.HasPrefix": "версия разметки читается из самого значения: чужая " +
				"версия значит иную функцию, и вычисляться такое значение не обязано",
			"compareArgon2id → strings.Cut": "разметка PHC делится на параметры, соль и тело. Части " +
				"остаются внутри вызывающей функции и уходят только в вычисление: наружу из файла " +
				"возвращаются исход, признак формата и числа",

			// Набор запасных кодов (Ф12 Р6): разбор материала набора — та же
			// дисциплина, что у compareArgon2id; части остаются внутри функции и
			// уходят только в сравнение и вычисление, наружу — исход и числа.
			"inspectSet → strings.HasPrefix": "признак формата и версия разметки читаются началом самого значения " +
				"набора: выбор читателя производен от хранимого, а не от настройки",
			"inspectSet → strings.Cut": "разметка набора делится на параметры, соль и тело; части остаются " +
				"внутри функции — наружу уходят числа, соль в вычисление и элементы в сравнение",

			// Проверяющий кода по времени (Ф12): материал строки `totp` — base64
			// обёртки; декодированные байты уходят обёртке, открытый секрет — в
			// HMAC той же функции, наружу — исход и ступень.
			"Verifier.Verify → base64.RawStdEncoding.DecodeString": "материал строки totp — base64 обёртки " +
				"AES-256-GCM; декодирование — первый шаг сверки, результат уходит обёртке того же файла и " +
				"не покидает функции",
		},
	}
}

// TestLoginVerifierStaysInside — гейт: материал способа входа выходит из своего
// типа только в разрешённых местах, и таблицу секрета называет только её
// адаптер.
//
// Что делать, если он сработал, — исходов три, четвёртого нет:
//
//  1. материал нужен этому месту по существу (проверка пароля) → разрешение
//     ФАЙЛУ с причиной, в `loginVerifierSpec`, в том же изменении, что и код;
//  2. нужен не материал, а факт о нём («способ есть», «вид такой-то») → брать
//     факт из строки способа, а не материал;
//  3. вызов попал по инерции → снять.
//
// Приписать место в разрешения без причины — НЕ исход: разрешение без названной
// причины неотличимо от прощённой утечки.
func TestLoginVerifierStaysInside(t *testing.T) {
	t.Parallel()

	wd, err := os.Getwd()
	require.NoErrorf(t, err, "проверка НЕ ИСПОЛНЯЛАСЬ: рабочий каталог не установлен")
	root, err := platformtree.ModuleRootFrom(wd)
	require.NoErrorf(t, err, "проверка НЕ ИСПОЛНЯЛАСЬ: корень модуля не найден")
	tree, err := treecorpus.NewTree(root)
	require.NoErrorf(t, err, "состав дерева не установлен — «ноль находок» здесь означало бы «ноль прочитанного»")
	corpus, err := check.CorpusFrom(tree, check.ProductionGoFile)
	require.NoErrorf(t, err, "проверка НЕ ИСПОЛНЯЛАСЬ: корпус не собран")

	findings, census, err := check.AuditLoginVerifierContainment(corpus, loginVerifierSpec())
	require.NoErrorf(t, err, "проверка НЕ ИСПОЛНЯЛАСЬ")
	t.Logf("%s; находок %d", census, len(findings))

	for _, f := range findings {
		t.Error(f)
	}
}
