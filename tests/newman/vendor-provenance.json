{
 "_": "ПОРОЖДАЕМАЯ ЗАПИСЬ. Правится генератором, а не руками: см. шапку tests/newman/scripts/vendor_provenance_test.py.",
 "upstream": {
  "repo": "PRO-Robotech/kacho",
  "revision": "370c154d6d61d5b42f39df70f06aa04fc2702af2"
 },
 "files": [
  {
   "path": "tests/newman/kacholib/gen_shared.py",
   "upstream_blob": "ff2f40fdf77dc1b101bd356abb6db6d250a959f5",
   "upstream_sha256": "9ec74fcc620db87f5e05ee6c31e29c21a3ae43bd42d079f20465b4d72cba1fc2",
   "local_sha256": "031bb7db013ad64414330cc0e66369d74ac9b3f92b9e85e4de70ce79804a6b9a",
   "rewrites": [
    {
     "line": 195,
     "upstream": "    `services/iam/tests/newman/scripts/js_literal_escape_test.py` — одна на все",
     "local": "    `tests/newman/scripts/js_literal_escape_test.py` — одна на все",
     "why": "координата пробы: в этом репозитории набор лежит в КОРНЕ, а не под services/iam"
    },
    {
     "line": 269,
     "upstream": "    `services/iam/tests/newman/scripts/js_name_position_test.py` — одна на все",
     "local": "    `tests/newman/scripts/js_name_position_test.py` — одна на все",
     "why": "координата пробы: в этом репозитории набор лежит в КОРНЕ, а не под services/iam"
    },
    {
     "line": 1083,
     "upstream": "    `services/iam/tests/newman/scripts/js_regex_literal_test.py`: враждебный",
     "local": "    `tests/newman/scripts/js_regex_literal_test.py`: враждебный",
     "why": "координата пробы: в этом репозитории набор лежит в КОРНЕ, а не под services/iam"
    },
    {
     "line": 1133,
     "upstream": "    `services/iam/tests/newman/scripts/js_regex_literal_test.py` — одна на все",
     "local": "    `tests/newman/scripts/js_regex_literal_test.py` — одна на все",
     "why": "координата пробы: в этом репозитории набор лежит в КОРНЕ, а не под services/iam"
    },
    {
     "line": 1348,
     "upstream": "# `services/vpc/tests/newman/scripts/selftest_autowrap.py` (её зовут `ci.yaml` и\n# `e2e-newman.yml`). ЭТА копия ею НЕ ПОКРЫТА: своей самопроверки у набора нет.\n# Правя предикат здесь, проверяй его сам — обещания, что правка проверена в обе\n# стороны, тут нет.",
     "local": "# самопробой набора vpc В ДЕРЕВЕ ПЛАТФОРМЫ (её зовут процессы её конвейера).\n# В ЭТОМ дереве той самопробы нет ни одним файлом, и этой копией она не\n# покрыта. Правя предикат здесь, проверяй его сам — обещания, что правка\n# проверена в обе стороны, тут нет.",
     "why": "координата не резолвится в этом репозитории вовсе: набора vpc здесь нет"
    },
    {
     "line": 1774,
     "upstream": "    # ПРИНИМАЕТСЯ ОДНО ИМЯ. Приём перечня имён (списка либо кортежа) снят вместе\n    # со своим последним вызывающим: единственный кейс, ждавший появления НЕСКОЛЬКИХ\n    # строк, жил в наборе службы доступа, а набор уехал в её репозиторий.\n    #\n    # Возможность без вызывающего — обещание, за которое никто не отвечает: гейт\n    # `TestNewmanHelperShapeCapabilityHasACaller` называет такую форму находкой, и\n    # он прав — форма, которую никто не зовёт, не исполняется ни одним прогоном и\n    # потому не проверена ничем.\n    #\n    # ПОЧИНКА, РАДИ КОТОРОЙ ФОРМА ЗАВОДИЛАСЬ, НЕ УТРАЧЕНА, и это важнее самой\n    # формы: тело по-прежнему ждёт появления ВСЕХ названных имён (`_want.every`),\n    # а не первого из них. Разбор того дефекта остался в шапке выше. Понадобится\n    # перечень снова — он возвращается ОДНОЙ строкой и ВМЕСТЕ со своим кейсом, а\n    # не вперёд него.\n    want = [id_env_var]",
     "local": "    # ПРИНИМАЕТСЯ ИМЯ ЛИБО ПЕРЕЧЕНЬ ИМЁН, И У ПЕРЕЧНЯ ЗДЕСЬ ЕСТЬ ВЫЗЫВАЮЩИЙ.\n    #\n    # В дереве платформы форма перечня снята — и снята верно ДЛЯ ТОГО дерева:\n    # единственный кейс, ждавший появления НЕСКОЛЬКИХ строк, жил в наборе службы\n    # доступа, а набор уехал в её репозиторий, то есть СЮДА. Гейт формы мерил круг\n    # вызывающих по своему дереву и второго репозитория не видит by construction;\n    # вызывающий жив и назван: `cases/iam-access-binding-account-scope.py`.\n    #\n    # ЦЕНА СНЯТИЯ ИЗМЕРЕНА, А НЕ ПРЕДПОЛОЖЕНА. Без перечня генератор подставляет\n    # в скрипт ИМЯ ПЕРЕЧНЯ вместо имён его элементов: `_want` становится\n    # `[undefined]`, `_want.every` не сходится НИ ПРИ КАКОМ ответе, ожидание\n    # выжигает весь бюджет на каждом прогоне и после fail-open уступает ровно той\n    # гонке, ради снятия которой обёртка писалась. Утверждение при этом не\n    # теряется — теряется ожидание, и потеря тихая.\n    #\n    # Вышестоящее дерево само оговаривает условие возврата: перечень возвращается\n    # ОДНОЙ строкой и ВМЕСТЕ со своим кейсом. Это она и есть. Тело ниже ждёт\n    # появления ВСЕХ названных имён (`_want.every`), а не первого из них.\n    want = list(id_env_var) if isinstance(id_env_var, (list, tuple)) else [id_env_var]",
     "why": "ФУНКЦИОНАЛЬНАЯ: перечень имён снова принимается — его вызывающий живёт в этом репозитории"
    }
   ]
  },
  {
   "path": "tests/newman/kacholib/casesindex.py",
   "upstream_blob": "517a2c99570464d0389ac966df10b0af0bab81c2",
   "upstream_sha256": "5fa927c18ae409036a0046bcc7abd07d45b5c5252a303abee2338ed7a02696d5",
   "local_sha256": "8dc99825d0caa0ba18f3cc74568ae07ba971f88d22804ce8f3c3f716f1dae018",
   "rewrites": [
    {
     "line": 14,
     "upstream": "      md5sum < services/$s/tests/newman/scripts/validate-cases.py; done | sort -u | wc -l",
     "local": "      md5sum < services/$s/tests/newman/scripts/validate-cases.py; done | sort -u | wc -l\n\nЗамер сделан В ДЕРЕВЕ ПЛАТФОРМЫ `PRO-Robotech/kacho`; в этом репозитории команда\nне выполнится вовсе — каталога `services/` здесь нет.",
     "why": "команда не выполнится в этом дереве — сказано, где она выполняется"
    }
   ]
  },
  {
   "path": "tests/newman/kacholib/stems.sh",
   "upstream_blob": "198ce74c16ac506cbe1c499ac25383235ab31f33",
   "upstream_sha256": "3612653835ee472c8e30448c9b02da43c2425517201d7a96c0e721ab42b85bd0",
   "local_sha256": "3612653835ee472c8e30448c9b02da43c2425517201d7a96c0e721ab42b85bd0",
   "rewrites": []
  },
  {
   "path": "tests/newman/scripts/assert-suites-green.sh",
   "upstream_blob": "b676e32b34c40d22966fc20daf023920beacaad1",
   "upstream_sha256": "063bf9748227d5c07a5c3e4ffe589495943760f4b184402a0bbd83047daa15c0",
   "local_sha256": "063bf9748227d5c07a5c3e4ffe589495943760f4b184402a0bbd83047daa15c0",
   "rewrites": []
  },
  {
   "path": "tests/newman/scripts/coverage.py",
   "upstream_blob": "eceee18fcef281870c14a0840ab70fc91f4b767d",
   "upstream_sha256": "7ca2f006022da4f2799de01ea811387be801ac88e42ca7043fff0df914bce7e9",
   "local_sha256": "2b0d600449c6892d4d10943cf06be13a291a4612a03cb6f8495747bb1d323792",
   "rewrites": [
    {
     "line": 9,
     "upstream": "Parses kacho-proto IAM .proto files for `service X { rpc Y(...) ... }` blocks",
     "local": "Parses the IAM .proto files for `service X { rpc Y(...) ... }` blocks",
     "why": "`kacho-proto` — имя ПРЕЖНЕГО репозитория; контракты приезжают модулем платформы"
    },
    {
     "line": 285,
     "upstream": "                   help=\"glob for .proto files (e.g. '../kacho-proto/proto/kaname/cloud/iam/v1/*.proto')\")",
     "local": "                   help=\"glob for .proto files (run.sh derives it from the pinned platform module)\")",
     "why": "пример адресовал раскладку полирепо, которой нет ни в одном из двух деревьев"
    }
   ]
  },
  {
   "path": "tests/newman/scripts/coverage_test.py",
   "upstream_blob": "b698d820df0b9344a4670cb629cb9312b3f216c3",
   "upstream_sha256": "4b44ca598efc6fd662d1b688f7f7a6a8f5860d595d5aa6b41354ec6d13ee1919",
   "local_sha256": "5604ec83d71e24aa4f38f09ac0ca7d08e241e1fd5d85a5aceb47e89fc46536e6",
   "rewrites": [
    {
     "line": 7,
     "upstream": "собирает ОБХОДОМ дерева по образцу `services/*/tests/newman/scripts/*_test.py` и",
     "local": "собирает ОБХОДОМ дерева по образцу `tests/newman/scripts/*_test.py` (в дереве\nплатформы к нему добавлен `services/*/tests/newman/scripts/*_test.py`) и",
     "why": "образец обхода: в этом репозитории набор лежит в корне"
    }
   ]
  },
  {
   "path": "tests/newman/scripts/exec-coverage.py",
   "upstream_blob": "766003b2130c94bcadce4e64caecde40ae99c09d",
   "upstream_sha256": "286f511da88be7b09604d2791a91b43d3d333145688c20b87184009bae4e0c80",
   "local_sha256": "26ec4d77b7db394cc5bccf55ea1a908bf882db3ab09fd85f0e649ee76d8d365d",
   "rewrites": [
    {
     "line": 174,
     "upstream": "gate does NOT produce and git does not track (`services/iam/tests/newman/.gitignore`",
     "local": "gate does NOT produce and git does not track (`tests/newman/.gitignore`",
     "why": "координата: файл лежит в корне набора этого репозитория"
    }
   ]
  },
  {
   "path": "tests/newman/scripts/exec-coverage-selftest.py",
   "upstream_blob": "08c1b694169222db21bbbaa2509e678e4741300e",
   "upstream_sha256": "2e5f4002ffaacce623c301a18acc5c17da6e1a223991b65241040a71adb2d22d",
   "local_sha256": "2e5f4002ffaacce623c301a18acc5c17da6e1a223991b65241040a71adb2d22d",
   "rewrites": []
  },
  {
   "path": "tests/newman/scripts/exec_coverage_test.py",
   "upstream_blob": "9400e3e23331aec27d07ced8fce86fe007f1f3ed",
   "upstream_sha256": "34fa9a8a10f533003dbc405ce571381551e6e942189c7e9e66c7965408103c0a",
   "local_sha256": "8fcd679bbc5464e5d04db4ed276a51cf2ef81b64d49b39aab766d6dfb0813bfc",
   "rewrites": [
    {
     "line": 18,
     "upstream": "собирает ОБХОДОМ дерева по образцу `services/*/tests/newman/scripts/*_test.py` и",
     "local": "собирает ОБХОДОМ дерева по образцу `tests/newman/scripts/*_test.py` (в дереве\nплатформы к нему добавлен `services/*/tests/newman/scripts/*_test.py`) и",
     "why": "образец обхода: в этом репозитории набор лежит в корне"
    }
   ]
  },
  {
   "path": "tests/newman/scripts/precondition_mark_test.py",
   "upstream_blob": "4d22a49f01505cc97bf7b142beef3185f3b922b8",
   "upstream_sha256": "8c29c309467c62827b22f3fa2dc3d68b2d16295bd6a9bcb7ebacd844de446f76",
   "local_sha256": "56e2f53f9cb575f41580407b3916cd69de18daa9551354b4c7ddad148b74b7f0",
   "rewrites": [
    {
     "line": 83,
     "upstream": "    roots = sorted(REPO.glob(\"services/*/tests/newman\"))",
     "local": "    # КОРНЕВОЙ НАБОР — форма ОТДЕЛЬНОГО репозитория службы: набор лежит прямо в\n    # `tests/newman`, а не под `services/<имя>/`. Строка верна и в дереве\n    # платформы: там `tests/newman/scripts/gen.py` не существует, и фильтр ниже\n    # отбрасывает корень сам — то есть это сужение ни от чего не освобождает.\n    roots = sorted(REPO.glob(\"tests/newman\"))\n    roots += sorted(REPO.glob(\"services/*/tests/newman\"))",
     "why": "ФУНКЦИОНАЛЬНАЯ: обход не находил в этом дереве НИ ОДНОГО набора"
    }
   ]
  },
  {
   "path": "tests/newman/scripts/precondition_mark_injection_test.py",
   "upstream_blob": "2230709c10cd9a49b416bcfdda7c2fa54585a3d9",
   "upstream_sha256": "4f10a5064e44c4eff59873f90aaef7f62d6ab01f429c706ffdae9804917ee57b",
   "local_sha256": "4f10a5064e44c4eff59873f90aaef7f62d6ab01f429c706ffdae9804917ee57b",
   "rewrites": []
  },
  {
   "path": "tests/newman/scripts/precondition_outcome_test.py",
   "upstream_blob": "8a9db47c0eeaf6faf3172d68f23e7a5ffaa40cae",
   "upstream_sha256": "fe30f4646181758980c301fa7239361c7859d83fba0b6952287412009a549f5d",
   "local_sha256": "ee9862256ea086b8a455fb36c5bfbd68792893b51d280b68330e330b08f62312",
   "rewrites": [
    {
     "line": 57,
     "upstream": "    found = sorted(REPO.glob(\"services/*/tests/newman/scripts/gen.py\"))",
     "local": "    # КОРНЕВОЙ НАБОР — форма ОТДЕЛЬНОГО репозитория службы. Строка верна и в\n    # дереве платформы: там такого файла нет, и `is_file()` ниже его отбросит.\n    found = sorted(REPO.glob(\"tests/newman/scripts/gen.py\"))\n    found += sorted(REPO.glob(\"services/*/tests/newman/scripts/gen.py\"))",
     "why": "ФУНКЦИОНАЛЬНАЯ: обход не находил в этом дереве НИ ОДНОГО производителя метки"
    }
   ]
  },
  {
   "path": ".github/scripts/run-python-probes.py",
   "upstream_blob": "cf9da99bfebc8ee409bfb9c998e33f8a6f465287",
   "upstream_sha256": "f80e2d9c7fe2f3be5fca2a31b399a9d2cd1d6f829e1081f2feba593bf44a42ad",
   "local_sha256": "cc12143a1d001661f8acd713c44a5550f787912add9c5d53a3201899fe6b4239",
   "rewrites": [
    {
     "line": 11,
     "upstream": "  * `tests/newman/scripts/*_test.py` — пробы ВЕРДИКТНОГО СЛОЯ, общего на дерево:\n    гейта суиты (`assert-suites-green.sh`), гейта ИСПОЛНЕННОСТИ прогона\n    (`exec-coverage.py`) и гейта покрытия (`coverage.py`). Слой судит все восемь\n    наборов и потому не принадлежит ни одному;",
     "local": "  * `tests/newman/scripts/*_test.py` — пробы набора СЛУЖБЫ и вендоренного сюда\n    вердиктного слоя (`assert-suites-green.sh`, `exec-coverage.py`, `coverage.py`).\n    В дереве платформы этот слой судит семь её наборов и потому не принадлежит ни\n    одному; здесь он ВЕНДОРЕН (`tests/newman/vendor-provenance.json`), потому что\n    подъём за ним из отдельного репозитория корня монорепо не достаёт;",
     "why": "слой не «один на восемь наборов»: в этом репозитории набор ОДИН, а слой вендорен"
    },
    {
     "line": 16,
     "upstream": "  * `tests/authz-fixtures/*_test.py` — пробы ПОСЕВА, который поднимает фикстуры\n    боевой посадки для всех наборов сразу. Слой третий, а не тот же самый: посев\n    исполняется ДО первой коллекции, и его отказ не даёт ни одного отчёта — то\n    есть вердиктный слой ему не судья, он лишь сообщает, что судить нечего.",
     "local": "  * `tests/authz-fixtures/*_test.py` — пробы ПОСЕВА, который поднимает фикстуры\n    боевой посадки. Слой третий, а не тот же самый: посев исполняется ДО первой\n    коллекции, и его отказ не даёт ни одного отчёта — то есть вердиктный слой ему\n    не судья, он лишь сообщает, что судить нечего.\n\n    ПОСЕВ В ЭТОМ РЕПОЗИТОРИИ ЕСТЬ, а проб ПО ЭТОМУ ОБРАЗЦУ у него ноль — и это\n    разные утверждения. В дереве `tests/authz-fixtures/` два файла\n    (`ls tests/authz-fixtures/*.py | wc -l` → 2): `seed_own_stand.py` — машинный\n    посев автономного стенда, и `principal_pairings.py`. Файлов `*_test.py` среди\n    них ноль (`ls tests/authz-fixtures/*_test.py 2>/dev/null | wc -l` → 0), потому\n    что посев доказывает себя ИНЪЕКЦИЕЙ в форме `--self-test`, которую зовёт\n    отдельный шаг конвейера ПЕРЕД тем, как поверить его вердикту, — та же форма,\n    что у гейтов `.github/scripts/`, и она уместна там, где предмет один.\n\n    Образец оставлен НАМЕРЕННО и виден в переписи на КАЖДОМ прогоне («по образцу\n    tests/authz-fixtures/*_test.py: 0»), то есть ноль называется числом, а не\n    молчанием: придёт проба посева в форме НАБОРА — образец найдёт её сам.\n\n    ПРЕЖНЯЯ РЕДАКЦИЯ УТВЕРЖДАЛА ОБРАТНОЕ И ПЕРЕЖИЛА СВОЙ ПРЕДМЕТ: она отрицала\n    наличие посева в дереве целиком и называла предикатом своего снятия историю\n    каталога (`git log --oneline --all -- tests/authz-fixtures | wc -l` → 0).\n    Предикат даёт 3, и первая единица приехала ещё до посева. Дословно отрицание\n    здесь НЕ ПЕРЕСКАЗАНО намеренно: предикат держателя читает прозу этого файла, и\n    цитата собственного снятого утверждения краснела бы как утверждение — тот же\n    класс, что проверка, считающая своё объяснение.\n    Держит факт проба `tests/newman/scripts/pipeline_claims_test.py`: она краснеет\n    на утверждении об отсутствии посева, когда посев в дереве есть, — и молчит,\n    когда его нет, потому что тогда утверждение ВЕРНО.",
     "why": "посев В ЭТОМ РЕПОЗИТОРИИ ЕСТЬ (tests/authz-fixtures/seed_own_stand.py), а проб по образцу *_test.py у него ноль: он доказывает себя инъекцией в форме --self-test. Прежняя редакция подстановки отрицала наличие посева и пережила свой предмет"
    },
    {
     "line": 99,
     "upstream": "# ПОЧЕМУ НЕ ОДИН ОБРАЗЕЦ СО ЗВЁЗДОЧКОЙ ВПЕРЕДИ. `*` в pathspec git пересекает\n# `/`, в `fnmatch` — пересекает, а в `filepath.Match` (гейт проводки\n# `tools/pythonprobes`) — НЕ пересекает. Один образец `*/tests/newman/…` читался\n# бы прогонщиком и его гейтом ПО-РАЗНОМУ, и разошлись бы они молча: гейт\n# объявил бы пробу невидимой там, где прогонщик её видит. Перечень явных\n# образцов, ни один из которых не пересекает `/`, читается всеми тремя\n# одинаково.",
     "local": "# ПОЧЕМУ НЕ ОДИН ОБРАЗЕЦ СО ЗВЁЗДОЧКОЙ ВПЕРЕДИ. `*` в pathspec git пересекает\n# `/`, в `fnmatch` — пересекает, а в `filepath.Match` (на нём написан гейт\n# проводки в дереве платформы) — НЕ пересекает. Один образец `*/tests/newman/…`\n# читался бы прогонщиком и его гейтом ПО-РАЗНОМУ, и разошлись бы они молча: гейт\n# объявил бы пробу невидимой там, где прогонщик её видит. Перечень явных\n# образцов, ни один из которых не пересекает `/`, читается всеми тремя\n# одинаково.",
     "why": "гейт проводки живёт в дереве платформы: координата тут не резолвится"
    },
    {
     "line": 107,
     "upstream": "# ОБРАЗЕЦ, НЕ НАХОДЯЩИЙ НИЧЕГО, — НАХОДКА, а не мелочь: расширение обхода, не\n# изменившее переписи, выглядит покрытием и им не является. Гейт проводки\n# (`tools/pythonprobes`) требует от каждого образца хотя бы одну пробу дерева.\n# Сегодня первый образец несут пробы ровно ОДНОГО набора; если этот набор\n# когда-нибудь уедет из дерева, образец останется без предмета и гейт\n# потребует решения — снять его либо назвать набор, ради которого он держится.\n# Это не поломка, а вопрос, который иначе был бы решён молчанием.",
     "local": "# ОБРАЗЕЦ, НЕ НАХОДЯЩИЙ НИЧЕГО, — НАХОДКА, а не мелочь: расширение обхода, не\n# изменившее переписи, выглядит покрытием и им не является. В дереве платформы\n# это требование держит гейт проводки; ЗДЕСЬ его нет, и держит только перепись по\n# каждому образцу отдельно — она печатается всегда, поэтому ноль у образца посева\n# виден на каждом прогоне и назван числом.",
     "why": "гейта проводки в этом репозитории нет: держит перепись, и это сказано прямо"
    },
    {
     "line": 119,
     "upstream": "# уровня — вторая половина гейта (`tools/pythonprobes`, «всякий отслеживаемый\n# файл проб покрыт образцом») покраснеет с его именем, потому что покрыть его\n# станет нечем.",
     "local": "# уровня — второй половине гейта проводки в дереве платформы («всякий\n# отслеживаемый файл проб покрыт образцом») покрыть его станет нечем. В ЭТОМ\n# репозитории набор свои пробы этого уровня НЕСЁТ (26 файлов на момент\n# вендоринга), и находит их ПЕРВЫЙ образец: `tests/newman/scripts/*_test.py` —\n# набор здесь лежит в корне, а не под `services/<имя>/`.",
     "why": "в этом репозитории набор лежит в корне, и первый образец находит его пробы"
    },
    {
     "line": 269,
     "upstream": "    # Ноль файлов — ОТКАЗ. Пустой состав отчитался бы «всё чисто», и именно так\n    # 48 проб прожили в дереве, не исполнившись ни разу.",
     "local": "    # Ноль файлов — ОТКАЗ. Пустой состав отчитался бы «всё чисто», и именно так\n    # пробы этого набора прожили в дереве, не исполнившись ни разу: в репозитории\n    # службы конвейер их не звал НИ ОДНИМ шагом (предикат на момент вендоринга:\n    # `grep -rc run-python-probes .github/workflows/` → 0).",
     "why": "число 48 — замер дерева платформы; здесь оно другое и не перемеряется прозой"
    },
    {
     "line": 305,
     "upstream": "            print(\"ОТКАЗ: нет pytest, а 48 проб написаны под него — прогон НЕ \"\n                  \"ВЫПОЛНЕН. Установи его в шаге (`python3 -m pip install pytest`).\",\n                  file=sys.stderr)",
     "local": "            print(f\"ОТКАЗ: нет pytest, а под него написано файлов проб: \"\n                  f\"{len(pytest_files)} (объявлено проб {declared}) — прогон НЕ \"\n                  f\"ВЫПОЛНЕН. Установи его в шаге \"\n                  f\"(`python3 -m pip install pytest`).\", file=sys.stderr)",
     "why": "число в тексте отказа было литералом чужого дерева — берётся из переписи"
    },
    {
     "line": 200,
     "upstream": "def run_pytest(root: Path, files: list[str]) -> tuple[int, int, list[str]]:\n    \"\"\"Прогон набора. Возвращает (исполнено, провалено, замечания).\n\n    Счёт берётся из junit-XML, а не из разбора человекочитаемого хвоста: хвост\n    меняется между версиями, и проверка, читающая его глазами, разошлась бы молча.\n    \"\"\"\n    if not files:\n        return 0, 0, []",
     "local": "# МЕТКА ТРЕТЬЕГО ИСХОДА. Та же, что у Go-проб этого репозитория и у переписи\n# `scripts/test-standalone.sh`: контракт между пробой и прогонщиком — ТЕКСТ метки,\n# а не число её производителей.\n#\n# ЗАЧЕМ ОНА ЗДЕСЬ. Пропуск в общем случае — способ обойти, и прогонщик правильно\n# считает его отказом. Но «условие не создано» — НЕ пропуск ради зелёного: это\n# третий исход, у которого нет ни находки, ни прохода, и подавать его красным\n# значит посылать читателя чинить то, чего не ломали. Здесь такой предмет реален:\n# часть проб набора судит фикстуры БОЕВОЙ посадки, чьего производителя в этом\n# репозитории нет — машинный посев дерева поднимает АВТОНОМНЫЙ стенд, а не её.\n#\n# ЭТО НЕ МАСКА, и различие проверяемо: помеченный пропуск идёт ОТДЕЛЬНЫМ числом,\n# печатается в переписи и в ИТОГЕ, и НЕ засчитывается ни в проход, ни в отказ.\n# Непомеченный пропуск остаётся отказом — то есть обойти по-прежнему нельзя, для\n# этого надо ВПИСАТЬ метку, а вписанная метка видна в диффе и в выводе.\nPRECONDITION_MARK = \"УСЛОВИЕ НЕ СОЗДАНО\"\n\n\ndef run_pytest(root: Path, files: list[str]) -> tuple[int, int, int, list[str]]:\n    \"\"\"Прогон набора. Возвращает (исполнено, провалено, не выполнилось, замечания).\n\n    Счёт берётся из junit-XML, а не из разбора человекочитаемого хвоста: хвост\n    меняется между версиями, и проверка, читающая его глазами, разошлась бы молча.\n    \"\"\"\n    if not files:\n        return 0, 0, 0, []",
     "why": "третий исход: помеченный пропуск считается отдельно, а не отказом"
    },
    {
     "line": 219,
     "upstream": "        if not xml.is_file():\n            return 0, 0, [\n                \"pytest не оставил junit-отчёта — прогон НЕ ВЫПОЛНЕН, \"\n                f\"и это не «ноль находок» (код выхода {proc.returncode})\"]",
     "local": "        if not xml.is_file():\n            return 0, 0, 0, [\n                \"pytest не оставил junit-отчёта — прогон НЕ ВЫПОЛНЕН, \"\n                f\"и это не «ноль находок» (код выхода {proc.returncode})\"]",
     "why": "та же подпись на пути отказа"
    },
    {
     "line": 235,
     "upstream": "        notes = []\n        if skipped:\n            # Маскировка запрещена: пропуск не идёт в зачёт прохода.\n            notes.append(\n                f\"{skipped} проб(а) ПРОПУЩЕНО — пропуск не засчитывается за проход; \"\n                f\"пробе нужна своя посадка, а не skip\")\n        if proc.returncode == 5:\n            notes.append(\"pytest не собрал НИ ОДНОЙ пробы из переданных файлов\")\n        elif proc.returncode not in (0, 1):\n            notes.append(f\"pytest вышел кодом {proc.returncode} — прогон недействителен\")\n        return total, failures + errors + skipped, notes",
     "local": "        notes = []\n\n        # ПРОПУСКИ РАЗБИРАЮТСЯ ПО ОДНОМУ, а не считаются числом из шапки: шапка\n        # не говорит, помечен ли пропуск, а именно это и различает второй исход от\n        # третьего. Читается ПРИЧИНА, объявленная пробой.\n        unmet_names, plain_names = [], []\n        for case in suite.iter(\"testcase\"):\n            node = case.find(\"skipped\")\n            if node is None:\n                continue\n            reason = (node.get(\"message\") or \"\") + \" \" + (node.text or \"\")\n            name = f\"{case.get('classname','')}::{case.get('name','')}\"\n            (unmet_names if PRECONDITION_MARK in reason else plain_names).append(\n                (name, reason.strip()))\n        unmet = len(unmet_names)\n        # `skipped` из шапки отчёта — ОРИЕНТИР, а не источник: он не говорит, помечен\n        # ли пропуск. Расхождение шапки с разбором по одному означает, что отчёт\n        # устроен иначе, чем думает этот разбор, — и молчать об этом нельзя.\n        if unmet + len(plain_names) != skipped:\n            notes.append(\n                f\"шапка отчёта называет пропусков {skipped}, разбор по одному нашёл \"\n                f\"{unmet + len(plain_names)} — разбор пропусков читает не то, что \"\n                f\"пишет pytest, и третий исход считается неверно\")\n\n        for name, reason in unmet_names:\n            # Третий исход НАЗЫВАЕТСЯ, а не проглатывается: иначе «не выполнилось»\n            # неотличимо от «прошло».\n            print(f\"  НЕ ВЫПОЛНИЛОСЬ: {name} — {reason}\")\n        if plain_names:\n            # Маскировка запрещена: непомеченный пропуск не идёт в зачёт прохода.\n            notes.append(\n                f\"{len(plain_names)} проб(а) ПРОПУЩЕНО без метки «{PRECONDITION_MARK}» \"\n                f\"— пропуск не засчитывается за проход; пробе нужна своя посадка, \"\n                f\"а не skip: \" + \", \".join(n for n, _ in plain_names))\n        if proc.returncode == 5:\n            notes.append(\"pytest не собрал НИ ОДНОЙ пробы из переданных файлов\")\n        elif proc.returncode not in (0, 1):\n            notes.append(f\"pytest вышел кодом {proc.returncode} — прогон недействителен\")\n        return total - unmet, failures + errors + len(plain_names), unmet, notes",
     "why": "пропуски разбираются по одному: помеченный — третий исход, прочий — отказ"
    },
    {
     "line": 327,
     "upstream": "    print()\n    executed = 0\n    failed = 0\n\n    if pytest_files:\n        print(f\"===== прогон набора pytest ({len(pytest_files)} файл(ов)) =====\")\n        ran, bad, notes = run_pytest(root, pytest_files)\n        executed += ran\n        failed += bad\n        problems += notes",
     "local": "    print()\n    executed = 0\n    failed = 0\n    unmet = 0\n\n    if pytest_files:\n        print(f\"===== прогон набора pytest ({len(pytest_files)} файл(ов)) =====\")\n        ran, bad, skipped_unmet, notes = run_pytest(root, pytest_files)\n        executed += ran\n        failed += bad\n        unmet += skipped_unmet\n        problems += notes",
     "why": "третий исход накапливается отдельным числом"
    },
    {
     "line": 345,
     "upstream": "        if ran and declared and ran < declared:\n            problems.append(\n                f\"объявлено {declared} проб, исполнено {ran} — часть файла не \"\n                f\"собралась; недобор состава молчать не должен\")",
     "local": "        # Недобор считается ОТ ИСПОЛНИМОГО: объявленные пробы, чьё условие не\n        # создано, исполниться не могли, и вычитать их надо здесь, а не из\n        # вердикта. Иначе третий исход читался бы как несобравшийся файл.\n        if ran and declared and ran < declared - skipped_unmet:\n            problems.append(\n                f\"объявлено {declared} проб (из них не выполнилось \"\n                f\"{skipped_unmet}), исполнено {ran} — часть файла не собралась; \"\n                f\"недобор состава молчать не должен\")",
     "why": "недобор считается от исполнимого, иначе третий исход читается как поломка сборки"
    },
    {
     "line": 358,
     "upstream": "    print(f\"===== ИТОГ: файлов {len(files)} \"\n          f\"(набор {len(pytest_files)}, гейт {len(script_files)}); \"\n          f\"проб исполнено {executed}; провалено {failed} =====\")",
     "local": "    # ИТОГ НЕСЁТ ВСЕ ТРИ ЧИСЛА. Третье не вычитается из вердикта и не\n    # засчитывается в успех, поэтому его отсутствие в строке итога сделало бы\n    # «не выполнилось» неотличимым от «прошло».\n    print(f\"===== ИТОГ: файлов {len(files)} \"\n          f\"(набор {len(pytest_files)}, гейт {len(script_files)}); \"\n          f\"проб исполнено {executed}; провалено {failed}; \"\n          f\"НЕ ВЫПОЛНИЛОСЬ {unmet} =====\")",
     "why": "итог печатает все три числа"
    },
    {
     "line": 383,
     "upstream": "    print(f\"PASS: все {executed} проб(ы) исполнены и зелёные\")\n    return 0",
     "local": "    if unmet:\n        print(f\"PASS: все {executed} исполненных проб(ы) зелёные; \"\n              f\"НЕ ВЫПОЛНИЛОСЬ {unmet} — условие не создано, и это не зачтено \"\n              f\"в проход (см. строки «НЕ ВЫПОЛНИЛОСЬ» выше)\")\n        return 0\n    print(f\"PASS: все {executed} проб(ы) исполнены и зелёные\")\n    return 0",
     "why": "успех не молчит о третьем исходе"
    },
    {
     "line": 553,
     "upstream": "    print(\"(f) пропуск не засчитывается за проход\")\n    rc, out = run(_at_root(\"skip_test.py\", _OK_PROBE + _SKIPPED_PROBE))\n    check(\"пропущенная проба роняет прогон\", rc == 1, out)\n    check(\"пропуск назван\", \"ПРОПУЩЕНО\" in out, out)\n",
     "local": "    print(\"(f) пропуск не засчитывается за проход\")\n    rc, out = run(_at_root(\"skip_test.py\", _OK_PROBE + _SKIPPED_PROBE))\n    check(\"пропущенная проба роняет прогон\", rc == 1, out)\n    check(\"пропуск назван\", \"ПРОПУЩЕНО\" in out, out)\n\n    # ── (j) ТРЕТИЙ ИСХОД ОТЛИЧИМ ОТ ВТОРОГО — И ОТ ПЕРВОГО ───────────────────\n    #\n    # Ось доказывается ТРОЙКОЙ, а не парой, потому что различить надо три\n    # состояния, а не два: помеченный пропуск НЕ роняет прогон (иначе «условие не\n    # создано» подавалось бы красным), НЕ засчитывается в проход (иначе он был бы\n    # маской) и НАЗЫВАЕТСЯ числом в итоге (иначе неотличим от прохода). Рядом —\n    # законный близнец из (f): тот же пропуск БЕЗ метки по-прежнему красный, то\n    # есть обойти можно только ВПИСАВ метку, а вписанная метка видна и в диффе, и\n    # в выводе.\n    print(\"(j) помеченный пропуск — третий исход, а не отказ и не проход\")\n    rc, out = run(_at_root(\"unmet_test.py\", _OK_PROBE + _UNMET_PROBE))\n    check(\"помеченный пропуск НЕ роняет прогон\", rc == 0, out)\n    check(\"он назван третьим исходом\", \"НЕ ВЫПОЛНИЛОСЬ\" in out, out)\n    check(\"итог печатает его число\", \"НЕ ВЫПОЛНИЛОСЬ 1 =====\" in out, out)\n    check(\"и он НЕ зачтён в проход\", \"исполнено 1;\" in out, out)\n    check(\"причина третьего исхода напечатана\",\n          \"посев не приехал\" in out or PRECONDITION_MARK in out, out)\n    rc2, out2 = run(_at_root(\"unmet_only_test.py\", _UNMET_PROBE))\n    check(\"один только третий исход — ОТКАЗ: исполнено ноль\", rc2 == 1, out2)\n    check(\"и отказ объясняет, что не исполнено ничего\",\n          \"не исполнено НИ ОДНОЙ пробы\" in out2, out2)\n",
     "why": "доказательство третьего исхода: тройка плюс законный близнец из (f)"
    },
    {
     "line": 455,
     "upstream": "_SKIPPED_PROBE = (",
     "local": "# Помеченный пропуск: третий исход. Метка — ТА ЖЕ, что читает разбор выше, и\n# берётся она из той же константы, а не выписывается второй раз.\n_UNMET_PROBE = (\n    \"import pytest\\n\\n\"\n    \"def test_unmet():\\n\"\n    f\"    pytest.skip('{PRECONDITION_MARK}: посев не приехал в этот репозиторий')\\n\")\n\n_SKIPPED_PROBE = (",
     "why": "фикстура третьего исхода берёт метку у того же производителя"
    },
    {
     "line": 637,
     "upstream": "    print(\"ДОКАЗАНО: прогонщик краснеет на дефекте, молчит на законной форме, \"\n          \"отвергает пустой обход, немой файл и пропуск.\")",
     "local": "    print(\"ДОКАЗАНО: прогонщик краснеет на дефекте, молчит на законной форме, \"\n          \"отвергает пустой обход, немой файл и НЕПОМЕЧЕННЫЙ пропуск, а помеченный \"\n          \"различает третьим исходом — не роняя прогон и не зачитывая в проход.\")",
     "why": "итоговая строка доказательства называет и третий исход"
    }
   ]
  },
  {
   "path": "tests/authz-fixtures/principal_pairings.py",
   "upstream_blob": "80183e8cc34fd0af8a54b863c8e61e3cc42aeab7",
   "upstream_sha256": "d1989123c4ed341a2265b5cfc53af8f04827379aa8583d81f57c06577527189e",
   "local_sha256": "d1989123c4ed341a2265b5cfc53af8f04827379aa8583d81f57c06577527189e",
   "rewrites": []
  }
 ]
}
