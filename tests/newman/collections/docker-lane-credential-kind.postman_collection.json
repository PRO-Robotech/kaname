{
  "info": {
    "_postman_id": "b35e6e71-ace1-59d5-95c1-c3befc4ac562",
    "name": "kacho-iam / newman / docker-lane-credential-kind",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  // runId формат: только [a-z0-9], без точки, начинается с буквы — чтобы проходить compute name regex",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', ('r' + t + r).replace(/[^a-z0-9]/g, '').slice(0, 11));",
          "}",
          "pm.environment.set('_suiteProjectId', pm.environment.get('existingProjectId'));",
          "pm.environment.set('_suiteFolderCrossId', pm.environment.get('existingProjectCrossId'));"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "try {",
          "  const _ct = (pm.response && pm.response.headers && pm.response.headers.get('Content-Type')) || '';",
          "  if (_ct.indexOf('json') !== -1) {",
          "    const _j = pm.response.json();",
          "    if (_j && _j.done === true && typeof _j.id === 'string' && _j.id !== '') {",
          "      if (_j.error) {",
          "        const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
          "        _pv.forEach(function (k) { pm.environment.unset(k); });",
          "      }",
          "      pm.environment.unset('_provisionalIds');",
          "    }",
          "  }",
          "} catch (e) { /* не JSON, не Operation — регистрация остаётся как есть */ }"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY — Докер-полоса принимает базовый токен доступа и отвергает ключевой материал в поле пароля тем же отказом, что и неверный секрет",
      "description": "class:SEC | class:CONF | class:NEG | priority:P0",
      "item": [
        {
          "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY :: ask-the-lane-which-audience-it-serves",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "token"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/token');",
                  "} else {",
                  "  pm.test('harness config: iamRegistryTokenBaseUrl is set — докер-полоса — у неё же и спрашиваем, кому она чеканит', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"\\u0432\\u044b\\u0437\\u043e\\u0432 \\u043f\\u043e\\u043b\\u043e\\u0441\\u044b \\u043d\\u0430 \\u0430\\u0443\\u0442\\u0435\\u043d\\u0442\\u0438\\u0444\\u0438\\u043a\\u0430\\u0446\\u0438\\u044e: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('полоса вызывает на аутентификацию (401)', () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.eql(401);",
                  "});",
                  "const _wa = pm.response.headers.get('WWW-Authenticate') || '';",
                  "const _m = _wa.match(/service=\"([^\"]+)\"/);",
                  "pm.test('вызов НАЗЫВАЕТ адресата, которому полоса чеканит', () => {",
                  "  pm.expect(_m && _m[1], 'WWW-Authenticate: ' + _wa)",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "});",
                  "if (_m && _m[1]) { pm.environment.set('dockerLaneServiceAud', _m[1]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY :: issue-secret-credential-for-a-service-account",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/serviceAccounts/{{svaAId}}/keys",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "serviceAccounts",
                "{{svaAId}}",
                "keys"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"serviceAccountId\": \"{{svaAId}}\", \"description\": \"docker lane credential kind {{runId}}\", \"credentialKind\": \"CREDENTIAL_KIND_SECRET\", \"ttlSeconds\": 2592000}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"SAKeyService.Issue \\u0432\\u0438\\u0434\\u0430 SECRET: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('операция завершена в ответе самого Issue (done=true)', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.done, 'operation.done').to.eql(true);",
                  "  pm.expect(j.error, 'operation.error: ' + JSON.stringify(j.error)).to.be.undefined;",
                  "});",
                  "const _r = pm.response.json().response || {};",
                  "pm.test('ответ несёт непустую строку секрета объявленной формы', () => {",
                  "  pm.expect(_r.secret, 'response.secret').to.be.a('string')",
                  "    .and.to.match(/^kacho_soc[0-9a-hjkmnp-tv-z]{17}_[0-9a-hjkmnp-tv-z]{32}$/);",
                  "});",
                  "pm.test('ответ вида SECRET НЕ несёт ключевого материала ни в одном поле', () => {",
                  "  pm.expect(_r.privateKeyPem || '', 'response.privateKeyPem').to.eql('');",
                  "  pm.expect(_r.publicKeyPem || '', 'response.publicKeyPem').to.eql('');",
                  "  pm.expect(_r.algorithm || '', 'response.algorithm').to.eql('');",
                  "});",
                  "const _p = String(_r.secret).split('_');",
                  "const _credId = _p.slice(1, _p.length - 1).join('_');",
                  "pm.test('строка называет своё удостоверение', () => {",
                  "  pm.expect(_credId, 'идентификатор из строки').to.be.a('string').with.length.greaterThan(0);",
                  "});",
                  "if (_r.secret) { pm.environment.set('dockerLaneSecret', _r.secret); }",
                  "if (_credId) { pm.environment.set('dockerLaneCredId', _credId); }",
                  "const _keyId = _r.keyId || (_r.key && _r.key.id) || _credId;",
                  "if (_keyId) { pm.environment.set('dockerLaneKeyId', _keyId); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY :: docker-login-with-the-basic-access-token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/token?service={{dockerLaneServiceAud}}&scope=repository:kacho/km-{{runId}}:pull",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "token?service={{dockerLaneServiceAud}}&scope=repository:kacho",
                "km-{{runId}}:pull"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/token?service={{dockerLaneServiceAud}}&scope=repository:kacho/km-{{runId}}:pull');",
                  "} else {",
                  "  pm.test('harness config: iamRegistryTokenBaseUrl is set — докер-полоса — ручка, которую реестр называет докер-клиенту', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' +",
                  "  CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "    pm.environment.get('dockerLaneCredId') + ':' + pm.environment.get('dockerLaneSecret')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"\\u0434\\u043e\\u043a\\u0435\\u0440-\\u0432\\u0445\\u043e\\u0434 \\u0431\\u0430\\u0437\\u043e\\u0432\\u044b\\u043c \\u0442\\u043e\\u043a\\u0435\\u043d\\u043e\\u043c \\u0434\\u043e\\u0441\\u0442\\u0443\\u043f\\u0430: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('живой базовый токен доступа принят докер-полосой', () => {",
                  "  pm.expect(pm.response.code, '401 здесь означает, что полоса не приняла годную '",
                  "    + 'строку; 503 — что авторитет не ответил. Тело: ' + pm.response.text()).to.eql(200);",
                  "});",
                  "pm.test('выдано удостоверение реестра', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.token, 'token').to.be.a('string').with.length.greaterThan(0);",
                  "  pm.expect(j.access_token, 'access_token — докер-клиент читает оба поля')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "});",
                  "pm.test('предъявленная строка не уехала в выданный токен', () => {",
                  "  pm.expect(pm.response.text()).to.not.include(pm.environment.get('dockerLaneSecret'));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY :: docker-login-with-key-material-is-refused",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/token?service={{dockerLaneServiceAud}}&scope=repository:kacho/km-{{runId}}:pull",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "token?service={{dockerLaneServiceAud}}&scope=repository:kacho",
                "km-{{runId}}:pull"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/token?service={{dockerLaneServiceAud}}&scope=repository:kacho/km-{{runId}}:pull');",
                  "} else {",
                  "  pm.test('harness config: iamRegistryTokenBaseUrl is set — докер-полоса — прежний вход ключевым материалом', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _km = '-----BEGIN PRIVATE KEY-----\\n'",
                  "  + 'not-a-real-key-1143\\n-----END PRIVATE KEY-----';",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' +",
                  "  CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "    pm.environment.get('dockerLaneCredId') + ':' + _km))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"\\u0434\\u043e\\u043a\\u0435\\u0440-\\u0432\\u0445\\u043e\\u0434 \\u043a\\u043b\\u044e\\u0447\\u0435\\u0432\\u044b\\u043c \\u043c\\u0430\\u0442\\u0435\\u0440\\u0438\\u0430\\u043b\\u043e\\u043c: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('ключевой материал в поле пароля отвергнут (#1143)', () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.eql(401);",
                  "});",
                  "pm.test('удостоверение реестра не выдано', () => {",
                  "  let j = null; try { j = pm.response.json(); } catch (e) { j = null; }",
                  "  pm.expect(j && j.token, JSON.stringify(j)).to.be.oneOf([undefined, null]);",
                  "});",
                  "pm.test('отказ называет годный вид удостоверения', () => {",
                  "  pm.expect(pm.response.text(), 'тело отказа').to.include('kacho_');",
                  "});",
                  "pm.environment.set('dockerLaneRefusalKind', pm.response.text());",
                  "pm.environment.set('dockerLaneRefusalKindWWW',",
                  "  pm.response.headers.get('WWW-Authenticate') || '');"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY :: docker-login-with-a-wrong-secret-is-refused-identically",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/token?service={{dockerLaneServiceAud}}&scope=repository:kacho/km-{{runId}}:pull",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "token?service={{dockerLaneServiceAud}}&scope=repository:kacho",
                "km-{{runId}}:pull"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/token?service={{dockerLaneServiceAud}}&scope=repository:kacho/km-{{runId}}:pull');",
                  "} else {",
                  "  pm.test('harness config: iamRegistryTokenBaseUrl is set — докер-полоса — контроль неразличимости отказа', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _wrong = 'kacho_' + pm.environment.get('dockerLaneCredId')",
                  "  + '_00000000000000000000000000000000';",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' +",
                  "  CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "    pm.environment.get('dockerLaneCredId') + ':' + _wrong))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"\\u0434\\u043e\\u043a\\u0435\\u0440-\\u0432\\u0445\\u043e\\u0434 \\u043d\\u0435\\u0432\\u0435\\u0440\\u043d\\u044b\\u043c \\u0441\\u0435\\u043a\\u0440\\u0435\\u0442\\u043e\\u043c: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('неверный секрет отвергнут', () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.eql(401);",
                  "});",
                  "pm.test('отказ снятому виду и отказ неверному секрету НЕРАЗЛИЧИМЫ', () => {",
                  "  pm.expect(pm.response.text(), 'тело: различимые тела сказали бы предъявителю, '",
                  "    + 'как разобран его вход').to.eql(pm.environment.get('dockerLaneRefusalKind'));",
                  "  pm.expect(pm.response.headers.get('WWW-Authenticate') || '', 'WWW-Authenticate')",
                  "    .to.eql(pm.environment.get('dockerLaneRefusalKindWWW'));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY :: revoke-the-credential",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/serviceAccounts/{{svaAId}}/keys/{{dockerLaneKeyId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "serviceAccounts",
                "{{svaAId}}",
                "keys",
                "{{dockerLaneKeyId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"SAKeyService.Revoke: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-DOCKER-LANE-BASIC-TOKEN-ONLY :: poll-op",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION GUARD — 'opId' is captured by the mutation this poll follows.",
                  "// Empty = that mutation returned no Operation. Report it (a skipped request",
                  "// leaves no trace at all) and skip, rather than polling a literal template.",
                  "if (!pm.environment.get('opId')) {",
                  "  pm.test('operation id opId was captured (the mutation returned an Operation)', () => {",
                  "    pm.expect.fail('opId is empty — the mutation this poll belongs to did not return an Operation (it was rejected, or its capture failed). Polling would hit an unresolved template; a previous case\\'s operation is NOT a substitute.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// poll-counter reset on first entry (request-name-scoped flag);",
                  "// re-invocations via setNextRequest skip the reset.",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 50) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}
