{
  "info": {
    "_postman_id": "27b51cb3-15cf-5d76-9840-f33e34ca06c7",
    "name": "kaname / newman / kaname-authorization-code",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  // runId формат: только [a-z0-9], без точки, начинается с буквы — чтобы проходить compute name regex",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', ('r' + t + r).replace(/[^a-z0-9]/g, '').slice(0, 11));",
          "}",
          "pm.environment.set('_suiteProjectId', pm.environment.get('existingProjectId'));",
          "pm.environment.set('_suiteFolderCrossId', pm.environment.get('existingProjectCrossId'));"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "try {",
          "  const _ct = (pm.response && pm.response.headers && pm.response.headers.get('Content-Type')) || '';",
          "  if (_ct.indexOf('json') !== -1) {",
          "    const _j = pm.response.json();",
          "    if (_j && _j.done === true && typeof _j.id === 'string' && _j.id !== '') {",
          "      if (_j.error) {",
          "        const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
          "        _pv.forEach(function (k) { pm.environment.unset(k); });",
          "      }",
          "      pm.environment.unset('_provisionalIds');",
          "    }",
          "  }",
          "} catch (e) { /* не JSON, не Operation — регистрация остаётся как есть */ }"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "IAM-AUTHCODE-OK-ISSUE-EXCHANGE-ACCEPT — 02→10→18: вход нашим входом, код перенаправлением, обмен на наш токен, фронт принимает его",
      "description": "class:CRUD | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-OK-ISSUE-EXCHANGE-ACCEPT :: Hp-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acHpCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acHpFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-ISSUE-EXCHANGE-ACCEPT :: Hp-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acHpCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acHpSessionCookie'); pm.environment.unset('_acHpUserId');",
                  "pm.environment.set('_acHpLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acHpFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acHpUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acHpLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acHpSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-ISSUE-EXCHANGE-ACCEPT :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acHpQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acHpQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acHpCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acHpVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acHpState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acHpRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acHpQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acHpQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acHpSessionCookie')) {",
                  "  pm.test('Hp: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acHpSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('02: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acHpRedirect1')).base));",
                  "pm.test('02: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('02: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acHpState1')]));",
                  "pm.test('02: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acHpCode1', _acGot.q.code[0]); }",
                  "pm.test('02: код не несёт ни субъекта, ни области в разбираемом виде', () => {",
                  "  const c = pm.environment.get('_acHpCode1') || ''; const uid = pm.environment.get('_acHpUserId') || '';",
                  "  let dec = ''; try { dec = CryptoJS.enc.Base64.parse(c.split('-').join('+').split('_').join('/')).toString(CryptoJS.enc.Latin1); } catch (e) { dec = ''; }",
                  "  pm.expect(c, 'код не выдан').to.not.eql('');",
                  "  pm.expect([c.includes(uid), dec.includes(uid), c.includes('openid'), dec.includes('openid')], 'код').to.eql([false, false, false, false]); });",
                  "pm.test('02: код — не токен с утверждениями (не три сегмента через точку)', () => pm.expect((pm.environment.get('_acHpCode1') || '').split('.').length, 'сегментов').to.not.eql(3));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-ISSUE-EXCHANGE-ACCEPT :: exchange",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acHpCode1')) {",
                  "  pm.test('Hp: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acHpCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acHpRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acHpVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acHpAccessToken1'); pm.environment.unset('_acHpRefreshToken1');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('10: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('10: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('10: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('10: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('10: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acHpAccessToken1', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acHpRefreshToken1', j.refresh_token); }",
                  "const _acClaims = (t) => { try { let b = String(t).split('.')[1].split('-').join('+').split('_').join('/');",
                  "  while (b.length % 4) { b += '='; } return JSON.parse(CryptoJS.enc.Base64.parse(b).toString(CryptoJS.enc.Utf8)); }",
                  "  catch (e) { return {}; } };",
                  "const c = _acClaims(j.access_token);",
                  "const uid = pm.environment.get('_acHpUserId');",
                  "pm.test('10: sub — человек, вошедший нашим входом', () => pm.expect(c.sub, JSON.stringify(Object.keys(c))).to.eql(uid));",
                  "pm.test('10: вид принципала — user', () => pm.expect(c.kaname_principal_type, 'kaname_principal_type').to.eql('user'));",
                  "pm.test('10: идентификатор принципала — тот же человек', () => pm.expect(c.kaname_principal_id, 'kaname_principal_id').to.eql(uid));",
                  "pm.test('10: client_id — клиент, которому выдан код', () => pm.expect(c.client_id, 'client_id').to.eql(pm.environment.get('oauthClientId')));",
                  "pm.test('10: acr — уровень сессии, в которой выдан код', () => pm.expect(String(c.acr), 'acr').to.eql(pm.environment.get('_acHpLevel')));",
                  "pm.test('10: auth_time — момент входа этой сессии (±60 с часов стенда)', () => {",
                  "  const at = Number(pm.environment.get('_acHpLoginAt')); const now = Math.floor(Date.now() / 1000);",
                  "  pm.expect(c.auth_time, 'auth_time').to.be.a('number');",
                  "  pm.expect(c.auth_time >= at - 60 && c.auth_time <= now + 60, 'auth_time=' + c.auth_time + ' вход=' + at).to.eql(true); });",
                  "pm.test('10: получатель токена назван', () => pm.expect([].concat(c.aud || []).length, 'aud').to.be.above(0));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-ISSUE-EXCHANGE-ACCEPT :: present-to-own-front",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — ownRestBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('ownRestBaseUrl') || pm.variables.get('ownRestBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/me');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: ownRestBaseUrl is set — собственный публичный фронт службы: рутинный глагол, которым проверяется, что выданный церемонией токен годен', () => {",
                  "    pm.expect.fail('ownRestBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acHpAccessToken1')) {",
                  "  pm.test('Hp: токен доступа не захвачен обменом', () => {",
                  "    pm.expect.fail('обмен выше не вернул access_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + pm.environment.get('_acHpAccessToken1')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ФРОНТ: субъект запроса — человек, вошедший нашим входом', () => pm.expect(j.subject, JSON.stringify(j)).to.eql('user:' + pm.environment.get('_acHpUserId')));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-BVA-STATE-AT-FLOOR — 30: state ровно 22 знака — код выдан, state возвращён дословно",
      "description": "class:BVA | priority:P1",
      "item": [
        {
          "name": "IAM-AUTHCODE-BVA-STATE-AT-FLOOR :: Sf-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acSfCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acSfFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-BVA-STATE-AT-FLOOR :: Sf-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acSfCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acSfSessionCookie'); pm.environment.unset('_acSfUserId');",
                  "pm.environment.set('_acSfLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acSfFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acSfUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acSfLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acSfSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-BVA-STATE-AT-FLOOR :: authorize-state-22",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acSfQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acSfQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acSfCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acSfVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 22);",
                  "pm.environment.set('_acSfState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acSfRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acSfQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acSfQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acSfSessionCookie')) {",
                  "  pm.test('Sf: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acSfSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('30: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acSfRedirect1')).base));",
                  "pm.test('30: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('30: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acSfState1')]));",
                  "pm.test('30: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acSfCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-STATE-BELOW-FLOOR — 29: state не прислан либо 21 знак — перенаправление только с error=invalid_request",
      "description": "class:NEG | class:BVA | priority:P1",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-STATE-BELOW-FLOOR :: Sb-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acSbCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acSbFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-STATE-BELOW-FLOOR :: Sb-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acSbCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acSbSessionCookie'); pm.environment.unset('_acSbUserId');",
                  "pm.environment.set('_acSbLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acSbFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acSbUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acSbLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acSbSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-STATE-BELOW-FLOOR :: authorize-without-state",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acSbQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acSbQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acSbCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acSbVerifier1', _acV);",
                  "const _acS = null;",
                  "pm.environment.set('_acSbState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acSbRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acSbQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acSbQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acSbSessionCookie')) {",
                  "  pm.test('Sb: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acSbSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('29/нет: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acSbRedirect1')).base));",
                  "pm.test('29/нет: в перенаправлении только error=invalid_request — ни кода, ни state, ни описания', () => {",
                  "  const own = _acParse(pm.environment.get('_acSbRedirect1')).q; const got = Object.assign({}, _acGot.q);",
                  "  Object.keys(own).forEach((k) => { if (JSON.stringify(got[k]) === JSON.stringify(own[k])) { delete got[k]; } });",
                  "  pm.expect(got, _acLoc).to.eql({error: ['invalid_request']}); });"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-STATE-BELOW-FLOOR :: authorize-state-21",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acSbQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acSbQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acSbCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acSbVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 21);",
                  "pm.environment.set('_acSbState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acSbRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acSbQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acSbQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acSbSessionCookie')) {",
                  "  pm.test('Sb: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acSbSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('29/21: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acSbRedirect2')).base));",
                  "pm.test('29/21: в перенаправлении только error=invalid_request — ни кода, ни state, ни описания', () => {",
                  "  const own = _acParse(pm.environment.get('_acSbRedirect2')).q; const got = Object.assign({}, _acGot.q);",
                  "  Object.keys(own).forEach((k) => { if (JSON.stringify(got[k]) === JSON.stringify(own[k])) { delete got[k]; } });",
                  "  pm.expect(got, _acLoc).to.eql({error: ['invalid_request']}); });"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-STATE-BELOW-FLOOR :: authorize-state-22-control",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acSbQ3}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acSbQ3}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acSbCode3');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acSbVerifier3', _acV);",
                  "const _acS = _acRand(48).slice(0, 22);",
                  "pm.environment.set('_acSbState3', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acSbRedirect3', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acSbQ3', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acSbQ3}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acSbSessionCookie')) {",
                  "  pm.test('Sb: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acSbSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('29/контроль: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acSbRedirect3')).base));",
                  "pm.test('29/контроль: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('29/контроль: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acSbState3')]));",
                  "pm.test('29/контроль: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acSbCode3', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-NO-SESSION — 03: запрос авторизации без сессии — 401 login_required; тот же запрос с сессией — код",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-NO-SESSION :: authorize-without-session",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acNsQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acNsQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acNsCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acNsVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acNsState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acNsRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acNsQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acNsQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(401));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('03: error — login_required', () => pm.expect(j.error, JSON.stringify(j)).to.eql('login_required'));",
                  "pm.test('03: перенаправления нет — заголовка Location в ответе нет', () => {",
                  "  const loc = pm.response.headers.get('Location');",
                  "  pm.expect(loc === null || loc === undefined || loc === '', 'Location=' + String(loc)).to.eql(true);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-NO-SESSION :: Ns-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acNsCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acNsFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-NO-SESSION :: Ns-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acNsCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acNsSessionCookie'); pm.environment.unset('_acNsUserId');",
                  "pm.environment.set('_acNsLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acNsFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acNsUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acNsLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acNsSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-NO-SESSION :: same-request-with-session",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acNsQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acNsQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acNsCode2');",
                  "pm.environment.set('_acNsQ2', pm.environment.get('_acNsQ1') || '');",
                  "pm.environment.set('_acNsVerifier2', pm.environment.get('_acNsVerifier1') || '');",
                  "pm.environment.set('_acNsState2', pm.environment.get('_acNsState1') || '');",
                  "pm.environment.set('_acNsRedirect2', pm.environment.get('_acNsRedirect1') || '');",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acNsQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acNsSessionCookie')) {",
                  "  pm.test('Ns: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acNsSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('03/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acNsRedirect2')).base));",
                  "pm.test('03/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('03/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acNsState2')]));",
                  "pm.test('03/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acNsCode2', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-REDIRECT-UNREGISTERED — 04: чужой адрес возврата — 400 без перенаправления; страница та же, что у неизвестного клиента",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-UNREGISTERED :: Ru-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acRuCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRuFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-UNREGISTERED :: Ru-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acRuCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acRuSessionCookie'); pm.environment.unset('_acRuUserId');",
                  "pm.environment.set('_acRuLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acRuFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acRuUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acRuLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRuSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-UNREGISTERED :: registered-redirect-control",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRuQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRuQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRuCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRuVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRuState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acRuRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRuQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRuQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRuSessionCookie')) {",
                  "  pm.test('Ru: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRuSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('04/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acRuRedirect1')).base));",
                  "pm.test('04/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('04/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acRuState1')]));",
                  "pm.test('04/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acRuCode1', _acGot.q.code[0]); }",
                  "pm.environment.set('_acRuControlStatus', String(pm.response.code));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-UNREGISTERED :: unregistered-redirect",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRuQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRuQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRuCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRuVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRuState2', _acS === null ? '' : _acS);",
                  "const _acR = 'https://unregistered-' + pm.environment.get('runId') + '.invalid/cb';",
                  "pm.environment.set('_acRuRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRuQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRuQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRuSessionCookie')) {",
                  "  pm.test('Ru: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRuSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('04: перенаправления нет — заголовка Location в ответе нет', () => {",
                  "  const loc = pm.response.headers.get('Location');",
                  "  pm.expect(loc === null || loc === undefined || loc === '', 'Location=' + String(loc)).to.eql(true);",
                  "});",
                  "pm.environment.set('_acRuRefusalPage', pm.response.text());"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-UNREGISTERED :: unknown-client",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRuQ3}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRuQ3}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRuCode3');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRuVerifier3', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRuState3', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acRuRedirect3', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', 'oic' + pm.environment.get('runId') + 'nosuchclient'],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRuQ3', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRuQ3}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRuSessionCookie')) {",
                  "  pm.test('Ru: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRuSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('04: неизвестный клиент и чужой адрес — ПОБАЙТОВО одна страница', () => {",
                  "  const first = pm.environment.get('_acRuRefusalPage');",
                  "  pm.expect(first, 'первый отказ не захвачен — сравнивать не с чем').to.be.a('string').and.not.empty;",
                  "  pm.expect(pm.environment.get('_acRuControlStatus'), 'близнец не выдал код — равенство отказов ни о чём не говорит').to.eql('302');",
                  "  pm.expect(pm.response.text()).to.eql(first); });"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-PKCE-MISSING-OR-PLAIN — 06: без code_challenge либо с методом plain — перенаправление только с error=invalid_request",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-PKCE-MISSING-OR-PLAIN :: Pk-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acPkCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acPkFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-PKCE-MISSING-OR-PLAIN :: Pk-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acPkCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acPkSessionCookie'); pm.environment.unset('_acPkUserId');",
                  "pm.environment.set('_acPkLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acPkFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acPkUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acPkLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acPkSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-PKCE-MISSING-OR-PLAIN :: authorize-without-pkce",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acPkQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acPkQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acPkCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acPkVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acPkState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acPkRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acPkQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acPkQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acPkSessionCookie')) {",
                  "  pm.test('Pk: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acPkSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('06/нет: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acPkRedirect1')).base));",
                  "pm.test('06/нет: в перенаправлении только error=invalid_request — ни кода, ни state, ни описания', () => {",
                  "  const own = _acParse(pm.environment.get('_acPkRedirect1')).q; const got = Object.assign({}, _acGot.q);",
                  "  Object.keys(own).forEach((k) => { if (JSON.stringify(got[k]) === JSON.stringify(own[k])) { delete got[k]; } });",
                  "  pm.expect(got, _acLoc).to.eql({error: ['invalid_request']}); });"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-PKCE-MISSING-OR-PLAIN :: authorize-pkce-plain",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acPkQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acPkQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acPkCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acPkVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acPkState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acPkRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acV], ['code_challenge_method', 'plain'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acPkQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acPkQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acPkSessionCookie')) {",
                  "  pm.test('Pk: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acPkSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('06/plain: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acPkRedirect2')).base));",
                  "pm.test('06/plain: в перенаправлении только error=invalid_request — ни кода, ни state, ни описания', () => {",
                  "  const own = _acParse(pm.environment.get('_acPkRedirect2')).q; const got = Object.assign({}, _acGot.q);",
                  "  Object.keys(own).forEach((k) => { if (JSON.stringify(got[k]) === JSON.stringify(own[k])) { delete got[k]; } });",
                  "  pm.expect(got, _acLoc).to.eql({error: ['invalid_request']}); });"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-PKCE-MISSING-OR-PLAIN :: authorize-pkce-s256-control",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acPkQ3}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acPkQ3}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acPkCode3');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acPkVerifier3', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acPkState3', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acPkRedirect3', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acPkQ3', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acPkQ3}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acPkSessionCookie')) {",
                  "  pm.test('Pk: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acPkSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('06/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acPkRedirect3')).base));",
                  "pm.test('06/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('06/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acPkState3')]));",
                  "pm.test('06/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acPkCode3', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER — 11: неверный code_verifier — invalid_grant; код погашен; свежий код с верным — 200",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER :: Wv-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acWvCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acWvFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER :: Wv-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acWvCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acWvSessionCookie'); pm.environment.unset('_acWvUserId');",
                  "pm.environment.set('_acWvLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acWvFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acWvUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acWvLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acWvSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acWvQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acWvQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acWvCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acWvVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acWvState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acWvRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acWvQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acWvQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acWvSessionCookie')) {",
                  "  pm.test('Wv: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acWvSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('11: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acWvRedirect1')).base));",
                  "pm.test('11: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('11: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acWvState1')]));",
                  "pm.test('11: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acWvCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER :: exchange-wrong-verifier",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acWvCode1')) {",
                  "  pm.test('Wv: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acWvCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acWvRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent((pm.environment.get('runId') + 'Z').repeat(8).slice(0, 43) || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('11: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('11: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER :: exchange-right-verifier-after",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acWvCode1')) {",
                  "  pm.test('Wv: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acWvCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acWvRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acWvVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('11/погашен: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('11/погашен: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER :: authorize-fresh",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acWvQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acWvQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acWvCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acWvVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acWvState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acWvRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acWvQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acWvQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acWvSessionCookie')) {",
                  "  pm.test('Wv: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acWvSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('11/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acWvRedirect2')).base));",
                  "pm.test('11/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('11/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acWvState2')]));",
                  "pm.test('11/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acWvCode2', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-WRONG-VERIFIER :: exchange-fresh-right-verifier",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acWvCode2')) {",
                  "  pm.test('Wv: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acWvCode2') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acWvRedirect2') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acWvVerifier2') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acWvAccessToken2'); pm.environment.unset('_acWvRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('11/близнец: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('11/близнец: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('11/близнец: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('11/близнец: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('11/близнец: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acWvAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acWvRefreshToken2', j.refresh_token); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED — 12: без секрета, с неверным секретом, с неверным секретом и неизвестным кодом — 401 invalid_client",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: Cu-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acCuCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acCuFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: Cu-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acCuCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acCuSessionCookie'); pm.environment.unset('_acCuUserId');",
                  "pm.environment.set('_acCuLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acCuFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acCuUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acCuLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acCuSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acCuQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acCuQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acCuCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acCuVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acCuState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acCuRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acCuQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acCuQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCuSessionCookie')) {",
                  "  pm.test('Cu: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acCuSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('12: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acCuRedirect1')).base));",
                  "pm.test('12: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('12: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acCuState1')]));",
                  "pm.test('12: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acCuCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: exchange-without-client-secret",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}&client_id={{_acFClientId}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCuCode1')) {",
                  "  pm.test('Cu: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCuCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCuRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCuVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(401));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('12/нет секрета: error — invalid_client', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_client'));",
                  "pm.test('12/нет секрета: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: exchange-wrong-client-secret",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCuCode1')) {",
                  "  pm.test('Cu: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCuCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCuRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCuVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent('not-the-secret-' + pm.environment.get('runId'))))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(401));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('12/неверный секрет: error — invalid_client', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_client'));",
                  "pm.test('12/неверный секрет: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: exchange-unknown-code-wrong-secret",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(('unknown' + pm.environment.get('runId')).repeat(4) || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCuRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCuVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent('not-the-secret-' + pm.environment.get('runId'))))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(401));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('12/неизвестный код: error — invalid_client', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_client'));",
                  "pm.test('12/неизвестный код: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: authorize-fresh",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acCuQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acCuQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acCuCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acCuVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acCuState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acCuRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acCuQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acCuQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCuSessionCookie')) {",
                  "  pm.test('Cu: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acCuSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('12/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acCuRedirect2')).base));",
                  "pm.test('12/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('12/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acCuState2')]));",
                  "pm.test('12/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acCuCode2', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CLIENT-UNAUTHENTICATED :: exchange-fresh-right-secret",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCuCode2')) {",
                  "  pm.test('Cu: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCuCode2') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCuRedirect2') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCuVerifier2') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acCuAccessToken2'); pm.environment.unset('_acCuRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('12/близнец: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('12/близнец: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('12/близнец: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('12/близнец: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('12/близнец: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acCuAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acCuRefreshToken2', j.refresh_token); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES — 13: второй обмен тем же кодом — invalid_grant, и токен первого обмена фронт больше не принимает",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: Cr-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acCrCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acCrFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: Cr-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acCrCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acCrSessionCookie'); pm.environment.unset('_acCrUserId');",
                  "pm.environment.set('_acCrLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acCrFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acCrUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acCrLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acCrSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acCrQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acCrQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acCrCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acCrVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acCrState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acCrRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acCrQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acCrQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCrSessionCookie')) {",
                  "  pm.test('Cr: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acCrSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('13: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acCrRedirect1')).base));",
                  "pm.test('13: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('13: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acCrState1')]));",
                  "pm.test('13: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acCrCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: exchange-first",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCrCode1')) {",
                  "  pm.test('Cr: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCrCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCrRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCrVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acCrAccessToken1'); pm.environment.unset('_acCrRefreshToken1');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('13/первый: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('13/первый: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('13/первый: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('13/первый: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('13/первый: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acCrAccessToken1', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acCrRefreshToken1', j.refresh_token); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: authorize-fresh",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acCrQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acCrQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acCrCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acCrVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acCrState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acCrRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acCrQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acCrQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCrSessionCookie')) {",
                  "  pm.test('Cr: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acCrSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('13/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acCrRedirect2')).base));",
                  "pm.test('13/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('13/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acCrState2')]));",
                  "pm.test('13/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acCrCode2', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: exchange-fresh",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCrCode2')) {",
                  "  pm.test('Cr: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCrCode2') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCrRedirect2') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCrVerifier2') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acCrAccessToken2'); pm.environment.unset('_acCrRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('13/близнец: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('13/близнец: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('13/близнец: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('13/близнец: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('13/близнец: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acCrAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acCrRefreshToken2', j.refresh_token); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: exchange-replay",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCrCode1')) {",
                  "  pm.test('Cr: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCrCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCrRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCrVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('13/повтор: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('13/повтор: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: present-token-of-the-replayed-code",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — ownRestBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('ownRestBaseUrl') || pm.variables.get('ownRestBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/me');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: ownRestBaseUrl is set — собственный публичный фронт службы: рутинный глагол, которым проверяется, что выданный церемонией токен годен', () => {",
                  "    pm.expect.fail('ownRestBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCrAccessToken1')) {",
                  "  pm.test('Cr: токен доступа не захвачен обменом', () => {",
                  "    pm.expect.fail('обмен выше не вернул access_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + pm.environment.get('_acCrAccessToken1')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(401));",
                  "pm.test('grpc code 16 (UNAUTHENTICATED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(16);",
                  "});",
                  "pm.test('ФРОНТ: отказ — единственный текст негодного удостоверения', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.message, JSON.stringify(j)).to.eql('credential is not accepted'); });"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-REPLAY-REVOKES :: present-token-of-the-fresh-code",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — ownRestBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('ownRestBaseUrl') || pm.variables.get('ownRestBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/me');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: ownRestBaseUrl is set — собственный публичный фронт службы: рутинный глагол, которым проверяется, что выданный церемонией токен годен', () => {",
                  "    pm.expect.fail('ownRestBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCrAccessToken2')) {",
                  "  pm.test('Cr: токен доступа не захвачен обменом', () => {",
                  "    pm.expect.fail('обмен выше не вернул access_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + pm.environment.get('_acCrAccessToken2')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ФРОНТ: субъект запроса — человек, вошедший нашим входом', () => pm.expect(j.subject, JSON.stringify(j)).to.eql('user:' + pm.environment.get('_acCrUserId')));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH — 15: обмен с другим, пусть зарегистрированным, адресом возврата — invalid_grant",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH :: Rm-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acRmCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRmFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH :: Rm-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acRmCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acRmSessionCookie'); pm.environment.unset('_acRmUserId');",
                  "pm.environment.set('_acRmLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acRmFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acRmUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acRmLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRmSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH :: alt-redirect-is-registered",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRmQ0}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRmQ0}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRmCode0');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRmVerifier0', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRmState0', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUriAlt');",
                  "pm.environment.set('_acRmRedirect0', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRmQ0', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRmQ0}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRmSessionCookie')) {",
                  "  pm.test('Rm: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRmSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('15/второй адрес: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acRmRedirect0')).base));",
                  "pm.test('15/второй адрес: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('15/второй адрес: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acRmState0')]));",
                  "pm.test('15/второй адрес: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acRmCode0', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRmQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRmQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRmCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRmVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRmState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acRmRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRmQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRmQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRmSessionCookie')) {",
                  "  pm.test('Rm: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRmSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('15: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acRmRedirect1')).base));",
                  "pm.test('15: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('15: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acRmState1')]));",
                  "pm.test('15: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acRmCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH :: exchange-with-alt-redirect",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRmCode1')) {",
                  "  pm.test('Rm: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acRmCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('oauthRedirectUriAlt') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acRmVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('15: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('15: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH :: authorize-fresh",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRmQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRmQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRmCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRmVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRmState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acRmRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRmQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRmQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRmSessionCookie')) {",
                  "  pm.test('Rm: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRmSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('15/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acRmRedirect2')).base));",
                  "pm.test('15/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('15/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acRmState2')]));",
                  "pm.test('15/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acRmCode2', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REDIRECT-MISMATCH :: exchange-with-same-redirect",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRmCode2')) {",
                  "  pm.test('Rm: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acRmCode2') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acRmRedirect2') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acRmVerifier2') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acRmAccessToken2'); pm.environment.unset('_acRmRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('15/близнец: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('15/близнец: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('15/близнец: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('15/близнец: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('15/близнец: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acRmAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acRmRefreshToken2', j.refresh_token); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-FOREIGN-CLIENT — 16: код первого клиента, предъявленный вторым с его верным секретом — invalid_grant",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-FOREIGN-CLIENT :: Fc-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acFcCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acFcFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-FOREIGN-CLIENT :: Fc-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acFcCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acFcSessionCookie'); pm.environment.unset('_acFcUserId');",
                  "pm.environment.set('_acFcLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acFcFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acFcUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acFcLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acFcSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-FOREIGN-CLIENT :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acFcQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acFcQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acFcCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acFcVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acFcState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acFcRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acFcQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acFcQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acFcSessionCookie')) {",
                  "  pm.test('Fc: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acFcSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('16: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acFcRedirect1')).base));",
                  "pm.test('16: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('16: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acFcState1')]));",
                  "pm.test('16: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acFcCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-FOREIGN-CLIENT :: exchange-by-other-client",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthOtherClientId') || !pm.environment.get('oauthOtherClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthOtherClientId, oauthOtherClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acFcCode1')) {",
                  "  pm.test('Fc: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acFcCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acFcRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acFcVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthOtherClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthOtherClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthOtherClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('16: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('16: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-FOREIGN-CLIENT :: authorize-fresh",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acFcQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acFcQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acFcCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acFcVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acFcState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acFcRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acFcQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acFcQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acFcSessionCookie')) {",
                  "  pm.test('Fc: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acFcSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('16/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acFcRedirect2')).base));",
                  "pm.test('16/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('16/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acFcState2')]));",
                  "pm.test('16/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acFcCode2', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-FOREIGN-CLIENT :: exchange-by-own-client",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acFcCode2')) {",
                  "  pm.test('Fc: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acFcCode2') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acFcRedirect2') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acFcVerifier2') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acFcAccessToken2'); pm.environment.unset('_acFcRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('16/близнец: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('16/близнец: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('16/близнец: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('16/близнец: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('16/близнец: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acFcAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acFcRefreshToken2', j.refresh_token); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES — 20: обмен выдаёт токен обновления; каждое предъявление даёт новую пару, цепочка продолжается",
      "description": "class:CRUD | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES :: Rr-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acRrCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRrFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES :: Rr-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acRrCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acRrSessionCookie'); pm.environment.unset('_acRrUserId');",
                  "pm.environment.set('_acRrLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acRrFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acRrUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acRrLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRrSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRrQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRrQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRrCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRrVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRrState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acRrRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRrQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRrQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRrSessionCookie')) {",
                  "  pm.test('Rr: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRrSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('20: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acRrRedirect1')).base));",
                  "pm.test('20: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('20: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acRrState1')]));",
                  "pm.test('20: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acRrCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES :: exchange",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRrCode1')) {",
                  "  pm.test('Rr: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acRrCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acRrRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acRrVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acRrAccessToken1'); pm.environment.unset('_acRrRefreshToken1');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('20/обмен: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('20/обмен: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('20/обмен: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('20/обмен: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('20/обмен: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acRrAccessToken1', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acRrRefreshToken1', j.refresh_token); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES :: refresh-first",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=refresh_token&refresh_token={{_acFRefresh}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRrRefreshToken1')) {",
                  "  pm.test('Rr: токен обновления не захвачен', () => {",
                  "    pm.expect.fail('обмен выше не вернул refresh_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFRefresh', encodeURIComponent(pm.environment.get('_acRrRefreshToken1') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acRrAccessToken2'); pm.environment.unset('_acRrRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('20/первое: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('20/первое: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('20/первое: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('20/первое: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('20/первое: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acRrAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acRrRefreshToken2', j.refresh_token); }",
                  "pm.test('20/первое: выдан НОВЫЙ токен обновления — прежний обёрнут, а не возвращён', () => pm.expect(j.refresh_token, 'refresh_token').to.not.eql(pm.environment.get('_acRrRefreshToken1')));",
                  "pm.test('20/первое: выдан НОВЫЙ токен доступа', () => pm.expect(j.access_token, 'access_token').to.not.eql(pm.environment.get('_acRrAccessToken1')));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES :: refresh-rotated",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=refresh_token&refresh_token={{_acFRefresh}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRrRefreshToken2')) {",
                  "  pm.test('Rr: токен обновления не захвачен', () => {",
                  "    pm.expect.fail('обмен выше не вернул refresh_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFRefresh', encodeURIComponent(pm.environment.get('_acRrRefreshToken2') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acRrAccessToken3'); pm.environment.unset('_acRrRefreshToken3');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('20/по цепочке: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('20/по цепочке: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('20/по цепочке: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('20/по цепочке: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('20/по цепочке: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acRrAccessToken3', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acRrRefreshToken3', j.refresh_token); }",
                  "pm.test('20/по цепочке: выдан НОВЫЙ токен обновления — прежний обёрнут, а не возвращён', () => pm.expect(j.refresh_token, 'refresh_token').to.not.eql(pm.environment.get('_acRrRefreshToken2')));",
                  "pm.test('20/по цепочке: выдан НОВЫЙ токен доступа', () => pm.expect(j.access_token, 'access_token').to.not.eql(pm.environment.get('_acRrAccessToken2')));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-OK-REFRESH-ROTATES :: present-rotated-access-token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — ownRestBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('ownRestBaseUrl') || pm.variables.get('ownRestBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/me');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: ownRestBaseUrl is set — собственный публичный фронт службы: рутинный глагол, которым проверяется, что выданный церемонией токен годен', () => {",
                  "    pm.expect.fail('ownRestBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRrAccessToken3')) {",
                  "  pm.test('Rr: токен доступа не захвачен обменом', () => {",
                  "    pm.expect.fail('обмен выше не вернул access_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + pm.environment.get('_acRrAccessToken3')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ФРОНТ: субъект запроса — человек, вошедший нашим входом', () => pm.expect(j.subject, JSON.stringify(j)).to.eql('user:' + pm.environment.get('_acRrUserId')));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY — 21: повтор обёрнутого токена обновления — invalid_grant, семейство отозвано; соседнее семейство живо",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: Rp-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acRpCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRpFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: Rp-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acRpCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acRpSessionCookie'); pm.environment.unset('_acRpUserId');",
                  "pm.environment.set('_acRpLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acRpFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acRpUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acRpLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acRpSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: authorize",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRpQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRpQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRpCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRpVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRpState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acRpRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRpQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRpQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpSessionCookie')) {",
                  "  pm.test('Rp: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRpSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('21: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acRpRedirect1')).base));",
                  "pm.test('21: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('21: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acRpState1')]));",
                  "pm.test('21: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acRpCode1', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: exchange",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpCode1')) {",
                  "  pm.test('Rp: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acRpCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acRpRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acRpVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acRpAccessToken1'); pm.environment.unset('_acRpRefreshToken1');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('21/обмен: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('21/обмен: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('21/обмен: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('21/обмен: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('21/обмен: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acRpAccessToken1', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acRpRefreshToken1', j.refresh_token); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: authorize-neighbour",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acRpQ4}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acRpQ4}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acRpCode4');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acRpVerifier4', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acRpState4', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acRpRedirect4', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acRpQ4', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acRpQ4}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpSessionCookie')) {",
                  "  pm.test('Rp: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acRpSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('21/сосед: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acRpRedirect4')).base));",
                  "pm.test('21/сосед: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('21/сосед: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acRpState4')]));",
                  "pm.test('21/сосед: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acRpCode4', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: exchange-neighbour",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpCode4')) {",
                  "  pm.test('Rp: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acRpCode4') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acRpRedirect4') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acRpVerifier4') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acRpAccessToken4'); pm.environment.unset('_acRpRefreshToken4');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('21/сосед: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('21/сосед: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('21/сосед: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('21/сосед: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('21/сосед: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acRpAccessToken4', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acRpRefreshToken4', j.refresh_token); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: refresh-first",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=refresh_token&refresh_token={{_acFRefresh}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpRefreshToken1')) {",
                  "  pm.test('Rp: токен обновления не захвачен', () => {",
                  "    pm.expect.fail('обмен выше не вернул refresh_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFRefresh', encodeURIComponent(pm.environment.get('_acRpRefreshToken1') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acRpAccessToken2'); pm.environment.unset('_acRpRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('21/обёртка: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('21/обёртка: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('21/обёртка: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('21/обёртка: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('21/обёртка: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acRpAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acRpRefreshToken2', j.refresh_token); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: replay-wrapped-refresh-token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=refresh_token&refresh_token={{_acFRefresh}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpRefreshToken1')) {",
                  "  pm.test('Rp: токен обновления не захвачен', () => {",
                  "    pm.expect.fail('обмен выше не вернул refresh_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFRefresh', encodeURIComponent(pm.environment.get('_acRpRefreshToken1') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('21/повтор: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('21/повтор: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: current-refresh-token-after-replay",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=refresh_token&refresh_token={{_acFRefresh}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpRefreshToken2')) {",
                  "  pm.test('Rp: токен обновления не захвачен', () => {",
                  "    pm.expect.fail('обмен выше не вернул refresh_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFRefresh', encodeURIComponent(pm.environment.get('_acRpRefreshToken2') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('21/текущий после повтора: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('21/текущий после повтора: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: present-family-access-token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — ownRestBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('ownRestBaseUrl') || pm.variables.get('ownRestBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/me');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: ownRestBaseUrl is set — собственный публичный фронт службы: рутинный глагол, которым проверяется, что выданный церемонией токен годен', () => {",
                  "    pm.expect.fail('ownRestBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpAccessToken2')) {",
                  "  pm.test('Rp: токен доступа не захвачен обменом', () => {",
                  "    pm.expect.fail('обмен выше не вернул access_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + pm.environment.get('_acRpAccessToken2')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(401));",
                  "pm.test('grpc code 16 (UNAUTHENTICATED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(16);",
                  "});",
                  "pm.test('ФРОНТ: отказ — единственный текст негодного удостоверения', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.message, JSON.stringify(j)).to.eql('credential is not accepted'); });"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-REFRESH-REPLAY-REVOKES-FAMILY :: present-neighbour-access-token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — ownRestBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('ownRestBaseUrl') || pm.variables.get('ownRestBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/me');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: ownRestBaseUrl is set — собственный публичный фронт службы: рутинный глагол, которым проверяется, что выданный церемонией токен годен', () => {",
                  "    pm.expect.fail('ownRestBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acRpAccessToken4')) {",
                  "  pm.test('Rp: токен доступа не захвачен обменом', () => {",
                  "    pm.expect.fail('обмен выше не вернул access_token — предъявлять нечего; причина — в шаге обмена, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + pm.environment.get('_acRpAccessToken4')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ФРОНТ: субъект запроса — человек, вошедший нашим входом', () => pm.expect(j.subject, JSON.stringify(j)).to.eql('user:' + pm.environment.get('_acRpUserId')));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED — 14: код, предъявленный позже 60 с, — invalid_grant; выданный тогда же и обменянный сразу — 200",
      "description": "class:NEG | class:BVA | priority:P1",
      "item": [
        {
          "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED :: Ce-login-csrf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=login"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('loginLaneEmail') || !pm.environment.get('loginLanePassword')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: loginLaneEmail, loginLanePassword заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: признак формы выдан', () => pm.expect(j.csrfToken, 'csrfToken').to.be.a('string').and.not.empty);",
                  "if (j.csrfToken) { pm.environment.set('_acCeCsrf', j.csrfToken); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_form').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acCeFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED :: Ce-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"password\": \"{{loginLanePassword}}\", \"csrfToken\": \"{{_acCeCsrf}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/login');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы входа паролем службы; поднимается только посадкой `own` — на посадке `external` его нет, и это условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.12'});",
                  "pm.environment.unset('_acCeSessionCookie'); pm.environment.unset('_acCeUserId');",
                  "pm.environment.set('_acCeLoginAt', String(Math.floor(Date.now() / 1000)));",
                  "pm.request.headers.upsert({key: 'Cookie', value: 'kaname_form=' + (pm.environment.get('_acCeFormCookie') || '')});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('ВХОД: тело называет человека', () => pm.expect(j.user && j.user.id, 'user.id').to.be.a('string').and.not.empty);",
                  "pm.test('ВХОД: сессия уровня 1 — вход паролем без второго фактора', () => pm.expect(j.session && j.session.assuranceLevel, 'session.assuranceLevel').to.eql('1'));",
                  "if (j.user && j.user.id) { pm.environment.set('_acCeUserId', j.user.id); }",
                  "if (j.session) { pm.environment.set('_acCeLevel', String(j.session.assuranceLevel)); }",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session='));",
                  "  pm.test('ВХОД: ответ ставит печенье kaname_session', () => pm.expect(__sc.length, 'Set-Cookie ' + 'kaname_session').to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('_acCeSessionCookie', __sc[0].value.split(';')[0].slice(15)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED :: authorize-to-expire",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acCeQ1}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acCeQ1}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acCeCode1');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acCeVerifier1', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acCeState1', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acCeRedirect1', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acCeQ1', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acCeQ1}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCeSessionCookie')) {",
                  "  pm.test('Ce: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acCeSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('14: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acCeRedirect1')).base));",
                  "pm.test('14: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('14: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acCeState1')]));",
                  "pm.test('14: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acCeCode1', _acGot.q.code[0]); }",
                  "if (pm.environment.get('_acCeCode1')) { pm.environment.set('_acCeIssuedAt1', String(Date.now())); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED :: authorize-to-exchange-now",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/authorize?{{_acCeQ2}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "authorize?{{_acCeQ2}}"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "followRedirects": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret') || !pm.environment.get('oauthRedirectUri') || !pm.environment.get('oauthRedirectUriAlt')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret, oauthRedirectUri, oauthRedirectUriAlt заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const _acB64u = (wa) => { let s = CryptoJS.enc.Base64.stringify(wa).split('+').join('-').split('/').join('_'); while (s.endsWith('=')) { s = s.slice(0, -1); } return s; };",
                  "const _acRand = (bytes) => _acB64u(CryptoJS.lib.WordArray.random(bytes));",
                  "const _acS256 = (v) => _acB64u(CryptoJS.SHA256(v));",
                  "pm.environment.unset('_acCeCode2');",
                  "const _acV = _acRand(32);",
                  "pm.environment.set('_acCeVerifier2', _acV);",
                  "const _acS = _acRand(48).slice(0, 43);",
                  "pm.environment.set('_acCeState2', _acS === null ? '' : _acS);",
                  "const _acR = pm.environment.get('oauthRedirectUri');",
                  "pm.environment.set('_acCeRedirect2', _acR || '');",
                  "const _acP = [",
                  "  ['response_type', 'code'],",
                  "  ['client_id', pm.environment.get('oauthClientId')],",
                  "  ['redirect_uri', _acR],",
                  "  ['scope', 'openid'],",
                  "  ['state', _acS],",
                  "  ['code_challenge', _acS256(_acV)], ['code_challenge_method', 'S256'],",
                  "];",
                  "const _acQ = _acP.filter((p) => p[1] !== null).map((p) => encodeURIComponent(p[0]) + '=' + encodeURIComponent(p[1])).join('&');",
                  "pm.environment.set('_acCeQ2', _acQ);",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/authorize?{{_acCeQ2}}');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCeSessionCookie')) {",
                  "  pm.test('Ce: сессия человека не захвачена входом', () => {",
                  "    pm.expect.fail('вход выше не выдал kaname_session — точке авторизации нечего предъявить; причина — в шаге входа, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  pm.request.headers.upsert({key: 'Cookie', value: 'kaname_session=' + pm.environment.get('_acCeSessionCookie')});",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 302', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(302));",
                  "const _acParse = (u) => { const s = String(u || ''); const i = s.indexOf('?'); const q = {};",
                  "  if (i >= 0) { s.slice(i + 1).split('#')[0].split('&').forEach((kv) => { if (!kv) { return; }",
                  "    const j = kv.indexOf('='); const dec = (x) => decodeURIComponent(x.split('+').join(' '));",
                  "    const k = dec(j < 0 ? kv : kv.slice(0, j)); (q[k] = q[k] || []).push(j < 0 ? '' : dec(kv.slice(j + 1))); }); }",
                  "  return { base: i < 0 ? s.split('#')[0] : s.slice(0, i), q: q }; };",
                  "const _acLoc = pm.response.headers.get('Location') || '';",
                  "const _acGot = _acParse(_acLoc);",
                  "pm.test('14/близнец: перенаправление ведёт на адрес возврата запроса', () => pm.expect(_acGot.base, _acLoc).to.eql(_acParse(pm.environment.get('_acCeRedirect2')).base));",
                  "pm.test('14/близнец: в перенаправлении ровно один непустой code', () => {",
                  "  pm.expect(_acGot.q.code, _acLoc).to.be.an('array').with.lengthOf(1);",
                  "  pm.expect(_acGot.q.code[0], 'code').to.be.a('string').and.not.empty; });",
                  "pm.test('14/близнец: state возвращён дословно', () => pm.expect(_acGot.q.state, _acLoc).to.eql([pm.environment.get('_acCeState2')]));",
                  "pm.test('14/близнец: отказа в перенаправлении нет', () => pm.expect(_acGot.q.error, _acLoc).to.eql(undefined));",
                  "if (_acGot.q.code && _acGot.q.code.length === 1) { pm.environment.set('_acCeCode2', _acGot.q.code[0]); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED :: exchange-within-ttl",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCeCode2')) {",
                  "  pm.test('Ce: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCeCode2') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCeRedirect2') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCeVerifier2') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.unset('_acCeAccessToken2'); pm.environment.unset('_acCeRefreshToken2');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('14/близнец: token_type — Bearer', () => pm.expect(j.token_type, JSON.stringify(Object.keys(j))).to.eql('Bearer'));",
                  "pm.test('14/близнец: access_token — непустая строка', () => pm.expect(j.access_token, 'access_token').to.be.a('string').and.not.empty);",
                  "pm.test('14/близнец: refresh_token — непустая строка', () => pm.expect(j.refresh_token, 'refresh_token').to.be.a('string').and.not.empty);",
                  "pm.test('14/близнец: expires_in — положительное число секунд', () => pm.expect(j.expires_in, 'expires_in').to.be.a('number').and.above(0));",
                  "pm.test('14/близнец: выданная область — openid', () => pm.expect(j.scope, 'scope').to.eql('openid'));",
                  "if (j.access_token) { pm.environment.set('_acCeAccessToken2', j.access_token); }",
                  "if (j.refresh_token) { pm.environment.set('_acCeRefreshToken2', j.refresh_token); }"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED :: wait-for-code-expiry",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/oauth-authorization-server",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "oauth-authorization-server"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/oauth-authorization-server');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (pm.environment.get('_acCeClockStep') !== pm.info.requestName) {",
                  "  pm.environment.set('_acCeClockTurns', '0'); pm.environment.set('_acCeClockStep', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _acTurn = parseInt(pm.environment.get('_acCeClockTurns') || '0', 10);",
                  "const _acSince = Number(pm.environment.get('_acCeIssuedAt1') || '0');",
                  "const _acLeft = _acSince > 0 ? 63000 - (Date.now() - _acSince) : 0;",
                  "if (_acLeft > 0 && _acTurn < 30) {",
                  "  pm.environment.set('_acCeClockTurns', String(_acTurn + 1));",
                  "  const _acTick = Date.now(); while (Date.now() - _acTick < Math.min(_acLeft, 5000)) { /* срок кода, не готовность */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_acCeClockTurns'); pm.environment.unset('_acCeClockStep');",
                  "pm.test('14/срок: момент выдачи кода записан шагом выдачи', () => pm.expect(_acSince, 'момент выдачи').to.be.above(0));",
                  "pm.test('14/срок: срок кода выдержан по часам прогона', () => pm.expect(_acLeft <= 0, 'осталось ' + _acLeft + ' мс после ' + _acTurn + ' оборотов').to.eql(true));",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-AUTHCODE-NEG-CODE-EXPIRED :: exchange-after-ttl",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "grant_type=authorization_code&code={{_acFCode}}&redirect_uri={{_acFRedirect}}&code_verifier={{_acFVerifier}}"
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false,
            "disableCookies": true
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "if (!pm.environment.get('oauthClientId') || !pm.environment.get('oauthClientSecret')) {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] посев стенда: oauthClientId, oauthClientSecret заданы', () => {",
                  "    pm.expect.fail('посев стенда посадки own не завёл конфиденциального интерактивного клиента церемонии (приёмка LINE-A-1 §1: клиент — Given посевом). Шаг не может исполниться, и молча выпасть ему нельзя.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!pm.environment.get('_acCeCode1')) {",
                  "  pm.test('Ce: код не захвачен точкой авторизации', () => {",
                  "    pm.expect.fail('шаг выдачи кода выше не выдал code — обменивать нечего; причина — в шаге выдачи, не здесь');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.variables.set('_acFCode', encodeURIComponent(pm.environment.get('_acCeCode1') || ''));",
                  "pm.variables.set('_acFRedirect', encodeURIComponent(pm.environment.get('_acCeRedirect1') || ''));",
                  "pm.variables.set('_acFVerifier', encodeURIComponent(pm.environment.get('_acCeVerifier1') || ''));",
                  "pm.variables.set('_acFClientId', encodeURIComponent(pm.environment.get('oauthClientId') || ''));",
                  "// HARNESS-CONFIG GUARD — iamRegistryTokenBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamRegistryTokenBaseUrl') || pm.variables.get('iamRegistryTokenBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/token');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: iamRegistryTokenBaseUrl is set — поверхность выдачи службы: точка авторизации и токен-эндпоинт церемонии стоят на ней и больше нигде', () => {",
                  "    pm.expect.fail('iamRegistryTokenBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'Authorization', value: 'Basic ' + CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(",
                  "  encodeURIComponent(pm.environment.get('oauthClientId') || '') + ':' + encodeURIComponent(pm.environment.get('oauthClientSecret') || '')))});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "pm.test('14: error — invalid_grant', () => pm.expect(j.error, JSON.stringify(j)).to.eql('invalid_grant'));",
                  "pm.test('14: токен не выдан', () => pm.expect([j.access_token, j.refresh_token], 'тело несёт токен').to.eql([undefined, undefined]));"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}
