{
  "info": {
    "_postman_id": "9a69b902-2bdb-5a6e-8a7b-01fdd1b021ed",
    "name": "kacho-iam / newman / iam-token-facade-conformance",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  // runId формат: только [a-z0-9], без точки, начинается с буквы — чтобы проходить compute name regex",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', ('r' + t + r).replace(/[^a-z0-9]/g, '').slice(0, 11));",
          "}",
          "pm.environment.set('_suiteProjectId', pm.environment.get('existingProjectId'));",
          "pm.environment.set('_suiteFolderCrossId', pm.environment.get('existingProjectCrossId'));"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "try {",
          "  const _ct = (pm.response && pm.response.headers && pm.response.headers.get('Content-Type')) || '';",
          "  if (_ct.indexOf('json') !== -1) {",
          "    const _j = pm.response.json();",
          "    if (_j && _j.done === true && typeof _j.id === 'string' && _j.id !== '') {",
          "      if (_j.error) {",
          "        const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
          "        _pv.forEach(function (k) { pm.environment.unset(k); });",
          "      }",
          "      pm.environment.unset('_provisionalIds');",
          "    }",
          "  }",
          "} catch (e) { /* не JSON, не Operation — регистрация остаётся как есть */ }"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "IBT-04-FACADE-VERIFIES-THE-BEARER-THE-EDGE-ACCEPTS — The facade publishes — in exactly one of its key-set records — the kid that signs the accepted Bearer, under the algorithm its header names; edge answers 200 (not 401, not 403)",
      "description": "class:SEC | class:CONF | priority:P0",
      "item": [
        {
          "name": "IBT-04-FACADE-VERIFIES-THE-BEARER-THE-EDGE-ACCEPTS :: facade-jwks",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "jwks.json"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — verification lane — the key material the edge verifies with is served by iam', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"facade-jwks: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _record = \"mirror\";",
                  "const _jwks = pm.response.json();",
                  "pm.test('facade JWKS [' + _record + ']: keys is a non-empty array', () => {",
                  "  pm.expect(_jwks.keys, JSON.stringify(_jwks)).to.be.an('array');",
                  "  pm.expect(_jwks.keys.length, 'a facade record serving zero keys verifies nothing')",
                  "    .to.be.greaterThan(0);",
                  "});",
                  "const _KTY_ALG = {RSA: 'RS256', EC: 'ES256', OKP: 'EdDSA'};",
                  "const _KTY_MEMBERS = {RSA: ['n', 'e'], EC: ['crv', 'x', 'y'], OKP: ['crv', 'x']};",
                  "pm.test('facade JWKS [' + _record + ']: every key is ASYMMETRIC verification material — kty and alg agree, and the public half is complete', () => {",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    pm.expect(Object.keys(_KTY_ALG), 'kty ' + k.kty + ' of ' + k.kid +",
                  "      ' — a facade publishing a symmetric key is publishing a SIGNING SECRET')",
                  "      .to.include(k.kty);",
                  "    pm.expect(k.alg, 'alg of ' + k.kid + ' against its key type ' + k.kty +",
                  "      ' — a key whose header algorithm its material cannot support is the alg-confusion shape seen from the publishing side').to.eql(_KTY_ALG[k.kty]);",
                  "    pm.expect(k.kid, JSON.stringify(k)).to.be.a('string').with.length.greaterThan(0);",
                  "    (_KTY_MEMBERS[k.kty] || []).forEach(m => pm.expect(k[m], 'member ' + m + ' of ' + k.kid +",
                  "      ' — an incomplete public half verifies nothing').to.be.a('string').with.length.greaterThan(0));",
                  "  });",
                  "});",
                  "pm.test('facade JWKS [' + _record + ']: carries PUBLIC material only (no d/p/q/dp/dq/qi)', () => {",
                  "  const priv = ['d', 'p', 'q', 'dp', 'dq', 'qi'];",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    priv.forEach(m => pm.expect(k[m], 'private JWK member ' + m +",
                  "      ' on the facade: what is published is what VERIFIES, never what signs').to.be.undefined);",
                  "  });",
                  "});",
                  "pm.environment.set(\"_facadeKids\", JSON.stringify((_jwks.keys || []).map(k => k.kid)));",
                  "pm.environment.set(\"_facadeByKid\", JSON.stringify((_jwks.keys || []).reduce((a, k) => {",
                  "  a[k.kid] = {kty: k.kty, alg: k.alg, n: k.n, e: k.e, crv: k.crv, x: k.x, y: k.y};",
                  "  return a;",
                  "}, {})));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-04-FACADE-VERIFIES-THE-BEARER-THE-EDGE-ACCEPTS :: facade-own-key-set",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/kacho/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "kacho",
                "jwks.json"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/kacho/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — verification lane — the OWN record of the facade; the platform signs with its own keyring and publishes the verifying half here', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"facade-own-key-set: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _record = \"own\";",
                  "const _jwks = pm.response.json();",
                  "pm.test('facade JWKS [' + _record + ']: keys is a non-empty array', () => {",
                  "  pm.expect(_jwks.keys, JSON.stringify(_jwks)).to.be.an('array');",
                  "  pm.expect(_jwks.keys.length, 'a facade record serving zero keys verifies nothing')",
                  "    .to.be.greaterThan(0);",
                  "});",
                  "const _KTY_ALG = {RSA: 'RS256', EC: 'ES256', OKP: 'EdDSA'};",
                  "const _KTY_MEMBERS = {RSA: ['n', 'e'], EC: ['crv', 'x', 'y'], OKP: ['crv', 'x']};",
                  "pm.test('facade JWKS [' + _record + ']: every key is ASYMMETRIC verification material — kty and alg agree, and the public half is complete', () => {",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    pm.expect(Object.keys(_KTY_ALG), 'kty ' + k.kty + ' of ' + k.kid +",
                  "      ' — a facade publishing a symmetric key is publishing a SIGNING SECRET')",
                  "      .to.include(k.kty);",
                  "    pm.expect(k.alg, 'alg of ' + k.kid + ' against its key type ' + k.kty +",
                  "      ' — a key whose header algorithm its material cannot support is the alg-confusion shape seen from the publishing side').to.eql(_KTY_ALG[k.kty]);",
                  "    pm.expect(k.kid, JSON.stringify(k)).to.be.a('string').with.length.greaterThan(0);",
                  "    (_KTY_MEMBERS[k.kty] || []).forEach(m => pm.expect(k[m], 'member ' + m + ' of ' + k.kid +",
                  "      ' — an incomplete public half verifies nothing').to.be.a('string').with.length.greaterThan(0));",
                  "  });",
                  "});",
                  "pm.test('facade JWKS [' + _record + ']: carries PUBLIC material only (no d/p/q/dp/dq/qi)', () => {",
                  "  const priv = ['d', 'p', 'q', 'dp', 'dq', 'qi'];",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    priv.forEach(m => pm.expect(k[m], 'private JWK member ' + m +",
                  "      ' on the facade: what is published is what VERIFIES, never what signs').to.be.undefined);",
                  "  });",
                  "});",
                  "pm.environment.set(\"_facadeOwnKids\", JSON.stringify((_jwks.keys || []).map(k => k.kid)));",
                  "pm.environment.set(\"_facadeOwnByKid\", JSON.stringify((_jwks.keys || []).reduce((a, k) => {",
                  "  a[k.kid] = {kty: k.kty, alg: k.alg, n: k.n, e: k.e, crv: k.crv, x: k.x, y: k.y};",
                  "  return a;",
                  "}, {})));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-04-FACADE-VERIFIES-THE-BEARER-THE-EDGE-ACCEPTS :: bearer-accepted-at-edge",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"edge acceptance: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "pm.test('edge accepted the presented Bearer: HTTP 200', () => pm.expect(pm.response.code,",
                  "  '401 here means the facade-signed token failed verification; 403 on an <exempt> RPC'",
                  "  + ' means the principal did not resolve; any other code means the edge never reached'",
                  "  + ' this lane. Body: ' + pm.response.text()).to.eql(200));",
                  "const _sent = _sentBearer();",
                  "pm.test('a Bearer was actually presented (an unauthenticated 200 would prove nothing)',",
                  "  () => pm.expect(_sent, 'Authorization header').to.be.a('string').with.length.greaterThan(0));",
                  "const _hdr = JSON.parse(_b64urlToText(_sent.split('.')[0]));",
                  "pm.test('presented Bearer is signed with an ASYMMETRIC algorithm (never HS*, never none)',",
                  "  () => pm.expect(['RS256', 'ES256', 'EdDSA'], JSON.stringify(_hdr) +",
                  "    ' — a symmetric or unsigned header means the presenter could have made this'",
                  "    + ' credential itself, and \"the edge answered 200\" would say nothing about the facade')",
                  "    .to.include(_hdr.alg));",
                  "pm.test('presented Bearer names a kid', () => {",
                  "  pm.expect(_hdr.kid, JSON.stringify(_hdr)).to.be.a('string').with.length.greaterThan(0);",
                  "});",
                  "const _mirrorByKid = JSON.parse(pm.environment.get('_facadeByKid') || '{}');",
                  "const _ownByKid = JSON.parse(pm.environment.get('_facadeOwnByKid') || '{}');",
                  "pm.test('BOTH facade records were captured (an empty keyset is satisfied by nothing)', () => {",
                  "  pm.expect(Object.keys(_mirrorByKid).length, 'mirror record, captured by the jwks step')",
                  "    .to.be.greaterThan(0);",
                  "  pm.expect(Object.keys(_ownByKid).length, 'own record, captured by the jwks step')",
                  "    .to.be.greaterThan(0);",
                  "});",
                  "pm.test('the kid that signed the accepted Bearer is SERVED BY THE FACADE — by EXACTLY ONE of its records', () => {",
                  "  const _serving = [];",
                  "  if (_mirrorByKid[_hdr.kid]) { _serving.push('mirror'); }",
                  "  if (_ownByKid[_hdr.kid]) { _serving.push('own'); }",
                  "  pm.expect(_serving, 'kid ' + _hdr.kid + ' — served by [' + _serving.join(',') + '];'",
                  "    + ' none means the edge verifies against key material this facade does not publish,'",
                  "    + ' both means the key of one issuer would verify the token of another'",
                  "    + ' (records read: mirror=' + Object.keys(_mirrorByKid).length + ', own=' + Object.keys(_ownByKid).length + ')').to.have.lengthOf(1);",
                  "});",
                  "pm.test('the publishing record declares the SAME algorithm the Bearer header names', () => {",
                  "  const _k = _mirrorByKid[_hdr.kid] || _ownByKid[_hdr.kid];",
                  "  pm.expect(_k, 'no facade key published for kid ' + _hdr.kid).to.be.an('object');",
                  "  pm.expect(_k.alg, 'header alg ' + _hdr.alg + ' against the alg the facade publishes for '",
                  "    + _hdr.kid + ' — a header naming an algorithm the key material does not support is'",
                  "    + ' the alg-confusion shape').to.eql(_hdr.alg);",
                  "});",
                  "const _pl = JSON.parse(_b64urlToText(_sent.split('.')[1]));",
                  "pm.test('presented Bearer carries an issuer and an audience', () => {",
                  "  pm.expect(_pl.iss, JSON.stringify(_pl)).to.be.a('string').with.length.greaterThan(0);",
                  "  const aud = [].concat(_pl.aud || []);",
                  "  pm.expect(aud.length, 'aud claim: an audience-less token is not edge-addressed')",
                  "    .to.be.greaterThan(0);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IBT-12-FACADE-JWKS-MIRRORS-THE-PROVIDER — iam's JWKS-proxy is byte-faithful to the provider's public JWKS (same kids, same moduli) — iam proxies, never mints",
      "description": "class:SEC | class:CONF | priority:P0",
      "item": [
        {
          "name": "IBT-12-FACADE-JWKS-MIRRORS-THE-PROVIDER :: facade-jwks-for-mirror",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "jwks.json"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — mirror comparison — the facade side of the pair', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"facade-jwks-for-mirror: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _record = \"mirror\";",
                  "const _jwks = pm.response.json();",
                  "pm.test('facade JWKS [' + _record + ']: keys is a non-empty array', () => {",
                  "  pm.expect(_jwks.keys, JSON.stringify(_jwks)).to.be.an('array');",
                  "  pm.expect(_jwks.keys.length, 'a facade record serving zero keys verifies nothing')",
                  "    .to.be.greaterThan(0);",
                  "});",
                  "const _KTY_ALG = {RSA: 'RS256', EC: 'ES256', OKP: 'EdDSA'};",
                  "const _KTY_MEMBERS = {RSA: ['n', 'e'], EC: ['crv', 'x', 'y'], OKP: ['crv', 'x']};",
                  "pm.test('facade JWKS [' + _record + ']: every key is ASYMMETRIC verification material — kty and alg agree, and the public half is complete', () => {",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    pm.expect(Object.keys(_KTY_ALG), 'kty ' + k.kty + ' of ' + k.kid +",
                  "      ' — a facade publishing a symmetric key is publishing a SIGNING SECRET')",
                  "      .to.include(k.kty);",
                  "    pm.expect(k.alg, 'alg of ' + k.kid + ' against its key type ' + k.kty +",
                  "      ' — a key whose header algorithm its material cannot support is the alg-confusion shape seen from the publishing side').to.eql(_KTY_ALG[k.kty]);",
                  "    pm.expect(k.kid, JSON.stringify(k)).to.be.a('string').with.length.greaterThan(0);",
                  "    (_KTY_MEMBERS[k.kty] || []).forEach(m => pm.expect(k[m], 'member ' + m + ' of ' + k.kid +",
                  "      ' — an incomplete public half verifies nothing').to.be.a('string').with.length.greaterThan(0));",
                  "  });",
                  "});",
                  "pm.test('facade JWKS [' + _record + ']: carries PUBLIC material only (no d/p/q/dp/dq/qi)', () => {",
                  "  const priv = ['d', 'p', 'q', 'dp', 'dq', 'qi'];",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    priv.forEach(m => pm.expect(k[m], 'private JWK member ' + m +",
                  "      ' on the facade: what is published is what VERIFIES, never what signs').to.be.undefined);",
                  "  });",
                  "});",
                  "pm.environment.set(\"_facadeKids\", JSON.stringify((_jwks.keys || []).map(k => k.kid)));",
                  "pm.environment.set(\"_facadeByKid\", JSON.stringify((_jwks.keys || []).reduce((a, k) => {",
                  "  a[k.kid] = {kty: k.kty, alg: k.alg, n: k.n, e: k.e, crv: k.crv, x: k.x, y: k.y};",
                  "  return a;",
                  "}, {})));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-12-FACADE-JWKS-MIRRORS-THE-PROVIDER :: provider-jwks",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "jwks.json"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — providerPublicBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('providerPublicBaseUrl') || pm.variables.get('providerPublicBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: providerPublicBaseUrl is set — mirror comparison — the provider side of the pair, read by the TEST as an oracle (this is the measurement, not a client path)', () => {",
                  "    pm.expect.fail('providerPublicBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"provider JWKS: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const _up = pm.response.json();",
                  "const _mirror = JSON.parse(pm.environment.get('_facadeByKid') || '{}');",
                  "pm.test('both keysets are non-empty (an equality satisfied by nothing is not an equality)', () => {",
                  "  pm.expect((_up.keys || []).length, 'provider keys').to.be.greaterThan(0);",
                  "  pm.expect(Object.keys(_mirror).length, 'facade keys').to.be.greaterThan(0);",
                  "});",
                  "const _upBy = (_up.keys || []).reduce((a, k) => { a[k.kid] = k; return a; }, {});",
                  "pm.test('kid sets are identical in BOTH directions', () => {",
                  "  const up = Object.keys(_upBy).sort();",
                  "  const mi = Object.keys(_mirror).sort();",
                  "  pm.expect(mi, 'facade kids vs provider kids: a kid the facade serves and the '",
                  "    + 'provider does not is a key iam minted itself').to.eql(up);",
                  "});",
                  "pm.test('every mirrored key is the provider key value-for-value (kty/alg/n/e)', () => {",
                  "  Object.keys(_mirror).forEach(kid => {",
                  "    const u = _upBy[kid];",
                  "    pm.expect(u, 'provider has no key ' + kid).to.be.an('object');",
                  "    pm.expect(_mirror[kid].kty, 'kty of ' + kid).to.eql(u.kty);",
                  "    pm.expect(_mirror[kid].alg, 'alg of ' + kid).to.eql(u.alg);",
                  "    pm.expect(_mirror[kid].n, 'modulus of ' + kid + ' differs — the facade is not '",
                  "      + 'mirroring this key, it is publishing another one').to.eql(u.n);",
                  "    pm.expect(_mirror[kid].e, 'exponent of ' + kid).to.eql(u.e);",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-12-FACADE-JWKS-MIRRORS-THE-PROVIDER :: facade-jwks-listener-is-a-narrow-proxy-discovery",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/openid-configuration",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "openid-configuration"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/openid-configuration');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — mirror lane — the facade listener must NOT be a general provider passthrough, and this is what tells the facade apart from the provider', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"facade listener, provider discovery document: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('the facade JWKS listener does NOT serve the provider discovery document (if it did, this variable points at the provider and the comparison above compared the provider with itself)',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.eql(404));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-12-FACADE-JWKS-MIRRORS-THE-PROVIDER :: facade-jwks-listener-is-a-narrow-proxy-token-endpoint",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/oauth2/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "oauth2",
                "token"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/oauth2/token');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — mirror lane — the facade listener must not expose the provider token endpoint', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"facade listener, provider token endpoint: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('the facade JWKS listener does NOT expose the provider token endpoint',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.eql(404));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS — SAKeyService.Issue/Revoke and UserTokenService.Issue/Revoke serve the acr-exempt service principal with 200 + credential material (no step-up challenge)",
      "description": "class:SEC | class:CONF | class:CRUD | priority:P0",
      "item": [
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: create-conformance-sa",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/serviceAccounts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "serviceAccounts"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"accountId\": \"{{ibtAccountId}}\", \"name\": \"ibt05-{{runId}}\", \"description\": \"IBT-05 facade-conformance fixture\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "function _claimForm(pl) {",
                  "  if (!pl || typeof pl !== 'object') { return 'none'; }",
                  "  if (pl.kacho_principal_id) { return 'top-level'; }",
                  "  if (pl.ext && pl.ext.ext_claims && pl.ext.ext_claims.kacho_principal_id) { return 'ext.ext_claims'; }",
                  "  if (pl.ext_claims && pl.ext_claims.kacho_principal_id) { return 'ext_claims'; }",
                  "  return 'none';",
                  "}",
                  "function _claim(pl, k) {",
                  "  if (pl && pl[k] !== undefined && pl[k] !== null && pl[k] !== '') { return pl[k]; }",
                  "  const _nested = (pl && pl.ext && pl.ext.ext_claims) || (pl && pl.ext_claims) || {};",
                  "  return _nested[k];",
                  "}",
                  "const _b = (pm.environment.get('jwtBootstrap') || '').split('.');",
                  "if (_b.length === 3) {",
                  "  try {",
                  "    var _t = _b[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "    while (_t.length % 4 !== 0) { _t += '='; }",
                  "    const _c = JSON.parse(CryptoJS.enc.Base64.parse(_t).toString(CryptoJS.enc.Utf8));",
                  "    pm.environment.set('ibtCallerClaimForm', _claimForm(_c));",
                  "    const _acct = _claim(_c, 'kacho_account_id');",
                  "    const _pid = _claim(_c, 'kacho_principal_id');",
                  "    const _ptype = _claim(_c, 'kacho_principal_type');",
                  "    if (_acct) pm.environment.set('ibtAccountId', _acct);",
                  "    if (_pid) pm.environment.set('ibtCallerPrincipalId', _pid);",
                  "    if (_ptype) pm.environment.set('ibtCallerPrincipalType', _ptype);",
                  "  } catch (e) { /* asserted in the test script, not swallowed */ }",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"create SA fixture: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('the caller Bearer carried the composed claims this suite reads', () => {",
                  "  const _form = pm.environment.get('ibtCallerClaimForm') || 'none';",
                  "  pm.expect(_form, 'no kacho_* claims in ANY of the three declared forms (top-level / ext.ext_claims / ext_claims)').to.not.eql('none');",
                  "  pm.expect(pm.environment.get('ibtAccountId'), 'kacho_account_id claim (form: ' + _form + ')')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "  pm.expect(pm.environment.get('ibtCallerPrincipalId'),",
                  "    'kacho_principal_id claim (form: ' + _form + ') — read by IBT-06')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "  pm.expect(pm.environment.get('ibtCallerPrincipalType'),",
                  "    'kacho_principal_type claim (form: ' + _form + ')')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().metadata.serviceAccountId);",
                  "  if (v !== undefined && v !== null) pm.environment.set('ibtSvaId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
                  "  if (_pv.indexOf('ibtSvaId') === -1) _pv.push('ibtSvaId');",
                  "  pm.environment.set('_provisionalIds', JSON.stringify(_pv));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: poll-op",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION GUARD — 'opId' is captured by the mutation this poll follows.",
                  "// Empty = that mutation returned no Operation. Report it (a skipped request",
                  "// leaves no trace at all) and skip, rather than polling a literal template.",
                  "if (!pm.environment.get('opId')) {",
                  "  pm.test('operation id opId was captured (the mutation returned an Operation)', () => {",
                  "    pm.expect.fail('opId is empty — the mutation this poll belongs to did not return an Operation (it was rejected, or its capture failed). Polling would hit an unresolved template; a previous case\\'s operation is NOT a substitute.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// poll-counter reset on first entry (request-name-scoped flag);",
                  "// re-invocations via setNextRequest skip the reset.",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 50) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД, А НЕ ТОЛЬКО ЗАВЕРШЕНИЕ: операция несёт предвыделенный идентификатор",
                  "// ресурса в metadata и тогда, когда завершилась ошибкой, — done у неё такой же",
                  "// true. Опубликованный без этой проверки идентификатор уезжает дальше",
                  "// координатой ресурса, которого нет, и падает уже не тот шаг, который ошибся.",
                  "(function () {",
                  "  var _po; try { _po = pm.response.json(); } catch (e) { return; }",
                  "  if (_po.error) {",
                  "    pm.environment.unset('ibtSvaId');",
                  "  }",
                  "  pm.test('operation succeeded (no phantom ibtSvaId)', function () {",
                  "    pm.expect(_po.error && JSON.stringify(_po.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: issue-sa-key",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/serviceAccounts/{{ibtSvaId}}/keys",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "serviceAccounts",
                "{{ibtSvaId}}",
                "keys"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"serviceAccountId\": \"{{ibtSvaId}}\", \"description\": \"IBT-05 facade-conformance {{runId}}\", \"audience\": [\"https://api.kacho.cloud\"]}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"SAKeyService.Issue: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('acr-gated Issue does NOT step-up-challenge the service principal (401 would break every machine seed)',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.not.eql(401));",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().metadata.keyId);",
                  "  if (v !== undefined && v !== null) pm.environment.set('ibtSaKeyId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
                  "  if (_pv.indexOf('ibtSaKeyId') === -1) _pv.push('ibtSaKeyId');",
                  "  pm.environment.set('_provisionalIds', JSON.stringify(_pv));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: poll-issue-sa-key",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('poll status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 50) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('Issue operation reached done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "pm.test('Issue operation SUCCEEDED (outcome asserted before any id is used)',",
                  "  () => pm.expect(j.error && JSON.stringify(j.error), 'operation.error').to.eql(undefined));",
                  "const r = (j.response || {});",
                  "pm.test('facade returned the client identity for the issued key',",
                  "  () => pm.expect(r.clientId, JSON.stringify(r)).to.be.a('string').with.length.greaterThan(0));",
                  "pm.test('facade returned the private key ONCE, in PEM', () => {",
                  "  pm.expect(r.privateKeyPem, 'privateKeyPem').to.be.a('string');",
                  "  pm.expect((r.privateKeyPem || '').indexOf('-----BEGIN'), 'PEM preamble').to.eql(0);",
                  "});",
                  "pm.test('issued key is ES256 and names the kid the assertion must carry', () => {",
                  "  pm.expect(r.algorithm, JSON.stringify(r)).to.eql('ES256');",
                  "  pm.expect(r.keyId, 'keyId').to.be.a('string').with.length.greaterThan(0);",
                  "});",
                  "pm.test('the issued credential is bound to the requested api audience', () => {",
                  "  pm.expect([].concat(r.audiences || []), JSON.stringify(r))",
                  "    .to.include('https://api.kacho.cloud');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: revoke-sa-key",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/serviceAccounts/{{ibtSvaId}}/keys/{{ibtSaKeyId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "serviceAccounts",
                "{{ibtSvaId}}",
                "keys",
                "{{ibtSaKeyId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"SAKeyService.Revoke: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('acr-gated Revoke does NOT step-up-challenge the service principal',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.not.eql(401));",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: poll-op #2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION GUARD — 'opId' is captured by the mutation this poll follows.",
                  "// Empty = that mutation returned no Operation. Report it (a skipped request",
                  "// leaves no trace at all) and skip, rather than polling a literal template.",
                  "if (!pm.environment.get('opId')) {",
                  "  pm.test('operation id opId was captured (the mutation returned an Operation)', () => {",
                  "    pm.expect.fail('opId is empty — the mutation this poll belongs to did not return an Operation (it was rejected, or its capture failed). Polling would hit an unresolved template; a previous case\\'s operation is NOT a substitute.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// poll-counter reset on first entry (request-name-scoped flag);",
                  "// re-invocations via setNextRequest skip the reset.",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 50) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: issue-user-token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/users/{{userAAAId}}/tokens",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "users",
                "{{userAAAId}}",
                "tokens"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"userId\": \"{{userAAAId}}\", \"description\": \"IBT-05 facade-conformance {{runId}}\", \"createdByUserId\": \"{{userAAAId}}\", \"name\": \"ibt05-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"UserTokenService.Issue: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('acr-gated user-token Issue does NOT step-up-challenge the service principal',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.not.eql(401));",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().metadata.keyId);",
                  "  if (v !== undefined && v !== null) pm.environment.set('ibtUserTokenId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
                  "  if (_pv.indexOf('ibtUserTokenId') === -1) _pv.push('ibtUserTokenId');",
                  "  pm.environment.set('_provisionalIds', JSON.stringify(_pv));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: poll-issue-user-token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('poll status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 50) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('user-token Issue operation reached done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "pm.test('user-token Issue SUCCEEDED (outcome asserted before any id is used)',",
                  "  () => pm.expect(j.error && JSON.stringify(j.error), 'operation.error').to.eql(undefined));",
                  "const r = (j.response || {});",
                  "pm.test('facade returned the user credential material (clientId + PEM + ES256 kid)', () => {",
                  "  pm.expect(r.clientId, JSON.stringify(r)).to.be.a('string').with.length.greaterThan(0);",
                  "  pm.expect((r.privateKeyPem || '').indexOf('-----BEGIN'), 'PEM preamble').to.eql(0);",
                  "  pm.expect(r.algorithm, JSON.stringify(r)).to.eql('ES256');",
                  "  pm.expect(r.keyId, 'keyId').to.be.a('string').with.length.greaterThan(0);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: revoke-user-token",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/users/{{userAAAId}}/tokens/{{ibtUserTokenId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "users",
                "{{userAAAId}}",
                "tokens",
                "{{ibtUserTokenId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"UserTokenService.Revoke: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: poll-op #3",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION GUARD — 'opId' is captured by the mutation this poll follows.",
                  "// Empty = that mutation returned no Operation. Report it (a skipped request",
                  "// leaves no trace at all) and skip, rather than polling a literal template.",
                  "if (!pm.environment.get('opId')) {",
                  "  pm.test('operation id opId was captured (the mutation returned an Operation)', () => {",
                  "    pm.expect.fail('opId is empty — the mutation this poll belongs to did not return an Operation (it was rejected, or its capture failed). Polling would hit an unresolved template; a previous case\\'s operation is NOT a substitute.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// poll-counter reset on first entry (request-name-scoped flag);",
                  "// re-invocations via setNextRequest skip the reset.",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 50) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: delete-conformance-sa",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/serviceAccounts/{{ibtSvaId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "serviceAccounts",
                "{{ibtSvaId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (opgate removed -> eventual-consistency); retries SELF only on 403/404.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// Ждать можно ПРАВА на существующее имя, а не появление самого имени: адрес,",
                  "// собранный из незахваченной переменной, окно видимости не наполнит никогда.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) {",
                  "  if (v === '') { return true; }",
                  "  var _d = v; try { _d = decodeURIComponent(v); } catch (e) { _d = v; }",
                  "  return /\\{\\{[^}]*\\}\\}/.test(_d);",
                  "});",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 15) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 400) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test(\"teardown: delete the conformance SA: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.environment.unset('opId');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (pm.response.json().id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-05-CREDENTIAL-LIFECYCLE-THROUGH-FACADE-RPCS :: poll-op #4",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION GUARD — 'opId' is captured by the mutation this poll follows.",
                  "// Empty = that mutation returned no Operation. Report it (a skipped request",
                  "// leaves no trace at all) and skip, rather than polling a literal template.",
                  "if (!pm.environment.get('opId')) {",
                  "  pm.test('operation id opId was captured (the mutation returned an Operation)', () => {",
                  "    pm.expect.fail('opId is empty — the mutation this poll belongs to did not return an Operation (it was rejected, or its capture failed). Polling would hit an unresolved template; a previous case\\'s operation is NOT a substitute.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// poll-counter reset on first entry (request-name-scoped flag);",
                  "// re-invocations via setNextRequest skip the reset.",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 50) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IBT-13-PRINCIPAL-CLAIMS-STAMPED-BY-THE-FACADE-HOOK — The machine Bearer carries the facade-composed kacho_* claims — in either lane's form — and they resolve to exactly the subject the platform reports for the caller",
      "description": "class:SEC | class:CONF | priority:P0",
      "item": [
        {
          "name": "IBT-13-PRINCIPAL-CLAIMS-STAMPED-BY-THE-FACADE-HOOK :: whoami-with-facade-token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"WhoAmI with the facade-issued token: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "function _claimForm(pl) {",
                  "  if (!pl || typeof pl !== 'object') { return 'none'; }",
                  "  if (pl.kacho_principal_id) { return 'top-level'; }",
                  "  if (pl.ext && pl.ext.ext_claims && pl.ext.ext_claims.kacho_principal_id) { return 'ext.ext_claims'; }",
                  "  if (pl.ext_claims && pl.ext_claims.kacho_principal_id) { return 'ext_claims'; }",
                  "  return 'none';",
                  "}",
                  "function _claim(pl, k) {",
                  "  if (pl && pl[k] !== undefined && pl[k] !== null && pl[k] !== '') { return pl[k]; }",
                  "  const _nested = (pl && pl.ext && pl.ext.ext_claims) || (pl && pl.ext_claims) || {};",
                  "  return _nested[k];",
                  "}",
                  "const _sent = _sentBearer();",
                  "const _pl = JSON.parse(_b64urlToText(_sent.split('.')[1]));",
                  "const _form = _claimForm(_pl);",
                  "pm.test('the presented token carries the facade-composed platform claims', () => {",
                  "  pm.expect(_form, 'no kacho_* claim in ANY of the three declared forms (top-level / ext.ext_claims / ext_claims) — this credential was signed without the'",
                  "    + ' facade composition, and on its own it names nobody on this platform')",
                  "    .to.not.eql('none');",
                  "  pm.expect(_claim(_pl, 'kacho_principal_type'), 'kacho_principal_type (form: ' + _form + ')')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "  pm.expect(_claim(_pl, 'kacho_principal_id'), 'kacho_principal_id (form: ' + _form + ')')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "});",
                  "pm.test('the composition carried values the SUBJECT cannot supply — this is what tells an enriched credential from a bare one', () => {",
                  "  pm.expect(_pl.sub, JSON.stringify(_pl)).to.be.a('string').with.length.greaterThan(0);",
                  "  const _keyId = _claim(_pl, 'kacho_sa_key_id');",
                  "  const _acct = _claim(_pl, 'kacho_account_id');",
                  "  const _pid = _claim(_pl, 'kacho_principal_id');",
                  "  pm.expect(_keyId, 'kacho_sa_key_id (form: ' + _form + ') — the credential-registry row this token was issued against; a bare client-credentials token has no such claim')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "  pm.expect(_acct, 'kacho_account_id (form: ' + _form + ') — the owning account; it is resolved by the composition, not carried by the exchange')",
                  "    .to.be.a('string').with.length.greaterThan(0);",
                  "  pm.expect(_keyId, 'kacho_sa_key_id equals kacho_principal_id — the composition restated the principal and added nothing, so this case could no longer tell an enriched credential from a bare one').to.not.eql(_pid);",
                  "  pm.expect(_keyId, 'kacho_sa_key_id equals sub — same reason, on the other side: the composition would be a restatement of the subject').to.not.eql(_pl.sub);",
                  "});",
                  "const _j = pm.response.json();",
                  "pm.test('the platform reports EXACTLY the principal the composition names', () => {",
                  "  const want = _claim(_pl, 'kacho_principal_type') + ':' + _claim(_pl, 'kacho_principal_id');",
                  "  pm.expect(_j.subject, JSON.stringify(_j) + ' vs claims ' + want).to.eql(want);",
                  "});",
                  "pm.test('the reported subject is a platform id, not an OAuth client id', () => {",
                  "  pm.expect(_j.subject, JSON.stringify(_j)).to.match(/^(user|service_account):(usr|sva)[a-z0-9-]+$/);",
                  "});",
                  "pm.test('createdAt truncated to seconds', () => {",
                  "  const ts = (pm.response.json().checkedAt);",
                  "  pm.expect(ts, 'createdAt present').to.be.a('string');",
                  "  // RFC3339; если есть дробная часть — это .000... либо отсутствует",
                  "  const m = ts.match(/\\.(\\d+)/);",
                  "  if (m) pm.expect(parseInt(m[1].padEnd(9,'0'), 10), 'sub-second part is zero').to.eql(0);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR — MintBootstrapToken is unreachable over REST on both api-gateway listeners, for a system-admin caller, indistinguishably from a nonsense path",
      "description": "class:SEC | class:NEG | class:CONF | priority:P0",
      "item": [
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: control-internal-listener-serves-internal-rpcs",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/internal/iam:lookupSubject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "iam:lookupSubject"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"id\": \"{{ibtCallerPrincipalId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/internal/iam:lookupSubject');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-06 positive control — the internal listener must be shown to serve Internal* routes, else the absence of the mint says nothing', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "function _claimForm(pl) {",
                  "  if (!pl || typeof pl !== 'object') { return 'none'; }",
                  "  if (pl.kacho_principal_id) { return 'top-level'; }",
                  "  if (pl.ext && pl.ext.ext_claims && pl.ext.ext_claims.kacho_principal_id) { return 'ext.ext_claims'; }",
                  "  if (pl.ext_claims && pl.ext_claims.kacho_principal_id) { return 'ext_claims'; }",
                  "  return 'none';",
                  "}",
                  "function _claim(pl, k) {",
                  "  if (pl && pl[k] !== undefined && pl[k] !== null && pl[k] !== '') { return pl[k]; }",
                  "  const _nested = (pl && pl.ext && pl.ext.ext_claims) || (pl && pl.ext_claims) || {};",
                  "  return _nested[k];",
                  "}",
                  "const _b = (pm.environment.get('jwtBootstrap') || '').split('.');",
                  "if (_b.length === 3) {",
                  "  try {",
                  "    var _t = _b[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "    while (_t.length % 4 !== 0) { _t += '='; }",
                  "    const _c = JSON.parse(CryptoJS.enc.Base64.parse(_t).toString(CryptoJS.enc.Utf8));",
                  "    pm.environment.set('ibtCallerClaimForm', _claimForm(_c));",
                  "    const _acct = _claim(_c, 'kacho_account_id');",
                  "    const _pid = _claim(_c, 'kacho_principal_id');",
                  "    const _ptype = _claim(_c, 'kacho_principal_type');",
                  "    if (_acct) pm.environment.set('ibtAccountId', _acct);",
                  "    if (_pid) pm.environment.set('ibtCallerPrincipalId', _pid);",
                  "    if (_ptype) pm.environment.set('ibtCallerPrincipalType', _ptype);",
                  "  } catch (e) { /* asserted in the test script, not swallowed */ }",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"internal listener control: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('the internal listener SERVES Internal* RPCs (200 for a subject that exists)',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "pm.test('and it answered about the caller principal, not something else', () => {",
                  "  const j = pm.response.json();",
                  "  const who = (j.serviceAccount && j.serviceAccount.id) || (j.user && j.user.id) || '';",
                  "  pm.expect(who, JSON.stringify(j)).to.eql(pm.environment.get('ibtCallerPrincipalId'));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: control-public-listener-404s-an-internal-route-it-lacks",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/internal/iam:lookupSubject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "iam:lookupSubject"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"id\": \"{{ibtCallerPrincipalId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"public listener isolation control: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('an Internal* route bound on the internal mux is a 404 mux-miss on the public one', () => pm.expect(pm.response.code, pm.response.text()).to.eql(404));",
                  "pm.test('and the 404 is the ROUTING miss, not a service-level not-found', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(5);",
                  "  pm.expect(j.message, JSON.stringify(j)).to.eql('Not Found');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: control-nonsense-path-public",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/ibt-nonsense-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "ibt-nonsense-{{runId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"public nonsense control: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test('nonsense control answered (its refusal shape is the yardstick for the mint probes)',",
                  "  () => pm.expect(pm.response.code).to.be.a('number'));",
                  "pm.environment.set('_ibtCtlPublic', _refusalShape(pm.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: control-nonsense-path-internal",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/ibt-nonsense-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "ibt-nonsense-{{runId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/ibt-nonsense-{{runId}}');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-06 control — how the internal listener answers a typo', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"internal nonsense control: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test('nonsense control answered on the internal listener',",
                  "  () => pm.expect(pm.response.code).to.be.a('number'));",
                  "pm.environment.set('_ibtCtlInternal', _refusalShape(pm.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: mint-unbound-form-on-public",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/kacho.cloud.iam.v1.InternalBootstrapTokenService/MintBootstrapToken",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "kacho.cloud.iam.v1.InternalBootstrapTokenService",
                "MintBootstrapToken"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"mint (grpc-gateway unbound form) on the public listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"mint (grpc-gateway unbound form) on the public listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"mint (grpc-gateway unbound form) on the public listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlPublic');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"mint (grpc-gateway unbound form) on the public listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: mint-unbound-form-on-internal",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/kacho.cloud.iam.v1.InternalBootstrapTokenService/MintBootstrapToken",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "kacho.cloud.iam.v1.InternalBootstrapTokenService",
                "MintBootstrapToken"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/kacho.cloud.iam.v1.InternalBootstrapTokenService/MintBootstrapToken');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-06 — the mint must have no door on the internal listener either', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"mint (grpc-gateway unbound form) on the internal listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"mint (grpc-gateway unbound form) on the internal listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"mint (grpc-gateway unbound form) on the internal listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlInternal');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"mint (grpc-gateway unbound form) on the internal listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: mint-acceptance-named-path-on-public",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/internal/bootstrapToken:mint",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "bootstrapToken:mint"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"mint (the path the acceptance spells) on the public listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"mint (the path the acceptance spells) on the public listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"mint (the path the acceptance spells) on the public listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlPublic');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"mint (the path the acceptance spells) on the public listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-06-BOOTSTRAP-MINT-HAS-NO-REST-DOOR :: mint-acceptance-named-path-on-internal",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/internal/bootstrapToken:mint",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "bootstrapToken:mint"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/internal/bootstrapToken:mint');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-06 — the acceptance-named path on the internal listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"mint (the path the acceptance spells) on the internal listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"mint (the path the acceptance spells) on the internal listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"mint (the path the acceptance spells) on the internal listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlInternal');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"mint (the path the acceptance spells) on the internal listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE — Provider admin-client registration, token endpoint and JWKS are not served by any api-gateway listener (the facade cannot be routed around)",
      "description": "class:SEC | class:NEG | class:CONF | priority:P0",
      "item": [
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: control-facade-serves-the-jwks-path",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "jwks.json"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — IBT-15 positive control — the JWKS path IS served, at the facade listener, so NOT-at-the-edge is isolation and not a misspelling', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"control-facade-serves-the-jwks-path: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _record = \"mirror\";",
                  "const _jwks = pm.response.json();",
                  "pm.test('facade JWKS [' + _record + ']: keys is a non-empty array', () => {",
                  "  pm.expect(_jwks.keys, JSON.stringify(_jwks)).to.be.an('array');",
                  "  pm.expect(_jwks.keys.length, 'a facade record serving zero keys verifies nothing')",
                  "    .to.be.greaterThan(0);",
                  "});",
                  "const _KTY_ALG = {RSA: 'RS256', EC: 'ES256', OKP: 'EdDSA'};",
                  "const _KTY_MEMBERS = {RSA: ['n', 'e'], EC: ['crv', 'x', 'y'], OKP: ['crv', 'x']};",
                  "pm.test('facade JWKS [' + _record + ']: every key is ASYMMETRIC verification material — kty and alg agree, and the public half is complete', () => {",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    pm.expect(Object.keys(_KTY_ALG), 'kty ' + k.kty + ' of ' + k.kid +",
                  "      ' — a facade publishing a symmetric key is publishing a SIGNING SECRET')",
                  "      .to.include(k.kty);",
                  "    pm.expect(k.alg, 'alg of ' + k.kid + ' against its key type ' + k.kty +",
                  "      ' — a key whose header algorithm its material cannot support is the alg-confusion shape seen from the publishing side').to.eql(_KTY_ALG[k.kty]);",
                  "    pm.expect(k.kid, JSON.stringify(k)).to.be.a('string').with.length.greaterThan(0);",
                  "    (_KTY_MEMBERS[k.kty] || []).forEach(m => pm.expect(k[m], 'member ' + m + ' of ' + k.kid +",
                  "      ' — an incomplete public half verifies nothing').to.be.a('string').with.length.greaterThan(0));",
                  "  });",
                  "});",
                  "pm.test('facade JWKS [' + _record + ']: carries PUBLIC material only (no d/p/q/dp/dq/qi)', () => {",
                  "  const priv = ['d', 'p', 'q', 'dp', 'dq', 'qi'];",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    priv.forEach(m => pm.expect(k[m], 'private JWK member ' + m +",
                  "      ' on the facade: what is published is what VERIFIES, never what signs').to.be.undefined);",
                  "  });",
                  "});",
                  "pm.environment.set(\"_facadeKids\", JSON.stringify((_jwks.keys || []).map(k => k.kid)));",
                  "pm.environment.set(\"_facadeByKid\", JSON.stringify((_jwks.keys || []).reduce((a, k) => {",
                  "  a[k.kid] = {kty: k.kty, alg: k.alg, n: k.n, e: k.e, crv: k.crv, x: k.x, y: k.y};",
                  "  return a;",
                  "}, {})));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: control-nonsense-path-public",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/ibt-nonsense2-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "ibt-nonsense2-{{runId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"public nonsense control: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test('nonsense control answered', () => pm.expect(pm.response.code).to.be.a('number'));",
                  "pm.environment.set('_ibtCtlPublic2', _refusalShape(pm.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: control-nonsense-path-internal",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/ibt-nonsense2-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "ibt-nonsense2-{{runId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/ibt-nonsense2-{{runId}}');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-15 control — how the internal listener answers a typo', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"internal nonsense control: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test('nonsense control answered', () => pm.expect(pm.response.code).to.be.a('number'));",
                  "pm.environment.set('_ibtCtlInternal2', _refusalShape(pm.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: admin-client-registration-on-public",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/clients",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "clients"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"/admin/clients on the public listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"/admin/clients on the public listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"/admin/clients on the public listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlPublic2');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"/admin/clients on the public listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: admin-client-registration-on-internal",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/clients",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "clients"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/admin/clients');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-15 — a provider surface must not be reachable on the internal listener either', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"/admin/clients on the internal listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"/admin/clients on the internal listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"/admin/clients on the internal listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlInternal2');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"/admin/clients on the internal listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: provider-token-endpoint-on-public",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/oauth2/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "oauth2",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"/oauth2/token on the public listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"/oauth2/token on the public listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"/oauth2/token on the public listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlPublic2');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"/oauth2/token on the public listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: provider-token-endpoint-on-internal",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/oauth2/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "oauth2",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/oauth2/token');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-15 — a provider surface must not be reachable on the internal listener either', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"/oauth2/token on the internal listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"/oauth2/token on the internal listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"/oauth2/token on the internal listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlInternal2');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"/oauth2/token on the internal listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: provider-jwks-at-the-edge-on-public",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "jwks.json"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"/.well-known/jwks.json on the public listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"/.well-known/jwks.json on the public listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"/.well-known/jwks.json on the public listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlPublic2');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"/.well-known/jwks.json on the public listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-15-PROVIDER-SURFACES-NOT-REACHABLE-THROUGH-THE-EDGE :: provider-jwks-at-the-edge-on-internal",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "jwks.json"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — IBT-15 — a provider surface must not be reachable on the internal listener either', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"/.well-known/jwks.json on the internal listener: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "function _refusalShape(resp) {",
                  "  var j = null; try { j = resp.json(); } catch (e) { return String(resp.code) + ' non-json'; }",
                  "  var info = ((j && j.details) || []).find(d => (d['@type'] || '').includes('ErrorInfo')) || {};",
                  "  var pf = ((j && j.details) || []).find(d => (d['@type'] || '').includes('PreconditionFailure')) || {};",
                  "  var v = ((pf.violations || [])[0] || {});",
                  "  return JSON.stringify({",
                  "    http: resp.code, code: j && j.code, reason: info.reason,",
                  "    violation: v.type, action: (info.metadata || {}).action",
                  "  });",
                  "}",
                  "pm.test(\"/.well-known/jwks.json on the internal listener: NEVER 2xx \\u2014 there is no door here\", () => {",
                  "  pm.expect(pm.response.code, pm.response.text()).to.not.be.within(200, 299);",
                  "});",
                  "pm.test(\"/.well-known/jwks.json on the internal listener: refused with the SAME SHAPE as a nonsense path on the same listener \\u2014 i.e. the address is not a routed endpoint at all\", () => {",
                  "  const ctl = pm.environment.get('_ibtCtlInternal2');",
                  "  pm.expect(ctl, 'control shape not captured — the control step did not run').to.be.a('string');",
                  "  pm.expect(_refusalShape(pm.response),",
                  "    'this address answers DIFFERENTLY from a typo on the same listener: something '",
                  "    + 'is routed here. Body: ' + pm.response.text()).to.eql(ctl);",
                  "});",
                  "pm.test(\"/.well-known/jwks.json on the internal listener: the refusal carries an EMPTY action \\u2014 no permission-catalog entry, so no such RPC is exposed\", () => {",
                  "  const j = pm.response.json();",
                  "  const info = (j.details || []).find(d => (d['@type'] || '').includes('ErrorInfo'));",
                  "  pm.expect(info, JSON.stringify(j)).to.be.an('object');",
                  "  pm.expect((info.metadata || {}).action, 'a NON-empty action means this path resolved to a catalogued RPC — it is exposed. ' + JSON.stringify(j)).to.eql('');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IBT-10-ONLY-FACADE-ISSUED-RS256-IS-ACCEPTED — Anonymous, alg=none and an HS256 alg-confusion forgery of the SAME payload keyed with the facade's own published material are all 401; the untouched facade Bearer is 200",
      "description": "class:SEC | class:NEG | class:CONF | priority:P0",
      "item": [
        {
          "name": "IBT-10-ONLY-FACADE-ISSUED-RS256-IS-ACCEPTED :: facade-jwks-for-forgery",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "jwks.json"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — IBT-10 — the public material used as the HMAC key of the alg-confusion forgery, mirror record', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"facade-jwks-for-forgery: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _record = \"mirror\";",
                  "const _jwks = pm.response.json();",
                  "pm.test('facade JWKS [' + _record + ']: keys is a non-empty array', () => {",
                  "  pm.expect(_jwks.keys, JSON.stringify(_jwks)).to.be.an('array');",
                  "  pm.expect(_jwks.keys.length, 'a facade record serving zero keys verifies nothing')",
                  "    .to.be.greaterThan(0);",
                  "});",
                  "const _KTY_ALG = {RSA: 'RS256', EC: 'ES256', OKP: 'EdDSA'};",
                  "const _KTY_MEMBERS = {RSA: ['n', 'e'], EC: ['crv', 'x', 'y'], OKP: ['crv', 'x']};",
                  "pm.test('facade JWKS [' + _record + ']: every key is ASYMMETRIC verification material — kty and alg agree, and the public half is complete', () => {",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    pm.expect(Object.keys(_KTY_ALG), 'kty ' + k.kty + ' of ' + k.kid +",
                  "      ' — a facade publishing a symmetric key is publishing a SIGNING SECRET')",
                  "      .to.include(k.kty);",
                  "    pm.expect(k.alg, 'alg of ' + k.kid + ' against its key type ' + k.kty +",
                  "      ' — a key whose header algorithm its material cannot support is the alg-confusion shape seen from the publishing side').to.eql(_KTY_ALG[k.kty]);",
                  "    pm.expect(k.kid, JSON.stringify(k)).to.be.a('string').with.length.greaterThan(0);",
                  "    (_KTY_MEMBERS[k.kty] || []).forEach(m => pm.expect(k[m], 'member ' + m + ' of ' + k.kid +",
                  "      ' — an incomplete public half verifies nothing').to.be.a('string').with.length.greaterThan(0));",
                  "  });",
                  "});",
                  "pm.test('facade JWKS [' + _record + ']: carries PUBLIC material only (no d/p/q/dp/dq/qi)', () => {",
                  "  const priv = ['d', 'p', 'q', 'dp', 'dq', 'qi'];",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    priv.forEach(m => pm.expect(k[m], 'private JWK member ' + m +",
                  "      ' on the facade: what is published is what VERIFIES, never what signs').to.be.undefined);",
                  "  });",
                  "});",
                  "pm.environment.set(\"_facadeKids\", JSON.stringify((_jwks.keys || []).map(k => k.kid)));",
                  "pm.environment.set(\"_facadeByKid\", JSON.stringify((_jwks.keys || []).reduce((a, k) => {",
                  "  a[k.kid] = {kty: k.kty, alg: k.alg, n: k.n, e: k.e, crv: k.crv, x: k.x, y: k.y};",
                  "  return a;",
                  "}, {})));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-10-ONLY-FACADE-ISSUED-RS256-IS-ACCEPTED :: facade-own-key-set-for-forgery",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/.well-known/kacho/jwks.json",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                ".well-known",
                "kacho",
                "jwks.json"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — iamJwksBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('iamJwksBaseUrl') || pm.variables.get('iamJwksBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/.well-known/kacho/jwks.json');",
                  "} else {",
                  "  pm.test('harness config: iamJwksBaseUrl is set — IBT-10 — the same, for the OWN record of the facade: the kid that signed the accepted Bearer may be published by either', () => {",
                  "    pm.expect.fail('iamJwksBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"facade-own-key-set-for-forgery: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _record = \"own\";",
                  "const _jwks = pm.response.json();",
                  "pm.test('facade JWKS [' + _record + ']: keys is a non-empty array', () => {",
                  "  pm.expect(_jwks.keys, JSON.stringify(_jwks)).to.be.an('array');",
                  "  pm.expect(_jwks.keys.length, 'a facade record serving zero keys verifies nothing')",
                  "    .to.be.greaterThan(0);",
                  "});",
                  "const _KTY_ALG = {RSA: 'RS256', EC: 'ES256', OKP: 'EdDSA'};",
                  "const _KTY_MEMBERS = {RSA: ['n', 'e'], EC: ['crv', 'x', 'y'], OKP: ['crv', 'x']};",
                  "pm.test('facade JWKS [' + _record + ']: every key is ASYMMETRIC verification material — kty and alg agree, and the public half is complete', () => {",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    pm.expect(Object.keys(_KTY_ALG), 'kty ' + k.kty + ' of ' + k.kid +",
                  "      ' — a facade publishing a symmetric key is publishing a SIGNING SECRET')",
                  "      .to.include(k.kty);",
                  "    pm.expect(k.alg, 'alg of ' + k.kid + ' against its key type ' + k.kty +",
                  "      ' — a key whose header algorithm its material cannot support is the alg-confusion shape seen from the publishing side').to.eql(_KTY_ALG[k.kty]);",
                  "    pm.expect(k.kid, JSON.stringify(k)).to.be.a('string').with.length.greaterThan(0);",
                  "    (_KTY_MEMBERS[k.kty] || []).forEach(m => pm.expect(k[m], 'member ' + m + ' of ' + k.kid +",
                  "      ' — an incomplete public half verifies nothing').to.be.a('string').with.length.greaterThan(0));",
                  "  });",
                  "});",
                  "pm.test('facade JWKS [' + _record + ']: carries PUBLIC material only (no d/p/q/dp/dq/qi)', () => {",
                  "  const priv = ['d', 'p', 'q', 'dp', 'dq', 'qi'];",
                  "  (_jwks.keys || []).forEach(k => {",
                  "    priv.forEach(m => pm.expect(k[m], 'private JWK member ' + m +",
                  "      ' on the facade: what is published is what VERIFIES, never what signs').to.be.undefined);",
                  "  });",
                  "});",
                  "pm.environment.set(\"_facadeOwnKids\", JSON.stringify((_jwks.keys || []).map(k => k.kid)));",
                  "pm.environment.set(\"_facadeOwnByKid\", JSON.stringify((_jwks.keys || []).reduce((a, k) => {",
                  "  a[k.kid] = {kty: k.kty, alg: k.alg, n: k.n, e: k.e, crv: k.crv, x: k.x, y: k.y};",
                  "  return a;",
                  "}, {})));"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-10-ONLY-FACADE-ISSUED-RS256-IS-ACCEPTED :: positive-control-real-bearer",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"positive control: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('the untouched facade Bearer is ACCEPTED (without this, every refusal below is satisfied by an edge that refuses everything)',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _sent = _sentBearer();",
                  "pm.environment.set('_ibtRealBearer', _sent);"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-10-ONLY-FACADE-ISSUED-RS256-IS-ACCEPTED :: anonymous-is-rejected",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"anonymous: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('anonymous is 401 (production posture: no unauthenticated access)',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.eql(401));",
                  "pm.test('grpc code 16 (UNAUTHENTICATED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(16);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-10-ONLY-FACADE-ISSUED-RS256-IS-ACCEPTED :: alg-none-forgery-is-rejected",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "const _real = pm.environment.get('_ibtRealBearer') || '';",
                  "const _p = _real.split('.');",
                  "if (_p.length === 3) {",
                  "  const h = JSON.parse(_b64urlToText(_p[0]));",
                  "  const h2 = _b64urlFromText(JSON.stringify({alg: 'none', kid: h.kid, typ: 'JWT'}));",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + h2 + '.' + _p[1] + '.'});",
                  "} else {",
                  "  pm.test('precondition: the positive control captured a three-part Bearer', () => {",
                  "    pm.expect.fail('_ibtRealBearer is not a JWT — the forgery cannot be built, and a forgery that was not built must not report a passing refusal.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"alg=none forgery: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('an unsigned token over the ACCEPTED payload is 401',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.eql(401));",
                  "pm.test('grpc code 16 (UNAUTHENTICATED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(16);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "IBT-10-ONLY-FACADE-ISSUED-RS256-IS-ACCEPTED :: hs256-alg-confusion-forgery-is-rejected",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "function _sentBearer() {",
                  "  var h = pm.request.headers.get('Authorization') || '';",
                  "  return h.replace(/^Bearer\\s+/i, '');",
                  "}",
                  "function _publicMaterial(k) {",
                  "  if (!k) { return ''; }",
                  "  if (k.n) { return k.n; }",
                  "  if (k.x && k.y) { return k.x + k.y; }",
                  "  if (k.x) { return k.x; }",
                  "  return '';",
                  "}",
                  "function _facadeKeyByKid(kid) {",
                  "  const _m = JSON.parse(pm.environment.get('_facadeByKid') || '{}');",
                  "  const _o = JSON.parse(pm.environment.get('_facadeOwnByKid') || '{}');",
                  "  return _m[kid] || _o[kid] || null;",
                  "}",
                  "const _real = pm.environment.get('_ibtRealBearer') || '';",
                  "const _p = _real.split('.');",
                  "const _kid = _p.length === 3 ? JSON.parse(_b64urlToText(_p[0])).kid : '';",
                  "const _key = _facadeKeyByKid(_kid);",
                  "const _mat = _publicMaterial(_key);",
                  "if (_p.length === 3 && _mat) {",
                  "  const h2 = _b64urlFromText(JSON.stringify({alg: 'HS256', kid: _kid, typ: 'JWT'}));",
                  "  const signing = h2 + '.' + _p[1];",
                  "  const mac = CryptoJS.HmacSHA256(signing, _mat).toString(CryptoJS.enc.Base64)",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + signing + '.' + mac});",
                  "} else {",
                  "  pm.test('precondition: real Bearer and the facade material for its kid are both available', () => {",
                  "    pm.expect.fail('cannot build the alg-confusion forgery (bearer parts=' + _p.length +",
                  "      ', kid=' + _kid + ', key published by the facade=' + (_key ? _key.kty : 'NONE') +",
                  "      ', public material=' + (_mat ? 'present' : 'MISSING') +",
                  "      '). A forgery that was not built must not report a passing refusal.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"HS256 alg-confusion forgery: request was ANSWERED (a check that did not run is not a check that passed)\", () => {",
                  "  pm.expect(pm.response && pm.response.code,",
                  "    'no response — the endpoint was not reached. This is a broken harness, not a passing check: fix reachability (the runner forwards the port and injects the base URL) or delete the probe.')",
                  "    .to.be.a('number');",
                  "});",
                  "pm.test('HS256 forgery of the SAME payload, keyed with the public modulus, is 401 (RS256 is pinned; the header does not choose the algorithm)',",
                  "  () => pm.expect(pm.response.code, pm.response.text()).to.eql(401));",
                  "pm.test('grpc code 16 (UNAUTHENTICATED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(16);",
                  "});",
                  "pm.test('the forgery did not become a principal', () => {",
                  "  let j = null; try { j = pm.response.json(); } catch (e) { j = null; }",
                  "  pm.expect(j && j.subject, JSON.stringify(j)).to.be.oneOf([undefined, null]);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}
