{
  "info": {
    "_postman_id": "b3fd96b3-4220-5f07-8d7f-feaa2de57e49",
    "name": "kaname / newman / kaname-recovery-lane",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  // runId формат: только [a-z0-9], без точки, начинается с буквы — чтобы проходить compute name regex",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', ('r' + t + r).replace(/[^a-z0-9]/g, '').slice(0, 11));",
          "}",
          "pm.environment.set('_suiteProjectId', pm.environment.get('existingProjectId'));",
          "pm.environment.set('_suiteFolderCrossId', pm.environment.get('existingProjectCrossId'));"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "try {",
          "  const _ct = (pm.response && pm.response.headers && pm.response.headers.get('Content-Type')) || '';",
          "  if (_ct.indexOf('json') !== -1) {",
          "    const _j = pm.response.json();",
          "    if (_j && _j.done === true && typeof _j.id === 'string' && _j.id !== '') {",
          "      if (_j.error) {",
          "        const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
          "        _pv.forEach(function (k) { pm.environment.unset(k); });",
          "      }",
          "      pm.environment.unset('_provisionalIds');",
          "    }",
          "  }",
          "} catch (e) { /* не JSON, не Operation — регистрация остаётся как есть */ }"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "IAM-RECOVERY-OK-REQUEST-SAME-ANSWER — Запрос кода восстановления отвечает одинаково для существующего и несуществующего адреса",
      "description": "class:CRUD | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-RECOVERY-OK-REQUEST-SAME-ANSWER :: csrf-recovery",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=recovery",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=recovery"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=recovery');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы формы службы; поднимается только посадкой `own` — на посадке `external` его нет, и это не отказ продукта, а условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.11'});",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.test('CSRF-RECOVERY: признак выдан строкой', () => pm.expect(j.csrfToken, JSON.stringify(j)).to.be.a('string').and.not.empty);",
                  "pm.environment.set('recoveryCsrfRequest', j.csrfToken);",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  pm.test('CSRF-RECOVERY: ответ ставит печенье kaname_form', () => pm.expect(__sc.length, JSON.stringify(pm.response.headers.all())).to.eql(1));",
                  "  if (__sc.length === 1) { pm.environment.set('recoveryFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-RECOVERY-OK-REQUEST-SAME-ANSWER :: request-existing-address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/recovery",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "recovery"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"csrfToken\": \"{{recoveryCsrfRequest}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/recovery');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы формы службы; поднимается только посадкой `own` — на посадке `external` его нет, и это не отказ продукта, а условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.11'});",
                  "const __cookie = ((pm.environment.get('recoveryFormCookie') ? 'kaname_form' + '=' + pm.environment.get('recoveryFormCookie') + '; ' : '')).replace(/; $/, '');",
                  "if (__cookie) { pm.request.headers.upsert({key: 'Cookie', value: __cookie}); }",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('REQUEST-EXISTING: тело — пустой объект, исход не сообщается', () => pm.expect(pm.response.json(), pm.response.text()).to.eql({}));",
                  "pm.environment.set('recoveryRequestBodyExisting', pm.response.text());",
                  "pm.test('REQUEST-EXISTING: носитель сессии НЕ выдан', () => pm.expect(pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session=')).length, JSON.stringify(pm.response.headers.all())).to.eql(0));",
                  "pm.test('REQUEST-EXISTING: контекст формы не переставлен', () => pm.expect(pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie').length, JSON.stringify(pm.response.headers.all())).to.eql(0));"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-RECOVERY-OK-REQUEST-SAME-ANSWER :: request-nobody-address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/recovery",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "recovery"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"nobody-{{runId}}@example.invalid\", \"csrfToken\": \"{{recoveryCsrfRequest}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/recovery');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы формы службы; поднимается только посадкой `own` — на посадке `external` его нет, и это не отказ продукта, а условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.11'});",
                  "const __cookie = ((pm.environment.get('recoveryFormCookie') ? 'kaname_form' + '=' + pm.environment.get('recoveryFormCookie') + '; ' : '')).replace(/; $/, '');",
                  "if (__cookie) { pm.request.headers.upsert({key: 'Cookie', value: __cookie}); }",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('REQUEST-NOBODY: тело побайтово равно ответу для существующего адреса (Ф5-02)', () => pm.expect(pm.response.text()).to.eql(pm.environment.get('recoveryRequestBodyExisting')));",
                  "pm.test('REQUEST-NOBODY: носитель сессии НЕ выдан', () => pm.expect(pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session=')).length, JSON.stringify(pm.response.headers.all())).to.eql(0));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-RECOVERY-NEG-WRONG-CODE — Неверный код восстановления: 401 одним текстом, носитель сессии не выдан",
      "description": "class:NEG | class:SEC | priority:P0",
      "item": [
        {
          "name": "IAM-RECOVERY-NEG-WRONG-CODE :: csrf-recovery-complete",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/csrf?form=recovery-complete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "csrf?form=recovery-complete"
              ]
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/csrf?form=recovery-complete');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы формы службы; поднимается только посадкой `own` — на посадке `external` его нет, и это не отказ продукта, а условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.11'});",
                  "const __cookie = ((pm.environment.get('recoveryFormCookie') ? 'kaname_form' + '=' + pm.environment.get('recoveryFormCookie') + '; ' : '')).replace(/; $/, '');",
                  "if (__cookie) { pm.request.headers.upsert({key: 'Cookie', value: __cookie}); }",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.test('CSRF-COMPLETE: признак выдан строкой', () => pm.expect(j.csrfToken, JSON.stringify(j)).to.be.a('string').and.not.empty);",
                  "pm.environment.set('recoveryCsrfComplete', j.csrfToken);",
                  "pm.test('CSRF-COMPLETE: признак предъявления отличается от признака запроса — вид формы замешан', () => pm.expect(j.csrfToken).to.not.eql(pm.environment.get('recoveryCsrfRequest')));",
                  "{",
                  "  const __sc = pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_form='));",
                  "  if (__sc.length === 1) { pm.environment.set('recoveryFormCookie', __sc[0].value.split(';')[0].slice(12)); }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "IAM-RECOVERY-NEG-WRONG-CODE :: complete-wrong-code",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/recovery/complete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "recovery",
                "complete"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"code\": \"AAAAA-AAAAA\", \"newPassword\": \"Recovered-{{runId}}-long-enough\", \"csrfToken\": \"{{recoveryCsrfComplete}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/recovery/complete');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы формы службы; поднимается только посадкой `own` — на посадке `external` его нет, и это не отказ продукта, а условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.11'});",
                  "const __cookie = ((pm.environment.get('recoveryFormCookie') ? 'kaname_form' + '=' + pm.environment.get('recoveryFormCookie') + '; ' : '')).replace(/; $/, '');",
                  "if (__cookie) { pm.request.headers.upsert({key: 'Cookie', value: __cookie}); }",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(401));",
                  "pm.test('grpc code 16 (UNAUTHENTICATED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(16);",
                  "});",
                  "pm.test('WRONG-CODE: текст отказа фиксирован', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.message, JSON.stringify(j)).to.eql('authentication failed');",
                  "});",
                  "pm.test('WRONG-CODE: носитель сессии НЕ выдан', () => pm.expect(pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session=')).length, JSON.stringify(pm.response.headers.all())).to.eql(0));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IAM-RECOVERY-NEG-CSRF-MISSING — Форма предъявления кода без признака: 400 с именем поля, глагол не исполняется",
      "description": "class:NEG | class:VAL | priority:P1",
      "item": [
        {
          "name": "IAM-RECOVERY-NEG-CSRF-MISSING :: complete-without-csrf",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/auth/recovery/complete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "auth",
                "recovery",
                "complete"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"email\": \"{{loginLaneEmail}}\", \"code\": \"AAAAA-AAAAA\", \"newPassword\": \"Recovered-{{runId}}-long-enough\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "protocolProfileBehavior": {
            "strictSSL": false
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: anonymous step",
                  "pm.request.headers.remove('Authorization');",
                  "// HARNESS-CONFIG GUARD — loginLaneBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('loginLaneBaseUrl') || pm.variables.get('loginLaneBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + pm.variables.replaceIn('/iam/v1/auth/recovery/complete');",
                  "} else {",
                  "  pm.test('[УСЛОВИЕ НЕ СОЗДАНО] harness config: loginLaneBaseUrl is set — слушатель полосы формы службы; поднимается только посадкой `own` — на посадке `external` его нет, и это не отказ продукта, а условие, которого стенд не создал', () => {",
                  "    pm.expect.fail('loginLaneBaseUrl is not set — the newman runner (deploy/scripts/newman-e2e.sh / newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.request.headers.upsert({key: 'X-Forwarded-For', value: '203.0.113.11'});",
                  "const __cookie = ((pm.environment.get('recoveryFormCookie') ? 'kaname_form' + '=' + pm.environment.get('recoveryFormCookie') + '; ' : '')).replace(/; $/, '');",
                  "if (__cookie) { pm.request.headers.upsert({key: 'Cookie', value: __cookie}); }",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('NO-CSRF: текст отказа фиксирован', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.message, JSON.stringify(j)).to.eql('Illegal argument csrfToken: required');",
                  "});",
                  "pm.test('NO-CSRF: носитель сессии НЕ выдан', () => pm.expect(pm.response.headers.all().filter(h => h.key.toLowerCase() === 'set-cookie' && h.value.startsWith('kaname_session=')).length, JSON.stringify(pm.response.headers.all())).to.eql(0));"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}
