{
  "info": {
    "_postman_id": "aa8763e4-9aa8-5848-a59e-5692f762d461",
    "name": "kacho-iam / newman / authz-failclosed",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  // runId формат: только [a-z0-9], без точки, начинается с буквы — чтобы проходить compute name regex",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', ('r' + t + r).replace(/[^a-z0-9]/g, '').slice(0, 11));",
          "}",
          "pm.environment.set('_suiteProjectId', pm.environment.get('existingProjectId'));",
          "pm.environment.set('_suiteFolderCrossId', pm.environment.get('existingProjectCrossId'));"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "try {",
          "  const _ct = (pm.response && pm.response.headers && pm.response.headers.get('Content-Type')) || '';",
          "  if (_ct.indexOf('json') !== -1) {",
          "    const _j = pm.response.json();",
          "    if (_j && _j.done === true && typeof _j.id === 'string' && _j.id !== '') {",
          "      if (_j.error) {",
          "        const _pv = JSON.parse(pm.environment.get('_provisionalIds') || '[]');",
          "        _pv.forEach(function (k) { pm.environment.unset(k); });",
          "      }",
          "      pm.environment.unset('_provisionalIds');",
          "    }",
          "  }",
          "} catch (e) { /* не JSON, не Operation — регистрация остаётся как есть */ }"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "AUTHZ-FAILCLOSED-OPENFGA-DOWN — gateway returns 503 Unavailable when the authorization store is unreachable (fail-closed)",
      "description": "class:AUTHZ | class:FLOW | class:FAIL_CLOSED | priority:P0",
      "item": [
        {
          "name": "AUTHZ-FAILCLOSED-OPENFGA-DOWN :: any-authz-gated-rpc-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/accounts/{{accountAId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "accounts",
                "{{accountAId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// fail-closed: когда источник вердикта недостижим, слой авторизации",
                  "// шлюза уходит в ветвь ошибки и при FailOpen=false отвечает",
                  "// 503 Unavailable / gRPC 14. НИКОГДА 200.",
                  "//",
                  "// Условие создаёт scripts/run-failclosed.sh (своя волна прогона):",
                  "// он прогревает снимок проверочных ключей края, сворачивает базу",
                  "// источника вердикта, ждёт истечения кэша решений шлюза, гоняет эту",
                  "// коллекцию и поднимает базу обратно.",
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] authz-gate: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] authz-gate: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] authz-gate: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 — every iam List refuses (503/14) at the edge while the verdict source is unreachable — never 200 with an empty page",
      "description": "class:AUTHZ | class:FLOW | class:FAIL_CLOSED | class:NEG | priority:P0",
      "item": [
        {
          "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 :: list-projects-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/projects?accountId={{accountAId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "projects?accountId={{accountAId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] projects: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] projects: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] projects: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});",
                  "pm.test('[FAIL-CLOSED] projects: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.projects) && _j.projects.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 :: list-accounts-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/accounts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "accounts"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] accounts: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] accounts: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] accounts: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});",
                  "pm.test('[FAIL-CLOSED] accounts: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.accounts) && _j.accounts.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 :: list-users-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/users?accountId={{accountAId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "users?accountId={{accountAId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] users: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] users: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] users: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});",
                  "pm.test('[FAIL-CLOSED] users: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.users) && _j.users.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 :: list-groups-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/groups?accountId={{accountAId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "groups?accountId={{accountAId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] groups: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] groups: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] groups: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});",
                  "pm.test('[FAIL-CLOSED] groups: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.groups) && _j.groups.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 :: list-service-accounts-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/serviceAccounts?accountId={{accountAId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "serviceAccounts?accountId={{accountAId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] service-accounts: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] service-accounts: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] service-accounts: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});",
                  "pm.test('[FAIL-CLOSED] service-accounts: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.serviceAccounts) && _j.serviceAccounts.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 :: list-roles-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/roles?accountId={{accountAId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "roles?accountId={{accountAId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] roles: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] roles: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] roles: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});",
                  "pm.test('[FAIL-CLOSED] roles: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.roles) && _j.roles.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "AUTHZ-FAILCLOSED-LIST-NEVER-EMPTY-200 :: list-access-bindings-during-verdict-source-outage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/accessBindings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "accessBindings"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtAccountAdminA'",
                  "const __t = pm.environment.get('jwtAccountAdminA') || pm.variables.get('jwtAccountAdminA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtAccountAdminA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtAccountAdminA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "pm.test('[FAIL-CLOSED] access-bindings: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] access-bindings: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] access-bindings: отказ произвёл КРАЙ, полоса чтения отзыва', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса чтения отзыва: тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:revocation');",
                  "});",
                  "pm.test('[FAIL-CLOSED] access-bindings: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.accessBindings) && _j.accessBindings.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "AUTHZ-FAILCLOSED-EDGE-KEY-SOURCE-LANE — the EDGE refuses (503/14) on its own key-source lane when the verification key set cannot be fetched",
      "description": "class:AUTHZ | class:AUTHN | class:FLOW | class:FAIL_CLOSED | class:NEG | priority:P0",
      "item": [
        {
          "name": "AUTHZ-FAILCLOSED-EDGE-KEY-SOURCE-LANE :: list-projects-with-a-key-id-the-key-source-must-be-asked-about",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/iam/v1/projects?accountId={{accountAId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "projects?accountId={{accountAId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// base64url ↔ текст. Восемь строк без продуктовой семантики:",
                  "// разойтись такой копии не с чем — расхождение не разбиралось бы",
                  "// вовсе, а роняло бы разбор заголовка на месте.",
                  "function _b64urlToText(s) {",
                  "  var t = String(s).replace(/-/g, '+').replace(/_/g, '/');",
                  "  while (t.length % 4 !== 0) { t += '='; }",
                  "  return CryptoJS.enc.Base64.parse(t).toString(CryptoJS.enc.Utf8);",
                  "}",
                  "function _b64urlFromText(s) {",
                  "  return CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(s))",
                  "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
                  "}",
                  "const _real = pm.environment.get('jwtAccountAdminA')",
                  "  || pm.variables.get('jwtAccountAdminA') || '';",
                  "const _parts = String(_real).split('.');",
                  "let _hdr = null;",
                  "if (_parts.length === 3 && _parts[0] !== '') {",
                  "  try { _hdr = JSON.parse(_b64urlToText(_parts[0])); } catch (_) { _hdr = null; }",
                  "}",
                  "if (!_hdr || typeof _hdr.alg !== 'string' || _hdr.alg === '') {",
                  "  pm.test('harness config: jwtAccountAdminA is a JWT this probe can derive from', () => {",
                  "    pm.expect.fail('jwtAccountAdminA отсутствует либо не разбирается как JWT — производное удостоверение построить не из чего. Отправить шаг без заголовка нельзя: анонимный вызывающий получил бы отказ, и кейс прошёл бы, ни разу не спросив источник ключей.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "} else {",
                  "  _hdr.kid = 'kacho-lane-probe-no-such-key';",
                  "  _parts[0] = _b64urlFromText(JSON.stringify(_hdr));",
                  "  pm.request.headers.upsert({",
                  "    key: 'Authorization', value: 'Bearer ' + _parts.join('.')",
                  "  });",
                  "}",
                  "(function () {",
                  "  var _u = '';",
                  "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
                  "  try {",
                  "    if (pm.request.body && pm.request.body.raw) { _u = _u + ' ' + pm.request.body.raw; }",
                  "  } catch (e) { /* тела может не быть — это не находка */ }",
                  "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
                  "  if (!_all) { return; }",
                  "  var _n = null;",
                  "  for (var _i = 0; _i < _all.length; _i++) {",
                  "    var _c = _all[_i].slice(2, -2);",
                  "    if (!pm.variables.has(_c)) { _n = _c; break; }",
                  "  }",
                  "  if (_n === null) { return; }",
                  "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
                  "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function _laneOf(j) {",
                  "  // Мост gRPC↔REST маршалит статус публичным маршаллером края",
                  "  // (EmitUnpopulated=true) ⇒ ключ `details` есть ВСЕГДА. Отказы самого края",
                  "  // пишутся руками объектом из двух полей ⇒ `details` в них нет никогда.",
                  "  var bridged = !!j && Object.prototype.hasOwnProperty.call(j, 'details')",
                  "    && Array.isArray(j.details);",
                  "  if (bridged) { return 'service'; }",
                  "  var m = (j && typeof j.message === 'string') ? j.message : '';",
                  "  // Ниже — ДИАГНОСТИКА, а не вердикт: вердикт вынесен строкой выше.",
                  "  if (m === 'token key source unavailable') { return 'edge:key-source'; }",
                  "  if (m === 'revocation check unavailable') { return 'edge:revocation'; }",
                  "  if (m === 'authz service unavailable') { return 'edge:authz'; }",
                  "  return 'unrecognised:' + JSON.stringify(m);",
                  "}",
                  "let _j; try { _j = pm.response.json(); } catch (_) { _j = {}; }",
                  "const _body = JSON.stringify(_j);",
                  "const _lane = _laneOf(_j);",
                  "// 401 здесь означал бы, что источник ключей ОТВЕТИЛ (набор добыт,",
                  "// ключа в нём нет ⇒ токен негоден) — то есть условие волны не",
                  "// создано. Утверждение об исходе ниже это и ловит.",
                  "pm.test('[FAIL-CLOSED] edge-key-source: 503 Unavailable', () => {",
                  "  pm.expect(pm.response.code, _body).to.eql(503);",
                  "});",
                  "pm.test('[FAIL-CLOSED] edge-key-source: grpc code 14 (Unavailable)', () => {",
                  "  pm.expect(_j.code, _body).to.eql(14);",
                  "});",
                  "pm.test('[FAIL-CLOSED] edge-key-source: отказ произвёл КРАЙ, полоса проверочных ключей (снимок ключей устарел и не обновился)', () => {",
                  "  pm.expect(_lane, 'отказ произвёл ' + _lane + ', а кейс утверждает про КРАЙ, полоса проверочных ключей (снимок ключей устарел и не обновился): тот же исход (503/14) производят ЧЕТЫРЕ разных места, и по исходу они неразличимы — ' + _body).to.eql('edge:key-source');",
                  "});",
                  "pm.test('[FAIL-CLOSED] edge-key-source: never a successful empty page', () => {",
                  "  const _emptyOk = pm.response.code === 200 && Array.isArray(_j.projects) && _j.projects.length === 0;",
                  "  pm.expect(_emptyOk, 'answered 200 with an empty array while the verdict source was unreachable: a tenant cannot tell that from \"nothing has been granted to you\" — ' + _body).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}
