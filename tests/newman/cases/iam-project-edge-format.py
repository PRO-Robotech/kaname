# Copyright (c) PRO-Robotech
# SPDX-License-Identifier: AGPL-3.0-or-later

"""Case-set iam-project-edge-format — ФОРМА ИДЕНТИФИКАТОРА ПРОЕКТА, КОТОРУЮ СУДИТ КРАЙ.

Один кейс, вынесенный из `iam-project.py` при переезде того модуля на собственный
фронт службы (задача #19): `IAM-PRJ-DL-NEG-MALFORMED-PREFIX` (приёмка
`docs/engineering/acceptance/non-empty-project-is-not-deleted.md`, IAM-PNE-1-08).

ПОЧЕМУ ОТДЕЛЬНЫМ МОДУЛЕМ. Пара `400` / `3` на строке с неизвестной приставкой —
производитель КРАЙ платформы: шаг короткого замыкания по форме идентификатора стоит
в нём ДО проверки прав (приёмка, Н4/Н14). На собственном фронте службы этого шага
нет: глагол доходит до проверки прав и отвечает `403` / `7` (`AUTHZ_DENIED`, область
`project`) — замер на автономном стенде 2026-09-16, сведённое дерево. Значит на
собственном фронте у пары нет производителя, а утверждение о форме, переписанное
под `403`, перестало бы быть о форме. Исход e2e-flow.md §7а — «оставить
утверждение платформе, расщепив коллекцию».

ДОМ — ПЛАТФОРМА: гоняет её конвейер через край; автономный стенд службы модуль не
исполняет, и печать долга называет его категорией C. Кейс переносится дословно,
без изменения ни строгости, ни «Тогда».

Чего этот модуль НЕ утверждает: как отвечает на тот же вход СОБСТВЕННЫЙ фронт
службы. Должен ли он замыкать по форме до проверки прав (api-conventions.md
§«Malformed-id — ПЕРВЫМ стейтментом RPC» против authz-first рубежа) — предмет
отдельной задачи, а не этого кейса.
"""

# ДОМ МОДУЛЯ — ПЛАТФОРМА: свойство производит её край (e2e-flow.md §7а).
# `HOME_REASON` называет производителя; сверяет `scripts/case_home_test.py`.
HOME = "kacho"
HOME_REASON = (
    "производитель пары — край платформы: короткое замыкание по приставке "
    "идентификатора стоит в нём ДО проверки прав и отвечает 400/3 без обращения к "
    "службе; собственный фронт службы такого шага не несёт и на том же входе "
    "отвечает 403/7 от проверки прав"
)

CASES = []

# ---------------------------------------------------------------------------
# IAM-PRJ-DL-NEG-MALFORMED-PREFIX — IAM-PNE-1-08: неправильная форма идентификатора
# отвергается СИНХРОННО, операция не создаётся.
#
# ПРОИЗВОДИТЕЛЬ ПАРЫ — КРАЙ. Шаг короткого замыкания по форме идентификатора стоит
# в крае ДО проверки прав и судит ТОЛЬКО приставку: строка, у которой ни первые три
# знака, ни сегмент до первого дефиса не входят в каталог приставок платформы, даёт
# 400 / 3 без обращения к службе. Идентификатор известной приставки не той длины
# (`prj123`) и идентификатор чужого семейства правильной формы край ПРОПУСКАЕТ к
# проверке прав, и тот отвечает 403, — поэтому такие входы сюда не входят: «Тогда»
# было бы строже производителя (приёмка §0.2в, Н4, Н14).
#
# СТРОГОСТЬ ПАРЫ ОСЛАБЛЯТЬ ЗАПРЕЩЕНО (testing.md §e2e-инварианты: «malformed-id … не
# ослаблять»): `oneOf` здесь был бы допуском на исход, которого край на этом входе
# не производит.
# ---------------------------------------------------------------------------

CASES.append(Case(
    id="IAM-PRJ-DL-NEG-MALFORMED-PREFIX",
    title="Delete /projects/{string whose prefix is unknown to the platform} → sync 400 INVALID_ARGUMENT (3)",
    classes=["NEG", "VAL"],
    priority="P1",
    steps=[
        Step(
            name="delete-malformed-prefix",
            method="DELETE",
            path="/iam/v1/projects/qqq-not-a-project",
            auth="jwtAccountAdminA",
            test_script=[
                *assert_status(400),
                *assert_grpc_code(3, "INVALID_ARGUMENT"),
                # Конверта операции нет: отказ синхронный, проверка формы стоит
                # раньше создания операции и этим изменением не сдвигается.
                "pm.test('no operation envelope on a sync refusal', () => {"
                " const j = pm.response.json();"
                " pm.expect(j.id, JSON.stringify(j)).to.be.undefined; });",
            ],
        ),
    ],
))
