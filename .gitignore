# Go build
*.exe
*.test
*.out
bin/
dist/
coverage.out
coverage-full.out
/kaname
/kaname-migrator

# Newman snapshots and run logs (audit trail, локально, не commit)
tests/newman/out/snap*-local-*.json
tests/newman/out/snap*-local-run.log

# IDE
.idea/
.vscode/
.DS_Store

# tests output
test-results/

# Python bytecode (newman Python case generators)
__pycache__/
*.pyc

go.work
go.work.sum
.claude/

# AI-оснастка (источник истины — kacho-workspace, локально синкается) — не коммитим
.claude/
CLAUDE.md
AGENTS.md
.codex/
.agents/

# Кэш golangci-lint: якорь — каталог модуля (Makefile, GOLANGCI_LINT_CACHE),
# потому что модуль стоит и самостоятельным клоном, где корня монорепо нет.
/.cache/

# Автономный стенд службы (.github/scripts/stand-own.sh): PKI, бинари и журнал
# прогона. Порождается, в поставку не входит.
.stand/
