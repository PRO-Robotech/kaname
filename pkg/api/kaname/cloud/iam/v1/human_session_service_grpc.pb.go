// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: Apache-2.0

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: kaname/cloud/iam/v1/human_session_service.proto

package iamv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	InternalHumanSessionService_Resolve_FullMethodName = "/kaname.cloud.iam.v1.InternalHumanSessionService/Resolve"
)

// InternalHumanSessionServiceClient is the client API for InternalHumanSessionService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// InternalHumanSessionService — the ONE question the edge asks about OUR human
// session (phase Ф3, task PRO-Robotech/kacho#1269; acceptance
// `docs/engineering/acceptance/login-lane-issues-our-session-and-logout-ends-it-server-side.md`,
// decisions Р1, Р7).
//
// It lives on the cluster-internal listener (:9091) under mTLS, next to
// `InternalSessionRevocationsService.SessionCutoffOf`, and is never routed to
// the external listener (ban #6). The edge asks it on the identity lane and on
// the `/iam/v1/auth/me` route, on every presentation — nothing is cached on the
// edge, because a cached answer cannot be withdrawn by a logout or a block.
//
// WHAT THIS RPC DOES NOT DO — said so it is not inferred.
//
//   - It does NOT apply the revocation cutoff. The cutoff is asked separately
//     (`SessionCutoffOf`) and compared INCLUSIVELY by the edge against
//     `authenticated_at`. One reader of the cutoff, and it is on the edge (Р7):
//     folding the cutoff in here would leave a second reader and make Ф1-56
//     («UNIMPLEMENTED only about the cutoff — pass loudly») unbuildable.
//   - It does NOT distinguish WHY there is no session. Unknown bearer, a
//     record ended by a logout, an expired record and a blocked person are ONE
//     answer (`found = false`, empty `session`). The reasons are visible only
//     in this service's counters and journal (Ф1-11, Ф1-17).
//   - It does NOT move the moment of last presentation: a presentation is a
//     password, a code or a key being checked, not a bearer riding a request.
type InternalHumanSessionServiceClient interface {
	// Resolve — the session behind a bearer, or «no session».
	//
	// INVALID_ARGUMENT when `bearer` is empty (named by field). UNAVAILABLE when
	// the store did not answer — the edge treats it as a refusal that leaves the
	// client's bearer intact (F4d-23).
	Resolve(ctx context.Context, in *ResolveHumanSessionRequest, opts ...grpc.CallOption) (*ResolveHumanSessionResponse, error)
}

type internalHumanSessionServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewInternalHumanSessionServiceClient(cc grpc.ClientConnInterface) InternalHumanSessionServiceClient {
	return &internalHumanSessionServiceClient{cc}
}

func (c *internalHumanSessionServiceClient) Resolve(ctx context.Context, in *ResolveHumanSessionRequest, opts ...grpc.CallOption) (*ResolveHumanSessionResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ResolveHumanSessionResponse)
	err := c.cc.Invoke(ctx, InternalHumanSessionService_Resolve_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// InternalHumanSessionServiceServer is the server API for InternalHumanSessionService service.
// All implementations must embed UnimplementedInternalHumanSessionServiceServer
// for forward compatibility.
//
// InternalHumanSessionService — the ONE question the edge asks about OUR human
// session (phase Ф3, task PRO-Robotech/kacho#1269; acceptance
// `docs/engineering/acceptance/login-lane-issues-our-session-and-logout-ends-it-server-side.md`,
// decisions Р1, Р7).
//
// It lives on the cluster-internal listener (:9091) under mTLS, next to
// `InternalSessionRevocationsService.SessionCutoffOf`, and is never routed to
// the external listener (ban #6). The edge asks it on the identity lane and on
// the `/iam/v1/auth/me` route, on every presentation — nothing is cached on the
// edge, because a cached answer cannot be withdrawn by a logout or a block.
//
// WHAT THIS RPC DOES NOT DO — said so it is not inferred.
//
//   - It does NOT apply the revocation cutoff. The cutoff is asked separately
//     (`SessionCutoffOf`) and compared INCLUSIVELY by the edge against
//     `authenticated_at`. One reader of the cutoff, and it is on the edge (Р7):
//     folding the cutoff in here would leave a second reader and make Ф1-56
//     («UNIMPLEMENTED only about the cutoff — pass loudly») unbuildable.
//   - It does NOT distinguish WHY there is no session. Unknown bearer, a
//     record ended by a logout, an expired record and a blocked person are ONE
//     answer (`found = false`, empty `session`). The reasons are visible only
//     in this service's counters and journal (Ф1-11, Ф1-17).
//   - It does NOT move the moment of last presentation: a presentation is a
//     password, a code or a key being checked, not a bearer riding a request.
type InternalHumanSessionServiceServer interface {
	// Resolve — the session behind a bearer, or «no session».
	//
	// INVALID_ARGUMENT when `bearer` is empty (named by field). UNAVAILABLE when
	// the store did not answer — the edge treats it as a refusal that leaves the
	// client's bearer intact (F4d-23).
	Resolve(context.Context, *ResolveHumanSessionRequest) (*ResolveHumanSessionResponse, error)
	mustEmbedUnimplementedInternalHumanSessionServiceServer()
}

// UnimplementedInternalHumanSessionServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedInternalHumanSessionServiceServer struct{}

func (UnimplementedInternalHumanSessionServiceServer) Resolve(context.Context, *ResolveHumanSessionRequest) (*ResolveHumanSessionResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Resolve not implemented")
}
func (UnimplementedInternalHumanSessionServiceServer) mustEmbedUnimplementedInternalHumanSessionServiceServer() {
}
func (UnimplementedInternalHumanSessionServiceServer) testEmbeddedByValue() {}

// UnsafeInternalHumanSessionServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to InternalHumanSessionServiceServer will
// result in compilation errors.
type UnsafeInternalHumanSessionServiceServer interface {
	mustEmbedUnimplementedInternalHumanSessionServiceServer()
}

func RegisterInternalHumanSessionServiceServer(s grpc.ServiceRegistrar, srv InternalHumanSessionServiceServer) {
	// If the following call panics, it indicates UnimplementedInternalHumanSessionServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&InternalHumanSessionService_ServiceDesc, srv)
}

func _InternalHumanSessionService_Resolve_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ResolveHumanSessionRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalHumanSessionServiceServer).Resolve(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalHumanSessionService_Resolve_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalHumanSessionServiceServer).Resolve(ctx, req.(*ResolveHumanSessionRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// InternalHumanSessionService_ServiceDesc is the grpc.ServiceDesc for InternalHumanSessionService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var InternalHumanSessionService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "kaname.cloud.iam.v1.InternalHumanSessionService",
	HandlerType: (*InternalHumanSessionServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Resolve",
			Handler:    _InternalHumanSessionService_Resolve_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "kaname/cloud/iam/v1/human_session_service.proto",
}
