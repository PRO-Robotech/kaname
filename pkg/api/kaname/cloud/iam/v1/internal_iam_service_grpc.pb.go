// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: Apache-2.0

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: kaname/cloud/iam/v1/internal_iam_service.proto

package iamv1

import (
	context "context"
	operation "github.com/PRO-Robotech/corelib/api/kacho/cloud/operation"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	InternalIAMService_LookupSubject_FullMethodName            = "/kaname.cloud.iam.v1.InternalIAMService/LookupSubject"
	InternalIAMService_Check_FullMethodName                    = "/kaname.cloud.iam.v1.InternalIAMService/Check"
	InternalIAMService_ForceLogout_FullMethodName              = "/kaname.cloud.iam.v1.InternalIAMService/ForceLogout"
	InternalIAMService_PollSubjectChanges_FullMethodName       = "/kaname.cloud.iam.v1.InternalIAMService/PollSubjectChanges"
	InternalIAMService_RegisterResource_FullMethodName         = "/kaname.cloud.iam.v1.InternalIAMService/RegisterResource"
	InternalIAMService_UnregisterResource_FullMethodName       = "/kaname.cloud.iam.v1.InternalIAMService/UnregisterResource"
	InternalIAMService_ResolveBasicCredential_FullMethodName   = "/kaname.cloud.iam.v1.InternalIAMService/ResolveBasicCredential"
	InternalIAMService_GetRoleCompiled_FullMethodName          = "/kaname.cloud.iam.v1.InternalIAMService/GetRoleCompiled"
	InternalIAMService_CheckBasicCredentialLive_FullMethodName = "/kaname.cloud.iam.v1.InternalIAMService/CheckBasicCredentialLive"
)

// InternalIAMServiceClient is the client API for InternalIAMService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// InternalIAMService — cluster-internal RPCs (port 9091).
//
// НЕ публикуется на external TLS listener. Регистрируется в api-gateway internal mux
// (внутренний listener) — нужен auth-interceptor api-gateway (звать LookupSubject
// из interceptor'а после валидации JWT (Ory Hydra)).
//
// Вызывается из admin-tooling (grpcurl) и authz-interceptor api-gateway.
type InternalIAMServiceClient interface {
	// Резолв subject'а по одному из ключей (external_id / id / email).
	//
	// REST exposed ONLY on the cluster-internal listener;
	// api-gateway isInternalPath() gates `/iam/v1/internal/*` to internal mux
	// and authz-public-allowlist permits the <exempt> catalog entry.
	LookupSubject(ctx context.Context, in *LookupSubjectRequest, opts ...grpc.CallOption) (*LookupSubjectResponse, error)
	// Check — single-tuple authorization check.
	// Вердикт о паре «субъект — отношение — объект». Складывает его реляционная
	// форма в собственной базе iam: внешний движок отношений, поверх которого этот
	// метод стоял прежде, снят стадией S6 эпика #747.
	// Используется per-service authz-interceptor'ами kacho-vpc / kacho-compute /
	// kacho-loadbalancer / self (kaname) для проверки прав на write/read RPC.
	// Latency budget: ≤20ms p95. Сетевого перехода к соседу больше нет — вопрос
	// задаётся той же базе, что и остальные чтения службы.
	//
	// REST exposed ONLY on the cluster-internal listener.
	Check(ctx context.Context, in *CheckRequest, opts ...grpc.CallOption) (*CheckResponse, error)
	// admin force-logout of a user. Writes
	// `session_revocations` rows for every active access token (enumerated via
	// Hydra introspection) + a `caep_outbox` row so federated downstream RPs
	// get notified. Async: returns Operation; the LISTEN/NOTIFY fan-out then
	// updates every api-gateway pod's revocation cache ≤ 1s.
	ForceLogout(ctx context.Context, in *ForceLogoutRequest, opts ...grpc.CallOption) (*operation.Operation, error)
	// PollSubjectChanges drains subject_change_outbox by ascending id cursor,
	// over the window `(since_id, settled]` — never "everything above the cursor".
	// Internal-only (cluster-internal listener) — drives api-gateway authz
	// decision-cache invalidation.
	//
	// UNAVAILABLE while the journal has no settled position yet (cold start): the
	// caller retries on its next tick. This is a state, not a failure — the
	// settled flag is monotone and never withdrawn once raised.
	//
	// OUT_OF_RANGE when `since_id` sits BELOW the journal floor — the rows between
	// them have been removed and will never be delivered. The refusal carries
	// `google.rpc.ErrorInfo` with reason `SUBJECT_CHANGE_POSITION_LOST`, domain
	// `iam.kacho.cloud` and metadata `earliest_resumable_position` (decimal), and
	// the caller keys on that token, never on the prose of the message.
	//
	// The two refusals advise OPPOSITE actions and must never be merged. UNAVAILABLE
	// says "ask again from the same position"; OUT_OF_RANGE says "that retry will
	// never succeed — flush your decision cache, close your open streams and resume
	// from the named position". A caller that read the second as the first would
	// retry from a lost position forever, and its cache would keep answering from
	// rights that are already gone — silently.
	//
	// Why an explicit refusal exists at all: the read window is
	// `id > since_id AND id <= settled`, and a removed row simply does not appear
	// in it. The cursor rolls past it on the last row read, so "no rows" becomes
	// indistinguishable from "rows were swept" — an unapplied revocation nobody
	// observes. Until this refusal existed, sweeping the journal was impossible
	// by construction, not by preference.
	PollSubjectChanges(ctx context.Context, in *PollSubjectChangesRequest, opts ...grpc.CallOption) (*PollSubjectChangesResponse, error)
	// RegisterResource — Internal FGA-proxy: apply an owner-hierarchy tuple
	// (subject holds `relation` on `object`) on behalf of the resource-owning
	// module. Called by the vpc/compute/nlb outbox-drainer over mTLS so
	// those modules никогда не ходят в FGA напрямую — только через IAM.
	//
	// ИДЕМПОТЕНТНО (контракт): повтор того же (subject_id, relation, object) →
	// gRPC OK, НЕ AlreadyExists. От этого зависит at-least-once outbox-retry —
	// drainer может ретраить безопасно.
	//
	// Sync unary (как Check), НЕ async через Operation: ретрай и at-least-once
	// обеспечивает сам drainer.
	//
	// authz: `<exempt>` на уровне permission-каталога (как все Internal IAM RPC).
	// Least-priv энфорсится в IAM-handler через ReBAC: mTLS client-cert →
	// ServiceAccount → relation `fga_writer` на системном объекте
	// `cluster:cluster_root`. Нет relation → PermissionDenied. cluster-internal
	// listener :9091 only — нет google.api.http.
	RegisterResource(ctx context.Context, in *RegisterResourceRequest, opts ...grpc.CallOption) (*RegisterResourceResponse, error)
	// UnregisterResource — Internal FGA-proxy: снять owner-hierarchy tuple
	// (зеркально RegisterResource; вызывается drainer'ом при Delete ресурса).
	//
	// ИДЕМПОТЕНТНО (контракт): снятие отсутствующего/уже снятого tuple → gRPC OK,
	// НЕ NotFound — drainer ретраит безопасно.
	//
	// Sync unary, НЕ Operation. authz: `<exempt>` + ReBAC `fga_writer` @
	// `cluster:cluster_root` (см. RegisterResource). cluster-internal :9091 only,
	// нет google.api.http.
	UnregisterResource(ctx context.Context, in *UnregisterResourceRequest, opts ...grpc.CallOption) (*UnregisterResourceResponse, error)
	// GetRoleCompiled — Internal two-projection read (redesign-2026 F5): returns a
	// Role's COMPILED permission set (`module.resource.resourceName.verb` 4-segment
	// form) that backs FGA emission. This authz-topology projection is Internal-ONLY
	// (:9091) — the public RoleService.Get/List never carry compiled `permissions`
	// (they expose only authored `rules[]`). A label-only role compiles to an empty
	// set (all rules ARM_LABELS are not compiled). cluster-internal listener only —
	// no google.api.http; admin-UI authz gating happens at the internal mux layer.
	// ResolveBasicCredential — АВТОРИТЕТ О ПРЕДЪЯВЛЕННОМ БАЗОВОМ СЕКРЕТЕ
	// (задача #1142, приёмка BAT-1 §5.3, §6).
	//
	// Край зовёт этот глагол на промахе своего кэша вердикта и получает ответ на
	// ОДИН вопрос: годно ли предъявленное СЕЙЧАС и чей это принципал.
	//
	// ОТЗЫВ ДОХОДИТ ДО ПРЕДЪЯВЛЕНИЯ КОНСТРУКЦИЕЙ, А НЕ ВТОРЫМ МЕХАНИЗМОМ. Отзыв
	// есть СНЯТИЕ строки; резолв ищет строку по первичному ключу, и предикат его
	// единственного оператора включает существование строки, вид, непросроченность
	// и активность владельца. Нет строки — нет удостоверения. Поэтому под отзыв
	// попадают и поводы, о которых глагол отзыва не знает: снятие владельца,
	// снятие участия, перевод владельца в неактивное состояние — повод привязан к
	// САМОМУ поводу, а не к перечню обязанных писать.
	//
	// ОТКАЗ ЕДИНЫЙ. Неизвестный идентификатор, неверный секрет, истёкший срок,
	// отозванное удостоверение, неактивный владелец — ОДИН И ТОТ ЖЕ код и ОДИН И
	// ТОТ ЖЕ текст. Различимый текст есть оракул: по нему отличают «нет такого» от
	// «есть, но не ваш». Различимость живёт ВНУТРЬ — в счётчиках по причинам.
	//
	// REST exposed ONLY on the cluster-internal listener.
	ResolveBasicCredential(ctx context.Context, in *ResolveBasicCredentialRequest, opts ...grpc.CallOption) (*ResolveBasicCredentialResponse, error)
	GetRoleCompiled(ctx context.Context, in *GetRoleCompiledRequest, opts ...grpc.CallOption) (*GetRoleCompiledResponse, error)
	// CheckBasicCredentialLive — ЖИВО ЛИ УДОСТОВЕРЕНИЕ, СПРОШЕННОЕ ПО ЕГО
	// ИДЕНТИФИКАТОРУ, БЕЗ ПРЕДЪЯВЛЕНИЯ СЕКРЕТА (задача #1450).
	//
	// ЗАЧЕМ ВТОРОЙ ВОПРОС, ЕСЛИ РЯДОМ ЕСТЬ `ResolveBasicCredential`. Тот отвечает
	// ПРЕДЪЯВИТЕЛЮ и потому требует самой предъявленной строки. Спрашивающий с
	// ОТКРЫТОГО соединения предъявителем не является: секрет он видел однажды, при
	// открытии, и держать его живым весь срок соединения значило бы завести
	// поверхность хранения ради контроля — то есть платить за отзыв тем самым, что
	// отзыв защищает.
	//
	// Поэтому вопрос задаётся по идентификатору СТРОКИ удостоверения. Предикат
	// живости у него ТОТ ЖЕ, что у резолва: существование строки, вид `SECRET`,
	// непросроченность и активность владельца. Разойтись они не могут — оператор
	// один, и сверка хеша в нём не участвует by construction.
	//
	// ОТВЕТ — ОТКАЗ, А НЕ ПОЛЕ. Живое удостоверение даёт `OK`, неживое —
	// `UNAUTHENTICATED` с тем же единым текстом, что у резолва; «спросить не
	// удалось» — `UNAVAILABLE`. Поле-признак означало бы, что вызывающий вправе его
	// не прочитать, и тогда контроль присутствует, провязан и не отказывает ни разу.
	// Ровно тот довод, что записан у `ResolveBasicCredentialResponse`.
	//
	// ОТКАЗ ЕДИНЫЙ И ЗДЕСЬ. Неизвестный идентификатор, истёкший срок, отозванное
	// удостоверение, неактивный владелец — один код и один текст: различимый
	// отличал бы «нет такого» от «есть, но не действует».
	//
	// БЕЗ `google.api.http` НАМЕРЕННО — как у `GetRoleCompiled`. Вызывающий ходит
	// gRPC; дружелюбного пути под этот вопрос не заводится, чтобы его нельзя было
	// выставить наружу опечаткой в таблице маршрутов края.
	CheckBasicCredentialLive(ctx context.Context, in *CheckBasicCredentialLiveRequest, opts ...grpc.CallOption) (*CheckBasicCredentialLiveResponse, error)
}

type internalIAMServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewInternalIAMServiceClient(cc grpc.ClientConnInterface) InternalIAMServiceClient {
	return &internalIAMServiceClient{cc}
}

func (c *internalIAMServiceClient) LookupSubject(ctx context.Context, in *LookupSubjectRequest, opts ...grpc.CallOption) (*LookupSubjectResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(LookupSubjectResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_LookupSubject_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) Check(ctx context.Context, in *CheckRequest, opts ...grpc.CallOption) (*CheckResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CheckResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_Check_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) ForceLogout(ctx context.Context, in *ForceLogoutRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(operation.Operation)
	err := c.cc.Invoke(ctx, InternalIAMService_ForceLogout_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) PollSubjectChanges(ctx context.Context, in *PollSubjectChangesRequest, opts ...grpc.CallOption) (*PollSubjectChangesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(PollSubjectChangesResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_PollSubjectChanges_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) RegisterResource(ctx context.Context, in *RegisterResourceRequest, opts ...grpc.CallOption) (*RegisterResourceResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RegisterResourceResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_RegisterResource_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) UnregisterResource(ctx context.Context, in *UnregisterResourceRequest, opts ...grpc.CallOption) (*UnregisterResourceResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(UnregisterResourceResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_UnregisterResource_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) ResolveBasicCredential(ctx context.Context, in *ResolveBasicCredentialRequest, opts ...grpc.CallOption) (*ResolveBasicCredentialResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ResolveBasicCredentialResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_ResolveBasicCredential_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) GetRoleCompiled(ctx context.Context, in *GetRoleCompiledRequest, opts ...grpc.CallOption) (*GetRoleCompiledResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetRoleCompiledResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_GetRoleCompiled_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *internalIAMServiceClient) CheckBasicCredentialLive(ctx context.Context, in *CheckBasicCredentialLiveRequest, opts ...grpc.CallOption) (*CheckBasicCredentialLiveResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CheckBasicCredentialLiveResponse)
	err := c.cc.Invoke(ctx, InternalIAMService_CheckBasicCredentialLive_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// InternalIAMServiceServer is the server API for InternalIAMService service.
// All implementations must embed UnimplementedInternalIAMServiceServer
// for forward compatibility.
//
// InternalIAMService — cluster-internal RPCs (port 9091).
//
// НЕ публикуется на external TLS listener. Регистрируется в api-gateway internal mux
// (внутренний listener) — нужен auth-interceptor api-gateway (звать LookupSubject
// из interceptor'а после валидации JWT (Ory Hydra)).
//
// Вызывается из admin-tooling (grpcurl) и authz-interceptor api-gateway.
type InternalIAMServiceServer interface {
	// Резолв subject'а по одному из ключей (external_id / id / email).
	//
	// REST exposed ONLY on the cluster-internal listener;
	// api-gateway isInternalPath() gates `/iam/v1/internal/*` to internal mux
	// and authz-public-allowlist permits the <exempt> catalog entry.
	LookupSubject(context.Context, *LookupSubjectRequest) (*LookupSubjectResponse, error)
	// Check — single-tuple authorization check.
	// Вердикт о паре «субъект — отношение — объект». Складывает его реляционная
	// форма в собственной базе iam: внешний движок отношений, поверх которого этот
	// метод стоял прежде, снят стадией S6 эпика #747.
	// Используется per-service authz-interceptor'ами kacho-vpc / kacho-compute /
	// kacho-loadbalancer / self (kaname) для проверки прав на write/read RPC.
	// Latency budget: ≤20ms p95. Сетевого перехода к соседу больше нет — вопрос
	// задаётся той же базе, что и остальные чтения службы.
	//
	// REST exposed ONLY on the cluster-internal listener.
	Check(context.Context, *CheckRequest) (*CheckResponse, error)
	// admin force-logout of a user. Writes
	// `session_revocations` rows for every active access token (enumerated via
	// Hydra introspection) + a `caep_outbox` row so federated downstream RPs
	// get notified. Async: returns Operation; the LISTEN/NOTIFY fan-out then
	// updates every api-gateway pod's revocation cache ≤ 1s.
	ForceLogout(context.Context, *ForceLogoutRequest) (*operation.Operation, error)
	// PollSubjectChanges drains subject_change_outbox by ascending id cursor,
	// over the window `(since_id, settled]` — never "everything above the cursor".
	// Internal-only (cluster-internal listener) — drives api-gateway authz
	// decision-cache invalidation.
	//
	// UNAVAILABLE while the journal has no settled position yet (cold start): the
	// caller retries on its next tick. This is a state, not a failure — the
	// settled flag is monotone and never withdrawn once raised.
	//
	// OUT_OF_RANGE when `since_id` sits BELOW the journal floor — the rows between
	// them have been removed and will never be delivered. The refusal carries
	// `google.rpc.ErrorInfo` with reason `SUBJECT_CHANGE_POSITION_LOST`, domain
	// `iam.kacho.cloud` and metadata `earliest_resumable_position` (decimal), and
	// the caller keys on that token, never on the prose of the message.
	//
	// The two refusals advise OPPOSITE actions and must never be merged. UNAVAILABLE
	// says "ask again from the same position"; OUT_OF_RANGE says "that retry will
	// never succeed — flush your decision cache, close your open streams and resume
	// from the named position". A caller that read the second as the first would
	// retry from a lost position forever, and its cache would keep answering from
	// rights that are already gone — silently.
	//
	// Why an explicit refusal exists at all: the read window is
	// `id > since_id AND id <= settled`, and a removed row simply does not appear
	// in it. The cursor rolls past it on the last row read, so "no rows" becomes
	// indistinguishable from "rows were swept" — an unapplied revocation nobody
	// observes. Until this refusal existed, sweeping the journal was impossible
	// by construction, not by preference.
	PollSubjectChanges(context.Context, *PollSubjectChangesRequest) (*PollSubjectChangesResponse, error)
	// RegisterResource — Internal FGA-proxy: apply an owner-hierarchy tuple
	// (subject holds `relation` on `object`) on behalf of the resource-owning
	// module. Called by the vpc/compute/nlb outbox-drainer over mTLS so
	// those modules никогда не ходят в FGA напрямую — только через IAM.
	//
	// ИДЕМПОТЕНТНО (контракт): повтор того же (subject_id, relation, object) →
	// gRPC OK, НЕ AlreadyExists. От этого зависит at-least-once outbox-retry —
	// drainer может ретраить безопасно.
	//
	// Sync unary (как Check), НЕ async через Operation: ретрай и at-least-once
	// обеспечивает сам drainer.
	//
	// authz: `<exempt>` на уровне permission-каталога (как все Internal IAM RPC).
	// Least-priv энфорсится в IAM-handler через ReBAC: mTLS client-cert →
	// ServiceAccount → relation `fga_writer` на системном объекте
	// `cluster:cluster_root`. Нет relation → PermissionDenied. cluster-internal
	// listener :9091 only — нет google.api.http.
	RegisterResource(context.Context, *RegisterResourceRequest) (*RegisterResourceResponse, error)
	// UnregisterResource — Internal FGA-proxy: снять owner-hierarchy tuple
	// (зеркально RegisterResource; вызывается drainer'ом при Delete ресурса).
	//
	// ИДЕМПОТЕНТНО (контракт): снятие отсутствующего/уже снятого tuple → gRPC OK,
	// НЕ NotFound — drainer ретраит безопасно.
	//
	// Sync unary, НЕ Operation. authz: `<exempt>` + ReBAC `fga_writer` @
	// `cluster:cluster_root` (см. RegisterResource). cluster-internal :9091 only,
	// нет google.api.http.
	UnregisterResource(context.Context, *UnregisterResourceRequest) (*UnregisterResourceResponse, error)
	// GetRoleCompiled — Internal two-projection read (redesign-2026 F5): returns a
	// Role's COMPILED permission set (`module.resource.resourceName.verb` 4-segment
	// form) that backs FGA emission. This authz-topology projection is Internal-ONLY
	// (:9091) — the public RoleService.Get/List never carry compiled `permissions`
	// (they expose only authored `rules[]`). A label-only role compiles to an empty
	// set (all rules ARM_LABELS are not compiled). cluster-internal listener only —
	// no google.api.http; admin-UI authz gating happens at the internal mux layer.
	// ResolveBasicCredential — АВТОРИТЕТ О ПРЕДЪЯВЛЕННОМ БАЗОВОМ СЕКРЕТЕ
	// (задача #1142, приёмка BAT-1 §5.3, §6).
	//
	// Край зовёт этот глагол на промахе своего кэша вердикта и получает ответ на
	// ОДИН вопрос: годно ли предъявленное СЕЙЧАС и чей это принципал.
	//
	// ОТЗЫВ ДОХОДИТ ДО ПРЕДЪЯВЛЕНИЯ КОНСТРУКЦИЕЙ, А НЕ ВТОРЫМ МЕХАНИЗМОМ. Отзыв
	// есть СНЯТИЕ строки; резолв ищет строку по первичному ключу, и предикат его
	// единственного оператора включает существование строки, вид, непросроченность
	// и активность владельца. Нет строки — нет удостоверения. Поэтому под отзыв
	// попадают и поводы, о которых глагол отзыва не знает: снятие владельца,
	// снятие участия, перевод владельца в неактивное состояние — повод привязан к
	// САМОМУ поводу, а не к перечню обязанных писать.
	//
	// ОТКАЗ ЕДИНЫЙ. Неизвестный идентификатор, неверный секрет, истёкший срок,
	// отозванное удостоверение, неактивный владелец — ОДИН И ТОТ ЖЕ код и ОДИН И
	// ТОТ ЖЕ текст. Различимый текст есть оракул: по нему отличают «нет такого» от
	// «есть, но не ваш». Различимость живёт ВНУТРЬ — в счётчиках по причинам.
	//
	// REST exposed ONLY on the cluster-internal listener.
	ResolveBasicCredential(context.Context, *ResolveBasicCredentialRequest) (*ResolveBasicCredentialResponse, error)
	GetRoleCompiled(context.Context, *GetRoleCompiledRequest) (*GetRoleCompiledResponse, error)
	// CheckBasicCredentialLive — ЖИВО ЛИ УДОСТОВЕРЕНИЕ, СПРОШЕННОЕ ПО ЕГО
	// ИДЕНТИФИКАТОРУ, БЕЗ ПРЕДЪЯВЛЕНИЯ СЕКРЕТА (задача #1450).
	//
	// ЗАЧЕМ ВТОРОЙ ВОПРОС, ЕСЛИ РЯДОМ ЕСТЬ `ResolveBasicCredential`. Тот отвечает
	// ПРЕДЪЯВИТЕЛЮ и потому требует самой предъявленной строки. Спрашивающий с
	// ОТКРЫТОГО соединения предъявителем не является: секрет он видел однажды, при
	// открытии, и держать его живым весь срок соединения значило бы завести
	// поверхность хранения ради контроля — то есть платить за отзыв тем самым, что
	// отзыв защищает.
	//
	// Поэтому вопрос задаётся по идентификатору СТРОКИ удостоверения. Предикат
	// живости у него ТОТ ЖЕ, что у резолва: существование строки, вид `SECRET`,
	// непросроченность и активность владельца. Разойтись они не могут — оператор
	// один, и сверка хеша в нём не участвует by construction.
	//
	// ОТВЕТ — ОТКАЗ, А НЕ ПОЛЕ. Живое удостоверение даёт `OK`, неживое —
	// `UNAUTHENTICATED` с тем же единым текстом, что у резолва; «спросить не
	// удалось» — `UNAVAILABLE`. Поле-признак означало бы, что вызывающий вправе его
	// не прочитать, и тогда контроль присутствует, провязан и не отказывает ни разу.
	// Ровно тот довод, что записан у `ResolveBasicCredentialResponse`.
	//
	// ОТКАЗ ЕДИНЫЙ И ЗДЕСЬ. Неизвестный идентификатор, истёкший срок, отозванное
	// удостоверение, неактивный владелец — один код и один текст: различимый
	// отличал бы «нет такого» от «есть, но не действует».
	//
	// БЕЗ `google.api.http` НАМЕРЕННО — как у `GetRoleCompiled`. Вызывающий ходит
	// gRPC; дружелюбного пути под этот вопрос не заводится, чтобы его нельзя было
	// выставить наружу опечаткой в таблице маршрутов края.
	CheckBasicCredentialLive(context.Context, *CheckBasicCredentialLiveRequest) (*CheckBasicCredentialLiveResponse, error)
	mustEmbedUnimplementedInternalIAMServiceServer()
}

// UnimplementedInternalIAMServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedInternalIAMServiceServer struct{}

func (UnimplementedInternalIAMServiceServer) LookupSubject(context.Context, *LookupSubjectRequest) (*LookupSubjectResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method LookupSubject not implemented")
}
func (UnimplementedInternalIAMServiceServer) Check(context.Context, *CheckRequest) (*CheckResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Check not implemented")
}
func (UnimplementedInternalIAMServiceServer) ForceLogout(context.Context, *ForceLogoutRequest) (*operation.Operation, error) {
	return nil, status.Error(codes.Unimplemented, "method ForceLogout not implemented")
}
func (UnimplementedInternalIAMServiceServer) PollSubjectChanges(context.Context, *PollSubjectChangesRequest) (*PollSubjectChangesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method PollSubjectChanges not implemented")
}
func (UnimplementedInternalIAMServiceServer) RegisterResource(context.Context, *RegisterResourceRequest) (*RegisterResourceResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RegisterResource not implemented")
}
func (UnimplementedInternalIAMServiceServer) UnregisterResource(context.Context, *UnregisterResourceRequest) (*UnregisterResourceResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method UnregisterResource not implemented")
}
func (UnimplementedInternalIAMServiceServer) ResolveBasicCredential(context.Context, *ResolveBasicCredentialRequest) (*ResolveBasicCredentialResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ResolveBasicCredential not implemented")
}
func (UnimplementedInternalIAMServiceServer) GetRoleCompiled(context.Context, *GetRoleCompiledRequest) (*GetRoleCompiledResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetRoleCompiled not implemented")
}
func (UnimplementedInternalIAMServiceServer) CheckBasicCredentialLive(context.Context, *CheckBasicCredentialLiveRequest) (*CheckBasicCredentialLiveResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method CheckBasicCredentialLive not implemented")
}
func (UnimplementedInternalIAMServiceServer) mustEmbedUnimplementedInternalIAMServiceServer() {}
func (UnimplementedInternalIAMServiceServer) testEmbeddedByValue()                            {}

// UnsafeInternalIAMServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to InternalIAMServiceServer will
// result in compilation errors.
type UnsafeInternalIAMServiceServer interface {
	mustEmbedUnimplementedInternalIAMServiceServer()
}

func RegisterInternalIAMServiceServer(s grpc.ServiceRegistrar, srv InternalIAMServiceServer) {
	// If the following call panics, it indicates UnimplementedInternalIAMServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&InternalIAMService_ServiceDesc, srv)
}

func _InternalIAMService_LookupSubject_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(LookupSubjectRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).LookupSubject(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_LookupSubject_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).LookupSubject(ctx, req.(*LookupSubjectRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_Check_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CheckRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).Check(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_Check_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).Check(ctx, req.(*CheckRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_ForceLogout_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ForceLogoutRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).ForceLogout(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_ForceLogout_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).ForceLogout(ctx, req.(*ForceLogoutRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_PollSubjectChanges_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(PollSubjectChangesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).PollSubjectChanges(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_PollSubjectChanges_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).PollSubjectChanges(ctx, req.(*PollSubjectChangesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_RegisterResource_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RegisterResourceRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).RegisterResource(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_RegisterResource_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).RegisterResource(ctx, req.(*RegisterResourceRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_UnregisterResource_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(UnregisterResourceRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).UnregisterResource(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_UnregisterResource_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).UnregisterResource(ctx, req.(*UnregisterResourceRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_ResolveBasicCredential_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ResolveBasicCredentialRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).ResolveBasicCredential(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_ResolveBasicCredential_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).ResolveBasicCredential(ctx, req.(*ResolveBasicCredentialRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_GetRoleCompiled_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetRoleCompiledRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).GetRoleCompiled(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_GetRoleCompiled_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).GetRoleCompiled(ctx, req.(*GetRoleCompiledRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InternalIAMService_CheckBasicCredentialLive_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CheckBasicCredentialLiveRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalIAMServiceServer).CheckBasicCredentialLive(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalIAMService_CheckBasicCredentialLive_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalIAMServiceServer).CheckBasicCredentialLive(ctx, req.(*CheckBasicCredentialLiveRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// InternalIAMService_ServiceDesc is the grpc.ServiceDesc for InternalIAMService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var InternalIAMService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "kaname.cloud.iam.v1.InternalIAMService",
	HandlerType: (*InternalIAMServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "LookupSubject",
			Handler:    _InternalIAMService_LookupSubject_Handler,
		},
		{
			MethodName: "Check",
			Handler:    _InternalIAMService_Check_Handler,
		},
		{
			MethodName: "ForceLogout",
			Handler:    _InternalIAMService_ForceLogout_Handler,
		},
		{
			MethodName: "PollSubjectChanges",
			Handler:    _InternalIAMService_PollSubjectChanges_Handler,
		},
		{
			MethodName: "RegisterResource",
			Handler:    _InternalIAMService_RegisterResource_Handler,
		},
		{
			MethodName: "UnregisterResource",
			Handler:    _InternalIAMService_UnregisterResource_Handler,
		},
		{
			MethodName: "ResolveBasicCredential",
			Handler:    _InternalIAMService_ResolveBasicCredential_Handler,
		},
		{
			MethodName: "GetRoleCompiled",
			Handler:    _InternalIAMService_GetRoleCompiled_Handler,
		},
		{
			MethodName: "CheckBasicCredentialLive",
			Handler:    _InternalIAMService_CheckBasicCredentialLive_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "kaname/cloud/iam/v1/internal_iam_service.proto",
}
