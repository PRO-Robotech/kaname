// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: BUSL-1.1

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: kacho/cloud/iam/v1/permission_catalog_service.proto

package iamv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	PermissionCatalogService_ListPermissionCatalog_FullMethodName = "/kacho.cloud.iam.v1.PermissionCatalogService/ListPermissionCatalog"
)

// PermissionCatalogServiceClient is the client API for PermissionCatalogService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// PermissionCatalogService — the grantable-token catalog for backend-driven
// role-rule editor dropdowns.
//
// This is the HONEST, public, sync-read taxonomy that the role-rule editor
// (kacho-ui RulesEditor) loads at runtime instead of carrying a hand-maintained
// copy of the backend taxonomy. It answers "which (module, resource) tokens are
// grantable, which verbs are closed, and what is the wildcard policy" — the
// metadata the editor needs to render `modules → resources`, the verb set, and
// the wildcard affordances.
//
// Ground-truth (the catalog mirrors EXACTLY this — no more, no less):
//   - kacho-iam `internal/authzmap` → `objectTypes` (the closed
//     `(module,resource) → FGA object_type` table; extending it requires a
//     lockstep edit of `fga_model.fga`) + `TypeHasVerbRelations(fgaType)`
//     (verb-bearing leaf vs tier-only ancestor);
//   - kacho-iam `internal/domain.ClosedVerbs` = {get,list,create,update,delete}
//     (+ the verb-`*` bounded expansion).
//
// The rule compiler fail-closed-SKIPs any `(module,resource)` NOT in
// `objectTypes` (the grant is a no-op), so such tokens never appear here.
//
// This service is DISTINCT from `InternalIAMService.ListPermissions` (which is
// removed): it is PUBLIC (tenant UI, authenticated-floor), carries rule-tokens
// `(module,resource)` rather than compiled `module.resource.verb` permission
// strings, and takes NO subject. Per-subject "who can do X" is answered by
// `AccessBindingService.ExpandAccess`.
//
// The catalog carries NO compiled `permissions[]` and NO FGA-relation
// names (`v_*`/`scope_grant`/`sg_*`) — only tenant-facing grantable tokens plus
// the editor/policy flags.
type PermissionCatalogServiceClient interface {
	// ListPermissionCatalog returns the whole grantable taxonomy in one sync read
	// (NOT an Operation — reads are sync). Takes no payload:
	// the catalog is one platform-wide taxonomy, immutable at runtime (it grows
	// only with a new backend release that extends `objectTypes` lockstep with
	// `fga_model.fga`), and is NOT scope-filtered per tenant. The UI caches it
	// (react-query).
	ListPermissionCatalog(ctx context.Context, in *ListPermissionCatalogRequest, opts ...grpc.CallOption) (*ListPermissionCatalogResponse, error)
}

type permissionCatalogServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewPermissionCatalogServiceClient(cc grpc.ClientConnInterface) PermissionCatalogServiceClient {
	return &permissionCatalogServiceClient{cc}
}

func (c *permissionCatalogServiceClient) ListPermissionCatalog(ctx context.Context, in *ListPermissionCatalogRequest, opts ...grpc.CallOption) (*ListPermissionCatalogResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListPermissionCatalogResponse)
	err := c.cc.Invoke(ctx, PermissionCatalogService_ListPermissionCatalog_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// PermissionCatalogServiceServer is the server API for PermissionCatalogService service.
// All implementations must embed UnimplementedPermissionCatalogServiceServer
// for forward compatibility.
//
// PermissionCatalogService — the grantable-token catalog for backend-driven
// role-rule editor dropdowns.
//
// This is the HONEST, public, sync-read taxonomy that the role-rule editor
// (kacho-ui RulesEditor) loads at runtime instead of carrying a hand-maintained
// copy of the backend taxonomy. It answers "which (module, resource) tokens are
// grantable, which verbs are closed, and what is the wildcard policy" — the
// metadata the editor needs to render `modules → resources`, the verb set, and
// the wildcard affordances.
//
// Ground-truth (the catalog mirrors EXACTLY this — no more, no less):
//   - kacho-iam `internal/authzmap` → `objectTypes` (the closed
//     `(module,resource) → FGA object_type` table; extending it requires a
//     lockstep edit of `fga_model.fga`) + `TypeHasVerbRelations(fgaType)`
//     (verb-bearing leaf vs tier-only ancestor);
//   - kacho-iam `internal/domain.ClosedVerbs` = {get,list,create,update,delete}
//     (+ the verb-`*` bounded expansion).
//
// The rule compiler fail-closed-SKIPs any `(module,resource)` NOT in
// `objectTypes` (the grant is a no-op), so such tokens never appear here.
//
// This service is DISTINCT from `InternalIAMService.ListPermissions` (which is
// removed): it is PUBLIC (tenant UI, authenticated-floor), carries rule-tokens
// `(module,resource)` rather than compiled `module.resource.verb` permission
// strings, and takes NO subject. Per-subject "who can do X" is answered by
// `AccessBindingService.ExpandAccess`.
//
// The catalog carries NO compiled `permissions[]` and NO FGA-relation
// names (`v_*`/`scope_grant`/`sg_*`) — only tenant-facing grantable tokens plus
// the editor/policy flags.
type PermissionCatalogServiceServer interface {
	// ListPermissionCatalog returns the whole grantable taxonomy in one sync read
	// (NOT an Operation — reads are sync). Takes no payload:
	// the catalog is one platform-wide taxonomy, immutable at runtime (it grows
	// only with a new backend release that extends `objectTypes` lockstep with
	// `fga_model.fga`), and is NOT scope-filtered per tenant. The UI caches it
	// (react-query).
	ListPermissionCatalog(context.Context, *ListPermissionCatalogRequest) (*ListPermissionCatalogResponse, error)
	mustEmbedUnimplementedPermissionCatalogServiceServer()
}

// UnimplementedPermissionCatalogServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedPermissionCatalogServiceServer struct{}

func (UnimplementedPermissionCatalogServiceServer) ListPermissionCatalog(context.Context, *ListPermissionCatalogRequest) (*ListPermissionCatalogResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListPermissionCatalog not implemented")
}
func (UnimplementedPermissionCatalogServiceServer) mustEmbedUnimplementedPermissionCatalogServiceServer() {
}
func (UnimplementedPermissionCatalogServiceServer) testEmbeddedByValue() {}

// UnsafePermissionCatalogServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to PermissionCatalogServiceServer will
// result in compilation errors.
type UnsafePermissionCatalogServiceServer interface {
	mustEmbedUnimplementedPermissionCatalogServiceServer()
}

func RegisterPermissionCatalogServiceServer(s grpc.ServiceRegistrar, srv PermissionCatalogServiceServer) {
	// If the following call panics, it indicates UnimplementedPermissionCatalogServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&PermissionCatalogService_ServiceDesc, srv)
}

func _PermissionCatalogService_ListPermissionCatalog_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListPermissionCatalogRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(PermissionCatalogServiceServer).ListPermissionCatalog(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: PermissionCatalogService_ListPermissionCatalog_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(PermissionCatalogServiceServer).ListPermissionCatalog(ctx, req.(*ListPermissionCatalogRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// PermissionCatalogService_ServiceDesc is the grpc.ServiceDesc for PermissionCatalogService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var PermissionCatalogService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "kacho.cloud.iam.v1.PermissionCatalogService",
	HandlerType: (*PermissionCatalogServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "ListPermissionCatalog",
			Handler:    _PermissionCatalogService_ListPermissionCatalog_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "kacho/cloud/iam/v1/permission_catalog_service.proto",
}
