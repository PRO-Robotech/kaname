// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: BUSL-1.1

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: kacho/cloud/iam/v1/access_binding_service.proto

package iamv1

import (
	context "context"
	operation "github.com/PRO-Robotech/kacho-corelib/proto/gen/go/kacho/cloud/operation"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AccessBindingService_Get_FullMethodName                   = "/kacho.cloud.iam.v1.AccessBindingService/Get"
	AccessBindingService_Create_FullMethodName                = "/kacho.cloud.iam.v1.AccessBindingService/Create"
	AccessBindingService_Delete_FullMethodName                = "/kacho.cloud.iam.v1.AccessBindingService/Delete"
	AccessBindingService_Update_FullMethodName                = "/kacho.cloud.iam.v1.AccessBindingService/Update"
	AccessBindingService_ListByScope_FullMethodName           = "/kacho.cloud.iam.v1.AccessBindingService/ListByScope"
	AccessBindingService_ListBySubject_FullMethodName         = "/kacho.cloud.iam.v1.AccessBindingService/ListBySubject"
	AccessBindingService_ListSubjectPrivileges_FullMethodName = "/kacho.cloud.iam.v1.AccessBindingService/ListSubjectPrivileges"
	AccessBindingService_ListAssignableRoles_FullMethodName   = "/kacho.cloud.iam.v1.AccessBindingService/ListAssignableRoles"
	AccessBindingService_ListByRole_FullMethodName            = "/kacho.cloud.iam.v1.AccessBindingService/ListByRole"
	AccessBindingService_ExpandAccess_FullMethodName          = "/kacho.cloud.iam.v1.AccessBindingService/ExpandAccess"
	AccessBindingService_ListByAccount_FullMethodName         = "/kacho.cloud.iam.v1.AccessBindingService/ListByAccount"
	AccessBindingService_ListOperations_FullMethodName        = "/kacho.cloud.iam.v1.AccessBindingService/ListOperations"
)

// AccessBindingServiceClient is the client API for AccessBindingService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// A set of methods for managing AccessBinding resources.
//
// kacho-iam ведет собственный ресурс AccessBinding в пакете
// kacho.cloud.iam.v1 — это канонический AccessBinding домена IAM,
// единственный источник истины для grant-привязок.
type AccessBindingServiceClient interface {
	// Returns the specified AccessBinding resource.
	Get(ctx context.Context, in *GetAccessBindingRequest, opts ...grpc.CallOption) (*AccessBinding, error)
	// Creates an access binding.
	// Идемпотентный INSERT — дубль (subject_type, subject_id, role_id, resource_type,
	// resource_id) возвращает existing.
	Create(ctx context.Context, in *CreateAccessBindingRequest, opts ...grpc.CallOption) (*operation.Operation, error)
	// Deletes the specified access binding.
	Delete(ctx context.Context, in *DeleteAccessBindingRequest, opts ...grpc.CallOption) (*operation.Operation, error)
	// Updates mutable fields of the specified access binding. The ONLY mutable
	// field is `deletion_protection`:
	// an owner / admin clears the flag (`update_mask=["deletion_protection"]`,
	// `deletion_protection=false`) so a protected binding can subsequently be
	// deleted. All other fields are immutable (AccessBinding is otherwise
	// Delete+Create); an `update_mask` referencing any non-mutable field →
	// INVALID_ARGUMENT. Async (returns Operation), like the other mutations.
	Update(ctx context.Context, in *UpdateAccessBindingRequest, opts ...grpc.CallOption) (*operation.Operation, error)
	// Lists access bindings attached to the specified scope (renamed from the
	// legacy `ListByResource`, whose wire-name is removed). The
	// `resource_type`/`resource_id` request pair names the scope anchor
	// (project|account|cluster).
	ListByScope(ctx context.Context, in *ListAccessBindingsByScopeRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error)
	// Lists access bindings assigned to the specified subject. Cluster-scoped
	// gate: any authenticated user can call (handler then filters returned
	// bindings — typical pattern is self-list, so subject=principal yields the
	// user's own bindings; cross-subject reads still require admin role on
	// each binding's target resource via per-row filter in the handler).
	ListBySubject(ctx context.Context, in *ListAccessBindingsBySubjectRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error)
	// Lists the privileges (enriched access bindings) attached to the specified
	// subject (User or ServiceAccount). Sync read (NOT an Operation). Unlike
	// `ListBySubject` (strict self-list, raw bindings, no existence-check), this
	// RPC:
	//   - resolves the human-readable `role_name` server-side via a within-DB
	//     JOIN `access_bindings ⋈ roles` (no N+1 client round-trips);
	//   - broadens authz to "self OR account-admin of the subject's home
	//     account" — caller may view another member's privileges if they
	//     own/admin the subject's home Account; cross-account reads → 403;
	//   - returns NOT_FOUND for a well-formed-but-missing subject
	//     (existence-resolve is required for authz);
	//   - is DIRECT-only in v1 (`derivation=DIRECT`); GROUP-derived (effective)
	//     roles are reserved forward-compat.
	//
	// Cluster-scoped catalog gate (anti-anon + ACR floor, parity with
	// `ListBySubject`); the precise self/account-admin policy is enforced
	// authoritatively in the kacho-iam handler. `subject_type ∈
	// {"user","service_account"}` for v1; malformed subject_id /
	// prefix↔type mismatch → INVALID_ARGUMENT.
	ListSubjectPrivileges(ctx context.Context, in *ListSubjectPrivilegesRequest, opts ...grpc.CallOption) (*ListSubjectPrivilegesResponse, error)
	// Lists the roles that are assignable on the specified resource.
	// Sync read (NOT an Operation). Returns ONLY the roles that
	// `AccessBinding.Create` would accept on (resource_type, resource_id) — the
	// single `isRoleAssignable` predicate is shared by this RPC (WHERE-filter)
	// and by Create-enforcement, so "what can be shown" == "what can be
	// created" (no bypass via direct API). Each returned `AssignableRole` is
	// annotated with a server-computed `scope_group` (SYSTEM / ACCOUNT /
	// PROJECT) so the UI groups without any client-side scope logic.
	//
	//   - `resource_type ∈ {"account","project","cluster"}` for v1; otherwise
	//     INVALID_ARGUMENT. `resource_id` must match the type's prefix
	//     (account⇒acc, project⇒prj; cluster⇒exactly cluster_kacho_root) —
	//     mismatch → INVALID_ARGUMENT.
	//   - well-formed-but-missing resource → NOT_FOUND (existence-resolve is
	//     required for authz); existing resource with 0 assignable roles →
	//     empty `roles` (not an error).
	//   - lean picker: `AssignableRole` carries no `permissions`.
	//
	// Scope-polymorphic catalog gate (anti-anon + ACR floor, parity with
	// `ListByScope`); the precise grant-authority policy is enforced
	// authoritatively in the kacho-iam handler via `requireGrantAuthority`
	// (owner of the owning Account/Project OR FGA `admin` on the scope object) —
	// same gate as `AccessBinding.Create` on this resource.
	ListAssignableRoles(ctx context.Context, in *ListAssignableRolesRequest, opts ...grpc.CallOption) (*ListAssignableRolesResponse, error)
	// Lists the access bindings that carry a given Role (audit "who holds role R").
	// Sync read (NOT an Operation).
	// Each returned `AccessBinding` is the full dual-projection: both the
	// new `subjects[]`/`scope_ref` and the legacy `subject_type`/`subject_id`/
	// `resource_type`/`resource_id`/`scope`. Scope-filtered like the other List
	// RPCs — a caller sees only bindings on scope objects they may read.
	//
	// Cluster-scoped catalog gate (anti-anon + ACR floor, parity with
	// `ListBySubject`); the precise grant-authority/admin policy is enforced
	// authoritatively in the kacho-iam handler (only a grant-authority holder /
	// admin on each binding's scope sees the row). Ordered by (created_at, id)
	// ASC; keyset paginated.
	ListByRole(ctx context.Context, in *ListAccessBindingsByRoleRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error)
	// Expands a userset on an object+relation into the concrete principals that
	// hold it. Sync read (NOT an
	// Operation). Answers "who can actually do X on this object" by resolving FGA
	// group-membership usersets (group → members) to concrete USER/SERVICE_ACCOUNT
	// principals — closing the effective-principal audit gap (a binding on a GROUP
	// subject otherwise only shows "group G", not its current members). Read-only,
	// viewer-floor gate.
	//
	// Cluster-scoped catalog gate (anti-anon + ACR floor); the precise read policy
	// (viewer on the queried object) is enforced authoritatively in the kacho-iam
	// handler.
	ExpandAccess(ctx context.Context, in *ExpandAccessRequest, opts ...grpc.CallOption) (*ExpandAccessResponse, error)
	// Lists access bindings in an account (all subjects). Requires `admin`
	// relation on the account (account-admin sees all who has access to the
	// account, including subjects he didn't grant himself). Returns:
	//
	//   - bindings directly attached to the account (resource_type='account',
	//     resource_id=<accountId>); plus
	//   - bindings on every project whose account_id matches.
	//
	// Filter by subject_type (optional) and include_revoked (default false).
	// Ordered by (created_at DESC, id ASC); keyset paginated.
	ListByAccount(ctx context.Context, in *ListAccessBindingsByAccountRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error)
	// Lists operations for the specified access binding.
	ListOperations(ctx context.Context, in *ListAccessBindingOperationsRequest, opts ...grpc.CallOption) (*ListAccessBindingOperationsResponse, error)
}

type accessBindingServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAccessBindingServiceClient(cc grpc.ClientConnInterface) AccessBindingServiceClient {
	return &accessBindingServiceClient{cc}
}

func (c *accessBindingServiceClient) Get(ctx context.Context, in *GetAccessBindingRequest, opts ...grpc.CallOption) (*AccessBinding, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(AccessBinding)
	err := c.cc.Invoke(ctx, AccessBindingService_Get_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) Create(ctx context.Context, in *CreateAccessBindingRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(operation.Operation)
	err := c.cc.Invoke(ctx, AccessBindingService_Create_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) Delete(ctx context.Context, in *DeleteAccessBindingRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(operation.Operation)
	err := c.cc.Invoke(ctx, AccessBindingService_Delete_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) Update(ctx context.Context, in *UpdateAccessBindingRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(operation.Operation)
	err := c.cc.Invoke(ctx, AccessBindingService_Update_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ListByScope(ctx context.Context, in *ListAccessBindingsByScopeRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListAccessBindingsResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ListByScope_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ListBySubject(ctx context.Context, in *ListAccessBindingsBySubjectRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListAccessBindingsResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ListBySubject_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ListSubjectPrivileges(ctx context.Context, in *ListSubjectPrivilegesRequest, opts ...grpc.CallOption) (*ListSubjectPrivilegesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListSubjectPrivilegesResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ListSubjectPrivileges_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ListAssignableRoles(ctx context.Context, in *ListAssignableRolesRequest, opts ...grpc.CallOption) (*ListAssignableRolesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListAssignableRolesResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ListAssignableRoles_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ListByRole(ctx context.Context, in *ListAccessBindingsByRoleRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListAccessBindingsResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ListByRole_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ExpandAccess(ctx context.Context, in *ExpandAccessRequest, opts ...grpc.CallOption) (*ExpandAccessResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ExpandAccessResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ExpandAccess_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ListByAccount(ctx context.Context, in *ListAccessBindingsByAccountRequest, opts ...grpc.CallOption) (*ListAccessBindingsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListAccessBindingsResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ListByAccount_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *accessBindingServiceClient) ListOperations(ctx context.Context, in *ListAccessBindingOperationsRequest, opts ...grpc.CallOption) (*ListAccessBindingOperationsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListAccessBindingOperationsResponse)
	err := c.cc.Invoke(ctx, AccessBindingService_ListOperations_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AccessBindingServiceServer is the server API for AccessBindingService service.
// All implementations must embed UnimplementedAccessBindingServiceServer
// for forward compatibility.
//
// A set of methods for managing AccessBinding resources.
//
// kacho-iam ведет собственный ресурс AccessBinding в пакете
// kacho.cloud.iam.v1 — это канонический AccessBinding домена IAM,
// единственный источник истины для grant-привязок.
type AccessBindingServiceServer interface {
	// Returns the specified AccessBinding resource.
	Get(context.Context, *GetAccessBindingRequest) (*AccessBinding, error)
	// Creates an access binding.
	// Идемпотентный INSERT — дубль (subject_type, subject_id, role_id, resource_type,
	// resource_id) возвращает existing.
	Create(context.Context, *CreateAccessBindingRequest) (*operation.Operation, error)
	// Deletes the specified access binding.
	Delete(context.Context, *DeleteAccessBindingRequest) (*operation.Operation, error)
	// Updates mutable fields of the specified access binding. The ONLY mutable
	// field is `deletion_protection`:
	// an owner / admin clears the flag (`update_mask=["deletion_protection"]`,
	// `deletion_protection=false`) so a protected binding can subsequently be
	// deleted. All other fields are immutable (AccessBinding is otherwise
	// Delete+Create); an `update_mask` referencing any non-mutable field →
	// INVALID_ARGUMENT. Async (returns Operation), like the other mutations.
	Update(context.Context, *UpdateAccessBindingRequest) (*operation.Operation, error)
	// Lists access bindings attached to the specified scope (renamed from the
	// legacy `ListByResource`, whose wire-name is removed). The
	// `resource_type`/`resource_id` request pair names the scope anchor
	// (project|account|cluster).
	ListByScope(context.Context, *ListAccessBindingsByScopeRequest) (*ListAccessBindingsResponse, error)
	// Lists access bindings assigned to the specified subject. Cluster-scoped
	// gate: any authenticated user can call (handler then filters returned
	// bindings — typical pattern is self-list, so subject=principal yields the
	// user's own bindings; cross-subject reads still require admin role on
	// each binding's target resource via per-row filter in the handler).
	ListBySubject(context.Context, *ListAccessBindingsBySubjectRequest) (*ListAccessBindingsResponse, error)
	// Lists the privileges (enriched access bindings) attached to the specified
	// subject (User or ServiceAccount). Sync read (NOT an Operation). Unlike
	// `ListBySubject` (strict self-list, raw bindings, no existence-check), this
	// RPC:
	//   - resolves the human-readable `role_name` server-side via a within-DB
	//     JOIN `access_bindings ⋈ roles` (no N+1 client round-trips);
	//   - broadens authz to "self OR account-admin of the subject's home
	//     account" — caller may view another member's privileges if they
	//     own/admin the subject's home Account; cross-account reads → 403;
	//   - returns NOT_FOUND for a well-formed-but-missing subject
	//     (existence-resolve is required for authz);
	//   - is DIRECT-only in v1 (`derivation=DIRECT`); GROUP-derived (effective)
	//     roles are reserved forward-compat.
	//
	// Cluster-scoped catalog gate (anti-anon + ACR floor, parity with
	// `ListBySubject`); the precise self/account-admin policy is enforced
	// authoritatively in the kacho-iam handler. `subject_type ∈
	// {"user","service_account"}` for v1; malformed subject_id /
	// prefix↔type mismatch → INVALID_ARGUMENT.
	ListSubjectPrivileges(context.Context, *ListSubjectPrivilegesRequest) (*ListSubjectPrivilegesResponse, error)
	// Lists the roles that are assignable on the specified resource.
	// Sync read (NOT an Operation). Returns ONLY the roles that
	// `AccessBinding.Create` would accept on (resource_type, resource_id) — the
	// single `isRoleAssignable` predicate is shared by this RPC (WHERE-filter)
	// and by Create-enforcement, so "what can be shown" == "what can be
	// created" (no bypass via direct API). Each returned `AssignableRole` is
	// annotated with a server-computed `scope_group` (SYSTEM / ACCOUNT /
	// PROJECT) so the UI groups without any client-side scope logic.
	//
	//   - `resource_type ∈ {"account","project","cluster"}` for v1; otherwise
	//     INVALID_ARGUMENT. `resource_id` must match the type's prefix
	//     (account⇒acc, project⇒prj; cluster⇒exactly cluster_kacho_root) —
	//     mismatch → INVALID_ARGUMENT.
	//   - well-formed-but-missing resource → NOT_FOUND (existence-resolve is
	//     required for authz); existing resource with 0 assignable roles →
	//     empty `roles` (not an error).
	//   - lean picker: `AssignableRole` carries no `permissions`.
	//
	// Scope-polymorphic catalog gate (anti-anon + ACR floor, parity with
	// `ListByScope`); the precise grant-authority policy is enforced
	// authoritatively in the kacho-iam handler via `requireGrantAuthority`
	// (owner of the owning Account/Project OR FGA `admin` on the scope object) —
	// same gate as `AccessBinding.Create` on this resource.
	ListAssignableRoles(context.Context, *ListAssignableRolesRequest) (*ListAssignableRolesResponse, error)
	// Lists the access bindings that carry a given Role (audit "who holds role R").
	// Sync read (NOT an Operation).
	// Each returned `AccessBinding` is the full dual-projection: both the
	// new `subjects[]`/`scope_ref` and the legacy `subject_type`/`subject_id`/
	// `resource_type`/`resource_id`/`scope`. Scope-filtered like the other List
	// RPCs — a caller sees only bindings on scope objects they may read.
	//
	// Cluster-scoped catalog gate (anti-anon + ACR floor, parity with
	// `ListBySubject`); the precise grant-authority/admin policy is enforced
	// authoritatively in the kacho-iam handler (only a grant-authority holder /
	// admin on each binding's scope sees the row). Ordered by (created_at, id)
	// ASC; keyset paginated.
	ListByRole(context.Context, *ListAccessBindingsByRoleRequest) (*ListAccessBindingsResponse, error)
	// Expands a userset on an object+relation into the concrete principals that
	// hold it. Sync read (NOT an
	// Operation). Answers "who can actually do X on this object" by resolving FGA
	// group-membership usersets (group → members) to concrete USER/SERVICE_ACCOUNT
	// principals — closing the effective-principal audit gap (a binding on a GROUP
	// subject otherwise only shows "group G", not its current members). Read-only,
	// viewer-floor gate.
	//
	// Cluster-scoped catalog gate (anti-anon + ACR floor); the precise read policy
	// (viewer on the queried object) is enforced authoritatively in the kacho-iam
	// handler.
	ExpandAccess(context.Context, *ExpandAccessRequest) (*ExpandAccessResponse, error)
	// Lists access bindings in an account (all subjects). Requires `admin`
	// relation on the account (account-admin sees all who has access to the
	// account, including subjects he didn't grant himself). Returns:
	//
	//   - bindings directly attached to the account (resource_type='account',
	//     resource_id=<accountId>); plus
	//   - bindings on every project whose account_id matches.
	//
	// Filter by subject_type (optional) and include_revoked (default false).
	// Ordered by (created_at DESC, id ASC); keyset paginated.
	ListByAccount(context.Context, *ListAccessBindingsByAccountRequest) (*ListAccessBindingsResponse, error)
	// Lists operations for the specified access binding.
	ListOperations(context.Context, *ListAccessBindingOperationsRequest) (*ListAccessBindingOperationsResponse, error)
	mustEmbedUnimplementedAccessBindingServiceServer()
}

// UnimplementedAccessBindingServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAccessBindingServiceServer struct{}

func (UnimplementedAccessBindingServiceServer) Get(context.Context, *GetAccessBindingRequest) (*AccessBinding, error) {
	return nil, status.Error(codes.Unimplemented, "method Get not implemented")
}
func (UnimplementedAccessBindingServiceServer) Create(context.Context, *CreateAccessBindingRequest) (*operation.Operation, error) {
	return nil, status.Error(codes.Unimplemented, "method Create not implemented")
}
func (UnimplementedAccessBindingServiceServer) Delete(context.Context, *DeleteAccessBindingRequest) (*operation.Operation, error) {
	return nil, status.Error(codes.Unimplemented, "method Delete not implemented")
}
func (UnimplementedAccessBindingServiceServer) Update(context.Context, *UpdateAccessBindingRequest) (*operation.Operation, error) {
	return nil, status.Error(codes.Unimplemented, "method Update not implemented")
}
func (UnimplementedAccessBindingServiceServer) ListByScope(context.Context, *ListAccessBindingsByScopeRequest) (*ListAccessBindingsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListByScope not implemented")
}
func (UnimplementedAccessBindingServiceServer) ListBySubject(context.Context, *ListAccessBindingsBySubjectRequest) (*ListAccessBindingsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListBySubject not implemented")
}
func (UnimplementedAccessBindingServiceServer) ListSubjectPrivileges(context.Context, *ListSubjectPrivilegesRequest) (*ListSubjectPrivilegesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListSubjectPrivileges not implemented")
}
func (UnimplementedAccessBindingServiceServer) ListAssignableRoles(context.Context, *ListAssignableRolesRequest) (*ListAssignableRolesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListAssignableRoles not implemented")
}
func (UnimplementedAccessBindingServiceServer) ListByRole(context.Context, *ListAccessBindingsByRoleRequest) (*ListAccessBindingsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListByRole not implemented")
}
func (UnimplementedAccessBindingServiceServer) ExpandAccess(context.Context, *ExpandAccessRequest) (*ExpandAccessResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ExpandAccess not implemented")
}
func (UnimplementedAccessBindingServiceServer) ListByAccount(context.Context, *ListAccessBindingsByAccountRequest) (*ListAccessBindingsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListByAccount not implemented")
}
func (UnimplementedAccessBindingServiceServer) ListOperations(context.Context, *ListAccessBindingOperationsRequest) (*ListAccessBindingOperationsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListOperations not implemented")
}
func (UnimplementedAccessBindingServiceServer) mustEmbedUnimplementedAccessBindingServiceServer() {}
func (UnimplementedAccessBindingServiceServer) testEmbeddedByValue()                              {}

// UnsafeAccessBindingServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AccessBindingServiceServer will
// result in compilation errors.
type UnsafeAccessBindingServiceServer interface {
	mustEmbedUnimplementedAccessBindingServiceServer()
}

func RegisterAccessBindingServiceServer(s grpc.ServiceRegistrar, srv AccessBindingServiceServer) {
	// If the following call panics, it indicates UnimplementedAccessBindingServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AccessBindingService_ServiceDesc, srv)
}

func _AccessBindingService_Get_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetAccessBindingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).Get(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_Get_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).Get(ctx, req.(*GetAccessBindingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_Create_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CreateAccessBindingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).Create(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_Create_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).Create(ctx, req.(*CreateAccessBindingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_Delete_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DeleteAccessBindingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).Delete(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_Delete_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).Delete(ctx, req.(*DeleteAccessBindingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_Update_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(UpdateAccessBindingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).Update(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_Update_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).Update(ctx, req.(*UpdateAccessBindingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ListByScope_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListAccessBindingsByScopeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ListByScope(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ListByScope_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ListByScope(ctx, req.(*ListAccessBindingsByScopeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ListBySubject_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListAccessBindingsBySubjectRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ListBySubject(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ListBySubject_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ListBySubject(ctx, req.(*ListAccessBindingsBySubjectRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ListSubjectPrivileges_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListSubjectPrivilegesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ListSubjectPrivileges(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ListSubjectPrivileges_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ListSubjectPrivileges(ctx, req.(*ListSubjectPrivilegesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ListAssignableRoles_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListAssignableRolesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ListAssignableRoles(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ListAssignableRoles_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ListAssignableRoles(ctx, req.(*ListAssignableRolesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ListByRole_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListAccessBindingsByRoleRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ListByRole(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ListByRole_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ListByRole(ctx, req.(*ListAccessBindingsByRoleRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ExpandAccess_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ExpandAccessRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ExpandAccess(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ExpandAccess_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ExpandAccess(ctx, req.(*ExpandAccessRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ListByAccount_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListAccessBindingsByAccountRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ListByAccount(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ListByAccount_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ListByAccount(ctx, req.(*ListAccessBindingsByAccountRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AccessBindingService_ListOperations_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListAccessBindingOperationsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AccessBindingServiceServer).ListOperations(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AccessBindingService_ListOperations_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AccessBindingServiceServer).ListOperations(ctx, req.(*ListAccessBindingOperationsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AccessBindingService_ServiceDesc is the grpc.ServiceDesc for AccessBindingService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AccessBindingService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "kacho.cloud.iam.v1.AccessBindingService",
	HandlerType: (*AccessBindingServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Get",
			Handler:    _AccessBindingService_Get_Handler,
		},
		{
			MethodName: "Create",
			Handler:    _AccessBindingService_Create_Handler,
		},
		{
			MethodName: "Delete",
			Handler:    _AccessBindingService_Delete_Handler,
		},
		{
			MethodName: "Update",
			Handler:    _AccessBindingService_Update_Handler,
		},
		{
			MethodName: "ListByScope",
			Handler:    _AccessBindingService_ListByScope_Handler,
		},
		{
			MethodName: "ListBySubject",
			Handler:    _AccessBindingService_ListBySubject_Handler,
		},
		{
			MethodName: "ListSubjectPrivileges",
			Handler:    _AccessBindingService_ListSubjectPrivileges_Handler,
		},
		{
			MethodName: "ListAssignableRoles",
			Handler:    _AccessBindingService_ListAssignableRoles_Handler,
		},
		{
			MethodName: "ListByRole",
			Handler:    _AccessBindingService_ListByRole_Handler,
		},
		{
			MethodName: "ExpandAccess",
			Handler:    _AccessBindingService_ExpandAccess_Handler,
		},
		{
			MethodName: "ListByAccount",
			Handler:    _AccessBindingService_ListByAccount_Handler,
		},
		{
			MethodName: "ListOperations",
			Handler:    _AccessBindingService_ListOperations_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "kacho/cloud/iam/v1/access_binding_service.proto",
}
